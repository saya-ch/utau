*.pyc
__pycache__/
*.zip
*.tmp
# Godot editor cache (regenerated on first run)
.godot/
# Godot binary extracted on first run (regenerated from .z0* + .zip parts)
godot/Godot_v4.6.3-stable_linux.x86_64
# T171 (#89) — Godot binary may also be extracted to repo root (z0* parts live at
# repo root in this fork).  Same regeneration story: from .z0* + .zip on first run.
Godot_v4.6.3-stable_linux.x86_64
# T068 — shop smoke test, data-only, not shipped with build
data/.test/
# Godot 4.4+ Resource UID cache (auto-generated on first run of a .gd script
# via `godot --headless --script`).  Same regeneration story as .godot/: the
# cache is rebuilt next time the script is launched, so it should never be
# committed.  See #135 review F021 cleanup for context.
*.uid
