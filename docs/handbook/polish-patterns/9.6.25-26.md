# Polish Patterns 9.6.25–26

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.25–9.6.26，共 2 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.25-26.md

<!-- shard 25-26 共 2 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 (5 段 canonical 1:1 序列) polish 模式 (T109 + T250 + T254 + T272 + T275 跨 5 任务 ~98 
轮落地) 文档化

> §9.6.1 记录 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫；§9.6.2 记录单个 VFX 5 layer 视觉 (L1-L5) polish；§9.6.3 
> 记录 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb；§9.6.4 记录 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法；§9.6.5 记录 6 verb 视觉组连贯 
> tooltip `_build_verb_achievement_tooltip` 8 行拼接扩展器 (T250 #168 落地, 含 `_VERB_ACHV_ICON_HINTS` + `_VERB_ACHV_INFO` + 
> `_build_verb_achievement_tooltip` 3 件套 + base_tooltip 3 行 100% 兼容 T109 #60)；§9.6.16 记录 player.gd 6 verb hit handler 5 
> 件套 1:1；§9.6.22 记录 6 verb VFX 视觉组 5 段；§9.6.24 记录 HUD 6 verb × 6 通道 = 36 通道. 本节记录 6 verb 视觉组连贯 tooltip 8 行拼接 (5 段 
> canonical 1:1 序列) polish 模式 — 跨 5 段 (Stage 1 `_VERB_ACHV_ICON_HINTS` 3 entry list + Stage 2 `_VERB_ACHV_INFO` 3 entry 
> × 6 字段 + Stage 3 base_tooltip 3 行 100% 兼容 T109 #60 + Stage 4 `_build_verb_achievement_tooltip` 5 element extra_lines 
> + Stage 5 3 闭环宪法同步) — 跨 5 任务 (T109 #60 + T250 #168 + T254 #173 + T272 #191 + T275 #194) ~98 轮迭代落地, 8 行拼接 1:1 严格分离 (3 
> base + 5 extra = 8 行, 0 漏 0 重叠 0 互混 0 复用 0 共享), 0 漂动 0 后续 主要 pre-existing 风险. 任何"加新 1 verb tooltip 扩展"或"修 1 关联成就 
> tooltip 视觉组"或"加新 1 tooltip 维度"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 5 1:1 严格), **再**按 Stage 1 `_VERB_ACHV_ICON_HINTS` 
> 1:1 加 1 entry (Stage 1 1:1 严格), **再**按 Stage 2 `_VERB_ACHV_INFO` 1:1 加 1 entry 6 字段 (Stage 2 1:1 严格), **再**按 Stage 4 
> `_build_verb_achievement_tooltip` 0 改既有 5 element extra_lines 0 改 1 行 (Stage 4 1:1 严格), **再**按 Stage 5 3 闭环宪法同步 
> (调色六元组 + HUD 6 行 6 通道 + tooltip 6 字段 0 漏 1 闭环). 跨 3 verb icon hint (echo_icon / wave_icon / whisper_icon) + 6 字段 
> (achv_id / verb_index / color / color_name / geometry_zh / visual_group) + 5 element extra_lines (空行 / 段名 header / 
> 序号+主色 hex+主色名 / 几何 / 视觉组) + 3 闭环宪法 (§9.6.4 调色六元组 + §9.6.3 HUD 6 行 6 通道 + §9.6.24 HUD 36 通道) 0 漏 1 verb 全部走同一套 5 段 1:1 
> 序列 0 例外.

- **症状**: `src/scripts/pause_menu.gd` 6 verb 视觉组连贯 tooltip 8 行拼接 (`_build_verb_achievement_tooltip`) 跨 3 verb icon hint 
反复 polish 期间出现 6 类典型 fragile: (1) "加新 1 verb 漏 Stage 1 列表同步" — 新 verb 接入成就 tooltip 视觉组时 (e.g. T275 #194 第 7 verb 假设) 漏 
`_VERB_ACHV_ICON_HINTS` 列表末尾加 1 entry (3 entry → 4 entry, T250 落地时 0 触碰既有 3 entry 顺序), 触发 
`_build_verb_achievement_tooltip` 内部 `_VERB_ACHV_ICON_HINTS.has(icon_hint)` guard 返回 false → 直接 `return base_tooltip` → 
新 verb slot tooltip 100% 退化为 T109 #60 既有 3 行 0 视觉组连贯段 0 调色 hex 0 几何 0 视觉组, 玩家 hover 看到 "title + desc + 解锁时间" 3 行 0 看到 
"• 第 7 verb · #HEX ColorName"; (2) "加新 1 verb 漏 Stage 2 dict 同步" — 新 verb 接入成就 tooltip 视觉组时漏 `_VERB_ACHV_INFO` dict 加 1 
entry 6 字段 (achv_id / verb_index / color / color_name / geometry_zh / visual_group, T250 落地后字段顺序 1:1 严格), 触发 
`_build_verb_achievement_tooltip` 内部 `_VERB_ACHV_INFO.has(icon_hint)` guard 返回 false → 直接 `return base_tooltip` → 玩家 
hover 看到 0 视觉组连贯 5 行段, 0 知道 "这是第几 verb / 主色是什么 / 几何是什么 / 视觉组短句", 即使 Stage 1 列表已加该 entry; (3) "修 1 关联成就 tooltip 漏 Stage 
2 字段顺序" — 修 `_VERB_ACHV_INFO` 既有 entry (e.g. `echo_icon`) 时改字段顺序 (e.g. 把 `color_name` 放到 `verb_index` 之前, 或把 
`visual_group` 放到 `geometry_zh` 之前), 触发 `_build_verb_achievement_tooltip` 内 `String(info["color"])` + 
`String(info["color_name"])` 紧邻渲染 ("• 第 N verb · #HEX  ColorName") 0 断行, 玩家看到 "• 第 4 verb · Glass Cyan" 而非 "• 第 4 verb ·
 #69C7CE  Glass Cyan" 0 主色 hex 0 调色宪法同步 (§9.6.4 调色六元组 entry 0 调色 hex 0 通道值), 视觉组连贯崩塌; (4) "加新 1 tooltip 维度漏 Stage 4 5 
element 同步" — 修 `_build_verb_achievement_tooltip` 函数体时改既有 5 element `Array[String] extra_lines` (空行 + 段名 header + 序号+主色 
hex+主色名 + 几何 + 视觉组) 任意 1 element (e.g. 删空行 0 段分隔 / 改段名 header 文字 / 改序号 format 字符串 / 删几何 / 删视觉组), 触发 base_tooltip 3 行 + 
extra 5 行 = 8 行 tooltip 硬约束破坏, 玩家 hover 看到 N 行 (N ≠ 8) 视觉组连贯段不完整 (e.g. 0 看到 "• 几何 — 盾球扩散" 几何 0 知道 verb 几何 motif) 或 
base_tooltip 3 行被错乱截断 (e.g. 0 看到 "解锁于 ..."), 6 verb 视觉组连贯 tooltip 拼接崩塌; (5) "加新 1 verb 漏 Stage 5 闭环宪法同步" — 新 verb 接入成就 
tooltip 视觉组时漏 §9.6.4 调色六元组加 1 entry (e.g. 第 7 verb 调色 hex 0 改 §9.6.4 调色六元组 0 撞既有 6 verb) 或漏 §9.6.3 HUD 6 行 6 通道 (e.g. 加 
verb 0 加 HUD 6 行 6 通道 1 行 0 撞 5 verb) 或漏 §9.6.24 HUD 36 通道 (e.g. 加 verb 0 加 6 通道 = 36+6=42 通道 0 撞 36 通道), 触发 3 闭环宪法不同步 
(调色 hex 0 镜像 §9.6.4 + HUD 6 行 0 镜像 §9.6.3 + 36 通道 0 镜像 §9.6.24), 玩家 hover tooltip 看到 "• 第 7 verb · #NEWHEX 
NewColorName" 但 HUD 6 行 0 该 verb 行, 视觉组连贯崩塌; (6) "加新 1 verb slot 漏 base_tooltip 调用拆分" — 新 verb 关联成就 slot tooltip 落地时 
(e.g. T250 #168 时改 `slot.tooltip_text = "..."` 内联长字符串为 `var base_tooltip := "..."; slot.tooltip_text = 
_build_verb_achievement_tooltip(base_tooltip, hint)` 单一职责拆分) 漏 base_tooltip 3 行 0 触碰 11 个非 6 verb 关联 slot tooltip 100% 
兼容 (T109 #60 既有 `"%s  %s\n解锁于 %s"` × title_zh + desc_zh + ts_str 3 行结构 0 改 0 回归), 11 slot 0 触碰 3 slot 升级约束破坏, 玩家 hover 
非 6 verb 关联 slot 看到 0 兼容 T109 #60 既有 3 行 (e.g. "0 title  0 desc\n解锁于 0 timestamp") 0 字 0 解锁时间, 11 slot tooltip 0 兼容. 6 
类症状根因: 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列 0 文档化, polish 期只能"凭记忆"同步 N 处 (1 Stage 1 list + 1 Stage 2 dict + 
1 Stage 3 base_tooltip 3 行 0 触碰 + 1 Stage 4 5 element extra_lines + 1 Stage 5 3 闭环宪法), 1 漏 1 = 8 行 tooltip 漂动 (3 base 
错乱 / 5 extra 漏 1 行 / 11 slot 0 兼容 / 3 闭环宪法 0 同步).
- **触发场景**: `src/scripts/pause_menu.gd` 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列落地需同步 5 件套: (1) **Stage 1 
`_VERB_ACHV_ICON_HINTS` 3 entry list 1:1 严格分离** — `const _VERB_ACHV_ICON_HINTS := ["echo_icon", "wave_icon", 
"whisper_icon"]` 3 entry 1:1 严格 (T250 #168 落地, 3 entry 顺序 1:1 严格 0 改 0 重命名 0 漏 1 entry, 改 1 entry 名 = 
`_build_verb_achievement_tooltip` 内部 `_VERB_ACHV_ICON_HINTS.has(icon_hint)` guard 0 触发 → `return base_tooltip` → 0 
视觉组连贯 5 行段 0 调色 hex 0 几何 0 视觉组), 0 触碰 11 个非 6 verb 关联 slot (返回 `base_tooltip` 原样 100% 兼容 T109 #60 既有 3 行 0 改 0 回归); (2) 
**Stage 2 `_VERB_ACHV_INFO` 3 entry × 6 字段 1:1 严格分离** — `const _VERB_ACHV_INFO := { "echo_icon": { "achv_id": 
"quadruple_voice", "verb_index": 4, "color": "#69C7CE", "color_name": "Glass Cyan", "geometry_zh": "盾球扩散 — 4 verb 
命中色向四周辐射", "visual_group": "5 verb 全是动态几何 (Pulse 圆环 / Bind 螺旋 / Cut 锋线 / Echo 盾球 / Wave 双环) — 玩家扫到 cyan = 4 verb 命中" }, 
"wave_icon": { ... 6 字段 ... }, "whisper_icon": { ... 6 字段 ... } }` 3 entry × 6 字段 = 18 字段 1:1 严格 (字段顺序 1:1 严格 "achv_id 
→ verb_index → color → color_name → geometry_zh → visual_group" 0 改 0 重排 0 漏 1 字段, 改 1 字段顺序 = 
`_build_verb_achievement_tooltip` 内 `String(info["color"])` + `String(info["color_name"])` 紧邻渲染 0 断行 → 玩家看到 "• 第 N verb 
· ColorName" 而非 "• 第 N verb · #HEX  ColorName" 0 主色 hex); (3) **Stage 3 base_tooltip 3 行 100% 兼容 T109 #60** — `var 
base_tooltip := "%s  %s\n解锁于 %s" % [title_zh, desc_zh, ts_str]` 3 行结构 100% 兼容 T109 #60 既有 3 行 0 改 0 回归, 11 slot 0 触碰 3 
slot 升级约束, base_tooltip 0 改 (T109 既有 100% 兼容) + `_build_verb_achievement_tooltip` 仅追加 5 行 0 触碰 base_tooltip 3 行结构; (4) 
**Stage 4 `_build_verb_achievement_tooltip` 5 element extra_lines 1:1 严格分离** — `func 
_build_verb_achievement_tooltip(base_tooltip: String, icon_hint: String) -> String:` 双 `has()` guard 
(`_VERB_ACHV_ICON_HINTS.has(icon_hint)` + `_VERB_ACHV_INFO.has(icon_hint)` 0 漏) + `var extra_lines: Array[String] = []` 
5 element (1. `""` 空行段分隔 / 2. `"6 verb 视觉组"` 段名 header / 3. `"• 第 %d verb · %s  %s"` 序号+主色 hex+主色名 / 4. `"• 几何 — %s"` 
几何 / 5. `"• 视觉组 — %s"` 视觉组) 0 反序 0 漏 0 改 1 element 0 改 1 alpha 公式 0 改 1 painter's order, `return base_tooltip + 
"\n".join(extra_lines)` 3 + 5 = 8 行 tooltip 硬约束 (1 漏 1 element = 8 行硬约束破坏, 玩家 hover 看到 N 行 N ≠ 8 视觉组连贯段不完整); (5) 
**Stage 5 3 闭环宪法同步 (调色六元组 + HUD 6 行 6 通道 + tooltip 6 字段)** — 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 6 字段 1:1 严格分离 0 漏 1 
闭环 (调色 hex 必须 0 改 §9.6.4 调色六元组 entry, HUD 6 行 6 通道必须 0 改 §9.6.3 5+1 verb 7 UI 通道, tooltip 6 字段必须 0 改 §9.6.24 HUD 36 通道 
0 漏 1 verb 0 改 1 通道值), 1 漏 1 闭环 = 视觉组连贯崩塌 (玩家 hover tooltip 看到 "• 第 4 verb · #69C7CE Glass Cyan" 但 HUD 6 行 0 4 verb 
Echo 行 0 4 verb 主题色 glow 反馈). 5 件套 1:1 同步, 1 漏 1 = 8 行 tooltip 漂动 (3 base 错乱 / 5 extra 漏 1 行 / 11 slot 0 兼容 / 3 闭环宪法 0 
同步 / 字段顺序 错位).
- **修复** (跨 5 任务 ~98 轮落地后已知 5 段 canonical 1:1 序列 + 8 行硬约束 0 例外):
  - **T109 (#60) base_tooltip 3 行奠基** — `src/scripts/pause_menu.gd:1237` 加 `var base_tooltip := "%s  %s\n解锁于 %s" % 
[title_zh, desc_zh, ts_str]` 3 行结构 (T109 落地, AchievementGrid 14 slot tooltip 0 触碰既有 slot 路径, 11 slot 0 触碰 3 slot 升级约束 
100% 兼容, slot 创建路径 0 改 (slot 仍是 16x16 TextureRect + mouse_filter=STOP + mouse_entered/exited 2 signal)), 0 触碰 6 verb 
视觉组连贯 (T109 落地时 6 verb 视觉组 0 接入成就系统 0 触发 tooltip 视觉组连贯 0 调色 hex 0 几何 0 视觉组).
  - **T250 (#168) `_build_verb_achievement_tooltip` 8 行拼接扩展器 + Stage 1 + Stage 2 + Stage 4 1:1 落地** — 
`src/scripts/pause_menu.gd:151` 加 `const _VERB_ACHV_ICON_HINTS := ["echo_icon", "wave_icon", "whisper_icon"]` 3 entry 
list (Stage 1 1:1 严格) + `src/scripts/pause_menu.gd:166-191` 加 `const _VERB_ACHV_INFO := { ... }` 3 entry dict × 6 字段 = 
18 字段 (Stage 2 1:1 严格, 字段顺序 1:1 严格 "achv_id → verb_index → color → color_name → geometry_zh → visual_group") + 
`src/scripts/pause_menu.gd:1354-1370` 加 `func _build_verb_achievement_tooltip(base_tooltip: String, icon_hint: String) 
-> String:` 双 `has()` guard + 5 element `Array[String] extra_lines` + `"\n".join(extra_lines)` 拼接到 base_tooltip 末尾 
(Stage 4 1:1 严格, 3 + 5 = 8 行 tooltip 硬约束) + `src/scripts/pause_menu.gd:1237-1238` 把 `slot.tooltip_text = "..."` 
内联长字符串拆成 `var base_tooltip := "..."; slot.tooltip_text = _build_verb_achievement_tooltip(base_tooltip, hint)` 单一职责拆分 
(Stage 3 base_tooltip 3 行 0 触碰 11 slot 0 兼容 3 slot 升级), 6 verb 调色 1:1 镜像 §9.6.4 调色六元组 (Echo Glass Cyan #69C7CE / Wave 
Pale Resonance #B7E7DD / Whisper Muted Mauve #C8A4D8 3 entry) 0 改 1 hex 0 改 1 通道值.
  - **T254 (#173) §9.6.4 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法** — `STYLE_GUIDE.md` §9.6.4 调色六元组落地 6 verb entry (Pulse 
Coral #E86D5A / Bind Muted Violet #65506A / Cut Amber #F2B66E / Echo Glass Cyan #69C7CE / Wave Pale Resonance #B7E7DD / 
Whisper Muted Mauve #C8A4D8) + `hud.gd` 6 verb 主题色 const (T233 #152 5 verb 冷光勾边 + F013.E + T247 #164 加 Whisper 第 6 
verb) + `pause_menu.gd` `_VERB_ACHV_INFO` 6 字段 "color" + "color_name" 紧邻渲染 "• 第 N verb · #HEX  ColorName" 0 断行, 3 闭环宪法 
1:1 同步 (Stage 5 1:1 严格, T250 #168 0 触碰既有 3 verb entry).
  - **T272 (#191) `_verb_ability_base.gd` 16 件套 共享契约 0 触碰 `_build_verb_achievement_tooltip`** — 
`src/scripts/_verb_ability_base.gd` 16 件套共享契约 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 兜底 + 1 
`is_globally_blocking` 扩展) 落地后 0 触碰 `_build_verb_achievement_tooltip` 函数体任何 const / var / 调用入口, 6 verb 子类 0 改 base 0 改, 
0 触碰 §9.6.5 既有 5 段 0 触碰 1 字段.
  - **T275 (#194) 1 verb Whisper 收回完成 + §9.6.25 5 段序列 1:1 镜像落地** — 1 verb Whisper 收回完成后 §9.6.25 5 段序列 1:1 镜像落地 
(`_VERB_ACHV_ICON_HINTS` 3 entry + `_VERB_ACHV_INFO` 3 entry × 6 字段 + base_tooltip 3 行 0 触碰 11 slot 100% 兼容 + 
`_build_verb_achievement_tooltip` 5 element extra_lines + 3 闭环宪法同步), 0 触碰既有 6 verb 任何 1 字段 0 改 1 hex 0 改 1 通道值 0 改 1 
alpha 公式 0 改 1 painter's order 0 改 1 dict 顺序 0 改 1 list 顺序 0 改 1 闭环宪法 0 改 1 base_tooltip 3 行结构.
  - 跨 5 任务 ~98 轮落地后 0 漂动, 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列 100% 闭环, 5 段 1:1 严格分离 (Stage 1 
`_VERB_ACHV_ICON_HINTS` 3 entry list 0 漏 0 改 0 重命名 0 反序 + Stage 2 `_VERB_ACHV_INFO` 3 entry × 6 字段 = 18 字段 0 漏 0 改 0 重排 
0 反序 字段顺序 1:1 严格 + Stage 3 base_tooltip 3 行 100% 兼容 T109 #60 0 改 0 回归 11 slot 0 触碰 3 slot 升级约束 + Stage 4 
`_build_verb_achievement_tooltip` 5 element extra_lines 0 漏 0 改 0 重排 0 反序 3 + 5 = 8 行硬约束 + Stage 5 3 闭环宪法 0 漏 0 改 0 错位 
0 漏 1 闭环), 6 verb 调色 1:1 严格分离 (Pulse Coral / Bind Violet / Cut Amber / Echo Cyan / Wave Pale / Whisper Mauve 6 entry 0 
漏 0 重叠 0 互混 0 复用 0 共享). 5 任务 0 后续 主要 pre-existing 风险.
- **预防**:
  1. 任何 polish 期给"加新 1 verb tooltip 视觉组连贯扩展"或"修 1 关联成就 tooltip 视觉组"或"加新 1 tooltip 维度"任务加新字段 / 加新方法 / 加新 verb 时**必须**严格按 
5 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 改 `_VERB_ACHV_ICON_HINTS` 3 entry list** (T250 
#168 落地后 3 entry 1:1 严格, `echo_icon` / `wave_icon` / `whisper_icon` 3 entry 0 改 0 重命名 0 漏 1 entry 0 反序, 改 1 entry = 
`_build_verb_achievement_tooltip` 内部 `_VERB_ACHV_ICON_HINTS.has(icon_hint)` guard 0 触发 → 0 视觉组连贯 5 行段), **Stage 2 0 改 
`_VERB_ACHV_INFO` 3 entry × 6 字段 dict** (T250 #168 落地后 18 字段 1:1 严格, 字段顺序 "achv_id → verb_index → color → color_name → 
geometry_zh → visual_group" 1:1 严格 0 改 0 漏 1 字段 0 反序 0 改 1 字段名, 改 1 字段顺序 = `String(info["color"])` + 
`String(info["color_name"])` 紧邻渲染 0 断行 → 玩家看到 "• 第 N verb · ColorName" 而非 "• 第 N verb · #HEX  ColorName" 0 主色 hex), 
**Stage 3 0 改 base_tooltip 3 行 100% 兼容 T109 #60** (T109 #60 落地后 `"%s  %s\n解锁于 %s"` × title_zh + desc_zh + ts_str 3 行结构 
100% 兼容 0 改 0 回归, 11 slot 0 触碰 3 slot 升级约束 0 破坏, 改 1 字段 (e.g. 改 `\n` 为 `  \n` 加段间距) 触发 11 slot 0 警告回归 0 触碰 base_tooltip 
3 行结构), **Stage 4 0 改 `_build_verb_achievement_tooltip` 5 element extra_lines** (T250 #168 落地后 5 element 
`Array[String]` 1:1 严格, `""` / `"6 verb 视觉组"` / `"• 第 %d verb · %s  %s"` / `"• 几何 — %s"` / `"• 视觉组 — %s"` 5 element 0 改 
0 漏 0 改 1 element 0 反序 0 改 1 alpha 公式 0 改 1 format 字符串, 改 1 element = 3 + 5 = 8 行 tooltip 硬约束破坏, 玩家 hover 看到 N 行 N ≠ 8 
视觉组连贯段不完整), **Stage 5 0 改 3 闭环宪法同步** (§9.6.4 调色六元组 + §9.6.3 HUD 6 行 6 通道 + §9.6.24 HUD 36 通道 0 漏 0 改 0 错位 0 漏 1 闭环, 调色 
hex 0 改 §9.6.4 entry + HUD 6 行 0 改 §9.6.3 5+1 verb + HUD 36 通道 0 改 §9.6.24, 1 漏 1 闭环 = 视觉组连贯崩塌). 任何"加新 1 verb tooltip 
视觉组连贯扩展"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 5 1:1 严格), **再**按 §9.6.3 5+1 verb 7 UI 通道 6 处 1:1 复制 (Stage 5 1:1 严格), 
**再**在 `_VERB_ACHV_ICON_HINTS` 列表末尾/中间加 1 entry (Stage 1 1:1 严格, 0 改既有 3 entry 顺序 0 改 1 字段名 0 漏 1 entry), **再**在 
`_VERB_ACHV_INFO` dict 加 1 entry 6 字段 (Stage 2 1:1 严格, 字段顺序 "achv_id → verb_index → color → color_name → geometry_zh → 
visual_group" 1:1 严格 0 改 0 漏 0 反序 0 改 1 字段名), **再**确认 `_build_verb_achievement_tooltip` 0 改既有 5 element extra_lines 
(Stage 4 1:1 严格, 0 反序 0 漏 0 改 1 element 0 改 1 format 字符串), **再**确认 base_tooltip 3 行 0 触碰 11 slot 0 兼容 (Stage 3 1:1 严格), 
0 触碰 §9.6.5 既有 5 段 0 触碰 1 字段 0 触碰 §9.6.4 既有 6 verb 调色 0 触碰 §9.6.3 既有 5+1 verb 7 UI 通道 0 触碰 §9.6.24 既有 36 通道.
  2. **5 段序列 0 触碰边界** — `src/scripts/pause_menu.gd` 任何 const / var / func 0 改 0 删 0 重排 (Stage 1-5 既有 1 字段 1 调点 0 改), 
任何"修 1 关联成就 tooltip 视觉组"polish 走 Stage 2 1:1 严格 (改 `_VERB_ACHV_INFO` 既有 entry 时**必须**保持字段顺序 "achv_id → verb_index → 
color → color_name → geometry_zh → visual_group" 1:1 严格 0 改 0 漏 0 反序, 0 改 1 字段名 0 改 1 hex 0 改 1 通道值 0 改 1 几何描述 0 改 1 
视觉组短句); 任何"修 1 tooltip 维度"polish 走 Stage 4 1:1 严格 (在 `_build_verb_achievement_tooltip` 函数体顶部 docblock 写明 "8 行 = 3 base 
+ 5 extra" 硬约束, 加 1 行 (e.g. 加 6 verb 关联成就 id 行 → 6 行 extra) 必须同步更新文档化 8 行结构, 0 改 base_tooltip 3 行 (T109 既有 100% 兼容)); 
任何"修 1 verb 调色"polish 走 Stage 5 1:1 严格 (改 `String(info["color"])` 时同步改 §9.6.4 调色六元组 6 verb entry 0 改 1 hex 0 改 1 通道值).
  3. **6 verb 调色 0 改 1 hex 0 改 1 通道值** (T250 + T254 落地后 0 触碰边界) — Pulse Coral `#E86D5A` (0.91, 0.43, 0.35, 1.0) / Bind 
Violet `#65506A` (0.40, 0.31, 0.42, 1.0) / Cut Amber `#F2B66E` (0.95, 0.71, 0.43, 1.0) / Echo Cyan `#69C7CE` (0.41, 
0.78, 0.81, 1.0) / Wave Pale Resonance `#B7E7DD` (0.72, 0.91, 0.87, 1.0) / Whisper Mauve `#C8A4D8` (0.78, 0.64, 0.85, 
1.0) 6 verb 调色 1:1 严格分离 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 hex / 1 通道值 = 6 verb 视觉组连贯 tooltip 8 行拼接 漂动 (Stage 5 漂动) + §9.6.4 
调色六元组 entry 漂动 + §9.6.24 HUD 36 通道 漂动 + §9.6.3 5+1 verb 7 UI 通道 漂动 + §9.6.22 6 verb VFX 视觉组 漂动, source-grep 验证 6 verb 
`_VERB_ACHV_INFO` `color` + `color_name` 字段 + §9.6.4 调色六元组 6 entry + §9.6.24 HUD 主题色 const 6 entry + §9.6.3 HUD 6 行 6 
通道 + §9.6.22 6 verb VFX `@export` color 字段 0 漏 0 改 1 字段 0 改 1 通道值.
  4. **8 行 tooltip 硬约束 0 反序 0 漏 0 改 1 行** (T250 #168 落地后 0 触碰边界) — 3 base + 5 extra = 8 行 tooltip 硬约束, 0 反序 0 漏 0 改 1 行 
0 改 1 base_tooltip 3 行结构 0 改 1 extra_lines 5 element, 任何"加新 1 tooltip 维度"polish 必须在 `_build_verb_achievement_tooltip` 
函数体顶部 docblock 写明 "8 行 = 3 base + 5 extra" 硬约束 + 5 element 各自作用 (空行段分隔 / 段名 header / 序号+主色 hex+主色名 / 几何 / 视觉组), 加 1 行 
(e.g. 加 6 verb 关联成就 id 行 → 6 行 extra) 必须同步更新文档化 8 行结构 + `_VERB_ACHV_INFO` 加 1 字段 0 改 1 字段顺序, 0 触碰 base_tooltip 3 行 
(T109 既有 100% 兼容), source-grep 验证 `_build_verb_achievement_tooltip` 函数体 `extra_lines.append` 调点 = 5 + `base_tooltip` 3 
行 0 漏 0 改 1 行 0 反序.
  5. **`_VERB_ACHV_INFO` 6 字段顺序 0 改 0 反序** (T250 #168 落地后 0 触碰边界) — `_VERB_ACHV_INFO` 3 entry × 6 字段 = 18 字段顺序 1:1 严格 
(achv_id → verb_index → color → color_name → geometry_zh → visual_group, 0 改 0 漏 0 反序 0 改 1 字段名 0 改 1 字段类型), 任何"修 1 
关联成就 tooltip 视觉组"polish 必须**先**在 entry 加 1 字段时按顺序末尾追加 (e.g. 加 `verb_unlock_ts` 字段 → 7 字段 0 改 6 字段顺序) 或插入到合适位置 (e.g. 加 
`icon_path` 字段插到 `achv_id` 之后 0 改 6 字段顺序), **再**在 `_build_verb_achievement_tooltip` 5 element extra_lines 同步更新 (e.g. 加 
`• 解锁时间 — %s` 行 0 改 5 element 顺序 0 改 1 format 字符串), 改 1 字段顺序 = `String(info["color"])` + `String(info["color_name"])` 
紧邻渲染 0 断行 → 玩家看到 "• 第 N verb · ColorName" 而非 "• 第 N verb · #HEX  ColorName" 0 主色 hex, source-grep 验证 `_VERB_ACHV_INFO` 
3 entry 各 6 字段 0 漏 0 改 1 字段名 0 反序.
  6. **3 闭环宪法 0 漏 1 闭环** (T254 #173 + §9.6.4 + §9.6.3 + §9.6.24 落地后 0 触碰边界) — 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 6 
字段 1:1 严格分离 0 漏 1 闭环 (调色 hex 必须 0 改 §9.6.4 entry, HUD 6 行 6 通道必须 0 改 §9.6.3 5+1 verb 7 UI 通道, tooltip 6 字段必须 0 改 
§9.6.24 HUD 36 通道 0 漏 1 verb 0 改 1 通道值), 任何"加新 1 verb tooltip 视觉组连贯扩展"polish 必须**先**在 §9.6.4 调色六元组加 1 entry (Stage 5 
1:1 严格, 0 漏 1 entry 0 改 1 hex), **再**在 §9.6.3 5+1 verb 7 UI 通道 6 处 1:1 复制 (Stage 5 1:1 严格, 0 改 1 字段 0 改 1 节点类型), **再**在 
§9.6.24 HUD 36 通道 0 漏 1 verb 0 改 1 通道值 (Stage 5 1:1 严格, 0 改 1 字段 0 改 1 dict key 0 改 1 stylebox 0 改 1 iteration list 
element), 0 触碰既有 6 verb 调色 / HUD 6 行 / 36 通道 任何 1 字段 0 改 1 hex 0 改 1 通道值 0 改 1 节点类型.
  7. 已知 drift risk: 6 verb 视觉组连贯 tooltip 8 行拼接 (5 段 canonical 1:1 序列) **可能**漏 1 段 1:1 镜像 (e.g. T250 #168 落地时漏 Stage 3 
base_tooltip 3 行 0 触碰 11 slot 0 兼容约束, 11 slot 0 警告回归; 漏 Stage 4 5 element extra_lines 第 3 行 `"• 第 %d verb · %s  %s"` 0 
改 format 字符串, 玩家看到 0 主色 hex; 漏 Stage 5 §9.6.24 HUD 36 通道 0 漏 1 verb 0 改 1 通道值, HUD 6 行 0 镜像 tooltip 视觉组连贯). 监控建议: 
任何"未来给 6 verb 视觉组连贯 tooltip 加新 verb 或加新 tooltip 维度"必须**先**在 5 段序列 (Stage 1 list + Stage 2 dict 6 字段 + Stage 3 
base_tooltip 3 行 + Stage 4 5 element extra_lines + Stage 5 3 闭环宪法) 1:1 镜像 0 漏 1 段. T250 (#168) 5 段序列 1:1 镜像落地后任何新 verb 
/ 新 tooltip 维度 加 5 段同步是 §9.6.25 5 段序列的"母集" (1 verb 5 段 1:1 严格, §9.6.25 5 段 1 verb 完整闭环, 3 verb × 5 段 = 15 件 1:1 严格, 含 3 
entry list 1:1 严格 + 3 entry × 6 字段 = 18 字段 1:1 严格 + 3 base + 5 extra = 8 行 1:1 严格 + 3 闭环宪法 1:1 严格). 另 1 类已知 drift: 
Stage 4 `_build_verb_achievement_tooltip` 函数体 docblock 0 写明 "8 行 = 3 base + 5 extra" 硬约束, 后续 polish 期若加新 tooltip 维度 
(e.g. 加 7th verb / 加 4 verb family path / 加成就解锁时间行) 必须**先**看 docblock 确认 N extra 元素结构, 0 改 base_tooltip 3 行 0 改 5 
element 顺序.

**0 副作用**: T109 / T250 / T254 / T272 / T275 任何 const / var 0 触碰 (`_VERB_ACHV_ICON_HINTS` 3 entry 0 改 0 删 0 重排 + 
`_VERB_ACHV_INFO` 3 entry × 6 字段 = 18 字段 0 改 0 删 0 重排 字段顺序 1:1 严格 + base_tooltip 3 行 0 改 0 删 0 重排 + 
`_build_verb_achievement_tooltip` 5 element extra_lines 0 改 0 删 0 重排 + 3 闭环宪法 0 改 0 漏 0 错位); 0 触碰 §9.6.1 6 verb VFX 跨类 
handler `is_instance_valid + has_method` 双守卫 (T281 0 改 6 verb `_current_<verb>_vfx` 任何字段); 0 触碰 §9.6.2 5 layer 视觉组既有 5 
verb 任何 1 字段 1 alpha 公式 1 layer 顺序; 0 触碰 §9.6.3 5+1 verb 7 UI 通道 既有 6 verb 任何 1 字段 1 调点 (T281 0 改 6 verb 4 @onready var 
+ 1 ability ref + 1 主题色 const + 1 风格 stylebox + 1 dict key + 1 iteration list element 0 触碰既有 5 步 1:1 复制 模板); 0 触碰 
§9.6.4 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (T281 0 改 6 verb 调色 1 hex 0 改 1 通道值 + 0 改 HUD 6 行 6 通道 + 0 改 tooltip 6 字段); 
0 触碰 §9.6.5 既有 `_build_verb_achievement_tooltip` 函数体 (T281 0 改 5 段序列任何 1 字段 0 改 `_VERB_ACHV_ICON_HINTS` 0 改 
`_VERB_ACHV_INFO` 0 改 8 行硬约束 0 改 3 闭环宪法同步); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T281 0 改 6 verb `_on_<verb>_hit` 
函数体任何 const / var / 屏染 / 屏抖 / VFX flash / 音频); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T281 0 改 6 verb 主信号 + 命中信号 + 结束信号 
+ 扩展信号 + PlayerActionGate register / unregister); 0 触碰 §9.6.18 16 件套 verb ability base (T281 0 改 6 verb ability 子类任何 
const / var / `@export` / `@onready` / 5 共享方法 / `_exit_tree` 兜底 / `is_globally_blocking` 扩展); 0 触碰 §9.6.19 7 件套 windup 
VFX base (T281 0 改 6 verb windup VFX 子类任何 const / var / `_ready()` / `_process(delta)` / `_activate_windup_tween()` / 
`fade_out_and_free()`); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 transition flow (T281 0 改 GFC + room_transition + room_door + 
GameState 5 段序列); 0 触碰 §9.6.21 8 段成就序列 (T281 0 改 PlayerStats 6 verb 字段 + record_ability_used 6 branch + get_stat 6 
entry + set_stat 6 entry + reset_run 6 行 + all_abilities_used 6 项 + achievements.json 4 tier ID + ICON_COLORS 6 entry + 
PNG 双路径); 0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T281 0 改 6 verb VFX 文件 任何 const / var / `@export` color 字段 / 5+ layer 视觉组 / 
几何 / 跨段镜像); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T281 0 改 audio_manager_enhanced.gd 7 桶 `prewarm_*` 函数体 0 改 0 删 0 重排 + 
prewarm_all_sfx() aggregator 7 行 0 改 0 删 0 重排 + 3 跨调用方 0 改 0 删 0 重排); 0 触碰 §9.6.24 HUD 6 verb × 6 通道 = 36 通道 5 段序列 
(T281 0 改 hud.gd 6 verb @onready var / var / const / dict key / stylebox / iteration list 任何 1 字段 0 改 0 删 0 重排); 0 触碰 
check_smoke_consistency.sh (T281 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh 
(T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, pause_menu.gd 
`_VERB_ACHV_ICON_HINTS` / `_VERB_ACHV_INFO` / `base_tooltip` / `_build_verb_achievement_tooltip` / 11 slot 0 改 0 删 0 
重排); 0 行为变化, 1 文档 1 段 (§9.6.25 ~50 行) + 1 smoke test (T281 20+ 断言).

**与 §9.6.1 / §9.6.2 / §9.6.3 / §9.6.4 / §9.6.5 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / 
§9.6.23 / §9.6.24 关系** — §9.6.1 覆盖 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫; §9.6.2 覆盖单个 VFX 5 layer 
视觉 (L1-L5) polish; §9.6.3 覆盖 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb 模式 (加新 1 verb 时 6 处 1:1 复制: tscn + 4 @onready var + 
1 ability ref + 1 const + 1 stylebox + dict key + iteration list); §9.6.4 覆盖 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 
三闭环宪法 (abstract 宪法 1 + 2 + 3, 宪法 2 = 6 verb HUD 6 行 4 主通道 + 2 派生通道 = 6 通道 per verb, 宪法 3 = tooltip 6 字段 0 漏); §9.6.5 覆盖 
6 verb 视觉组连贯 tooltip `_build_verb_achievement_tooltip` 8 行拼接扩展器 (T250 #168 落地, 含 `_VERB_ACHV_ICON_HINTS` + 
`_VERB_ACHV_INFO` + `_build_verb_achievement_tooltip` 3 件套 + base_tooltip 3 行 100% 兼容 T109 #60); §9.6.16 覆盖 player.gd 6 
verb hit handler 5 件套 1:1 严格分离; §9.6.17 覆盖 player.gd `_ready` 6 verb signal 桥接 5+1 件套 1:1 严格分离; §9.6.18 覆盖 
`_verb_ability_base.gd` 6 verb ability 子类共享契约 16 件 1:1 严格分离; §9.6.19 覆盖 `_verb_windup_vfx_base.gd` 6 verb windup VFX 
子类共享契约 7 件 1:1 严格分离; §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow 5 段 canonical 1:1 序列; §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 
canonical 1:1 序列; §9.6.22 覆盖 6 verb VFX 视觉组 5 段 canonical 1:1 序列; §9.6.23 覆盖 `audio_manager_enhanced.gd` prewarm 
aggregator 7 桶 5+1+1 canonical 1:1 序列 + 3 跨调用方 1:1 镜像; §9.6.24 覆盖 `src/scripts/hud.gd` 6 verb HUD 6 行 6 色色域分工 6 通道 5 段 
canonical 1:1 序列. §9.6.25 覆盖 `src/scripts/pause_menu.gd` 6 verb 视觉组连贯 tooltip 8 行拼接 (5 段 canonical 1:1 序列) (Stage 1 
`_VERB_ACHV_ICON_HINTS` 3 entry list + Stage 2 `_VERB_ACHV_INFO` 3 entry × 6 字段 + Stage 3 base_tooltip 3 行 100% 兼容 T109 
#60 + Stage 4 `_build_verb_achievement_tooltip` 5 element extra_lines + Stage 5 3 闭环宪法同步). §9.6.1 / §9.6.2 关注"verb VFX 
跨类 + 5 layer 视觉", §9.6.3 关注"HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb structural sync", §9.6.4 关注"6 verb 调色六元组 + HUD 6 行 6 
通道 + tooltip 三闭环宪法 (abstract 宪法 1+2+3)", §9.6.5 关注"6 verb 视觉组连贯 tooltip 8 行拼接扩展器 (T250 落地 polish 模式, 5 段 1:1 严格分离待 
§9.6.25 文档化)", §9.6.16-§9.6.19 关注"verb ability + signal + hit handler + windup VFX 共享契约", §9.6.20 关注"跨 scene transition 
flow 时序契约", §9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格)", §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约 (调色 + 几何 + layer + 
跨段镜像)", §9.6.23 关注"prewarm aggregator 5+1+1 序列 1:1 严格分离契约 + 3 跨调用方 1:1 镜像", §9.6.24 关注"HUD 6 verb × 6 通道 = 36 通道 1:1 
严格分离契约 (5 段 1:1 严格分离)", §9.6.25 关注"6 verb 视觉组连贯 tooltip 8 行拼接 1:1 严格分离契约 (5 段 canonical 1:1 序列: Stage 1 list + Stage 2 
dict 6 字段 + Stage 3 base 3 行 + Stage 4 5 element extra_lines + Stage 5 3 闭环宪法)". 13 套 polish 模式 0 反序 0 漏 0 改 1 字符. 
任何"加新 1 verb tooltip 视觉组连贯扩展"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 5 1:1 严格), **再**按 §9.6.3 5+1 verb 7 UI 通道 6 处 1:1 复制 
(Stage 5 1:1 严格), **再**按 §9.6.24 HUD 36 通道 0 漏 1 verb 0 改 1 通道值 (Stage 5 1:1 严格), **再**按 §9.6.25 Stage 1 
`_VERB_ACHV_ICON_HINTS` 1:1 加 1 entry (Stage 1 1:1 严格), **再**按 §9.6.25 Stage 2 `_VERB_ACHV_INFO` 1:1 加 1 entry 6 字段 
(Stage 2 1:1 严格), **再**按 §9.6.25 Stage 4 `_build_verb_achievement_tooltip` 0 改既有 5 element extra_lines 0 改 1 行 (Stage 4 
1:1 严格), **再**按 §9.6.25 Stage 3 base_tooltip 3 行 0 触碰 11 slot 0 兼容 (Stage 3 1:1 严格). §9.6.5 与 §9.6.25 0 互混 0 复用 0 共享: 
§9.6.5 关注"6 verb 视觉组连贯 tooltip 8 行拼接扩展器 (T250 落地, 5 段 1:1 严格分离待 §9.6.25 文档化)" (与 5 段 canonical 1:1 序列 0 相关), §9.6.25 
关注"6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列 (Stage 1-5: list + dict 6 字段 + base 3 行 + 5 element extra_lines + 3 
闭环宪法)" (与 8 行拼接扩展器 0 相关). §9.6.4 与 §9.6.25 0 互混 0 复用 0 共享: §9.6.4 关注"6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 
(abstract 宪法 1+2+3, 1 句宪法 0 触及 5 段序列 0 触及 5 element extra_lines)" (与 5 段序列 0 相关), §9.6.25 关注"6 verb 视觉组连贯 tooltip 8 行拼接 
5 段 canonical 1:1 序列" (与 abstract 宪法 0 相关). §9.6.22 与 §9.6.25 0 互混 0 复用 0 共享: §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约 
(Stage 1-5: class_name + 调色 + layer + 几何 + 跨段镜像, 5 段全在 `<verb>_vfx.gd` 文件)" (与 tooltip 视觉组连贯 0 相关), §9.6.25 关注"6 verb 
视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列 (5 段全在 `pause_menu.gd` 文件, 5 element extra_lines 拼接到 base_tooltip 末尾)" (与 VFX 
视觉组 0 相关). §9.6.24 与 §9.6.25 0 互混 0 复用 0 共享: §9.6.24 关注"HUD 6 verb × 6 通道 = 36 通道 1:1 严格分离契约 (5 段 canonical 1:1 序列, 5 
段全在 `hud.gd` 文件 + 1 段 tscn 6 verb row 4 子节点)" (与 tooltip 视觉组连贯 0 相关), §9.6.25 关注"6 verb 视觉组连贯 tooltip 8 行拼接 5 段 
canonical 1:1 序列 (5 段全在 `pause_menu.gd` 文件)" (与 HUD 36 通道 0 相关, Stage 5 3 闭环宪法同步 0 改既有 36 通道). §9.6.25 是 13 套 polish 
模式**唯一**关注"6 verb 视觉组连贯 tooltip 8 行拼接 1:1 严格分离 (5 段 canonical 1:1 序列: Stage 1 list + Stage 2 dict 6 字段 + Stage 3 base 3 
行 + Stage 4 5 element extra_lines + Stage 5 3 闭环宪法)"的 1 套, 与 §9.6.1-§9.6.5 / §9.6.16-§9.6.24 12 套 verb VFX / HUD 6 verb 
行 / 调色 + HUD 6 行 6 通道 + tooltip 拼接扩展器 / verb ability / signal / hit handler / windup VFX / transition / 成就 / VFX 视觉组 / 
prewarm / HUD 36 通道 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.25 5 段序列走 §9.1 9 步落地的"const" (3) + "icon" (6) 2 步 (`src/scripts/pause_menu.gd` 
`_VERB_ACHV_ICON_HINTS` 3 entry list + `_VERB_ACHV_INFO` 3 entry × 6 字段 = 18 字段 const + 
`_build_verb_achievement_tooltip` 5 element extra_lines 拼接器 + base_tooltip 3 行 0 触碰 11 slot 0 兼容 + `STYLE_GUIDE.md` 
§9.6.4 调色六元组 6 verb entry), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "input" (4) + "shop" (5) + "vfx" (7) + "audio" 
(8) + "HUD 冷光勾边" (9) 7 步 (tooltip 视觉组连贯 0 走 verb enum + ability 子类 + input map + shop 系统 + VFX 视觉组 + audio 副作用 + HUD 
渲染). 任何"加新 1 verb tooltip 视觉组连贯扩展"必须**先**按 §9.1 9 步落地 (含 "const" (3) + "icon" (6) 步), **再**按 §9.6.25 5 段序列 1:1 复制 (0 反序 
0 漏 0 改 1 字段). 任何"加新 1 verb 调色"必须**先**在 `STYLE_GUIDE.md` §9.6.4 调色六元组加 1 entry (Stage 5 1:1 严格, 0 漏 1 entry 0 改 1 hex), 
**再**在 `_VERB_ACHV_INFO` 加 1 entry 6 字段 (Stage 2 1:1 严格, 0 漏 1 entry 0 改 1 字段 0 改 1 字段顺序), 0 触碰 §9.1 9 步任何 "verb" (1) + 
"ability" (2) 步 (tooltip 视觉组连贯 0 触碰 verb enum + ability 子类). 任何"加新 1 verb icon hint"必须**先**在 `_VERB_ACHV_ICON_HINTS` 
列表末尾/中间加 1 entry (Stage 1 1:1 严格), **再**在 `_VERB_ACHV_INFO` dict 加 1 entry 6 字段 (Stage 2 1:1 严格, 字段顺序 1:1 严格), 0 触碰 
§9.1 9 步任何 "icon" (6) 步既有 6 verb icon PNG 双路径. 任何"加新 1 tooltip 维度"必须**先**在 `_build_verb_achievement_tooltip` 函数体顶部 
docblock 写明 "8 行 = 3 base + 5 extra" 硬约束 (Stage 4 1:1 严格), **再**在 5 element extra_lines 末尾/中间加 1 element (Stage 4 1:1 
严格, 0 漏 0 反序 0 改 1 element 0 改 1 format 字符串), 0 触碰 base_tooltip 3 行 (T109 既有 100% 兼容). 任何"加新 1 跨段镜像"必须**先**在 §9.6.3 / 
§9.6.4 / §9.6.22 / §9.6.24 4 跨段加 1 镜像 (Stage 5 1:1 严格, 0 漏 1 跨段 0 改 1 字段), 0 触碰 §9.1 9 步任何 const / var / 节点 (5 段序列仅在 
`pause_menu.gd` `_VERB_ACHV_ICON_HINTS` + `_VERB_ACHV_INFO` + `base_tooltip` + `_build_verb_achievement_tooltip` 4 件 + 
`STYLE_GUIDE.md` §9.6.4 调色六元组 1 件, 6 verb 子类 0 改 base 0 改, 5 verb lazy-init 守卫 0 删 0 改, 含 1 verb Whisper T275 #194 
收回完成).


### 9.6.26 6 verb icon 视觉组 (5 段 canonical 1:1 序列) polish 模式 (T013 + T034 + T040 + T085 + T103 + T241 + T245 跨 6 任务 ~158 
轮落地) 文档化

> §9.6.3 记录 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb 模式; §9.6.4 记录 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法; §9.6.5 记录 
> 6 verb 视觉组连贯 tooltip `_build_verb_achievement_tooltip` 8 行拼接扩展器; §9.6.21 记录 6 verb 成就系统 闭环 8 段 canonical 1:1 序列 (含 
> ICON_COLORS 6 entry + PNG 双路径 1 段); §9.6.22 记录 6 verb VFX 视觉组 5 段; §9.6.24 记录 HUD 6 verb × 6 通道 = 36 通道; §9.6.25 记录 6 
> verb 视觉组连贯 tooltip 8 行拼接. 本节记录 `assets/ui/<verb>_icon/` + `assets/ui/achievements/<verb>_icon/` 6 verb icon 视觉组 (5 段 
> canonical 1:1 序列) polish 模式 — 跨 5 段 (Stage 1 6 verb family 路径 1:1 + Stage 2 6 verb disc + outer ring 1:1 + Stage 3 6 
> verb 调色 1:1 + Stage 4 6 verb 几何 1:1 + Stage 5 6 verb 双 export + 3 verb 双路径 1:1) — 跨 6 任务 (T013 #9 + T034 #31 + T040 
> #36 + T085 #49 + T103 #74 + T241 #158 + T245 #162) ~158 轮迭代落地, 6 verb icon 视觉组 1:1 严格分离 (6 verb 共享 disc + outer ring 
> + 6 verb 调色六元组 1:1 + 6 verb 几何 1:1 + 6 verb 双 export + 3 verb achievement 双路径), 0 漂动 0 后续 主要 pre-existing 风险. 任何"加新 1 
> verb icon"或"修 1 verb icon 视觉"或"修 1 verb 调色"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 3 1:1 严格), **再**按 Stage 1 family 路径 
> 1:1 加 1 目录 (Stage 1 1:1 严格), **再**按 Stage 2 disc + outer ring 1:1 复制 (Stage 2 1:1 严格), **再**按 Stage 4 6 verb 几何 1:1 加 
> 1 motif (Stage 4 1:1 严格), **再**按 Stage 5 双 export 1:1 加 2 文件 (32x32 + 64x64, Stage 5 1:1 严格), **再**按 §9.6.21 PNG 双路径 
> 1:1 镜像 (3 verb achievement path 1:1 严格, Stage 5 1:1 严格). 跨 6 verb (Pulse Coral / Bind Violet / Cut Amber / Echo Cyan 
> / Wave Pale / Whisper Mauve) + 6 verb 调色 1:1 + 6 verb 几何 1:1 + 6 verb 双 export 1:1 (32x32 + 64x64) + 3 verb 
> achievement 双路径 1:1 (echo_icon / wave_icon / whisper_icon) 0 漏 1 verb 全部走同一套 5 段 1:1 序列 0 例外.

- **症状**: `assets/ui/<verb>_icon/` + `assets/ui/achievements/<verb>_icon/` 6 verb icon 视觉组跨 6 task 反复 polish 期间出现 6 类典型 
fragile: (1) "加新 1 verb 漏 Stage 1 family 路径 1:1" — 新 verb 接入 icon 视觉组时漏 `assets/ui/<verb>_icon/` family 路径 1 目录创建, 触发 
`STYLE_GUIDE.md` 6 verb icon 列表 0 镜像 实际 PNG 路径 (`res://assets/ui/pulse_icon/pulse_icon.png` 等 6 verb 1:1 严格) 0 撞既有 5 
verb 0 改既有 5 verb 既有 path; (2) "修 1 verb icon 漏 Stage 2 disc + outer ring 1:1 镜像" — 修 Pulse icon 时改 deep ink navy 
#05070D disc 颜色 (e.g. 误改 #0A0E15 偏深, 与 glass cyan #69C7CE 1px outer ring 1:1 严格 0 撞 5 verb disc 配色) 或 0 改 outer ring 
1px (e.g. 改 2px), 触发 6 verb disc + outer ring 1:1 严格 0 撞既有 5 verb disc 配色 + 1px outer ring 0 撞, 玩家扫 6 verb icon 时 Pulse 
disc 颜色偏深与其他 5 verb disc 0 协调, outer ring 粗细不一致; (3) "加新 1 verb 漏 Stage 3 调色 1:1 同步" — 新 verb 接入 icon 时漏 
`STYLE_GUIDE.md` §9.6.4 调色六元组加 1 entry (e.g. 第 7 verb 调色 hex 0 改 §9.6.4 调色六元组 0 撞既有 6 verb), 触发 调色六元组 6 entry 0 撞 新 
verb 调色, 玩家看到新 verb icon motif 颜色与 §9.6.4 调色 0 镜像 视觉组连贯崩塌; (4) "加新 1 verb 漏 Stage 4 几何 1:1 同步" — 新 verb 接入 icon 时漏 6 
verb 几何 motif 1:1 (Pulse 圆环 / Bind 螺旋 / Cut 锋线 / Echo 盾球 / Wave 双环 / Whisper constant 球, 5 verb 动态几何 + 1 verb 静态球), 0 在 
§9.6.5 `_VERB_ACHV_INFO` `geometry_zh` 字段同步 0 在 §9.6.22 6 verb VFX 视觉组 几何 1:1 镜像, 玩家扫 icon 0 看到几何 motif 1:1 0 知道 "这是哪一 
verb"; (5) "加新 1 verb 漏 Stage 5 双 export 1:1 同步" — 新 verb 接入 icon 时漏 双 export (32x32 game + 64x64 UI, 2 文件 1:1 严格), 触发 
`pause_menu.gd` `_profile_abilities` TextureRect load 0 找到 64x64 路径 0 走 fallback 0 走 icon 视觉组连贯, 玩家扫 profile 看到 0 64x64 
高清 icon 0 32x32 占位 0 视觉组连贯; (6) "修 3 verb achievement path 漏 Stage 5 双路径 1:1 同步" — 修 3 verb (echo / wave / whisper) 
achievement path 时漏 `assets/ui/achievements/<verb>_icon/<verb>_icon.png` + `<verb>_icon_32x32.png` 2 文件 1:1 严格 (T245 
#162 落地 4 PNG 双路径 = verb family 2 文件 + achievement 2 文件), 0 在 §9.6.21 PNG 双路径 1:1 镜像, 玩家扫成就 grid 看到 0 64x64 high-res 
icon 0 32x32 缩略 视觉组连贯崩塌. 6 类症状根因: 6 verb icon 视觉组 5 段 canonical 1:1 序列 0 文档化, polish 期只能"凭记忆"同步 N 处 (1 Stage 1 family 
路径 + 1 Stage 2 disc + outer ring + 1 Stage 3 调色六元组 entry + 1 Stage 4 几何 motif + 1 Stage 5 双 export + 1 Stage 5 3 verb 
achievement 双路径), 1 漏 1 = 6 verb icon 视觉组 漂动 (family 路径 0 镜像 / disc 配色 0 协调 / 调色 0 镜像 §9.6.4 / 几何 0 镜像 §9.6.22 / 64x64 
UI 0 加载 / 3 verb achievement 0 镜像 §9.6.21).
- **触发场景**: `assets/ui/<verb>_icon/` + `assets/ui/achievements/<verb>_icon/` 6 verb icon 视觉组 5 段 canonical 1:1 序列落地需同步 
5 件套: (1) **Stage 1 6 verb family 路径 1:1 严格分离** — `assets/ui/<verb>_icon/` 6 verb family 路径 1:1 严格 (pulse_icon / 
bind_icon / cut_icon / echo_icon / wave_icon / whisper_icon 6 目录 0 漏 0 改 0 重命名 0 反序 0 改 1 字符, T013 #9 + T034 #31 + T040 
#36 + T085 #49 + T103 #74 + T245 #162 跨 6 task 落地 0 漂动), 0 触碰既有 5 verb family 路径 0 改 0 漏; (2) **Stage 2 6 verb disc + 
outer ring 1:1 严格分离** — 6 verb icon 共享 deep ink navy #05070D 背景 disc + glass cyan #69C7CE 1px outer ring 1:1 严格 (6 verb 
1:1 严格 0 改 0 漏 0 改 1 hex 0 改 1 px, 与 §9.6.5 tooltip 视觉组连贯 0 撞 0 漏 1 verb), 0 触碰既有 5 verb disc + outer ring; (3) **Stage 
3 6 verb 调色 1:1 严格分离** — 6 verb 调色六元组 (与 §9.6.4 调色六元组 1:1 严格镜像, 0 漏 0 改 1 hex 0 改 1 通道值, 0 漏 0 重叠 0 互混 0 复用 0 共享, 6 
verb 调色 1:1 严格: Pulse Coral #E86D5A / Bind Muted Violet #65506A / Cut Amber Voice #F2B66E / Echo Glass Cyan #69C7CE / 
Wave Pale Resonance #B7E6DC / Whisper Muted Mauve #C8A4D8) 1:1 严格, 0 触碰既有 5 verb 调色 0 改 1 hex; (4) **Stage 4 6 verb 几何 
1:1 严格分离** — 6 verb 几何 motif 1:1 严格 (Pulse 圆环 / Bind 螺旋 / Cut 锋线 / Echo 盾球 / Wave 双环 / Whisper constant 球, 5 verb 动态几何 
+ 1 verb 静态球, T245 #162 落地时 Whisper 改走 constant 球 区别于前 5 verb 动态几何, 与 §9.6.5 `_VERB_ACHV_INFO` `geometry_zh` 字段 1:1 镜像 
+ 与 §9.6.22 6 verb VFX 视觉组 几何 motif 1:1 镜像), 0 触碰既有 5 verb 几何; (5) **Stage 5 6 verb 双 export + 3 verb 双路径 1:1 严格分离** — 
`assets/ui/<verb>_icon/<verb>_icon.png` 32x32 game icon + `<verb>_icon_64x64.png` 64x64 UI icon 2 文件 1:1 严格 (6/6 0 漏 0 
改 0 重命名 0 反序 0 改 1 字符) + `assets/ui/achievements/<verb>_icon/<verb>_icon.png` + `<verb>_icon_32x32.png` 2 文件 1:1 严格 
(3/6 only echo_icon / wave_icon / whisper_icon, T245 #162 落地 4 PNG 双路径, 与 §9.6.21 PNG 双路径 1:1 镜像). 5 件套 1:1 同步, 1 漏 1 = 
6 verb icon 视觉组 漂动.
- **修复** (跨 6 任务 ~158 轮落地后已知 5 段 canonical 1:1 序列 + 6 verb icon 视觉组 0 例外):
  - **T013 (#9) A025 Pulse icon 起步 (5 verb 时代 1 verb icon 模板)** — `assets/ui/pulse_icon/pulse_icon.png` 32x32 game icon 
+ `pulse_icon_32x32.png` (optional) 起步 (T013 落地时 5 verb 时代 1 verb icon 模板, disc + outer ring 0 文档化 0 共享 1 verb, 5 verb 
接入 icon 时 0 触碰 Pulse icon 既有字段), 0 触碰既有 5 verb icon 0 改 0 漏.
  - **T034 (#31) A033 Bind icon + Stage 1 family 路径 1:1 镜像** — `assets/ui/bind_icon/bind_icon.png` 32x32 game icon + 
`bind_icon_64x64.png` 64x64 UI icon 2 文件 1:1 严格落地 (Stage 1 1:1 严格, T034 落地时 Bind family 路径 1:1 复制 Pulse family 路径模板 0 改 
0 漏 0 漏 1 字符), 0 触碰 Pulse icon 既有字段 0 改 0 漏.
  - **T040 (#36) A038 Cut icon + Stage 2 disc + outer ring 共享模式** — `assets/ui/cut_icon/cut_icon.png` + 
`cut_icon_64x64.png` 2 文件 1:1 严格 + 与 Pulse + Bind 共享 disc + outer ring 1:1 严格 (Stage 2 1:1 严格, T040 落地时 3 verb 共享 disc 
+ outer ring 0 触碰 0 改 0 漏 1 通道), 0 触碰既有 2 verb 0 改 0 漏.
  - **T085 (#49) A061 Echo icon + Stage 3 调色六元组 1:1 镜像** — `assets/ui/echo_icon/echo_icon.png` + `echo_icon_64x64.png` 
2 文件 1:1 严格 + Stage 3 调色六元组 4 verb entry (Pulse Coral + Bind Violet + Cut Amber + Echo Glass Cyan) 1:1 严格 (Stage 3 1:1 
严格, 0 改既有 3 verb 调色 0 改 1 hex 0 改 1 通道值), 0 触碰既有 3 verb 0 改 0 漏.
  - **T103 (#74) A071 Wave icon + Stage 4 5 verb 几何 motif 1:1 共享** — `assets/ui/wave_icon/wave_icon.png` + 
`wave_icon_64x64.png` 2 文件 1:1 严格 + 5 verb 几何 motif 共享 1:1 严格 (Pulse 圆环 / Bind 螺旋 / Cut 锋线 / Echo 盾球 / Wave 双环, 5 verb 
动态几何, Stage 4 1:1 严格, 0 改既有 4 verb 几何 0 改 1 motif), 0 触碰既有 4 verb 0 改 0 漏.
  - **T241 (#158) + T245 (#162) A074 Whisper icon + Stage 4 constant 球 静态几何区别 + Stage 5 4 PNG 双路径 1:1** — 
`assets/ui/whisper_icon/whisper_icon.png` + `whisper_icon_64x64.png` 2 verb family 文件 1:1 严格 + 
`assets/ui/achievements/whisper_icon/whisper_icon.png` + `whisper_icon_32x32.png` 2 achievement 文件 1:1 严格 (Stage 5 4 
PNG 双路径 1:1 严格, T245 落地时 3 verb achievement path 0 触碰既有 3 verb 既有 0 改 0 漏 0 漏 1 文件) + 6 verb 几何 motif 1:1 严格 (Whisper 
改走 constant 球 静态几何区别于前 5 verb 动态几何, T245 (#162) 落地时 5 verb 动态几何 0 触碰, 0 触碰 §9.6.5 `_VERB_ACHV_INFO` `geometry_zh` 字段 0 
改 0 漏) + 6 verb 调色六元组第 6 entry (Whisper Muted Mauve #C8A4D8 0 撞 5 verb 调色, 与 §9.6.4 调色六元组 1:1 镜像 0 改 0 漏 1 entry 0 改 1 
hex).
  - 跨 6 任务 ~158 轮落地后 0 漂动, 6 verb icon 视觉组 5 段 canonical 1:1 序列 100% 闭环, 5 段 1:1 严格分离 (Stage 1 6 verb family 路径 1:1 严格 
0 漏 0 改 0 重命名 0 反序 + Stage 2 6 verb disc + outer ring 1:1 严格 0 漏 0 改 0 改 1 hex 0 改 1 px + Stage 3 6 verb 调色 1:1 严格 0 漏 
0 改 0 重叠 0 互混 0 复用 0 共享 + Stage 4 6 verb 几何 motif 1:1 严格 0 漏 0 改 0 改 1 motif + Stage 5 6 verb 双 export 1:1 严格 0 漏 0 改 0 
重命名 0 反序 + 3 verb achievement 双路径 1:1 严格 0 漏 0 改 0 漏 1 文件), 6 verb 调色 1:1 严格分离 (Pulse Coral / Bind Violet / Cut Amber / 
Echo Cyan / Wave Pale / Whisper Mauve 6 entry 0 漏 0 重叠 0 互混 0 复用 0 共享). 6 任务 0 后续 主要 pre-existing 风险.
- **预防**:
  1. 任何 polish 期给"加新 1 verb icon"或"修 1 verb icon 视觉"或"修 1 verb 调色"任务加新 verb / 加新几何 / 加新调色时**必须**严格按 5 段 canonical 1:1 
序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 改 6 verb family 路径** (T013 + T034 + T040 + T085 + T103 + T245 落地后 
6 verb family 路径 1:1 严格, `pulse_icon` / `bind_icon` / `cut_icon` / `echo_icon` / `wave_icon` / `whisper_icon` 6 目录 0 改 
0 重命名 0 漏 1 目录 0 反序, 改 1 目录名 = `pause_menu.gd` `_profile_abilities` TextureRect load 0 找到 path 0 走 fallback 0 视觉组连贯崩塌), 
**Stage 2 0 改 6 verb disc + outer ring 1:1** (T013 + T034 + T040 + T085 + T103 + T245 落地后 6 verb 共享 deep ink navy 
#05070D disc + glass cyan #69C7CE 1px outer ring 1:1 严格, 0 改 0 漏 0 改 1 hex 0 改 1 px, 改 1 hex / 1 px = 6 verb icon 
视觉组连贯崩塌), **Stage 3 0 改 6 verb 调色 0 改 1 hex 0 改 1 通道值** (T013 + T034 + T040 + T085 + T103 + T245 落地后 6 verb 调色六元组 1:1 
严格, 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 hex / 1 通道值 = 6 verb icon 视觉组连贯崩塌 + §9.6.4 调色六元组 entry 漂动 + §9.6.24 HUD 36 通道 漂动 + 
§9.6.3 5+1 verb 7 UI 通道 漂动 + §9.6.22 6 verb VFX 视觉组 漂动), **Stage 4 0 改 6 verb 几何 motif** (T013 + T034 + T040 + T085 + 
T103 + T245 落地后 6 verb 几何 1:1 严格, 0 改 0 漏 0 改 1 motif, 改 1 motif = 6 verb icon 视觉组连贯崩塌 + §9.6.5 `_VERB_ACHV_INFO` 
`geometry_zh` 字段 漂动 + §9.6.22 6 verb VFX 视觉组 几何 漂动), **Stage 5 0 改 6 verb 双 export + 3 verb 双路径** (T013 + T034 + T040 + 
T085 + T103 + T245 落地后 6 verb 双 export 1:1 严格 (6/6) + 3 verb achievement 双路径 1:1 严格 (3/6), 0 改 0 漏 0 改 1 文件名 0 反序 0 改 1 
px 0 改 1 文件类型, 改 1 文件名 / 1 文件 = 6 verb icon 双 export 漂动 + 3 verb achievement 双路径 漂动). 任何"加新 1 verb icon"必须**先**按 §9.6.4 
调色六元组加 1 entry (Stage 3 1:1 严格), **再**按 Stage 1 family 路径 1:1 加 1 目录 (Stage 1 1:1 严格, 0 改既有 6 verb family 路径 0 改 1 字符), 
**再**按 Stage 2 disc + outer ring 1:1 复制 (Stage 2 1:1 严格, 0 改 0 漏 0 改 1 hex 0 改 1 px), **再**按 Stage 4 6 verb 几何 1:1 加 1 
motif (Stage 4 1:1 严格, 0 改既有 6 verb 几何 0 改 1 motif), **再**按 Stage 5 双 export 1:1 加 2 文件 (32x32 + 64x64, Stage 5 1:1 严格, 
0 漏 0 改既有 6 verb 双 export 0 漏 1 文件 0 改 1 文件名), **再**按 §9.6.21 PNG 双路径 1:1 镜像 (3 verb achievement path 1:1 严格, 0 改既有 3 
verb 0 漏 1 文件).
  2. **5 段序列 0 触碰边界** — `STYLE_GUIDE.md` 任何 const / var 0 改 0 删 0 重排 (5 段 0 触碰边界), 任何"修 1 verb 调色"polish 走 Stage 3 1:1 
严格 (改 §9.6.4 调色六元组 既有 entry 时**必须**同步改 icon motif 颜色 + §9.6.24 HUD 主题色 const + §9.6.3 HUD 6 行 6 通道 + §9.6.22 6 verb VFX 
`@export` color 字段, 0 改 1 hex 0 改 1 通道值 0 漏 1 跨段); 任何"修 1 verb 几何"polish 走 Stage 4 1:1 严格 (改 6 verb 几何 motif 0 改 1 
motif 0 漏 1 motif 同步改 §9.6.5 `_VERB_ACHV_INFO` `geometry_zh` 字段 + §9.6.22 6 verb VFX 视觉组 几何 1:1 镜像).
  3. **6 verb 调色 0 改 1 hex 0 改 1 通道值** (T013 + T034 + T040 + T085 + T103 + T245 落地后 0 触碰边界) — Pulse Coral #E86D5A / 
Bind Muted Violet #65506A / Cut Amber Voice #F2B66E / Echo Glass Cyan #69C7CE / Wave Pale Resonance #B7E6DC / Whisper 
Muted Mauve #C8A4D8 6 verb 调色 1:1 严格分离 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 hex / 1 通道值 = 6 verb icon 视觉组 漂动 (Stage 3 漂动) + 
§9.6.4 调色六元组 entry 漂动 + §9.6.24 HUD 36 通道 漂动 + §9.6.3 5+1 verb 7 UI 通道 漂动 + §9.6.22 6 verb VFX 视觉组 漂动, source-grep 验证 6 
verb icon motif 颜色 + §9.6.4 调色六元组 6 entry + §9.6.24 HUD 主题色 const 6 entry + §9.6.3 HUD 6 行 6 通道 + §9.6.22 6 verb VFX 
`@export` color 字段 0 漏 0 改 1 字段 0 改 1 通道值.
  4. **6 verb 几何 0 改 0 漏 0 改 1 motif** (T013 + T034 + T040 + T085 + T103 + T245 落地后 0 触碰边界) — 6 verb 几何 motif 1:1 严格 
(Pulse 圆环 / Bind 螺旋 / Cut 锋线 / Echo 盾球 / Wave 双环 / Whisper constant 球, 5 verb 动态几何 + 1 verb 静态球, T245 (#162) 落地时 
Whisper 改走 constant 球区别于前 5 verb 动态几何, 0 改既有 5 verb 几何 motif 0 改 1 motif), 改 1 motif / 漏 1 motif = 6 verb icon 视觉组连贯崩塌 
+ §9.6.5 `_VERB_ACHV_INFO` `geometry_zh` 字段 漂动 + §9.6.22 6 verb VFX 视觉组 几何 漂动, source-grep 验证 6 verb icon motif + 
§9.6.5 `_VERB_ACHV_INFO` 3 entry `geometry_zh` 字段 + §9.6.22 6 verb VFX 视觉组 几何 motif 0 漏 0 改 1 motif.
  5. **6 verb 双 export 0 漏 0 改 0 漏 1 文件 0 改 1 文件名 0 改 1 文件类型** (T013 + T034 + T040 + T085 + T103 + T245 落地后 0 触碰边界) — 
`assets/ui/<verb>_icon/<verb>_icon.png` 32x32 + `<verb>_icon_64x64.png` 64x64 2 文件 1:1 严格 (6/6 0 漏 0 改 0 漏 1 文件 0 改 1 
文件名 0 改 1 文件类型, 0 触碰既有 5 verb 双 export), 改 1 文件名 / 漏 1 文件 = 6 verb icon 双 export 漂动, source-grep 验证 6 verb 双 export 2 
文件 0 漏 0 改 1 文件名 0 改 1 文件类型 0 改 1 字符.
  6. **3 verb achievement 双路径 0 漏 0 改 0 漏 1 文件** (T245 #162 落地后 0 触碰边界) — 
`assets/ui/achievements/<verb>_icon/<verb>_icon.png` + `<verb>_icon_32x32.png` 2 文件 1:1 严格 (3/6 only echo_icon / 
wave_icon / whisper_icon, T245 (#162) 落地 4 PNG 双路径, 与 §9.6.21 PNG 双路径 1:1 镜像), 0 触碰既有 3 verb 0 改 0 漏 1 文件, 改 1 文件名 / 漏 
1 文件 = 3 verb achievement 双路径 漂动 + §9.6.21 PNG 双路径 漂动, source-grep 验证 3 verb achievement 2 文件 0 漏 0 改 1 文件名 0 改 1 文件类型 
0 改 1 字符.
  7. 已知 drift risk: 6 verb icon 视觉组 (5 段 canonical 1:1 序列) **可能**漏 1 段 1:1 镜像 (e.g. T245 (#162) 落地时漏 Stage 5 4 PNG 双路径 
1:1 严格, 3 verb achievement path 0 镜像 §9.6.21 PNG 双路径 0 触碰; 漏 Stage 4 6 verb 几何 motif 0 镜像 §9.6.5 `_VERB_ACHV_INFO` 
`geometry_zh` 字段 0 触碰; 漏 Stage 3 6 verb 调色 0 镜像 §9.6.4 调色六元组 0 触碰). 监控建议: 任何"未来给 6 verb icon 加新 verb 或修 1 verb icon 
视觉或修 1 verb 调色"必须**先**在 5 段序列 (Stage 1 family 路径 + Stage 2 disc + outer ring + Stage 3 调色 + Stage 4 几何 + Stage 5 双 
export + 3 verb 双路径) 1:1 镜像 0 漏 1 段. T245 (#162) 5 段序列 1:1 镜像落地后任何新 verb / 新几何 / 新调色 加 5 段同步是 §9.6.26 5 段序列的"母集" (1 
verb 5 段 1:1 严格, §9.6.26 5 段 1 verb 完整闭环, 6 verb × 5 段 = 30 件 1:1 严格, 含 6 verb 双 export 12 文件 0 漏 0 改 + 3 verb 
achievement 双路径 4 文件 0 漏 0 改). 另 1 类已知 drift: Stage 2 disc + outer ring 0 改 1 hex / 1 px (e.g. 改 deep ink navy #05070D 
为 #0A0E15 偏深, 0 触碰既有 5 verb disc 配色), 后续 polish 期若加新 verb 必须**先**用 1:1 镜像 deep ink navy #05070D + glass cyan #69C7CE 
1px outer ring 模板, 0 改既有 5 verb disc + outer ring.

**0 副作用**: T013 / T034 / T040 / T085 / T103 / T241 / T245 任何 const / var 0 触碰 (6 verb 调色 0 改 0 删 0 重排 + 6 verb 几何 motif 
0 改 0 删 0 重排 + 6 verb family 路径 0 改 0 删 0 重排 + 6 verb 双 export 12 文件 0 改 0 删 0 重排 + 3 verb achievement 双路径 4 文件 0 改 0 删 
0 重排); 0 触碰 §9.6.1 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫 (T282 0 改 6 verb `_current_<verb>_vfx` 
任何字段); 0 触碰 §9.6.2 5 layer 视觉组既有 5 verb 任何 1 字段 1 alpha 公式 1 layer 顺序; 0 触碰 §9.6.3 5+1 verb 7 UI 通道 既有 6 verb 任何 1 字段 1 
调点 (T282 0 改 6 verb 4 @onready var + 1 ability ref + 1 主题色 const + 1 风格 stylebox + 1 dict key + 1 iteration list 
element 0 触碰既有 5 步 1:1 复制 模板); 0 触碰 §9.6.4 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (T282 0 改 6 verb 调色 1 hex 0 改 1 通道值 + 0 
改 HUD 6 行 6 通道 + 0 改 tooltip 6 字段); 0 触碰 §9.6.5 6 verb 视觉组连贯 tooltip `_build_verb_achievement_tooltip` 函数体 (T282 0 改 5 
段序列任何 1 字段 0 改 `_VERB_ACHV_ICON_HINTS` 0 改 `_VERB_ACHV_INFO` 0 改 8 行硬约束 0 改 3 闭环宪法同步); 0 触碰 §9.6.16 6 verb hit handler 
5 件套 (T282 0 改 6 verb `_on_<verb>_hit` 函数体任何 const / var / 屏染 / 屏抖 / VFX flash / 音频); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 
件套 (T282 0 改 6 verb 主信号 + 命中信号 + 结束信号 + 扩展信号 + PlayerActionGate register / unregister); 0 触碰 §9.6.18 16 件套 verb ability 
base (T282 0 改 6 verb ability 子类任何 const / var / `@export` / `@onready` / 5 共享方法 / `_exit_tree` 兜底 / 
`is_globally_blocking` 扩展); 0 触碰 §9.6.19 7 件套 windup VFX base (T282 0 改 6 verb windup VFX 子类任何 const / var / `_ready()` 
/ `_process(delta)` / `_activate_windup_tween()` / `fade_out_and_free()`); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 transition 
flow (T282 0 改 GFC + room_transition + room_door + GameState 5 段序列); 0 触碰 §9.6.21 8 段成就序列 (T282 0 改 PlayerStats 6 verb 
字段 + record_ability_used 6 branch + get_stat 6 entry + set_stat 6 entry + reset_run 6 行 + all_abilities_used 6 项 + 
achievements.json 4 tier ID + ICON_COLORS 6 entry + PNG 双路径); 0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T282 0 改 6 verb VFX 文件 
任何 const / var / `@export` color 字段 / 5+ layer 视觉组 / 几何 / 跨段镜像); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T282 0 改 
audio_manager_enhanced.gd 7 桶 `prewarm_*` 函数体 0 改 0 删 0 重排 + prewarm_all_sfx() aggregator 7 行 0 改 0 删 0 重排 + 3 跨调用方 0 改 
0 删 0 重排); 0 触碰 §9.6.24 HUD 6 verb × 6 通道 = 36 通道 5 段序列 (T282 0 改 hud.gd 6 verb @onready var / var / const / dict key / 
stylebox / iteration list 任何 1 字段 0 改 0 删 0 重排); 0 触碰 §9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 5 段序列 (T282 0 改 pause_menu.gd 
`_VERB_ACHV_ICON_HINTS` / `_VERB_ACHV_INFO` / `base_tooltip` / `_build_verb_achievement_tooltip` / 11 slot 0 改 0 删 0 
重排); 0 触碰 check_smoke_consistency.sh (T282 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + 
install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, 0 触碰 6 
verb icon 既有 PNG 文件 0 改既有 6 verb family 路径 0 改既有 6 verb 双 export 0 改既有 3 verb achievement 双路径); 0 行为变化, 1 文档 1 段 
(§9.6.26 ~50 行) + 1 smoke test (T282 20+ 断言).

**与 §9.6.3 / §9.6.4 / §9.6.5 / §9.6.21 / §9.6.22 / §9.6.24 / §9.6.25 关系** — §9.6.3 覆盖 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 
verb 模式 (加新 1 verb 时 6 处 1:1 复制: tscn + 4 @onready var + 1 ability ref + 1 const + 1 stylebox + dict key + iteration 
list); §9.6.4 覆盖 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 1+2+3); §9.6.5 覆盖 6 verb 视觉组连贯 tooltip 
`_build_verb_achievement_tooltip` 8 行拼接扩展器; §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 canonical 1:1 序列 (含 ICON_COLORS 6 entry + PNG 
双路径 1 段); §9.6.22 覆盖 6 verb VFX 视觉组 5 段 canonical 1:1 序列; §9.6.24 覆盖 HUD 6 verb × 6 通道 = 36 通道 5 段 canonical 1:1 序列; 
§9.6.25 覆盖 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列. §9.6.26 覆盖 `assets/ui/<verb>_icon/` + 
`assets/ui/achievements/<verb>_icon/` 6 verb icon 视觉组 (5 段 canonical 1:1 序列) (Stage 1 6 verb family 路径 1:1 + Stage 2 6 
verb disc + outer ring 1:1 + Stage 3 6 verb 调色 1:1 + Stage 4 6 verb 几何 motif 1:1 + Stage 5 6 verb 双 export + 3 verb 
achievement 双路径 1:1). §9.6.3 关注"HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb structural sync (加新 1 verb 时 6 处 1:1 复制)", §9.6.4 
关注"6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 1+2+3)", §9.6.5 关注"6 verb 视觉组连贯 tooltip 8 行拼接扩展器 (T250 落地 
polish 模式, 5 段 1:1 严格分离待 §9.6.25 文档化)", §9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格, 含 PNG 双路径)", §9.6.22 关注"6 verb VFX 视觉组 
5 段 1:1 严格分离契约 (调色 + 几何 + layer + 跨段镜像)", §9.6.24 关注"HUD 6 verb × 6 通道 = 36 通道 1:1 严格分离契约 (5 段 1:1 严格分离)", §9.6.25 关注"6 
verb 视觉组连贯 tooltip 8 行拼接 1:1 严格分离契约 (5 段 canonical 1:1 序列)", §9.6.26 关注"6 verb icon 视觉组 1:1 严格分离契约 (5 段 canonical 1:1 
序列: Stage 1 family 路径 + Stage 2 disc + outer ring + Stage 3 调色 + Stage 4 几何 + Stage 5 双 export + 3 verb achievement 
双路径)". 14 套 polish 模式 0 反序 0 漏 0 改 1 字符. 任何"加新 1 verb icon"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 3 1:1 严格), **再**按 
§9.6.3 5+1 verb 7 UI 通道 6 处 1:1 复制 (Stage 3 1:1 严格), **再**按 §9.6.24 HUD 36 通道 0 漏 1 verb 0 改 1 通道值 (Stage 3 1:1 严格), 
**再**按 §9.6.26 Stage 1 family 路径 1:1 加 1 目录 (Stage 1 1:1 严格), **再**按 §9.6.26 Stage 2 disc + outer ring 1:1 复制 (Stage 2 
1:1 严格), **再**按 §9.6.26 Stage 4 几何 motif 1:1 加 1 motif (Stage 4 1:1 严格), **再**按 §9.6.26 Stage 5 双 export 1:1 加 2 文件 
(Stage 5 1:1 严格), **再**按 §9.6.21 PNG 双路径 1:1 镜像 (3 verb achievement path 1:1 严格). §9.6.4 与 §9.6.26 0 互混 0 复用 0 共享: 
§9.6.4 关注"6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 1+2+3, 1 句宪法 0 触及 5 段序列 0 触及 6 verb icon 双 export 0 
触及 3 verb achievement 双路径)" (与 5 段序列 0 相关), §9.6.26 关注"6 verb icon 视觉组 5 段 canonical 1:1 序列 (5 段全在 PNG 文件 + 
STYLE_GUIDE.md 6 verb icon 列表)" (与 abstract 宪法 0 相关, Stage 3 1:1 严格 镜像 §9.6.4 调色六元组 6 entry 0 改 1 hex). §9.6.21 与 
§9.6.26 0 互混 0 复用 0 共享: §9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格, 含 ICON_COLORS 6 entry + PNG 双路径 1 段)" (与 5 段序列 0 相关, 
PNG 双路径仅 1 段), §9.6.26 关注"6 verb icon 视觉组 5 段 canonical 1:1 序列 (5 段全在 PNG 文件 + STYLE_GUIDE.md 6 verb icon 列表, 含 3 verb 
achievement 双路径 4 文件 1:1 镜像 §9.6.21 PNG 双路径)" (与 6 verb 成就系统 8 段 0 相关). §9.6.22 与 §9.6.26 0 互混 0 复用 0 共享: §9.6.22 关注"6 
verb VFX 视觉组 5 段 1:1 严格分离契约 (Stage 1-5: class_name + 调色六元组 + layer + 几何 + 跨段镜像, 5 段全在 `<verb>_vfx.gd` 文件)" (与 icon 视觉组 
0 相关, 仅 Stage 4 几何 motif 1:1 镜像 §9.6.26 Stage 4), §9.6.26 关注"6 verb icon 视觉组 5 段 canonical 1:1 序列 (5 段全在 PNG 文件 + 
STYLE_GUIDE.md 6 verb icon 列表)" (与 VFX 视觉组 0 相关). §9.6.26 是 14 套 polish 模式**唯一**关注"6 verb icon 视觉组 1:1 严格分离 (5 段 
canonical 1:1 序列: Stage 1 family 路径 + Stage 2 disc + outer ring + Stage 3 调色 + Stage 4 几何 + Stage 5 双 export + 3 verb 
achievement 双路径)"的 1 套, 与 §9.6.3 / §9.6.4 / §9.6.5 / §9.6.21 / §9.6.22 / §9.6.24 / §9.6.25 13 套 HUD 6 verb 行 / 调色 + HUD 
6 行 6 通道 + tooltip 拼接扩展器 / 成就 / VFX 视觉组 / HUD 36 通道 / 视觉组连贯 tooltip 拼接 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.26 5 段序列走 §9.1 9 步落地的 "const" (3) + "icon" (6) 2 步 (`STYLE_GUIDE.md` §9.6.4 
调色六元组 6 verb entry + `assets/ui/<verb>_icon/` 6 verb family 路径 + `assets/ui/<verb>_icon/<verb>_icon.png` 32x32 + 
`<verb>_icon_64x64.png` 64x64 6 verb 双 export 12 文件 + `assets/ui/achievements/<verb>_icon/` 3 verb achievement 双路径 4 文件 
+ 6 verb disc + outer ring 1:1 + 6 verb 几何 motif 1:1), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "input" (4) + 
"shop" (5) + "vfx" (7) + "audio" (8) + "HUD 冷光勾边" (9) 7 步 (6 verb icon 视觉组 0 走 verb enum + ability 子类 + input map + 
shop 系统 + VFX 视觉组 + audio 副作用 + HUD 渲染). 任何"加新 1 verb icon"必须**先**按 §9.1 9 步落地 (含 "const" (3) + "icon" (6) 步), **再**按 
§9.6.26 5 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 任何"加新 1 verb 调色"必须**先**在 `STYLE_GUIDE.md` §9.6.4 调色六元组加 1 entry (Stage 3 1:1 
严格, 0 漏 1 entry 0 改 1 hex), **再**在 6 verb icon motif 颜色 1:1 镜像 (Stage 3 1:1 严格, 0 漏 1 通道 0 改 1 通道值), 0 触碰 §9.1 9 步任何 
"verb" (1) + "ability" (2) 步 (6 verb icon 视觉组 0 触碰 verb enum + ability 子类). 任何"加新 1 verb icon"必须**先**在 
`assets/ui/<verb>_icon/` family 路径 1:1 加 1 目录 (Stage 1 1:1 严格), **再**在 6 verb 双 export 1:1 加 2 文件 (Stage 5 1:1 严格, 0 漏 
1 文件 0 改 1 文件名), 0 触碰 §9.1 9 步任何 "icon" (6) 步既有 6 verb icon PNG 双路径. 任何"加新 1 verb achievement 双路径"必须**先**在 §9.6.21 PNG 
双路径 1:1 镜像 (Stage 5 1:1 严格), **再**在 `assets/ui/achievements/<verb>_icon/` 3 verb achievement 双路径 1:1 加 2 文件 (Stage 5 
1:1 严格, 0 漏 1 文件 0 改 1 文件名), 0 触碰 §9.6.21 既有 3 verb 0 改 0 漏 1 文件. 任何"加新 1 跨段镜像"必须**先**在 §9.6.3 / §9.6.4 / §9.6.21 / 
§9.6.22 / §9.6.24 / §9.6.25 6 跨段加 1 镜像 (5 段序列 1:1 严格, 0 漏 1 跨段 0 改 1 字段), 0 触碰 §9.1 9 步任何 const / var / 节点 (5 段序列仅在 
`STYLE_GUIDE.md` 6 verb icon 列表 + §9.6.4 调色六元组 + 6 verb icon PNG 文件 + 3 verb achievement PNG 文件, 6 verb 子类 0 改 base 0 改,
 5 verb lazy-init 守卫 0 删 0 改, 含 1 verb Whisper T245 #162 接入完成).
