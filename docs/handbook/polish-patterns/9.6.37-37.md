# Polish Patterns 9.6.37–37

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.37–9.6.37，共 1 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.37-37.md

<!-- shard 37-37 共 1 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.37 SaveSystem save data CRC32 校验 5 段 1:1 严格分离契约 polish 模式 (T128 #109 + T224 #146 跨 2 任务 ~107 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 27 套 polish 模式均在 "pause_menu / 6 verb / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 
> 持久化层 / audio 数据层 / PlayerActionGate 单 autoload 4 件套" 范畴, 0 涉及 "SaveSystem save data CRC32 校验 5 段 1:1 严格分离契约" 落地. 
> §9.6.15 (T271 #190 落地) 覆盖 SaveSystem `audit_save_slots` 6 字段 巡检 单件, §9.6.34 (T127-T209 #128 落地) 覆盖 PlayerStats 
> `_best_stats` + `_run_history` 持久化层字段扩展 6 段, §9.6.35 (T062-T118 #63 落地) 覆盖 AudioPresets `MUSIC_PRESETS` preset 字段扩展 5 
> 段 — 这 3 段分别覆盖 SaveSystem 巡检 单件 / PlayerStats 持久化层 / audio 数据层, 但**未**覆盖 "SaveSystem save data CRC32 校验 5 段 
> (`_crc32_of_string` 1 字段 + `_verify_and_unwrap` 1 方法 + `audit_save_slots` 1 巡检 + `save_dict` 1 dict + `loaded_data` 1 
> dict) 1:1 严格分离契约" 必须同时 1:1 镜像 5 段 (Stage 1 crc32 算法 字段 + Stage 2 verify 校验 方法 + Stage 3 audit 巡检 字段 + Stage 4 write 
> 包装 dict + Stage 5 read 解包 dict) 的契约. 本节 (T293 #217 落地) 首个落在**SaveSystem save data CRC32 校验 5 段 1:1 严格分离契约** 
> (`src/autoload/save_system.gd` 1 字段 `_crc32_of_string` IEEE CRC32 (poly 0xEDB88320 / init 0xFFFFFFFF / xorout 
> 0xFFFFFFFF) + 1 方法 `_verify_and_unwrap` 校验 + 1 巡检 `audit_save_slots` 6 字段 巡检 + 1 dict `save_dict` 包装层 `{ "data": ..., 
> "checksum": <crc32> }` + 1 dict `loaded_data` 解包层 `_read_json` 走 `_verify_and_unwrap` 镜像) polish 模式 — 跨 5 段 (Stage 1 
> crc32 字段 1:1 严格 0 漏 1 字段 + Stage 2 verify 方法 1:1 严格 0 漏 1 边 + Stage 3 audit 巡检 1:1 严格 0 漏 1 巡检 + Stage 4 write dict 
> 1:1 严格 0 漏 1 dict + Stage 5 read dict 1:1 严格 0 漏 1 dict) — 跨 1 套 polish 模式 (T293 #217 落地) 跨 2 任务 (T128 #109 + T224 
> #146 ~107 轮落地, 0 漂动 0 后续 主要 pre-existing 风险). 任何"加新 1 字段" (e.g. 未来加 `_sha256_of_string` 升级 SHA-256) 或"加新 1 方法" (e.g. 
> 未来加 `_sign_with_hmac` 加密签名) 或"加新 1 巡检" (e.g. 未来加 `audit_cloud_saves` 云存档巡检) 或"加新 1 dict" (e.g. 未来加 
> `version_migration_dict` 存档版本迁移 dict) 必须**先**按 Stage 1 crc32 字段 1:1 严格 加 1 entry (0 漏 1 字段 0 改 1 字段 0 反序 0 反向), 
> **再**按 Stage 2 verify 方法 1:1 镜像 (1:1 严格, 0 漏 1 边 0 改 1 边 0 反序 0 反向), **再**按 Stage 3 audit 巡检 1:1 镜像 (1:1 严格, 0 漏 1 巡检 
> 0 改 1 巡检 0 反序 0 反向), **再**按 Stage 4 write dict 1:1 镜像 (1:1 严格, 0 漏 1 dict 0 改 1 dict 0 反序 0 反向), **再**按 Stage 5 read 
> dict 1:1 镜像 (1:1 严格, 0 漏 1 dict 0 改 1 dict 0 反序 0 反向). 跨 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 
> 0 例外.

- **症状**: `SaveSystem` save data CRC32 校验 5 段 (`src/autoload/save_system.gd`, T128 #109 + T224 #146 跨 2 任务 ~107 轮落地) 
现有的 5 段 (1 字段 `_crc32_of_string` IEEE CRC32 (poly 0xEDB88320 / init 0xFFFFFFFF / xorout 0xFFFFFFFF) + 1 方法 
`_verify_and_unwrap` 校验 + 1 巡检 `audit_save_slots` 6 字段 巡检 (empty / ok / corrupted / drift + ids) + 1 dict `save_dict` 
包装层 `{ "data": ..., "checksum": <crc32> }` + 1 dict `loaded_data` 解包层 `_read_json` 走 `_verify_and_unwrap` 镜像), 在反复 
polish (T128 #109 → T224 #146 跨 2 任务 ~107 轮落地) 期间出现 4 类典型 fragile: (1) **"加新 1 字段 漏 5 段"** — 加新 1 字段 (e.g. 假设未来加 
`_sha256_of_string` 升级 SHA-256 算法) 落地时漏 5 段 (e.g. 漏 Stage 1 crc32 字段 中 `_sha256_of_string` 字段, 或漏 Stage 2 verify 方法 中 
`_sha256_of_string` 引用, 或漏 Stage 4 write dict 中 checksum 字段改 `_sha256_of_string` 调用, 或漏 Stage 5 read dict 中 verify 路径改 
`_sha256_of_string` 验证), 1 字段扩展 0 5 段 0 闭环 0 漂动 (1 字段 0 crc32 算法 = 1 字段 0 checksum 0 漂动; 1 字段 0 verify = 1 字段 0 校验 0 漂动;
 1 字段 0 write = 1 字段 0 包装 0 漂动; 1 字段 0 read = 1 字段 0 解包 0 漂动); (2) **"加新 1 方法 漏 1 边"** — 加新 1 方法 (e.g. 假设未来加 
`_sign_with_hmac` 加密签名) 落地时漏 1 边 (e.g. 漏 Stage 2 verify 方法 中加新 1 边 `if not _sign_with_hmac(...): return {}` 1:1 严格 0 改 
0 漏 0 反序 0 反向, 或漏 Stage 4 write dict 中加新 1 边 `"signature": _sign_with_hmac(...)` 1:1 严格 0 改 0 漏 0 反序 0 反向), 1 方法扩展 0 5 
段 0 闭环 0 漂动 (1 方法 0 verify = 1 方法 0 校验 0 漂动; 1 方法 0 write = 1 方法 0 包装 0 漂动); (3) **"修 1 字段 / 1 方法 0 触碰既有 4 字段 / 4 方法"** 
— 修 1 crc32 字段 (e.g. 改 `_crc32_of_string` poly 0xEDB88320 → 0x82F63B78 不同多项式) 或修 1 方法 (e.g. 改 `_verify_and_unwrap` 0 改 
`if expected != actual` 守卫) 时改硬编码 0 改 既有 4 字段 / 4 方法 (5 段任何 1 字段 / 5 段任何 1 方法), 跨字段 1:1 漂动 (1 修 1 字段 0 改既有 4 字段 = 1 字段 
0 5 字段 0 闭环 0 漂动; 1 修 1 方法 0 改既有 4 方法 = 1 方法 0 5 方法 0 闭环 0 漂动); (4) **"加新 1 巡检 0 触碰既有 4 段"** — 加新 1 巡检 (e.g. 假设未来加 
`audit_cloud_saves` 云存档巡检) 落地时漏 5 段 (e.g. 漏 Stage 3 audit 巡检 中 1 巡检 0 1 巡检 镜像, 1 巡检 0 5 段 0 闭环 0 漂动; 或漏 Stage 5 read 
dict 中云存档路径 0 镜像 `_read_json` 1 边). 4 类症状根因: SaveSystem save data CRC32 校验 5 段 1:1 序列 0 文档化, 任何新 1 字段 / 1 方法 / 1 巡检 / 1 
dict 接入只能"凭经验" 1:1 镜像 5 段 (1 crc32 字段 1:1 严格 + 1 verify 方法 1:1 严格 + 1 audit 巡检 1:1 严格 + 1 write dict 1:1 严格 + 1 read 
dict 1:1 严格), 1 漏 1 段 = 1 字段 / 1 方法 / 1 巡检 / 1 dict 0 5 段 0 闭环 0 漂动 (1 漏 1 段 0 5 段 0 1:1 严格分离契约 0 漂动 / 1 漏 1 边 0 字段引用 0 
1:1 严格 0 漂动).

- **触发场景**: 加新 1 字段 / 1 方法 / 1 巡检 (T128 #109 模式参考, 假设未来加 `_sha256_of_string`) 时**必须**严格按 5 段 1:1 复制 T128 落地模式: (1) 
**Stage 1 crc32 字段 1:1 严格** — 在 `save_system.gd` 底部 字段方法声明 区 加 1 entry (e.g. T128 落地时 1 字段 `_crc32_of_string` 1:1 严格 
(IEEE CRC32 poly 0xEDB88320 / init 0xFFFFFFFF / xorout 0xFFFFFFFF, 与 zlib/PNG/zip 跨工具可验证, 表驱动 256 项 每次调用重建, 32*4 = 128 
字节, 节省静态字段初始化成本 每次 save 调用开销 < 100μs), 假设未来加 `sha256` 字段, 底部 加 `func _sha256_of_string(s: String) -> int` 1 字段, 与既有 1 
字段 0 漏 0 改 0 反序 0 反向, 0 触碰既有 1 字段任何 1 字符, 0 漏 1 字段, 0 触碰既有 5 段), source-grep 验证 `save_system.gd` 字段声明 1 字段 → 2 字段 0 漏 0 
改 0 漏 1 entry 0 反向; (2) **Stage 2 verify 方法 1:1 严格** — 在 `save_system.gd` `_verify_and_unwrap(payload, path)` 方法 镜像 
(e.g. T128 落地时 `_verify_and_unwrap` 1:1 严格 (verify checksum 走 `_crc32_of_string` + `_normalize_int_floats` 抵消 Godot 4 
JSON.parse_string int→float 副作用, mismatch → push_warning + return {}), 假设未来加 `_sign_with_hmac` 方法, 0 触碰既有 
`_verify_and_unwrap` 任何 1 字符, 加新 1 边 `_sign_with_hmac` 1:1 严格 0 漏 0 改 0 反向), source-grep 验证 1 方法 → 2 方法 0 漏 0 改 0 漏 1 
方法 0 反向; (3) **Stage 3 audit 巡检 1:1 严格** — 在 `save_system.gd` `audit_save_slots()` 函数 加 1 巡检 (e.g. T224 #146 落地时 
`audit_save_slots` 1:1 严格 (6 字段: empty / ok / corrupted / drift + corrupted_ids / drift_ids, 5 slot 巡检, ok 通过 
_read_json + 3 required fields check, drift 走 _apply_snapshot 修复不删档, corrupted → push_warning 显示 "X ok / Y corrupted / 
Z drift / W empty")), 假设未来加 `audit_cloud_saves` 巡检, 0 触碰既有 `audit_save_slots` 任何 1 字符, 加新 1 边 `cloud_saves` 巡检 1:1 严格 0 
漏 0 改 0 反向), source-grep 验证 `audit_save_slots` 1 巡检 → 2 巡检 0 漏 0 改 0 漏 1 巡检 0 反向; (4) **Stage 4 write dict 1:1 严格** — 在 
`save_system.gd` `_write_json(path, data)` 函数 包装层 加 1 dict (e.g. T128 落地时 `save_dict` 1:1 严格 (`{ "data": data, 
SAVE_CHECKSUM_KEY: _crc32_of_string(JSON.stringify(data, "  ")) }`, JSON.stringify `"  "` 2-space canonical 保证读端 
byte-by-byte 还原), 0 漏 1 dict 0 改 1 dict 0 反序 0 反向), source-grep 验证 `save_dict` 1 dict 0 漏 0 改 0 漏 1 字符 0 反向; (5) 
**Stage 5 read dict 1:1 严格** — 在 `save_system.gd` `_read_json(path)` 函数 解包层 加 1 dict (e.g. T128 落地时 `loaded_data` 1:1 
严格 (走 `_verify_and_unwrap` 镜像 `save_dict`, 旧格式无 SAVE_CHECKSUM_KEY 顶层字段走 legacy 兼容路径 返回整个 dict 当作 data 下次 save_to_slot 
自动重写), 0 漏 1 dict 0 改 1 dict 0 反序 0 反向), source-grep 验证 `loaded_data` 1 dict 0 漏 0 改 0 漏 1 字符 0 反向. 5 段 1:1 同步, 1 漏 1 段 
/ 1 漏 1 边 = 1 字段 / 1 方法 / 1 巡检 / 1 dict 0 5 段 0 闭环 0 漂动. T128 #109 0 触碰既有 0 改 `_crc32_of_string` / `_verify_and_unwrap` 
/ `_write_json` 包装 / `_read_json` 解包 任何 1 字符 (0 改 crc32 字段 任何 1 字段, 0 改 verify 方法 任何 1 字符, 0 改 audit 巡检 任何 1 字符, 0 改 
write dict 任何 1 字符, 0 改 read dict 任何 1 字符).

- **修复** (跨 1 套 polish 模式 T293 #217 落地后已知 5 段 0 例外):
  - **T293 #217 首个 SaveSystem save data CRC32 校验 5 段 接入 1:1 严格分离契约 落地 (`save_system.gd` 1 字段 `_crc32_of_string` + 1 方法 
`_verify_and_unwrap` + 1 巡检 `audit_save_slots` + 1 dict `save_dict` + 1 dict `loaded_data`)** — §9.6.37 段 是 1 套 polish 
模式首个 1:1 包含 5 段 (1 `save_system.gd` crc32 字段 1:1 严格 + 1 `save_system.gd` verify 方法 1:1 严格 + 1 `save_system.gd` audit 巡检 
1:1 严格 + 1 `save_system.gd` write dict 1:1 严格 + 1 `save_system.gd` read dict 1:1 严格) 的 SaveSystem save data CRC32 校验 
polish 模式, 5 段 1:1 严格分离 (1 crc32 字段 1:1 严格 0 漏 0 改 + 1 verify 方法 1:1 严格 0 漏 0 改 + 1 audit 巡检 1:1 严格 0 漏 0 改 + 1 write 
dict 1:1 严格 0 漏 0 改 + 1 read dict 1:1 严格 0 漏 0 改, 0 漏 1 段 0 改 1 字段 0 反序 0 反向), 0 触碰既有 (T293 0 触碰 §9.6.6 / §9.6.7 / 
§9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / 
§9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / 
§9.6.36 27 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / 
README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / 
REVIEW_LOG_ARCHIVE.md 内容).
  - **T293 #217 Stage 1 crc32 字段 1:1 严格分离契约 落地 (`save_system.gd` 1 字段 `_crc32_of_string` IEEE CRC32)** — §9.6.37 段 1:1 
显式 crc32 字段 (T293 落地时 `save_system.gd` 1 字段 `func _crc32_of_string(s: String) -> int` 1:1 严格 (Standard IEEE CRC32 poly 
0xEDB88320 / init 0xFFFFFFFF / xorout 0xFFFFFFFF, 与 zlib/PNG/zip 跨工具可验证, 表驱动 256 项 每次调用重建 32*4 = 128 字节 节省静态字段初始化成本 每次 
save 调用开销 < 100μs, to_utf8_buffer 字节流驱动), 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), crc32 字段 1:1 严格 (1 `save_system.gd` 1 字段 1:1 严格 
0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), 0 触碰既有 (T293 0 改 `save_system.gd` 
`_crc32_of_string` 字段任何 1 字符).
  - **T293 #217 Stage 2 verify 方法 1:1 严格分离契约 落地 (`save_system.gd` 1 方法 `_verify_and_unwrap`)** — §9.6.37 段 1:1 显式 
verify 方法 (T293 落地时 `save_system.gd` `func _verify_and_unwrap(payload: Dictionary, path: String) -> Dictionary` 1:1 严格 
(verify checksum 走 `_crc32_of_string(JSON.stringify(_normalize_int_floats(data_raw), "  "))`, expected != actual → 
push_warning "CRC32 mismatch (expected %d, got %d) — file corrupted, rejecting" + return {} 4 行, data_raw 不是 Dictionary 
→ push_warning + return {} 守卫, 抵消 Godot 4 JSON.parse_string int→float 副作用 通过 `_normalize_int_floats`), 0 漏 1 方法 0 改 1 
方法 0 反序 0 反向), verify 方法 1:1 严格 (1 1 方法 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 方法 0 改 1 字符 0 反序 0 反向), 
0 触碰既有 (T293 0 改 `save_system.gd` `_verify_and_unwrap` 任何 1 字符).
  - **T293 #217 Stage 3 audit 巡检 1:1 严格分离契约 落地 (`save_system.gd` 1 巡检 `audit_save_slots` 6 字段)** — §9.6.37 段 1:1 显式 
audit 巡检 (T293 落地时 `save_system.gd` `func audit_save_slots() -> Dictionary` 1:1 严格 (6 字段: empty / ok / corrupted / 
drift / corrupted_ids / drift_ids + total = SLOT_COUNT, 5 slot 巡检, ok 通过 _read_json + 3 required fields check 
(data.has("version") + gs.current_room != "" + meta.saved_at_unix > 0), drift 走 _apply_snapshot 修复不删档, corrupted → 
push_warning 显示 "Save audit: X ok / Y corrupted / Z drift / W empty" 在 Godot Output panel 启动时 + title screen + pause 
menu 按需刷新), 0 漏 1 巡检 0 改 1 巡检 0 反序 0 反向), audit 巡检 1:1 严格 (1 1 巡检 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 
1 巡检 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T293 0 改 `save_system.gd` `audit_save_slots` 任何 1 字符).
  - **T293 #217 Stage 4 write dict 1:1 严格分离契约 落地 (`save_system.gd` 1 dict `save_dict` `{ data, checksum }` 包装层)** — 
§9.6.37 段 1:1 显式 write dict (T293 落地时 `save_system.gd` `_write_json` 中 `var payload := { "data": data, 
SAVE_CHECKSUM_KEY: _crc32_of_string(JSON.stringify(data, "  ")) }` 1:1 严格 (JSON.stringify `"  "` 2-space canonical 保证读端 
byte-by-byte 还原, SAVE_CHECKSUM_KEY 顶层字段 + data dict 二元组结构, 0 漏 1 dict 0 改 1 dict 0 反序 0 反向), write dict 1:1 严格 (1 1 
dict 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 dict 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T293 0 改 
`save_system.gd` `save_dict` 任何 1 字符).
  - **T293 #217 Stage 5 read dict 1:1 严格分离契约 落地 (`save_system.gd` 1 dict `loaded_data` 解包层)** — §9.6.37 段 1:1 显式 read 
dict (T293 落地时 `save_system.gd` `_read_json` 中 `if parsed.has(SAVE_CHECKSUM_KEY): return _verify_and_unwrap(parsed, 
path) / return parsed` 1:1 严格 (新格式走 `_verify_and_unwrap` 镜像 `save_dict`, 旧格式无 SAVE_CHECKSUM_KEY 顶层字段走 legacy 兼容路径 返回整个 
dict 当作 data 下次 save_to_slot 自动重写, FileAccess.file_exists 缺失 + FileAccess.open null + JSON.parse_string null + parsed 
is not Dictionary 4 守卫 全部 return {} 6 行), 0 漏 1 dict 0 改 1 dict 0 反序 0 反向), read dict 1:1 严格 (1 1 dict 1:1 严格 0 漏 0 改 + 
1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 dict 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T293 0 改 `save_system.gd` `loaded_data` 任何 1 
字符).
  - 跨 1 套 polish 模式 T293 #217 落地后 0 漂动, §9.6.37 段 1:1 严格 (5 段 1:1 严格分离 + crc32 字段 1:1 严格 + verify 方法 1:1 严格 + audit 巡检 
1:1 严格 + write dict 1:1 严格 + read dict 1:1 严格) 100% 闭环, 5 段 1:1 严格分离 (1 Stage 1 crc32 字段 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 
2 verify 方法 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 3 audit 巡检 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 4 write dict 1:1 严格 0 漏 0 改 0 反序 + 1 
Stage 5 read dict 1:1 严格 0 漏 0 改 0 反序). 1 套 polish 模式 0 后续 主要 pre-existing 风险.

- **预防**:
  1. 任何 polish 期给 SaveSystem save data CRC32 加新 1 字段 (e.g. `_sha256_of_string` / `_blake3_of_string` / 
`_crc64_of_string` 升级算法) 或加新 1 方法 (e.g. `_sign_with_hmac` / `_verify_signature` 加密签名) 或加新 1 巡检 (e.g. 
`audit_cloud_saves` / `audit_replay_saves` 云存档 / 回放巡检) 或加新 1 dict (e.g. `version_migration_dict` / `cloud_sync_dict` 
存档版本迁移 / 云同步 dict) 任务**必须**严格按 5 段 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 改 crc32 字段 1:1 严格分离契约** (T293 落地后 
`save_system.gd` 1 字段 `_crc32_of_string` IEEE CRC32 已切 1:1 strict 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反序 0 反向, 改 1 字段 / 漏 1 字段 
= 1 字段 / 1 方法 / 1 巡检 / 1 dict 扩展 0 5 段 0 闭环 0 漂动), **Stage 2 0 改 verify 方法 1:1 严格分离契约** (T293 落地后 `save_system.gd` 
`_verify_and_unwrap(payload, path)` 已切 1:1 strict 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 0 反向, 改 1 字符 / 漏 1 字符 = 1 方法扩展 0 
verify 0 闭环 0 漂动), **Stage 3 0 改 audit 巡检 1:1 严格分离契约** (T293 落地后 `save_system.gd` `audit_save_slots` 6 字段 (empty / ok / 
corrupted / drift + corrupted_ids / drift_ids) 已切 1:1 strict 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 0 反向, 改 1 字符 / 漏 1 字符 = 1 
巡检扩展 0 复合 audit 0 闭环 0 漂动), **Stage 4 0 改 write dict 1:1 严格分离契约** (T293 落地后 `save_system.gd` `_write_json` 中 
`save_dict` `{ "data": data, SAVE_CHECKSUM_KEY: _crc32_of_string(...) }` 包装层 已切 1:1 strict 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 
反序 0 反向, 改 1 字符 / 漏 1 字符 = 1 dict 扩展 0 包装层 0 闭环 0 漂动), **Stage 5 0 改 read dict 1:1 严格分离契约** (T293 落地后 `save_system.gd` 
`_read_json` 中 `loaded_data` 解包层 已切 1:1 strict 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 0 反向, 改 1 字符 / 漏 1 字符 = 1 dict 扩展 0 解包层 0 
闭环 0 漂动). 任何"加新 1 字段"必须**先**按 Stage 1 crc32 字段 1:1 加 1 字段 (Stage 1 1:1 严格, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向, **0 漏 1 段**), 
**再**按 Stage 2 verify 方法 1:1 加 1 边 (Stage 2 1:1 严格, 0 漏 1 边 0 改 1 边 0 反序 0 反向), **再**按 Stage 3 audit 巡检 1:1 加 1 边 
(Stage 3 1:1 严格, 0 漏 1 边 0 改 1 边 0 反序 0 反向), **再**按 Stage 4 write dict 1:1 镜像 (Stage 4 1:1 严格, 0 漏 1 dict 0 改 1 字符 0 反序 
0 反向), **再**按 Stage 5 read dict 1:1 镜像 (Stage 5 1:1 严格, 0 漏 1 dict 0 改 1 字符 0 反序 0 反向).
  2. **5 段 0 触碰边界** — `save_system.gd` crc32 字段 + verify 方法 + audit 巡检 + write dict + read dict 任何 1 件 0 改 0 删 0 重排 (5 
段 0 触碰边界), 任何"修 1 件"polish 走 Stage 1 1:1 严格 (改 件既有 5 段时**必须**保持 5 段 0 改 0 漏 0 反向 0 反序 0 触碰既有 1 件); 任何"加新 1 边"polish 走 
Stage 2 1:1 严格 (改 边既有 verify 时**必须**保持 1 方法 0 改 0 漏 0 反向 0 反序 0 漏 1 边); 任何"加新 1 边"polish 走 Stage 3 1:1 严格 (改 边既有 audit 
巡检时**必须**保持 1 巡检 0 改 0 漏 0 反向 0 反序 0 漏 1 边); 任何"加新 1 dict"polish 走 Stage 4 1:1 严格 (改 dict 既有 save_dict 时**必须**保持 1 dict 
0 改 0 漏 0 反向 0 反序 0 漏 1 dict); 任何"加新 1 dict"polish 走 Stage 5 1:1 严格 (改 dict 既有 loaded_data 时**必须**保持 1 dict 0 改 0 漏 0 
反向 0 反序 0 漏 1 dict).
  3. **0 改 1 字段 0 漏 1 字段 0 反向** (T293 落地后 0 触碰边界) — SaveSystem save data CRC32 5 段 (1 `save_system.gd` 1 字段 
`_crc32_of_string` + 1 `save_system.gd` 1 方法 `_verify_and_unwrap` + 1 `save_system.gd` 1 巡检 `audit_save_slots` + 1 
`save_system.gd` 1 dict `save_dict` + 1 `save_system.gd` 1 dict `loaded_data`) 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反向, 
改 1 字段 / 漏 1 字段 / 反向 1 字段 = 1 字段 / 1 方法 / 1 巡检 / 1 dict 扩展 0 5 段 0 漂动, source-grep 验证 5 段 0 漏 0 改 1 字段 0 漏 1 字段 0 改 1 
字符 0 反向.
  4. **0 改 1 边 0 漏 1 边 0 反向** (T293 落地后 0 触碰边界) — 接入契约 (1 crc32 字段 1:1 严格 + 1 verify 方法 1:1 严格 + 1 audit 巡检 1:1 严格 + 1 
write dict 1:1 严格 + 1 read dict 1:1 严格) 1:1 严格分离 0 漏 0 改 0 改 1 边 0 漏 1 边 0 反向, 改 1 边 / 漏 1 边 / 反向 1 边 = 1 字段 / 1 方法 / 1 
巡检 / 1 dict 扩展 0 接入契约 0 漂动, source-grep 验证 5 段 0 触碰接入契约 0 漏 0 改 1 边 0 漏 1 边 0 改 1 字符 0 反向.
  5. **T162 brittle 修复流程 0 触碰边界** — SaveSystem save data CRC32 5 段 0 副作用 子段 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 改 1 字符 0 反向 0 漏 
1 字段 0 触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构 (T162 brittle 
修复流程 = 1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), 改 1 字段 / 漏 
1 字段 / 反向 1 字段 = 1 字段 / 1 方法 / 1 巡检 / 1 dict 扩展 0 0 副作用 0 漂动 0 闭环, source-grep 验证 SaveSystem save data CRC32 5 段 0 副作用 
子段 0 漏 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 
段内容 0 触碰既有 0 改 1 段结构.
  6. **1 套 polish 模式 0 互混 0 复用 0 共享 唯一性 标注 0 改 1 段 0 漏 1 段 0 反向** — §9.6.37 段 唯一性 标注 1:1 严格分离 0 漏 0 改 0 改 1 套 polish 模式 
0 漏 1 套 polish 模式 0 反向 0 触碰既有 0 改 1 段 (本节是 28 套 polish 模式**唯一**关注 SaveSystem save data CRC32 校验 5 段 接入 1:1 严格分离契约 的 1 套,
 0 互混 0 复用 0 共享), 改 1 套 polish 模式 / 漏 1 套 polish 模式 / 反向 1 套 polish 模式 = 1 §9.6.37 段 0 唯一性 标注 0 漂动 0 闭环, source-grep 验证 
§9.6.37 段 唯一性 标注 0 漏 0 改 1 套 polish 模式 0 漏 1 套 polish 模式 0 改 1 字符 0 反向.
  7. **§9.6.37 是 28 套 polish 模式**唯一**关注 "SaveSystem save data CRC32 校验 5 段 接入 1:1 严格分离契约 (`save_system.gd` 1 字段 
`_crc32_of_string` IEEE CRC32 (poly 0xEDB88320 / init 0xFFFFFFFF / xorout 0xFFFFFFFF) + 1 方法 `_verify_and_unwrap` 校验 + 
1 巡检 `audit_save_slots` 6 字段 (empty / ok / corrupted / drift + corrupted_ids / drift_ids) + 1 dict `save_dict` 包装层 `{ 
"data": ..., "checksum": <crc32> }` + 1 dict `loaded_data` 解包层 `_read_json` 走 `_verify_and_unwrap` 镜像) + crc32 字段 1:1 
严格 (1 字段 1:1 严格 0 漏 0 改) + verify 方法 1:1 严格 (1 方法 1:1 严格 0 漏 0 改) + audit 巡检 1:1 严格 (1 巡检 1:1 严格 0 漏 0 改) + write dict 
1:1 严格 (1 dict 1:1 严格 0 漏 0 改) + read dict 1:1 严格 (1 dict 1:1 严格 0 漏 0 改) + 0 触碰游戏代码 + 0 触碰 §10 决策记录流程 0 触碰既有" 的 1 套** 
— 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
§9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / 
§9.6.34 / §9.6.35 / §9.6.36 27 套 polish 模式 0 互混 0 复用 0 共享. §9.6.15 (T271 #190 落地) 关注 "SaveSystem audit_save_slots 6 字段 
巡检" (与 SaveSystem save data CRC32 5 段 0 相关, 1 巡检 0 涉及 5 段**之间**接入契约 自身 1:1 严格分离, §9.6.15 仅覆盖 audit_save_slots 单件, 
§9.6.37 覆盖 5 段**之间**接入契约), §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.33 5 套关注 "6 verb 接入 单层 / 跨层 字节码 (6 verb hit 
handler 5 件套 / 6 verb signal 桥接 5+1 件套 / 16 件套 verb ability base / 7 件套 windup VFX base / 6 verb 接入 4 件套 字节码一致性)" (与 
SaveSystem save data CRC32 5 段 0 相关, 5 套 6 verb 字节码 0 涉及 SaveSystem save data CRC32 校验 5 段 自身 1:1 严格分离), §9.6.20 / 
§9.6.21 / §9.6.25 / §9.6.26 / §9.6.27 5 套关注 "6 verb 跨组件 / 跨房间 / 跨段 视觉组" (与 SaveSystem save data CRC32 5 段 0 相关, 5 套跨组件 
0 涉及 5 段**之间**接入契约 自身 1:1 严格分离), §9.6.22 / §9.6.23 / §9.6.24 3 套关注 "6 verb VFX / prewarm / HUD" (与 SaveSystem save data 
CRC32 5 段 0 相关, 3 套 6 verb 0 涉及 SaveSystem save data CRC32 校验 5 段 自身 1:1 严格分离), §9.6.28 / §9.6.29 / §9.6.30 3 套关注 "工具链 
/ smoke test / docs polish" (与 SaveSystem save data CRC32 5 段 0 相关, 3 套工具链 0 涉及 5 段**之间**接入契约 自身 1:1 严格分离), §9.6.31 / 
§9.6.32 2 套关注 "文档归档 3 件套 1:1 严格分离契约" (与 SaveSystem save data CRC32 5 段 0 相关, 2 套文档归档 0 涉及 5 段**之间**接入契约 自身 1:1 严格分离), 
§9.6.34 / §9.6.35 2 套关注 "数据层字段扩展 6 段 / 5 段 canonical 1:1 严格分离契约 (PlayerStats 持久化层字段扩展 6 段 / audio 数据层 preset 字段扩展 5 段)" 
(与 SaveSystem save data CRC32 5 段 0 相关, 2 套数据层字段扩展 0 涉及 SaveSystem save data CRC32 校验 5 段 自身 1:1 严格分离, §9.6.34 关注 
PlayerStats 字段 6 段, §9.6.35 关注 audio preset 字段 5 段, §9.6.37 关注 SaveSystem save data CRC32 校验 5 段 — 3 套数据层 polish 模式 0 
互混 0 复用 0 共享), §9.6.36 关注 "PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约" (与 SaveSystem save data CRC32 5 段 0 相关, 4 件套 
1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 4 件套 = 4 元素 1:1 严格, §9.6.37 关注 SaveSystem save data CRC32 5 段 = 1 
套 polish 模式 × 5 段 = 5 元素 1:1 严格 — 2 套 polish 模式 0 互混 0 复用 0 共享). §9.6.37 关注 "SaveSystem save data CRC32 校验 5 段 接入 1:1 
严格分离契约 5 段 1:1 视觉组连贯" (与 6 verb 单层 / 6 verb 跨组件 / 工具链 / 文档归档 / 数据层字段扩展 / PlayerActionGate 单 autoload 4 件套 polish 0 相关, 
5 段 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格). 任何"加新 1 字段"或"加新 1 方法"或"加新 1 巡检"或"加新 1 
dict"必须**先**按 §9.6.37 Stage 1 crc32 字段 1:1 严格 加 1 字段 (Stage 1 1:1 严格, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向, **0 漏 1 段**), **再**按 
§9.6.37 Stage 2 verify 方法 1:1 严格 加 1 边 (Stage 2 1:1 严格, 0 漏 1 边 0 改 1 边 0 反序 0 反向), **再**按 §9.6.37 Stage 3 audit 巡检 1:1 
严格 加 1 边 (Stage 3 1:1 严格, 0 漏 1 边 0 改 1 边 0 反序 0 反向), **再**按 §9.6.37 Stage 4 write dict 1:1 严格 镜像 (Stage 4 1:1 严格, 0 漏 
1 dict 0 改 1 字符 0 反序 0 反向), **再**按 §9.6.37 Stage 5 read dict 1:1 严格 镜像 (Stage 5 1:1 严格, 0 漏 1 dict 0 改 1 字符 0 反序 0 反向).
  8. 已知 drift risk: SaveSystem save data CRC32 校验 5 段 接入 **可能**漏 1 段 / 1 边 1:1 镜像 (e.g. 加新 1 字段 时漏 Stage 1 crc32 字段 中 1 
字段 0 镜像, 1 字段 0 5 段 0 闭环; 加新 1 字段 时漏 Stage 2 verify 中 0 镜像, 1 字段 0 verify 0 闭环; 加新 1 字段 时漏 Stage 3 audit 巡检 中 0 镜像, 1 
字段 0 audit 0 闭环; 加新 1 字段 时漏 Stage 4 write dict 中 0 镜像, 1 字段 0 包装 0 闭环; 加新 1 字段 时漏 Stage 5 read dict 中 0 镜像, 1 字段 0 解包 0 
闭环). 监控建议: 任何"未来给 SaveSystem save data CRC32 加新 1 字段 / 加新 1 方法 / 加新 1 巡检 / 加新 1 dict"必须**先**在 5 段 (Stage 1 crc32 字段 1:1 
严格 + Stage 2 verify 方法 1:1 严格 + Stage 3 audit 巡检 1:1 严格 + Stage 4 write dict 1:1 严格 + Stage 5 read dict 1:1 严格) 1:1 镜像 
0 漏 1 段 / 0 漏 1 边 / 0 漏 1 字段 / **0 漏 1 巡检**. T293 (#217) 5 段 1:1 镜像落地后任何新字段 / 新方法 / 新巡检 / 新 dict 是 §9.6.37 5 段的"母集" (1 
字段 / 1 方法 / 1 巡检 / 1 dict 扩展 × 5 段 = 5 元素 1:1 严格, §9.6.37 5 段 1 套 polish 模式 完整闭环, 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格, 含 
Stage 1 crc32 字段 1:1 严格 1 套 polish 模式 5 段 0 漏 0 改 + Stage 2 verify 方法 1:1 严格 1 套 polish 模式 verify 0 漏 0 改 + Stage 3 
audit 巡检 1:1 严格 1 套 polish 模式 audit 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 件 + Stage 4 write dict 1:1 严格 1 套 polish 模式 
save_dict 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 字符 + Stage 5 read dict 1:1 严格 1 套 polish 模式 loaded_data 0 漏 0 改 0 反向 0 触碰既有 
0 触碰既有 0 改 1 字符). 另 1 类已知 drift: Stage 1 crc32 字段 1:1 严格 0 改 1 字段 0 漏 1 字段 0 反向 (e.g. 后续 polish 期加新 1 字段时漏 0 镜像 1 字段 任何 
1 字段, 1 字段 0 crc32 字段 0 闭环; 或漏 0 镜像 1 方法 任何 1 方法, 1 字段 0 verify 0 闭环; 或改 1 字段 0 改 1 字段 0 反向 0 触碰既有), 后续 polish 期若加新 1 
字段必须**先**用 1:1 镜像 5 段 模板, 0 改既有 0 漏 1 段 0 漏 1 边 0 反向 **0 漏 1 字段**.

**0 副作用**: T293 (#217) 任何 const / var 0 触碰 (`save_system.gd` 0 改 0 删 0 重排 `_crc32_of_string` 字段任何 1 字符; 
`save_system.gd` 0 改 0 删 0 重排 `_verify_and_unwrap` 方法任何 1 字符; `save_system.gd` 0 改 0 删 0 重排 `audit_save_slots` 巡检任何 1 
字符; `save_system.gd` 0 改 0 删 0 重排 `save_dict` 包装 dict 任何 1 字符; `save_system.gd` 0 改 0 删 0 重排 `loaded_data` 解包 dict 任何 1 
字符; CONTRIBUTING.md §9.6.37 段 0 改 0 删 0 重排, 仅新加 1 段 (~50 行)); 0 触碰 §9.6.6 ProfileRecentList 5 行 row 文本 5→7 字段 format 
字符串扩展模式 (T293 0 改 §9.6.6 任何 1 字符); 0 触碰 §9.6.7 ProfileRecentList 5 行 row hover feedback 三件套 (T293 0 改 §9.6.7 任何 1 字符); 
0 触碰 §9.6.8 ProfileQuickStats 4 段独立 hover 联动 (T293 0 改 §9.6.8 任何 1 字符); 0 触碰 §9.6.9 AchievementGrid + ProfileRecentList 
跨面板 `_alpha_base` Dictionary 双源 (T293 0 改 §9.6.9 任何 1 字符); 0 触碰 §9.6.10 「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (T293 0 
改 §9.6.10 任何 1 字符); 0 触碰 §9.6.15 SaveSystem audit_save_slots (T293 0 改 §9.6.15 任何 1 字符); 0 触碰 §9.6.16 6 verb hit 
handler 5 件套 (T293 0 改 §9.6.16 任何 1 字符); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T293 0 改 §9.6.17 任何 1 字符); 0 触碰 §9.6.18 
16 件套 verb ability base (T293 0 改 §9.6.18 任何 1 字符); 0 触碰 §9.6.19 7 件套 windup VFX base (T293 0 改 §9.6.19 任何 1 字符); 0 触碰 
§9.6.20 Hub ↔ archive 跨房间 transition flow (T293 0 改 §9.6.20 任何 1 字符); 0 触碰 §9.6.21 8 段成就序列 (T293 0 改 §9.6.21 任何 1 字符); 
0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T293 0 改 §9.6.22 任何 1 字符); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T293 0 改 §9.6.23 任何 1 字符); 
0 触碰 §9.6.24 HUD 6 verb × 6 通道 = 36 通道 5 段序列 (T293 0 改 §9.6.24 任何 1 字符); 0 触碰 §9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 5 段序列 
(T293 0 改 §9.6.25 任何 1 字符); 0 触碰 §9.6.26 6 verb icon 视觉组 5 段 canonical 1:1 序列 (T293 0 改 §9.6.26 任何 1 字符); 0 触碰 §9.6.27 
PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 5 段序列 (T293 0 改 §9.6.27 任何 1 字符); 0 触碰 §9.6.28 smoke test ITERATION_COUNT 
跨迭代 + 段边界 find 5 段序列 (T293 0 改 §9.6.28 任何 1 字符); 0 触碰 §9.6.29 polish 文档化 5 段 canonical 1:1 序列 模板 + 4 段关系段 模板 (T293 0 改 
§9.6.29 任何 1 字符); 0 触碰 §9.6.30 工具链 3 件套 1:1 严格分离契约 (T293 0 改 §9.6.30 任何 1 字符); 0 触碰 §9.6.31 CHANGELOG 归档 3 件套 1:1 
严格分离契约 (T293 0 改 §9.6.31 任何 1 字符); 0 触碰 §9.6.32 REVIEW_LOG 归档 3 件套 1:1 严格分离契约 (T293 0 改 §9.6.32 任何 1 字符); 0 触碰 §9.6.33 
6 verb 接入 4 件套 字节码一致性 (T293 0 改 §9.6.33 任何 1 字符); 0 触碰 §9.6.34 PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约 (T293 0 改 
§9.6.34 任何 1 字符); 0 触碰 §9.6.35 AudioPresets `MUSIC_PRESETS` preset 字段扩展 5 段 canonical 1:1 严格分离契约 (T293 0 改 §9.6.35 任何 1 
字符); 0 触碰 §9.6.36 PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约 (T293 0 改 §9.6.36 任何 1 字符); 0 触碰 
check_smoke_consistency.sh (T293 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh 
(T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, 0 触碰 `save_system.gd` 
任何 1 字段 / 1 方法 / 1 巡检 / 1 dict, 0 触碰 `player_action_gate.gd` 任何 1 字段); 0 行为变化, 1 文档 1 段 (§9.6.37 ~50 行) + 0 字节码修改 (T293 
仅文档化既有 SaveSystem save data CRC32 校验 5 段 接入 1:1 严格分离契约, 0 改 `_crc32_of_string` 字段 0 改 `_verify_and_unwrap` 方法 0 改 
`audit_save_slots` 巡检 0 改 `save_dict` 包装 dict 0 改 `loaded_data` 解包 dict 0 触碰既有) + 1 smoke test (T293 30+ 断言).

**与 §9.6.36 关系** — §9.6.36 (T292 #216 落地) 关注 "PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约" (4 件套 = 1 autoload 字段 + 2 
register / unregister 方法 + 3 is_blocked() probe + 1 get_player() helper, 4 件套覆盖 PlayerActionGate autoload 4 件套 接入 0 涉及 
SaveSystem save data CRC32 校验 5 段). §9.6.37 关注 "SaveSystem save data CRC32 校验 5 段 接入 1:1 严格分离契约" (1 crc32 字段 + 1 verify 
方法 + 1 audit 巡检 + 1 write dict + 1 read dict, 5 段覆盖 SaveSystem save data CRC32 校验 5 段 0 涉及 PlayerActionGate 单 autoload 
4 件套). §9.6.36 与 §9.6.37 0 互混 0 复用 0 共享: §9.6.36 关注 "PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约" (与 SaveSystem save 
data CRC32 5 段 0 相关, 4 件套 0 涉及 crc32 / verify / audit / save_dict / loaded_data 5 段), §9.6.37 关注 "SaveSystem save data 
CRC32 校验 5 段 接入 1:1 严格分离契约 5 段 1:1 视觉组连贯" (与 PlayerActionGate 单 autoload 4 件套 0 相关, 5 段 1:1 严格 镜像 1 套 polish 模式 既有 1:1 
严格分离, 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格). §9.6.37 与 §9.6.36 是 "姊妹段" (1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 镜像 1 套 polish 模式 
× 4 件套 = 4 元素 1:1 严格, 一是 SaveSystem save data CRC32 校验 5 段 接入, 一是 PlayerActionGate 单 autoload 4 件套 接入; 1 套 polish 模式 × 
5 段 = 5 元素 1:1 严格 + 1 套 polish 模式 × 4 件套 = 4 元素 1:1 严格). 任何"加新 1 字段"必须**先**按 §9.6.37 5 段 1:1 复制 (5 段 1:1 严格, 0 漏 1 段 0 
改 1 字段 0 反向) **或** 按 §9.6.36 4 件套 1:1 复制 (4 件套 1:1 严格, 0 漏 1 件套 0 改 1 字段 0 反向), **再**按 §9.6.37 5 段 1:1 同步 (5 段 1:1 严格, 
0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.36 任何 1 字符) **或** 按 §9.6.36 4 件套 1:1 同步 (4 件套 1:1 严格, 0 漏 1 件套 0 漏 1 
边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.37 任何 1 字符).

**与 §9.6.34 关系** — §9.6.34 (T127-T209 #128 落地) 关注 "PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约" (6 段 = `_best_stats` 
+ `_run_history` 字段 6 段 canonical, 6 段覆盖 PlayerStats 持久化层 0 涉及 SaveSystem save data CRC32 校验 5 段). §9.6.37 关注 
"SaveSystem save data CRC32 校验 5 段 接入 1:1 严格分离契约" (5 段覆盖 SaveSystem save data CRC32 校验 5 段 0 涉及 PlayerStats 持久化层 字段 6 
段). §9.6.34 与 §9.6.37 0 互混 0 复用 0 共享: §9.6.34 关注 "PlayerStats 持久化层字段扩展 6 段" (与 SaveSystem save data CRC32 5 段 0 相关, 6 段 
0 涉及 crc32 / verify / audit / save_dict / loaded_data 5 段), §9.6.37 关注 "SaveSystem save data CRC32 校验 5 段 接入 1:1 严格分离契约 
5 段 1:1 视觉组连贯" (与 PlayerStats 持久化层 字段 6 段 0 相关, 5 段 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 5 段 = 5 元素 1:1 
严格). 2 套 polish 模式 是 "姊妹段" (1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 镜像 1 套 polish 模式 × 6 段 = 6 元素 1:1 严格, 一是 SaveSystem save 
data CRC32 校验 5 段 接入, 一是 PlayerStats 持久化层字段扩展 6 段; 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 + 1 套 polish 模式 × 6 段 = 6 元素 1:1 
严格). 任何"加新 1 字段"必须**先**按 §9.6.37 5 段 1:1 复制 (5 段 1:1 严格, 0 漏 1 段 0 改 1 字段 0 反向) **或** 按 §9.6.34 6 段 1:1 复制 (6 段 1:1 严格, 
0 漏 1 段 0 改 1 字段 0 反向), **再**按 §9.6.37 5 段 1:1 同步 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.34 任何 
1 字符) **或** 按 §9.6.34 6 段 1:1 同步 (6 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.37 任何 1 字符).

**与 §9.6.35 关系** — §9.6.35 (T062-T118 #63 落地) 关注 "AudioPresets `MUSIC_PRESETS` preset 字段扩展 5 段 canonical 1:1 严格分离契约" (5 
段 = preset 字段 5 段 canonical, 5 段覆盖 audio 数据层 0 涉及 SaveSystem save data CRC32 校验 5 段). §9.6.37 关注 "SaveSystem save data 
CRC32 校验 5 段 接入 1:1 严格分离契约" (5 段覆盖 SaveSystem save data CRC32 校验 5 段 0 涉及 audio 数据层 preset 字段 5 段). §9.6.35 与 §9.6.37 0 
互混 0 复用 0 共享: §9.6.35 关注 "audio 数据层 preset 字段扩展 5 段" (与 SaveSystem save data CRC32 5 段 0 相关, 5 段 0 涉及 crc32 / verify / 
audit / save_dict / loaded_data 5 段), §9.6.37 关注 "SaveSystem save data CRC32 校验 5 段 接入 1:1 严格分离契约 5 段 1:1 视觉组连贯" (与 
audio 数据层 preset 字段 5 段 0 相关, 5 段 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格). 2 套 polish 
模式 是 "姊妹段" (1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 镜像 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格, 一是 SaveSystem save data CRC32 校验 5 
段 接入, 一是 audio 数据层 preset 字段扩展 5 段; 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 + 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格). 任何"加新 1 
字段"必须**先**按 §9.6.37 5 段 1:1 复制 (5 段 1:1 严格, 0 漏 1 段 0 改 1 字段 0 反向) **或** 按 §9.6.35 5 段 1:1 复制 (5 段 1:1 严格, 0 漏 1 段 0 改 
1 字段 0 反向), **再**按 §9.6.37 5 段 1:1 同步 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.35 任何 1 字符) **或** 
按 §9.6.35 5 段 1:1 同步 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.37 任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 
0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 轮 0 后续 持续收敛), §9.6.37 是"SaveSystem save data CRC32 校验 5 段 接入 1:1 严格分离契约" (5 段 1:1 
严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约). T162 与 §9.6.37 0 互混 0 复用 0 共享: T162 关注 "brittle 修复流程 5 步骤" (1 expect 反转 + 1 
docblock 说明 + 1 段 find 反转 + 0 触碰既有 + 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 步骤 1:1 严格), §9.6.37 关注 "5 段 
1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约" (与 5 步骤 0 相关, 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离). 任何"加新 1 
brittle 修复"必须**先**按 T162 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), **再**按 §9.6.37 5 
段 1:1 镜像 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.37 5 段走 §9.1 9 步落地的 0 步 (SaveSystem save data CRC32 校验 5 段 接入 是 save_system 
持久化层 范畴, 0 走 9 步任何 1 步), 0 触碰 9 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` 
PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染). 任何"加新 1 字段"必须**先**按 §9.6.37 5 段 1:1 镜像 (5 段 1:1 严格, 0 漏 1 段 
0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 9 步 (`verb` 枚举 + `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG 
+ `vfx` 视觉组 + `audio` preset 字段 + `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 步"必须**先**在 §9.6.37 5 段 加 1 边 (1 crc32 字段 entry 或 1 
verify 方法 边 或 1 audit 巡检 边 或 1 write dict 边 或 1 read dict 边), 0 触碰既有 9 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 
1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).
