# Polish Patterns 9.6.13–15

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.13–9.6.15，共 3 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.13-15.md

<!-- shard 13-15 共 3 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.13 settings_menu `_save_settings` ↔ `_load_settings` 1:1 同步 + `user://settings.cfg` 持久化 4 section × N key 双源映射 
polish 模式 (T079 + T136 + T195 + T196 + T202.B + T205 + T239 跨 7 任务 ~15 轮落地) 文档化

> §9.6.11 记录 settings_menu ReduceAllCheck 三态 (`_syncing_from_master` 防递归 + `set_block_signals` 双层保险 + `_reduce_all` 
> 数据独立)；§9.6.12 记录 settings_menu 4 tab 状态机 (Tab 枚举 + 1 状态字段 + 4 tab Button @onready + 4 panel Control @onready + 4 
> signal connect + `_switch_tab` 7 步骤 + 初始渲染 1 行) 7 件套硬约束。本节记录 settings_menu 持久化层 —— `_save_settings` 写 
> `user://settings.cfg` ↔ `_load_settings` 从 `user://settings.cfg` 读回 1:1 双源映射 4 section × N key (14 + ACTION_NAMES) 
> polish 模式。T037 (#23) 落地 4 audio 滑块 + T079 (#67) 加 respawn 策略 + T136 (#77) 加 autosave 3 key (enabled / interval / 
> slot) + T195 (#112) 加 reduce_shake / reduce_flash 2 key + T196 (#113) 加 reduce_vibration 1 key + T202.B (#121) 加 
> reduce_all 1 key + T205 (#122) 加 audio 4 label 还原 (load 后立即刷新 label text) + T239 (#157) 加 music preview button (0 
> 触碰持久化层), 7 个任务跨 ~15 轮反复落地, 任何 polish 期给 settings_menu 加新 1 key (e.g. 玩家偏好 / UI 主题 / 字幕 / 难度 / 帧率上限) 或加新 1 section 
> (e.g. [ui] / [graphics] / [audio_advanced]) 时先查本节, 0 漏 1 处同步 (e.g. 漏 `_save_settings` 写 key 但 `_load_settings` 0 读 → 
> 玩家重启游戏后新 key 永远 0 还原, 永远是默认值, 视觉组"settings 持久化"语义破坏; 漏 `_load_settings` 读 key 但 `_save_settings` 0 写 → 玩家重启游戏后读到 
> stale 旧 key 值, 0 反映当前 cfg 状态; 漏 default 参数 → 玩家首次开 settings (0 cfg 文件) `get_value` 抛错, 走 else 兜底, 任何"new key 0 
> default"polish 都会让 fresh clone 0 启动 settings menu; 漏 4 section 顺序 (audio → video → gameplay → accessibility → input) 
> → 跨任务 polishing 期间 cfg 文件结构错位, 玩家从旧版升级到新版时 cfg 兼容 0 优雅; 漏 last-writer-wins 模式 (settings_menu vs SaveSystem 都写) → 玩家关 
> settings 时 autosave 段被 settings_menu 覆盖, 即使 SaveSystem 期间写过 1 次, 视觉组"settings 是 source of truth"语义破坏)。详细记录 4 段 
> (症状/触发场景/修复/预防)：

- **症状**：polish 期给 settings_menu 加新 1 key (e.g. "音量" 加 master_mute / "屏幕" 加 vsync / "玩法" 加难度 / "无障碍" 加字幕大小) 时, 最常见的 
fragile 是「(a) `_save_settings` 写 key 但 `_load_settings` 0 读 → 玩家重启后 key 永远 0 还原」— T079 (#67) 落地 respawn_to_hub 时显式用 
`cfg.set_value("gameplay", "respawn_to_hub", _respawn_to_hub)` + `cfg.get_value("gameplay", "respawn_to_hub", true)`, 
任何"加 key 但 0 同步读"polish 都会让玩家重启游戏后, 玩家在 settings 中勾选/调整的值 0 还原, 永远走 default, 视觉组"settings 持久化"语义破坏;「(b) 
`_load_settings` 读 key 但 `_save_settings` 0 写 → 玩家重启后读到 stale 旧 key 值」— T136 (#77) 落地 autosave 3 key (enabled / 
interval / slot) 时显式用 `_save_settings` 3 行 `cfg.set_value` + `_load_settings` 3 行 `cfg.get_value`, 任何"读 key 但 0 
同步写"polish 都会让玩家首次写 cfg 时 0 含新 key, 后续读时拿到 `get_value` 兜底 default, 但 0 反映玩家当前勾选状态, 视觉组"settings 持久化"语义破坏;「(c) 
`cfg.get_value` 0 传 default 参数 → fresh clone 启动 settings menu 抛错」— T079 落地 respawn_to_hub 时显式用 
`cfg.get_value("gameplay", "respawn_to_hub", true)` 传 default `true` (默认走 hub 安全区), 任何"漏 default"polish 都会让 Godot 4 
ConfigFile 报 `Key not found` 错误 (虽然不致命但 push_warning 噪音), 玩家首次开 settings 0 cfg 还能走 else 兜底, 但 0 优雅;「(d) 4 section 顺序错位 
(audio → video → gameplay → accessibility → input) → 跨任务 polishing 期间 cfg 文件结构错位」— T037 落地 2 tab (audio / video) 
时按视觉组顺序排 `[audio]` 在前 (4 slider 音量) + `[video]` 在中 (fullscreen / scale) + `[gameplay]` 居中 (respawn 策略) + 
`[accessibility]` 靠后 (4 reduce_*_check 视觉减弱) + `[input]` 末位 (按键重映射), 任何"重排 section 顺序"polish 都会让 cfg 文件结构错位, 
玩家从旧版升级到新版时 cfg 兼容 0 优雅, 读 key 时走 `cfg.get_value("section", "key", default)` section 名独立 0 受顺序影响但 0 优雅;「(e) 
last-writer-wins 模式 0 触碰 (settings_menu vs SaveSystem 都写 [gameplay] section) → 玩家关 settings 时 autosave 段被 settings_menu 
覆盖」— T136 落地时显式用 `cfg.set_value("gameplay", "autosave_enabled", _autosave_enabled_check.button_pressed)` (走控制 live 状态而非 
cfg 缓存) 而非 `cfg.set_value("gameplay", "autosave_enabled", _autosave_enabled)` 字段 (字段可能 stale), 任何"统一 
last-writer-wins"polish 都会让 SaveSystem 期间写的 autosave 段在 settings_menu 关时被覆盖, 视觉组"settings 是 source of truth"语义破坏。
- **触发场景**：T079 (#67) + T136 (#77) + T195 (#112) + T196 (#113) + T202.B (#121) + T205 (#122) + T239 (#157) 4 section × 
N key 1:1 双源映射落地需同步 5 件套：(1) `src/scripts/settings_menu.gd:1071-1119` `_save_settings()` 函数 (49 行) — 4 section × N key 
`cfg.set_value` 0 漏 1 顺序: (1.1) `[audio]` 4 key (master / sfx / music / ambience, T037 #23 落地, `_master_volume` / 
`_sfx_volume` / `_music_volume` / `_ambience_volume` 字段直接写, 0 走 `button_pressed`, 0 触碰 §9.6.12 4 tab 状态机); (1.2) 
`[video]` 2 key (fullscreen / window_scale, T037 落地, `_fullscreen` / `_window_scale` 字段直接写); (1.3) `[gameplay]` 4 key 
(respawn_to_hub / autosave_enabled / autosave_interval / autosave_slot, T079 + T136 落地, respawn_to_hub 字段直接写 + autosave 
3 key 走控件 `button_pressed` / `value` / `selected`); (1.4) `[accessibility]` 4 key (reduce_shake / reduce_flash / 
reduce_vibration / reduce_all, T195 + T196 + T202.B 落地, reduce_*_check 3 个 `button_pressed` + reduce_all 走 
`_reduce_all` 字段而非 indeterminate 显示); (1.5) `[input]` N key (ACTION_NAMES 字典 keys, T086 落地, `for action in 
ACTION_NAMES.keys()` 循环 + `InputMap.action_get_events(action)` 取第 1 个 event 存); (1.6) `cfg.save("user://settings.cfg")` 
1 行写盘 + push_warning 错误处理; (2) `src/scripts/settings_menu.gd:1121-1230` `_load_settings()` 函数 (~110 行) — 4 section × N 
key `cfg.get_value` 1:1 对应 `_save_settings` 0 漏 1: (2.1) `[audio]` 4 key `get_value` 0 漏 default (1.0 默认满音量) + 4 slider 
实时推 `_master_slider.value = _master_volume * 100.0` + T205 4 label `_refresh_audio_volume_label` 还原 text; (2.2) 
`[video]` 2 key `get_value` 0 漏 default (false / 4) + `_fullscreen_check.button_pressed` + 
`_scale_options.select(clampi(...))`; (2.3) `[gameplay]` 4 key `get_value` 0 漏 default (true / 各种) + 
`_populate_autosave_controls_from_cfg(cfg)` 1 行 helper (T136 落地) 推 3 autosave 控件 + live-push 到 SaveSystem autoload; 
(2.4) `[accessibility]` 4 key `get_value` 0 漏 default (false / 各种) + `set_block_signals(true/false)` 包裹 + 4 
reduce_*_check `button_pressed` 推 (T195 + T196 + T202.B 模式) + T202.B `_reduce_all` indeterminate 状态 `set_block_signals` 
+ `_syncing_from_master = true; _apply_three_children(true); _set_three_children_disabled(true); _syncing_from_master = 
false` 防递归; (2.5) `[input]` N key `get_value` 0 漏, 但 _save_settings 写的是 event 字典, _load_settings 通常 0 还原按键 (按键重映射是 
settings 的"导出"功能而非"导入"功能, 玩家重启后按 Godot 默认 InputMap 走); (2.6) `cfg.load("user://settings.cfg")` 1 行读盘 + 错误处理 `if err != 
OK: return` 早退; (3) `src/scripts/settings_menu.gd:265` `_ready` 末尾 `_load_settings()` 1 行初始加载 — 显式调一次, 玩家首次打开 settings 
时立即看到 4 section × N key 还原 (4 audio slider 位置 + 4 audio label text + 2 video toggle + 1 respawn toggle + 3 autosave 
toggle / slider / select + 4 reduce_*_check 状态), 视觉组"settings 持久化"100% 闭环; (4) `src/scripts/settings_menu.gd:316` 
`_on_close()` 末尾 `_save_settings()` 1 行持久化 — 玩家关 settings menu 立即写盘 (4 section × N key live 状态), 视觉组"关 settings 
即持久化"100% 闭环, 与 §9.6.12 _switch_tab 7 步骤 + §9.6.11 ReduceAllCheck 三态 0 互混; (5) `src/scripts/settings_menu.gd:1071-1119` 
+ `src/scripts/settings_menu.gd:1121-1230` `_save_settings` ↔ `_load_settings` 1:1 双源映射 4 section × N key —— 
`_save_settings` 写 N key, `_load_settings` 必须读 N key (0 漏 0 错位), 任何"加新 key 但漏 1 边"polish 都会让持久化双向断开 (写 0 读 或 读 0 写), 
视觉组"settings 持久化"语义破坏。5 件套 1:1 同步, 1 漏 1 = 玩家重启后新 key 0 还原 / 玩家首次启动 settings 抛错 / section 顺序错位 / last-writer-wins 模式破坏 
/ 初始加载 0 同步 / 关闭 menu 0 持久化。
- **修复**：`src/scripts/settings_menu.gd` `_save_settings` ↔ `_load_settings` 1:1 同步 + `user://settings.cfg` 持久化 4 
section × N key 双源映射 polish 模式 (T079 + T136 + T195 + T196 + T202.B + T205 + T239 跨 7 任务 ~15 轮落地)：
  - **T037 (#23) `_save_settings` + `_load_settings` 4 audio + 2 video 6 key 基础** — `_save_settings()` `[audio]` 4 key 
`cfg.set_value` (master / sfx / music / ambience) + `[video]` 2 key (fullscreen / window_scale) + `_load_settings()` 6 
key 1:1 `cfg.get_value` 0 漏 default (1.0 / false / 4) + 4 slider 推 `value` + 2 video 推 `button_pressed` / `select`。0 触碰 
§9.6.12 4 tab 状态机 (4 slider 是 `_audio_panel` 内子节点, 0 触碰 tab 状态机 7 件套); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T037 落地时 T202.B 
还没加, 0 触碰 reduce_*_check 任何字段)。
  - **T079 (#67) `[gameplay] respawn_to_hub` 1 key 加 gameplay section** — `_save_settings` 加 1 行 
`cfg.set_value("gameplay", "respawn_to_hub", _respawn_to_hub)` + `_load_settings` 加 1 行 `cfg.get_value("gameplay", 
"respawn_to_hub", true)` (默认 `true` = 重生到 hub 安全区, 玩家最常见选择) + `_respawn_hub_check.button_pressed = _respawn_to_hub` + 
`GameState.set_respawn_to_hub(_respawn_to_hub)` 跨 autoload live-push (玩家关 settings 后 GameState 立即遵守, 0 需重启)。0 触碰 T037 4 
audio + 2 video 既有 6 key (T079 0 改 [audio] / [video] section 任何 key, 仅 [gameplay] 扩 1 key)。
  - **T136 (#77) `[gameplay] autosave 3 key` (enabled / interval / slot) 加 SaveSystem autoload 联动** — `_save_settings` 
3 行 `cfg.set_value` 走控件 `button_pressed` / `value` / `selected` (last-writer-wins 模式: settings_menu 与 SaveSystem 都写, 关 
menu 时 settings_menu 覆盖 SaveSystem 期间写的值) + `_load_settings` 3 行 `cfg.get_value` 0 漏 default (true / 60.0 / 0) + 
`_populate_autosave_controls_from_cfg(cfg)` 1 行 helper 函数 (T136 落地, 推 3 autosave 控件 + live-push 到 SaveSystem)。0 触碰 T079 
respawn_to_hub 1 key (T136 0 改 [gameplay] respawn_to_hub, 仅 [gameplay] 扩 3 key)。
  - **T195 (#112) `[accessibility] reduce_shake / reduce_flash` 2 key 加 accessibility section** — `_save_settings` 2 行 
`cfg.set_value` 走控件 `button_pressed` (走控制 live 状态而非 `_reduced_shake` 字段, 与 T136 autosave 同样的 last-writer-wins 模式) + 
`_load_settings` 2 行 `cfg.get_value` 0 漏 default (false 默认全功能) + `set_block_signals(true/false)` 包裹避免 
`_on_reduce_*_toggled` 反复触发写 cfg + `ScreenShake.set_reduce_*` 跨 autoload live-push (同 T136 SaveSystem 模式)。0 触碰 T037 4 
audio + 2 video + T079 1 respawn + T136 3 autosave 既有 10 key (T195 0 改 [audio] / [video] / [gameplay] section 任何 key, 仅 
[accessibility] 扩 2 key)。
  - **T196 (#113) `[accessibility] reduce_vibration` 1 key** — `_save_settings` 1 行 `cfg.set_value` + `_load_settings` 
1 行 `cfg.get_value` 0 漏 default (false) + `set_block_signals` 包裹 + `ScreenShake.set_reduce_vibration` 跨 autoload 
live-push, 与 T195 reduce_shake 完全同模式 (1 key 同步 0 触碰 1 行)。0 触碰 T195 reduce_shake / reduce_flash 2 key (T196 0 改 
reduce_shake / reduce_flash 任何字段, 仅 reduce_vibration 扩 1 key)。
  - **T202.B (#121) `[accessibility] reduce_all` 1 key 加主开关数据独立** — `_save_settings` 1 行 `cfg.set_value("accessibility",
 "reduce_all", _reduce_all)` 走 `_reduce_all` 字段而非 `_reduce_all_check.button_pressed` (indeterminate 时 button_pressed 仍为 
false 但 _reduce_all 字段可能为 true, 存数据不存显示) + `_load_settings` 1 行 `cfg.get_value` 0 漏 default (false) + 
`set_block_signals` 包裹 + `indeterminate = false` 显式清 (indeterminate 是 UI 反映 0 持久化) + `_syncing_from_master = true; 
_apply_three_children(true); _set_three_children_disabled(true); _syncing_from_master = false` 防递归 (与 §9.6.11 三态 100% 
闭环)。0 触碰 T195 + T196 reduce_shake / reduce_flash / reduce_vibration 3 key (T202.B 0 改 reduce_shake / reduce_flash / 
reduce_vibration 任何字段, 仅 reduce_all 扩 1 key, T202.B 落地时 0 触碰 `_reduced_shake` / `_reduced_flash` / `_reduced_vibration` 
字段, 0 触碰 `ScreenShake.set_reduce_*` 调用 1:1 严格分离)。
  - **T205 (#122) `[audio] 4 label text 还原`** — `_load_settings` 4 audio 滑块 `value` 推后立即调 
`_refresh_audio_volume_label(_master_label, _master_slider.value, "主音量")` 等 4 行, 让玩家首次开 settings 立即看到 4 label 显示当前 cfg 
音量 ("主音量: 80") 而非 stale 默认值 ("主音量: 100"), 0 触碰 cfg 持久化层 (T205 0 改 `_save_settings` 任何 key, 仅 `_load_settings` 末尾加 4 行 
label 还原)。0 触碰 T037 + T079 + T136 + T195 + T196 + T202.B 任何 key (T205 0 改 4 audio 之外的任何 key, 0 改 cfg 写 / 读路径, 仅 4 label 
还原)。
  - **T239 (#157) `[audio] music preview button` (0 触碰持久化层)** — T239 加 `_music_preview_btn` 1 个 Button + 
`_on_music_preview_pressed` 1 个 handler + 3s 预览 1 个 BGM 主题, 0 触碰 `_save_settings` / `_load_settings` 任何 key (music 
preview 是临时播放, 0 持久化, 玩家关 settings 后 0 残留预览)。T239 是 §9.6.12 4 tab 状态机扩展 (audio_panel 内加 1 个 Button), 0 触碰 cfg 任何 key, 0 
触碰 §9.6.11 ReduceAllCheck 三态, 0 触碰 §9.6.12 _switch_tab 7 步骤, 与本节 5 件套 0 互混 0 复用 0 共享。
  - 0 副作用：T037 / T079 / T136 / T195 / T196 / T202.B / T205 / T239 任何 1 key 0 触碰既有 N-1 key (`_save_settings` 写 N-1 行 0 改,
 仅新加 1 行; `_load_settings` 读 N-1 行 0 改, 仅新加 1 行); 4 section 顺序 (audio → video → gameplay → accessibility → input) 0 改; 
last-writer-wins 模式 0 改 (settings_menu vs SaveSystem 都写 [gameplay] section, 走控件 live 状态, 0 触碰 cfg 缓存); 0 触碰 §9.6.12 4 
tab 状态机 (T037 + T079 + T136 + T195 + T196 + T202.B + T205 + T239 任何 const / var 0 触碰 7 件套 1:1 严格分离); 0 触碰 §9.6.11 
ReduceAllCheck 三态 (T195 + T196 + T202.B 0 触碰 `_syncing_from_master` 守卫 + `set_block_signals` 双层保险, 0 触碰 `_reduce_all` 
数据独立)。
- **预防**：
  1. 任何 polish 期给 settings_menu 加新 1 key (e.g. 玩家偏好 / UI 主题 / 字幕 / 难度 / 帧率上限 / 音量归一化) 或加新 1 section (e.g. [ui] / 
[graphics] / [audio_advanced] / [language]) 时**必须**严格按 5 件套 1:1 复制既有 4 section × N key 双源映射模式: (1) `_save_settings` 加 1 
行 `cfg.set_value("section", "key", value)` (走控件 live 状态而非字段缓存, 0 漏 1) + (2) `_load_settings` 加 1 行 
`cfg.get_value("section", "key", default)` 0 漏 default 参数 (fresh clone 走 default 兜底) + (3) `_load_settings` 末尾推控件 
`button_pressed` / `value` / `selected` + 跨 autoload live-push (T079 GameState / T136 SaveSystem / T195+T196+T202.B 
ScreenShake, 让相关 autoload 立即遵守) + (4) `set_block_signals(true/false)` 包裹避免 `_on_*_toggled` 反复触发写 cfg (T195 + T196 + 
T202.B 模式, 与 §9.6.11 三态 100% 闭环) + (5) `_ready` 末尾 `_load_settings()` 1 行 + `_on_close` 末尾 `_save_settings()` 1 行 
(玩家首次开 settings 立即还原 + 关 menu 立即持久化)。建议在 `_save_settings` 上方 docblock 写明 "5 件套 1:1 同步 (写 + 读 + default + 控件推 + 跨 
autoload live-push)" 硬约束。
  2. 4 section 顺序 (audio → video → gameplay → accessibility → input) 0 改 — `[audio]` 首位 (4 slider 音量) + `[video]` 第 2 位 
(fullscreen / scale) + `[gameplay]` 居中 (respawn / autosave) + `[accessibility]` 靠后 (4 reduce_*_check 视觉减弱) + `[input]` 
末位 (按键重映射), 任何"重排 section 顺序"polish 都会让 cfg 文件结构错位, 玩家从旧版升级到新版时 cfg 兼容 0 优雅, source-grep 验证 4 section 顺序 0 漏 0 改 1 
section。
  3. `cfg.get_value` 0 漏 default 参数 — `cfg.get_value("section", "key", default_value)` 3 个参数 0 漏第 3 个 (任何"漏 
default"polish 都会让 Godot 4 ConfigFile `Key not found` 错误, fresh clone 0 cfg 时 0 优雅), source-grep 验证 4 section × N key 
`cfg.get_value` 调用 0 漏 default 0 改 1 字符。`save_settings` 0 触碰 `get_value` 反之亦然, source-grep 验证 14 + N 行 `get_value` 0 漏 
0 改 1 行。
  4. last-writer-wins 模式 0 改 (settings_menu vs SaveSystem 都写 [gameplay] section) — `_save_settings` 3 autosave key 走控件 
`_autosave_enabled_check.button_pressed` / `_autosave_interval_slider.value` / `_autosave_slot_options.selected` 而非字段缓存 
(字段可能 stale), 任何"统一 last-writer-wins"polish 都会让 SaveSystem 期间写的 autosave 段在 settings_menu 关时被覆盖, source-grep 验证 3 
autosave `cfg.set_value` 0 漏 0 改 1 行 (走控件 0 走字段)。
  5. `_save_settings` ↔ `_load_settings` 1:1 双源映射 0 漏 1 边 — `_save_settings` 写 N key, `_load_settings` 必须读 N key (任何"加新 
key 但漏 1 边"polish 都会让持久化双向断开), 2 函数 N 行 `cfg.set_value` ↔ N 行 `cfg.get_value` 0 漏 0 错位 0 复用 0 共享, source-grep 验证 14 + N 
行 `cfg.set_value` + 14 + N 行 `cfg.get_value` 0 漏 0 改 1 字符 (按 section / key 1:1 对齐)。
  6. `set_block_signals(true/false)` 包裹 0 删 (T195 + T196 + T202.B accessibility 4 key 模式) — `_load_settings` 4 
reduce_*_check `set_block_signals(true)` + `button_pressed = ...` + `set_block_signals(false)` 包裹避免 
`_on_reduce_*_toggled` 反复触发写 cfg (与 §9.6.11 三态 100% 闭环), 任何"删 set_block_signals"polish 都会让 `_load_settings` 推控件时反复触发 
`_on_*_toggled` 写 cfg, 性能 0 优雅, source-grep 验证 4 reduce_*_check `set_block_signals` 0 漏 0 改 1 字符。
  7. T205 audio 4 label 还原 0 删 (T205 落地 `_refresh_audio_volume_label` 4 行) — `_load_settings` 4 audio 滑块 `value` 推后立即调 
`_refresh_audio_volume_label(_X_label, _X_slider.value, "X")` 4 行 (master / sfx / music / ambience), 任何"删 4 label 
还原"polish 都会让玩家首次开 settings 看到 stale 默认 label text ("主音量: 100") 而非当前 cfg 音量 ("主音量: 80"), 视觉组"settings 持久化"语义破坏, 
source-grep 验证 4 行 `_refresh_audio_volume_label` 0 漏 0 改 1 字符 (4 label + 4 slider + 4 base_text "主音量" / "音效" / "音乐" / 
"环境音" 1:1 严格对齐)。
  8. T037 / T079 / T136 / T195 / T196 + T202.B / T205 / T239 5 件套 0 触碰既有 N-1 key —— `_save_settings` 写 N-1 行 0 改, 仅新加 1 
行; `_load_settings` 读 N-1 行 0 改, 仅新加 1 行; `_load_settings` 末尾推控件 + 跨 autoload live-push N-1 行 0 改, 仅新加 1 行; 
`_load_settings` 末尾 `set_block_signals` 包裹 N-1 行 0 改, 仅新加 1 行 (T195 + T196 + T202.B 模式); `_ready` 末尾 `_load_settings()` 
+ `_on_close` 末尾 `_save_settings()` 0 触碰 (T037 落地时已设, 后续 polish 0 改 0 删 0 重排)。0 触碰 §9.6.12 4 tab 状态机 (任何 const / var 0 
触碰 7 件套 1:1 严格分离); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T195 + T196 + T202.B 0 触碰 `_syncing_from_master` 守卫 + `_reduce_all` 
数据独立); 0 触碰 §9.6.9 / §9.6.10 PauseMenu 跨面板 hover 反馈 (settings_menu 与 pause_menu 是 2 个独立 menu, 0 复用任何字段)。


### 9.6.14 settings_menu `[input]` section N key ACTION_NAMES 循环 + InputMap.action_get_events / action_add_event / 
action_erase_events 跨 InputEvent dict 序列化 + 1:1 持久化 polish 模式 (T086 + T194 + T195 + T196 + T202.B + T205 + T239 跨 7 任务 
~10 轮落地) 文档化

> §9.6.11 记录 settings_menu ReduceAllCheck 三态 (indeterminate + _syncing_from_master 防递归 + _reduce_all 数据独立); §9.6.12 记录 
> settings_menu 4 tab 状态机 (Tab 枚举 + 1 状态字段 + 4 tab Button @onready + 4 panel Control @onready + 4 signal connect + 
> _switch_tab 7 步骤 + 初始渲染 1 行) 7 件套硬约束; §9.6.13 记录 settings_menu `_save_settings` ↔ `_load_settings` 1:1 同步 + 
> `user://settings.cfg` 持久化 4 section × N key (audio + video + gameplay + accessibility + input) 双源映射 polish 模式。本节聚焦 
> §9.6.13 提到的 `(1.5) [input]` 段 —— ACTION_NAMES 字典 1:1 循环写 + InputMap.action_get_events 取第 1 个 event 存 dict + 
> `_load_settings` 1:1 循环读 + InputMap.action_erase_events / action_add_event 还原 InputMap 4 件套硬约束, 涉及 6 件套：(1) `const 
> ACTION_NAMES` 字典 (5-7 actions: move_left/right/jump + verb_primary/secondary + interaction) (2) `const 
> ACTION_CATEGORY` 字典 (按 category 3 段分组: movement/verb/interaction) (3) `const CATEGORY_RENDER_ORDER` 数组 (3 段 render 顺序,
>  含 name + key + color) (4) `const _DEFAULT_BINDINGS` 字典 (InputMap 默认值, 用于 "重置" 按钮) (5) `_save_settings` 末尾 4 行循环写 N 
> key (6) `_load_settings` 末尾 5 行循环读 N key + InputMap.action_erase_events + InputMap.action_add_event 还原。T037 (#23) 落地 
> 4 audio 滑块 + 2 video + T086 (#44) 落地 5 动作到 ACTION_NAMES + T194 (#112) 落地 5 verb 分组标题 (移动/声波能力/交互) 到 ACTION_CATEGORY + 
> CATEGORY_RENDER_ORDER + T195 (#112) accessibility reduce_shake / reduce_flash + T196 (#113) accessibility 
> reduce_vibration + T202.B (#121) accessibility reduce_all + T205 (#122) audio 4 label 还原 + T239 (#157) music preview 
> button, 8 个任务跨 ~10 轮反复落地 [input] section, 任何 polish 期给 settings_menu 加新 1 动作 (e.g. 切分 hot key 单独绑 / 6th verb 接入 / 
> 重置默认按键 / 玩家偏好键) 或加新 1 category 段 (e.g. UI 导航 / 战斗 / 调试) 时先查本节, 0 漏 1 处同步 (e.g. 漏 `ACTION_NAMES` 但 `_save_settings` 0 
> 漏 `for action in ACTION_NAMES.keys()` → 玩家在 CONTROLS tab 看到默认绑定但按 0 生效, 视觉组"按键重映射"语义破坏; 漏 `ACTION_CATEGORY` 但 
> `CATEGORY_RENDER_ORDER` 含 1 category → 玩家在 CONTROLS tab 看到 1 category 段 0 分组, 按 0 找; 漏 `_save_settings` 写 [input] 
> section 但 `_load_settings` 0 读 → 玩家在 CONTROLS tab 改键后重启游戏 0 还原, 永远走默认 InputMap; 漏 `if events.size() > 0` 守卫 → 玩家在 
> CONTROLS tab 看到 0 events 的 action 也存 `null` 到 cfg, `_load_settings` `if ev is InputEvent` 守卫能挡但 0 优雅; 漏 
> `InputMap.action_erase_events` 预清 → `_load_settings` 还原时残留 Godot 默认 InputMap + 玩家重映射 2 个键, 玩家按第 2 个键时同时触发 2 个 action; 
> 漏 `if ev is InputEvent` 守卫 → `cfg.get_value` 拿到 `null` (fresh clone 0 cfg) 时 `null.action_erase_events` 抛错; 漏 `for 
> action in ACTION_NAMES.keys()` 循环而走 5 行动词硬编码 → 任何"加 1 动作"polish 都会忘 1 行硬编码, source-grep 验证 6 步循环 0 漏 0 改 1 字符)。详细记录 4 
> 段 (症状/触发场景/修复/预防)：

- **症状**：polish 期给 settings_menu 加新 1 动作 (e.g. 切分 hot key 单独绑 / 6th verb 接入 / 重置默认按键 / 玩家偏好键) 时, 最常见的 fragile 是「(a) 
`ACTION_NAMES` 字典漏 1 action → `_save_settings` 循环 0 写, 玩家改键后 0 持久化」— T086 (#44) 落地 5 动作 (move_left / move_right / jump 
/ verb_primary / verb_secondary) 时显式用 5 行 dict literal `const ACTION_NAMES := {"move_left": "向左移动", ..., 
"verb_secondary": "次声波 (E)"}`, 任何"加 1 动作但漏 ACTION_NAMES"polish 都会让玩家在 CONTROLS tab 看不到新动作, `_save_settings` 循环 0 写新 
action, 玩家改键后重启游戏 0 还原, 视觉组"按键重映射"语义破坏;「(b) `ACTION_CATEGORY` 字典漏 1 action → `CATEGORY_RENDER_ORDER` 渲染时 0 分组」— T194 
(#112) 落地 5 verb 分组标题 (移动 / 声波能力 / 交互) 时显式用 `const ACTION_CATEGORY := {"move_left": "movement", ..., "interact": 
"interaction"}` + `const CATEGORY_RENDER_ORDER := [{"name": "移动", "key": "movement", "color": ...}, ...]` 3 段顺序, 任何"加 1 
category 段但漏 ACTION_CATEGORY"polish 都会让玩家在 CONTROLS tab 看到 1 段无标题的 0 分组, 视觉组"按键按 category 分组"语义破坏;「(c) `_save_settings` 
写 [input] section 但 `_load_settings` 0 读 → 玩家改键后重启游戏 0 还原」— T086 落地时显式用 `_save_settings` 4 行循环 `for action in 
ACTION_NAMES.keys(): var events := InputMap.action_get_events(action); if events.size() > 0: cfg.set_value("input", 
action, events[0])` (1112-1115) + `_load_settings` 5 行循环 `for action in ACTION_NAMES.keys(): var ev = 
cfg.get_value("input", action, null); if ev is InputEvent: InputMap.action_erase_events(action); 
InputMap.action_add_event(action, ev)` (1234-1238), 任何"加 [input] 段但漏 _load_settings"polish 都会让玩家首次开 settings 时新键 0 还原, 
永远走 Godot 默认 InputMap;「(d) 漏 `if events.size() > 0` 守卫 → 0 events 的 action 存 `null` 到 cfg」— T086 落地时显式用 `if 
events.size() > 0:` 守卫避免存 `null`, 任何"漏守卫"polish 都会让 cfg 文件 [input] section 含 `action = null` 行, `_load_settings` `if ev 
is InputEvent` 守卫能挡 (null 0 走 erase_events + add_event) 但 cfg 文件 0 优雅 + Godot 4 ConfigFile 序列化 `null` 时 0 一致;「(e) 漏 
`InputMap.action_erase_events` 预清 → 还原时残留 Godot 默认 InputMap」— T086 落地时显式用 `InputMap.action_erase_events(action); 
InputMap.action_add_event(action, ev)` 2 步 (先清再添), 任何"漏 action_erase_events"polish 都会让玩家重映射 1 个键后 InputMap 同时有 Godot 
默认键 + 玩家重映射键, 玩家按重映射键时同时触发 2 个 action, 视觉组"按键重映射 1:1 唯一"语义破坏;「(f) 漏 `if ev is InputEvent` 守卫 → `cfg.get_value` 拿 `null` 
抛错」— T086 落地时显式用 `if ev is InputEvent` 守卫避免 `null.action_erase_events` 抛错, 任何"漏守卫"polish 都会让 fresh clone 0 cfg 启动 
settings 时 Godot 4 ConfigFile `Key not found` 走 `get_value` 兜底 `null` 抛错, 玩家首次开 settings 0 启动。
- **触发场景**：T037 (#23) + T086 (#44) + T194 (#112) + T195 (#112) + T196 (#113) + T202.B (#121) + T205 (#122) + T239 
(#157) [input] section 6 件套落地需同步 5 步: (1) `src/scripts/settings_menu.gd:113-128` `const ACTION_NAMES := {...}` 字典 (5-7 
actions: move_left/right/jump + verb_primary/verb_secondary + interaction, 16 行) —— T086 落地 5 动作 + T194 扩 
verb_secondary/tertiary 等, 任何"加新动作"polish 必须先加 ACTION_NAMES 1 行 `dict literal`; (2) 
`src/scripts/settings_menu.gd:130-143` `const ACTION_CATEGORY := {...}` 字典 (按 category 3 段分组: movement / verb / 
interaction, 14 行) —— T194 落地 5 verb 分组, 任何"加新 category 段"polish 必须先加 ACTION_CATEGORY 1 行 + CATEGORY_RENDER_ORDER 1 行 
`dict literal`; (3) `src/scripts/settings_menu.gd:145-183` `const CATEGORY_RENDER_ORDER := [...]` 数组 (3 段 render 顺序, 含 
name + key + color 4 字段, 39 行) —— T194 落地 3 段, 颜色与 STYLE_GUIDE.md 的 Pale Resonance / Amber Voice / Verdant Echo 3 主色对应; 
(4) `src/scripts/settings_menu.gd:185-230` `const _DEFAULT_BINDINGS := {...}` 字典 (InputMap 默认值, 用于 "重置默认按键" 按钮, 46 行) 
—— T086 落地 5 动作默认 binding, 任何"加新动作"polish 必须加 1 行 `dict literal` 描述默认 binding; (5) 
`src/scripts/settings_menu.gd:1111-1115` `_save_settings()` 末尾 5 行 `[input]` section N key 写 —— 严格 4 步: (5.1) `# Save 
input map` 注释 1 行 (1111) + (5.2) `for action in ACTION_NAMES.keys():` 循环 1 行 (1112) + (5.3) `var events := 
InputMap.action_get_events(action)` 1 行 (1113) + (5.4) `if events.size() > 0:` 守卫 1 行 (1114) + (5.5) 
`cfg.set_value("input", action, events[0])` 1 行 (1115), 5 行 0 漏 0 错位 0 改 1 字符; (6) 
`src/scripts/settings_menu.gd:1233-1238` `_load_settings()` 末尾 6 行 `[input]` section N key 读 + InputMap 还原 —— 严格 6 步: 
(6.1) `# Load input map` 注释 1 行 (1233) + (6.2) `for action in ACTION_NAMES.keys():` 循环 1 行 (1234) + (6.3) `var ev = 
cfg.get_value("input", action, null)` 1 行 (1235) + (6.4) `if ev is InputEvent:` 守卫 1 行 (1236) + (6.5) 
`InputMap.action_erase_events(action)` 1 行 (1237) + (6.6) `InputMap.action_add_event(action, ev)` 1 行 (1238), 6 行 0 漏 0 
错位 0 改 1 字符。5 步 0 漏 1 = 玩家改键后 0 持久化 / 玩家首次开 settings 0 还原 / 玩家重映射 1 个键后按 1 下触发 2 个 action / fresh clone 0 cfg 启动 
settings 抛错 / CONTROLS tab 看到 0 分组的 1 段 0 标题。
- **修复**：`src/scripts/settings_menu.gd` [input] section 6 件套 (ACTION_NAMES 字典 + ACTION_CATEGORY 字典 + 
CATEGORY_RENDER_ORDER 数组 + _DEFAULT_BINDINGS 字典 + `_save_settings` 末尾 4 步循环写 + `_load_settings` 末尾 5 步循环读 + InputMap 
还原) polish 模式 (T037 + T086 + T194 + T195 + T196 + T202.B + T205 + T239 跨 8 任务 ~10 轮落地)：
  - **T037 (#23) `_save_settings` + `_load_settings` 4 audio + 2 video 6 key 基础** — T037 落地 4 audio slider + 2 video 6 
key, 0 触碰 [input] section (T037 落地时 T086 还没加, [input] 段 0 写 0 读)。0 触碰 §9.6.12 4 tab 状态机 (4 slider 是 `_audio_panel` 
内子节点); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T037 落地时 T202.B 还没加)。
  - **T086 (#44) `[input]` section 5 动作 + `_save_settings` 4 行循环写 + `_load_settings` 5 行循环读 + InputMap 还原** — `const 
ACTION_NAMES` 字典 5 动作 (move_left / move_right / jump / verb_primary / verb_secondary, 5 行 dict literal) + `const 
_DEFAULT_BINDINGS` 字典 5 动作默认 binding (5 行 dict literal) + `_save_settings` 末尾 4 行 `[input]` section 循环 (for / 
action_get_events / size() > 0 守卫 / set_value) + `_load_settings` 末尾 5 行 `[input]` section 循环 (for / get_value / is 
InputEvent 守卫 / action_erase_events / action_add_event), 0 触碰 T037 4 audio + 2 video 既有 6 key (T086 0 改 [audio] / 
[video] section 任何 key, 仅扩 1 section [input])。T086 落地时 T194 还没加 ACTION_CATEGORY + CATEGORY_RENDER_ORDER, 0 触碰 category 
分组, `_save_settings` / `_load_settings` 直接走 `for action in ACTION_NAMES.keys()` 循环 0 走 category 段。
  - **T194 (#112) ACTION_CATEGORY 字典 + CATEGORY_RENDER_ORDER 数组 3 段 (移动 / 声波能力 / 交互) 加 CONTROLS tab 渲染** — `const 
ACTION_CATEGORY` 字典 5 动作按 category 3 段分组 (movement / verb / interaction, 14 行) + `const CATEGORY_RENDER_ORDER` 数组 3 段 
render 顺序 (含 name + key + color 4 字段, 颜色与 STYLE_GUIDE.md 3 主色对应: Pale Resonance / Amber Voice / Verdant Echo, 39 行), 
让玩家在 CONTROLS tab 看到按键按 3 段分组渲染 (移动: A/D/Space / 声波能力: Q/E / 交互: F), 视觉组"按键按 category 分组渲染"100% 闭环。0 触碰 T086 5 动作既有 5 行 
`const ACTION_NAMES` (T194 0 改 5 动作任何字段, 仅加 2 个 const 字典 + 数组); 0 触碰 T086 `_save_settings` 末尾 4 行 + `_load_settings` 末尾 
5 行 [input] section 循环 (T194 0 改 `_save_settings` / `_load_settings` 任何字段, 仅 `const ACTION_CATEGORY` + `const 
CATEGORY_RENDER_ORDER` 2 个新 const); 0 触碰 T037 4 audio + 2 video 既有 6 key (T194 0 改 [audio] / [video] section 任何 key)。
  - **T195 (#112) `[accessibility] reduce_shake / reduce_flash` 2 key** — T195 落地 accessibility 2 key (reduce_shake / 
reduce_flash), 0 触碰 [input] section 任何 const / var / 函数 (T195 0 改 ACTION_NAMES / ACTION_CATEGORY / 
CATEGORY_RENDER_ORDER / _DEFAULT_BINDINGS 任何字段, 0 改 `_save_settings` 末尾 4 行 + `_load_settings` 末尾 5 行 [input] section 
循环, 0 改 `InputMap.action_erase_events` / `InputMap.action_add_event` 调用)。T195 落地 `_on_reduce_*_toggled` 2 个 handler + 
`ScreenShake.set_reduce_*` 跨 autoload live-push, 与 [input] section 0 互混 0 复用 0 共享。
  - **T196 (#113) `[accessibility] reduce_vibration` 1 key** — T196 加 1 个 reduce_vibration 控件 + 1 个 
`_on_reduce_vibration_toggled` handler + `ScreenShake.set_reduce_vibration` 1 个跨 autoload live-push, 0 触碰 [input] 
section 任何 const / var / 函数 (与 T195 完全同模式, 1 key 同步 0 触碰 1 行)。
  - **T202.B (#121) `[accessibility] reduce_all` 1 key 加主开关数据独立** — T202.B 落地 ReduceAllCheck 三态 (enabled / disabled / 
indeterminate) + 3 子项 reduce_*_check 联动 + 防递归 `_syncing_from_master` 守卫, 0 触碰 [input] section 任何 const / var / 函数 
(T202.B 0 改 ACTION_NAMES / ACTION_CATEGORY / CATEGORY_RENDER_ORDER / _DEFAULT_BINDINGS 任何字段, 0 改 `_save_settings` / 
`_load_settings` 任何 key, 0 改 `InputMap.action_*` 调用)。T202.B 落地时 §9.6.11 三态 100% 闭环, 与 [input] section 0 互混 0 复用 0 共享。
  - **T205 (#122) `[audio] 4 label text 还原`** — T205 加 4 行 `_refresh_audio_volume_label(_X_label, _X_slider.value, 
"X")` (master / sfx / music / ambience), 0 触碰 [input] section 任何 const / var / 函数 (T205 0 改 ACTION_NAMES / 
ACTION_CATEGORY / CATEGORY_RENDER_ORDER / _DEFAULT_BINDINGS 任何字段, 0 改 `_save_settings` / `_load_settings` 任何 key, 0 改 
`InputMap.action_*` 调用)。
  - **T239 (#157) `[audio] music preview button` (0 触碰持久化层)** — T239 加 `_music_preview_btn` 1 个 Button + 
`_on_music_preview_pressed` 1 个 handler + 3s 预览 1 个 BGM 主题, 0 触碰 [input] section 任何 const / var / 函数 (T239 0 改 
ACTION_NAMES / ACTION_CATEGORY / CATEGORY_RENDER_ORDER / _DEFAULT_BINDINGS 任何字段, 0 改 `_save_settings` / 
`_load_settings` 任何 key, 0 改 `InputMap.action_*` 调用)。
  - 0 副作用：T037 / T086 / T194 / T195 / T196 / T202.B / T205 / T239 任何 1 key / 1 const / 1 行 0 触碰既有 N-1 元素 
(`_save_settings` 写 N-1 行 0 改, 仅新加 1 行; `_load_settings` 读 N-1 行 0 改, 仅新加 1 行; `const ACTION_NAMES` 5 动作 0 改, 仅新加 1 动作; 
`const ACTION_CATEGORY` 5 动作 0 改, 仅新加 1 动作; `const CATEGORY_RENDER_ORDER` 3 段 0 改, 仅新加 1 段); [input] section 5 件套 
(ACTION_NAMES + ACTION_CATEGORY + CATEGORY_RENDER_ORDER + _DEFAULT_BINDINGS + 4 行 save + 5 行 load) 0 改 0 删 0 重排; 
last-writer-wins 模式 0 改 (settings_menu vs SaveSystem 都写 [gameplay] section, [input] section 仅 settings_menu 写 0 互混); 0 
触碰 §9.6.13 4 section × N key 双源映射 (T086 + T194 0 改 [audio] / [video] / [gameplay] / [accessibility] section 任何 key, 仅 
[input] section 扩 5 行); 0 触碰 §9.6.12 4 tab 状态机 (T086 + T194 0 触碰 7 件套 1:1 严格分离); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T195 + 
T196 + T202.B 0 触碰 `_syncing_from_master` 守卫 + `_reduce_all` 数据独立); 0 触碰 §9.6.9 / §9.6.10 PauseMenu 跨面板 hover 反馈。
- **预防**：
  1. 任何 polish 期给 settings_menu 加新 1 动作 (e.g. 切分 hot key 单独绑 / 6th verb 接入 / 重置默认按键 / 玩家偏好键) 时**必须**严格按 5 件套 1:1 复制既有 
[input] section 6 件套同步模式: (1) `const ACTION_NAMES` 字典加 1 行 `dict literal` (0 漏 1 action) + (2) `const ACTION_CATEGORY` 
字典加 1 行 `dict literal` (按 category 3 段分组: movement / verb / interaction) + (3) `const _DEFAULT_BINDINGS` 字典加 1 行 `dict 
literal` (描述默认 binding, 用于 "重置默认按键" 按钮) + (4) `const CATEGORY_RENDER_ORDER` 数组若新增 1 category 段则加 1 行 `dict literal` (含 
name + key + color 4 字段, 颜色与 STYLE_GUIDE.md 主色对应) + (5) `_save_settings` 末尾 4 行循环 (`for / action_get_events / size() > 
0 守卫 / set_value`) 与 `_load_settings` 末尾 5 行循环 (`for / get_value / is InputEvent 守卫 / action_erase_events / 
action_add_event`) 0 触碰 (T086 + T194 0 改 4+5 行循环, 仅 ACTION_NAMES / ACTION_CATEGORY / _DEFAULT_BINDINGS / 
CATEGORY_RENDER_ORDER 4 个 const 加新 1 行)。建议在 `_save_settings` 末尾 `[input]` 循环上方 docblock 写明 "5 件套 1:1 同步 (const + const 
+ const + const + 4 行 save + 5 行 load)" 硬约束。
  2. [input] section 6 件套 0 漏 1 边 —— `const ACTION_NAMES` 5 动作 0 漏 0 改 1 字符; `const ACTION_CATEGORY` 5 动作 0 漏 0 改 1 字符; 
`const CATEGORY_RENDER_ORDER` 3 段顺序 (移动 → 声波能力 → 交互) 0 改; `const _DEFAULT_BINDINGS` 5 动作 0 漏 0 改 1 字符; `_save_settings` 
末尾 4 行循环 0 漏 0 改 1 行 (`for / action_get_events / size() > 0 / set_value`); `_load_settings` 末尾 5 行循环 0 漏 0 改 1 行 (`for 
/ get_value / is InputEvent / action_erase_events / action_add_event`)。任何"加新动作但漏 1 边"polish 都会让玩家改键后 0 持久化 / 玩家首次开 
settings 0 还原 / 玩家重映射 1 个键后按 1 下触发 2 个 action, source-grep 验证 4 个 const + 4+5 行循环 0 漏 0 改 1 字符 (按 5 动作 / 3 段 category 
1:1 对齐)。
  3. `if events.size() > 0` 守卫 0 删 (T086 落地时显式用 `if events.size() > 0:` 守卫避免存 `null` 到 cfg) —— 任何"删 size() > 0 
守卫"polish 都会让 0 events 的 action 也存 `null` 到 cfg 文件, `_load_settings` `if ev is InputEvent` 守卫能挡 (null 0 走 erase_events 
+ add_event) 但 cfg 文件 0 优雅 + Godot 4 ConfigFile 序列化 `null` 时 0 一致, source-grep 验证 `_save_settings` 末尾 4 行循环 0 漏 
`events.size() > 0` 守卫 0 改 1 字符。
  4. `if ev is InputEvent` 守卫 0 删 (T086 落地时显式用 `if ev is InputEvent:` 守卫避免 `null.action_erase_events` 抛错) —— 任何"删 is 
InputEvent 守卫"polish 都会让 fresh clone 0 cfg 启动 settings 时 `cfg.get_value("input", action, null)` 拿到 `null` 后 
`null.action_erase_events` 抛错, Godot 4 ConfigFile `Key not found` 走 `get_value` 兜底 `null` 但 0 优雅, source-grep 验证 
`_load_settings` 末尾 5 行循环 0 漏 `is InputEvent` 守卫 0 改 1 字符。
  5. `InputMap.action_erase_events` 预清 0 删 (T086 落地时显式用 `InputMap.action_erase_events(action); 
InputMap.action_add_event(action, ev)` 2 步先清再添) —— 任何"删 action_erase_events"polish 都会让玩家重映射 1 个键后 InputMap 同时有 Godot 
默认键 + 玩家重映射键, 玩家按重映射键时同时触发 2 个 action, 视觉组"按键重映射 1:1 唯一"语义破坏, source-grep 验证 `_load_settings` 末尾 5 行循环 0 漏 
`action_erase_events` 0 漏 `action_add_event` 0 改 1 行 (2 步 1:1 严格分离)。
  6. `for action in ACTION_NAMES.keys()` 循环 0 改硬编码 (T086 落地时显式用 `for action in ACTION_NAMES.keys()` 循环 0 写 5 行动词硬编码) —— 
任何"改硬编码"polish 都会让"加 1 动作"必须改 2 处 (const + 循环), 任何"加 1 动作但漏循环硬编码"polish 都会让新动作 0 持久化, source-grep 验证 `_save_settings` 
末尾 + `_load_settings` 末尾 2 个 `for action in ACTION_NAMES.keys()` 循环 0 改 0 删 0 重排。
  7. T037 / T086 / T194 / T195 / T196 / T202.B / T205 / T239 5 件套 0 触碰既有 N-1 元素 —— `_save_settings` 写 N-1 行 0 改, 仅新加 1 
行; `_load_settings` 读 N-1 行 0 改, 仅新加 1 行; `const ACTION_NAMES` N-1 动作 0 改, 仅新加 1 动作; `const ACTION_CATEGORY` N-1 动作 0 改,
 仅新加 1 动作; `const CATEGORY_RENDER_ORDER` N-1 段 0 改, 仅新加 1 段; `const _DEFAULT_BINDINGS` N-1 动作 0 改, 仅新加 1 动作; `_ready` 
末尾 `_load_settings()` + `_on_close` 末尾 `_save_settings()` 0 触碰 (T037 落地时已设, 后续 polish 0 改 0 删 0 重排)。0 触碰 §9.6.13 4 
section × N key 双源映射 (任何 const / var 0 触碰 5 件套 1:1 严格分离); 0 触碰 §9.6.12 4 tab 状态机 (任何 const / var 0 触碰 7 件套 1:1 严格分离); 0 
触碰 §9.6.11 ReduceAllCheck 三态 (T195 + T196 + T202.B 0 触碰 `_syncing_from_master` 守卫 + `_reduce_all` 数据独立); 0 触碰 §9.6.9 / 
§9.6.10 PauseMenu 跨面板 hover 反馈 (settings_menu 与 pause_menu 是 2 个独立 menu, 0 复用任何字段)。


### 9.6.15 SaveSystem `audit_save_slots()` 4 状态巡检 (empty / ok / corrupted / drift) + `_refresh_profile_audit` 1 行 4 字段 
+ `has_method` 跨调用方守卫 + `call_deferred` 启动延迟 polish 模式 (T088 + T131 + T224 + T229 + T265 跨 5 任务 ~40 轮落地) 文档化

> §9.6.13 记录 settings_menu `_save_settings` ↔ `_load_settings` 1:1 同步; §9.6.14 记录 settings_menu `[input]` section 6 
> 件套持久化。本节跳出 settings_menu 范畴, 记录 SaveSystem autoload 的 5 slot 存档健康度巡检 polish 模式 —— `audit_save_slots()` 公共方法返回 4 状态 
> (empty / ok / corrupted / drift) + 4 ids arrays Dictionary, 走 5 件套 1:1 同步: (1) `SaveSystem.audit_save_slots()` 公共入口 
> (1 行委托 `_audit_save_slots()`), (2) `_audit_save_slots()` 内部 worker (返回原始 Dictionary), (3) `SaveSystem._ready()` 末尾 
> `call_deferred("audit_save_slots")` 启动延迟 1 行 (避免 _ready 半完成时 IO 阻塞), (4) `pause_menu.gd _refresh_profile_audit()` 1 行 
> 4 字段渲染 (ok / 损坏 / 漂移 / 空, 中点 `·` 分隔) + 3 档色域分工 (损坏>0 暖红 / 漂移>0 暖黄 / 全 ok 暖白), (5) `title_screen.gd _ready()` 
> `has_method("audit_save_slots")` 守卫 + 1 行调用 (TitleScreen re-entry 时再 audit 一次, 玩家继续前看到 fresh 巡检结果)。T088 (#45) 落地 
> SLOT_COUNT 顶层常量 3→5 + T131 (#80) 落地 ProfileAudit 1 行 + T224 (#146) 落地 `audit_save_slots` 4 状态巡检 + T229 (#149) 落地 
> `_refresh_profile_audit` PauseMenu 渲染 + T265 (#186) 落地 F002 self-test commit hook 集成 (5 任务跨 ~40 轮反复落地), 任何 polish 期给 
> SaveSystem 加新 1 状态 (e.g. "stale" 30 天没动 / "pinned" 玩家 pin 防删 / "shared" 多人共享 / "encrypted" 加密存档) 或加新 1 调用方 (e.g. 
> settings_menu 加 "存档健康" 面板 / SaveLoadMenu 加 "巡检" 按钮 / DeathScreen 死亡时 audit) 时**必须**严格按 5 件套 1:1 复制既有 audit_save_slots 
> 4 状态巡检模式, 0 漏 1 处同步 (e.g. 漏 `call_deferred` 启动延迟 → SaveSystem._ready 期间 IO 阻塞主线程, 玩家启动黑屏 +0.1-0.3s, 视觉组"60 秒竖切"语义破坏; 
> 漏 `has_method` 守卫 → SaveSystem autoload 在 headless / 反序列化失败时为 null, `_refresh_profile_audit` 调 
> `SaveSystem.audit_save_slots()` 抛 NPE, 玩家打开 PauseMenu 看存档 1 行变 null 黑屏; 漏 4 状态 (empty / ok / corrupted / drift) 任一 → 
> 巡检结果 0 完整, 例如漏 corrupted 计数 → 玩家存档损坏但 ProfileAudit 1 行显示 "5 ok · 0 损坏 · 0 漂移 · 0 空" 误导玩家以为存档健康; 漏 4 ids arrays 
> (ok_ids / corrupted_ids / drift_ids / empty_ids) → 巡检日志仅给计数, tech-savvy 玩家切到 Output panel 看 push_warning 行也 0 知道具体哪个 
> slot 损坏, debug 0 友好; 漏 1:1 双层 (公共 `audit_save_slots` + 内部 `_audit_save_slots`) → 测试脚本无法直接调 worker, 必须走公共层 + 承受 
> push_warning 噪音, smoke test 0 干净; 漏 3 档色域分工 (损坏>0 暖红 / 漂移>0 暖黄 / 全 ok 暖白) → 玩家看到损坏存档但 PauseMenu 1 行仍是暖白, 
> 视觉组"存档健康优先级"语义破坏; 漏 `int(report.get("ok", 0))` 0 漏 default → report 字典缺 key 时 (例如 fresh SaveSystem 4 状态未初始化) 
> `_refresh_profile_audit` 报 "Invalid get index" 错误, 玩家打开 PauseMenu 抛错; 漏 `SLOT_COUNT` 顶层常量 0 改硬编码 → 加 slot 6 时 
> audit_save_slots 仅巡检前 5 slot, slot 6 永远 0 巡检, source-grep 验证 5 件套 0 漏 0 改 1 字符 (按 SLOT_COUNT 1:1 严格对齐))。详细记录 4 段 
> (症状/触发场景/修复/预防)：

- **症状**：polish 期给 SaveSystem 加新 1 状态 (e.g. "stale" 30 天没动 / "pinned" 玩家 pin 防删 / "shared" 多人共享 / "encrypted" 加密存档) 时, 
最常见的 fragile 是「(a) 漏 `call_deferred` 启动延迟 → SaveSystem._ready 期间 IO 阻塞主线程」— T131 (#80) 落地时显式用 
`call_deferred("audit_save_slots")` 1 行 (save_system.gd:133), 任何"删 call_deferred 改直接调"polish 都会让 SaveSystem._ready() 期间 
audit_save_slots 直接调, 5 slot × read + CRC32 + 字典 alloc 阻塞主线程 0.1-0.3s, 玩家启动黑屏 + 视觉组"60 秒竖切"语义破坏;「(b) 漏 `has_method` 守卫 
→ SaveSystem autoload 为 null 时 NPE」— T229 (#149) 落地时显式用 `if not SaveSystem or not 
SaveSystem.has_method("audit_save_slots"):` 守卫, 任何"删 has_method 守卫"polish 都会让 SaveSystem autoload 在 headless / 反序列化失败时为 
null, `_refresh_profile_audit` 调 `SaveSystem.audit_save_slots()` 抛 NPE, 玩家打开 PauseMenu 看存档 1 行变 null 黑屏, 视觉组"PauseMenu 
0 崩溃"语义破坏;「(c) 漏 4 状态任一 → 巡检结果 0 完整」— T224 (#146) 落地时显式用 `var report := {"empty": 0, "ok": 0, "corrupted": 0, "drift": 
0, "corrupted_ids": [] as Array, "drift_ids": [] as Array, "empty_ids": [] as Array, "ok_ids": [] as Array, "total": 
SLOT_COUNT}` 4 + 4 = 8 key 1:1 完整 Dictionary, 任何"漏 1 状态 (e.g. 加 stale 但漏 0 改 4 状态)"polish 都会让 audit 报告缺 key, 
`_refresh_profile_audit` `int(report.get("stale", 0))` 拿 default 0 但 stale 实际计数 >0, 玩家看不到"30 天没动"提醒;「(d) 漏 4 ids arrays 
→ 巡检日志仅给计数 0 知道哪个 slot 损坏」— T224 落地时显式用 `(report["corrupted_ids"] as Array).append(i)` 等 4 处 append, 任何"漏 ids 
arrays"polish 都会让 push_warning 行仅打印计数, tech-savvy 玩家切到 Output panel 也 0 知道具体哪个 slot 损坏, debug 0 友好;「(e) 漏 1:1 双层 (公共 
audit_save_slots + 内部 _audit_save_slots) → smoke test 走公共层 + 承受 push_warning 噪音」— T224 落地时显式用 2 函数 1:1 严格分离 (公共 
`audit_save_slots` 1 行委托 + `push_warning` 1 行; 内部 `_audit_save_slots` 返回 Dictionary 0 副作用), 任何"合并 2 函数"polish 都会让 smoke 
test 走公共层承受 push_warning 噪音 (虽然 push_warning 是 stderr, 但 CI log 0 干净);「(f) 漏 3 档色域分工 (损坏>0 暖红 / 漂移>0 暖黄 / 全 ok 暖白) → 
玩家看到损坏存档但 PauseMenu 1 行仍是暖白」— T229 落地时显式用 3 档 `if corrupted_n > 0: color = Color(0.85, 0.45, 0.4, 1.0); elif drift_n > 
0: color = Color(0.85, 0.71, 0.43, 1.0); else: color = Color(0.875, 0.835, 0.784, 1.0)`, 任何"统一暖白"polish 
都会让损坏存档视觉组"存档健康优先级"语义破坏;「(g) 漏 `int(report.get("ok", 0))` 0 漏 default → report 字典缺 key 时抛错」— T229 落地时显式用 4 行 
`int(report.get("ok", 0))` / `int(report.get("corrupted", 0))` / `int(report.get("drift", 0))` / 
`int(report.get("empty", 0))` 0 漏 default 0 漏 1 行, 任何"漏 default"polish 都会让 report 字典缺 key 时 (例如 fresh SaveSystem 4 
状态未初始化) `int(report.get("ok"))` 报 "Invalid get index" 错误, 玩家打开 PauseMenu 抛错;「(h) 漏 `SLOT_COUNT` 顶层常量 0 改硬编码 5 → 加 slot 
6 时 audit 0 巡检」— T088 (#45) 落地时显式用 `SLOT_COUNT = 5` 顶层 const, 任何"硬编码 5"polish 都会让 audit_save_slots 写 `for i in 
range(5):` 0 走 SLOT_COUNT, 加 slot 6 时 audit 仅巡检前 5 slot, slot 6 永远 0 巡检, 玩家存档损坏 slot 6 但 ProfileAudit 1 行显示 "5 ok" 误导。
- **触发场景**：T088 (#45) + T131 (#80) + T224 (#146) + T229 (#149) + T265 (#186) audit_save_slots 5 件套落地需同步 5 步：(1) 
`src/autoload/save_system.gd:800-801` `func audit_save_slots() -> Dictionary:` 公共入口 2 行 (`return _audit_save_slots()` 1 
行委托, push_warning 1 行) —— T224 落地时显式用 2 函数 1:1 严格分离, 公共层仅委托 + 日志, 内部 worker 负责实际巡检; (2) 
`src/autoload/save_system.gd:803-852` `func _audit_save_slots() -> Dictionary:` 内部 worker 50 行 —— 严格 8 步: (2.1) 初始化 
report dict 8 key (empty / ok / corrupted / drift + 4 ids arrays + total) 1 行, (2.2) `for i in range(SLOT_COUNT):` 循环 1 
行 (走 SLOT_COUNT 顶层 const 0 走硬编码 5), (2.3) `var path := _slot_path(i)` 1 行, (2.4) `if not FileAccess.file_exists(path):` 
守卫 + empty 计数 + ids append 3 行, (2.5) `var data := _read_json(path)` 1 行, (2.6) `if data.is_empty():` 守卫 + corrupted 计数 
+ ids append 3 行, (2.7) drift 检查 3 字段 (has_version / has_room / has_meta) 5 行 + drift 计数 + ids append 2 行, (2.8) ok 计数 
+ ids append 2 行, (2.9) 末尾 `if report["corrupted"] > 0 or report["drift"] > 0: push_warning(...)` 7 行 + else print 1 行 
0 漏 0 错位; (3) `src/autoload/save_system.gd:133` `call_deferred("audit_save_slots")` 启动延迟 1 行 (在 _ready 末尾) —— T131 
落地时显式用 call_deferred 避免 _ready 半完成时 IO 阻塞, 任何"删 call_deferred 改直接调"polish 都会让 5 slot 巡检在 _ready 期间同步执行, 主线程 0.1-0.3s 阻塞,
 玩家启动黑屏; (4) `src/scripts/pause_menu.gd:1705-1732` `_refresh_profile_audit()` 渲染 28 行 —— 严格 8 步: (4.1) `if not 
_profile_audit: return` 守卫 2 行, (4.2) `if not SaveSystem or not SaveSystem.has_method("audit_save_slots"):` 守卫 3 行 + 
"—" 占位 + 暖白 0 灰化, (4.3) `var report: Dictionary = SaveSystem.audit_save_slots()` 1 行, (4.4) 4 行 `int(report.get("ok", 
0))` / `int(report.get("corrupted", 0))` / `int(report.get("drift", 0))` / `int(report.get("empty", 0))` 0 漏 default, 
(4.5) `var color: Color` + 3 档 `if corrupted_n > 0: ... elif drift_n > 0: ... else: ...` 6 行色域分工, (4.6) 
`_profile_audit.text = "存档  %d ok  ·  %d 损坏  ·  %d 漂移  ·  %d 空" % [ok_n, corrupted_n, drift_n, empty_n]` 1 行 4 字段 (中点 "·
" 与 ProfileQuickStats 4 段 + ProfileRecentList 7 字段行风格完全一致), (4.7) `_profile_audit.add_theme_color_override("font_color",
 color)` 1 行; (5) `src/scripts/title_screen.gd:61-62` `if SaveSystem and SaveSystem.has_method("audit_save_slots"): 
SaveSystem.audit_save_slots()` 2 行 (TitleScreen re-entry 时再 audit 一次) —— T265 落地时显式用 has_method 守卫避免 headless / 反序列化失败时 
NPE, 玩家继续前看到 fresh 巡检结果。5 件套 0 漏 1 = 玩家启动黑屏 0.1-0.3s / 玩家打开 PauseMenu 抛 NPE / 巡检结果 0 完整 / 巡检日志 0 知道哪个 slot 损坏 / smoke 
test 走公共层 + 承受 push_warning 噪音 / 损坏存档视觉组"存档健康优先级"语义破坏 / report 字典缺 key 抛 "Invalid get index" 错误 / 加 slot 6 永远 0 巡检。
- **修复**：`SaveSystem.audit_save_slots` 5 件套 (公共入口 + 内部 worker + 启动 call_deferred + PauseMenu 渲染 + TitleScreen 守卫) 
polish 模式 (T088 + T131 + T224 + T229 + T265 跨 5 任务 ~40 轮落地)：
  - **T088 (#45) `SLOT_COUNT = 5` 顶层 const** — T088 把 3 → 5 槽位扩展时显式用 `const SLOT_COUNT := 5` 顶层常量 + `for i in 
range(SLOT_COUNT):` 循环 0 走硬编码 3/5, 任何"加 slot 6"polish 必须改 SLOT_COUNT 1 行 + audit_save_slots 0 触碰。0 触碰 §9.6.13 
settings_menu `[gameplay]` section (autosave 3 key 走 `SaveSystem.SLOT_COUNT` 0 写死 5); 0 触碰 §9.6.14 settings_menu 
`[input]` section (T086 0 改 SaveSystem 任何字段, 0 改 SLOT_COUNT)。
  - **T131 (#80) `call_deferred("audit_save_slots")` 启动延迟 1 行** — T131 落地 ProfileAudit 1 行 + SaveSystem._ready 末尾 1 行 
`call_deferred("audit_save_slots")`, 让 5 slot 巡检在 _ready 完成 + 首帧后才执行, 避免 IO 阻塞主线程 0.1-0.3s, 玩家启动黑屏 → 60 秒竖切内 0 卡顿。0 触碰 
T088 SLOT_COUNT 顶层常量 (T131 0 改 SLOT_COUNT, 仅加 1 行 call_deferred)。
  - **T224 (#146) `audit_save_slots` 4 状态巡检 + 双层 (公共 + 内部)** — T224 落地 2 函数 1:1 严格分离: 公共 `audit_save_slots` 1 行委托 + 
push_warning 1 行; 内部 `_audit_save_slots` 50 行 4 状态 (empty / ok / corrupted / drift) + 4 ids arrays + total + drift 检查 3 
字段 (has_version / has_room / has_meta) 8 步 1:1 严格分离。0 触碰 T088 SLOT_COUNT 顶层常量 (T224 0 改 SLOT_COUNT, 仅加 2 函数 50 行); 0 触碰 
T131 call_deferred (T224 0 改 call_deferred, 仅扩 audit_save_slots 函数体)。
  - **T229 (#149) `pause_menu.gd _refresh_profile_audit` 1 行 4 字段 + 3 档色域分工** — T229 加 28 行渲染 (4 字段中点 "·" + 3 档色域分工 + 
has_method 守卫 + 4 行 int(report.get) 0 漏 default), 0 触碰 T224 audit_save_slots 2 函数 (T229 0 改 save_system.gd 任何字段, 仅 
pause_menu.gd 加 1 函数 28 行)。T229 落地时 §9.6.9 / §9.6.10 PauseMenu 跨面板 hover 反馈已落地, _refresh_profile_audit 是独立数据源 (本函数看存档健康,
 aggregate 看 run history, 2 个完全独立), 0 复用任何字段。
  - **T265 (#186) `title_screen.gd` `has_method` 守卫 + 1 行 audit** — T265 加 2 行守卫 + 调用, 让 TitleScreen re-entry 时再 audit 
一次, 玩家继续前看到 fresh 巡检结果。0 触碰 T088 + T131 + T224 + T229 任何 const / var / 函数 (T265 0 改 save_system.gd 任何字段, 0 改 
pause_menu.gd 任何字段, 仅 title_screen.gd 加 2 行)。
  - 0 副作用：T088 / T131 / T224 / T229 / T265 任何 const / var / 函数 0 触碰既有 N-1 元素 (`SLOT_COUNT` 顶层常量 0 改, 仅扩 1 行; 
`audit_save_slots` 公共入口 0 改, 仅新加 1 函数; `_audit_save_slots` 内部 worker 0 改既有 4 状态, 仅新加 N 状态; `call_deferred` 启动延迟 0 删 0 改 
1 行; `_refresh_profile_audit` 1 函数 0 改既有 4 字段, 仅新加 1 字段; `_ready` 末尾 `call_deferred` + `_refresh_profile_audit` 0 改 0 删 
0 重排); 4 状态顺序 (empty / ok / corrupted / drift) 0 改; 4 ids arrays (empty_ids / ok_ids / corrupted_ids / drift_ids) 0 改; 
3 档色域分工 (损坏>0 暖红 / 漂移>0 暖黄 / 全 ok 暖白) 0 改; `int(report.get("X", 0))` 0 漏 default 0 改 1 字符; 中点 "·" 分隔符 0 改; 0 触碰 §9.6.13 
settings_menu 4 section × N key (T131 + T224 + T229 + T265 0 改 [audio] / [video] / [gameplay] / [accessibility] / 
[input] section 任何 key, 仅 save_system.gd + pause_menu.gd + title_screen.gd 3 文件); 0 触碰 §9.6.14 settings_menu `[input]` 
section 6 件套 (T131 + T224 + T229 + T265 0 改 ACTION_NAMES / ACTION_CATEGORY / CATEGORY_RENDER_ORDER / _DEFAULT_BINDINGS 
任何 const, 0 改 `_save_settings` 末尾 4 行 + `_load_settings` 末尾 5 行 [input] section 循环); 0 触碰 §9.6.12 4 tab 状态机 (T131 + 
T224 + T229 + T265 0 触碰 settings_menu.gd 7 件套 1:1 严格分离); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T131 + T224 + T229 + T265 0 触碰 
`_syncing_from_master` 守卫 + `_reduce_all` 数据独立); 0 触碰 §9.6.9 / §9.6.10 PauseMenu 跨面板 hover 反馈 (audit_save_slots 走独立数据源, 
与 hover 反馈 0 互混)。
- **预防**：
  1. 任何 polish 期给 SaveSystem 加新 1 状态 (e.g. "stale" 30 天没动 / "pinned" 玩家 pin 防删 / "shared" 多人共享 / "encrypted" 加密存档) 或加新 
1 调用方 (e.g. settings_menu 加 "存档健康" 面板 / SaveLoadMenu 加 "巡检" 按钮 / DeathScreen 死亡时 audit) 时**必须**严格按 5 件套 1:1 复制既有 
audit_save_slots 4 状态巡检模式: (1) 公共 `audit_save_slots` 加 1 行委托 + push_warning (1 行日志) (0 漏 1 边) + (2) 内部 
`_audit_save_slots` 扩 N 状态 + N ids arrays (走 `for i in range(SLOT_COUNT):` 循环 0 改硬编码 5) + (3) SaveSystem._ready 末尾 
`call_deferred("audit_save_slots")` 1 行 (0 删 0 改) + (4) `pause_menu.gd _refresh_profile_audit` 加 1 行 4 字段渲染 
(`int(report.get("X", 0))` 0 漏 default) + N 档色域分工 (0 触碰 3 档暖红/暖黄/暖白) + 中点 "·" 分隔符 (0 改) + (5) `title_screen.gd _ready` 
`has_method` 守卫 + 1 行 audit (0 删 0 改)。建议在 `_audit_save_slots` 上方 docblock 写明 "5 件套 1:1 同步 (公共入口 + 内部 worker + 
call_deferred + PauseMenu 渲染 + TitleScreen 守卫)" 硬约束。
  2. `call_deferred("audit_save_slots")` 启动延迟 0 删 (T131 落地时显式用 call_deferred 避免 _ready 半完成时 IO 阻塞) —— 任何"删 
call_deferred 改直接调"polish 都会让 5 slot 巡检在 _ready 期间同步执行, 主线程 0.1-0.3s 阻塞, 玩家启动黑屏, source-grep 验证 SaveSystem._ready 末尾 
`call_deferred("audit_save_slots")` 0 漏 0 改 1 字符。
  3. `has_method("audit_save_slots")` 跨调用方守卫 0 删 (T229 + T265 落地时显式用 has_method 守卫避免 SaveSystem autoload 在 headless / 
反序列化失败时 NPE) —— 任何"删 has_method 守卫"polish 都会让 SaveSystem autoload 为 null 时 `_refresh_profile_audit` / 
`title_screen._ready` 调 `SaveSystem.audit_save_slots()` 抛 NPE, 玩家打开 PauseMenu / TitleScreen 抛错黑屏, source-grep 验证 
pause_menu.gd `_refresh_profile_audit` + title_screen.gd `_ready` 2 处 `has_method` 守卫 0 漏 0 改 1 字符。
  4. 4 状态 (empty / ok / corrupted / drift) + 4 ids arrays (empty_ids / ok_ids / corrupted_ids / drift_ids) 1:1 双源 —— 
`_audit_save_slots` 4 状态 + 4 ids arrays 0 漏 0 错位 0 复用 0 共享, `int(report.get("X", 0))` 4 行 default 0 漏, 任何"加新状态但漏 ids 
arrays"polish 都会让 push_warning 仅打印计数 0 知道具体哪个 slot 损坏, source-grep 验证 4 状态 + 4 ids arrays 0 漏 0 改 1 字符 (按 SLOT_COUNT 
1:1 严格对齐)。
  5. 3 档色域分工 (损坏>0 暖红 (0.85, 0.45, 0.4) / 漂移>0 暖黄 (0.85, 0.71, 0.43) / 全 ok 暖白 (0.875, 0.835, 0.784)) 0 改 (T229 落地时显式用 
3 档 if/elif/else 色域分工) —— 任何"统一暖白"polish 都会让损坏存档视觉组"存档健康优先级"语义破坏, source-grep 验证 `_refresh_profile_audit` 3 档 `if 
corrupted_n > 0: ... elif drift_n > 0: ... else: ...` 0 漏 0 改 1 行。
  6. `int(report.get("X", 0))` 0 漏 default 0 改 1 字符 (T229 落地时显式用 4 行 `int(report.get("ok", 0))` / 
`int(report.get("corrupted", 0))` / `int(report.get("drift", 0))` / `int(report.get("empty", 0))`) —— 任何"漏 
default"polish 都会让 report 字典缺 key 时 (例如 fresh SaveSystem 4 状态未初始化) `int(report.get("ok"))` 报 "Invalid get index" 错误, 
玩家打开 PauseMenu 抛错, source-grep 验证 4 行 `int(report.get(X, 0))` 0 漏 0 改 1 字符。
  7. `SLOT_COUNT` 顶层常量 0 改硬编码 5 (T088 落地时显式用 `const SLOT_COUNT := 5` 顶层 const + `for i in range(SLOT_COUNT):` 循环 0 走硬编码 
3/5) —— 任何"硬编码 5"polish 都会让 audit_save_slots 写 `for i in range(5):` 0 走 SLOT_COUNT, 加 slot 6 时 audit 仅巡检前 5 slot, slot 
6 永远 0 巡检, source-grep 验证 `for i in range(SLOT_COUNT):` 0 漏 0 改 1 字符 (走 SLOT_COUNT 0 走硬编码)。
  8. 1:1 双层 (公共 `audit_save_slots` + 内部 `_audit_save_slots`) 0 合并 (T224 落地时显式用 2 函数 1:1 严格分离) —— 任何"合并 2 函数"polish 都会让 
smoke test 走公共层 + 承受 push_warning 噪音, 0 干净, source-grep 验证 `func audit_save_slots()` + `func _audit_save_slots()` 2 函数 
0 漏 0 改 1 行 (公共层 1 行委托 + 内部层 50 行 worker 1:1 严格分离)。
  9. 中点 "·" 分隔符 0 改 (T229 落地时显式用 4 字段中点 "·" 与 ProfileQuickStats 4 段 + ProfileRecentList 7 字段行风格完全一致) —— 
任何"改中点为其他字符"polish 都会让玩家视觉组"ProfileAudit 1 行"与其他 4 字段行风格不连贯, 视觉组"PauseMenu 4 字段行 1:1 风格统一"语义破坏, source-grep 验证 
`_profile_audit.text = "存档  %d ok  ·  %d 损坏  ·  %d 漂移  ·  %d 空"` 中点 0 漏 0 改 1 字符。
  10. T088 / T131 / T224 / T229 / T265 5 件套 0 触碰既有 N-1 元素 —— `SLOT_COUNT` 顶层常量 0 改, 仅新加 1 slot 改 1 行; 
`audit_save_slots` 公共入口 0 改, 仅新加 1 行; `_audit_save_slots` 内部 worker 0 改既有 4 状态, 仅新加 1 状态; 
`call_deferred("audit_save_slots")` 启动延迟 0 删 0 改 1 行; `_refresh_profile_audit` 1 函数 0 改既有 4 字段, 仅新加 1 字段; `_ready` 末尾 
`call_deferred` + `_refresh_profile_audit` 0 改 0 删 0 重排; `title_screen.gd _ready` `has_method` 守卫 + 1 行 audit 0 删 0 改 1 
行。0 触碰 §9.6.13 settings_menu 4 section × N key (任何 const / var 0 触碰 5 件套 1:1 严格分离); 0 触碰 §9.6.14 settings_menu 
`[input]` section 6 件套 (任何 const / var 0 触碰 ACTION_NAMES / ACTION_CATEGORY / CATEGORY_RENDER_ORDER / _DEFAULT_BINDINGS 
4 const + 4+5 行循环); 0 触碰 §9.6.12 4 tab 状态机 (任何 const / var 0 触碰 7 件套 1:1 严格分离); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T131 + 
T224 + T229 + T265 0 触碰 `_syncing_from_master` 守卫 + `_reduce_all` 数据独立); 0 触碰 §9.6.9 / §9.6.10 PauseMenu 跨面板 hover 反馈 
(audit_save_slots 走独立数据源, 与 hover 反馈 0 互混 0 复用 0 共享)。
