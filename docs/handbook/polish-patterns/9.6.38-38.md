# Polish Patterns 9.6.38–38

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.38–9.6.38，共 1 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.38-38.md

<!-- shard 38-38 共 1 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.38 6 verb audio 家族 19 cue 字段扩展 5 段 1:1 严格分离契约 polish 模式 (T181 #97 + T220 #142 + F013.B #106 + T270 #189 跨 4 任务 
~118 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 28 套 polish 模式均在 "pause_menu / 6 verb / Hub / 成就 / prewarm / 工具链 / 文档归档 / 
> PlayerStats 持久化层 / audio 数据层 / PlayerActionGate 单 autoload 4 件套 / SaveSystem save data CRC32 校验 5 段" 范畴, 0 涉及 "6 verb 
> audio 家族 19 cue 字段扩展 5 段 1:1 严格分离契约" 落地. §9.6.23 (T279 #199 落地) 覆盖 prewarm aggregator 7 桶 5+1+1 canonical 1:1 序列, 
> §9.6.35 (T291 #214 落地) 覆盖 `audio_presets.gd` `MUSIC_PRESETS` 9 preset × 13 字段 canonical 1:1 严格分离契约, §9.6.37 (T293 
> #217 落地) 覆盖 SaveSystem save data CRC32 校验 5 段 1:1 严格分离契约 — 这 3 段分别覆盖 prewarm aggregator 5+1+1 / audio 数据层 preset 字段扩展 
> / save_system 持久化层 5 段 接入, 但**未**覆盖 "6 verb audio 家族 19 cue 字段扩展 5 段 (`audio_presets.gd` 19 cue 字典 + 
> `audio_manager_enhanced.gd` 19 cue 引用 + `verb_ability_base.gd` 6 verb → audio cue 映射 + `SFX_PRESETS` dict + prewarm 
> SFX cache key) 1:1 严格分离契约" 必须同时 1:1 镜像 5 段 (Stage 1 cue 字典 1:1 严格 + Stage 2 cue 引用 1:1 严格 + Stage 3 verb → cue 映射 1:1 
> 严格 + Stage 4 SFX dict 1:1 严格 + Stage 5 prewarm cache key 1:1 严格) 的契约. 本节 (T294 #218 落地) 首个落在**6 verb audio 家族 19 cue 
> 字段扩展 5 段 1:1 严格分离契约** (`src/scripts/audio_presets.gd` 19 cue 字典 (5 verb fire + 5 verb cooldown tail + 5 verb cooldown 
> ready + 4 misc bind/cut/echo/wave_fire) + 1 `src/scripts/audio_manager_enhanced.gd` 19 cue 引用 (5 verb fire 
> `prewarm_verb_fire_sfx` 4 stream 镜像 + 5 verb tail `prewarm_verb_cooldown_tails` 5 stream + 5 verb ready 
> `prewarm_verb_cooldown_readys` 5 stream) + 1 `src/scripts/_verb_ability_base.gd` 6 verb → audio cue 映射 
> (verb_ability_base.gd 16 件套 与 audio cue 1:1 镜像) + 1 `SFX_PRESETS` dict (audio_presets.gd 与 SFX_PRESETS 1:1 严格) + 1 
> prewarm SFX cache key (`prewarm_verb_fire_sfx` / `prewarm_verb_cooldown_tails` / `prewarm_verb_cooldown_readys` 3 桶 
> cache key 1:1 严格)) polish 模式 — 跨 5 段 (Stage 1 cue 字典 1:1 严格 0 漏 1 entry + Stage 2 cue 引用 1:1 严格 0 漏 1 边 + Stage 3 
> verb → cue 映射 1:1 严格 0 漏 1 映射 + Stage 4 SFX dict 1:1 严格 0 漏 1 dict + Stage 5 prewarm cache key 1:1 严格 0 漏 1 key) — 跨 
> 1 套 polish 模式 (T294 #218 落地) 跨 4 任务 (T181 #97 + T220 #142 + F013.B #106 + T270 #189 ~118 轮落地, 0 漂动 0 后续 主要 
> pre-existing 风险). 任何"加新 1 cue" (e.g. 未来加 6th verb 7th cue 升级 whisper) 或"加新 1 SFX dict" (e.g. 未来加 `SFX_BOSS_PRESETS` 
> 扩展 boss 专属 SFX) 或"加新 1 prewarm 桶" (e.g. 未来加 `prewarm_verb_charge_sfx` 蓄力 SFX 桶) 必须**先**按 Stage 1 cue 字典 1:1 严格 加 1 
> entry (0 漏 1 entry 0 改 1 entry 0 反序 0 反向), **再**按 Stage 2 cue 引用 1:1 镜像 (1:1 严格, 0 漏 1 边 0 改 1 边 0 反序 0 反向), **再**按 
> Stage 3 verb → cue 映射 1:1 镜像 (1:1 严格, 0 漏 1 映射 0 改 1 映射 0 反序 0 反向), **再**按 Stage 4 SFX dict 1:1 镜像 (1:1 严格, 0 漏 1 
> dict 0 改 1 dict 0 反序 0 反向), **再**按 Stage 5 prewarm cache key 1:1 镜像 (1:1 严格, 0 漏 1 key 0 改 1 key 0 反序 0 反向). 跨 1 套 
> polish 模式 × 5 段 = 5 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: `6 verb audio 家族 19 cue 字段扩展 5 段` (`src/scripts/audio_presets.gd` + `src/scripts/audio_manager_enhanced.gd` + 
`src/scripts/_verb_ability_base.gd` + `SFX_PRESETS` dict + prewarm SFX cache key, T181 #97 + T220 #142 + F013.B #106 + 
T270 #189 跨 4 任务 ~118 轮落地) 现有的 5 段 (1 `audio_presets.gd` 19 cue 字典 (5 verb fire + 5 verb cooldown tail + 5 verb 
cooldown ready + 4 misc bind/cut/echo/wave_fire = 19 cue) + 1 `audio_manager_enhanced.gd` 19 cue 引用 (5 verb fire 
`prewarm_verb_fire_sfx` 4 stream 镜像 + 5 verb tail `prewarm_verb_cooldown_tails` 5 stream + 5 verb ready 
`prewarm_verb_cooldown_readys` 5 stream) + 1 `_verb_ability_base.gd` 6 verb → audio cue 映射 (16 件套 0 改 与 audio cue 1:1 
镜像) + 1 `SFX_PRESETS` dict (audio_presets.gd 与 SFX_PRESETS 1:1 严格) + 1 prewarm SFX cache key (`prewarm_verb_fire_sfx` / 
`prewarm_verb_cooldown_tails` / `prewarm_verb_cooldown_readys` 3 桶 cache key 1:1 严格)), 在反复 polish (T181 #97 → T270 #189 
跨 4 任务 ~118 轮落地) 期间出现 4 类典型 fragile: (1) **"加新 1 cue 漏 5 段"** — 加新 1 cue (e.g. 假设未来加 6th verb "whisper" 第 6 verb SFX 
cue) 落地时漏 5 段 (e.g. 漏 Stage 1 cue 字典 中 `_whisper_stream` entry, 或漏 Stage 2 cue 引用 中 `prewarm_verb_fire_sfx` 加 whisper 
branch, 或漏 Stage 3 verb → cue 映射 中 `verb_ability_base` 加 whisper 1 边, 或漏 Stage 4 SFX dict 中加 whisper entry, 或漏 Stage 5 
prewarm cache key 中加 whisper key), 1 cue 扩展 0 5 段 0 闭环 0 漂动; (2) **"加新 1 SFX dict 漏 1 边"** — 加新 1 SFX dict (e.g. 假设未来加 
`SFX_BOSS_PRESETS` 扩展 boss 专属 SFX) 落地时漏 1 边 (e.g. 漏 Stage 4 SFX dict 中 `_boss_stream` entry 或 漏 Stage 5 prewarm cache 
key 中 `prewarm_boss_sfx` 桶), 1 dict 扩展 0 5 段 0 闭环 0 漂动; (3) **"修 1 cue 0 触碰既有 18 cue"** — 修 1 cue (e.g. 改 
`_pulse_stream` 频率 0 改 既有) 或 修 1 SFX dict (e.g. 改 `SFX_PRESETS` 5 entry 任何 1 entry) 时 0 触碰既有 18 cue (5 段任何 1 cue / 5 
段任何 1 dict 0 改), 跨字段 1:1 漂动; (4) **"加新 1 prewarm 桶 0 触碰既有 3 桶"** — 加新 1 prewarm 桶 (e.g. 假设未来加 `prewarm_verb_charge_sfx` 
蓄力 SFX 桶) 落地时漏 5 段 (e.g. 漏 Stage 2 cue 引用 中 `prewarm_all_sfx` aggregator 加新 1 调点, 1 桶 0 5 段 0 闭环 0 漂动; 或漏 Stage 5 
prewarm cache key 中新 1 桶 key, 1 桶 0 cache key 0 闭环). 4 类症状根因: 6 verb audio 家族 19 cue 字段扩展 5 段 1:1 序列 0 文档化, 任何新 1 cue / 
1 dict / 1 桶 接入只能"凭经验" 1:1 镜像 5 段 (1 cue 字典 1:1 严格 + 1 cue 引用 1:1 严格 + 1 verb → cue 映射 1:1 严格 + 1 SFX dict 1:1 严格 + 1 
prewarm cache key 1:1 严格), 1 漏 1 段 = 1 cue / 1 dict / 1 桶 扩展 0 5 段 0 闭环 0 漂动.

- **触发场景**: 加新 1 cue / 1 SFX dict / 1 prewarm 桶 (T181 #97 模式参考, 假设未来加 6th verb "whisper" 第 6 verb SFX cue) 时**必须**严格按 5 
段 1:1 复制 T181 落地模式: (1) **Stage 1 cue 字典 1:1 严格** — 在 `audio_presets.gd` 底部 cue 字典 区 加 1 entry (e.g. T181 落地时 
`audio_presets.gd` 19 cue 字典 1:1 严格 (5 verb fire + 5 verb cooldown tail + 5 verb cooldown ready + 4 misc = 19 cue, 与 
`_pulse_stream` / `_bind_stream` / `_cut_stream` / `_echo_stream` / `_wave_fire_stream` 5 verb fire 1:1 严格 + 与 
`_verb_cooldown_streams[pulse/bind/cut/echo/wave]` 5 verb ready 1:1 严格 + 与 
`_verb_cooldown_tail_streams[pulse/bind/cut/echo/wave]` 5 verb tail 1:1 严格 + 与 `_unlock_chime_stream` / 
`_shop_purchase_confirm_stream` / `_shop_level_up_streams[0..4]` / `_death_lay_down_stream` 4 misc 1:1 严格), 假设未来加 6th 
verb "whisper" 第 6 verb SFX cue, 底部 cue 字典 加 `_whisper_stream` 1 entry, 与既有 19 cue 0 漏 0 改 0 反序 0 反向, 0 触碰既有 19 cue 任何 
1 character, 0 漏 1 entry, 0 触碰既有 5 段), source-grep 验证 `audio_presets.gd` cue 字典 19 entry → 20 entry 0 漏 0 改 0 漏 1 entry 
0 反向; (2) **Stage 2 cue 引用 1:1 严格** — 在 `audio_manager_enhanced.gd` `prewarm_verb_fire_sfx()` / 
`prewarm_verb_cooldown_tails()` / `prewarm_verb_cooldown_readys()` 3 桶 函数 加 1 边 (e.g. T181 落地时 
`prewarm_verb_fire_sfx()` 1:1 严格 (5 verb fire 1:1 严格 0 漏 0 改, `_pulse_stream` non-lazy in _ready + `_bind_stream` / 
`_cut_stream` / `_echo_stream` / `_wave_fire_stream` 4 stream lazy prewarm, ~15 ms total), 假设未来加 6th verb, 0 触碰既有 
`prewarm_verb_fire_sfx()` 任何 1 character, 加新 1 边 `_whisper_stream = _generate_whisper_sfx()` 1:1 严格 0 漏 0 改 0 反向), 
source-grep 验证 5 verb → 6 verb 0 漏 0 改 0 漏 1 边 0 反向; (3) **Stage 3 verb → cue 映射 1:1 严格** — 在 `_verb_ability_base.gd` 
16 件套 中 加 1 映射 (e.g. T181 落地时 `_verb_ability_base.gd` 6 verb → audio cue 映射 1:1 严格 (6 verb ability 子类 与 audio cue 1:1 
严格 0 漏 0 改, `pulse_ability.gd` / `bind_ability.gd` / `cut_ability.gd` / `echo_ability.gd` / `resonance_wave_ability.gd` 
/ `whisper_ability.gd` 6 verb ability 与 audio cue 1:1 镜像), 假设未来加 7th verb, 0 触碰既有 `_verb_ability_base.gd` 任何 1 
character, 加新 1 边 `_whisper_ability` 1:1 严格 0 漏 0 改 0 反向), source-grep 验证 6 verb → 7 verb 0 漏 0 改 0 漏 1 映射 0 反向; (4) 
**Stage 4 SFX dict 1:1 严格** — 在 `audio_presets.gd` `SFX_PRESETS` dict 区 加 1 dict (e.g. T181 落地时 `SFX_PRESETS` 1:1 严格 (5 
verb fire 1:1 严格 0 漏 0 改, `pulse_fire` / `bind_fire` / `cut_fire` / `echo_fire` / `wave_fire` 5 entry 0 改 1 hex 0 改 1 
通道值), 假设未来加 6th verb, 0 触碰既有 `SFX_PRESETS` 任何 1 character, 加新 1 entry `_whisper_fire` 1:1 严格 0 漏 0 改 0 反向), source-grep 
验证 5 entry → 6 entry 0 漏 0 改 0 漏 1 entry 0 反向; (5) **Stage 5 prewarm cache key 1:1 严格** — 在 `audio_manager_enhanced.gd` 
3 prewarm 桶 + 1 aggregator (prewarm_all_sfx) 加 1 key (e.g. T181 落地时 prewarm 桶 cache key 1:1 严格 
(`_verb_cooldown_streams[pulse/bind/cut/echo/wave]` 5 key 0 改 1 string 0 反向 + 
`_verb_cooldown_tail_streams[pulse/bind/cut/echo/wave]` 5 key 0 反向), 假设未来加 6th verb, 0 触碰既有 3 桶 cache key 任何 1 
character, 加新 1 key `_verb_cooldown_streams[whisper]` 1:1 严格 0 漏 0 改 0 反向), source-grep 验证 5 key → 6 key 0 漏 0 改 0 漏 1 
key 0 反向. 5 段 1:1 同步, 1 漏 1 段 / 1 漏 1 边 = 1 cue / 1 dict / 1 桶 扩展 0 5 段 0 闭环 0 漂动. T181 #97 0 触碰既有 0 改 
`audio_presets.gd` 19 cue 字典 / 0 改 `audio_manager_enhanced.gd` 19 cue 引用 / 0 改 `_verb_ability_base.gd` 6 verb → audio 
cue 映射 / 0 改 `SFX_PRESETS` dict / 0 改 prewarm SFX cache key 任何 1 character (0 改 cue 字典 任何 1 entry, 0 改 cue 引用 任何 1 
character, 0 改 verb → cue 映射 任何 1 character, 0 改 SFX dict 任何 1 character, 0 改 prewarm cache key 任何 1 character).

- **修复** (跨 1 套 polish 模式 T294 #218 落地后已知 5 段 0 例外):
  - **T294 #218 首个 6 verb audio 家族 19 cue 字段扩展 5 段 接入 1:1 严格分离契约 落地 (`audio_presets.gd` 19 cue 字典 + 
`audio_manager_enhanced.gd` 19 cue 引用 + `_verb_ability_base.gd` 6 verb → audio cue 映射 + `SFX_PRESETS` dict + prewarm 
SFX cache key)** — §9.6.38 段 是 1 套 polish 模式首个 1:1 包含 5 段 (1 `audio_presets.gd` cue 字典 1:1 严格 + 1 
`audio_manager_enhanced.gd` cue 引用 1:1 严格 + 1 `_verb_ability_base.gd` verb → cue 映射 1:1 严格 + 1 `SFX_PRESETS` dict 1:1 
严格 + 1 prewarm SFX cache key 1:1 严格) 的 6 verb audio 家族 19 cue 字段扩展 polish 模式, 5 段 1:1 严格分离 (1 cue 字典 1:1 严格 0 漏 0 改 + 1 
cue 引用 1:1 严格 0 漏 0 改 + 1 verb → cue 映射 1:1 严格 0 漏 0 改 + 1 SFX dict 1:1 严格 0 漏 0 改 + 1 prewarm cache key 1:1 严格 0 漏 0 改,
 0 漏 1 段 0 改 1 字段 0 反序 0 反向), 0 触碰既有 (T294 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / 
§9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / 
§9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 28 套 polish 模式任何 1 character, 0 
触碰游戏代码 .gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / 
CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).
  - **T294 #218 Stage 1 cue 字典 1:1 严格分离契约 落地 (`audio_presets.gd` 19 cue 字典)** — §9.6.38 段 1:1 显式 cue 字典 (T294 落地时 
`audio_presets.gd` 19 cue 字典 1:1 严格 (5 verb fire `_pulse_stream` non-lazy + `_bind_stream` + `_cut_stream` + 
`_echo_stream` + `_wave_fire_stream` 4 stream lazy prewarm = 5 verb fire + 5 verb cooldown tail 
`_verb_cooldown_tail_streams[pulse/bind/cut/echo/wave]` + 5 verb cooldown ready 
`_verb_cooldown_streams[pulse/bind/cut/echo/wave]` + 4 misc `_unlock_chime_stream` + `_shop_purchase_confirm_stream` + 
`_shop_level_up_streams[0..4]` + `_death_lay_down_stream` = 19 cue 1:1 严格 0 漏 0 改 0 反序 0 反向), cue 字典 1:1 严格 (1 
`audio_presets.gd` 19 entry 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 entry 0 改 1 entry 0 反序 0 反向), 0 
触碰既有 (T294 0 改 `audio_presets.gd` 19 cue 字典任何 1 character).
  - **T294 #218 Stage 2 cue 引用 1:1 严格分离契约 落地 (`audio_manager_enhanced.gd` 19 cue 引用 + 3 桶 prewarm 函数)** — §9.6.38 段 1:1 
显式 cue 引用 (T294 落地时 `audio_manager_enhanced.gd` 19 cue 引用 1:1 严格 (5 verb fire `prewarm_verb_fire_sfx()` 4 stream 
`_bind_stream` / `_cut_stream` / `_echo_stream` / `_wave_fire_stream` + `_pulse_stream` non-lazy 1:1 严格 + 5 verb tail 
`prewarm_verb_cooldown_tails()` 5 stream `_verb_cooldown_tail_streams[pulse/bind/cut/echo/wave]` 1:1 严格 + 5 verb ready 
`prewarm_verb_cooldown_readys()` 5 stream `_verb_cooldown_streams[pulse/bind/cut/echo/wave]` 1:1 严格), 0 漏 1 边 0 改 1 边 0 
反序 0 反向), cue 引用 1:1 严格 (1 3 桶 prewarm 函数 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 边 0 改 1 character 0 
反序 0 反向), 0 触碰既有 (T294 0 改 `audio_manager_enhanced.gd` `prewarm_verb_fire_sfx()` / `prewarm_verb_cooldown_tails()` / 
`prewarm_verb_cooldown_readys()` 任何 1 character).
  - **T294 #218 Stage 3 verb → cue 映射 1:1 严格分离契约 落地 (`_verb_ability_base.gd` 6 verb → audio cue 映射)** — §9.6.38 段 1:1 
显式 verb → cue 映射 (T294 落地时 `_verb_ability_base.gd` 6 verb → audio cue 映射 1:1 严格 (6 verb ability 子类 `pulse_ability.gd` / 
`bind_ability.gd` / `cut_ability.gd` / `echo_ability.gd` / `resonance_wave_ability.gd` / `whisper_ability.gd` 与 audio 
cue 1:1 镜像 1:1 严格 0 漏 0 改 0 反向), verb → cue 映射 1:1 严格 (1 6 verb ability 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 
改, 0 漏 1 映射 0 改 1 character 0 反序 0 反向), 0 触碰既有 (T294 0 改 `_verb_ability_base.gd` 16 件套任何 1 character).
  - **T294 #218 Stage 4 SFX dict 1:1 严格分离契约 落地 (`audio_presets.gd` `SFX_PRESETS` dict)** — §9.6.38 段 1:1 显式 SFX dict 
(T294 落地时 `audio_presets.gd` `SFX_PRESETS` dict 1:1 严格 (5 verb fire `pulse_fire` / `bind_fire` / `cut_fire` / 
`echo_fire` / `wave_fire` 5 entry 0 改 1 hex 0 改 1 通道值 0 反序 0 反向), SFX dict 1:1 严格 (1 5 entry 1:1 严格 0 漏 0 改 + 1 
source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 entry 0 改 1 character 0 反序 0 反向), 0 触碰既有 (T294 0 改 `audio_presets.gd` 
`SFX_PRESETS` dict 任何 1 character).
  - **T294 #218 Stage 5 prewarm cache key 1:1 严格分离契约 落地 (`audio_manager_enhanced.gd` 3 桶 prewarm cache key + 1 
aggregator)** — §9.6.38 段 1:1 显式 prewarm cache key (T294 落地时 `audio_manager_enhanced.gd` 3 桶 prewarm cache key 1:1 严格 
(`prewarm_verb_fire_sfx()` 4 cache key `_bind_stream` / `_cut_stream` / `_echo_stream` / `_wave_fire_stream` + 
`prewarm_verb_cooldown_tails()` 5 cache key `_verb_cooldown_tail_streams[pulse/bind/cut/echo/wave]` + 
`prewarm_verb_cooldown_readys()` 5 cache key `_verb_cooldown_streams[pulse/bind/cut/echo/wave]` 1:1 严格 0 漏 0 改 0 反向), 
prewarm cache key 1:1 严格 (1 3 桶 14 cache key 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 key 0 改 1 
character 0 反序 0 反向), 0 触碰既有 (T294 0 改 `audio_manager_enhanced.gd` 3 桶 prewarm cache key 任何 1 character).
  - 跨 1 套 polish 模式 T294 #218 落地后 0 漂动, §9.6.38 段 1:1 严格 (5 段 1:1 严格分离 + cue 字典 1:1 严格 + cue 引用 1:1 严格 + verb → cue 映射 
1:1 严格 + SFX dict 1:1 严格 + prewarm cache key 1:1 严格) 100% 闭环, 5 段 1:1 严格分离 (1 Stage 1 cue 字典 1:1 严格 0 漏 0 改 0 反序 + 1 
Stage 2 cue 引用 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 3 verb → cue 映射 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 4 SFX dict 1:1 严格 0 漏 0 改 0 
反序 + 1 Stage 5 prewarm cache key 1:1 严格 0 漏 0 改 0 反序). 1 套 polish 模式 0 后续 主要 pre-existing 风险.

- **预防**:
  1. 任何 polish 期给 6 verb audio 家族 加新 1 cue (e.g. 6th verb "whisper" / 7th verb 升级扩展) 或加新 1 SFX dict (e.g. 
`SFX_BOSS_PRESETS` / `SFX_AMBIENT_PRESETS` boss / ambient SFX 扩展) 或加新 1 prewarm 桶 (e.g. `prewarm_verb_charge_sfx` / 
`prewarm_boss_sfx` 蓄力 / boss 桶) 任务**必须**严格按 5 段 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 改 cue 字典 1:1 
严格分离契约** (T294 落地后 `audio_presets.gd` 19 cue 字典 已切 1:1 strict 0 漏 0 改 0 改 1 entry 0 漏 1 entry 0 反序 0 反向, 改 1 entry / 漏 
1 entry = 1 cue 扩展 0 5 段 0 闭环 0 漂动), **Stage 2 0 改 cue 引用 1:1 严格分离契约** (T294 落地后 `audio_manager_enhanced.gd` 19 cue 引用 
+ 3 桶 prewarm 函数 已切 1:1 strict 0 漏 0 改 0 改 1 character 0 漏 1 character 0 反序 0 反向, 改 1 character / 漏 1 character = 1 cue 
扩展 0 cue 引用 0 闭环 0 漂动), **Stage 3 0 改 verb → cue 映射 1:1 严格分离契约** (T294 落地后 `_verb_ability_base.gd` 6 verb → audio cue 
映射 已切 1:1 strict 0 漏 0 改 0 改 1 character 0 漏 1 character 0 反序 0 反向, 改 1 character / 漏 1 character = 1 cue 扩展 0 映射 0 闭环 
0 漂动), **Stage 4 0 改 SFX dict 1:1 严格分离契约** (T294 落地后 `audio_presets.gd` `SFX_PRESETS` dict 5 entry 已切 1:1 strict 0 漏 0 
改 0 改 1 character 0 漏 1 character 0 反序 0 反向, 改 1 character / 漏 1 character = 1 dict 扩展 0 闭环 0 漂动), **Stage 5 0 改 
prewarm cache key 1:1 严格分离契约** (T294 落地后 `audio_manager_enhanced.gd` 3 桶 prewarm cache key 14 key 已切 1:1 strict 0 漏 0 改 
0 改 1 character 0 漏 1 character 0 反序 0 反向, 改 1 character / 漏 1 character = 1 桶 扩展 0 cache key 0 闭环 0 漂动). 任何"加新 1 
cue"必须**先**按 Stage 1 cue 字典 1:1 加 1 entry (Stage 1 1:1 严格, 0 漏 1 entry 0 改 1 entry 0 反序 0 反向, **0 漏 1 段**), **再**按 
Stage 2 cue 引用 1:1 加 1 边 (Stage 2 1:1 严格, 0 漏 1 边 0 改 1 边 0 反序 0 反向), **再**按 Stage 3 verb → cue 映射 1:1 加 1 边 (Stage 3 
1:1 严格, 0 漏 1 边 0 改 1 边 0 反序 0 反向), **再**按 Stage 4 SFX dict 1:1 镜像 (Stage 4 1:1 严格, 0 漏 1 entry 0 改 1 character 0 反序 0 
反向), **再**按 Stage 5 prewarm cache key 1:1 镜像 (Stage 5 1:1 严格, 0 漏 1 key 0 改 1 character 0 反序 0 反向).
  2. **5 段 0 触碰边界** — `audio_presets.gd` cue 字典 + `audio_manager_enhanced.gd` cue 引用 + `_verb_ability_base.gd` verb → 
cue 映射 + `audio_presets.gd` SFX dict + `audio_manager_enhanced.gd` prewarm cache key 任何 1 件 0 改 0 删 0 重排 (5 段 0 触碰边界), 
任何"修 1 件"polish 走 Stage 1 1:1 严格 (改 件既有 5 段时**必须**保持 5 段 0 改 0 漏 0 反向 0 反序 0 触碰既有 1 件); 任何"加新 1 边"polish 走 Stage 2 1:1 
严格 (改 边既有 cue 引用 时**必须**保持 1 cue 引用 0 改 0 漏 0 反向 0 反序 0 漏 1 边); 任何"加新 1 边"polish 走 Stage 3 1:1 严格 (改 边既有 verb → cue 映射 
时**必须**保持 1 映射 0 改 0 漏 0 反向 0 反序 0 漏 1 边); 任何"加新 1 entry"polish 走 Stage 4 1:1 严格 (改 entry 既有 SFX dict 时**必须**保持 1 entry 
0 改 0 漏 0 反向 0 反序 0 漏 1 entry); 任何"加新 1 key"polish 走 Stage 5 1:1 严格 (改 key 既有 prewarm cache key 时**必须**保持 1 key 0 改 0 漏 
0 反向 0 反序 0 漏 1 key).
  3. **0 改 1 字段 0 漏 1 字段 0 反向** (T294 落地后 0 触碰边界) — 6 verb audio 家族 19 cue 字段扩展 5 段 (1 `audio_presets.gd` 19 cue 字典 + 1 
`audio_manager_enhanced.gd` 19 cue 引用 + 1 `_verb_ability_base.gd` 6 verb → audio cue 映射 + 1 `audio_presets.gd` 
`SFX_PRESETS` dict + 1 `audio_manager_enhanced.gd` 3 桶 prewarm cache key) 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反向, 改 1 
字段 / 漏 1 字段 / 反向 1 字段 = 1 cue / 1 dict / 1 映射 / 1 key 扩展 0 5 段 0 漂动, source-grep 验证 5 段 0 漏 0 改 1 字段 0 漏 1 字段 0 改 1 
character 0 反向.
  4. **0 改 1 边 0 漏 1 边 0 反向** (T294 落地后 0 触碰边界) — 接入契约 (1 cue 字典 1:1 严格 + 1 cue 引用 1:1 严格 + 1 verb → cue 映射 1:1 严格 + 1 
SFX dict 1:1 严格 + 1 prewarm cache key 1:1 严格) 1:1 严格分离 0 漏 0 改 0 改 1 边 0 漏 1 边 0 反向, 改 1 边 / 漏 1 边 / 反向 1 边 = 1 cue / 1 
dict / 1 映射 / 1 key 扩展 0 接入契约 0 漂动, source-grep 验证 5 段 0 触碰接入契约 0 漏 0 改 1 边 0 漏 1 边 0 改 1 character 0 反向.
  5. **T162 brittle 修复流程 0 触碰边界** — 6 verb audio 家族 19 cue 字段扩展 5 段 0 副作用 子段 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 改 1 character 
0 反向 0 漏 1 字段 0 触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构 (T162 
brittle 修复流程 = 1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 character 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 
锚点), 改 1 字段 / 漏 1 字段 / 反向 1 字段 = 1 cue / 1 dict / 1 映射 / 1 key 扩展 0 0 副作用 0 漂动 0 闭环, source-grep 验证 6 verb audio 家族 19 
cue 字段扩展 5 段 0 副作用 子段 0 漏 0 改 1 字段 0 改 1 character 0 反向 0 漏 1 字段 0 触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 
触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构.
  6. **1 套 polish 模式 0 互混 0 复用 0 共享 唯一性 标注 0 改 1 段 0 漏 1 段 0 反向** — §9.6.38 段 唯一性 标注 1:1 严格分离 0 漏 0 改 0 改 1 套 polish 模式 
0 漏 1 套 polish 模式 0 反向 0 触碰既有 0 改 1 段 (本节是 29 套 polish 模式**唯一**关注 6 verb audio 家族 19 cue 字段扩展 5 段 接入 1:1 严格分离契约 的 1 套, 
0 互混 0 复用 0 共享), 改 1 套 polish 模式 / 漏 1 套 polish 模式 / 反向 1 套 polish 模式 = 1 §9.6.38 段 0 唯一性 标注 0 漂动 0 闭环, source-grep 验证 
§9.6.38 段 唯一性 标注 0 漏 0 改 1 套 polish 模式 0 漏 1 套 polish 模式 0 改 1 character 0 反向.
  7. **§9.6.38 是 29 套 polish 模式**唯一**关注 "6 verb audio 家族 19 cue 字段扩展 5 段 接入 1:1 严格分离契约 (`audio_presets.gd` 19 cue 字典 (5 
verb fire + 5 verb cooldown tail + 5 verb cooldown ready + 4 misc) + `audio_manager_enhanced.gd` 19 cue 引用 + 
`_verb_ability_base.gd` 6 verb → audio cue 映射 + `audio_presets.gd` `SFX_PRESETS` dict + `audio_manager_enhanced.gd` 3 桶 
prewarm cache key) + cue 字典 1:1 严格 (19 entry 1:1 严格 0 漏 0 改) + cue 引用 1:1 严格 (19 cue 1:1 严格 0 漏 0 改) + verb → cue 映射 
1:1 严格 (6 verb 1:1 严格 0 漏 0 改) + SFX dict 1:1 严格 (5 entry 1:1 严格 0 漏 0 改) + prewarm cache key 1:1 严格 (14 key 1:1 严格 0 漏 
0 改) + 0 触碰游戏代码 + 0 触碰 §10 决策记录流程 0 触碰既有" 的 1 套** — 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / 
§9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / 
§9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 28 套 polish 模式 0 互混 0 复用 0 共享. 
§9.6.23 (T279 #199 落地) 关注 "prewarm aggregator 7 桶 5+1+1 canonical 1:1 序列" (与 6 verb audio 家族 19 cue 字段扩展 5 段 0 相关, 7 桶 
prewarm 0 涉及 cue 字典 / cue 引用 / verb → cue 映射 / SFX dict / prewarm cache key 5 段**之间**接入契约 自身 1:1 严格分离, §9.6.23 仅覆盖 
prewarm aggregator 7 桶单件, §9.6.38 覆盖 5 段**之间**接入契约), §9.6.35 (T291 #214 落地) 关注 "audio 数据层 (`MUSIC_PRESETS` 字典) preset 
字段扩展 5 段 canonical 1:1 严格分离契约" (与 6 verb audio 家族 19 cue 字段扩展 5 段 0 相关, 5 段是 audio 数据层 BGM preset 字段扩展 0 涉及 6 verb SFX 
cue 字段扩展 自身 1:1 严格分离, §9.6.35 与 §9.6.38 是 "姊妹段", 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 镜像 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格,
 一是 audio 数据层 BGM preset 字段扩展 5 段, 一是 6 verb audio 家族 19 cue 字段扩展 5 段), §9.6.37 (T293 #217 落地) 关注 "SaveSystem save data 
CRC32 校验 5 段 接入 1:1 严格分离契约" (与 6 verb audio 家族 19 cue 字段扩展 5 段 0 相关, 5 段是 save_system 持久化层 5 段 接入 0 涉及 6 verb audio 家族 
19 cue 字段扩展 自身 1:1 严格分离, §9.6.37 5 段与 §9.6.38 5 段 是 "姊妹段", 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 镜像 1 套 polish 模式 × 5 段 = 5 
元素 1:1 严格). 任何"加新 1 cue"或"加新 1 dict"或"加新 1 桶"必须**先**按 §9.6.38 Stage 1 cue 字典 1:1 严格 加 1 entry (Stage 1 1:1 严格, 0 漏 1 
entry 0 改 1 entry 0 反序 0 反向, **0 漏 1 段**), **再**按 §9.6.38 Stage 2 cue 引用 1:1 严格 加 1 边 (Stage 2 1:1 严格, 0 漏 1 边 0 改 1 边 
0 反序 0 反向), **再**按 §9.6.38 Stage 3 verb → cue 映射 1:1 严格 加 1 边 (Stage 3 1:1 严格, 0 漏 1 边 0 改 1 边 0 反序 0 反向), **再**按 
§9.6.38 Stage 4 SFX dict 1:1 严格 镜像 (Stage 4 1:1 严格, 0 漏 1 entry 0 改 1 character 0 反序 0 反向), **再**按 §9.6.38 Stage 5 
prewarm cache key 1:1 严格 镜像 (Stage 5 1:1 严格, 0 漏 1 key 0 改 1 character 0 反序 0 反向).
  8. 已知 drift risk: 6 verb audio 家族 19 cue 字段扩展 5 段 接入 **可能**漏 1 段 / 1 边 1:1 镜像 (e.g. 加新 1 cue 时漏 Stage 1 cue 字典 中 1 
entry 0 镜像, 1 cue 0 5 段 0 闭环; 加新 1 cue 时漏 Stage 2 cue 引用 中 0 镜像, 1 cue 0 cue 引用 0 闭环; 加新 1 cue 时漏 Stage 3 verb → cue 映射 
中 0 镜像, 1 cue 0 映射 0 闭环; 加新 1 cue 时漏 Stage 4 SFX dict 中 0 镜像, 1 cue 0 闭环; 加新 1 cue 时漏 Stage 5 prewarm cache key 中 0 镜像, 
1 cue 0 cache key 0 闭环). 监控建议: 任何"未来给 6 verb audio 家族 19 cue 字段扩展加新 1 cue / 加新 1 dict / 加新 1 桶"必须**先**在 5 段 (Stage 1 
cue 字典 1:1 严格 + Stage 2 cue 引用 1:1 严格 + Stage 3 verb → cue 映射 1:1 严格 + Stage 4 SFX dict 1:1 严格 + Stage 5 prewarm cache 
key 1:1 严格) 1:1 镜像 0 漏 1 段 / 0 漏 1 边 / 0 漏 1 entry / **0 漏 1 key**. T294 (#218) 5 段 1:1 镜像落地后任何新 cue / 新 dict / 新 桶 是 
§9.6.38 5 段的"母集" (1 cue / 1 dict / 1 桶 扩展 × 5 段 = 5 元素 1:1 严格, §9.6.38 5 段 1 套 polish 模式 完整闭环, 1 套 polish 模式 × 5 段 = 5 
元素 1:1 严格, 含 Stage 1 cue 字典 1:1 严格 1 套 polish 模式 5 段 0 漏 0 改 + Stage 2 cue 引用 1:1 严格 1 套 polish 模式 cue 引用 0 漏 0 改 + 
Stage 3 verb → cue 映射 1:1 严格 1 套 polish 模式 映射 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 件 + Stage 4 SFX dict 1:1 严格 1 套 polish 
模式 SFX dict 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 character + Stage 5 prewarm cache key 1:1 严格 1 套 polish 模式 prewarm cache 
key 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 character). 另 1 类已知 drift: Stage 1 cue 字典 1:1 严格 0 改 1 entry 0 漏 1 entry 0 反向 
(e.g. 后续 polish 期加新 1 cue 时漏 0 镜像 1 entry 任何 1 entry, 1 entry 0 cue 字典 0 闭环; 或漏 0 镜像 1 边 任何 1 character, 1 entry 0 cue 
引用 0 闭环; 或改 1 entry 0 改 1 entry 0 反向 0 触碰既有), 后续 polish 期若加新 1 cue 必须**先**用 1:1 镜像 5 段 模板, 0 改既有 0 漏 1 段 0 漏 1 边 0 反向 
**0 漏 1 entry**.

**0 副作用**: T294 (#218) 任何 const / var 0 触碰 (`audio_presets.gd` 0 改 0 删 0 重排 19 cue 字典任何 1 character; 
`audio_manager_enhanced.gd` 0 改 0 删 0 重排 19 cue 引用任何 1 character; `_verb_ability_base.gd` 0 改 0 删 0 重排 6 verb → audio 
cue 映射任何 1 character; `audio_presets.gd` 0 改 0 删 0 重排 `SFX_PRESETS` dict 任何 1 character; `audio_manager_enhanced.gd` 0 
改 0 删 0 重排 3 桶 prewarm cache key 任何 1 character; CONTRIBUTING.md §9.6.38 段 0 改 0 删 0 重排, 仅新加 1 段 (~50 行)); 0 触碰 §9.6.6 
ProfileRecentList 5 行 row 文本 5→7 字段 format 字符串扩展模式 (T294 0 改 §9.6.6 任何 1 character); 0 触碰 §9.6.7 ProfileRecentList 5 行 
row hover feedback 三件套 (T294 0 改 §9.6.7 任何 1 character); 0 触碰 §9.6.8 ProfileQuickStats 4 段独立 hover 联动 (T294 0 改 §9.6.8 
任何 1 character); 0 触碰 §9.6.9 AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` Dictionary 双源 (T294 0 改 §9.6.9 任何 1 
character); 0 触碰 §9.6.10 「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (T294 0 改 §9.6.10 任何 1 character); 0 触碰 §9.6.15 
SaveSystem audit_save_slots (T294 0 改 §9.6.15 任何 1 character); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T294 0 改 §9.6.16 
任何 1 character); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T294 0 改 §9.6.17 任何 1 character); 0 触碰 §9.6.18 16 件套 verb 
ability base (T294 0 改 §9.6.18 任何 1 character); 0 触碰 §9.6.19 7 件套 windup VFX base (T294 0 改 §9.6.19 任何 1 character); 0 
触碰 §9.6.20 Hub ↔ archive 跨房间 transition flow (T294 0 改 §9.6.20 任何 1 character); 0 触碰 §9.6.21 8 段成就序列 (T294 0 改 §9.6.21 
任何 1 character); 0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T294 0 改 §9.6.22 任何 1 character); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 
(T294 0 改 §9.6.23 任何 1 character); 0 触碰 §9.6.24 HUD 6 verb × 6 通道 = 36 通道 5 段序列 (T294 0 改 §9.6.24 任何 1 character); 0 触碰 
§9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 5 段序列 (T294 0 改 §9.6.25 任何 1 character); 0 触碰 §9.6.26 6 verb icon 视觉组 5 段 canonical 
1:1 序列 (T294 0 改 §9.6.26 任何 1 character); 0 触碰 §9.6.27 PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 5 段序列 (T294 0 改 
§9.6.27 任何 1 character); 0 触碰 §9.6.28 smoke test ITERATION_COUNT 跨迭代 + 段边界 find 5 段序列 (T294 0 改 §9.6.28 任何 1 character);
 0 触碰 §9.6.29 polish 文档化 5 段 canonical 1:1 序列 模板 + 4 段关系段 模板 (T294 0 改 §9.6.29 任何 1 character); 0 触碰 §9.6.30 工具链 3 件套 
1:1 严格分离契约 (T294 0 改 §9.6.30 任何 1 character); 0 触碰 §9.6.31 CHANGELOG 归档 3 件套 1:1 严格分离契约 (T294 0 改 §9.6.31 任何 1 
character); 0 触碰 §9.6.32 REVIEW_LOG 归档 3 件套 1:1 严格分离契约 (T294 0 改 §9.6.32 任何 1 character); 0 触碰 §9.6.33 6 verb 接入 4 件套 
字节码一致性 (T294 0 改 §9.6.33 任何 1 character); 0 触碰 §9.6.34 PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约 (T294 0 改 §9.6.34 
任何 1 character); 0 触碰 §9.6.35 AudioPresets `MUSIC_PRESETS` preset 字段扩展 5 段 canonical 1:1 严格分离契约 (T294 0 改 §9.6.35 任何 1 
character); 0 触碰 §9.6.36 PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约 (T294 0 改 §9.6.36 任何 1 character); 0 触碰 §9.6.37 
SaveSystem save data CRC32 校验 5 段 接入 1:1 严格分离契约 (T294 0 改 §9.6.37 任何 1 character); 0 触碰 check_smoke_consistency.sh 
(T294 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 
.git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, 0 触碰 `audio_presets.gd` 任何 1 character, 0 触碰 
`audio_manager_enhanced.gd` 任何 1 character, 0 触碰 `_verb_ability_base.gd` 任何 1 character); 0 行为变化, 1 文档 1 段 (§9.6.38 ~50 
行) + 0 字节码修改 (T294 仅文档化既有 6 verb audio 家族 19 cue 字段扩展 5 段 接入 1:1 严格分离契约, 0 改 `audio_presets.gd` 19 cue 字典 0 改 
`audio_manager_enhanced.gd` 19 cue 引用 0 改 `_verb_ability_base.gd` 6 verb → audio cue 映射 0 改 `SFX_PRESETS` dict 0 改 
prewarm SFX cache key 0 触碰既有) + 1 smoke test (T294 70+ 断言).

**与 §9.6.35 关系** — §9.6.35 (T291 #214 落地) 关注 "AudioPresets `MUSIC_PRESETS` preset 字段扩展 5 段 canonical 1:1 严格分离契约" (5 段 = 
preset 字段 5 段 canonical, 5 段覆盖 audio 数据层 0 涉及 6 verb audio 家族 19 cue 字段扩展). §9.6.38 关注 "6 verb audio 家族 19 cue 字段扩展 5 段 
接入 1:1 严格分离契约" (5 段覆盖 6 verb audio 家族 19 cue 字段扩展 0 涉及 audio 数据层 preset 字段 5 段). §9.6.35 与 §9.6.38 0 互混 0 复用 0 共享: 
§9.6.35 关注 "audio 数据层 preset 字段扩展 5 段" (与 6 verb audio 家族 19 cue 字段扩展 5 段 0 相关, 5 段 0 涉及 cue 字典 / cue 引用 / verb → cue 
映射 / SFX dict / prewarm cache key 5 段), §9.6.38 关注 "6 verb audio 家族 19 cue 字段扩展 5 段 接入 1:1 严格分离契约 5 段 1:1 视觉组连贯" (与 
audio 数据层 preset 字段 5 段 0 相关, 5 段 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格). 2 套 polish 
模式 是 "姊妹段" (1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 镜像 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格, 一是 6 verb audio 家族 19 cue 字段扩展 5 段 
接入, 一是 audio 数据层 preset 字段扩展 5 段; 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 + 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格). 任何"加新 1 
cue"必须**先**按 §9.6.38 5 段 1:1 复制 (5 段 1:1 严格, 0 漏 1 段 0 改 1 字段 0 反向) **或** 按 §9.6.35 5 段 1:1 复制 (5 段 1:1 严格, 0 漏 1 段 0 改 
1 字段 0 反向), **再**按 §9.6.38 5 段 1:1 同步 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 character 0 反向 0 触碰既有 §9.6.35 任何 1 
character) **或** 按 §9.6.35 5 段 1:1 同步 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 character 0 反向 0 触碰既有 §9.6.38 任何 1 
character).

**与 §9.6.37 关系** — §9.6.37 (T293 #217 落地) 关注 "SaveSystem save data CRC32 校验 5 段 接入 1:1 严格分离契约" (5 段 = crc32 字段 + verify 
方法 + audit 巡检 + write dict + read dict, 5 段覆盖 SaveSystem save data CRC32 校验 5 段 0 涉及 6 verb audio 家族 19 cue 字段扩展). 
§9.6.38 关注 "6 verb audio 家族 19 cue 字段扩展 5 段 接入 1:1 严格分离契约" (5 段覆盖 6 verb audio 家族 19 cue 字段扩展 0 涉及 SaveSystem save data 
CRC32 校验 5 段). §9.6.37 与 §9.6.38 0 互混 0 复用 0 共享: §9.6.37 关注 "SaveSystem save data CRC32 校验 5 段" (与 6 verb audio 家族 19 
cue 字段扩展 5 段 0 相关, 5 段 0 涉及 cue 字典 / cue 引用 / verb → cue 映射 / SFX dict / prewarm cache key 5 段), §9.6.38 关注 "6 verb 
audio 家族 19 cue 字段扩展 5 段 接入 1:1 严格分离契约 5 段 1:1 视觉组连贯" (与 SaveSystem save data CRC32 校验 5 段 0 相关, 5 段 1:1 严格 镜像 1 套 
polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格). 2 套 polish 模式 是 "姊妹段" (1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 镜像 
1 套 polish 模式 × 5 段 = 5 元素 1:1 严格, 一是 6 verb audio 家族 19 cue 字段扩展 5 段 接入, 一是 SaveSystem save data CRC32 校验 5 段 接入; 1 套 
polish 模式 × 5 段 = 5 元素 1:1 严格 + 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格). 任何"加新 1 cue"必须**先**按 §9.6.38 5 段 1:1 复制 (5 段 1:1 严格,
 0 漏 1 段 0 改 1 字段 0 反向) **或** 按 §9.6.37 5 段 1:1 复制 (5 段 1:1 严格, 0 漏 1 段 0 改 1 字段 0 反向), **再**按 §9.6.38 5 段 1:1 同步 (5 段 
1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 character 0 反向 0 触碰既有 §9.6.37 任何 1 character) **或** 按 §9.6.37 5 段 1:1 同步 (5 段 
1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 character 0 反向 0 触碰既有 §9.6.38 任何 1 character).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 
character 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 轮 0 后续 持续收敛), §9.6.38 是"6 verb audio 家族 19 cue 字段扩展 5 段 接入 1:1 严格分离契约" (5 段 
1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约). T162 与 §9.6.38 0 互混 0 复用 0 共享: T162 关注 "brittle 修复流程 5 步骤" (1 expect 反转 + 1 
docblock 说明 + 1 段 find 反转 + 0 触碰既有 + 0 改 1 字段 0 改 1 character 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 步骤 1:1 严格), §9.6.38 关注 
"5 段 1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约" (与 5 步骤 0 相关, 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离). 
任何"加新 1 brittle 修复"必须**先**按 T162 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), **再**按 
§9.6.38 5 段 1:1 镜像 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 character 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.38 5 段走 §9.1 9 步落地的 1 步 (`audio` 步 8, 6 verb audio 家族 19 cue 字段扩展 5 段 是 audio 
步 范畴, 0 走其他 8 步), 0 触碰其他 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG 
+ 1 `vfx` 视觉组 + 1 `HUD 冷光勾边` 渲染). 任何"加新 1 cue"必须**先**按 §9.6.38 5 段 1:1 镜像 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 
character 0 反向 0 触碰既有 8 步 (`verb` 枚举 + `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `vfx` 视觉组 + 
`HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 步"必须**先**在 §9.6.38 5 段 加 1 边 (1 cue 字典 entry 或 1 cue 引用 边 或 1 verb → cue 映射 边 或 1 SFX 
dict entry 或 1 prewarm cache key), 0 触碰既有 9 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 
系统 + 1 `icon` PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).
