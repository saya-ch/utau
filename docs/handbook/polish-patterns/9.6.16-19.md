# Polish Patterns 9.6.16–19

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.16–9.6.19，共 4 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.16-19.md

<!-- shard 16-19 共 4 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.16 player.gd 6 verb hit handler 5 件套 1:1 polish 模式 (T098 + T170c + T170d + T181 + T251 + T252 跨 6 任务 ~90 轮落地) 
文档化

> T251 (#169) + T270 (#189) 跨 6 任务在 `player.gd` 落地 6 verb hit handler (`_on_pulse_hit` / `_on_cut_hit` / `_on_bind_hit` 
> / `_on_echo_hit` / `_on_wave_hit` / `_on_whisper_hit`)，跨 4 autoload (ScreenShake / AudioManagerEnhanced / 
> Pulse/Cut/Bind 5 verb VFX 家族 / Whisper 6 verb VFX 家族) live-push 1:1 polish 模式. 任何"加新 1 verb 命中反馈" (e.g. 6 verb 升级 7 
> verb / Echo reflect 升级 2 段 reflect) 时**必须**严格按 5 件套 1:1 复制既有模式 (0 漏 0 改 1 字符).

**症状 / 触发场景 / 修复 / 预防** 4 段完整结构：

1. **症状** — 6 verb hit handler 行为漂移，5 反馈通道 (1 `if target == null: return` 守卫 + 1 屏染 / VFX flash + 1 屏抖 / VFX pulse + 1 
音频 + 1 跨 autoload 守卫) 任一漏 → 玩家命中反馈缺失 / 屏抖 0 触发 / SFX 0 响 / 跨类 NPE / 视觉组语义破坏.
2. **触发场景** — polish 期加 1 段新 1 verb 命中反馈 (e.g. T181 加 Pulse 命中 SFX / T170c 加 4 verb 屏抖 / T170d 加 Cut 屏染 / T251 录 4 verb 
屏染查表 / T270 加 Whisper L6 EXPANDING_RING), 5 件套任一漏 0 拍打 → 命中"哑火"/反馈"延迟"/视觉"漂移".
3. **修复** — 严格按 5 件套 1:1 复制既有模式:
   - **`target == null` 守卫** 1 行 — `_on_*_hit` 第 1 行 (pulse_ability.gd:126 末尾 emit(null, ...) 是占位 emit, 应不触发屏闪). 任何"加新 
1 verb 命中"必须 0 漏.
   - **屏染 1 行** (4 verb: Pulse / Cut / Bind / Echo reflect) — `ScreenShake.flash_color(ScreenShake.VERB_HIT_X_COLOR, 
duration, peak)` 走 §F009 4 verb 命中色查表 (T251 落地). 1 verb 屏染查表 0 漏 0 改 1 字符 (5 verb Wave / 6 verb Whisper 走独立 VFX 系统, 0 
触发屏染).
   - **屏抖 1 行** (4 verb) — `ScreenShake.shake_preset(ScreenShake.Preset.LIGHT)` 1.0/0.08s 屏抖. 1 verb 屏抖 0 漏 0 改 1 字符 (5 
verb Wave / 6 verb Whisper 走独立 VFX 系统, 0 触发屏抖, 避免和 5 verb ring / 6 verb sphere 视觉组冲突).
   - **VFX flash 1 行** (5 verb Wave / 6 verb Whisper + Echo reflect) — Wave `add_hit_flash(target.global_position)` / 
Whisper `flash_hit(target.global_position)` / Echo reflect `add_bounce_flash(target.global_position)` 走 5 verb ring / 6 
verb sphere / Echo VFX 独立系统. 4 verb 走屏染, 5+1 verb 走 VFX flash, **0 互混 0 复用 0 共享**.
   - **音频 1 行** (T181 throttled) — `if AudioManagerEnhanced and AudioManagerEnhanced.has_method("play_X_hit"): 
AudioManagerEnhanced.play_X_hit()`. 1 verb 音频 + has_method 守卫 0 漏 0 改 1 字符 (Echo reflect 0 触发音频 / Wave 0 触发音频 / Whisper 
0 触发音频, 避免 5 verb + 1 verb 音频组和 4 verb 命中音频冲突).
   - **跨类守卫** — `if ScreenShake and ScreenShake.has_method("flash_color")` / `if AudioManagerEnhanced and 
AudioManagerEnhanced.has_method("play_X_hit")` 2 处 has_method 守卫 (§9.6.1 跨类 handler `is_instance_valid + has_method` 
双守卫) 0 漏 0 改 1 字符.
4. **预防** — 任何 polish 期给 6 verb hit handler 加新 1 反馈通道 (e.g. 加 1 verb / 加屏染 / 加屏抖 / 加 VFX flash / 加音频) 时**必须**严格按 5 件套 
1:1 复制既有模式, 0 漏 0 改 1 字符. 0 触碰 _on_pulse_hit / _on_cut_hit / _on_bind_hit / _on_echo_hit / _on_wave_hit / 
_on_whisper_hit 既有 5 件套, 0 触碰 `VERB_HIT_PULSE_COLOR` / `VERB_HIT_CUT_COLOR` / `VERB_HIT_BIND_COLOR` / 
`VERB_HIT_ECHO_COLOR` 4 verb 屏染查表, 0 触碰 `add_hit_flash` / `flash_hit` / `add_bounce_flash` 5 verb + 1 verb + Echo 
reflect VFX flash 接口, 0 触碰 `play_pulse_hit` / `play_cut_hit` / `play_bind_hit` 3 verb 音频接口.

**5 verb + 1 verb 6 事件 handler 跨 4 autoload 1:1 严格分离** (T270 #189 落地后, 6 verb hit handler 6 函数 0 互混 0 复用 0 共享, 4 verb 
走屏染 + 屏抖 + 音频 3 件套 / 5 verb 走 VFX flash 1 件套 / 6 verb 走 VFX flash 1 件套 / Echo 走 2 路径 reflect 屏染 + VFX flash 1 件套, 4 
verb + 5 verb + 1 verb + Echo reflect 4 套视觉语义 0 互混):

| Verb | 函数 | target 守卫 | 屏染 | 屏抖 | VFX flash | 音频 |
|------|------|------|------|------|------|------|
| Pulse | `_on_pulse_hit` | ✓ | ✓ VERB_HIT_PULSE_COLOR | ✓ LIGHT | ✗ | ✓ play_pulse_hit |
| Cut | `_on_cut_hit` | ✗ (接 `_target` 未用) | ✓ VERB_HIT_CUT_COLOR | ✓ LIGHT | ✗ | ✓ play_cut_hit |
| Bind | `_on_bind_hit` | ✓ | ✓ VERB_HIT_BIND_COLOR | ✓ LIGHT | ✗ | ✓ play_bind_hit |
| Echo (non-reflect) | `_on_echo_hit` reflect=false | ✗ (reflect 分支守卫) | ✓ VERB_HIT_ECHO_COLOR 0.06/0.12 温和 | ✗ | ✗ | ✗ 
| |
| Echo (reflect) | `_on_echo_hit` reflect=true | ✗ (reflect 分支守卫) | ✓ VERB_HIT_ECHO_COLOR 0.08/0.20 强化 | ✗ | ✓ 
| add_bounce_flash | ✓ play_echo_hit |
| Wave | `_on_wave_hit` | ✗ (走 VFX 守卫) | ✗ (5 verb 独立 ring 系统) | ✗ (避免 ring + 抖 视觉冲突) | ✓ add_hit_flash | ✗ (5 verb 命中 
| = 圆环扩散, 0 额外 SFX) |
| Whisper | `_on_whisper_hit` | ✗ (走 VFX 守卫) | ✗ (6 verb 独立 sphere 系统) | ✗ (避免 sphere + 抖 视觉冲突) | ✓ flash_hit | ✗ (6 
| verb 命中 = sphere 闪, 0 额外 SFX) |


### 9.6.17 player.gd `_ready` 6 verb 信号桥接 5+1 件套 1:1 polish 模式 (D002.B + T098 + T158 + T146 + T170a + T170c + T170d + 
T181 + T147 + T142 + D001 + F013.E + T251 + T270 跨 14 任务 ~95 轮落地) 文档化

> D002.B (#98) + F013.E (#159) + T098 (#43) + T146 (#76) + T147 + T158 (#81) + T170a (#88) + T170c + T170d + T181 (#97) 
> + T142 (#75) + D001 (#82) + T251 (#169) + T270 (#189) 跨 14 任务在 `player.gd _ready` 落地 6 verb signal 桥接 
> (`_on_pulse_fired` / `_on_bind_fired` / `_on_cut_fired` / `_on_echo_fired` / `_on_wave_fired` / `_on_whisper_fired` 6 
> 主信号 + 6 命中信号 + 4 结束信号 + 2 扩展信号 + 1 PlayerActionGate autoload register), 5+1 件套 1:1 严格分离. 任何"加新 1 verb" (e.g. 6 verb 
> 升级 7 verb) 或"加新 1 信号" (e.g. 5 verb `*_expired` / 6 verb `*_expired`) 时**必须**严格按 5+1 件套 1:1 复制既有模式 (0 漏 0 改 1 字符).

**症状 / 触发场景 / 修复 / 预防** 4 段完整结构：

1. **症状** — 6 verb signal 桥接漂移, 5+1 件套任一漏 → 玩家 verb 触发 0 反馈 / 命中 0 触发反馈 / 结束 0 触发清理 / 扩展信号 (combo / multi_reflect) 0 触发 
/ PlayerActionGate 反向查询 0 命中 / `_exit_tree` 对称 unregister 0 调 → 玩家死亡后 verb 锁定状态泄漏 / 玩家转场景时 6 verb 残留 VFX 0 清理.
2. **触发场景** — polish 期加新 1 verb 信号桥接 (e.g. F013.E #159 加 Whisper 6 verb 信号桥接 / T158 #81 加 Echo multi_reflect / T146 #76 
加 Wave combo / T098 #43 加 Pulse/Cut 屏染信号 / T170a #88 加 Bind 屏染信号), 5+1 件套任一漏 0 拍打 → 反馈"哑火"/信号"延迟"/"对称" 失配.
3. **修复** — 严格按 5+1 件套 1:1 复制既有模式 (per verb):
   - **`if verb_ability:` null guard 1 行** — `@onready var verb_ability` 在 player.tscn 子节点上存在时 (`$VerbAbility` 路径 0 改), 
任何"加新 1 verb"必须新增 1 个 `@onready` 字段 (5 verb + 1 verb = 6 verb) + 1 个 null guard 行. 1 verb null guard 0 漏 0 改 1 字符.
   - **主信号 `verb_fired.connect(_on_verb_fired)` 1 行** — 6 verb × 1 行 = 6 行. Pulse / Bind / Cut / Echo / Wave / Whisper 
6 主信号 0 互混 0 复用 0 共享. 任何"加新 1 verb"必须加 1 主信号 (e.g. Whisper 加 `whisper_fired.connect(_on_whisper_fired)` 1 行).
   - **命中信号 `verb_hit.connect(_on_verb_hit)` 1 行** — 6 verb × 1 行 = 6 行. 3 verb (Pulse / Bind / Cut, T098 + T170a 落地) 走 
`if verb_ability.has_signal("verb_hit"):` has_signal 守卫 0 漏 (signal 是 polish 期加的, 0 触碰早期 save 兼容). 3 verb (Echo / Wave 
/ Whisper, T103 + T146 + F013.E 落地) 0 走 has_signal 守卫 (signal 是基础 verb 接入路径 0 漏的, 早期 save 0 兼容) — 3+3 视觉语义 0 互混 0 复用 0 
共享. 1 verb 命中信号 0 漏 0 改 1 字符.
   - **结束信号 `verb_expired.connect(_on_verb_expired)` 1 行** — 4 verb × 1 行 = 4 行 (Echo / Wave 加 verb_expired 结束信号). 2 
verb (Pulse / Bind / Cut / Whisper) 0 触发结束信号 — Echo / Wave 走"持续扩散期"语义, 0 互混 0 复用 0 共享. 任何"加新 1 verb"若是持续扩散期语义必须加 1 结束信号,
 若是瞬发语义 (Pulse / Bind / Cut / Whisper) 0 加.
   - **扩展信号 `if verb_ability.has_signal("verb_extra"):` has_signal 守卫 1 行** — 2 verb × 1 行 = 2 行 (Echo multi_reflect 
T158 #81 + Wave combo T146 #76). has_signal 守卫防 pre-existing save 0 兼容 (signal 是 polish 期加的, 早期 save 缺该 signal). 任何"加新 
1 扩展信号"必须走 has_signal 守卫 0 漏 0 改 1 字符.
   - **`PlayerActionGate.register_player(self)` 1 行** (D001 #82) — 1 个 autoload register 入口. 守卫 `if 
Engine.has_singleton("PlayerActionGate") or _has_player_action_gate_autoload():` 0 漏 (headless 测试 0 autoload 0 NPE). 
任何"加新 1 autoload 反向查询"必须仿 `PlayerActionGate.register_player(self)` 0 漏 0 改 1 字符.
   - **对称 `_exit_tree` `PlayerActionGate.unregister_player(self)` 1 行** (D001 #82) — 1 个对称 unregister 入口. 守卫同 `if 
Engine.has_singleton("PlayerActionGate") or _has_player_action_gate_autoload():` 0 漏. 任何"加新 1 autoload 反向查询"必须仿 
`_exit_tree` 对称 unregister 0 漏 0 改 1 字符.
4. **预防** — 任何 polish 期给 6 verb signal 桥接加新 1 件套 (e.g. 加 1 verb / 加主信号 / 加命中信号 / 加结束信号 / 加扩展信号 / 加 PlayerActionGate 
register) 时**必须**严格按 5+1 件套 1:1 复制既有模式, 0 漏 0 改 1 字符. 0 触碰 6 verb `@onready` 字段顺序 (`pulse_ability` / `bind_ability` / 
`cut_ability` / `echo_ability` / `wave_ability` / `whisper_ability`), 0 触碰 `_ready` 末尾 
`PlayerActionGate.register_player(self)` 守卫, 0 触碰 `_exit_tree` 末尾 `PlayerActionGate.unregister_player(self)` 守卫, 0 触碰 
§9.6.16 6 verb hit handler 5 件套 (`_on_pulse_hit` / `_on_cut_hit` / `_on_bind_hit` / `_on_echo_hit` / `_on_wave_hit` / 
`_on_whisper_hit` 6 函数 0 互混 0 复用 0 共享), 0 触碰 §9.1 6 verb 接入路径 9 步 (`verb` / `ability` / `const` / `input` / `shop` / 
`icon` / `vfx` / `audio` / `HUD 冷光勾边`).

**5+1 件套 1:1 严格分离总表** (T270 #189 落地后, 6 verb signal 桥接 5+1 件套 0 互混 0 复用 0 共享, 1 玩家 + 6 verb 主信号 + 6 verb 命中信号 + 4 verb 
结束信号 + 2 verb 扩展信号 + 1 PlayerActionGate register 14 行 + 1 对称 unregister 1 行 = 15 行 + 守卫):

| Verb | null guard | 主信号 | 命中信号 | 结束信号 | 扩展信号 |
|------|------|------|------|------|------|
| Pulse | ✓ `if pulse_ability:` | ✓ `pulse_fired` | ✓ `pulse_hit` (T098 has_signal 守卫) | ✗ (瞬发语义) | ✗ |
| Bind | ✓ `if bind_ability:` | ✓ `bind_fired` | ✓ `bind_hit` (T170a has_signal 守卫) | ✗ (瞬发语义) | ✗ |
| Cut | ✓ `if cut_ability:` | ✓ `cut_fired` | ✓ `cut_hit` (T098 has_signal 守卫) | ✗ (瞬发语义) | ✗ |
| Echo | ✓ `if echo_ability:` | ✓ `echo_fired` | ✓ `echo_hit` (基础接入, 0 has_signal 守卫) | ✓ `echo_expired` (持续扩散) | ✓ 
| `echo_multi_reflect` (T158 has_signal 守卫) |
| Wave | ✓ `if wave_ability:` | ✓ `wave_fired` | ✓ `wave_hit` (基础接入, 0 has_signal 守卫) | ✓ `wave_expired` (0.4s 扩散期) | ✓ 
| `wave_combo` (T146 has_signal 守卫) |
| Whisper | ✓ `if whisper_ability:` | ✓ `whisper_fired` | ✓ `whisper_hit` (基础接入, 0 has_signal 守卫) | ✗ (0.15s 静默场, 
| 不走扩散期) | ✗ |
| PlayerActionGate | ✓ `if Engine.has_singleton or _has_player_action_gate_autoload():` | 
| `PlayerActionGate.register_player(self)` (`_ready` 末尾) | n/a | n/a | `PlayerActionGate.unregister_player(self)` 
| (`_exit_tree` 末尾, 对称 unregister) |

**与 §9.6.16 关系** — §9.6.16 覆盖 6 verb hit handler 5 件套 (target 守卫 / 屏染 / 屏抖 / VFX flash / 音频), §9.6.17 覆盖 6 verb signal 
桥接 5+1 件套 (null guard / 主信号 / 命中信号 / 结束信号 / 扩展信号 / PlayerActionGate). §9.6.16 关注"信号触发后 handler 函数体 5 件套", §9.6.17 
关注"`_ready` 阶段 signal connect 5+1 件套". 任何"加新 1 verb"必须**先**按 §9.6.17 在 `_ready` 加 5+1 件套 (signal connect), **再**按 
§9.6.16 在 player.gd 加 5 件套 hit handler (`_on_*_hit` 函数体). 0 反序 0 漏 0 改 1 字符. 0 触碰 §9.6.16 6 verb hit handler 任何 const / 
var / 函数体 (5+1 件套 §9.6.17 + 5 件套 §9.6.16 = 11 件套 1:1 严格分离, 0 互混 0 复用 0 共享, 任何"加新 1 verb"必须双 1:1 复制 11 件套).

**与 §9.1 6 verb 接入路径 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` 
(6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.17 5+1 件套走"ability (2)" 步 (`@onready var verb_ability` 字段) + §9.1 
9 步落地的"signal connect" 链 (per verb 主信号 / 命中信号 / 结束信号 / 扩展信号). 任何"加新 1 verb"必须**先**按 §9.1 9 步落地, **再**按 §9.6.17 5+1 件套 
1:1 复制 (0 反序 0 漏 0 改 1 字符). §9.6.16 5 件套是"signal 触发后 handler 内部 5 件套" (与 §9.1 9 步平行, 0 互混). §9.6.17 5+1 件套是"signal 
connect 阶段 5+1 件套" (与 §9.1 9 步同步, 0 复用). 0 触碰 §9.1 9 步任何 const / var / 节点 (5+1 件套仅在 `_ready` 末尾 + `_exit_tree` 末尾, 0 触碰 
§9.1 9 步任何位置).


### 9.6.18 `_verb_ability_base.gd` 共享契约 1:1 严格分离 polish 模式 (F007 #87 + D002.B #98 + H001 #99 + T166 #85 + T173 #92 + 
T181 #97 + T270 #189 跨 7 任务 ~95 轮落地) 文档化

> F007 (#87) + D002.B (#98) + H001 (#99) + T166 (#85) + T173 (#92) + T181 (#97) + T270 (#189) 跨 7 任务在 
> `src/scripts/_verb_ability_base.gd` (`class_name VerbAbilityBase extends Node`) 落地 6 verb ability (PulseAbility / 
> BindAbility / CutAbility / EchoAbility / ResonanceWaveAbility / WhisperAbility) 共享契约 (6 字段 + 1 `@onready` + 2 
> `@export` + 6 共享方法 + 1 `_exit_tree` 兜底) 1:1 严格分离. 任何"加新 1 verb" (e.g. 6 verb 升级 7 verb) 或"重构共享契约" (e.g. 把 6 verb 
> `is_globally_blocking` 提到 base) 时**必须**严格按 1:1 共享契约复制既有模式 (0 漏 0 改 1 字符). 跳出 §9.6.16 6 verb hit handler 5 件套 + 
> §9.6.17 6 verb signal 桥接 5+1 件套 范畴 记录 **verb ability 类层级共享契约** polish 模式.

**症状 / 触发场景 / 修复 / 预防** 4 段完整结构：

1. **症状** — `_verb_ability_base.gd` 共享契约漂移, 6 verb ability 子类 (Pulse / Bind / Cut / Echo / Wave / Whisper) 6 件共享契约任一漏 → 
玩家 verb 共享状态 (cooldown_timer / windup_timer / is_winding_up / pending_origin / pending_direction / windup_vfx) 重复实现 / 
字段 shadow / parse 错误 / Godot 4 静态分析 `The member 'cooldown' already exists in parent class` 类 pre-existing 风险 / 
`_exit_tree` 兜底 VFX 清理丢失 (Wave #92 修复前 case) / `is_globally_blocking` 跨 D001 PlayerActionGate autoload 缺接口.
2. **触发场景** — polish 期加新 1 verb (e.g. F013.E #159 加 Whisper 6 verb) 或重构共享契约 (e.g. H001 #99 把 `cooldown` / `windup_time` 
从 5 verb 各自 `@export` 集中到 base), 共享契约 1:1 严格分离任一漏 0 拍打 → 字段 shadow parse 错误 / VFX 兜底清理丢失 / autoload 跨类反查 0 命中 / 共享方法 6 
verb 各自 byte-identical copy 漂移.
3. **修复** — 严格按 6 件共享契约 1:1 复制既有模式 (`_verb_ability_base.gd` 是 canonical 副本, 6 verb 各自 extends 继承 0 重写):
   - **6 共享字段** 0 重声明 — `_cooldown_timer: float = 0.0` (T181 #97 cooldown decrement) + `_windup_timer: float = 0.0` 
(T166 #85 windup state) + `_is_winding_up: bool = false` (T142 #75 anti-misinput) + `_pending_origin: Vector2 = 
Vector2.ZERO` (T166 #85 windup → execute 桥接) + `_pending_direction: Vector2 = Vector2.ZERO` (T166 #85 windup → execute 
桥接) + `_windup_vfx: Node2D = null` (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T173.C #92 windup VFX 句柄). 6 verb 
子类**必须** 0 重声明 (会 shadow base 字段破坏 lifecycle contract). 任何"加新 1 verb 共享字段"必须严格在 base 加 1 个字段 + 6 verb 各自 0 改 0 重声明 0 
shadow.
   - **1 `@onready` 共享 `_player`** 0 重声明 — `@onready var _player: CharacterBody2D = get_parent() as CharacterBody2D` 
(resolve player 父节点, `_ready` 第 1 步 assert 非 null). 6 verb 子类**必须** 0 重声明 (与 base 共享). 任何"加新 1 verb" 0 触碰.
   - **2 `@export` 共享 `cooldown` / `windup_time`** 0 重声明 (H001 #99 落地) — `@export var cooldown: float = 0.5` (默认 Pulse 
值, "feels-good floor", 0 override 时子类继承) + `@export var windup_time: float = 0.10` (默认 Pulse 值). **H001 #99 关键踩坑**: 5 
verb 各自 `@export var cooldown` 重声明触发 Godot 4 静态分析 `The member 'cooldown' already exists in parent class` 错误, 5 verb 
ability parse 阶段 1 次性破. 修复: 严格 0 重声明, .tscn 端 override (player.tscn Pulse cooldown=0.5 / Bind=1.2 / Cut=0.8 / Echo=4.0 
/ Wave=6.0 / Whisper=8.0 1:1). 任何"加新 1 verb" 0 重声明, 仅在 .tscn 端 override.
   - **6 共享方法 0 重写 (除 `is_globally_blocking` 2 verb 重写)** — `_consume_verb_cost(cost: int) -> bool` (F007 #87 cost 
consumption, `GameState` autoload null 守卫) + `_setup_windup_state(origin, direction)` (F007 #87 windup 4 字段赋值) + 
`_process_cooldown(delta, verb_name)` (T181 #97 cooldown decrement + jingle 守卫, 6 verb 各自 opt-in 调 
`_process_cooldown(delta, "<verb>")`) + `get_cooldown_ratio() -> float` (HUD 4 verb cooldown bar 0 调, `cooldown <= 0` 
守卫 0 除零) + `is_winding_up() -> bool` (D001 #82 PlayerActionGate autoload 反查入口) + `_exit_tree()` (T173 #92 兜底 VFX 
`fade_out_and_free()` 1 字段 null guard). 4 verb (Pulse / Bind / Cut / Echo) **必须** 0 重写 5 件套方法, 1 verb 显式重写 0 触碰 base 
既有实现 (resonance_wave_ability.gd 重复 `get_cooldown_ratio` + `is_winding_up` byte-identical copy 是 T270 #189 重构目标, 1 verb 
重复 0 触法 6 verb 共享契约 0 漏).
   - **`is_globally_blocking() -> bool` 扩展接口 (D001 #82)** — 2 verb (Wave / Whisper) 重写, 4 verb (Pulse / Bind / Cut / 
Echo) 0 需 (走 base `is_winding_up` 或自家 `is_shield_active` 等 1 字段). Wave 0.10s windup 期间需阻断其他 4 verb (D001 #82 5-verb 
chain anti-misinput). Whisper 0.10s windup 期间也走 anti-misinput 链抑制 (T270 #189). 任何"加新 1 verb"若是持续扩散期或 windup 期间需 
anti-misinput 必须 override 1 个 `is_globally_blocking` 入口 (1 行 `return _is_winding_up`).
   - **`super._ready()` / `super._exit_tree()` 1:1 调用顺序** — 6 verb 子类 `_ready` 必须 `super._ready()` 首行调用 (base 1 行 
`_player` resolve + assert, 然后子类加 verb-specific perk application), 0 改顺序 0 漏 super. `_exit_tree` 仅 1 verb (Wave T173 
#92 之前漏) 显式 super 调用, 5 verb 0 override (base `_exit_tree` 兜底 `_windup_vfx.fade_out_and_free()` + null 1:1). 任何"加新 1 
verb"必须 0 漏 `super._ready()` 首行, `super._exit_tree()` 0 必 (base 1:1 兜底, 0 重复).
4. **预防** — 任何 polish 期给 6 verb ability 子类 (Pulse / Bind / Cut / Echo / Wave / Whisper) 加新 1 共享契约字段 (e.g. `_shared_fx`) 
或方法 (e.g. `_shared_fx_apply`) 时**必须**严格按 1:1 共享契约复制既有模式, 0 漏 0 改 1 字符. 0 重声明 base 6 字段 (`_cooldown_timer` / 
`_windup_timer` / `_is_winding_up` / `_pending_origin` / `_pending_direction` / `_windup_vfx`), 0 重声明 1 `@onready` 
(`_player`), 0 重声明 2 `@export` (`cooldown` / `windup_time`, H001 #99 关键踩坑), 0 重写 5 件套方法 (`_consume_verb_cost` / 
`_setup_windup_state` / `_process_cooldown` / `get_cooldown_ratio` / `is_winding_up`), 0 漏 `super._ready()` 首行 (1 字段 + 
1 assert), 0 漏 `super._exit_tree()` 兜底 (Wave T173 #92 修复前 case, 5 verb 0 override 0 漏). 0 触碰 `is_globally_blocking` 2 
verb (Wave / Whisper) 重写接口 (D001 #82 PlayerActionGate 反查入口, 0 改 0 删 0 重命名).

**6 件共享契约 1:1 严格分离总表** (D002.B #98 落地后, 6 verb ability 子类 extends VerbAbilityBase 共享契约 0 互混 0 复用 0 共享, 6 字段 + 1 
`@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 兜底 = 15 件 + 守卫, 0 触碰 0 重写 0 重声明):

| 共享契约 | 数量 | base 落地 | 6 verb 子类复制 | 0 触碰边界 |
|------|------|------|------|------|
| 共享字段 | 6 (cooldown_timer / windup_timer / is_winding_up / pending_origin / pending_direction / windup_vfx) | D002.B 
| #98 + T166 #85 + T173 #92 | 0 重声明 (会 shadow) | 6 verb 0 改 0 删 0 重排 |
| `@onready` | 1 (`_player`) | D002.B #98 | 0 重声明 | 6 verb 0 改 0 删 0 重排 |
| `@export` | 2 (`cooldown` / `windup_time`) | H001 #99 hotfix (5 verb 各自重声明触发 parse 错误) | 0 重声明 (.tscn 端 override) | 6 
| verb 0 重声明 (1 字段 1:1) |
| 共享方法 | 5 (`_consume_verb_cost` / `_setup_windup_state` / `_process_cooldown` / `get_cooldown_ratio` / 
| `is_winding_up`) | F007 #87 + T181 #97 + D001 #82 | 4 verb 0 重写 (Pulse / Bind / Cut / Echo), 1 verb 显式重写 (Wave 
| byte-identical 重复, T270 #189 重构目标) | 0 改 base 既有 5 方法 |
| `is_globally_blocking` 扩展 | 1 (D001 #82) | 0 提供 (基类无此方法) | 2 verb 重写 (Wave / Whisper), 4 verb 0 需 (走 base 
| `is_winding_up` 或自家 `is_shield_active`) | 0 改 0 删 0 重命名 |
| `_exit_tree` 兜底 | 1 (T173 #92) | base 1:1 提供 `fade_out_and_free()` | 5 verb 0 override, 1 verb (Wave) T173 #92 之前漏, 
| T173.C 修复后 0 override | Wave 0 触碰 5 verb `super._exit_tree()` 0 改 |

**与 §9.6.16 关系** — §9.6.16 覆盖 6 verb hit handler 5 件套 (target 守卫 / 屏染 / 屏抖 / VFX flash / 音频), §9.6.18 覆盖 6 verb ability 
子类共享契约 1:1 严格分离 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 兜底). §9.6.16 关注"信号触发后 handler 函数体 5 件套", 
§9.6.18 关注"verb ability 类层级共享契约 6 件 1:1 分离". 任何"加新 1 verb"必须**先**按 §9.6.18 在 `_verb_ability_base.gd` 加 1 行共享契约 (1 verb 
extends, 0 改 base 既有 6 字段), **再**按 §9.6.16 在 player.gd 加 5 件套 hit handler, **同时**按 §9.6.17 在 player.gd `_ready` 加 5+1 
件套 signal 桥接. 3 套 polish 模式 0 反序 0 漏 0 改 1 字符.

**与 §9.1 6 verb 接入路径 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` 
(6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.18 共享契约走"ability (2)" 步 (`src/scripts/_verb_ability_base.gd` 共享契约 
1:1 严格分离) + §9.1 9 步落地的"verb ability 子类 extends" 链 (per verb `extends "res://src/scripts/_verb_ability_base.gd"`). 
任何"加新 1 verb"必须**先**按 §9.1 9 步落地, **再**按 §9.6.18 共享契约 1:1 复制 (0 反序 0 漏 0 改 1 字符). 0 触碰 §9.1 9 步任何 const / var / 节点 
(共享契约仅在 base `_verb_ability_base.gd`, 6 verb 子类 0 改 base 0 改).

**与 §9.6.17 关系** — §9.6.17 覆盖 `player.gd _ready` 6 verb signal 桥接 5+1 件套 (null guard / 主信号 / 命中信号 / 结束信号 / 扩展信号 / 
PlayerActionGate register), §9.6.18 覆盖 6 verb ability 子类共享契约 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法). 任何"加新 1 
verb"必须**先**按 §9.6.18 写 1 verb ability 子类 extends `_verb_ability_base.gd` (共享契约 0 重写), **再**按 §9.6.17 在 player.gd 
`_ready` 加 5+1 件套 signal 桥接 (per verb 主信号 / 命中信号 / 结束信号 / 扩展信号 + PlayerActionGate register). 2 套 polish 模式 0 反序 0 漏 0 改 
1 字符.


### 9.6.19 `_verb_windup_vfx_base.gd` 共享契约 1:1 严格分离 polish 模式 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + 
T173 #92 + T174 #93 + T174.B #94 + F013.E #159 + T275 #194 跨 10 任务 ~115 轮落地) 文档化

> T166 (#85) + T167 (#86) + T168 (#86) + T169 (#87) + T171 (#89) + T173 (#92) + T174 (#93) + T174.B (#94) + F013.E 
> (#159) + **T275 (#194)** 跨 10 任务在 `src/scripts/_verb_windup_vfx_base.gd` (`class_name VerbWindupVFXBase extends 
> Node2D`) 落地 **6 verb** windup VFX (PulseWindupVFX / BindWindupVFX / CutWindupVFX / EchoWindupVFX / WaveWindupVFX / 
> **WhisperWindupVFX**) 共享契约 (3 共享字段 + 1 `_ready()` z_index 集中 + 1 `_process(delta)` 生命周期 + 1 
> `_activate_windup_tween()` ramp-in tween + 1 `fade_out_and_free()` 0.05s 退出 = 7 件) 1:1 严格分离. **T275 (#194) 收回 F013.E 
> #159 漂移** —— WhisperWindupVFX 现 `extends "res://src/scripts/_verb_windup_vfx_base.gd"` (5 verb 0 extend 0 重写 0 触碰) + 
> 删 3 自实现字段 + 删 `_process(delta)` + 删 `fade_out_and_free()` + 字段名 `_is_active` → `_active` 1:1 rename, 6 verb windup 
> VFX 共享契约 1:1 严格分离 100% 闭环. 业务特例保留: Whisper z_index=50 (VFX 在玩家上方, 区别于 5 verb z_index=10 在世界上方), 显式 override 
> `_ready()` 调 `super._ready()` 集中 z_index=10 + 重设 z_index=50 (与 §9.6.18 Wave 显式重写 byte-identical 模式 镜像). 任何"加新 1 
> verb"或"重构共享契约"时**必须**严格按 1:1 共享契约复制既有模式 (0 漏 0 改 1 字符). 跳出 §9.6.18 6 verb ability 共享契约 范畴 记录 **windup VFX 类层级共享契约** 
> polish 模式 (T174.B #94 byte-identical `fade_out_and_free()` 5 verb 落地后, F013.E #159 第 6 verb 时 0 走该 base 1:1, T275 
> #194 已收回该 drift, 文档化该 polish 模式防 polish 期重踩 "加新 1 verb 共享字段 0 复制 1:1 / 6 verb windup 各自 byte-identical copy 漂移 / 
> Whisper 自实现 vs 5 verb 共享 base 漂移" 类 pre-existing 风险).

**症状 / 触发场景 / 修复 / 预防** 4 段完整结构:

1. **症状** — `_verb_windup_vfx_base.gd` 共享契约漂移, 5 verb windup VFX (Pulse / Bind / Cut / Echo / Wave) 7 件共享契约任一漏 → 玩家 
verb 风蓄期 VFX 共享状态 (_lifetime / _max_lifetime / _active) 重复实现 / 字段 shadow / VFX 0 退出 (queue_free 漏) / 风蓄 tween 漂移 (4 
verb 各自 byte-identical copy 漂移) / 1 verb (Whisper) 0 extend base 命名偏离 (`_is_active` vs base `_active`).
2. **触发场景** — polish 期加新 1 verb (e.g. F013.E #159 加 Whisper 6 verb) 或重构共享契约 (e.g. T174.B #94 把 5 verb 
`fade_out_and_free` 集中到 base), 共享契约 1:1 严格分离任一漏 0 拍打 → 字段 shadow / 5 verb 各自 byte-identical 漂移 / VFX 退出漏 / 1 verb 
(Whisper) 0 extend base 漂移 (命名偏离 + 自实现 fade_out_and_free 漂移).
3. **修复** — 严格按 7 件共享契约 1:1 复制既有模式 (`_verb_windup_vfx_base.gd` 是 canonical 副本, **6 verb** 各自 extends 继承 0 重写 0 漂):
   - **3 共享字段** 0 重声明 — `_max_lifetime: float = 0.10` (T166 #85 5 verb 默认 windup_time, 0.04~0.10s 区间, tween duration / 
safety net 双重数据源) + `_lifetime: float = 0.0` (T166 #85 _process 累加, 跨 frame monotonic 计数) + `_active: bool = false` 
(T166 #85 _process 早退守卫 + fade_out_and_free 0 重入守卫). **6 verb** 子类**必须** 0 重声明 (会 shadow base 字段破坏 lifecycle contract). 
任何"加新 1 verb 共享字段"必须严格在 base 加 1 个字段 + 6 verb 各自 0 改 0 重声明 0 shadow. **T275 (#194) 收回 Whisper 漂移** — 
`whisper_windup_vfx.gd` 现 0 字段, 字段名 `_active` 与 base 1:1 对称, 6 verb (含 Whisper) 共享契约 1:1 严格分离 100% 闭环.
   - **1 `_ready()` z_index 集中** 5 verb 0 override + **1 verb Whisper 显式 override 业务特例** — 5 verb `z_index = 10` 
(windup VFX 在世界 (player/sprites) 之上 HUD 之下, 集中 base 避免 5 verb 各自漏设) 由 base 1:1 集中, 5 verb 0 override `_ready()`. 1 verb 
Whisper 业务特例: `z_index = 50` (VFX 在玩家上方, 5 verb 视觉组连贯需在玩家上, 区别于 5 verb z_index=10 在世界上方), 显式 override `_ready()` 调 
`super._ready()` 集中 z_index=10 + 重设 z_index=50 (与 §9.6.18 Wave 显式重写 byte-identical 模式 镜像). 任何"加新 1 verb" 0 override 
`_ready()` (除非业务需求 1:1 镜像 Wave 模式, 显式 override 调 super + 重设业务值).
   - **1 `_process(delta)` 生命周期 0 重写** — 5 verb 各自 `_process` 是 byte-identical copy (T166 #85 + T167 #86 + T168 #86 + 
T169 #87 + T171 #89 落地时各写一遍), 后由 base 1:1 集中: `if not _active: return` 守卫 + `_lifetime += delta` 累加 + `if _lifetime >= 
_max_lifetime: _active = false; queue_free(); return` safety net + `queue_redraw()` 跨 frame 触发 `_draw()`. **6 verb** 
子类**必须** 0 override `_process()`. **T275 (#194) 收回 Whisper 漂移** — `whisper_windup_vfx.gd _process` 已删, 走 base 1:1 集中 
`_lifetime` + safety net + `queue_redraw()`.
   - **1 `_activate_windup_tween()` ramp-in tween (T174 #93 落地) 0 重写** — `modulate.a = 0.0` 初始 alpha + `create_tween()` 
+ `set_trans(Tween.TRANS_QUAD)` + `set_ease(Tween.EASE_OUT)` + `tween_property(self, "modulate:a", 1.0, _max_lifetime)` 
(modulate:a 0.0→1.0 over _max_lifetime, TRANS_QUAD EASE_OUT front-loads 可见性) + `queue_redraw()`. **6 verb** `trigger()` 
末尾必须 `_activate_windup_tween()` (T174.B #94 替代 T174 #93 之前 5 verb 各自 4-line tween block + T275 #194 收回 Whisper 漂移). **6 
verb** 子类**必须** 0 override `_activate_windup_tween()`. 任何"加新 1 verb" 0 触碰 base tween 实现.
   - **1 `fade_out_and_free()` 0.05s 退出 (T173 #92 落地) 0 重写** — 5 verb 各自 `fade_out_and_free()` 是 byte-identical copy 
(T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 落地时各写一遍), 后由 base 1:1 集中: `if not _active: queue_free(); return` 
守卫 (idempotent) + `_active = false` 停止 `_process` 与 tween 竞争 + `create_tween()` + `tween_property(self, "modulate:a", 
0.0, 0.05)` + `tween_callback(queue_free)`. 由 parent ability 的 `_exit_tree()` 在 scene change / death 时统一调用 (T173 #92 
设计本意: 0.05s 短于 room transition ≥0.4s, 长于 abrupt _exit_tree 平滑). **6 verb** 子类**必须** 0 override `fade_out_and_free()`. 
**T275 (#194) 收回 Whisper 漂移** — `whisper_windup_vfx.gd fade_out_and_free` 已删, 走 base 0.05s 退出 1:1 集中.
4. **预防** — 任何 polish 期给 6 verb windup VFX 子类 (Pulse / Bind / Cut / Echo / Wave / Whisper) 加新 1 共享契约字段 (e.g. 
`_shared_windup_color`) 或方法 (e.g. `_shared_windup_init`) 时**必须**严格按 1:1 共享契约复制既有模式, 0 漏 0 改 1 字符. 0 重声明 base 3 字段 
(`_max_lifetime` / `_lifetime` / `_active`), 0 override `_ready()` (z_index=10 base 集中, 业务特例需 override 时 1:1 镜像 §9.6.18 
Wave 模式: 调 super + 重设业务值), 0 override `_process(delta)` (lifetime + safety net), 0 override `_activate_windup_tween()` 
(ramp-in tween), 0 override `fade_out_and_free()` (0.05s 退出). 0 触碰 6 verb 各自 `trigger()` 末尾的 `_activate_windup_tween()` 
1 行调用 (T174.B #94 替代 5 verb 各自 4-line tween block, T275 #194 收回 Whisper 漂移后 6 verb 1:1 严格分离). **T275 (#194) 收回完成** — 
F013.E (#159) 落地时漂移已 0, 6 verb (含 Whisper) 共享契约 1:1 严格分离 100% 闭环. 任何"加新 1 verb"必须**先**在 §9.1 9 步落地, **再**严格按 7 件共享契约 
1:1 复制 (`extends "res://src/scripts/_verb_windup_vfx_base.gd"` + 0 字段 shadow + 0 override 6 共享方法), 0 重蹈 F013.E 漂移 (T275 
#194 已 0 漂).

**7 件共享契约 1:1 严格分离总表** (T275 #194 落地后, 6 verb windup VFX 子类 extends VerbWindupVFXBase 共享契约 0 互混 0 复用 0 共享, 3 字段 + 1 
`_ready()` + 1 `_process(delta)` + 1 `_activate_windup_tween()` + 1 `fade_out_and_free()` = 7 件 + 守卫, 0 触碰 0 重写 0 重声明, 
1 verb Whisper 业务特例: 显式 override `_ready()` 调 super + 重设 z_index=50):

| 共享契约 | 数量 | base 落地 | 6 verb 子类复制 | Whisper 业务特例 | 0 触碰边界 |
|------|------|------|------|------|------|
| 共享字段 | 3 (`_max_lifetime` / `_lifetime` / `_active`) | T166 #85 + T173 #92 | 0 重声明 (会 shadow) | T275 #194 收回 0 字段, 走 
| base 1:1 | 6 verb 0 改 0 删 0 重排 |
| `_ready()` | 1 (`z_index = 10`) | T174.B #94 | 5 verb 0 override | **1 verb Whisper 显式 override** 调 super + 重设 
| `z_index = 50` (业务特例, 与 §9.6.18 Wave 显式重写 模式 镜像) | 5 verb 0 override |
| `_process(delta)` | 1 (lifetime + safety net + `queue_redraw()`) | T174.B #94 | 0 override | T275 #194 收回 0 自实现 | 6 
| verb 0 override |
| `_activate_windup_tween()` | 1 (T174 #93 ramp-in modulate:a 0.0→1.0 over _max_lifetime, TRANS_QUAD EASE_OUT) | T174 
| #93 | 0 override, `trigger()` 末尾 1 行调用 | T275 #194 收回 0 自实现, `trigger()` 末尾 1 行调用 base | 6 verb 0 override |
| `fade_out_and_free()` | 1 (T173 #92 0.05s modulate:a 1.0→0.0 + `tween_callback(queue_free)`, idempotent) | T173 #92 + 
| T174.B #94 | 0 override | T275 #194 收回 0 自实现, 走 base 0.05s 退出 | 6 verb 0 override |
| `super._ready()` 首行 | 1 (base 1 行 z_index=10 集中) | T174.B #94 隐式 | 5 verb 0 override → super 自动调用 | **1 verb Whisper 
| 显式调** `super._ready()` 集中 z_index=10 + 重设 z_index=50 | 5 verb 0 漏 |
| `extends VerbWindupVFXBase` 1:1 | 1 (T174.B #94 5 verb 0 重写 base 实现, T275 #194 收回 Whisper 漂移) | T174.B #94 + T275 
| #194 | 6 verb 全 `extends "res://src/scripts/_verb_windup_vfx_base.gd"` | T275 #194 收回, Whisper 现 extends 
| VerbWindupVFXBase | 6 verb 0 改 `extends` |

**T275 (#194) 收回 F013.E #159 漂移 4 项完成状态** (1 verb Whisper 0 extend base → 6 verb 全 extends, 6 verb windup VFX 共享契约 1:1 
严格分离 100% 闭环):

| 漂移项 | Whisper 落地时 (F013.E #159) | T275 (#194) 收回后 | base 5 verb 实现 | 业务特例保留 |
|------|------|------|------|------|
| `extends` | `extends Node2D` (0 extend base) | `extends "res://src/scripts/_verb_windup_vfx_base.gd"` | `extends 
| "res://src/scripts/_verb_windup_vfx_base.gd"` | N/A |
| 字段 `_active` vs `_is_active` | `var _is_active: bool = true` (命名偏离) | 0 字段, 走 base `var _active: bool = false` | `var 
| _active: bool = false` | N/A |
| `_ready()` z_index | `z_index = 50` 自实现 | 显式 override `_ready()` 调 `super._ready()` + 重设 `z_index = 50` (与 §9.6.18 
| Wave 显式重写 模式 镜像) | base `z_index = 10` | **z_index=50 业务需求保留** (VFX 在玩家上方, 5 verb 视觉组连贯) |
| `_process(delta)` 退出路径 | 自实现 `if _lifetime >= _max_lifetime: fade_out_and_free()` (0 触碰 base) | 0 `_process` 自实现, 走 
| base 1:1 集中 | base 1:1 集中 lifetime + safety net + `queue_redraw()` | N/A |
| `fade_out_and_free()` 守卫 | 自实现 `is_inside_tree()` 守卫 + `_is_active = false` + tween (守卫更严格) | 0 自实现, 走 base `_active` 
| 守卫 + `_active = false` + tween | base `_active` 守卫 + `_active = false` + tween (idempotent) | N/A |

**与 §9.6.18 关系** — §9.6.18 覆盖 `_verb_ability_base.gd` 6 verb ability 子类共享契约 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法 
+ 1 `_exit_tree` 兜底 + 1 `is_globally_blocking` 扩展 = 16 件 1:1 严格分离), §9.6.19 覆盖 `_verb_windup_vfx_base.gd` 6 verb windup 
VFX 子类共享契约 (3 字段 + 1 `_ready()` + 1 `_process(delta)` + 1 `_activate_windup_tween()` + 1 `fade_out_and_free()` = 7 件 
1:1 严格分离). §9.6.18 关注"verb ability 类层级共享契约" (PulseAbility / BindAbility / ...), §9.6.19 关注"windup VFX 类层级共享契约" 
(PulseWindupVFX / BindWindupVFX / ...). 任何"加新 1 verb"必须**先**按 §9.6.18 在 `_verb_ability_base.gd` 加 1 个 verb ability 子类 
extends (0 改 base 既有 6 字段), **同时**按 §9.6.19 在 `_verb_windup_vfx_base.gd` 加 1 个 windup VFX 子类 extends (0 改 base 既有 7 
件共享契约, 0 改 6 verb Whisper 任何代码 — T275 #194 收回完成), **再**按 §9.6.16 在 player.gd 加 5 件套 hit handler, **最后**按 §9.6.17 在 
player.gd `_ready` 加 5+1 件套 signal 桥接. 4 套 polish 模式 0 反序 0 漏 0 改 1 字符. §9.6.18 + §9.6.19 是 ability + VFX **双 base 1:1 
严格分离**镜像, 6 verb 子类 (ability) + 6 verb 子类 (windup VFX) **双 6 件 1:1 严格分离 = 12 件 1:1 严格分离 0 互混 0 复用 0 共享**.

**与 §9.6.16 / §9.6.17 / §9.6.18 / §9.1 6 verb 接入路径 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + 
`input` (4) + `shop` (5) + `icon` (6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.19 共享契约走"vfx (7)" 步 
(`src/scripts/_verb_windup_vfx_base.gd` 共享契约 1:1 严格分离) + §9.1 9 步落地的"verb windup VFX 子类 extends" 链 (per verb `extends 
"res://src/scripts/_verb_windup_vfx_base.gd"`). 任何"加新 1 verb"必须**先**按 §9.1 9 步落地, **再**按 §9.6.19 共享契约 1:1 复制 (0 反序 0 漏 
0 改 1 字符). 0 触碰 §9.1 9 步任何 const / var / 节点 (共享契约仅在 base `_verb_windup_vfx_base.gd`, 6 verb 子类 0 改 base 0 改, 含 1 verb 
Whisper T275 #194 收回完成).
