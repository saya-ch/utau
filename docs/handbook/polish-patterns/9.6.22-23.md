# Polish Patterns 9.6.22–23

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.22–9.6.23，共 2 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.22-23.md

<!-- shard 22-23 共 2 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.22 6 verb VFX 视觉组 (5 段 canonical 1:1 序列) polish 模式 (T098 + T158 + T170a + T170c + T170d + T181 + F013.E + T241 
+ T251 + T252 + T270 跨 11 任务 ~135 轮落地) 文档化

> §9.6.1 记录 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫; §9.6.2 记录单个 VFX 类 5 layer 视觉 (L1-L5) polish; 
> §9.6.16 记录 player.gd 6 verb hit handler 5 件套 1:1; §9.6.18 记录 `_verb_ability_base.gd` 6 verb ability 共享契约; §9.6.19 记录 
> `_verb_windup_vfx_base.gd` 6 verb windup VFX 共享契约; §9.6.21 记录 6 verb 成就系统 闭环 8 段 1:1 序列. 本节记录 6 verb VFX 视觉组 跨 5 段 
> canonical 1:1 序列 (Stage 1 class_name + extends Node2D 模板 + Stage 2 6 verb 调色六元组 1:1 + Stage 3 5+ layer 视觉组 1:1 + 
> Stage 4 6 verb 几何 1:1 严格分离 + Stage 5 6 verb 跨段 1:1 镜像) — 跨 6 verb VFX 文件 (`pulse_vfx.gd` / `bind_vfx.gd` / 
> `cut_vfx.gd` / `echo_vfx.gd` / `resonance_wave_vfx.gd` / `whisper_vfx.gd`) + 6 verb 调色六元组 + 6 verb 几何 (圆环 / 螺旋 / 弧斩 / 
> 护盾 / 光环 / 球) + 6 verb 跨段镜像 (与 §9.6.16 / §9.6.18 / §9.6.19 / §9.6.21) 反复落地的「6 verb VFX 视觉组」1:1 严格分离 polish 模式. 跨 11 任务 
> (T098 #65 + T158 #89 + T170a + T170c + T170d + T181 #97 + F013.E #159 + T241 #159 + T251 #169 + T252 #171 + T270 
> #189) ~135 轮迭代落地, 5 段 canonical 1:1 序列, 0 反序 0 漏 0 改 1 字符. 任何"加新 1 verb 接入 VFX 视觉组"必须**先**按 §9.6.4 调色六元组加 1 entry, 
> **再**按 §9.6.2 5+ layer 视觉组 1:1 复制 (0 反序 0 漏 0 改 1 layer), **再**按 §9.6.18 16 件套 verb ability base 1:1 复制 (0 反序 0 漏 0 改 
> 1 字段), **再**按 §9.6.19 7 件套 windup VFX base 1:1 复制 (0 反序 0 漏 0 改 1 字段), **再**按 §9.6.16 5 件套 hit handler 1:1 复制 (0 反序 0 
> 漏 0 改 1 字段), **再**按 §9.6.21 8 段序列 1:1 复制 (成就系统, 0 反序 0 漏 0 改 1 字段), **再**按 §9.6.22 5 段序列 1:1 复制 (VFX 视觉组, 0 反序 0 漏 0 
> 改 1 字段). 跨 6 verb (Pulse 圆环 cyan / Bind 螺旋 violet / Cut 弧斩 amber / Echo 护盾 cyan 球 / Wave 光环 + 8 棱镜 pale / Whisper 
> constant 球 mauve) + 6 verb 调色六元组 (Pulse Coral / Bind Violet / Cut Amber / Echo Cyan / Wave Pale / Whisper Mauve) + 6 
> verb 几何 (圆环 / 螺旋 / 弧斩 / 护盾 / 光环 / 球, 0 互混 0 复用 0 共享) 全部走同一套 5 段 1:1 序列 0 例外.

- **症状**: 6 verb VFX (Pulse / Bind / Cut / Echo / Wave / Whisper) 跨 6 个独立 .gd 文件 (`pulse_vfx.gd` / `bind_vfx.gd` / 
`cut_vfx.gd` / `echo_vfx.gd` / `resonance_wave_vfx.gd` / `whisper_vfx.gd`) 反复 polish 期间出现 4 类典型 fragile: (1) "加新 1 verb 
VFX 漏 1 跨段镜像" — 新 verb VFX 落地时漏 §9.6.16 5 件套 hit handler 1:1 镜像 (e.g. T251 #169 落地 Whisper 5 layer 时漏 `_on_whisper_hit` 
与 `_current_whisper_vfx.flash_hit` 1:1 镜像, 走 pass 占位, 命中时 0 视觉反馈); (2) "加新 1 verb VFX 漏 1 几何" — 新 verb VFX 落地时与既有 5 
verb 几何 0 区分 (e.g. T241 #159 落地 Whisper constant 球时 0 标注"与 Wave 圆环扩散形成对比", 玩家看到 2 个圆环状 VFX 误以为是同 1 套视觉组); (3) "修 1 verb 
VFX 调色 漏 §9.6.4 调色六元组 1:1" — 修 Pulse VFX ring_color 时改硬编码 hex 0 改 §9.6.4 调色六元组 entry, 跨段 §9.6.21 ICON_COLORS + §9.6.3 
HUD 6 行 6 色色域分工 6 通道 + §9.6.4 tooltip 三闭环宪法全部漂动; (4) "加新 1 verb VFX 5+ layer 漏 L5 HIT_FLASH" — 新 verb VFX 落地时只画 L1-L4 4 
layer, 漏 L5 HIT_FLASH 命中闪烁, 玩家命中时 0 视觉确认 (与 §9.6.2 5 layer polish 1 字段 0 镜像). 4 类症状根因: 6 verb VFX 视觉组 5 段 canonical 1:1 
序列 0 文档化, polish 期只能"凭记忆"同步, 1 漏 1 = 跨段漂动.
- **触发场景**: 6 verb VFX (Pulse / Bind / Cut / Echo / Wave / **Whisper**) 5 段 canonical 1:1 序列落地需同步 5 件套: (1) 
**`pulse_vfx.gd` / `bind_vfx.gd` / `cut_vfx.gd` / `echo_vfx.gd` / `resonance_wave_vfx.gd` / `whisper_vfx.gd` 6 verb VFX 
文件 顶层模板** — `class_name <Verb>VFX` (e.g. `class_name PulseVFX`) + `extends Node2D` 0 改 `extends Node`, 1 漏 `class_name` 
= autoload `_current_<verb>_vfx` 引用断, 1 漏 `extends Node2D` = `_draw` 函数 0 触发 (Node 0 _draw 回调); (2) **6 verb `@export` 
color 字段 0 改 1 hex** — `@export var ring_color: Color = Color("#69C7CE")` (Pulse 圆环 cyan) / `@export var core_color: 
Color = Color("#F2B66E")` (Pulse 球心 amber) / `@export var fade_color: Color = Color("#E86D5A")` (Pulse 衰减 coral) / 
`@export var wave_color: Color = Color("#B7E7DD")` (Pulse 波 pale), Bind 用 `#65506A` violet + `#69C7CE` cyan + `#B7E7DD` 
pale, Cut 用 `#E86D5A` coral + `#B7E7DD` pale + `#F2B66E` amber, Echo 用 `#69C7CE` cyan + `#B7E7DD` pale + `#F2B66E` 
amber + `#E86D5A` coral 反弹, Wave 用 `#65506A` muted + `#B7E7DD` pale + `#69C7CE` cyan, Whisper 用 `#C8A4D8` mauve, 6 verb 
调色严格走 §9.6.4 调色六元组 0 改 1 hex 0 改 1 通道值; (3) **6 verb `_draw` 函数 5+ layer 视觉组 1:1 严格分离** — Pulse 5 layer (core 球心 + 3 
ring 圆环 + spark 碎片 + waveform_arc 弧) / Bind 5 layer (core 球心 + spiral 螺旋 + wave 波 + pull 牵引) / Cut 5 layer (slash 弧 + 
edge 刃 + flash 闪 + fragment 碎片 + trail 拖尾) / Echo 6 layer (shield 护盾球 + halo 光环 + prism 棱镜 × 8 + core 球心 + reflect 反弹 + 
reflect_flash 反弹闪) / Wave 6 layer (L1 DEEP_SHADOW + L2 INNER_HALO + L3 RING_FILL + L4 RING_STROKE + L5 PRISM_RAYS × 8 + 
L6 OUTER_WISPS × 12) / Whisper 6 layer (L1 OUTER_FILL + L2 SPHERE_RING + L3 EDGE_HIGHLIGHT + L4 CORE_DOT + L5 HIT_FLASH 
× N + L6 EXPANDING_RING), 6 verb 5+ layer 1:1 严格分离 0 互混 0 复用 (Pulse 圆环 0 复用 Bind 螺旋 0 复用 Cut 弧斩 0 复用 Echo 护盾 0 复用 Wave 
光环 0 复用 Whisper 球, 6 verb 几何 0 共享 1 行 draw_*); (4) **6 verb 几何 1:1 严格分离** — 圆环 / 螺旋 / 弧斩 / 护盾 / 光环 / 球 6 套视觉 motif, 1 
verb 1 几何 0 例外, 几何一致 → 视觉组连贯 (`STYLE_GUIDE.md` "6 verb 视觉组 1 verb 1 几何" 段) + tooltip 8 行拼接 (T250 #168 
`_build_verb_achievement_tooltip`) 0 互混 0 改 1 字段; (5) **6 verb 跨段 1:1 镜像** — 6 verb VFX 1:1 镜像到 §9.6.16 5 件套 hit 
handler (1 verb 1 L5 HIT_FLASH 1 `_on_<verb>_hit` 1 `_current_<verb>_vfx.flash_hit(target)` 调点) + §9.6.18 16 件套 verb 
ability base (1 verb 1 `<verb>_ability.gd` 1 `trigger_player_hit(target)` 调点 spawn VFX) + §9.6.19 7 件套 windup VFX base 
(1 verb 1 `<verb>_windup_vfx.gd` 1 `_verb_windup_vfx_base` 共享契约) + §9.6.21 8 段成就序列 (1 verb 1 `<verb>_used` 字段 1 
`<verb>_icon` ICON_COLORS 1 PNG 双路径), 5 跨段镜像 0 漏 1 边 0 改 1 字段. 5 件套 1:1 同步, 1 漏 1 = 6 verb VFX 视觉组 漂动 (调色漂动 / 几何互混 / 
layer 漏 L5 / 跨段镜像漏 1 边 / 成就 PNG 双路径漏 1 路径).
- **修复** (跨 11 任务 ~135 轮落地后已知 5 段 canonical 1:1 序列 + 6 verb 0 例外):
  - **T098 (#65) 5 verb 视觉组奠基 + 6 verb 调色六元组落地 (5 verb)** — `src/scripts/pulse_vfx.gd` / `src/scripts/bind_vfx.gd` / 
`src/scripts/cut_vfx.gd` / `src/scripts/echo_vfx.gd` 4 verb (Pulse / Bind / Cut / Echo) 4 套几何 1:1 严格分离 (圆环 / 螺旋 / 弧斩 / 
护盾) + 4 verb `@export` color 字段 1:1 镜像 §9.6.4 调色六元组 (Pulse Coral / Bind Violet / Cut Amber / Echo Cyan 4 entry) + 4 
verb 5+ layer 视觉组 (Pulse 5 layer core+ring×3+spark+arc / Bind 5 layer core+spiral+wave+pull / Cut 5 layer 
slash+edge+flash+fragment+trail / Echo 6 layer shield+halo+prism×8+core+reflect+reflect_flash), 0 触碰 §9.6.16 5 件套 hit 
handler (T098 落地时 hit handler 走屏染 + 屏抖 + 音频 3 件套 0 走 VFX flash 1 件套, T170c 后才升级到 VFX flash).
  - **T158 (#89) Echo 棱镜 × 8 + 中心暖点** — `src/scripts/echo_vfx.gd` 加 8 方向棱镜光线 (T158 升级 Echo 5 layer → 6 layer, 加 prism × 
8 + halo + core 球心 + reflect 反弹 4 件, 与 Pulse 圆环 0 互混 0 复用 0 共享, 1 漏 prism 数量 = 玩家看到 6 棱镜误以为是 Bind 螺旋).
  - **T170a + T170c + T170d 5 verb VFX flash 接口落地** — `src/scripts/pulse_vfx.gd.flash_hit(target_pos)` + 
`bind_vfx.gd.flash_hit(target_pos)` + `cut_vfx.gd.flash_hit(target_pos)` 3 verb VFX flash 接口 1:1 严格分离 (T170c 落地 
`_current_pulse_vfx.flash_hit(target.global_position)` 调点, T170d 同步 Bind / Cut 1:1 复制 0 改调点), 0 触碰 §9.6.16 5 件套 hit 
handler 既有屏染 + 屏抖 + 音频 3 件套 (T170c 升级到 VFX flash 是"加 1 件" 0 改既有 3 件).
  - **T181 (#97) 5 verb 闭环 19 cue + 5 verb VFX 100% 闭环** — `src/scripts/pulse_vfx.gd` / `bind_vfx.gd` / `cut_vfx.gd` / 
`echo_vfx.gd` / `resonance_wave_vfx.gd` 5 verb 视觉组 100% 闭环 (5 verb 几何 1:1 严格分离 + 5 verb 调色六元组 1:1 + 5 verb 5+ layer 1:1 
+ 5 verb 跨段 1:1 镜像 §9.6.16 hit handler + §9.6.18 ability base + §9.6.19 windup VFX base 3 跨段), 0 例外 0 漂移, 5 verb VFX 
视觉组 100% 闭环.
  - **F013.E (#159) 6 verb Whisper 接入** — `src/scripts/whisper_vfx.gd` 加 class_name + extends Node2D + 5 layer 视觉组 (L1 
OUTER_FILL + L2 SPHERE_RING + L3 EDGE_HIGHLIGHT + L4 CORE_DOT + L5 HIT_FLASH × N) + `@export var whisper_color: Color = 
Color("#C8A4D8")` mauve 调色 + 1:1 镜像 §9.6.4 调色六元组 6 verb entry + 1:1 镜像 §9.6.16 `_on_whisper_hit` 5 件套 hit handler + 1:1 
镜像 §9.6.18 `whisper_ability.gd` 16 件套 ability base + 1:1 镜像 §9.6.19 `whisper_windup_vfx.gd` 7 件套 windup VFX base + 1:1 
镜像 §9.6.21 `whisper_used` 字段 + `whisper_icon` ICON_COLORS + PNG 双路径, 6 跨段镜像 0 漏 1 边 0 改 1 字段, 6 verb 视觉组 100% 闭环 (5 
verb + 1 verb = 6 verb).
  - **T241 (#159) 6 verb 字段 + 6 verb 调色 1:1 严格** — `src/autoload/player_stats.gd` 加 `whisper_used: int = 0` 字段 (6 verb 
`<verb>_used` 字段 1:1 严格) + `record_ability_used("whisper")` 分支 (6 branch 1:1 严格) + `get_stat("whisper_used")` entry (6 
entry 1:1 严格) + `set_stat("whisper_used", ...)` entry (6 entry 1:1 严格) + `reset_run` 加 `whisper_used = 0` 1 行 (6 行 
reset 顺序 1:1 严格) + `all_abilities_used` 6 项 `and` 检查 0 漏 0 反序 0 改 `>= 1` 阈值 + `STYLE_GUIDE.md` §9.6.4 调色六元组加 `Whisper 
Mauve #C8A4D8 Color(0.784, 0.643, 0.847, 1.0)` 1 entry (6 verb 1:1 严格).
  - **T251 (#169) Whisper 5 layer 视觉组 polish + L3 EDGE_HIGHLIGHT + L5 HIT_FLASH** — `src/scripts/whisper_vfx.gd` 加 L3 
EDGE_HIGHLIGHT (1.04× R 1px α0.40 外侧薄光) + L5 HIT_FLASH × N (Warm Parchment 2..4px α0.70 0.15s 衰减命中闪烁) 2 layer, 与既有 L1 
OUTER_FILL + L2 SPHERE_RING + L4 CORE_DOT 3 layer 1:1 严格分离 (5 layer 0 漏 0 反序 0 改 1 layer 0 改 1 alpha 公式), 
`_on_whisper_hit` 从 pass 占位升级到 `_current_whisper_vfx.flash_hit(target.global_position)` 1:1 镜像 §9.6.16 5 件套 hit handler 
既有 5 verb `flash_hit` 调点 0 改调点 0 改 1 字段, 0 触碰 §9.6.2 5 layer 视觉组 既有 `sin(t * PI)` 共享起伏 (5 layer 共享 1 个 `t` 变量 + 5 个 
`*_alpha = sin(t * PI) * <const>` 派生 alpha 公式 0 改 0 删 0 重排).
  - **T252 (#171) `flash_hit` 跨 6 verb VFX 1:1 严格** — `pulse_vfx.gd` / `bind_vfx.gd` / `cut_vfx.gd` / `echo_vfx.gd` / 
`resonance_wave_vfx.gd` / `whisper_vfx.gd` 6 verb VFX 6 个 `flash_hit(target_pos)` 函数 1:1 严格分离 (6 函数 0 互混 0 复用 0 共享 1 行),
 T252 把 T170c 5 verb flash_hit 升级到 6 verb (加 Whisper 1 verb) 0 改既有 5 verb 函数体 0 改 1 字段 0 改 1 调点.
  - **T270 (#189) Whisper L6 EXPANDING_RING 玩家可读性 v7 强化** — `src/scripts/whisper_vfx.gd` 加 L6 EXPANDING_RING 
(0.60→1.20× R α0.50→0 1px 0.15s 涟漪外环) 1 个 layer, 5 layer → 6 layer 升级 0 反序 0 漏 0 改 1 既有 layer, 0 触碰 §9.6.2 5 layer 视觉既有 
5 layer alpha 公式 (L6 独立计算 `var expanding_alpha: float = (1.0 - age / 0.15) * 0.50` 0 复用 L1-L5 `sin(t * PI)` 起伏), 0 触碰 
§9.6.16 `_on_whisper_hit` 既有 flash_hit 调点 0 改 1 字段, 0 触碰 §9.6.4 调色六元组 `Whisper Mauve` 1 hex 0 改 1 通道值.
  - 跨 11 任务 ~135 轮落地后 0 漂移, 6 verb VFX 视觉组 5 段 canonical 1:1 序列 100% 闭环, 6 verb 调色 1:1 严格分离 (5 verb + 1 verb = 6 verb, 
0 漏 0 重叠 0 互混 0 复用 0 共享) + 6 verb 几何 1:1 严格分离 (圆环 / 螺旋 / 弧斩 / 护盾 / 光环 / 球, 0 互混 0 复用 0 共享) + 6 verb 5+ layer 视觉组 1:1 
严格分离 (Pulse 5 / Bind 5 / Cut 5 / Echo 6 / Wave 6 / Whisper 6 layer, 0 漏 1 layer) + 6 verb 跨段 1:1 镜像 (§9.6.16 5 件套 hit 
handler + §9.6.18 16 件套 ability base + §9.6.19 7 件套 windup VFX base + §9.6.21 8 段成就序列, 4 跨段 0 漏 1 边). 11 任务 0 后续 主要 
pre-existing 风险.
- **预防**:
  1. 任何 polish 期给"加新 1 verb 接入 VFX 视觉组"或"修 1 verb VFX 调色"或"加新 1 verb VFX layer"或"加新 1 跨段镜像 (hit handler / ability base 
/ windup VFX base / 成就 PNG)"任务加新字段 / 加新方法 / 加新 layer / 加新 verb 时**必须**严格按 5 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 
字段. 5 段 0 触碰边界: **Stage 1 0 改 `class_name <Verb>VFX` 模板** (T098 + F013.E 落地后 6 verb class_name 1:1 严格, `class_name 
PulseVFX` / `class_name BindVFX` / `class_name CutVFX` / `class_name EchoVFX` / `class_name ResonanceWaveVFX` / 
`class_name WhisperVFX` 6 class_name 0 改 0 重命名, 改 1 字符破坏 `_current_<verb>_vfx` autoload 引用), **Stage 2 0 改 6 verb 
`@export` color 字段名 1:1 镜像 §9.6.4 调色六元组** (T098 + F013.E + T241 落地后 6 verb color 字段 1:1 严格, `ring_color` / `core_color` 
/ `fade_color` / `wave_color` Pulse 4 字段 + `core_color` / `wave_color` / `pull_color` Bind 3 字段 + `slash_color` / 
`edge_color` / `flash_color` Cut 3 字段 + `shield_color` / `halo_color` / `prism_color` / `core_color` Echo 4 字段 + 
`outer_ring_color` / `inner_ring_color` / `prism_color` Wave 3 字段 + `whisper_color` Whisper 1 字段, 改 1 字段名破坏 §9.6.4 
调色六元组 entry 引用), **Stage 3 0 改 6 verb 5+ layer 视觉组 0 反序 0 漏 0 改 1 layer** (T098 + T158 + T170a/c/d + T181 + F013.E + 
T251 + T270 落地后 6 verb 5+ layer 1:1 严格, Pulse 5 layer core+ring×3+spark+arc / Bind 5 layer / Cut 5 layer / Echo 6 layer 
shield+halo+prism×8+core+reflect+reflect_flash / Wave 6 layer L1-L6 / Whisper 6 layer L1-L6 0 漏 0 反序 0 改 1 layer 0 改 1 
alpha 公式), **Stage 4 0 改 6 verb 几何 1:1 严格分离** (5 verb + 1 verb 6 套视觉 motif 0 互混 0 复用 0 共享, 圆环 / 螺旋 / 弧斩 / 护盾 / 光环 / 球 0 
例外, 改 1 几何 = 视觉组连贯破坏 玩家看到 2 个圆环状 VFX 误以为是同 1 套), **Stage 5 0 改 6 verb 跨段 1:1 镜像 4 跨段** (与 §9.6.16 5 件套 hit handler 0 漏 
1 边 + 与 §9.6.18 16 件套 ability base 0 漏 1 边 + 与 §9.6.19 7 件套 windup VFX base 0 漏 1 边 + 与 §9.6.21 8 段成就序列 0 漏 1 边, 1 verb 
1 跨段 4 边 0 漏 0 改 1 字段). 任何"加新 1 verb 接入 VFX 视觉组"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格), **再**在 
`src/scripts/<verb>_vfx.gd` 加 class_name + extends Node2D + 5+ layer 视觉组 (Stage 1 + 3 1:1 严格, 0 触碰既有 5 verb VFX), 
**再**按 §9.6.18 16 件套 ability base 1:1 复制 (Stage 5 1:1 严格), **再**按 §9.6.19 7 件套 windup VFX base 1:1 复制 (Stage 5 1:1 严格), 
**再**按 §9.6.16 5 件套 hit handler 1:1 复制 (Stage 5 1:1 严格), **再**按 §9.6.21 8 段成就序列 1:1 复制 (Stage 5 1:1 严格), 0 触碰 §9.6.2 既有 
5 layer 视觉组既有 5 verb 任何 1 字段 1 alpha 公式 1 layer 顺序.
  2. **5 段序列 0 触碰边界** — `src/scripts/pulse_vfx.gd` / `bind_vfx.gd` / `cut_vfx.gd` / `echo_vfx.gd` / 
`resonance_wave_vfx.gd` / `whisper_vfx.gd` 6 verb VFX 文件任何 const / var / `@export` / `_draw` 调点 0 改 0 删 0 重排 (Stage 1-4 
既有 1 字段 1 调点 0 改), 任何"加新 1 verb 调色"polish 走 Stage 2 1:1 严格 (改 `whisper_color` 时同步改 §9.6.4 调色六元组 6 verb entry 0 改 1 hex 
0 改 1 通道值); 任何"加新 1 verb VFX layer"polish 走 Stage 3 1:1 严格 (在 `_draw` 函数体顶部 docblock 写明新 layer 作用, 0 反序 0 漏 0 改 1 既有 
layer, 0 改既有 `sin(t * PI)` 共享起伏公式, L5 HIT_FLASH 必须独立 alpha 公式 0 复用 L1-L4); 任何"修 1 verb VFX 几何"polish 走 Stage 4 1:1 严格 
(0 改 1 verb 几何主 motif, 1 verb 1 几何 0 例外 圆环 / 螺旋 / 弧斩 / 护盾 / 光环 / 球 0 互混 0 复用 0 共享); 任何"加新 1 跨段镜像"polish 走 Stage 5 1:1 
严格 (1 verb 1 跨段 4 边 0 漏 1 边, hit handler + ability base + windup VFX base + 成就 PNG 4 跨段 1:1 镜像).
  3. **6 verb 调色 0 改 1 hex** (T098 + F013.E + T241 落地后 0 触碰边界) — Pulse Coral `#E86D5A` (0.91, 0.43, 0.35, 1.0) / Bind 
Violet `#65506A` (0.40, 0.31, 0.42, 1.0) / Cut Amber `#F2B66E` (0.95, 0.71, 0.43, 1.0) / Echo Cyan `#69C7CE` (0.41, 
0.78, 0.81, 1.0) / Wave Pale Resonance `#B7E7DD` (0.72, 0.91, 0.87, 1.0) / Whisper Mauve `#C8A4D8` (0.78, 0.64, 0.85, 
1.0) 6 verb 调色 1:1 严格分离 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 hex / 1 通道值 = 6 verb 视觉组 漂动 (Stage 2 漂动) + §9.6.4 调色六元组 entry 漂动 + 
§9.6.21 ICON_COLORS 漂动 + §9.6.3 HUD 6 行 6 色色域分工 6 通道 漂动 + §9.6.5 tooltip 视觉组连贯 8 行拼接 漂动, source-grep 验证 6 verb 
`@export` color 字段 + §9.6.4 调色六元组 6 entry + §9.6.21 ICON_COLORS 6 entry + §9.6.3 HUD 6 行 6 色 6 通道 0 漏 0 改 1 字段.
  4. **5+ layer 视觉组 0 反序 0 改 1 layer** (T251 + T270 落地后 0 触碰边界) — Pulse 5 layer (core 球心 → 3 ring 圆环 → spark 碎片 → 
waveform_arc 弧) / Bind 5 layer (core 球心 → spiral 螺旋 → wave 波 → pull 牵引) / Cut 5 layer (slash 弧 → edge 刃 → flash 闪 → 
fragment 碎片 → trail 拖尾) / Echo 6 layer (shield 护盾球 → halo 光环 → prism × 8 棱镜 → core 球心 → reflect 反弹 → reflect_flash 反弹闪) 
/ Wave 6 layer (L1 DEEP_SHADOW → L2 INNER_HALO → L3 RING_FILL → L4 RING_STROKE → L5 PRISM_RAYS × 8 → L6 OUTER_WISPS × 
12) / Whisper 6 layer (L1 OUTER_FILL → L2 SPHERE_RING → L3 EDGE_HIGHLIGHT → L4 CORE_DOT → L5 HIT_FLASH × N → L6 
EXPANDING_RING) 6 verb 5+ layer 0 反序 0 漏 0 改 1 layer 0 改 1 alpha 公式 0 改 1 painter's order, 任何"加新 1 layer"必须在文件顶部 
docblock 写明 5+ layer 各自作用 + alpha 公式, source-grep 验证 6 verb `_draw` 函数 5+ `draw_*` 调点 0 漏 0 改 1 顺序.
  5. **6 verb 几何 0 改 1 几何 motif** (T098 + F013.E 落地后 0 触碰边界) — 圆环 (Pulse) / 螺旋 (Bind) / 弧斩 (Cut) / 护盾 (Echo) / 光环 
(Wave) / 球 (Whisper) 6 套视觉 motif 0 互混 0 复用 0 共享, 1 verb 1 几何 0 例外, 改 1 几何 = 视觉组连贯破坏 (玩家看到 2 个圆环状 VFX 误以为是同 1 套) + 
tooltip 8 行拼接 漂动 (T250 #168 `_build_verb_achievement_tooltip` "几何" 字段 0 改), source-grep 验证 6 verb `_draw` 函数 几何 motif 0 
漏 0 改 1 调点 0 改 1 顺序.
  6. **跨段 1:1 镜像 0 漏 1 边** (T181 + F013.E + T251 + T252 + T270 落地后 0 触碰边界) — 1 verb 1 跨段 4 边 (与 §9.6.16 5 件套 hit 
handler 1:1 镜像 + 与 §9.6.18 16 件套 ability base 1:1 镜像 + 与 §9.6.19 7 件套 windup VFX base 1:1 镜像 + 与 §9.6.21 8 段成就序列 1:1 
镜像), 0 漏 1 边 0 改 1 字段, 任何"加新 1 跨段"polish 必须在 4 跨段 1:1 同步, source-grep 验证 6 verb VFX 4 跨段镜像 0 漏 1 边.
  7. 已知 drift risk: 6 verb VFX `flash_hit(target_pos)` 函数 (T170c + T252 落地) **可能**漏 1 verb 1:1 镜像 (e.g. T252 落地时漏 
`whisper_vfx.gd.flash_hit`, T251 #169 收回). 监控建议: 任何"未来给 6 verb VFX 加新接口"必须**先**检查 6 verb 1:1 镜像 0 漏 1 verb, 0 漏 1 边. 
F013.E (#159) 6 verb 接入路径是 §9.6.22 5 段序列的"母集" (1 verb 5 段 1:1 严格, §9.6.22 5 段 1 verb 完整闭环, 6 verb × 5 段 = 30 件 1:1 严格, 
含 4 跨段 6 verb × 4 = 24 跨段镜像 1:1 严格).

**0 副作用**: T098 / T158 / T170a / T170c / T170d / T181 / F013.E / T241 / T251 / T252 / T270 任何 const / var 0 触碰 (6 verb 
VFX class_name + extends Node2D 0 改 0 删 0 重排 + 6 verb `@export` color 字段 0 改 0 删 0 重排 + 6 verb 5+ layer 视觉组 0 改 0 删 0 
重排 + 6 verb 几何 motif 0 改 0 删 0 重排 + 6 verb 跨段镜像 4 边 0 改 0 删 0 重排); 0 触碰 §9.6.1 6 verb VFX 跨类 handler `is_instance_valid 
+ has_method` 双守卫 (T278 0 改 6 verb `_current_<verb>_vfx` 任何字段); 0 触碰 §9.6.2 5 layer 视觉组既有 5 verb 任何 1 字段 1 alpha 公式 1 
layer 顺序; 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T278 0 改 6 verb `_on_<verb>_hit` 函数体任何 const / var / 屏染 / 屏抖 / VFX 
flash / 音频); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T278 0 改 6 verb 主信号 + 命中信号 + 结束信号 + 扩展信号 + PlayerActionGate register 
/ unregister); 0 触碰 §9.6.18 16 件套 verb ability base (T278 0 改 6 verb ability 子类任何 const / var / `@export` / `@onready` 
/ 5 共享方法 / `_exit_tree` 兜底 / `is_globally_blocking` 扩展); 0 触碰 §9.6.19 7 件套 windup VFX base (T278 0 改 6 verb windup VFX 
子类任何 const / var / `_ready()` / `_process(delta)` / `_activate_windup_tween()` / `fade_out_and_free()`); 0 触碰 §9.6.20 
Hub ↔ archive 跨房间 transition flow (T278 0 改 GFC + room_transition + room_door + GameState 5 段序列); 0 触碰 §9.6.21 8 段成就序列 
(T278 0 改 PlayerStats 6 verb 字段 + record_ability_used 6 branch + get_stat 6 entry + set_stat 6 entry + reset_run 6 行 + 
all_abilities_used 6 项 + achievements.json 4 tier ID + ICON_COLORS 6 entry + PNG 双路径); 0 触碰 §9.6.4 调色六元组 + §9.6.3 HUD 6 
行 6 色色域分工 6 通道 + §9.6.5 tooltip 视觉组连贯 8 行拼接既有 6 verb 任何 1 hex / 1 通道值 / 1 字段 / 1 顺序 (T278 仅 0 触碰既有 6 verb 字段, 仅 0 触碰 6 
verb 调色 / 几何 / layer / 跨段镜像任何 1 字段); 0 触碰 check_smoke_consistency.sh (T278 0 改任何规则); 0 触碰 _parse_recent_section.py + 
pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 
任何 gameplay code 0 改, 6 verb VFX 任何 const / var 0 改, 含 1 verb Whisper T275 #194 收回完成); 0 行为变化, 1 文档 1 段 (§9.6.22 ~50 行) 
+ 1 smoke test (T278 18 断言).

**与 §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 关系** — §9.6.16 覆盖 player.gd 6 verb hit handler 5 件套 1:1 
严格分离 (verb ability `.trigger_player_hit` → 5 件套 `_on_verb_hit_pulse` / `_on_verb_hit_bind` / `_on_verb_hit_cut` / 
`_on_verb_hit_echo` / `_on_verb_hit_wave` + 1 verb Whisper `_on_whisper_hit`), §9.6.17 覆盖 player.gd `_ready` 6 verb 
signal 桥接 5+1 件套 1:1 严格分离 (5 verb `verb_triggered` signal + 1 verb Whisper 业务特例 `whisper_ability_executed` signal), 
§9.6.18 覆盖 `_verb_ability_base.gd` 6 verb ability 子类共享契约 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 
兜底 + 1 `is_globally_blocking` 扩展 = 16 件 1:1 严格分离), §9.6.19 覆盖 `_verb_windup_vfx_base.gd` 6 verb windup VFX 子类共享契约 (3 字段 
+ 1 `_ready()` + 1 `_process(delta)` + 1 `_activate_windup_tween()` + 1 `fade_out_and_free()` = 7 件 1:1 严格分离), §9.6.20 
覆盖 Hub ↔ archive 跨房间 transition flow 5 段 canonical 1:1 序列 (Stage 1 触发检测 + Stage 2 GFC 状态转换 + Stage 3 fade_out + Stage 4 
场景切换 + Stage 5 新 scene 恢复), §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 canonical 1:1 序列 (Stage 1 PlayerStats 字段声明 + Stage 2 
`record_ability_used` 分支 + Stage 3 `get_stat` entry + Stage 4 `set_stat` entry + Stage 5 `reset_run` 重置 + Stage 6 
`all_abilities_used` 条件 + Stage 7 `achievements.json` tier entry + Stage 8 `ICON_COLORS` entry + PNG 双路径). §9.6.22 覆盖 6 
verb VFX 视觉组 5 段 canonical 1:1 序列 (Stage 1 class_name + extends Node2D 模板 + Stage 2 6 verb 调色六元组 1:1 + Stage 3 5+ layer 
视觉组 1:1 + Stage 4 6 verb 几何 1:1 严格分离 + Stage 5 6 verb 跨段 1:1 镜像). §9.6.16 / §9.6.17 关注"verb ability + signal 桥接", 
§9.6.18 关注"verb ability 类层级共享契约", §9.6.19 关注"verb windup VFX 类层级共享契约", §9.6.20 关注"跨 scene transition flow 时序契约", 
§9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格)", §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约". 7 套 polish 模式 0 反序 0 漏 0 改 1 字符. 
任何"加新 1 verb 接入 VFX 视觉组"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格), **再**按 §9.6.2 5+ layer 视觉组 1:1 复制 (Stage 3 1:1 
严格), **再**按 §9.6.18 16 件套 ability base 1:1 复制 (Stage 5 1:1 严格), **再**按 §9.6.19 7 件套 windup VFX base 1:1 复制 (Stage 5 1:1 
严格), **再**按 §9.6.16 5 件套 hit handler 1:1 复制 (Stage 5 1:1 严格), **再**按 §9.6.21 8 段成就序列 1:1 复制 (Stage 5 1:1 严格), **再**按 
§9.6.22 5 段序列 1:1 复制 (VFX 视觉组 1:1 严格). §9.6.20 与 §9.6.22 0 互混 0 复用 0 共享: §9.6.20 关注"跨 scene transition flow 时序契约" (与 
verb VFX lifecycle 0 相关), §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约" (与跨 scene transition 0 相关). §9.6.22 是 7 套 polish 
模式**唯一**关注"6 verb VFX 视觉组 5 段 1:1 严格分离 (Stage 1-5: class_name + 调色 + layer + 几何 + 跨段镜像)"的 1 套, 与 §9.6.16-§9.6.21 6 套 
verb / VFX / transition / 成就 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.22 5 段序列走 §9.1 9 步落地的"verb" (1) + "ability" (2) + "const" (3) + "vfx" (7) 4 步 
(`src/scripts/pulse_vfx.gd` / `bind_vfx.gd` / `cut_vfx.gd` / `echo_vfx.gd` / `resonance_wave_vfx.gd` / `whisper_vfx.gd` 
6 verb VFX 文件 + `src/scripts/_verb_ability_base.gd` 6 verb ability base + `src/scripts/_verb_windup_vfx_base.gd` 6 verb 
windup VFX base + `src/autoload/player_stats.gd` 6 verb `<verb>_used` 字段 + `STYLE_GUIDE.md` §9.6.4 调色六元组 6 verb entry), 
0 触碰 §9.1 9 步任何 "input" (4) + "shop" (5) + "icon" (6) + "audio" (8) + "HUD 冷光勾边" (9) 5 步 (VFX 视觉组 0 走 input map + 0 走 
shop 系统 + 0 触碰 verb icon PNG 双路径 + 0 走 audio 副作用 + 0 走 HUD 渲染). 任何"加新 1 verb 接入 VFX 视觉组"必须**先**按 §9.1 9 步落地 (含 `verb` 
(1) + "ability" (2) + "const" (3) + "vfx" (7) 步), **再**按 §9.6.22 5 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 任何"加新 1 verb 
调色"必须**先**在 `STYLE_GUIDE.md` §9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格, 0 漏 1 entry 0 改 1 hex), **再**在 `<verb>_vfx.gd` 加 
`@export var <verb>_color: Color = Color("#HEX")` 1:1 镜像 (Stage 2 1:1 严格, 0 漏 1 字段 0 改 1 通道值), 0 触碰 §9.1 9 步任何 "verb" 
(1) + "ability" (2) + "const" (3) 步 (VFX 视觉组 0 触碰 verb enum + ability 子类 + const 字段). 任何"加新 1 verb VFX 
layer"必须**先**在文件顶部 docblock 写明 5+ layer 各自作用 (Stage 3 1:1 严格), **再**在 `_draw` 函数体顶部加 `var *_alpha: float = ...` 派生 
alpha 公式 (Stage 3 1:1 严格, 0 复用 L1-L5 `sin(t * PI)` 起伏), 0 触碰 §9.1 9 步任何 "vfx" (7) 步既有 6 verb VFX 任何 1 字段 1 alpha 公式 1 
layer 顺序. 任何"加新 1 verb 几何"必须**先**在 `_draw` 函数体加新 `draw_*` 调点 (Stage 4 1:1 严格, 0 复用既有 5 verb 几何 0 改 1 调点 0 改 1 顺序), 
**再**在 tooltip 8 行拼接 (T250 #168) 同步加 1 几何字段 (0 改 1 字段 0 改 1 顺序), 0 触碰 §9.1 9 步任何 "verb" (1) 步既有 6 verb 任何 1 几何 motif. 
任何"加新 1 跨段镜像"必须**先**在 §9.6.16 / §9.6.18 / §9.6.19 / §9.6.21 4 跨段加 1 镜像 (Stage 5 1:1 严格, 0 漏 1 跨段 0 改 1 字段), 0 触碰 §9.1 9 
步任何 const / var / 节点 (5 段序列仅在 6 verb VFX + 6 verb ability + 6 verb windup VFX + 6 verb PlayerStats + 6 verb 调色六元组 6 跨段 
24 镜像, 6 verb 子类 0 改 base 0 改, 含 1 verb Whisper T275 #194 收回完成).


### 9.6.23 7 桶 prewarm 5+1+1 polish 模式 (T066 + T184 + T185.B + F013.B + T220 跨 5 任务 ~140 轮落地) 文档化

> §9.6.15 记录 SaveSystem `audit_save_slots()` 4 状态巡检; §9.6.16 记录 player.gd 6 verb hit handler 5 件套 1:1; §9.6.17 记录 
> player.gd `_ready` 6 verb signal 桥接 5+1 件套 1:1; §9.6.18 记录 `_verb_ability_base.gd` 共享契约 1:1; §9.6.19 记录 
> `_verb_windup_vfx_base.gd` 共享契约 1:1; §9.6.20 记录 Hub ↔ archive 跨房间 transition flow 5 段; §9.6.21 记录 6 verb 成就系统 闭环 8 段; 
> §9.6.22 记录 6 verb VFX 视觉组 5 段. 本节记录 `audio_manager_enhanced.gd` prewarm aggregator 7 桶 5+1+1 canonical 1:1 序列 (Stage 
> 1 5 桶 baseline 预热 + Stage 2 +1 verb fire SFX 桶 + Stage 3 +1 verb cooldown READY 桶) + 3 跨调用方 1:1 镜像 aggregator — 跨 6 
> 任务 (T066 #4 + T181 #102 + T184 #102 + T185.B #103 + F013.B #106 + T220 #142) ~140 轮迭代落地, 0 漏 0 反序 0 改 1 桶. 任何"加新 1 桶" 
> (e.g. UI chime / save-slot preview / 7 verb+ SFX / 5 verb+ fire tail) 必须**先**确定其 5+1+1 落点 (5 桶 baseline 中插入 / +1+1 
> 末尾追加) + **再**在 `prewarm_all_sfx()` aggregator 末尾/中间加 1 行调点, **再**在 `title_screen.gd._prewarm_bgm()` (partial 4/5 
> baseline inline) / `hub_controller.gd enter_hub` (full aggregator) / `game_flow_controller.gd _on_state_changed` 
> (full aggregator) 末尾/中间加 1 行调点 (与 aggregator 顺序 1:1 镜像, 0 反序 0 漏 0 改 1 行). 跨 6 任务 0 漂动 0 漏 0 例外.

- **症状**: `audio_manager_enhanced.gd` prewarm aggregator 跨 5+1+1 = 7 个独立 `prewarm_*` 桶函数 (prewarm_music_streams + 
prewarm_hit_sfx + prewarm_shop_sfx + prewarm_misc_sfx + prewarm_verb_cooldown_tails + prewarm_verb_fire_sfx + 
prewarm_verb_cooldown_readys) 反复 polish 期间出现 6 类典型 fragile: (1) "加新 1 桶漏 aggregator 调点" — 新桶 (e.g. T220 #142 F022 加 
`prewarm_verb_fire_sfx` + `prewarm_verb_cooldown_readys` 2 桶) 落地时漏 `prewarm_all_sfx()` aggregator 中加 1 行调点, 玩家 pause 
menu 30 min 后回 hub 走 `prewarm_all_sfx()` 重预热时漏 1 桶, 任何 stream 0 触发即 0.1s+ synth 延迟; (2) "加新 1 桶漏 title screen 4/5 
baseline 调点" — 新 baseline 桶 (e.g. T184 #102 加 `prewarm_shop_sfx`) 落地时漏 `title_screen.gd._prewarm_bgm()` 末尾加 1 行 
`prewarm_<category>()` inline 调点, 玩家进游戏走 title screen 0 触发新 baseline 桶, archive_01 第一次 cast 全部 0.1s+ synth 延迟 (虽然 
aggregator 函数体已加); (3) "加新 1 桶漏 hub/state aggregator 调点" — 新桶落地时漏 `hub_controller.gd enter_hub` 或 
`game_flow_controller.gd _on_state_changed` 末尾加 1 行 aggregator 调点, 玩家 pause menu 30 min 后回 hub 走 `prewarm_all_sfx()` 
重预热时漏 1 桶, 任何 stream 0 触发即 0.1s+ synth 延迟; (4) "修 1 桶顺序漏 aggregator 顺序 0 同步" — 修 `prewarm_verb_cooldown_tails` 桶顺序时漏 
`prewarm_all_sfx()` aggregator 顺序 0 同步, gameplay timing 错乱 (e.g. fire 在 tail 之前 → cooldown events 紧邻 cast 0 完整闭环); (5) 
"加新 1 桶 5+1+1 结构 0 标注" — 新桶落地时 0 标注其 5+1+1 落点 (5 桶 baseline 中插入 / +1+1 末尾追加), 后续 polish 期"加新 1 桶"时 0 知道 5+1+1 顺序 → 
漂到末尾或漏 aggregator 调点; (6) "title screen partial 4/5 baseline vs aggregator full 7 桶 漂动" — title screen inline partial 
prewarm (4/5 baseline buckets) vs aggregator full prewarm (7 桶) 设计意图混: title screen 设计为 partial inline (无 cooldown 
state 时 4 个最常用 baseline 桶 inline, 0 走 aggregator), aggregator 设计为 full canonical 7 桶 (5+1+1 顺序, 0 漏 0 改 1 桶), polish 
期若漏 title screen 4/5 baseline 调点 0 区分 (e.g. T185.B #103 加 `prewarm_misc_sfx` 在 title screen 加 inline 调点 vs T220 #142 加 
`prewarm_verb_fire_sfx` 不在 title screen 加 inline 调点, 因为 verb_fire 是 cast 触发而非 title screen 触发), 会导致 title screen 
partial inline 与 aggregator full canonical 1:1 镜像错位. 6 类症状根因: 7 桶 prewarm aggregator 5+1+1 canonical 1:1 序列 + 3 跨调用方 
(title_screen partial inline + hub_controller full aggregator + game_flow_controller full aggregator) 0 文档化, polish 
期只能"凭记忆"同步 N 处调点 (1 aggregator + 1 title screen 4 inline + 2 aggregator cross-caller), 1 漏 1 = prewarm 链断.

- **触发场景**: `audio_manager_enhanced.gd` prewarm aggregator 7 桶 5+1+1 canonical 1:1 序列 + 3 跨调用方 1:1 镜像落地需同步 4 件套: (1) 
**`audio_manager_enhanced.gd` 7 桶 `prewarm_*` 函数 顶层模板** — `prewarm_music_streams()` (T066 #4) + `prewarm_hit_sfx()` 
(T181 #102) + `prewarm_shop_sfx()` (T184 #102) + `prewarm_misc_sfx()` (T185.B #103) + `prewarm_verb_cooldown_tails()` 
(F013.B #106) + `prewarm_verb_fire_sfx()` (T220 #142) + `prewarm_verb_cooldown_readys()` (T220 #142) 7 桶 0 改 
`prewarm_*` 命名 (`prewarm_` 前缀 1:1 严格, 0 改 0 重命名, 改 1 函数名 = `prewarm_all_sfx()` aggregator 调点 0 触发 / title screen inline 
调点 0 触发 / hub_controller 调点 0 触发); (2) **`prewarm_all_sfx()` aggregator 5+1+1 canonical 顺序** — 5 桶 baseline 
`prewarm_music_streams() → prewarm_hit_sfx() → prewarm_shop_sfx() → prewarm_misc_sfx() → prewarm_verb_cooldown_tails()` 
0 反序 0 漏 + +1 `prewarm_verb_fire_sfx()` 第 6 桶 0 反序 (在 tail 之后, cooldown events 紧邻 cast 顺序) + +1 
`prewarm_verb_cooldown_readys()` 第 7 桶 0 反序 (在 fire 之后, ready 是 cooldown 完成事件, 后续 cast 之前的 0 延迟是 "我可以再 cast 了" 反馈) = 7 
桶 1:1 严格, gameplay timing 前向走 (BGM starts → first cast fires → cooldown tail → cooldown complete → ready jingle → next 
cast); (3) **3 跨调用方 1:1 镜像 (title_screen partial inline + hub_controller full aggregator + game_flow_controller full 
aggregator)** — `title_screen.gd._prewarm_bgm()` 4/5 baseline inline 调点 (music + hit + shop + misc, T181/T184/T185.B 3 
轮迭代沉淀, 0 走 aggregator 是设计意图: title screen 无 cooldown state, 4/5 baseline 桶足够, 0 触碰 verb_cooldown_tails + verb_fire + 
verb_cooldown_readys 3 桶 inline) + `hub_controller.gd enter_hub` 末尾 `ame.call("prewarm_all_sfx")` 调点 (1 行, has_method 
守卫包裹 0 改, T184 #102 落地, full aggregator 7 桶 0 漏) + `game_flow_controller.gd _on_state_changed` 末尾 
`ame_prewarm.call("prewarm_all_sfx")` 调点 (1 行, has_method 守卫包裹 0 改, T184 #102 落地, full aggregator 7 桶 0 漏) = 3 跨调用方 
partial + full + full 1:1 镜像 aggregator, 1 漏 1 边 = prewarm 链断; (4) **5+1+1 桶内 0 改 cache 守卫** — 7 桶每桶 idempotent guard 
(e.g. `if not _verb_cooldown_tail_streams.has(verb_name):` 守卫 0 改 0 删 0 重排, T184 #102 落地后 0 触碰, 7 桶 aggregator 可重复触发 0 
副作用). 4 件套 1:1 同步, 1 漏 1 = prewarm 链断 (新桶 0 触发) / gameplay timing 错乱 (桶顺序错) / 跨调用方漏 1 边 (1 处 scene 0 走 prewarm) / title 
screen partial vs aggregator full 漂动 (4/5 baseline vs 7 桶 0 区分).

- **修复** (跨 6 任务 ~140 轮落地后已知 5+1+1 canonical 1:1 序列 + 7 桶 0 例外):
  - **T066 (#4) 第 1 桶 music baseline** — `src/scripts/audio_manager_enhanced.gd` 加 `prewarm_music_streams()` 函数 (T066 
落地, ~9 BGM 主题 0 合成延迟, title screen `_prewarm_bgm` 阶段一次性预热 9 stream), 1 桶 baseline 0 触碰后续 6 桶.
  - **T181 (#102) 第 2 桶 hit baseline** — `src/scripts/audio_manager_enhanced.gd` 加 `prewarm_hit_sfx()` 函数 (T181 落地, 4 
verb hit 12 stream 0 合成延迟, ~15 ms 一次性), 配合 `prewarm_all_sfx()` aggregator 1.0 版 (music → hit, 2 桶 baseline), 0 触碰 T066 
music 桶.
  - **T184 (#102) 第 3 桶 shop baseline + aggregator 4 件套 1:1 镜像落地** — `src/scripts/audio_manager_enhanced.gd` 加 
`prewarm_shop_sfx()` 函数 (T184 落地, 5 shop SFX 0 合成延迟) + `prewarm_all_sfx()` aggregator 加 1 行 `prewarm_shop_sfx()` (3 桶 
aggregator 1.1 版) + `title_screen.gd._prewarm_bgm()` 末尾加 1 行 `ame.call("prewarm_all_sfx")` 调点 (1 行) + 
`hub_controller.gd enter_hub` 末尾加 1 行 `ame.call("prewarm_all_sfx")` 调点 (1 行, has_method 守卫) + `game_flow_controller.gd 
_on_state_changed` 末尾加 1 行 `ame_prewarm.call("prewarm_all_sfx")` 调点 (1 行, has_method 守卫), 3 跨调用方 1:1 镜像落地.
  - **T185.B (#103) 第 4 桶 misc baseline** — `src/scripts/audio_manager_enhanced.gd` 加 `prewarm_misc_sfx()` 函数 (T185.B 
落地, F014 achievement unlock chime + F015 save-slot delete click 2 类 SFX 0 合成延迟, ~3 ms 一次性) + `prewarm_all_sfx()` 
aggregator 加 1 行 `prewarm_misc_sfx()` (4 桶 aggregator 1.2 版), 0 触碰 T066+T181+T184 3 桶既有顺序 0 反序 0 漏.
  - **F013.B (#106) 第 5 桶 verb_cooldown_tail baseline** — `src/scripts/audio_manager_enhanced.gd` 加 
`prewarm_verb_cooldown_tails()` 函数 (F013.B 落地, 5 verb cooldown TAIL jingle 5 stream 0 合成延迟, ~6 ms 一次性) + 
`prewarm_all_sfx()` aggregator 加 1 行 `prewarm_verb_cooldown_tails()` (5 桶 aggregator 1.3 版 final), 0 触碰 
T066+T181+T184+T185.B 4 桶既有顺序 0 反序 0 漏 0 改 1 桶, 5 桶 baseline 全部到位 (5 verb cooldown TAIL 5 stream 全部 0 合成延迟, verb 自身事件 + 
cast 紧邻播放链完整).
  - **T220 (#142) F022 +1 verb fire SFX + +1 verb cooldown READY 桶** — `src/scripts/audio_manager_enhanced.gd` 加 
`prewarm_verb_fire_sfx()` 函数 (T220 落地, 5 verb fire SFX 5 stream 0 合成延迟, ~15 ms 一次性) + `prewarm_verb_cooldown_readys()` 
函数 (T220 落地, 5 verb cooldown READY jingle 5 stream 0 合成延迟, ~10 ms 一次性) + `prewarm_all_sfx()` aggregator 加 2 行 
`prewarm_verb_fire_sfx()` (第 6 桶, tail 之后) + `prewarm_verb_cooldown_readys()` (第 7 桶, fire 之后), 5 桶 → 7 桶 完整闭环, 
gameplay timing 前向走 (BGM starts → first cast fires → cooldown tail → cooldown complete → ready jingle → next cast). 30 
verb-related stream (5 fire + 5 hit + 5 tail + 5 ready + 5 成就 chime + 5 misc) 全部 0 合成延迟从 session 1 frame 0. 0 触碰 5 桶 
baseline 顺序 0 反序 0 漏 0 改 1 桶; 0 触碰 3 跨调用方 (`title_screen.gd._prewarm_bgm` + `hub_controller.gd enter_hub` + 
`game_flow_controller.gd _on_state_changed`) 0 改 1 字符 0 删 1 行 0 触碰 has_method 守卫.
  - 跨 6 任务 ~140 轮落地后 0 漂动 0 漏, 7 桶 prewarm 5+1+1 canonical 1:1 序列 100% 闭环, 5 桶 baseline (music + hit + shop + misc + 
verb_cooldown_tail) 1:1 严格分离 (0 漏 0 反序 0 改 1 桶 0 改 1 桶顺序) + +1+1 末尾追加 (verb_fire + verb_cooldown_ready, 0 漏 0 反序 0 改 1 
桶 0 改 1 桶顺序) + 3 跨调用方 1:1 镜像 aggregator (title_screen + hub_controller + game_flow_controller, 0 漏 1 边 0 改 1 调点). 6 任务 
0 后续 主要 pre-existing 风险.

- **预防**:
  1. 任何 polish 期给"加新 1 桶 prewarm 类别" (e.g. UI chime / save-slot preview / 7 verb+ SFX / 5 verb+ fire tail / 8 verb+ 
ready jingle) 任务加新函数 / 加新桶 / 加新 verb 时**必须**严格按 5+1+1 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 桶. 5+1+1 0 触碰边界: **5 桶 
baseline 0 改 0 漏 0 反序** (T066+T181+T184+T185.B+F013.B 落地后 `prewarm_music_streams` + `prewarm_hit_sfx` + 
`prewarm_shop_sfx` + `prewarm_misc_sfx` + `prewarm_verb_cooldown_tails` 5 桶 1:1 严格, 改 1 桶名 = aggregator 调点 0 触发, 改 1 
桶顺序 = gameplay timing 错乱), **+1 桶 0 改 0 漏 0 反序** (T220 落地后 `prewarm_verb_fire_sfx` 第 6 桶 0 改 0 漏 0 改顺序 = fire 0 在 tail 
之后, cooldown events 紧邻 cast 0 完整闭环), **+1 桶 0 改 0 漏 0 反序** (T220 落地后 `prewarm_verb_cooldown_readys` 第 7 桶 0 改 0 漏 0 改顺序 
= ready 0 在 fire 之后, "我可以再 cast 了" 反馈 0 完整闭环), **3 跨调用方 0 改 0 漏 0 反序** (`title_screen.gd._prewarm_bgm` 4/5 baseline 
inline + `hub_controller.gd enter_hub` 1 aggregator + `game_flow_controller.gd _on_state_changed` 1 aggregator 3 调点 0 改 
0 漏 0 改 1 行, 改 1 调点 = 该 scene 0 走 prewarm). 任何"加新 1 桶"必须**先**确定其 5+1+1 落点 (5 桶 baseline 中插入 / +1+1 末尾追加) + title_screen 
是否需要 inline (baseline 5 桶 yes / +1+1 桶 no, 因为 +1+1 桶需要 cast/cooldown 触发), **再**在 `audio_manager_enhanced.gd` 加 1 个 
`prewarm_<category>` 函数 (顶层模板 1:1 严格, 0 改 0 删 0 重排 7 桶既有函数体), **再**在 `prewarm_all_sfx()` aggregator 末尾/中间加 1 行调点 (5+1+1 
顺序 1:1 严格, 0 反序 0 漏 0 改 1 行), **再**(若 baseline 5 桶)在 `title_screen.gd._prewarm_bgm()` 末尾/中间加 1 行 `prewarm_<category>()` 
inline 调点 (partial 4/5 → 4/5 + 1 baseline 镜像, 0 改 0 删, has_method 守卫 0 改), **再**在 `hub_controller.gd enter_hub` 末尾/中间加 
1 行 aggregator 调点 (full aggregator 1:1 镜像, has_method 守卫 0 改 0 删), **再**在 `game_flow_controller.gd _on_state_changed` 
末尾/中间加 1 行 aggregator 调点 (full aggregator 1:1 镜像, has_method 守卫 0 改 0 删).
  2. **5+1+1 桶顺序 0 改 1 顺序** (T066 + T181 + T184 + T185.B + F013.B + T220 落地后 0 触碰边界) — 7 桶顺序 `prewarm_music_streams → 
prewarm_hit_sfx → prewarm_shop_sfx → prewarm_misc_sfx → prewarm_verb_cooldown_tails → prewarm_verb_fire_sfx → 
prewarm_verb_cooldown_readys` 按 gameplay timing 前向走, 0 反序 0 漏 0 改 1 桶, 任何"加新 1 桶"必须在文件顶部 docblock 写明其 5+1+1 落点 (5 桶 
baseline 中插入 / +1+1 末尾追加) + gameplay timing 理由 + title_screen 是否需要 inline (baseline 5 桶 yes / +1+1 桶 no), source-grep 
验证 7 桶 `prewarm_*` 函数 + `prewarm_all_sfx()` aggregator 7 行调点 0 漏 0 改 1 顺序.
  3. **3 跨调用方 0 漏 1 边** (T184 #102 + T220 #142 落地后 0 触碰边界) — `title_screen.gd._prewarm_bgm()` 4/5 baseline inline 调点 (4 
个 `if ame and ame.has_method("prewarm_<category>"): ame.call("prewarm_<category>")` 块, music + hit + shop + misc, T181 
#102 / T184 #102 / T185.B #103 3 轮迭代沉淀, 0 触碰 0 改 0 删 0 重排, has_method 守卫 0 改) + `hub_controller.gd enter_hub` 末尾 
`ame.call("prewarm_all_sfx")` 调点 (1 行, has_method 守卫包裹 0 改 0 删 0 重排, T184 #102 落地) + `game_flow_controller.gd 
_on_state_changed` 末尾 `ame_prewarm.call("prewarm_all_sfx")` 调点 (1 行, has_method 守卫包裹 0 改 0 删 0 重排, T184 #102 落地) = 3 
跨调用方 (partial inline + full aggregator + full aggregator) 0 漏 1 边 0 改 1 调点, 任何"加新 1 跨调用方" (e.g. settings_menu._ready / 
save_load_menu._ready / shop_menu._ready) 时**必须**在末尾加 1 行 aggregator 调点 (1:1 镜像, has_method 守卫 0 改 0 删), source-grep 验证 
3 跨调用方 0 漏 1 边.
  4. **7 桶 idempotent guard 0 改 0 删** (T184 #102 落地后 0 触碰边界) — 7 桶每桶 idempotent guard (e.g. `if not 
_verb_cooldown_tail_streams.has(verb_name): _verb_cooldown_tail_streams[verb_name] = 
_generate_verb_cooldown_tail_jingle(start_midi)` 守卫 0 改 0 删 0 重排, prewarm 跑了也再走守卫, 7 桶 aggregator 可重复触发 0 副作用), 任何"加新 1 
桶"必须**先**写 idempotent guard 1 行 (e.g. `if not _<category>_streams.has(<key>): _<category>_streams[<key>] = 
_generate_<category>(<args>)`), **再**在桶函数体内循环 0 触碰 0 反序 0 漏, source-grep 验证 7 桶 guard 0 漏 0 改 1 行.
  5. **桶函数命名 0 改 1 字符** (T066 + T181 + T184 + T185.B + F013.B + T220 落地后 0 触碰边界) — `prewarm_music_streams` / 
`prewarm_hit_sfx` / `prewarm_shop_sfx` / `prewarm_misc_sfx` / `prewarm_verb_cooldown_tails` / `prewarm_verb_fire_sfx` / 
`prewarm_verb_cooldown_readys` 7 桶函数名 0 改 0 删 0 重命名, 改 1 字符 = `prewarm_all_sfx()` aggregator 调点 0 触发 + 
`title_screen.gd._prewarm_bgm` 4 inline 调点 0 触发 + `hub_controller.gd enter_hub` 调点 0 触发 + `game_flow_controller.gd 
_on_state_changed` 调点 0 触发 = prewarm 链断, source-grep 验证 7 桶函数名 0 漏 0 改 1 字符.
  6. **0 触碰 lazy-init 守卫** (T066 + T181 落地后 0 触碰边界) — `play_pulse` / `play_bind` / `play_cut` / `play_echo` / 
`play_wave_fire` (F004.B #96) 内部 lazy-init 守卫生成 `_pulse_stream` / `_bind_stream` / `_cut_stream` / `_echo_stream` / 
`_wave_fire_stream` 5 stream 守卫 0 删 (T220 prewarm 跑了也再走守卫, 0 副作用), `play_verb_cooldown_ready(verb_name)` 内部 lazy-init 
守卫生成 `_verb_cooldown_streams[verb_name]` 5 stream 守卫 0 删 (同样 0 副作用), 任何"加新 1 桶"必须**先**在 lazy-init 守卫 0 删 0 改 0 重排, 
**再**在 prewarm 桶函数体内 idempotent guard 0 删 0 改, source-grep 验证 5 verb lazy-init 守卫 0 删 0 改 1 行.
  7. 已知 drift risk: 7 桶 prewarm aggregator 5+1+1 canonical 1:1 序列 **可能**漏 1 跨调用方 1:1 镜像 (e.g. T184 #102 落地时漏 
`game_flow_controller.gd _on_state_changed` 1 调点, pause menu 30 min 后回 hub 走 prewarm 链断, 玩家 cast 5 verb 仍有 0.1s+ synth 
延迟). 监控建议: 任何"未来给 prewarm aggregator 加新 1 跨调用方" (e.g. settings_menu._ready / save_load_menu._ready / shop_menu._ready / 
pause_menu._ready) 必须**先**在 3 跨调用方 (`title_screen.gd._prewarm_bgm` 4/5 baseline inline + `hub_controller.gd enter_hub` 
1 aggregator + `game_flow_controller.gd _on_state_changed` 1 aggregator) 1:1 镜像 0 漏 1 边. T184 (#102) 3 跨调用方 1:1 
镜像落地后任何新 scene _ready 加 1 行 aggregator 调点是 §9.6.23 5+1+1 序列的"母集" (1 桶 + 1 跨调用方 1:1 镜像, 7 桶 × 3 调点 = 21 镜像 1:1 严格). 另 1 
类已知 drift: title screen partial 4/5 baseline inline vs aggregator full 7 桶 设计意图混 (T220 #142 落地时 title screen 0 加 
`prewarm_verb_fire_sfx` inline 调点, 因为 verb_fire 是 cast 触发而非 title screen 触发, 是设计意图而非漏调; 后续 polish 期若加新 baseline 
桶必须**先**决定是否在 title screen 加 inline 调点, 加新 +1+1 桶则在 title screen 0 加 inline 调点, 是 §9.6.23 5+1+1 序列的"边界").

**0 副作用**: T066 / T181 / T184 / T185.B / F013.B / T220 任何 const / var 0 触碰 (5 verb lazy-init 守卫 0 改 0 删 0 重排 + 7 桶 
`prewarm_*` 函数体 0 改 0 删 0 重排 + `prewarm_all_sfx()` aggregator 7 行调点 0 改 0 删 0 重排 + 3 跨调用方 
(`title_screen.gd._prewarm_bgm` + `hub_controller.gd enter_hub` + `game_flow_controller.gd _on_state_changed`) 0 改 0 删 
0 重排); 0 触碰 §9.6.4 调色六元组 (T279 0 改 6 verb 调色 1 hex); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T279 0 改 6 verb 
`_on_<verb>_hit` 函数体任何 const / var / 屏染 / 屏抖 / VFX flash / 音频); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T279 0 改 6 verb 
主信号 + 命中信号 + 结束信号 + 扩展信号 + PlayerActionGate register / unregister); 0 触碰 §9.6.18 16 件套 verb ability base (T279 0 改 6 
verb ability 子类任何 const / var / `@export` / `@onready` / 5 共享方法 / `_exit_tree` 兜底 / `is_globally_blocking` 扩展); 0 触碰 
§9.6.19 7 件套 windup VFX base (T279 0 改 6 verb windup VFX 子类任何 const / var / `_ready()` / `_process(delta)` / 
`_activate_windup_tween()` / `fade_out_and_free()`); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 transition flow (T279 0 改 GFC + 
room_transition + room_door + GameState 5 段序列); 0 触碰 §9.6.21 8 段成就序列 (T279 0 改 PlayerStats 6 verb 字段 + 
record_ability_used 6 branch + get_stat 6 entry + set_stat 6 entry + reset_run 6 行 + all_abilities_used 6 项 + 
achievements.json 4 tier ID + ICON_COLORS 6 entry + PNG 双路径); 0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T279 0 改 6 verb VFX 文件 
任何 const / var / `@export` color 字段 / 5+ layer 视觉组 / 几何 / 跨段镜像); 0 触碰 check_smoke_consistency.sh (T279 0 改任何规则); 0 触碰 
_parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓);
 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, audio_manager_enhanced.gd 7 桶 prewarm 函数体 0 改 0 删 0 重排 + 
prewarm_all_sfx() aggregator 7 行 0 改 0 删 0 重排 + 3 跨调用方 0 改 0 删 0 重排); 0 行为变化, 1 文档 1 段 (§9.6.23 ~50 行) + 1 smoke test 
(T279 19+ 断言).

**与 §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 关系** — §9.6.15 覆盖 SaveSystem 
`audit_save_slots()` 4 状态巡检 (autoload 跨调用方 1:1 镜像); §9.6.16 覆盖 player.gd 6 verb hit handler 5 件套 1:1 严格分离; §9.6.17 覆盖 
player.gd `_ready` 6 verb signal 桥接 5+1 件套 1:1 严格分离; §9.6.18 覆盖 `_verb_ability_base.gd` 6 verb ability 共享契约 1:1 严格分离; 
§9.6.19 覆盖 `_verb_windup_vfx_base.gd` 6 verb windup VFX 共享契约 1:1 严格分离; §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow 5 段 
canonical 1:1 序列; §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 canonical 1:1 序列; §9.6.22 覆盖 6 verb VFX 视觉组 5 段 canonical 1:1 序列. 
§9.6.23 覆盖 `audio_manager_enhanced.gd` prewarm aggregator 7 桶 5+1+1 canonical 1:1 序列 (5 桶 baseline + +1 verb fire SFX + 
+1 verb cooldown READY 桶) + 3 跨调用方 1:1 镜像 aggregator. §9.6.15 / §9.6.16 关注"autoload / verb hit handler 1:1 镜像", §9.6.17 
关注"verb signal 桥接 1:1 严格分离", §9.6.18 关注"verb ability 类层级共享契约", §9.6.19 关注"verb windup VFX 类层级共享契约", §9.6.20 关注"跨 scene 
transition flow 时序契约", §9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格)", §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约", §9.6.23 
关注"prewarm aggregator 5+1+1 序列 1:1 严格分离契约 + 3 跨调用方 1:1 镜像". 8 套 polish 模式 0 反序 0 漏 0 改 1 字符. 任何"加新 1 桶 prewarm 
类别"必须**先**按 §9.6.23 5+1+1 序列 1:1 复制 (0 反序 0 漏 0 改 1 桶 0 改 1 跨调用方 1 调点), 0 触碰 §9.6.15-§9.6.22 7 套既有 polish 模式任何 1 字符. 
§9.6.22 与 §9.6.23 0 互混 0 复用 0 共享: §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约 (Stage 1-5: class_name + 调色 + layer + 几何 + 
跨段镜像)" (与 prewarm 桶链 0 相关), §9.6.23 关注"prewarm aggregator 5+1+1 序列 1:1 严格分离契约 (5 桶 baseline + +1+1 末尾追加)" (与 6 verb VFX 
视觉组 0 相关). §9.6.23 是 8 套 polish 模式**唯一**关注"prewarm aggregator 5+1+1 序列 1:1 严格分离 + 3 跨调用方 1:1 镜像"的 1 套, 与 
§9.6.15-§9.6.22 7 套 audit / verb / VFX / transition / 成就 / VFX 视觉组 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.23 5+1+1 序列走 §9.1 9 步落地的"audio" (8) 步 (`src/scripts/audio_manager_enhanced.gd` 
7 桶 `prewarm_*` 函数 + `prewarm_all_sfx()` aggregator + `src/scripts/title_screen.gd._prewarm_bgm` 1 调点 + 
`src/scripts/hub_controller.gd enter_hub` 1 调点 + `src/scripts/game_flow_controller.gd _on_state_changed` 1 调点), 0 触碰 
§9.1 9 步任何 "verb" (1) + "ability" (2) + "const" (3) + "input" (4) + "shop" (5) + "icon" (6) + "vfx" (7) + "HUD 冷光勾边" 
(9) 8 步 (prewarm 桶链 0 走 verb enum + ability 子类 + const 字段 + input map + shop 系统 + verb icon PNG 双路径 + VFX 视觉组 + HUD 
渲染). 任何"加新 1 桶 prewarm 类别"必须**先**按 §9.1 9 步落地 (含 "audio" (8) 步), **再**按 §9.6.23 5+1+1 序列 1:1 复制 (0 反序 0 漏 0 改 1 桶 0 改 1 
跨调用方 1 调点), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "const" (3) + "input" (4) + "shop" (5) + "icon" (6) + "vfx" 
(7) + "HUD 冷光勾边" (9) 8 步. 任何"加新 1 桶"必须**先**在 `audio_manager_enhanced.gd` 加 1 个 `prewarm_<category>` 函数 (顶层模板 1:1 严格, 0 
漏 0 改 1 字符, idempotent guard 0 改 0 删), **再**在 `prewarm_all_sfx()` aggregator 末尾/中间加 1 行调点 (5+1+1 顺序 1:1 严格, 0 漏 0 改 1 
行), **再**在 `title_screen.gd._prewarm_bgm` 末尾/中间加 1 行 aggregator 调点 (1:1 镜像, has_method 守卫 0 改 0 删), **再**在 
`hub_controller.gd enter_hub` 末尾/中间加 1 行 aggregator 调点 (1:1 镜像, has_method 守卫 0 改 0 删), **再**在 `game_flow_controller.gd 
_on_state_changed` 末尾/中间加 1 行 aggregator 调点 (1:1 镜像, has_method 守卫 0 改 0 删), 0 触碰 §9.1 9 步任何 const / var / 节点 (5+1+1 
序列仅在 7 桶 prewarm 函数 + 1 aggregator + 3 跨调用方, 6 verb 子类 0 改 base 0 改, 5 verb lazy-init 守卫 0 删 0 改).

---
