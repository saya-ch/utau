# Polish Patterns 9.6.36–36

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.36–9.6.36，共 1 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.36-36.md

<!-- shard 36-36 共 1 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.36 PlayerActionGate 接入 4 件套 1:1 严格分离契约 polish 模式 (T075 + T142 #75 + T145 #76 + D001 #82 跨 4 任务 ~140 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 26 套 polish 模式均在 "pause_menu / 6 verb / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / 
> audio 数据层" 范畴, 0 涉及 "PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约" 落地. §9.6.16 (T271 #190 落地) 覆盖 6 verb hit handler 
> 5 件套, §9.6.17 (T272 #191 落地) 覆盖 6 verb signal 桥接 5+1 件套, §9.6.18 (T273 #192 落地) 覆盖 `_verb_ability_base.gd` 16 件套, 
> §9.6.19 (T274 #193 落地) 覆盖 `_verb_windup_vfx_base.gd` 7 件套, §9.6.33 (T289 #212 落地) 覆盖 6 verb 接入 4 件套 字节码一致性 — 这 5 
> 段分别覆盖 6 verb 单层 / 跨层 接入 字节码契约, 但**未**覆盖 "PlayerActionGate 单 autoload 4 件套 接入 (`autoload 字段 _player` + `register / 
> unregister 2 方法` + `is_blocked() 3 probe` + `get_player() 1 helper`) 1:1 严格分离契约" 必须同时 1:1 镜像 4 件套 (Stage 1 autoload 
> 字段 + Stage 2 register / unregister + Stage 3 is_blocked() + Stage 4 get_player()) 的契约. 本节 (T292 #216 落地) 
> 首个落在**PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约** (`src/autoload/player_action_gate.gd` 1 weak-typed `var 
> _player: Node = null` 字段 + `register_player(p)` 幂等 (last-registered wins) + `unregister_player(p)` 守门 (only clear if 
> same player) + `is_blocked()` 3 probe (`_is_dying` + `wave_ability` + `is_globally_blocking` 4-step probe 模式) + 
> `get_player()` 1 convenience helper) polish 模式 — 跨 4 件套 (Stage 1 autoload 字段 1:1 严格 0 漏 1 字段 + Stage 2 register / 
> unregister 1:1 严格 0 漏 1 边 + Stage 3 is_blocked() 1:1 严格 0 漏 1 probe + Stage 4 get_player() 1:1 严格 0 漏 1 helper) — 跨 1 
> 套 polish 模式 (T292 #216 落地) 跨 0 任务 (~216 轮落地, 0 漂动 0 后续 主要 pre-existing 风险). 任何"加新 1 probe" (e.g. 未来加 `stun` / `pause` 
> / `cutscene` probe) 或"加新 1 方法" (e.g. 未来加 `is_stunned()` / `pause_player()`) 或"加新 1 字段" (e.g. 未来加 `_stun_timer` 
> 全局晕眩计时器) 或"加新 1 边" (e.g. 未来加 6 verb 子类 → `is_globally_blocking()` 多 1 边 接入 PlayerActionGate 3 probe) 必须**先**按 Stage 1 
> autoload 字段 1:1 严格 加 1 entry (0 漏 1 字段 0 改 1 字段 0 反序 0 反向), **再**按 Stage 2 register / unregister 1:1 镜像 (1:1 严格, 0 漏 
> 1 边 0 改 1 边 0 反序 0 反向), **再**按 Stage 3 is_blocked() 1:1 镜像 (1:1 严格, 0 漏 1 probe 0 改 1 probe 0 反序 0 反向), **再**按 Stage 
> 4 get_player() 1:1 镜像 (1:1 严格, 0 漏 1 helper 0 改 1 helper 0 反序 0 反向). 跨 1 套 polish 模式 × 4 件套 = 4 元素 1:1 严格 0 漏 1 元素 0 
> 改 1 字段 0 改 1 字符 0 例外.

- **症状**: `PlayerActionGate` 单 autoload (`src/autoload/player_action_gate.gd`, T075 + T142 #75 + T145 #76 + D001 #82 跨 
4 任务 ~140 轮落地) 现有的 4 件套 (1 weak-typed `var _player: Node = null` autoload 字段 + `register_player(p)` 1 方法 + 
`unregister_player(p)` 1 方法 + `is_blocked()` 1 复合 blocking check (含 `_is_dying` 1 probe + `wave_ability` 1 probe + 
`is_globally_blocking` 1 probe 共 3 probe) + `get_player()` 1 convenience helper), 在反复 polish (T075 → T142 #75 → T145 
#76 → D001 #82 跨 4 任务 ~140 轮落地) 期间出现 4 类典型 fragile: (1) **"加新 1 probe 漏 1 件套"** — 加新 1 probe (e.g. 假设未来加 `stun` probe 
控制全局晕眩: `if _player.get("_stun_timer") > 0.0: return true`) 落地时漏 1 件套 (e.g. 漏 Stage 1 autoload 字段 中 `_stun_timer` 字段, 
或漏 Stage 3 is_blocked() 中 3 probe → 4 probe 顺序, 或漏 Stage 4 get_player() 中 1 helper), 1 probe 扩展 0 4 件套 0 闭环 0 漂动 (1 
probe 0 autoload 字段 = 1 probe 0 状态 0 漂动; 1 probe 0 is_blocked() = 1 probe 0 blocking check 0 漂动; 1 probe 0 get_player() 
= 1 probe 0 boss / cutscene 入口 0 漂动); (2) **"加新 1 方法 漏 1 边"** — 加新 1 方法 (e.g. 假设未来加 `is_stunned()` / `pause_player()` / 
`resume_player()` 3 方法) 落地时漏 1 边 (e.g. 漏 Stage 2 register / unregister 镜像漏 1 边 `unregister_player` 1:1 严格 0 改 0 漏 0 反序 
0 反向, 或漏 Stage 3 is_blocked() 中加新 1 边 `if _stun_timer > 0.0: return true` 与 is_stunned() 1:1 严格同步), 1 方法扩展 0 4 件套 0 闭环 
0 漂动 (1 方法 0 register / unregister 镜像 = 1 方法 0 对称反注册 0 漂动; 1 方法 0 is_blocked() = 1 方法 0 复合 check 0 漂动); (3) **"修 1 字段 / 
1 方法 0 触碰既有 3 字段 / 3 方法"** — 修 1 autoload 字段 (e.g. 改 `_player: Node` → `_player: Node2D` 类型 hint) 或修 1 方法 (e.g. 改 
`register_player(p)` body 0 改 `if _player == p: return` 守卫) 时改硬编码 0 改 既有 3 字段 / 3 方法 (3 字段任何 1 字段 / 3 方法任何 1 方法), 跨字段 
1:1 漂动 (1 修 1 字段 0 改既有 3 字段 = 1 字段 0 4 字段 0 闭环 0 漂动; 1 修 1 方法 0 改既有 3 方法 = 1 方法 0 4 方法 0 闭环 0 漂动); (4) **"加新 1 边 6 verb 
接入 漏 1 边 is_globally_blocking"** — 加新 1 6 verb 子类 接入 PlayerActionGate 3 probe (e.g. 假设未来加 7th verb "Drain" 接入 
PlayerActionGate 3 probe 需补 `_drain_ability.is_globally_blocking()` 1 边) 漏 1 边 `is_globally_blocking()` (6 verb → 7 
verb 接入 0 镜像 1 边), 1 verb 接入 0 4 件套 0 闭环 0 漂动 (1 verb 0 is_globally_blocking = 1 verb 0 复合 blocking check 0 漂动). 4 
类症状根因: PlayerActionGate 单 autoload 4 件套 1:1 序列 0 文档化, 任何新 1 probe / 1 方法 / 1 字段 / 1 边接入只能"凭经验" 1:1 镜像 4 件套 (1 autoload 
字段 1:1 严格 + 1 register / unregister 1:1 严格 + 1 is_blocked() 1:1 严格 + 1 get_player() 1:1 严格), 1 漏 1 件套 = 1 probe / 1 方法 
/ 1 字段 / 1 边 0 4 件套 0 闭环 0 漂动 (1 漏 1 件套 0 4 件套 0 1:1 严格分离契约 0 漂动 / 1 漏 1 边 0 字段引用 0 1:1 严格 0 漂动).

- **触发场景**: 加新 1 probe / 1 方法 / 1 字段 (T145 #76 模式参考, 假设未来加 `stun` probe) 时**必须**严格按 4 件套 1:1 复制 T145 落地模式: (1) **Stage 
1 autoload 字段 1:1 严格** — 在 `player_action_gate.gd` 顶部 字段声明 区 加 1 entry (e.g. T145 落地时 1 weak-typed `var _player: Node = 
null` 字段 1:1 严格, 假设未来加 `stun` 字段, 顶部 加 `var _stun_timer: float = 0.0` 1 字段, 与既有 1 字段 0 漏 0 改 0 反序 0 反向, 0 触碰既有 1 字段任何 1 
字符, 0 漏 1 字段, 0 触碰既有 4 件套), source-grep 验证 `player_action_gate.gd` 字段声明 1 字段 → 2 字段 0 漏 0 改 0 漏 1 entry 0 反向; (2) 
**Stage 2 register / unregister 1:1 严格** — 在 `player_action_gate.gd` `register_player(p)` / `unregister_player(p)` 2 方法 
镜像 (e.g. T145 落地时 `register_player(p)` 1:1 严格 (idempotent + last-registered wins) + `unregister_player(p)` 1:1 严格 (only 
clear if `_player == p` 守卫, 避免 0 触碰 新加载 player), 假设未来加 `pause_player()` / `resume_player()` 2 方法, 0 触碰既有 register / 
unregister 任何 1 字符, 加新 1 边 `pause_player` / `resume_player` 1:1 严格 0 漏 0 改 0 反向), source-grep 验证 2 方法 → 4 方法 0 漏 0 改 0 
漏 1 方法 0 反向; (3) **Stage 3 is_blocked() 1:1 严格** — 在 `player_action_gate.gd` `is_blocked()` 函数 加 1 probe (e.g. T145 落地时 
3 probe 1:1 严格 (`_is_dying` 1 + `wave_ability` 1 + `is_globally_blocking` 1, 4-step probe 模式: `_player == null` 守卫 → 
`_player.get("_is_dying")` 1 probe → `_player.get("wave_ability")` + `has_method` 守卫 + `call("is_globally_blocking")` 2 
probe → `return bool(...)` 1 probe), 假设未来加 `stun` probe → `is_blocked()` 末尾 加 `if _stun_timer > 0.0: return true` 1 
probe, 0 触碰既有 3 probe 任何 1 字符, 加新 1 边 `stun` probe 1:1 严格 0 漏 0 改 0 反向), source-grep 验证 `is_blocked()` 3 probe → 4 
probe 0 漏 0 改 0 漏 1 probe 0 反向; (4) **Stage 4 get_player() 1:1 严格** — 在 `player_action_gate.gd` `get_player()` helper 
函数 保持 1:1 严格 (e.g. T145 落地时 `get_player()` 1:1 严格 (return `_player`, 0 改 0 漏 0 反序 0 反向, boss / cutscene scripts 1:1 严格 
entry point), 加新 1 字段 / 1 方法 / 1 probe 时 0 触碰 `get_player()` 任何 1 字符, helper 1:1 严格 0 漏 0 改 0 反向), source-grep 验证 
`get_player()` 1 helper 0 漏 0 改 0 漏 1 字符 0 反向. 4 件套 1:1 同步, 1 漏 1 件套 / 1 漏 1 边 = 1 probe / 1 方法 / 1 字段 / 1 边 0 4 件套 0 
闭环 0 漂动. T145 #76 0 触碰既有 0 改 `_is_dying` / `wave_ability` / `is_globally_blocking` 3 probe 任何 1 字符 (0 改 autoload 字段 任何 
1 字段, 0 改 register / unregister 任何 1 字符, 0 改 is_blocked() 任何 1 probe, 0 改 get_player() 任何 1 字符).

- **修复** (跨 1 套 polish 模式 T292 #216 落地后已知 4 件套 0 例外):
  - **T292 #216 首个 PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约 落地 (`player_action_gate.gd` 1 weak-typed `var 
_player: Node = null` 字段 + `register_player(p)` 1 幂等方法 + `unregister_player(p)` 1 守门方法 + `is_blocked()` 1 复合 check (3 
probe: `_is_dying` + `wave_ability` + `is_globally_blocking`) + `get_player()` 1 convenience helper)** — §9.6.36 段 是 1 
套 polish 模式首个 1:1 包含 4 件套 (1 `player_action_gate.gd` autoload 字段 1:1 严格 + 1 `register_player / unregister_player` 2 方法 
1:1 严格 + 1 `is_blocked()` 3 probe 1:1 严格 + 1 `get_player()` 1 helper 1:1 严格) 的 PlayerActionGate 接入 polish 模式, 4 件套 1:1 
严格分离 (1 autoload 字段 1:1 严格 0 漏 0 改 + 1 register / unregister 2 方法 1:1 严格 0 漏 0 改 + 1 is_blocked() 3 probe 1:1 严格 0 漏 0 
改 + 1 get_player() 1 helper 1:1 严格 0 漏 0 改, 0 漏 1 件套 0 改 1 字段 0 反序 0 反向), 0 触碰既有 (T292 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / 
§9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / 
§9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 26 套 polish 
模式任何 1 字符, 0 触碰游戏代码 .gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容, 0 触碰 
ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).
  - **T292 #216 Stage 1 autoload 字段 1:1 严格分离契约 落地 (`player_action_gate.gd` 1 weak-typed `var _player: Node = null` 
字段)** — §9.6.36 段 1:1 显式 autoload 字段 (T292 落地时 `player_action_gate.gd` 1 字段 `var _player: Node = null` 1:1 严格 
(weak-typed `Node` 而非强类型 `Player` 引用, 0 hard dependency on `Player.gd` class_name resolution order, callers in 
player.gd 0 触碰 `.has_method` 守卫), 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), autoload 字段 1:1 严格 (1 `player_action_gate.gd` 1 字段 1:1 
严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), 0 触碰既有 (T292 0 改 `player_action_gate.gd` 
`_player` 字段任何 1 字符).
  - **T292 #216 Stage 2 register / unregister 2 方法 1:1 严格分离契约 落地 (`player_action_gate.gd` `register_player(p)` + 
`unregister_player(p)` 2 方法)** — §9.6.36 段 1:1 显式 register / unregister (T292 落地时 `player_action_gate.gd` 
`register_player(p)` 1:1 严格 (idempotent: last-registered wins, defensive against scene transitions where stale player 
might still hold a callback, `_player = p` 1 行) + `unregister_player(p)` 1:1 严格 (only clear if `_player == p` 守卫, 避免 0 
触碰新加载 player, `if _player == p: _player = null` 3 行), 0 漏 1 方法 0 改 1 方法 0 反序 0 反向), register / unregister 1:1 严格 (1 2 
方法 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 方法 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T292 0 改 
`player_action_gate.gd` `register_player` / `unregister_player` 任何 1 字符).
  - **T292 #216 Stage 3 is_blocked() 3 probe 1:1 严格分离契约 落地 (`player_action_gate.gd` `is_blocked()` 4-step probe 模式: 1 
守卫 + 3 probe)** — §9.6.36 段 1:1 显式 is_blocked() 3 probe (T292 落地时 `player_action_gate.gd` `is_blocked()` 4-step probe 
模式 1:1 严格: (a) `if _player == null: return false` 1 守卫 (避免 0 player 时 `null` 访问) + (b) `if _player.get("_is_dying") == 
true: return true` 1 probe (T075 _is_dying 0.5s 死亡 lay-down + 1.0s fade-out window) + (c) `var wave_ability: Node = 
_player.get("wave_ability")` + `if wave_ability == null: return false` + `if not 
wave_ability.has_method("is_globally_blocking"): return false` 1 probe (T142 #75 wave_ability.is_globally_blocking 
0.10s wave windup) + (d) `return bool(wave_ability.call("is_globally_blocking"))` 1 probe (delegate, 0 shadow), 0 漏 1 
probe 0 改 1 probe 0 反序 0 反向), is_blocked() 3 probe 1:1 严格 (1 3 probe 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 
0 漏 1 probe 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T292 0 改 `player_action_gate.gd` `is_blocked()` 任何 1 字符).
  - **T292 #216 Stage 4 get_player() 1 helper 1:1 严格分离契约 落地 (`player_action_gate.gd` `get_player()` 1 convenience 
helper)** — §9.6.36 段 1:1 显式 get_player() helper (T292 落地时 `player_action_gate.gd` `get_player() -> Node` 1:1 严格 
(return `_player` 或 `null`, boss / cutscene scripts 1:1 严格 entry point, 0 触碰 player 内部 0 hard Player-class import), 0 漏 
1 helper 0 改 1 helper 0 反序 0 反向), get_player() helper 1:1 严格 (1 1 helper 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 
改, 0 漏 1 helper 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T292 0 改 `player_action_gate.gd` `get_player()` 任何 1 字符).
  - 跨 1 套 polish 模式 T292 #216 落地后 0 漂动, §9.6.36 段 1:1 严格 (4 件套 1:1 严格分离 + autoload 字段 1:1 严格 + register / unregister 2 
方法 1:1 严格 + is_blocked() 3 probe 1:1 严格 + get_player() 1 helper 1:1 严格) 100% 闭环, 4 件套 1:1 严格分离 (1 Stage 1 autoload 字段 
1:1 严格 0 漏 0 改 0 反序 + 1 Stage 2 register / unregister 2 方法 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 3 is_blocked() 3 probe 1:1 严格 
0 漏 0 改 0 反序 + 1 Stage 4 get_player() 1 helper 1:1 严格 0 漏 0 改 0 反序). 1 套 polish 模式 0 后续 主要 pre-existing 风险.

- **预防**:
  1. 任何 polish 期给 PlayerActionGate 加新 1 probe (e.g. `stun` / `pause` / `cutscene` probe) 或加新 1 方法 (e.g. `is_stunned()` 
/ `pause_player()` / `resume_player()`) 或加新 1 字段 (e.g. `_stun_timer: float = 0.0` 全局晕眩计时器) 或加新 1 边 6 verb 子类 → 
`is_globally_blocking()` 接入 (e.g. 未来 7th verb "Drain" 接入 PlayerActionGate 3 probe 需补 1 边) 任务**必须**严格按 4 件套 1:1 复制, 0 反序 
0 漏 0 改 1 字段. 4 件套 0 触碰边界: **Stage 1 0 改 autoload 字段 1:1 严格分离契约** (T292 落地后 `player_action_gate.gd` 1 字段 `_player: Node 
= null` 已切 1:1 strict 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反序 0 反向, 改 1 字段 / 漏 1 字段 = 1 probe / 1 方法 / 1 字段扩展 0 4 件套 0 闭环 0 漂动), 
**Stage 2 0 改 register / unregister 2 方法 1:1 严格分离契约** (T292 落地后 `player_action_gate.gd` `register_player(p)` 1:1 严格 
(idempotent last-wins) + `unregister_player(p)` 1:1 严格 (only clear if same player) 已切 1:1 strict 0 漏 0 改 0 改 1 方法 0 漏 1 
方法 0 反序 0 反向, 改 1 方法 / 漏 1 方法 = 1 方法扩展 0 register / unregister 0 闭环 0 漂动), **Stage 3 0 改 is_blocked() 3 probe 1:1 
严格分离契约** (T292 落地后 `player_action_gate.gd` `is_blocked()` 4-step probe 模式 (`_player == null` 守卫 + `_is_dying` 1 probe + 
`wave_ability` + `has_method` 守卫 + `call("is_globally_blocking")` 1 probe) 已切 1:1 strict 0 漏 0 改 0 改 1 probe 0 漏 1 
probe 0 反序 0 反向, 改 1 probe / 漏 1 probe = 1 probe 扩展 0 复合 check 0 闭环 0 漂动), **Stage 4 0 改 get_player() 1 helper 1:1 
严格分离契约** (T292 落地后 `player_action_gate.gd` `get_player() -> Node` helper 已切 1:1 strict 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 0 
反向, 改 1 字符 / 漏 1 字符 = 1 helper 扩展 0 boss / cutscene 入口 0 闭环 0 漂动). 任何"加新 1 probe"必须**先**按 Stage 1 autoload 字段 1:1 加 1 
字段 (Stage 1 1:1 严格, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向, **0 漏 1 件套**), **再**按 Stage 2 register / unregister 1:1 加 1 边 (Stage 2 
1:1 严格, 0 漏 1 边 0 改 1 边 0 反序 0 反向), **再**按 Stage 3 is_blocked() 1:1 加 1 probe (Stage 3 1:1 严格, 0 漏 1 probe 0 改 1 probe 
0 反序 0 反向), **再**按 Stage 4 get_player() 1:1 镜像 (Stage 4 1:1 严格, 0 漏 1 helper 0 改 1 字符 0 反向).
  2. **4 件套 0 触碰边界** — `player_action_gate.gd` autoload 字段 + `register_player(p)` / `unregister_player(p)` 2 方法 + 
`is_blocked()` 3 probe + `get_player()` 1 helper 任何 1 件 0 改 0 删 0 重排 (4 件套 0 触碰边界), 任何"修 1 件"polish 走 Stage 1 1:1 严格 (改 
件既有 4 件套时**必须**保持 4 件套 0 改 0 漏 0 反向 0 反序 0 触碰既有 1 件); 任何"加新 1 边"polish 走 Stage 2 1:1 严格 (改 边既有 register / unregister 
时**必须**保持 2 方法 0 改 0 漏 0 反向 0 反序 0 漏 1 边); 任何"加新 1 probe"polish 走 Stage 3 1:1 严格 (改 probe 既有 is_blocked() 3 probe 
时**必须**保持 3 probe 0 改 0 漏 0 反向 0 反序 0 漏 1 probe); 任何"加新 1 helper"polish 走 Stage 4 1:1 严格 (改 helper 既有 get_player() 
时**必须**保持 1 helper 0 改 0 漏 0 反向 0 反序 0 漏 1 字符).
  3. **0 改 1 字段 0 漏 1 字段 0 反向** (T292 落地后 0 触碰边界) — PlayerActionGate 单 autoload 4 件套 (1 `player_action_gate.gd` 1 字段 
`_player: Node` + 1 `register_player(p)` 1 方法 + 1 `unregister_player(p)` 1 方法 + 1 `is_blocked()` 3 probe + 1 
`get_player()` 1 helper) 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反向, 改 1 字段 / 漏 1 字段 / 反向 1 字段 = 1 probe / 1 方法 / 1 字段 / 1 
边扩展 0 4 件套 0 漂动, source-grep 验证 4 件套 0 漏 0 改 1 字段 0 漏 1 字段 0 改 1 字符 0 反向.
  4. **0 改 1 边 0 漏 1 边 0 反向** (T292 落地后 0 触碰边界) — 接入契约 (1 autoload 字段 1:1 严格 + 1 register / unregister 2 方法 1:1 严格 + 1 
is_blocked() 3 probe 1:1 严格 + 1 get_player() 1 helper 1:1 严格) 1:1 严格分离 0 漏 0 改 0 改 1 边 0 漏 1 边 0 反向, 改 1 边 / 漏 1 边 / 反向 
1 边 = 1 probe / 1 方法 / 1 字段 / 1 边扩展 0 接入契约 0 漂动, source-grep 验证 4 件套 0 触碰接入契约 0 漏 0 改 1 边 0 漏 1 边 0 改 1 字符 0 反向.
  5. **T162 brittle 修复流程 0 触碰边界** — PlayerActionGate 单 autoload 4 件套 0 副作用 子段 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 改 1 字符 0 反向 0 
漏 1 字段 0 触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构 (T162 
brittle 修复流程 = 1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), 改 
1 字段 / 漏 1 字段 / 反向 1 字段 = 1 probe / 1 方法 / 1 字段 / 1 边扩展 0 0 副作用 0 漂动 0 闭环, source-grep 验证 PlayerActionGate 单 autoload 4 
件套 0 副作用 子段 0 漏 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 
触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构.
  6. **1 套 polish 模式 0 互混 0 复用 0 共享 唯一性 标注 0 改 1 段 0 漏 1 段 0 反向** — §9.6.36 段 唯一性 标注 1:1 严格分离 0 漏 0 改 0 改 1 套 polish 模式 
0 漏 1 套 polish 模式 0 反向 0 触碰既有 0 改 1 段 (本节是 27 套 polish 模式**唯一**关注 PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约 的 1 套, 
0 互混 0 复用 0 共享), 改 1 套 polish 模式 / 漏 1 套 polish 模式 / 反向 1 套 polish 模式 = 1 §9.6.36 段 0 唯一性 标注 0 漂动 0 闭环, source-grep 验证 
§9.6.36 段 唯一性 标注 0 漏 0 改 1 套 polish 模式 0 漏 1 套 polish 模式 0 改 1 字符 0 反向.
  7. **§9.6.36 是 27 套 polish 模式**唯一**关注 "PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约 (`player_action_gate.gd` 1 
weak-typed `var _player: Node = null` 字段 + `register_player(p)` 1 幂等方法 + `unregister_player(p)` 1 守门方法 + `is_blocked()` 
1 复合 check (3 probe: `_is_dying` + `wave_ability` + `is_globally_blocking`) + `get_player()` 1 convenience helper) + 
autoload 字段 1:1 严格 (1 字段 1:1 严格 0 漏 0 改) + register / unregister 2 方法 1:1 严格 (2 方法 1:1 严格 0 漏 0 改) + is_blocked() 3 
probe 1:1 严格 (3 probe 1:1 严格 0 漏 0 改) + get_player() 1 helper 1:1 严格 (1 helper 1:1 严格 0 漏 0 改) + 0 触碰游戏代码 + 0 触碰 §10 
决策记录流程 0 触碰既有" 的 1 套** — 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / 
§9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / 
§9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 26 套 polish 模式 0 互混 0 复用 0 共享. §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / 
§9.6.33 5 套关注 "6 verb 接入 单层 / 跨层 字节码 (6 verb hit handler 5 件套 / 6 verb signal 桥接 5+1 件套 / 16 件套 verb ability base / 7 
件套 windup VFX base / 6 verb 接入 4 件套 字节码一致性)" (与 PlayerActionGate 单 autoload 4 件套 0 相关, 5 套 6 verb 字节码 0 涉及 
PlayerActionGate autoload 4 件套 自身 1:1 严格分离), §9.6.20 / §9.6.21 / §9.6.25 / §9.6.26 / §9.6.27 5 套关注 "6 verb 跨组件 / 跨房间 / 
跨段 视觉组 (Hub ↔ archive 跨房间 transition / 6 verb 成就系统 闭环 8 段 / 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 / 6 verb icon 视觉组 5 段 / 
PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯)" (与 PlayerActionGate 单 autoload 4 件套 0 相关, 5 套跨组件 0 涉及 4 件套**之间**接入契约 自身 
1:1 严格分离), §9.6.22 / §9.6.23 / §9.6.24 3 套关注 "6 verb VFX / prewarm / HUD (6 verb VFX 视觉组 5 段 / 7 桶 prewarm 5+1+1 / 6 
verb HUD 6 行 6 通道 36 通道 5 段)" (与 PlayerActionGate 单 autoload 4 件套 0 相关, 3 套 6 verb 0 涉及 PlayerActionGate autoload 4 件套 
自身 1:1 严格分离), §9.6.28 / §9.6.29 / §9.6.30 3 套关注 "工具链 / smoke test / docs polish (smoke test ITERATION_COUNT 跨迭代 + 段边界 
find 5 段 / polish 文档化 5 段 canonical 1:1 序列 模板 / 工具链 3 件套 1:1 严格分离契约)" (与 PlayerActionGate 单 autoload 4 件套 0 相关, 3 套工具链 
0 涉及 4 件套**之间**接入契约 自身 1:1 严格分离), §9.6.31 / §9.6.32 2 套关注 "文档归档 3 件套 1:1 严格分离契约 (CHANGELOG 归档 3 件套 1:1 严格分离契约 / 
REVIEW_LOG 归档 3 件套 1:1 严格分离契约)" (与 PlayerActionGate 单 autoload 4 件套 0 相关, 2 套文档归档 0 涉及 4 件套**之间**接入契约 自身 1:1 严格分离), 
§9.6.34 / §9.6.35 2 套关注 "数据层字段扩展 6 段 / 5 段 canonical 1:1 严格分离契约 (PlayerStats 持久化层字段扩展 6 段 / audio 数据层 preset 字段扩展 5 段)" 
(与 PlayerActionGate 单 autoload 4 件套 0 相关, 2 套数据层字段扩展 0 涉及 PlayerActionGate autoload 4 件套 自身 1:1 严格分离). §9.6.36 关注 
"PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约 4 件套 1:1 视觉组连贯" (与 6 verb 单层 / 6 verb 跨组件 / 工具链 / 文档归档 / 数据层字段扩展 polish 
0 相关, 4 件套 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 4 件套 = 4 元素 1:1 严格). §9.6.36 是 27 套 polish 模式**唯一**关注 
"PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约 (`player_action_gate.gd` 1 weak-typed `var _player: Node = null` 字段 + 
`register_player(p)` 1 幂等方法 + `unregister_player(p)` 1 守门方法 + `is_blocked()` 1 复合 check (3 probe: `_is_dying` + 
`wave_ability` + `is_globally_blocking`) + `get_player()` 1 convenience helper) + autoload 字段 1:1 严格 (1 字段 1:1 严格 0 漏 0 
改) + register / unregister 2 方法 1:1 严格 (2 方法 1:1 严格 0 漏 0 改) + is_blocked() 3 probe 1:1 严格 (3 probe 1:1 严格 0 漏 0 改) + 
get_player() 1 helper 1:1 严格 (1 helper 1:1 严格 0 漏 0 改) + 0 触碰游戏代码 + 0 触碰 §10 决策记录流程 0 触碰既有" 的 1 套, 与 §9.6.6 / §9.6.7 / 
§9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / 
§9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 
26 套 ProfileRecentList 5 行 / ProfileQuickStats 4 段 / AchievementGrid / 6 verb 单层 字节码 / 6 verb 跨组件 / SaveSystem / 跨房间 
transition / PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 / smoke test ITERATION_COUNT 跨迭代 + 段边界 find / polish 文档化 5 段 
canonical 1:1 序列 模板 / 工具链 3 件套 1:1 严格分离契约 / CHANGELOG 归档 3 件套 1:1 严格分离契约 / REVIEW_LOG 归档 3 件套 1:1 严格分离契约 / 6 verb 接入 4 
件套 字节码一致性 / PlayerStats 持久化层字段扩展 6 段 / audio 数据层 preset 字段扩展 5 段 0 互混 0 复用 0 共享. 任何"加新 1 probe"或"加新 1 方法"或"加新 1 字段"或"加新 
1 边 6 verb 接入"必须**先**按 §9.6.36 Stage 1 autoload 字段 1:1 严格 加 1 字段 (Stage 1 1:1 严格, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向, **0 漏 1 
件套**), **再**按 §9.6.36 Stage 2 register / unregister 1:1 严格 加 1 边 (Stage 2 1:1 严格, 0 漏 1 边 0 改 1 边 0 反序 0 反向), **再**按 
§9.6.36 Stage 3 is_blocked() 1:1 严格 加 1 probe (Stage 3 1:1 严格, 0 漏 1 probe 0 改 1 probe 0 反序 0 反向), **再**按 §9.6.36 Stage 
4 get_player() 1:1 严格 镜像 (Stage 4 1:1 严格, 0 漏 1 helper 0 改 1 字符 0 反序 0 反向).
  8. 已知 drift risk: PlayerActionGate 单 autoload 4 件套 接入 **可能**漏 1 件套 / 1 边 1:1 镜像 (e.g. 加新 1 probe 时漏 Stage 1 autoload 
字段 中 1 字段 0 镜像, 1 probe 0 4 件套 0 闭环; 加新 1 probe 时漏 Stage 2 register / unregister 中 0 镜像, 1 probe 0 register / 
unregister 0 闭环; 加新 1 probe 时漏 Stage 3 is_blocked() 中 3 probe → 4 probe 顺序 0 镜像, 1 probe 0 复合 check 0 闭环; 加新 1 probe 时漏 
Stage 4 get_player() 中 1 helper 0 镜像, 1 probe 0 boss / cutscene 入口 0 闭环). 监控建议: 任何"未来给 PlayerActionGate 单 autoload 加新 1 
probe / 加新 1 方法 / 加新 1 字段 / 加新 1 边 6 verb 接入"必须**先**在 4 件套 (Stage 1 autoload 字段 1:1 严格 + Stage 2 register / unregister 
1:1 严格 + Stage 3 is_blocked() 1:1 严格 + Stage 4 get_player() 1:1 严格) 1:1 镜像 0 漏 1 件套 / 0 漏 1 边 / 0 漏 1 字段 / **0 漏 1 
probe**. T292 (#216) 4 件套 1:1 镜像落地后任何新 probe / 新方法 / 新字段 / 新 1 边 6 verb 接入 是 §9.6.36 4 件套的"母集" (1 probe / 1 方法 / 1 字段 / 
1 边扩展 × 4 件套 = 4 元素 1:1 严格, §9.6.36 4 件套 1 套 polish 模式 完整闭环, 1 套 polish 模式 × 4 件套 = 4 元素 1:1 严格, 含 Stage 1 autoload 字段 
1:1 严格 1 套 polish 模式 4 件套 0 漏 0 改 + Stage 2 register / unregister 1:1 严格 1 套 polish 模式 register / unregister 0 漏 0 改 + 
Stage 3 is_blocked() 1:1 严格 1 套 polish 模式 is_blocked() 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 件 + Stage 4 get_player() 1:1 严格 
1 套 polish 模式 get_player() 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 字符). 另 1 类已知 drift: Stage 1 autoload 字段 1:1 严格 0 改 1 字段 0 漏 
1 字段 0 反向 (e.g. 后续 polish 期加新 1 probe 时漏 0 镜像 1 字段 任何 1 字段, 1 probe 0 autoload 字段 0 闭环; 或漏 0 镜像 1 方法 任何 1 方法, 1 probe 0 
register / unregister 0 闭环; 或改 1 字段 0 改 1 字段 0 反向 0 触碰既有), 后续 polish 期若加新 1 probe 必须**先**用 1:1 镜像 4 件套 模板, 0 改既有 0 漏 1 
件套 0 漏 1 边 0 反向 **0 漏 1 probe**.

**0 副作用**: T292 (#216) 任何 const / var 0 触碰 (`player_action_gate.gd` 0 改 0 删 0 重排 `_player: Node = null` 字段任何 1 字符; 
`player_action_gate.gd` 0 改 0 删 0 重排 `register_player(p)` / `unregister_player(p)` 2 方法任何 1 字符; `player_action_gate.gd` 
0 改 0 删 0 重排 `is_blocked()` 3 probe 任何 1 字符; `player_action_gate.gd` 0 改 0 删 0 重排 `get_player() -> Node` helper 任何 1 字符;
 CONTRIBUTING.md §9.6.36 段 0 改 0 删 0 重排, 仅新加 1 段 (~50 行)); 0 触碰 §9.6.6 ProfileRecentList 5 行 row 文本 5→7 字段 format 
字符串扩展模式 (T292 0 改 §9.6.6 任何 1 字符); 0 触碰 §9.6.7 ProfileRecentList 5 行 row hover feedback 三件套 (T292 0 改 §9.6.7 任何 1 字符); 
0 触碰 §9.6.8 ProfileQuickStats 4 段独立 hover 联动 (T292 0 改 §9.6.8 任何 1 字符); 0 触碰 §9.6.9 AchievementGrid + ProfileRecentList 
跨面板 `_alpha_base` Dictionary 双源 (T292 0 改 §9.6.9 任何 1 字符); 0 触碰 §9.6.10 「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (T292 0 
改 §9.6.10 任何 1 字符); 0 触碰 §9.6.15 SaveSystem audit_save_slots (T292 0 改 §9.6.15 任何 1 字符); 0 触碰 §9.6.16 6 verb hit 
handler 5 件套 (T292 0 改 §9.6.16 任何 1 字符); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T292 0 改 §9.6.17 任何 1 字符); 0 触碰 §9.6.18 
16 件套 verb ability base (T292 0 改 §9.6.18 任何 1 字符); 0 触碰 §9.6.19 7 件套 windup VFX base (T292 0 改 §9.6.19 任何 1 字符); 0 触碰 
§9.6.20 Hub ↔ archive 跨房间 transition flow (T292 0 改 §9.6.20 任何 1 字符); 0 触碰 §9.6.21 8 段成就序列 (T292 0 改 §9.6.21 任何 1 字符); 
0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T292 0 改 §9.6.22 任何 1 字符); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T292 0 改 §9.6.23 任何 1 字符); 
0 触碰 §9.6.24 HUD 6 verb × 6 通道 = 36 通道 5 段序列 (T292 0 改 §9.6.24 任何 1 字符); 0 触碰 §9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 5 段序列 
(T292 0 改 §9.6.25 任何 1 字符); 0 触碰 §9.6.26 6 verb icon 视觉组 5 段 canonical 1:1 序列 (T292 0 改 §9.6.26 任何 1 字符); 0 触碰 §9.6.27 
PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 5 段序列 (T292 0 改 §9.6.27 任何 1 字符); 0 触碰 §9.6.28 smoke test ITERATION_COUNT 
跨迭代 + 段边界 find 5 段序列 (T292 0 改 §9.6.28 任何 1 字符); 0 触碰 §9.6.29 polish 文档化 5 段 canonical 1:1 序列 模板 + 4 段关系段 模板 (T292 0 改 
§9.6.29 任何 1 字符); 0 触碰 §9.6.30 工具链 3 件套 1:1 严格分离契约 (T292 0 改 §9.6.30 任何 1 字符); 0 触碰 §9.6.31 CHANGELOG 归档 3 件套 1:1 
严格分离契约 (T292 0 改 §9.6.31 任何 1 字符); 0 触碰 §9.6.32 REVIEW_LOG 归档 3 件套 1:1 严格分离契约 (T292 0 改 §9.6.32 任何 1 字符); 0 触碰 §9.6.33 
6 verb 接入 4 件套 字节码一致性 (T292 0 改 §9.6.33 任何 1 字符); 0 触碰 §9.6.34 PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约 (T292 0 改 
§9.6.34 任何 1 字符); 0 触碰 §9.6.35 AudioPresets `MUSIC_PRESETS` preset 字段扩展 5 段 canonical 1:1 严格分离契约 (T292 0 改 §9.6.35 任何 1 
字符); 0 触碰 check_smoke_consistency.sh (T292 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + 
install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, 0 触碰 
`player_action_gate.gd` 任何 1 字段 / 1 方法 / 1 probe / 1 helper, 0 触碰 `player.gd` 任何 1 字段); 0 行为变化, 1 文档 1 段 (§9.6.36 ~50 
行) + 0 字节码修改 (T292 仅文档化既有 PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约, 0 改 `_player: Node = null` 字段 0 改 
`register_player(p)` 0 改 `unregister_player(p)` 0 改 `is_blocked()` 3 probe 0 改 `get_player()` helper 0 触碰既有) + 1 smoke 
test (T292 30+ 断言).

**与 §9.6.33 关系** — §9.6.33 (T289 #212 落地) 关注 "6 verb 接入 4 件套 字节码一致性 1:1 严格分离契约" (4 件套 = `_verb_ability_base.gd` 16 件套 + 
`_verb_windup_vfx_base.gd` 7 件套 + 6 verb VFX 视觉组 5 段 + HUD 6 verb × 6 通道 = 36 通道 5 段, 4 件套覆盖 6 verb 跨层 字节码一致性 0 涉及 
PlayerActionGate 单 autoload 4 件套). §9.6.36 关注 "PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约" (1 autoload 字段 + 2 
register / unregister 方法 + 3 is_blocked() probe + 1 get_player() helper, 4 件套覆盖 PlayerActionGate autoload 4 件套 接入 0 涉及 
6 verb 跨层 字节码). §9.6.33 与 §9.6.36 0 互混 0 复用 0 共享: §9.6.33 关注 "6 verb 接入 4 件套 字节码一致性 1:1 严格分离契约" (与 PlayerActionGate 单 
autoload 4 件套 0 相关, 4 件套 0 涉及 autoload 字段 / register / unregister / is_blocked() / get_player() 4 件套), §9.6.36 关注 
"PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约 4 件套 1:1 视觉组连贯" (与 6 verb 接入 4 件套 0 相关, 4 件套 1:1 严格 镜像 1 套 polish 模式 既有 
1:1 严格分离, 1 套 polish 模式 × 4 件套 = 4 元素 1:1 严格). §9.6.36 与 §9.6.33 是 "姊妹段" (1 套 polish 模式 × 4 件套 = 4 元素 1:1 严格 镜像 1 套 
polish 模式 × 4 件套 = 4 元素 1:1 严格, 一是 6 verb 接入 4 件套 字节码一致性, 一是 PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约; 1 套 polish 
模式 × 4 件套 = 4 元素 1:1 严格 + 1 套 polish 模式 × 4 件套 = 4 元素 1:1 严格). 任何"加新 1 字节码"必须**先**按 §9.6.33 4 件套 1:1 复制 (4 件套 1:1 严格, 0 
漏 1 件套 0 改 1 字段 0 反向) **或** 按 §9.6.36 4 件套 1:1 复制 (4 件套 1:1 严格, 0 漏 1 件套 0 改 1 字段 0 反向), **再**按 §9.6.36 4 件套 1:1 同步 (4 
件套 1:1 严格, 0 漏 1 件套 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.33 任何 1 字符) **或** 按 §9.6.33 4 件套 1:1 同步 (4 件套 1:1 严格, 0 
漏 1 件套 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.36 任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 
0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 轮 0 后续 持续收敛), §9.6.36 是"PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约" (4 件套 1:1 
严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约). T162 与 §9.6.36 0 互混 0 复用 0 共享: T162 关注 "brittle 修复流程 5 步骤" (1 expect 反转 + 1 
docblock 说明 + 1 段 find 反转 + 0 触碰既有 + 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 步骤 1:1 严格), §9.6.36 关注 "4 件套 
1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约" (与 5 步骤 0 相关, 1 套 polish 模式 × 4 件套 = 4 元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离). 任何"加新 1 
brittle 修复"必须**先**按 T162 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), **再**按 §9.6.36 4 
件套 1:1 镜像 (4 件套 1:1 严格, 0 漏 1 件套 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.36 4 件套走 §9.1 9 步落地的 0 步 (PlayerActionGate 单 autoload 4 件套 接入 是 autoload 范畴, 0 
走 9 步任何 1 步), 0 触碰 9 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 
`vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染). 任何"加新 1 probe"必须**先**按 §9.6.36 4 件套 1:1 镜像 (4 件套 1:1 严格, 0 漏 1 件套 0 
漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 9 步 (`verb` 枚举 + `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + 
`vfx` 视觉组 + `audio` preset 字段 + `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 步"必须**先**在 §9.6.36 4 件套 加 1 边 (1 autoload 字段 entry 或 1 
register / unregister 边 或 1 is_blocked() probe 或 1 get_player() helper), 0 触碰既有 9 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 
`const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).
