# Polish Patterns 9.6.55–56

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.55–9.6.56，共 2 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.55-56.md

<!-- shard 55-56 共 2 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.55 5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约 polish 模式 (T166 #85 + T167 #86 + T168 
#86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 + T304 #231 + T307 #234 跨 9 任务 ~150 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / §9.6.51 / §9.6.52 / §9.6.53 / §9.6.54 45 套 polish 模式均在 
> "pause_menu / 6 verb 单层 / 6 verb 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / audio 数据层 / 
> PlayerActionGate 单 autoload 4 件套 / SaveSystem CRC32 校验 / T162 brittle 修复流程 / 6 verb audio 家族 19 cue / 6 verb cooldown 
> ready jingle 5 段 / 6 verb `_ready()` / `_exit_tree()` 双 hook 串联 / 5 verb windup VFX 4 hook lifecycle / 5 verb windup 
> VFX `_draw()` verb-specific / 5 verb windup VFX `trigger()` verb-specific / 5 verb windup VFX 共享 4 hook 0 override / 
> 5 verb windup VFX `trigger()` + `_draw()` 双 verb-specific / 5 verb windup VFX base 3 内部状态字段 / 5 verb windup VFX 视觉组连贯 
> lifecycle / 5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 / 5 verb windup VFX `trigger()` + 视觉组 拼接" 范畴, 0 涉及 "5 verb 
> windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约" 独立聚焦段 落地. §9.6.48 (T304 #231 落地) 关注 "5 verb windup 
> VFX `trigger()` verb-specific 1:1 严格分离契约" (32 元素 1:1 严格). §9.6.51 (T307 #234 落地) 关注 "5 verb windup VFX base 3 内部状态字段 
> 0 override verb-specific 0 触碰既有 1:1 严格分离契约" (9 元素 1:1 严格). §9.6.52 (T308 #236 落地) 关注 "5 verb windup VFX 视觉组连贯 
> lifecycle 1:1 严格分离契约" (7 元素 1:1 严格). §9.6.53 (T309 #237 落地) 关注 "5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格分离契约" 
> (12 元素 1:1 严格, 5 verb 1 视觉组 1 段 + 5 verb 0 override 3 内部状态字段 + 1 显式契约 + 1 拼接 0 触碰既有 = 12 元素). §9.6.54 (T310 #238 落地) 
> 关注 "5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格分离契约" (12 元素 1:1 严格, 5 verb 1 视觉组 1 段 + 5 verb 0 override 
> `trigger()` verb-specific + 1 显式契约 + 1 拼接 0 触碰既有 = 12 元素). 上述 4 段 (§9.6.48 + §9.6.51 + §9.6.52 + §9.6.53 + §9.6.54) 
> 均聚焦 5 verb windup VFX 单一维度或 2 维度拼接 (`trigger()` OR 视觉组 OR base 3 内部状态字段 OR `trigger()` + 视觉组 OR 视觉组 + base 3 内部状态字段) 
> 1:1 严格分离契约, 0 涉及 "5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约" 独立聚焦段 自身 (即 5 verb 
> `trigger()` 5 段 1 verb-specific 0 override + 5 verb 视觉组 5 段 1:1 + 5 verb 0 override 3 内部状态字段 5 段 1:1, 3 维度 1 拼接 0 漏 1 
> 边). 本节 (T311 #239 落地) 首个落在**5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约** 聚焦段 (5 verb 0 
> override `trigger()` verb-specific 5 元素 1:1 严格 (Pulse `trigger(origin, half_radius, duration)` 3 参 + Bind 
> `trigger(origin, half_radius, duration)` 3 参 + Cut `trigger(origin, half_radius, direction, duration)` 4 参 1 
> verb-specific `_direction` + Echo `trigger(origin, half_radius, full_radius, duration)` 4 参 1 verb-specific 
> `_max_radius` + Wave `trigger(origin, half_radius, duration)` 3 参, 跨 5 verb 各 1 元素 = 5 元素 1:1 严格 0 漏 0 改 0 反序 0 反向) + 
> 5 verb 1 视觉组 1 段 1:1 严格 (Pulse 同心圆环 + Coral Pulse 核 / Bind 向内螺旋 + Muted Violet 核 / Cut 4 三角碎片 + Amber Voice 核 / Echo 
> 8 棱镜折射 + Glass Cyan 核 / Wave 3 同心圆环 + Pale Resonance 核, 跨 5 verb 各 1 元素 = 5 元素 1:1 严格 0 漏 0 改 0 反序 0 反向) + 5 verb 0 
> override 3 内部状态字段 1:1 严格 (Pulse + Bind + Cut + Echo + Wave 各自 0 override `_max_lifetime` + `_lifetime` + `_active` 3 
> 内部状态字段 1:1 严格继承 base `_verb_windup_vfx_base.gd:45-47`, 跨 5 verb 各 1 元素 = 5 元素 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "5 
> verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格 (1 verb 1 `trigger()` 1 verb-specific 0 override 必 1 
> 视觉组 1 段 必 0 override 3 内部状态字段, 5 verb 0 漏 1 3 维度拼接 边)" 1 段 1:1 严格 (1 契约 1 元素) + 1 3 维度拼接 0 触碰既有 1:1 严格 (1 verb 1 
> `trigger()` 1 verb-specific 0 override 拼接 1 视觉组 1 段 拼接 0 override 3 内部状态字段 0 漏 1 边 0 反序 0 反向, 1 抽象契约 1 元素)) polish 模式 
> — 跨 5 verb × 0 override `trigger()` verb-specific + 5 verb × 1 视觉组 1 段 + 5 verb × 0 override 3 内部状态字段 + 1 显式契约 + 1 3 
> 维度拼接 0 触碰既有 = 5 + 5 + 5 + 1 + 1 = 17 元素 1:1 严格 (注意: 5 verb 0 override `trigger()` verb-specific 5 元素 1:1 严格 = 5 verb 
> 各自 1 元素 = 5 元素 + 5 verb 1 视觉组 1 段 1:1 严格 = 5 verb 各自 1 元素 = 5 元素 + 5 verb 0 override 3 内部状态字段 1:1 严格 = 5 verb 各自 1 元素 
> = 5 元素 + 1 显式契约 1 元素 + 1 3 维度拼接 0 触碰既有 1 元素 = 5 + 5 + 5 + 1 + 1 = 17 元素 1:1 严格, 0 漂动 0 后续 主要 pre-existing 风险) 0 触碰既有 
> 5 verb 任何 1 字符 0 反向 0 例外. 任何"加新 1 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接" (e.g. 未来加 7th verb 
> "Drain" 接入 5 verb `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离) 必须**先**按 Stage 1 Pulse 1 verb 1 `trigger()` 1 
> verb-specific 0 override + 1 verb 1 视觉组 1 段 + 1 verb 0 override 3 内部状态字段 1:1 拼接 加 1 verb entry (Stage 1 1:1 严格, 0 漏 1 
> `trigger()` 0 改 1 字段 0 反序 0 反向, **0 漏 1 3 维度拼接 边 0 漏 1 verb**), **再**按 Stage 2 Bind / Stage 3 Cut / Stage 4 Echo / 
> Stage 5 Wave 1 verb 1 `trigger()` 1 verb-specific 0 override + 1 verb 1 视觉组 1 段 + 1 verb 0 override 3 内部状态字段 1:1 拼接 4 
> verb entries (4 verb 1:1 严格, 0 漏 1 `trigger()` 0 改 1 字段 0 反序 0 反向, 5 verb 0 漏 0 改 0 反序 0 例外), **再**按 1 显式契约 "5 verb 
> windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格" 1:1 严格 (0 漏 1 3 维度拼接 共享契约 0 改 1 字符 0 反序 0 反向). 跨 1 套 
> polish 模式 × 5 verb 0 override `trigger()` verb-specific 5 元素 + 5 verb 1 视觉组 1 段 5 元素 + 5 verb 0 override 3 内部状态字段 5 
> 元素 + 1 显式契约 + 1 3 维度拼接 0 触碰既有 = 5 + 5 + 5 + 1 + 1 = 17 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: 5 verb windup VFX (`src/scripts/pulse_windup_vfx.gd` + `src/scripts/bind_windup_vfx.gd` + 
`src/scripts/cut_windup_vfx.gd` + `src/scripts/echo_windup_vfx.gd` + `src/scripts/wave_windup_vfx.gd`) `trigger()` + 
视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 + 
T304 #231 + T307 #234 跨 9 任务 ~150 轮落地) 现有的 17 元素 1:1 严格 (1 Pulse 1 verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格 
(`pulse_windup_vfx.gd` `trigger(origin, half_radius, duration)` 3 参 verb-specific = 1 verb 1 `trigger()` 1 
verb-specific 0 override 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 Pulse 1 verb 1 视觉组 1 段 1:1 严格 = 1 verb 1 视觉组 1 段 1:1 严格 (1 视觉组 1 
段 0 漏 0 改 0 反序 0 反向) + 1 Pulse 1 verb 0 override 3 内部状态字段 1:1 严格 = 1 verb 0 override 3 内部状态字段 1:1 严格 (`_max_lifetime` + 
`_lifetime` + `_active` 3 内部状态字段 0 override 0 触碰 base 1:1 严格继承) + 1 verb 1 3 维度拼接 1:1 严格 (1 `trigger()` 1 verb-specific 
0 override + 1 视觉组 1 段 + 0 override 3 内部状态字段 = 3 元素 1 3 维度拼接 0 漏 0 改 0 反序 0 反向) + 1 Bind 1 verb 1 `trigger()` 1 
verb-specific 0 override 1:1 严格 (`bind_windup_vfx.gd` `trigger(origin, half_radius, duration)` 3 参 verb-specific = 1 
verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格) + 1 Bind 1 verb 1 视觉组 1 段 1:1 严格 = 1 verb 1 3 维度拼接 1:1 严格 + 1 
Bind 1 verb 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 1:1 严格 + 1 Cut 1 verb 1 `trigger()` 1 verb-specific 0 override 
1:1 严格 (`cut_windup_vfx.gd` `trigger(origin, half_radius, direction, duration)` 4 参 verb-specific = 1 verb 1 
`trigger()` 1 verb-specific 0 override 1:1 严格 含 1 verb-specific `_direction` 1 元素) + 1 Cut 1 verb 1 视觉组 1 段 1:1 严格 = 1 
verb 1 3 维度拼接 1:1 严格 + 1 Cut 1 verb 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 1:1 严格 + 1 Echo 1 verb 1 `trigger()` 1 
verb-specific 0 override 1:1 严格 (`echo_windup_vfx.gd` `trigger(origin, half_radius, full_radius, duration)` 4 参 
verb-specific = 1 verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格 含 1 verb-specific `_max_radius` 1 元素) + 1 Echo 1 
verb 1 视觉组 1 段 1:1 严格 = 1 verb 1 3 维度拼接 1:1 严格 + 1 Echo 1 verb 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 1:1 严格 + 1 
Wave 1 verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格 (`wave_windup_vfx.gd` `trigger(origin, half_radius, 
duration)` 3 参 verb-specific = 1 verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格) + 1 Wave 1 verb 1 视觉组 1 段 1:1 严格 
= 1 verb 1 3 维度拼接 1:1 严格 + 1 Wave 1 verb 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 1:1 严格 + 1 显式契约 "5 verb windup 
VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格 (1 verb 1 `trigger()` 1 verb-specific 0 override 必 1 视觉组 1 段 必 0 
override 3 内部状态字段, 5 verb 0 漏 1 3 维度拼接 边)" 1 段 1:1 严格 + 1 3 维度拼接 0 触碰既有 1:1 严格 (1 抽象契约 1 元素)), 在反复 polish (T166 #85 + 
T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 + T304 #231 + T307 #234 跨 9 任务 ~150 轮落地) 期间出现 3 类典型 
fragile: (1) **"加新 1 verb windup VFX 漏 1 边 '5 verb `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格' 共享契约"** — 加新 1 verb 
windup VFX (e.g. 未来加 7th verb "Drain" 接入 5 verb `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离) 落地时漏 1 边 "5 verb 
windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格" 共享契约 (e.g. 漏 7th verb 1 `trigger()` 1 verb-specific 0 
override + 1 视觉组 1 段 + 0 override 3 内部状态字段 1 3 维度拼接 1:1 严格 0 镜像 1 verb, 1 verb 0 1 3 维度拼接 0 闭环 0 漂动); (2) **"加新 1 verb 
windup VFX 漏 1 边 '1 显式契约 5 verb `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格' 契约"** — 加新 1 verb windup VFX (e.g. 7th 
verb "Drain" 接入 `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离) 落地时漏 1 边 "1 显式契约 5 verb windup VFX `trigger()` + 视觉组 
+ base 3 内部状态字段 3 维度拼接 1:1 严格 (1 verb 1 `trigger()` 1 verb-specific 0 override 必 1 视觉组 1 段 必 0 override 3 内部状态字段, 5 
verb 0 漏 1 3 维度拼接 边)" 显式契约 (e.g. 漏 `_verb_windup_vfx_base.gd` 中 0 include 1 显式契约 "5 verb windup VFX `trigger()` + 视觉组 + 
base 3 内部状态字段 3 维度拼接 1:1 严格" 1 段 0 镜像 1 base, 1 base 0 1 显式契约 0 闭环 0 漂动); (3) **"修 1 verb windup VFX `trigger()` 
verb-specific 0 override / 视觉组 / 3 内部状态字段 拼接 0 触碰既有 1 元素 0 触碰边界"** — 修 1 verb windup VFX `trigger()` verb-specific 
(e.g. 改 Cut 1 verb `trigger()` 中 `_direction` 顺序) 或 修 1 verb 视觉组 (e.g. 改 Pulse 1 verb 视觉组 中 "同心圆环" 顺序) 或 修 1 verb 3 
内部状态字段 (e.g. 修 Pulse 0 override 3 内部状态字段) 时 0 触碰既有 1 元素 (1 3 维度拼接 0 触碰既有 1:1 严格), 跨 verb 1:1 漂动. 3 类症状根因: 5 verb windup 
VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 序列 0 文档化独立聚焦段, 任何新 1 verb windup VFX 1 `trigger()` verb-specific / 1 
视觉组 / 0 override 3 内部状态字段 / 1 3 维度拼接 共享契约 / 1 3 维度拼接 0 触碰既有 接入只能"凭经验" 1:1 镜像 17 元素 (5 verb 0 override `trigger()` 
verb-specific 5 元素 + 5 verb 1 视觉组 1 段 5 元素 + 5 verb 0 override 3 内部状态字段 5 元素 + 1 显式契约 + 1 3 维度拼接 0 触碰既有 = 17 元素 1:1 严格),
 1 漏 1 verb 1 3 维度拼接 / 1 漏 1 显式契约 / 1 漏 1 3 维度拼接 0 触碰既有 = 1 verb 扩展 0 17 元素 0 闭环 0 漂动.

- **触发场景**: 加新 1 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格 (e.g. 未来加 7th verb "Drain" 接入 5 verb 
`trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离, 实际现状: 5 verb `trigger()` 5 段 1 verb-specific 0 override + 5 verb 视觉组 
5 段 + 5 verb 0 override 3 内部状态字段 5 段 全部 0 漏 0 改 0 反序 0 反向) / 加新 1 显式契约 (T166 + T167 + T168 + T169 + T171 + T302 + T303 
+ T304 + T307 模式参考) 时**必须**严格按 17 元素 1:1 复制 T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 
#229 + T304 #231 + T307 #234 落地模式: (1) **Stage 1 Pulse 1 verb 1 `trigger()` 1 verb-specific 0 override + 1 verb 1 视觉组 1 
段 + 1 verb 0 override 3 内部状态字段 1:1 拼接 1:1 严格** — 在 `pulse_windup_vfx.gd` `trigger(origin, half_radius, duration)` 3 参 
verb-specific 0 override + 1 同心圆环 + 1 Coral Pulse 主色 + 0 override `_max_lifetime` + `_lifetime` + `_active` 3 内部状态字段 
1:1 严格继承 base `_verb_windup_vfx_base.gd:45-47` = 1 verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格 + 1 verb 1 视觉组 
1 段 1:1 严格 + 1 verb 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 1:1 严格 (3 元素 1 3 维度拼接 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "5 
verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格" 1 段 1:1 严格 镜像 §9.6.48 + §9.6.51 + §9.6.52 + §9.6.53 + 
§9.6.54 (0 漏 1 3 维度拼接 共享契约 0 改 1 字符 0 反序 0 反向) + 1 3 维度拼接 0 触碰既有 1:1 严格 (1 抽象契约 1 元素) = 17 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, 
0 触碰既有 5 verb 任何 1 字符, source-grep 验证 `pulse_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 override + 1 视觉组 1 段 + 0 
override 3 内部状态字段 + `bind_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 override + 1 视觉组 1 段 + 0 override 3 内部状态字段 + 
`cut_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 override (含 1 `_direction` 1 verb-specific 1 元素) + 1 视觉组 1 段 + 0 
override 3 内部状态字段 + `echo_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 override (含 1 `_max_radius` 1 verb-specific 1 
元素) + 1 视觉组 1 段 + 0 override 3 内部状态字段 + `wave_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 override + 1 视觉组 1 段 + 0 
override 3 内部状态字段 + 1 显式契约 + 1 3 维度拼接 0 触碰既有 → 17 元素 0 漏 0 改 0 漏 1 边 0 反向; (2) **Stage 2 Bind 1 verb 1 `trigger()` 1 
verb-specific 0 override + 1 verb 1 视觉组 1 段 + 1 verb 0 override 3 内部状态字段 1:1 拼接 1:1 严格** — 17 元素 1:1 严格 0 漏 0 改 0 反序 0 
反向, source-grep 验证 17 元素 0 漏 0 改 0 漏 1 边 0 反向; (3) **Stage 3 Cut 1 verb 1 `trigger()` 1 verb-specific 0 override + 1 
verb 1 视觉组 1 段 + 1 verb 0 override 3 内部状态字段 1:1 拼接 1:1 严格** — 17 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (4) **Stage 4 Echo 1 verb 
1 `trigger()` 1 verb-specific 0 override + 1 verb 1 视觉组 1 段 + 1 verb 0 override 3 内部状态字段 1:1 拼接 1:1 严格** — 17 元素 1:1 严格 
0 漏 0 改 0 反序 0 反向; (5) **Stage 5 Wave 1 verb 1 `trigger()` 1 verb-specific 0 override + 1 verb 1 视觉组 1 段 + 1 verb 0 
override 3 内部状态字段 1:1 拼接 1:1 严格** — 17 元素 1:1 严格 0 漏 0 改 0 反序 0 反向. 17 元素 1:1 同步, 1 漏 1 verb 1 `trigger()` 1 
verb-specific 0 override / 1 漏 1 verb 1 视觉组 1 段 / 1 漏 1 verb 0 override 3 内部状态字段 / 1 漏 1 显式契约 / 1 漏 1 3 维度拼接 0 触碰既有 = 1 
verb / 1 显式契约 / 1 3 维度拼接 0 触碰既有 扩展 0 17 元素 0 闭环 0 漂动. T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 
+ T303 #229 + T304 #231 + T307 #234 0 触碰既有 0 改 `_verb_windup_vfx_base.gd` 任何 1 字符, 0 改 5 verb windup VFX `trigger()` 
verb-specific 任何 1 字段, 0 改 5 verb windup VFX 视觉组任何 1 像素, 0 改 5 verb windup VFX 3 内部状态字段 任何 1 字符, 0 改 §9.6.48 32 元素 任何 1 
字符, 0 改 §9.6.51 9 元素 任何 1 字符, 0 改 §9.6.52 7 元素 任何 1 字符, 0 改 §9.6.53 12 元素 任何 1 字符, 0 改 §9.6.54 12 元素 任何 1 字符.

- **修复** (跨 1 套 polish 模式 T311 #239 落地后已知 17 元素 0 例外):
  - **T311 #239 首个 5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约 独立聚焦段 落地 
(`pulse_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 override 1:1 严格 + 1 视觉组 1 段 1:1 严格 + 1 verb 0 override 3 内部状态字段 
1:1 严格 = 1 verb 1 3 维度拼接 + `bind_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 override 1:1 严格 + 1 视觉组 1 段 1:1 严格 + 1 
verb 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 + `cut_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 override 1:1 严格 
(含 1 `_direction` 1 verb-specific 1 元素) + 1 视觉组 1 段 1:1 严格 + 1 verb 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 + 
`echo_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 override 1:1 严格 (含 1 `_max_radius` 1 verb-specific 1 元素) + 1 视觉组 1 
段 1:1 严格 + 1 verb 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 + `wave_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 
override 1:1 严格 + 1 视觉组 1 段 1:1 严格 + 1 verb 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 + 1 显式契约 "5 verb windup VFX 
`trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格 (1 verb 1 `trigger()` 1 verb-specific 0 override 必 1 视觉组 1 段 必 0 
override 3 内部状态字段, 5 verb 0 漏 1 3 维度拼接 边)" 1 段 1:1 严格 镜像 §9.6.48 + §9.6.51 + §9.6.52 + §9.6.53 + §9.6.54 + 1 3 维度拼接 0 
触碰既有 1:1 严格)** — §9.6.55 段 是 1 套 polish 模式首个 1:1 包含 5 verb 0 override `trigger()` verb-specific 5 元素 + 5 verb 1 视觉组 1 段 
5 元素 + 5 verb 0 override 3 内部状态字段 5 元素 + 1 显式契约 + 1 3 维度拼接 0 触碰既有 = 5 + 5 + 5 + 1 + 1 = 17 元素 1:1 严格 的 5 verb windup 
VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约 聚焦段 polish 模式, 17 元素 1:1 严格分离 (1 Pulse 1 `trigger()` 1 
verb-specific 0 override 1:1 严格 + 1 Pulse 1 视觉组 1 段 1:1 严格 + 1 Pulse 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 1:1 
严格 (3 元素 1 3 维度拼接 0 漏 0 改 0 反序 0 反向) + 1 Bind 1 `trigger()` 1 verb-specific 0 override 1:1 严格 + 1 Bind 1 视觉组 1 段 1:1 严格 
+ 1 Bind 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 1:1 严格 + 1 Cut 1 `trigger()` 1 verb-specific 0 override 1:1 严格 + 
1 Cut 1 视觉组 1 段 1:1 严格 + 1 Cut 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 1:1 严格 + 1 Echo 1 `trigger()` 1 
verb-specific 0 override 1:1 严格 + 1 Echo 1 视觉组 1 段 1:1 严格 + 1 Echo 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 3 维度拼接 1:1 严格 
+ 1 Wave 1 `trigger()` 1 verb-specific 0 override 1:1 严格 + 1 Wave 1 视觉组 1 段 1:1 严格 + 1 Wave 0 override 3 内部状态字段 1:1 严格 
= 1 verb 1 3 维度拼接 1:1 严格 + 1 显式契约 "5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格" 1 段 1:1 严格 镜像 
§9.6.48 + §9.6.51 + §9.6.52 + §9.6.53 + §9.6.54 (0 漏 1 3 维度拼接 共享契约 0 改 1 字符 0 反序 0 反向) + 1 3 维度拼接 0 触碰既有 1:1 严格 (1 抽象契约 
1 元素), 0 漏 1 元素 0 改 1 字段 0 漏 1 字段 0 反向), 0 触碰既有 (T311 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / 
§9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / 
§9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / 
§9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / §9.6.51 / 
§9.6.52 / §9.6.53 / §9.6.54 45 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 
README.md / README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / 
REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).

**与 §9.6.48 关系** — §9.6.48 (T304 #231 落地) 关注 "5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约" (32 元素 1:1 严格). 
§9.6.55 关注 "5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约" (17 元素 1:1 严格, 5 verb 0 override 
`trigger()` verb-specific 5 元素 + 5 verb 1 视觉组 1 段 5 元素 + 5 verb 0 override 3 内部状态字段 5 元素 + 1 显式契约 + 1 3 维度拼接 0 触碰既有 = 
17 元素). §9.6.48 与 §9.6.55 是 "姊妹段 + 拼接段" (1 套 polish 模式 × 5 verb 0 override `trigger()` verb-specific 5 元素 1:1 严格 ⊂ 1 套 
polish 模式 × 5 verb 1 3 维度拼接 15 元素 1:1 严格, 1 套 polish 模式 × 32 元素 = 32 元素 1:1 严格 ∩ 1 套 polish 模式 × 17 元素 = 17 元素 1:1 严格 = 
6 元素 1:1 严格 交集 (5 verb 0 override `trigger()` verb-specific 5 元素 + 1 显式契约 1 元素 0 漏 0 改 0 反序 0 反向, 32 元素 1:1 严格 是 17 元素 
1:1 严格 父集 0 涉及 5 verb 1 视觉组 1 段 0 涉及 5 verb 0 override 3 内部状态字段 0 涉及 1 3 维度拼接 0 触碰既有). 任何"加新 1 verb windup VFX 
`trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接"必须**先**按 §9.6.48 32 元素 1:1 复制 5 verb 0 override `trigger()` verb-specific 5 元素 
(5 元素 1:1 严格, 0 漏 1 `trigger()` 0 改 1 字段 0 反序 0 反向 0 漏 1 段), **再**按 §9.6.52 7 元素 1:1 复制 5 verb 1 视觉组 1 段 5 元素, **再**按 
§9.6.51 9 元素 1:1 复制 5 verb 0 override 3 内部状态字段 5 元素, **再**按 §9.6.55 17 元素 1:1 拼接 (17 元素 1:1 严格, 0 漏 1 3 维度拼接 边 0 改 1 字段 
0 反向 0 触碰既有 §9.6.48 任何 1 字符 0 触碰既有 §9.6.51 任何 1 字符 0 触碰既有 §9.6.52 任何 1 字符).

**与 §9.6.51 关系** — §9.6.51 (T307 #234 落地) 关注 "5 verb windup VFX base 3 内部状态字段 0 override verb-specific 0 触碰既有 1:1 
严格分离契约" (9 元素 1:1 严格, 5 verb 0 override 3 内部状态字段 + 1 base 3 内部状态字段 0 触碰既有 + 1 Whisper 0 override 3 内部状态字段 1:1 严格 rename 
镜像 + 1 显式契约 + 1 3 内部状态字段 0 override verb-specific 0 触碰既有 = 9 元素). §9.6.55 关注 "5 verb windup VFX `trigger()` + 视觉组 + 
base 3 内部状态字段 3 维度拼接 1:1 严格分离契约" (17 元素 1:1 严格). §9.6.51 与 §9.6.55 是 "姊妹段 + 拼接段" (1 套 polish 模式 × 5 verb 0 override 3 
内部状态字段 5 元素 1:1 严格 ⊂ 1 套 polish 模式 × 5 verb 1 3 维度拼接 15 元素 1:1 严格, 1 套 polish 模式 × 9 元素 = 9 元素 1:1 严格 ∩ 1 套 polish 模式 × 
17 元素 = 17 元素 1:1 严格 = 6 元素 1:1 严格 交集 (5 verb 0 override 3 内部状态字段 5 元素 + 1 显式契约 1 元素 0 漏 0 改 0 反序 0 反向, 9 元素 1:1 严格 是 
17 元素 1:1 严格 子集 0 涉及 5 verb 0 override `trigger()` verb-specific 0 涉及 5 verb 1 视觉组 1 段 0 涉及 1 3 维度拼接 0 触碰既有). 任何"加新 1 
verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接"必须**先**按 §9.6.51 9 元素 1:1 复制 5 verb 0 override 3 内部状态字段 5 元素, 
**再**按 §9.6.55 17 元素 1:1 拼接.

**与 §9.6.52 关系** — §9.6.52 (T308 #236 落地) 关注 "5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约" (7 元素 1:1 严格, 5 verb 1 视觉组 
1 段 + 1 显式契约 + 1 视觉组 0 触碰既有 = 7 元素). §9.6.55 关注 "5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约" 
(17 元素 1:1 严格). §9.6.52 与 §9.6.55 是 "姊妹段 + 拼接段" (1 套 polish 模式 × 5 verb 1 视觉组 1 段 5 元素 1:1 严格 ⊂ 1 套 polish 模式 × 5 verb 
1 3 维度拼接 15 元素 1:1 严格, 1 套 polish 模式 × 7 元素 = 7 元素 1:1 严格 ∩ 1 套 polish 模式 × 17 元素 = 17 元素 1:1 严格 = 6 元素 1:1 严格 交集 (5 
verb 1 视觉组 1 段 5 元素 + 1 显式契约 1 元素 0 漏 0 改 0 反序 0 反向, 7 元素 1:1 严格 是 17 元素 1:1 严格 子集 0 涉及 5 verb 0 override `trigger()` 
verb-specific 0 涉及 5 verb 0 override 3 内部状态字段 0 涉及 1 3 维度拼接 0 触碰既有). 任何"加新 1 verb windup VFX `trigger()` + 视觉组 + base 3 
内部状态字段 3 维度拼接"必须**先**按 §9.6.52 7 元素 1:1 复制 5 verb 1 视觉组 1 段 5 元素, **再**按 §9.6.55 17 元素 1:1 拼接.

**与 §9.6.53 关系** — §9.6.53 (T309 #237 落地) 关注 "5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格分离契约" (12 元素 1:1 严格, 5 
verb 1 视觉组 1 段 + 5 verb 0 override 3 内部状态字段 + 1 显式契约 + 1 拼接 0 触碰既有 = 12 元素). §9.6.55 关注 "5 verb windup VFX `trigger()` 
+ 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约" (17 元素 1:1 严格, 5 verb 0 override `trigger()` verb-specific 5 元素 + 5 verb 1 视觉组 
1 段 5 元素 + 5 verb 0 override 3 内部状态字段 5 元素 + 1 显式契约 + 1 3 维度拼接 0 触碰既有 = 17 元素). §9.6.53 与 §9.6.55 是 "姊妹段 + 父集段" (1 套 
polish 模式 × 12 元素 1:1 严格 ⊂ 1 套 polish 模式 × 17 元素 1:1 严格, 1 套 polish 模式 × 12 元素 = 12 元素 1:1 严格 ∩ 1 套 polish 模式 × 17 元素 = 
17 元素 1:1 严格 = 12 元素 1:1 严格 交集 (5 verb 1 视觉组 1 段 5 元素 + 5 verb 0 override 3 内部状态字段 5 元素 + 1 显式契约 + 1 2 维度拼接 0 触碰既有 0 涉及 
5 verb 0 override `trigger()` verb-specific 0 涉及 1 3 维度拼接 0 触碰既有 替换 1 2 维度拼接 0 触碰既有 双方 各自 独立). 任何"加新 1 verb windup VFX 
`trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接"必须**先**按 §9.6.53 12 元素 1:1 复制 (12 元素 1:1 严格, 0 漏 1 拼接 边 0 改 1 字段 0 反序 0 反向 0 漏 
1 段), **再**按 §9.6.55 17 元素 1:1 复制 5 verb 0 override `trigger()` verb-specific 替换 1 2 维度拼接 0 触碰既有 (5 元素 1:1 严格, 0 漏 1 
`trigger()` 0 改 1 字段 0 反序 0 反向 0 漏 1 段 0 触碰既有 §9.6.53 任何 1 字符).

**与 §9.6.54 关系** — §9.6.54 (T310 #238 落地) 关注 "5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格分离契约" (12 元素 1:1 严格, 5 verb 
1 视觉组 1 段 + 5 verb 0 override `trigger()` verb-specific + 1 显式契约 + 1 拼接 0 触碰既有 = 12 元素). §9.6.55 关注 "5 verb windup VFX 
`trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约" (17 元素 1:1 严格, 5 verb 0 override `trigger()` verb-specific 5 元素 + 
5 verb 1 视觉组 1 段 5 元素 + 5 verb 0 override 3 内部状态字段 5 元素 + 1 显式契约 + 1 3 维度拼接 0 触碰既有 = 17 元素). §9.6.54 与 §9.6.55 是 "姊妹段 + 
父集段" (1 套 polish 模式 × 12 元素 1:1 严格 ⊂ 1 套 polish 模式 × 17 元素 1:1 严格, 1 套 polish 模式 × 12 元素 = 12 元素 1:1 严格 ∩ 1 套 polish 模式 
× 17 元素 = 17 元素 1:1 严格 = 12 元素 1:1 严格 交集 (5 verb 1 视觉组 1 段 5 元素 + 5 verb 0 override `trigger()` verb-specific 5 元素 + 1 
显式契约 + 1 2 维度拼接 0 触碰既有 0 涉及 5 verb 0 override 3 内部状态字段 0 涉及 1 3 维度拼接 0 触碰既有 替换 1 2 维度拼接 0 触碰既有 双方 各自 独立). 任何"加新 1 verb 
windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接"必须**先**按 §9.6.54 12 元素 1:1 复制 (12 元素 1:1 严格, 0 漏 1 拼接 边 0 改 1 字段 0 
反序 0 反向 0 漏 1 段), **再**按 §9.6.55 17 元素 1:1 复制 5 verb 0 override 3 内部状态字段 替换 1 2 维度拼接 0 触碰既有 (5 元素 1:1 严格, 0 漏 1 内部状态字段 
0 改 1 字段 0 反序 0 反向 0 漏 1 段 0 触碰既有 §9.6.54 任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 brittle 修复流程 5 步骤 1:1 严格分离契约 (T295 #219 §9.6.39 文档化) 跨 1 套 5 步骤 1:1 严格 0 涉及 5 verb 
windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约 自身, 1 套 5 步骤 = 5 步骤 1:1 严格 ∩ 1 套 17 元素 = 17 元素 1:1 严格 = 
0 元素 1:1 严格 交集 (T162 5 步骤关注 "复查根因 / 加新测试 / 修复 / 验证 / 跟踪" 5 步骤, 0 涉及 5 verb `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 
严格分离契约 自身, 5 步骤 1:1 严格 0 涉及 17 元素 1:1 严格). 任何"加新 1 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接"必须**先**按 
§9.6.55 17 元素 1:1 复制 (17 元素 1:1 严格, 0 漏 1 3 维度拼接 边 0 改 1 字段 0 反序 0 反向 0 漏 1 段), **再**走 T162 5 步骤 1:1 严格 (5 步骤 1:1 严格, 0 
漏 1 步 0 改 1 步 0 反序 0 反向 0 触碰既有 §9.6.55 任何 1 字符).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.55 5 verb 0 override `trigger()` verb-specific 5 元素 + 5 verb 1 视觉组 1 段 5 元素 + 
5 verb 0 override 3 内部状态字段 5 元素 + 1 显式契约 + 1 3 维度拼接 0 触碰既有 = 17 元素 走 §9.1 9 步落地的 1 步 (Stage 7 windup VFX `trigger()` + 
视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格, 跨 5 verb 各 3 元素 (1 `trigger()` 1 verb-specific 0 override + 1 视觉组 1 段 + 0 override 3 
内部状态字段 1 3 维度拼接) + 1 显式契约 1 元素 + 1 3 维度拼接 0 触碰既有 1 元素), 0 触碰 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 
`input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染). 任何"加新 1 verb windup VFX `trigger()` 
+ 视觉组 + base 3 内部状态字段 3 维度拼接"必须**先**按 §9.6.55 17 元素 1:1 镜像 (17 元素 1:1 严格, 0 漏 1 3 维度拼接 边 0 改 1 字段 0 反序 0 反向 0 漏 1 段 0 
触碰既有 8 步 (`verb` 枚举 + `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `audio` preset 字段 + `HUD 冷光勾边` 
渲染)). 任何"加新 1 §9.1 步"必须**先**在 §9.6.55 17 元素 加 1 边 (1 verb 1 3 维度拼接 1 边 或 1 显式契约 1 边 或 1 3 维度拼接 0 触碰既有 1 边), 0 触碰既有 8 步 
(1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 
`HUD 冷光勾边` 渲染 0 触碰既有).


### 9.6.56 T162 brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 polish 模式 
(T295 #219 + T162 跨 #185-#240 51 修复 ~52 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / §9.6.51 / §9.6.52 / §9.6.53 / §9.6.54 / §9.6.55 46 套 
> polish 模式均在 "pause_menu / 6 verb 单层 / 6 verb 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / audio 数据层 / 
> PlayerActionGate 单 autoload 4 件套 / SaveSystem CRC32 校验 / T162 brittle 修复流程 5 步骤模板 / 6 verb audio 家族 19 cue / 6 verb 
> cooldown ready jingle 5 段 / 6 verb `_ready()` / `_exit_tree()` 双 hook 串联 / 5 verb windup VFX 4 hook lifecycle / 5 
> verb windup VFX `_draw()` verb-specific / 5 verb windup VFX `trigger()` verb-specific / 5 verb windup VFX 共享 4 hook 0 
> override / 5 verb windup VFX `trigger()` + `_draw()` 双 verb-specific / 5 verb windup VFX base 3 内部状态字段 / 5 verb 
> windup VFX 视觉组连贯 lifecycle / 5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 / 5 verb windup VFX `trigger()` + 视觉组 拼接 / 5 
> verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接" 范畴, 0 涉及 "T162 brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 
> 5 文件 light sync 1:1 严格 0 漏 0 改 0 反向" 独立聚焦段 落地. §9.6.39 (T295 #219 落地) 关注 "T162 brittle 修复流程 5 步骤 1:1 严格分离契约 模板" (5 元素 
> 1:1 严格, 1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 1 0 触碰既有 + 1 cross-section 5 文件 同步, 跨 5 步骤 各 1 元素 = 5 元素 1:1 严格). 
> §9.6.55 (T311 #239 落地) 关注 "5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约" (17 元素 1:1 严格). 上述 
> 2 段 (§9.6.39 + §9.6.55) 均聚焦 单一契约模板 OR 单一 verb 3 维度拼接 1:1 严格分离契约, 0 涉及 "T162 brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 实际 累计 
> 51 修复 (T162 #185 起步 1 + #187 1 + #190 1 + #195 3 + #200 4 + #205 3 + #210 14 + #215 4 + #220 3 + #225 6 + #230 7 + 
> #235 2 + #240 2 = 51 修复) 每 1 修复 1:1 严格 镜像 5 步骤 模板" 独立聚焦段 自身 (即 13 审查轮 × 51 修复 × 5 步骤 1:1 严格 镜像, 跨 51 修复 各自 5 步骤 1:1 
> 严格 0 漏 0 改 0 反序 0 反向). 本节 (T312 #241 落地) 首个落在**T162 brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync + 1 
> 显式契约 + 1 0 触碰既有 + 1 0 副作用** 聚焦段 (5 步骤 5 元素 1:1 严格 (Stage 1 expect 反转 1:1 严格 0 漏 1 字段 + Stage 2 docblock 说明 1:1 严格 0 漏 
> 1 字符 + Stage 3 段 find 反转 1:1 严格 0 漏 1 锚点 + Stage 4 0 触碰既有 1:1 严格 0 漏 1 段 + Stage 5 cross-section 5 文件 同步 1:1 严格 0 漏 1 
> 文件, 跨 5 步骤 各 1 元素 = 5 元素 1:1 严格 0 漏 0 改 0 反序 0 反向) + 51 修复 51 元素 1:1 严格 (T162 #185 起步 1 + #187 1 + #190 1 + #195 3 + 
> #200 4 + #205 3 + #210 14 + #215 4 + #220 3 + #225 6 + #230 7 + #235 2 + #240 2 = 51 修复, 跨 51 修复 各自 5 步骤 1:1 严格 镜像, 0 
> 漏 1 修复 0 改 1 字段 0 改 1 字符 0 反序 0 反向) + 5 文件 light sync 5 元素 1:1 严格 (CHANGELOG.md 顶部 1 段 + ROADMAP.md 顶部 1 时间戳 + 
> README.md 'Recent completed work' 1 段 + README.zh-CN.md '最近完成的工作' 1 段 + ITERATION_COUNT.txt 1 计数, 跨 5 文件 各 1 元素 = 5 
> 元素 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "T162 brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync 1:1 严格 (1 修复 
> 必 5 步骤 1:1 严格 必 5 文件 1:1 严格, 51 修复 0 漏 1 步 0 漏 1 文件 0 改 1 字段 0 反序 0 反向)" 1 段 1:1 严格 (1 契约 1 元素) + 1 0 触碰既有 1:1 严格 (1 
> 修复 1 5 步骤 1:1 严格 镜像 1 5 文件 1:1 严格 拼接 0 改 §9.6.39 5 步骤 模板 任何 1 字符 0 改 跨 50 修复 任何 1 字符 0 漏 1 边 0 反序 0 反向, 1 抽象契约 1 元素) 
> + 1 0 副作用 1:1 严格 (1 修复 1 5 步骤 0 触碰既有 工具链 任何 1 文件 0 触碰游戏代码 任何 1 字段 0 触碰 §10 决策记录流程 0 触碰 README 内容 0 触碰 ITERATION_COUNT 
> 0 触碰 CHANGELOG / ROADMAP / REVIEW_LOG 内容, 1 抽象契约 1 元素)) polish 模式 — 跨 5 步骤 × 51 修复 × 5 文件 = 5 + 51 + 5 = 61 元素 1:1 严格 
> + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 3 元素 1:1 严格 = 5 + 51 + 5 + 1 + 1 + 1 = 64 元素 1:1 严格 (注意: 5 步骤 5 元素 1:1 严格 = 5 步骤 各自 1 
> 元素 = 5 元素 + 51 修复 51 元素 1:1 严格 = 51 修复 各自 1 元素 = 51 元素 + 5 文件 5 元素 1:1 严格 = 5 文件 各自 1 元素 = 5 元素 + 1 显式契约 1 元素 + 1 0 
> 触碰既有 1 元素 + 1 0 副作用 1 元素 = 5 + 51 + 5 + 1 + 1 + 1 = 64 元素 1:1 严格, 0 漂动 0 后续 主要 pre-existing 风险) 0 触碰既有 51 修复 任何 1 字符 
> 0 反向 0 例外. 任何"加新 1 修复" (e.g. 未来 #245+ 加新 1 brittle 修复) 必须**先**按 Stage 1 expect 反转 1:1 严格 加 1 修复 entry (Stage 1 1:1 严格,
>  0 漏 1 字段 0 改 1 字符 0 反序 0 反向, **0 漏 1 步 0 漏 1 文件 0 漏 1 修复**), **再**按 Stage 2 docblock 说明 1:1 严格 加 1 修复 entry, **再**按 
> Stage 3 段 find 反转 1:1 严格 加 1 修复 entry, **再**按 Stage 4 0 触碰既有 1:1 严格 镜像 (0 漏 1 段 0 改 1 字符 0 反序 0 反向), **再**按 Stage 5 
> cross-section 5 文件 同步 1:1 严格 镜像 (0 漏 1 文件 0 改 1 字符 0 反序 0 反向). 跨 1 套 polish 模式 × 5 步骤 × 51 修复 × 5 文件 + 3 元素 = 5 + 51 
> + 5 + 1 + 1 + 1 = 64 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: `T162 brittle 修复流程 51 修复` (#185 1 + #187 1 + #190 1 + #195 3 + #200 4 + #205 3 + #210 14 + #215 4 + #220 3 + 
#225 6 + #230 7 + #235 2 + #240 2 = 51 修复, 跨 13 审查轮 累计 ~52 轮 落地) 现有的 5 步骤 (Stage 1 expect 反转 1:1 严格 + Stage 2 docblock 
说明 1:1 严格 + Stage 3 段 find 反转 1:1 严格 + Stage 4 0 触碰既有 1:1 严格 + Stage 5 cross-section 5 文件 同步 1:1 严格) + 5 文件 light sync 
(CHANGELOG.md + ROADMAP.md + README.md + README.zh-CN.md + ITERATION_COUNT.txt), 在反复 polish (T162 #185 起步 → T311 #239 
累计 51 修复 ~52 轮 落地) 期间出现 3 类典型 fragile: (1) **"加新 1 修复 漏 5 步骤 1:1 严格"** — 加新 1 修复 (e.g. 假设未来 #245+ 加新 1 §9.6.57 段 polish 
触发 1 修复) 落地时漏 5 步骤 (e.g. 漏 Stage 1 expect 反转 中 expect 改 反向, 或漏 Stage 2 docblock 说明 中 docblock 解释为什么反向, 或漏 Stage 3 段 
find 反转 中 find 字符串 改 反向, 或漏 Stage 4 0 触碰既有 中既 verify test_*.gd 任何其他 expect / docblock / find 0 改, 或漏 Stage 5 
cross-section 5 文件 同步 中 5 文件 light sync 缺 1), 1 修复 0 5 步骤 0 闭环 0 漂动; (2) **"加新 1 docblock 漏 1 行"** — 加新 1 docblock 
(e.g. 假设未来 #245+ 加新 1 修复 的 docblock 行 解释) 落地时漏 1 行 (e.g. 漏 docblock 上方 `#` 注释行 或 docblock 自身行 1 行), 1 docblock 扩展 0 5 
步骤 0 闭环 0 漂动; (3) **"修 1 expect 0 触碰既有 50 修复"** — 修 1 expect (e.g. 改 1 test_*.gd 中 expect 行 0 改 既有) 或 修 1 段 find 字符串 
(e.g. 改 1 test_*.gd 中 find 字符串 0 改 既有) 或 加新 1 修复 (e.g. 跨 51 修复 加 1 修复 = 52 修复) 时 0 触碰既有 50 修复 任何 1 字段, 跨修复 1:1 漂动. 3 
类症状根因: T162 brittle 修复流程 51 修复 1:1 序列 0 文档化独立聚焦段, 任何新 1 修复 接入只能"凭经验" 1:1 镜像 5 步骤 模板 (§9.6.39 5 元素 1:1 严格), 1 漏 1 步 = 1 
修复 0 5 步骤 0 闭环 0 漂动 (1 漏 1 步 0 5 步骤 0 1:1 严格分离契约 0 漂动 / 1 漏 1 行 0 docblock 0 1:1 严格 0 漂动 / 1 漏 1 字符 0 cross-section 0 
1:1 严格 0 漂动).

- **触发场景**: 加新 1 修复 (e.g. 未来 #245+ 加新 1 brittle 修复 走 T162 流程, 实际现状: 51 修复 跨 13 审查轮 累计 0 漏 0 改 0 反序 0 反向) / 加新 1 
docblock (T162 流程参考) / 加新 1 cross-section 文件 (e.g. 假设未来加 REVIEW_LOG.md 顶部 1 段 同步) 时**必须**严格按 64 元素 1:1 复制 T295 #219 + 
T162 跨 51 修复 落地模式: (1) **Stage 1 expect 反转 1:1 严格** — 在 test_*.gd 中定位 1 expect 行, 改 反向 期望 == / != / ≥ / ≤ 1 字符 (Stage 1 
1:1 严格, 0 漏 1 字段 0 改 1 字符 0 反序 0 反向, **0 漏 1 修复 0 漏 1 步**); (2) **Stage 2 docblock 说明 1:1 严格** — 在 expect 上方加 1 行 
docblock 解释为什么反向 (Stage 2 1:1 严格, 0 漏 1 字符 0 改 1 字符 0 反序 0 反向, 0 漏 1 行 0 触碰既有 4 步骤); (3) **Stage 3 段 find 反转 1:1 严格** — 
在 test_*.gd 中定位 1 段 find 字符串, 改 反向 字符串 (Stage 3 1:1 严格, 0 漏 1 锚点 0 改 1 锚点 0 反序 0 反向, 0 漏 1 段 0 触碰既有 4 步骤); (4) **Stage 
4 0 触碰既有 1:1 严格 镜像** — 0 改 test_*.gd 任何其他 expect / docblock / find / 段 (Stage 4 1:1 严格, 0 漏 1 段 0 改 1 字符 0 反序 0 反向, 0 漏 
1 段 0 漏 1 文件 0 漏 1 锚点); (5) **Stage 5 cross-section 5 文件 同步 1:1 严格 镜像** — CHANGELOG.md 顶部 1 段 + ROADMAP.md 顶部 1 时间戳 + 
README.md 'Recent completed work' 1 段 + README.zh-CN.md '最近完成的工作' 1 段 + ITERATION_COUNT.txt 1 计数 (Stage 5 1:1 严格, 0 漏 1 
文件 0 改 1 字符 0 反序 0 反向, 0 漏 1 字段 0 漏 1 锚点 0 漏 1 边 0 漏 1 修复). 64 元素 1:1 同步 (5 步骤 5 元素 1:1 严格 + 51 修复 51 元素 1:1 严格 + 5 文件 
5 元素 1:1 严格 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 64 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 漏 1 修复 0 漏 1 步 0 漏 1 文件), 1 漏 1 步 / 1 漏 1 
修复 / 1 漏 1 文件 = 1 修复 / 1 步 / 1 文件 扩展 0 64 元素 0 闭环 0 漂动. T295 #219 + T162 跨 51 修复 0 触碰既有 0 改 test_*.gd 任何其他 expect 任何 1 
字符, 0 改 test_*.gd 任何其他 docblock 任何 1 字符, 0 改 test_*.gd 任何其他 段 find 任何 1 字符, 0 改 既有 50 修复 任何 1 字段, 0 改 §9.6.39 5 步骤 模板 
任何 1 字符, 0 改 §9.6.55 17 元素 任何 1 字符.

- **修复** (跨 1 套 polish 模式 T312 #241 落地后已知 64 元素 0 例外):
  - **T312 #241 首个 T162 brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 独立聚焦段 
落地 (5 步骤 5 元素 1:1 严格 + 51 修复 51 元素 1:1 严格 + 5 文件 5 元素 1:1 严格 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 5 + 51 + 5 + 1 + 1 + 1 = 
64 元素 1:1 严格)** — §9.6.56 段 是 1 套 polish 模式首个 1:1 包含 5 步骤 (Stage 1 expect 反转 1:1 严格 + Stage 2 docblock 说明 1:1 严格 + 
Stage 3 段 find 反转 1:1 严格 + Stage 4 0 触碰既有 1:1 严格 + Stage 5 cross-section 5 文件 同步 1:1 严格) + 51 修复 (T162 跨 #185-#240 累计 
51 修复 1:1 严格 镜像 5 步骤 模板, 0 漏 1 修复 0 改 1 字段 0 反序 0 反向) + 5 文件 light sync (CHANGELOG.md + ROADMAP.md + README.md + 
README.zh-CN.md + ITERATION_COUNT.txt) + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 5 + 51 + 5 + 1 + 1 + 1 = 64 元素 1:1 严格 的 T162 
brittle 修复流程 51 修复 1:1 严格 polish 模式, 64 元素 1:1 严格分离 (5 步骤 5 元素 1:1 严格 0 漏 0 改 0 漏 1 步 + 51 修复 51 元素 1:1 严格 0 漏 0 改 0 漏 
1 修复 + 5 文件 5 元素 1:1 严格 0 漏 0 改 0 漏 1 文件 + 1 显式契约 1 元素 1:1 严格 0 漏 0 改 + 1 0 触碰既有 1 元素 1:1 严格 0 漏 0 改 + 1 0 副作用 1 元素 1:1 
严格 0 漏 0 改, 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T312 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / 
§9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / 
§9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / 
§9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / 
§9.6.51 / §9.6.52 / §9.6.53 / §9.6.54 / §9.6.55 46 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / .tscn / 任何 gameplay code, 0 触碰 
§10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容 (除顶部 1 段 + 'Recent completed work' / '最近完成的工作' 段 1 行 light sync), 0 触碰 
ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容 (除顶部 
1 段 light sync + ITERATION_COUNT.txt +1)).

**与 §9.6.39 关系** — §9.6.39 (T295 #219 落地) 关注 "T162 brittle 修复流程 5 步骤 接入 1:1 严格分离契约 模板" (5 元素 1:1 严格, 1 expect 反转 + 1 
docblock 说明 + 1 段 find 反转 + 1 0 触碰既有 + 1 cross-section 5 文件 同步, 跨 5 步骤 各 1 元素 = 5 元素 1:1 严格). §9.6.56 关注 "T162 brittle 
修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用" (64 元素 1:1 严格, 5 步骤 5 元素 + 51 修复 51 
元素 + 5 文件 5 元素 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 64 元素). §9.6.39 与 §9.6.56 是 "姊妹段 + 母集段" (1 套 polish 模式 × 5 步骤 5 元素 1:1 
严格 ⊂ 1 套 polish 模式 × 64 元素 1:1 严格, 1 套 polish 模式 × 5 元素 = 5 元素 1:1 严格 ∩ 1 套 polish 模式 × 64 元素 = 64 元素 1:1 严格 = 5 元素 1:1 
严格 交集 (5 步骤 5 元素 + 5 文件 5 元素 0 涉及 51 修复 任何 1 修复 0 涉及 1 显式契约 0 涉及 1 0 触碰既有 0 涉及 1 0 副作用, 5 元素 1:1 严格 是 64 元素 1:1 严格 子集 0 
涉及 51 修复 任何 1 修复 0 涉及 1 显式契约 0 涉及 1 0 触碰既有 0 涉及 1 0 副作用). 任何"加新 1 修复"必须**先**按 §9.6.39 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步 
0 改 1 字段 0 反序 0 反向 0 漏 1 锚点 0 漏 1 字符 0 漏 1 文件), **再**按 §9.6.56 64 元素 1:1 拼接 51 修复 51 元素 替换 1 抽象契约 1 元素 (51 元素 1:1 严格, 0 
漏 1 修复 0 改 1 字段 0 反序 0 反向 0 漏 1 段 0 触碰既有 §9.6.39 任何 1 字符 0 触碰既有 50 修复 任何 1 字符).

**与 §9.6.55 关系** — §9.6.55 (T311 #239 落地) 关注 "5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格分离契约" 
(17 元素 1:1 严格, 5 verb 0 override `trigger()` verb-specific 5 元素 + 5 verb 1 视觉组 1 段 5 元素 + 5 verb 0 override 3 内部状态字段 5 
元素 + 1 显式契约 + 1 3 维度拼接 0 触碰既有 = 17 元素). §9.6.56 关注 "T162 brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync + 
1 显式契约 + 1 0 触碰既有 + 1 0 副作用" (64 元素 1:1 严格). §9.6.55 与 §9.6.56 0 互混 0 复用 0 共享: §9.6.55 关注 "5 verb windup VFX 
`trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 1:1 严格" (与 T162 brittle 修复流程 51 修复 0 相关, 17 元素 0 涉及 5 步骤 expect 反转 / docblock 
说明 / 段 find 反转 / 0 触碰既有 / cross-section 同步 5 步骤 0 涉及 51 修复 任何 1 修复 0 涉及 5 文件 light sync 任何 1 文件), §9.6.56 关注 "T162 
brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用" (与 5 verb windup VFX 3 维度拼接 
0 相关, 64 元素 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 64 元素 = 64 元素 1:1 严格). 2 套 polish 模式 是 "姊妹段" (1 套 
polish 模式 × 17 元素 = 17 元素 1:1 严格 镜像 1 套 polish 模式 × 64 元素 = 64 元素 1:1 严格, 一是 5 verb windup VFX 3 维度拼接 1:1 严格, 一是 T162 
brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync; 1 套 polish 模式 × 17 元素 = 17 元素 1:1 严格 + 1 套 polish 模式 × 64 
元素 = 64 元素 1:1 严格). 任何"加新 1 修复"必须**先**按 §9.6.39 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步 0 改 1 字段 0 反向) **或** 按 §9.6.55 17 元素 
1:1 复制 (17 元素 1:1 严格, 0 漏 1 元素 0 改 1 字段 0 反向), **再**按 §9.6.56 64 元素 1:1 同步 (64 元素 1:1 严格, 0 漏 1 元素 0 漏 1 边 0 改 1 字段 0 改 
1 字符 0 反向 0 触碰既有 §9.6.55 任何 1 字符) **或** 按 §9.6.55 17 元素 1:1 同步 (17 元素 1:1 严格, 0 漏 1 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 
触碰既有 §9.6.56 任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 
0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 51 修复 累计 0 后续 持续收敛), §9.6.56 是 "T162 brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 
light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用" (64 元素 1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约). T162 与 §9.6.56 0 互混 0 复用 0 
共享: T162 关注 "brittle 修复流程 5 步骤" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 + 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 
漏 1 段 0 漏 1 锚点, 5 步骤 1:1 严格), §9.6.56 关注 "5 步骤 + 51 修复 + 5 文件 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 1:1 严格分离契约 + 1 套 polish 模式 
既有 1:1 严格分离契约" (与 5 步骤 0 相关, 1 套 polish 模式 × 64 元素 = 64 元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离, 1 套 polish 模式 × 64 元素 = 64 元素 1:1 
严格). 任何"加新 1 修复"必须**先**按 T162 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), **再**按 
§9.6.56 64 元素 1:1 镜像 (64 元素 1:1 严格, 0 漏 1 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点 0 漏 1 文件).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.56 5 步骤 + 51 修复 + 5 文件 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 64 元素 走 §9.1 9 步落地的 0 步 
(T162 brittle 修复流程 51 修复 跨 13 审查轮 5 步骤 + 5 文件 light sync 接入 是 polish 工具链 范畴, 0 走 9 步任何 1 步), 0 触碰 9 步 (1 `verb` 枚举 + 1 
`ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 
冷光勾边` 渲染). 任何"加新 1 修复"必须**先**按 §9.6.56 64 元素 1:1 镜像 (64 元素 1:1 严格, 0 漏 1 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 修复 0 漏 
1 步 0 漏 1 文件 0 触碰既有 9 步 (`verb` 枚举 + `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `vfx` 视觉组 + 
`audio` preset 字段 + `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 步"必须**先**在 §9.6.56 64 元素 加 1 边 (1 修复 1 5 步骤 1 边 或 1 显式契约 1 边 或 1 0 
触碰既有 1 边 或 1 0 副作用 1 边), 0 触碰既有 9 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 
`icon` PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).
