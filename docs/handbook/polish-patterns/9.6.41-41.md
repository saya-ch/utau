# Polish Patterns 9.6.41–41

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.41–9.6.41，共 1 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.41-41.md

<!-- shard 41-41 共 1 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.41 6 verb `_ready()` super 调用顺序 1:1 严格分离契约 polish 模式 (D002.B #98 + T166 #85 + T167 #86 + T168 #86 + T169 #87 + 
T171 #89 + T173 #92 + T174 #93 + F013.E #159 跨 9 任务 ~123 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 31 套 polish 模式均在 "pause_menu / 6 verb 单层 / 6 
> verb 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / audio 数据层 / PlayerActionGate 单 autoload 4 件套 / 
> SaveSystem CRC32 校验 / T162 brittle 修复流程 / 6 verb audio 家族 19 cue / 6 verb cooldown ready jingle 5 段" 范畴, 0 涉及 "6 verb 
> `_ready()` super 调用顺序 1:1 严格分离契约" 落地. §9.6.18 (T273 #192 落地) 关注 "16 件套 verb ability base (5 verb ability 共享 base 0 涉及 
> `_ready()` super 调用顺序 1:1 严格分离契约)" (与 6 verb `_ready()` super 调用顺序 0 相关, 16 件套仅 verb ability base 字节码 0 涉及 `_ready()` 
> super 调用顺序 自身 1:1 严格分离), §9.6.40 (T296 #221 落地) 关注 "6 verb cooldown ready jingle 5 段 1:1 严格分离契约" (与 6 verb `_ready()` 
> super 调用顺序 0 相关, 5 段 0 涉及 `_ready()` super 调用顺序 自身 1:1 严格分离). 本节 (T297 #222 落地) 首个落在**6 verb `_ready()` super 调用顺序 
> 1:1 严格分离契约** (`src/scripts/_verb_ability_base.gd` 显式契约 "Lifecycle contract (subclasses MUST call `super._ready()` and 
> `super._exit_tree()` from their overrides)" 1 段 + 6 verb 子类 (`pulse_ability.gd` / `bind_ability.gd` / 
> `cut_ability.gd` / `echo_ability.gd` / `resonance_wave_ability.gd` / `whisper_ability.gd`) 各自 `_ready()` 0 触碰既有 1:1 
> 严格 调用 `super._ready()` 第 1 行 + verb-specific perk application 后续行 + 6 verb 各自 `_exit_tree()` 0 触碰既有 1:1 严格 调用 
> `super._exit_tree()` 0 漏 1 边 + 1 `_player` non-null assertion 0 触碰既有 1:1 严格) polish 模式 — 跨 6 verb 1:1 严格 (Stage 1 
> Pulse 1 verb 1:1 严格 0 漏 0 改 + Stage 2 Bind 1 verb 1:1 严格 0 漏 0 改 + Stage 3 Cut 1 verb 1:1 严格 0 漏 0 改 + Stage 4 Echo 1 
> verb 1:1 严格 0 漏 0 改 + Stage 5 Wave 1 verb 1:1 严格 0 漏 0 改 + Stage 6 Whisper 1 verb 1:1 严格 0 漏 0 改) — 跨 1 套 polish 模式 
> (T297 #222 落地) 跨 9 任务 (D002.B #98 + T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T173 #92 + T174 #93 + 
> F013.E #159 ~123 轮落地, 0 漂动 0 后续 主要 pre-existing 风险). 任何"加新 1 verb" (e.g. 未来加 7th verb "Drain" 接入 `_ready()` super 
> 调用顺序) 或"加新 1 perk" (e.g. 未来加 7th verb "Drain" 接入 perk application) 或"修 1 verb `_ready()` 顺序" (e.g. 假设未来微调 6 verb 任意 1 
> verb `_ready()` super 调用顺序) 必须**先**按 Stage 1 Pulse 1 verb 1:1 严格 加 1 entry (0 漏 1 verb 0 改 1 字符 0 反序 0 反向), **再**按 
> Stage 2 Bind 1 verb 1:1 镜像 (1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), **再**按 Stage 3 Cut 1 verb 1:1 镜像 (1:1 严格, 0 漏 1 
> verb 0 改 1 字符 0 反序 0 反向), **再**按 Stage 4 Echo 1 verb 1:1 镜像 (1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), **再**按 Stage 5 
> Wave 1 verb 1:1 镜像 (1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), **再**按 Stage 6 Whisper 1 verb 1:1 镜像 (1:1 严格, 0 漏 1 verb 
> 0 改 1 字符 0 反序 0 反向). 跨 1 套 polish 模式 × 6 verb = 6 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: 6 verb `_ready()` super 调用顺序 (`src/scripts/_verb_ability_base.gd` `_ready()` 1 共享方法 + 6 verb 各自 `_ready()` 
1:1 严格 调用 `super._ready()` 第 1 行 + verb-specific perk application 后续行, D002.B #98 + T166 #85 + T167 #86 + T168 #86 + 
T169 #87 + T171 #89 + T173 #92 + T174 #93 + F013.E #159 跨 9 任务 ~123 轮落地) 现有的 6 verb (1 Pulse 1 verb `_ready()` 第 1 行 
`super._ready()` + 1 Bind 1 verb `_ready()` 第 1 行 `super._ready()` + 1 Cut 1 verb `_ready()` 第 1 行 `super._ready()` + 1 
Echo 1 verb `_ready()` 第 1 行 `super._ready()` + 1 Wave 1 verb `_ready()` 第 1 行 `super._ready()` + 1 Whisper 1 verb 
`_ready()` 第 1 行 `super._ready()` + 1 显式契约 "Lifecycle contract (subclasses MUST call `super._ready()` and 
`super._exit_tree()` from their overrides)" 1 段), 在反复 polish (D002.B #98 → F013.E #159 跨 9 任务 ~123 轮落地) 期间出现 4 类典型 
fragile: (1) **"加新 1 verb 漏 6 verb 1:1 严格"** — 加新 1 verb (e.g. 假设未来加 7th verb "Drain" 接入 `_ready()` super 调用顺序 系统) 落地时漏 
6 verb 1:1 严格 (e.g. 漏 Stage 1 Pulse 1 verb → Stage 2 Bind 1 verb → Stage 3 Cut 1 verb → Stage 4 Echo 1 verb → Stage 5 
Wave 1 verb → Stage 6 Whisper 1 verb 6 verb `_ready()` super 调用顺序 链 0 镜像 1 verb, 或漏 1 边 1 verb `_ready()` 第 1 行 
`super._ready()` 1:1 严格 0 漏 0 改 0 反向), 1 verb 扩展 0 6 verb 0 闭环 0 漂动; (2) **"加新 1 perk 漏 1 边"** — 加新 1 perk (e.g. 假设未来加 
7th verb "Drain" 接入 perk application 落地时漏 1 边 1 verb `_ready()` 后续 perk application 0 镜像, 1 verb 0 6 verb 0 闭环 0 漂动); 
(3) **"修 1 verb `_ready()` 顺序 0 触碰既有 5 verb"** — 修 1 verb `_ready()` 顺序 (e.g. 改 Pulse 1 verb `_ready()` 第 1 行 
`super._ready()` → 第 2 行 `super._ready()`, 改 1 行) 或 修 1 verb perk application 顺序 (e.g. 改 Wave 1 verb `_ready()` perk 
application 顺序 → 后续行 改 1 行) 时 0 触碰既有 5 verb (6 verb 任何 1 verb `_ready()` 任何 1 字符 0 改), 跨 verb 1:1 漂动; (4) **"加新 1 verb 
漏 1 边 `_player` non-null assertion"** — 加新 1 verb (e.g. 7th verb "Drain" 接入 `_ready()` super 调用顺序) 落地时漏 1 边 `_player` 
non-null assertion (e.g. 漏 `_verb_ability_base.gd` `_ready()` 中 `assert(_player != null, "VerbAbilityBase subclass must 
be child of CharacterBody2D")` 1 边 0 镜像 1 verb, 1 verb 0 `_player` non-null assertion 0 闭环 0 漂动). 4 类症状根因: 6 verb 
`_ready()` super 调用顺序 1:1 序列 0 文档化, 任何新 1 verb / 1 perk / 1 `_ready()` 顺序 接入只能"凭经验" 1:1 镜像 6 verb (1 Pulse 1:1 严格 + 1 
Bind 1:1 严格 + 1 Cut 1:1 严格 + 1 Echo 1:1 严格 + 1 Wave 1:1 严格 + 1 Whisper 1:1 严格), 1 漏 1 verb = 1 verb / 1 perk / 1 
`_ready()` 顺序 扩展 0 6 verb 0 闭环 0 漂动.

- **触发场景**: 加新 1 verb (e.g. 未来加 7th verb "Drain" 接入 `_ready()` super 调用顺序) / 加新 1 perk / 加新 1 `_ready()` 顺序 (D002.B #98 
模式参考) 时**必须**严格按 6 verb 1:1 复制 D002.B #98 落地模式: (1) **Stage 1 Pulse 1 verb 1:1 严格** — 在 `_verb_ability_base.gd` 
`_ready()` 方法 中 显式契约 "Lifecycle contract (subclasses MUST call `super._ready()` and `super._exit_tree()` from their 
overrides)" + 6 verb 各自 `_ready()` 第 1 行 `super._ready()` 1:1 严格 (6 verb 1:1 严格 0 漏 0 改 0 反序 0 反向), 0 触碰既有 6 verb 任何 1 
字符, source-grep 验证 `_ready()` 6 verb → 6 verb 0 漏 0 改 0 漏 1 边 0 反向; (2) **Stage 2 Bind 1 verb 1:1 严格** — 在 
`_verb_ability_base.gd` `_ready()` 方法 中 显式契约 + 6 verb 各自 `_ready()` 第 1 行 `super._ready()` 1:1 严格 (6 verb 1:1 严格 0 漏 0 
改 0 反序 0 反向), 0 触碰既有 6 verb 任何 1 字符, source-grep 验证 `_ready()` 6 verb → 6 verb 0 漏 0 改 0 漏 1 边 0 反向; (3) **Stage 3 Cut 
1 verb 1:1 严格** — 在 `_verb_ability_base.gd` `_ready()` 方法 中 显式契约 + 6 verb 各自 `_ready()` 第 1 行 `super._ready()` 1:1 严格 
(6 verb 1:1 严格 0 漏 0 改 0 反序 0 反向), 0 触碰既有 6 verb 任何 1 字符, source-grep 验证 `_ready()` 6 verb → 6 verb 0 漏 0 改 0 漏 1 边 0 
反向; (4) **Stage 4 Echo 1 verb 1:1 严格** — 在 `_verb_ability_base.gd` `_ready()` 方法 中 显式契约 + 6 verb 各自 `_ready()` 第 1 行 
`super._ready()` 1:1 严格 (6 verb 1:1 严格 0 漏 0 改 0 反序 0 反向), 0 触碰既有 6 verb 任何 1 字符, source-grep 验证 `_ready()` 6 verb → 6 
verb 0 漏 0 改 0 漏 1 边 0 反向; (5) **Stage 5 Wave 1 verb 1:1 严格** — 在 `_verb_ability_base.gd` `_ready()` 方法 中 显式契约 + 6 verb 
各自 `_ready()` 第 1 行 `super._ready()` 1:1 严格 (6 verb 1:1 严格 0 漏 0 改 0 反序 0 反向), 0 触碰既有 6 verb 任何 1 字符, source-grep 验证 
`_ready()` 6 verb → 6 verb 0 漏 0 改 0 漏 1 边 0 反向; (6) **Stage 6 Whisper 1 verb 1:1 严格** — 在 `_verb_ability_base.gd` 
`_ready()` 方法 中 显式契约 + 6 verb 各自 `_ready()` 第 1 行 `super._ready()` 1:1 严格 (6 verb 1:1 严格 0 漏 0 改 0 反序 0 反向), 0 触碰既有 6 
verb 任何 1 字符, source-grep 验证 `_ready()` 6 verb → 6 verb 0 漏 0 改 0 漏 1 边 0 反向. 6 verb 1:1 同步, 1 漏 1 verb / 1 漏 1 边 = 1 
verb / 1 perk / 1 `_ready()` 顺序 扩展 0 6 verb 0 闭环 0 漂动. D002.B #98 0 触碰既有 0 改 `_verb_ability_base.gd` `_ready()` 任何 1 字符 
(0 改 Pulse 1 verb 任何 1 字符, 0 改 Bind 1 verb 任何 1 字符, 0 改 Cut 1 verb 任何 1 字符, 0 改 Echo 1 verb 任何 1 字符, 0 改 Wave 1 verb 任何 
1 字符, 0 改 Whisper 1 verb 任何 1 字符).

- **修复** (跨 1 套 polish 模式 T297 #222 落地后已知 6 verb 0 例外):
  - **T297 #222 首个 6 verb `_ready()` super 调用顺序 1:1 严格分离契约 落地 (`_verb_ability_base.gd` `_ready()` 1 共享方法 + 显式契约 1 段 + 6 
verb 各自 `_ready()` 第 1 行 `super._ready()` 1:1 严格 + verb-specific perk application 后续行 + `_player` non-null assertion 0 
触碰既有 1:1 严格)** — §9.6.41 段 是 1 套 polish 模式首个 1:1 包含 6 verb (1 Pulse 1 verb 1:1 严格 + 1 Bind 1 verb 1:1 严格 + 1 Cut 1 verb 
1:1 严格 + 1 Echo 1 verb 1:1 严格 + 1 Wave 1 verb 1:1 严格 + 1 Whisper 1 verb 1:1 严格) 的 6 verb `_ready()` super 调用顺序 polish 
模式, 6 verb 1:1 严格分离 (1 Pulse 1 verb 1:1 严格 0 漏 0 改 + 1 Bind 1 verb 1:1 严格 0 漏 0 改 + 1 Cut 1 verb 1:1 严格 0 漏 0 改 + 1 
Echo 1 verb 1:1 严格 0 漏 0 改 + 1 Wave 1 verb 1:1 严格 0 漏 0 改 + 1 Whisper 1 verb 1:1 严格 0 漏 0 改, 0 漏 1 verb 0 改 1 字段 0 反序 0 
反向), 0 触碰既有 (T297 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / 
§9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / 
§9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 31 套 polish 模式任何 1 字符, 0 触碰游戏代码 
.gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / 
CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).
  - **T297 #222 Stage 1 Pulse 1 verb 1:1 严格分离契约 落地 (`pulse_ability.gd` `_ready()` 第 1 行 `super._ready()`)** — §9.6.41 段 
1:1 显式 Pulse 1 verb (T297 落地时 `pulse_ability.gd` `_ready()` 第 1 行 `super._ready()` 1:1 严格 (6 verb 1:1 严格 0 漏 0 改 0 反序 0 
反向), Pulse 1 verb 1:1 严格 (1 `pulse_ability.gd` `_ready()` 第 1 行 `super._ready()` 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 
严格 0 漏 0 改, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T297 0 改 `pulse_ability.gd` `_ready()` 第 1 行任何 1 字符)).
  - **T297 #222 Stage 2 Bind 1 verb 1:1 严格分离契约 落地 (`bind_ability.gd` `_ready()` 第 1 行 `super._ready()`)** — §9.6.41 段 
1:1 显式 Bind 1 verb (T297 落地时 `bind_ability.gd` `_ready()` 第 1 行 `super._ready()` 1:1 严格 (6 verb 1:1 严格 0 漏 0 改 0 反序 0 
反向), Bind 1 verb 1:1 严格 (1 `bind_ability.gd` `_ready()` 第 1 行 `super._ready()` 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 
0 漏 0 改, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T297 0 改 `bind_ability.gd` `_ready()` 第 1 行任何 1 字符)).
  - **T297 #222 Stage 3 Cut 1 verb 1:1 严格分离契约 落地 (`cut_ability.gd` `_ready()` 第 1 行 `super._ready()`)** — §9.6.41 段 1:1 
显式 Cut 1 verb (T297 落地时 `cut_ability.gd` `_ready()` 第 1 行 `super._ready()` 1:1 严格 (6 verb 1:1 严格 0 漏 0 改 0 反序 0 反向), 
Cut 1 verb 1:1 严格 (1 `cut_ability.gd` `_ready()` 第 1 行 `super._ready()` 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 
改, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T297 0 改 `cut_ability.gd` `_ready()` 第 1 行任何 1 字符)).
  - **T297 #222 Stage 4 Echo 1 verb 1:1 严格分离契约 落地 (`echo_ability.gd` `_ready()` 第 1 行 `super._ready()`)** — §9.6.41 段 
1:1 显式 Echo 1 verb (T297 落地时 `echo_ability.gd` `_ready()` 第 1 行 `super._ready()` 1:1 严格 (6 verb 1:1 严格 0 漏 0 改 0 反序 0 
反向), Echo 1 verb 1:1 严格 (1 `echo_ability.gd` `_ready()` 第 1 行 `super._ready()` 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 
0 漏 0 改, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T297 0 改 `echo_ability.gd` `_ready()` 第 1 行任何 1 字符)).
  - **T297 #222 Stage 5 Wave 1 verb 1:1 严格分离契约 落地 (`resonance_wave_ability.gd` `_ready()` 第 1 行 `super._ready()`)** — 
§9.6.41 段 1:1 显式 Wave 1 verb (T297 落地时 `resonance_wave_ability.gd` `_ready()` 第 1 行 `super._ready()` 1:1 严格 (6 verb 1:1 
严格 0 漏 0 改 0 反序 0 反向), Wave 1 verb 1:1 严格 (1 `resonance_wave_ability.gd` `_ready()` 第 1 行 `super._ready()` 1:1 严格 0 漏 0 
改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T297 0 改 `resonance_wave_ability.gd` 
`_ready()` 第 1 行任何 1 字符)).
  - **T297 #222 Stage 6 Whisper 1 verb 1:1 严格分离契约 落地 (`whisper_ability.gd` `_ready()` 第 1 行 `super._ready()`)** — 
§9.6.41 段 1:1 显式 Whisper 1 verb (T297 落地时 `whisper_ability.gd` `_ready()` 第 1 行 `super._ready()` 1:1 严格 (6 verb 1:1 严格 
0 漏 0 改 0 反序 0 反向), Whisper 1 verb 1:1 严格 (1 `whisper_ability.gd` `_ready()` 第 1 行 `super._ready()` 1:1 严格 0 漏 0 改 + 1 
source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T297 0 改 `whisper_ability.gd` `_ready()` 第 1 行任何 
1 字符)).
  - 跨 1 套 polish 模式 T297 #222 落地后 0 漂动, §9.6.41 段 1:1 严格 (6 verb 1:1 严格分离 + Pulse 1 verb 1:1 严格 + Bind 1 verb 1:1 严格 + 
Cut 1 verb 1:1 严格 + Echo 1 verb 1:1 严格 + Wave 1 verb 1:1 严格 + Whisper 1 verb 1:1 严格) 100% 闭环, 6 verb 1:1 严格分离 (1 Stage 
1 Pulse 1 verb 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 2 Bind 1 verb 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 3 Cut 1 verb 1:1 严格 0 漏 0 改 0 
反序 + 1 Stage 4 Echo 1 verb 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 5 Wave 1 verb 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 6 Whisper 1 verb 
1:1 严格 0 漏 0 改 0 反序). 1 套 polish 模式 0 后续 主要 pre-existing 风险.

- **预防**:
  1. 任何 polish 期给 6 verb `_ready()` super 调用顺序 加新 1 verb (e.g. 未来加 7th verb "Drain" 接入 `_ready()` super 调用顺序 系统) 或加新 1 
perk (e.g. 未来加 7th verb "Drain" 接入 perk application 落地时) 或加新 1 `_ready()` 顺序 (e.g. 未来微调 6 verb 任意 1 verb `_ready()` 
super 调用顺序) 任务**必须**严格按 6 verb 1:1 复制, 0 反序 0 漏 0 改 1 字段. 6 verb 0 触碰边界: **Stage 1 0 改 Pulse 1 verb 1:1 严格分离契约** (T297 
落地后 `pulse_ability.gd` `_ready()` 第 1 行 `super._ready()` 已切 1:1 strict 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 0 反向, 改 1 字符 / 漏 
1 字符 = 1 verb 扩展 0 6 verb 0 闭环 0 漂动), **Stage 2 0 改 Bind 1 verb 1:1 严格分离契约** (T297 落地后 `bind_ability.gd` `_ready()` 第 1 
行 `super._ready()` 已切 1:1 strict 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 0 反向, 改 1 字符 / 漏 1 字符 = 1 verb 扩展 0 6 verb 0 闭环 0 漂动), 
**Stage 3 0 改 Cut 1 verb 1:1 严格分离契约** (T297 落地后 `cut_ability.gd` `_ready()` 第 1 行 `super._ready()` 已切 1:1 strict 0 漏 0 
改 0 改 1 字符 0 漏 1 字符 0 反序 0 反向, 改 1 字符 / 漏 1 字符 = 1 verb 扩展 0 6 verb 0 闭环 0 漂动), **Stage 4 0 改 Echo 1 verb 1:1 严格分离契约** 
(T297 落地后 `echo_ability.gd` `_ready()` 第 1 行 `super._ready()` 已切 1:1 strict 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 0 反向, 改 1 字符 
/ 漏 1 字符 = 1 verb 扩展 0 6 verb 0 闭环 0 漂动), **Stage 5 0 改 Wave 1 verb 1:1 严格分离契约** (T297 落地后 `resonance_wave_ability.gd` 
`_ready()` 第 1 行 `super._ready()` 已切 1:1 strict 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 0 反向, 改 1 字符 / 漏 1 字符 = 1 verb 扩展 0 6 
verb 0 闭环 0 漂动), **Stage 6 0 改 Whisper 1 verb 1:1 严格分离契约** (T297 落地后 `whisper_ability.gd` `_ready()` 第 1 行 
`super._ready()` 已切 1:1 strict 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 0 反向, 改 1 字符 / 漏 1 字符 = 1 verb 扩展 0 6 verb 0 闭环 0 漂动). 
任何"加新 1 verb"必须**先**按 Stage 1 Pulse 1 verb 1:1 加 1 verb (Stage 1 1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向, **0 漏 1 verb**),
 **再**按 Stage 2 Bind 1 verb 1:1 加 1 verb (Stage 2 1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), **再**按 Stage 3 Cut 1 verb 1:1 
加 1 verb (Stage 3 1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), **再**按 Stage 4 Echo 1 verb 1:1 加 1 verb (Stage 4 1:1 严格, 0 漏 
1 verb 0 改 1 字符 0 反序 0 反向), **再**按 Stage 5 Wave 1 verb 1:1 加 1 verb (Stage 5 1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), 
**再**按 Stage 6 Whisper 1 verb 1:1 加 1 verb (Stage 6 1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向).
  2. **6 verb 0 触碰边界** — `pulse_ability.gd` `_ready()` + `bind_ability.gd` `_ready()` + `cut_ability.gd` `_ready()` + 
`echo_ability.gd` `_ready()` + `resonance_wave_ability.gd` `_ready()` + `whisper_ability.gd` `_ready()` 任何 1 verb 0 改 0 
删 0 重排 (6 verb 0 触碰边界), 任何"修 1 verb"polish 走 Stage 1 1:1 严格 (改 verb 既有 6 verb 时**必须**保持 6 verb 0 改 0 漏 0 反向 0 反序 0 触碰既有 
1 verb); 任何"加新 1 perk"polish 走 Stage 1 1:1 严格 (改 perk 既有 6 verb 时**必须**保持 6 verb 0 改 0 漏 0 反向 0 反序 0 漏 1 perk); 任何"加新 1 
`_ready()` 顺序"polish 走 Stage 1 1:1 严格 (改 `_ready()` 顺序 既有 6 verb 时**必须**保持 6 verb 0 改 0 漏 0 反向 0 反序 0 漏 1 顺序).
  3. **0 改 1 字段 0 漏 1 字段 0 反向** (T297 落地后 0 触碰边界) — 6 verb `_ready()` super 调用顺序 6 verb (1 Pulse 1 verb 第 1 行 
`super._ready()` + 1 Bind 1 verb 第 1 行 `super._ready()` + 1 Cut 1 verb 第 1 行 `super._ready()` + 1 Echo 1 verb 第 1 行 
`super._ready()` + 1 Wave 1 verb 第 1 行 `super._ready()` + 1 Whisper 1 verb 第 1 行 `super._ready()`) 1:1 严格分离 0 漏 0 改 0 改 
1 字段 0 漏 1 字段 0 反向, 改 1 字段 / 漏 1 字段 / 反向 1 字段 = 1 verb / 1 perk / 1 `_ready()` 顺序 扩展 0 6 verb 0 漂动, source-grep 验证 6 
verb 0 漏 0 改 1 字段 0 漏 1 字段 0 改 1 字符 0 反向.
  4. **0 改 1 边 0 漏 1 边 0 反向** (T297 落地后 0 触碰边界) — 接入契约 (1 Pulse 1 verb 1:1 严格 + 1 Bind 1 verb 1:1 严格 + 1 Cut 1 verb 1:1 
严格 + 1 Echo 1 verb 1:1 严格 + 1 Wave 1 verb 1:1 严格 + 1 Whisper 1 verb 1:1 严格) 1:1 严格分离 0 漏 0 改 0 改 1 边 0 漏 1 边 0 反向, 改 1 
边 / 漏 1 边 / 反向 1 边 = 1 verb / 1 perk / 1 `_ready()` 顺序 扩展 0 接入契约 0 漂动, source-grep 验证 6 verb 0 触碰接入契约 0 漏 0 改 1 边 0 漏 1 
边 0 改 1 字符 0 反向.
  5. **T162 brittle 修复流程 0 触碰边界** — 6 verb `_ready()` super 调用顺序 6 verb 0 副作用 子段 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 改 1 字符 0 
反向 0 漏 1 字段 0 触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构 (T162 
brittle 修复流程 = 1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), 改 
1 字段 / 漏 1 字段 / 反向 1 字段 = 1 verb / 1 perk / 1 `_ready()` 顺序 扩展 0 0 副作用 0 漂动 0 闭环, source-grep 验证 6 verb `_ready()` 
super 调用顺序 6 verb 0 副作用 子段 0 漏 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 
0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构.
  6. **1 套 polish 模式 0 互混 0 复用 0 共享 唯一性 标注 0 改 1 段 0 漏 1 段 0 反向** — §9.6.41 段 唯一性 标注 1:1 严格分离 0 漏 0 改 0 改 1 套 polish 模式 
0 漏 1 套 polish 模式 0 反向 0 触碰既有 0 改 1 段 (本节是 32 套 polish 模式**唯一**关注 6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约 的 1 套, 
0 互混 0 复用 0 共享), 改 1 套 polish 模式 / 漏 1 套 polish 模式 / 反向 1 套 polish 模式 = 1 §9.6.41 段 0 唯一性 标注 0 漂动 0 闭环, source-grep 验证 
§9.6.41 段 唯一性 标注 0 漏 0 改 1 套 polish 模式 0 漏 1 套 polish 模式 0 改 1 字符 0 反向.
  7. **§9.6.41 是 32 套 polish 模式**唯一**关注 "6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约 (`_verb_ability_base.gd` 
`_ready()` 1 共享方法 + 显式契约 1 段 + 6 verb 各自 `_ready()` 第 1 行 `super._ready()` 1:1 严格 + verb-specific perk application 后续行 
+ `_player` non-null assertion 0 触碰既有 1:1 严格) + Pulse 1 verb 1:1 严格 (1 1:1 严格 0 漏 0 改) + Bind 1 verb 1:1 严格 (1 1:1 严格 0 
漏 0 改) + Cut 1 verb 1:1 严格 (1 1:1 严格 0 漏 0 改) + Echo 1 verb 1:1 严格 (1 1:1 严格 0 漏 0 改) + Wave 1 verb 1:1 严格 (1 1:1 严格 0 
漏 0 改) + Whisper 1 verb 1:1 严格 (1 1:1 严格 0 漏 0 改) + 0 触碰游戏代码 + 0 触碰 §10 决策记录流程 0 触碰既有" 的 1 套** — 与 §9.6.6 / §9.6.7 / 
§9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / 
§9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / 
§9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 31 套 polish 模式 0 互混 0 复用 0 共享. §9.6.18 (T273 #192 落地) 关注 "16 件套 verb 
ability base (5 verb ability 共享 base 0 涉及 `_ready()` super 调用顺序 6 verb 1:1 严格分离契约)" (与 6 verb `_ready()` super 调用顺序 0 
相关, 16 件套仅 verb ability base 字节码 0 涉及 `_ready()` super 调用顺序 6 verb 自身 1:1 严格分离), §9.6.40 (T296 #221 落地) 关注 "6 verb 
cooldown ready jingle 5 段 1:1 严格分离契约" (与 6 verb `_ready()` super 调用顺序 0 相关, 5 段 0 涉及 `_ready()` super 调用顺序 6 verb 自身 
1:1 严格分离, §9.6.40 与 §9.6.41 是 "姊妹段", 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 镜像 1 套 polish 模式 × 6 verb = 6 元素 1:1 严格, 一是 6 
verb cooldown ready jingle 5 段, 一是 6 verb `_ready()` super 调用顺序 6 verb). 任何"加新 1 verb"或"加新 1 perk"或"加新 1 `_ready()` 
顺序"必须**先**按 §9.6.41 Stage 1 Pulse 1 verb 1:1 严格 加 1 verb (Stage 1 1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向, **0 漏 1 
verb**), **再**按 §9.6.41 Stage 2 Bind 1 verb 1:1 严格 加 1 verb (Stage 2 1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), **再**按 
§9.6.41 Stage 3 Cut 1 verb 1:1 严格 加 1 verb (Stage 3 1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), **再**按 §9.6.41 Stage 4 Echo 
1 verb 1:1 严格 加 1 verb (Stage 4 1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), **再**按 §9.6.41 Stage 5 Wave 1 verb 1:1 严格 加 1 
verb (Stage 5 1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向), **再**按 §9.6.41 Stage 6 Whisper 1 verb 1:1 严格 加 1 verb (Stage 6 
1:1 严格, 0 漏 1 verb 0 改 1 字符 0 反序 0 反向).
  8. 已知 drift risk: 6 verb `_ready()` super 调用顺序 6 verb 接入 **可能**漏 1 verb / 1 边 1:1 镜像 (e.g. 加新 1 verb 时漏 Stage 1 Pulse 
1 verb 中 1 verb 0 镜像, 1 verb 0 6 verb 0 闭环; 加新 1 verb 时漏 Stage 2 Bind 1 verb 中 0 镜像, 1 verb 0 Bind 0 闭环; 加新 1 verb 时漏 
Stage 3 Cut 1 verb 中 0 镜像, 1 verb 0 Cut 0 闭环; 加新 1 verb 时漏 Stage 4 Echo 1 verb 中 0 镜像, 1 verb 0 Echo 0 闭环; 加新 1 verb 时漏 
Stage 5 Wave 1 verb 中 0 镜像, 1 verb 0 Wave 0 闭环; 加新 1 verb 时漏 Stage 6 Whisper 1 verb 中 0 镜像, 1 verb 0 Whisper 0 闭环). 
监控建议: 任何"未来给 6 verb `_ready()` super 调用顺序 加新 1 verb / 加新 1 perk / 加新 1 `_ready()` 顺序"必须**先**在 6 verb (Stage 1 Pulse 1 
verb 1:1 严格 + Stage 2 Bind 1 verb 1:1 严格 + Stage 3 Cut 1 verb 1:1 严格 + Stage 4 Echo 1 verb 1:1 严格 + Stage 5 Wave 1 verb 
1:1 严格 + Stage 6 Whisper 1 verb 1:1 严格) 1:1 镜像 0 漏 1 verb / 0 漏 1 边 / 0 漏 1 字符 / **0 漏 1 行**. T297 (#222) 6 verb 1:1 
镜像落地后任何新 verb / 新 perk / 新 `_ready()` 顺序 是 §9.6.41 6 verb 的"母集" (1 verb / 1 perk / 1 `_ready()` 顺序 扩展 × 6 verb = 6 元素 
1:1 严格, §9.6.41 6 verb 1 套 polish 模式 完整闭环, 1 套 polish 模式 × 6 verb = 6 元素 1:1 严格, 含 Stage 1 Pulse 1 verb 1:1 严格 1 套 
polish 模式 6 verb 0 漏 0 改 + Stage 2 Bind 1 verb 1:1 严格 1 套 polish 模式 Bind 0 漏 0 改 + Stage 3 Cut 1 verb 1:1 严格 1 套 polish 
模式 Cut 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 段 + Stage 4 Echo 1 verb 1:1 严格 1 套 polish 模式 Echo 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 
改 1 字符 + Stage 5 Wave 1 verb 1:1 严格 1 套 polish 模式 Wave 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 字符 + Stage 6 Whisper 1 verb 1:1 
严格 1 套 polish 模式 Whisper 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 字符). 另 1 类已知 drift: Stage 1 Pulse 1 verb 1:1 严格 0 改 1 字符 0 漏 
1 字符 0 反向 (e.g. 后续 polish 期加新 1 verb 时漏 0 镜像 1 verb 任何 1 字符, 1 verb 0 闭环; 或漏 0 镜像 1 perk 任何 1 perk, 1 verb 0 perk 0 闭环; 
或改 1 字符 0 改 1 字符 0 反向 0 触碰既有), 后续 polish 期若加新 1 verb 必须**先**用 1:1 镜像 6 verb 模板, 0 改既有 0 漏 1 verb 0 漏 1 边 0 反向 **0 漏 1 
字符**.

**0 副作用**: T297 (#222) 任何 const / var 0 触碰 (`_verb_ability_base.gd` 0 改 0 删 0 重排 `_ready()` 方法任何 1 字符; 
`pulse_ability.gd` 0 改 0 删 0 重排 `_ready()` 第 1 行 `super._ready()` 任何 1 字符; `bind_ability.gd` 0 改 0 删 0 重排 `_ready()` 第 
1 行 `super._ready()` 任何 1 字符; `cut_ability.gd` 0 改 0 删 0 重排 `_ready()` 第 1 行 `super._ready()` 任何 1 字符; 
`echo_ability.gd` 0 改 0 删 0 重排 `_ready()` 第 1 行 `super._ready()` 任何 1 字符; `resonance_wave_ability.gd` 0 改 0 删 0 重排 
`_ready()` 第 1 行 `super._ready()` 任何 1 字符; `whisper_ability.gd` 0 改 0 删 0 重排 `_ready()` 第 1 行 `super._ready()` 任何 1 字符; 
CONTRIBUTING.md §9.6.41 段 0 改 0 删 0 重排, 仅新加 1 段 (~50 行)); 0 触碰 §9.6.6 ProfileRecentList 5 行 row 文本 5→7 字段 format 
字符串扩展模式 (T297 0 改 §9.6.6 任何 1 字符); 0 触碰 §9.6.7 ProfileRecentList 5 行 row hover feedback 三件套 (T297 0 改 §9.6.7 任何 1 字符); 
0 触碰 §9.6.8 ProfileQuickStats 4 段独立 hover 联动 (T297 0 改 §9.6.8 任何 1 字符); 0 触碰 §9.6.9 AchievementGrid + ProfileRecentList 
跨面板 `_alpha_base` Dictionary 双源 (T297 0 改 §9.6.9 任何 1 字符); 0 触碰 §9.6.10 「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (T297 0 
改 §9.6.10 任何 1 字符); 0 触碰 §9.6.15 SaveSystem audit_save_slots (T297 0 改 §9.6.15 任何 1 字符); 0 触碰 §9.6.16 6 verb hit 
handler 5 件套 (T297 0 改 §9.6.16 任何 1 字符); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T297 0 改 §9.6.17 任何 1 字符); 0 触碰 §9.6.18 
16 件套 verb ability base (T297 0 改 §9.6.18 任何 1 字符); 0 触碰 §9.6.19 7 件套 windup VFX base (T297 0 改 §9.6.19 任何 1 字符); 0 触碰 
§9.6.20 Hub ↔ archive 跨房间 transition flow (T297 0 改 §9.6.20 任何 1 字符); 0 触碰 §9.6.21 8 段成就序列 (T297 0 改 §9.6.21 任何 1 字符); 
0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T297 0 改 §9.6.22 任何 1 字符); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T297 0 改 §9.6.23 任何 1 字符); 
0 触碰 §9.6.24 HUD 6 verb × 6 通道 = 36 通道 5 段序列 (T297 0 改 §9.6.24 任何 1 字符); 0 触碰 §9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 5 段序列 
(T297 0 改 §9.6.25 任何 1 字符); 0 触碰 §9.6.26 6 verb icon 视觉组 5 段 canonical 1:1 序列 (T297 0 改 §9.6.26 任何 1 字符); 0 触碰 §9.6.27 
PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 5 段序列 (T297 0 改 §9.6.27 任何 1 字符); 0 触碰 §9.6.28 smoke test ITERATION_COUNT 
跨迭代 + 段边界 find 5 段序列 (T297 0 改 §9.6.28 任何 1 字符); 0 触碰 §9.6.29 polish 文档化 5 段 canonical 1:1 序列 模板 + 4 段关系段 模板 (T297 0 改 
§9.6.29 任何 1 字符); 0 触碰 §9.6.30 工具链 3 件套 1:1 严格分离契约 (T297 0 改 §9.6.30 任何 1 字符); 0 触碰 §9.6.31 CHANGELOG 归档 3 件套 1:1 
严格分离契约 (T297 0 改 §9.6.31 任何 1 字符); 0 触碰 §9.6.32 REVIEW_LOG 归档 3 件套 1:1 严格分离契约 (T297 0 改 §9.6.32 任何 1 字符); 0 触碰 §9.6.33 
6 verb 接入 4 件套 字节码一致性 (T297 0 改 §9.6.33 任何 1 字符); 0 触碰 §9.6.34 PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约 (T297 0 改 
§9.6.34 任何 1 字符); 0 触碰 §9.6.35 AudioPresets `MUSIC_PRESETS` preset 字段扩展 5 段 canonical 1:1 严格分离契约 (T297 0 改 §9.6.35 任何 1 
字符); 0 触碰 §9.6.36 PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约 (T297 0 改 §9.6.36 任何 1 字符); 0 触碰 §9.6.37 SaveSystem 
save data CRC32 校验 5 段 接入 1:1 严格分离契约 (T297 0 改 §9.6.37 任何 1 字符); 0 触碰 §9.6.38 6 verb audio 家族 19 cue 字段扩展 5 段 接入 1:1 
严格分离契约 (T297 0 改 §9.6.38 任何 1 字符); 0 触碰 §9.6.39 T162 brittle 修复流程 5 步骤 接入 1:1 严格分离契约 (T297 0 改 §9.6.39 任何 1 字符); 0 触碰 
§9.6.40 6 verb cooldown ready jingle 5 段 1:1 严格分离契约 (T297 0 改 §9.6.40 任何 1 字符); 0 触碰 check_smoke_consistency.sh (T297 0 
改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 
.git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, 0 触碰 `_verb_ability_base.gd` `_ready()` 方法任何 1 字符, 
0 触碰 6 verb 各自 `_ready()` 第 1 行 `super._ready()` 任何 1 字符); 0 行为变化, 1 文档 1 段 (§9.6.41 ~50 行) + 0 字节码修改 (T297 仅文档化既有 6 
verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约, 0 改 `_verb_ability_base.gd` 任何 1 字符 0 改 6 verb 各自 `_ready()` 第 1 行 
`super._ready()` 任何 1 字符 0 触碰既有) + 1 smoke test (T297 50+ 断言).

**与 §9.6.40 关系** — §9.6.40 (T296 #221 落地) 关注 "6 verb cooldown ready jingle 5 段 1:1 严格分离契约" (5 段 = Pulse 1 段 + Bind 1 段 
+ Cut 1 段 + Echo 1 段 + Wave 1 段, 5 段覆盖 6 verb cooldown ready jingle 5 段 0 涉及 6 verb `_ready()` super 调用顺序 6 verb 1:1 
严格分离契约). §9.6.41 关注 "6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约" (6 verb = Pulse 1 verb + Bind 1 verb + Cut 1 verb 
+ Echo 1 verb + Wave 1 verb + Whisper 1 verb, 6 verb 覆盖 6 verb `_ready()` super 调用顺序 6 verb 0 涉及 6 verb cooldown ready 
jingle 5 段). §9.6.40 与 §9.6.41 0 互混 0 复用 0 共享: §9.6.40 关注 "6 verb cooldown ready jingle 5 段" (与 6 verb `_ready()` super 
调用顺序 0 相关, 5 段 0 涉及 Pulse 1 verb / Bind 1 verb / Cut 1 verb / Echo 1 verb / Wave 1 verb / Whisper 1 verb 6 verb 
`_ready()` super 调用顺序), §9.6.41 关注 "6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约 6 verb 1:1 视觉组连贯" (与 6 verb cooldown 
ready jingle 5 段 0 相关, 6 verb 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 6 verb = 6 元素 1:1 严格). §9.6.41 与 
§9.6.40 是 "姊妹段" (1 套 polish 模式 × 6 verb = 6 元素 1:1 严格 镜像 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格, 一是 6 verb cooldown ready 
jingle 5 段, 一是 6 verb `_ready()` super 调用顺序 6 verb; 1 套 polish 模式 × 6 verb = 6 元素 1:1 严格 + 1 套 polish 模式 × 5 段 = 5 元素 
1:1 严格). 任何"加新 1 verb"必须**先**按 §9.6.41 6 verb 1:1 复制 (6 verb 1:1 严格, 0 漏 1 verb 0 改 1 字段 0 反向) **或** 按 §9.6.40 5 段 1:1 
复制 (5 段 1:1 严格, 0 漏 1 段 0 改 1 字段 0 反向), **再**按 §9.6.41 6 verb 1:1 同步 (6 verb 1:1 严格, 0 漏 1 verb 0 漏 1 边 0 改 1 字段 0 改 1 
字符 0 反向 0 触碰既有 §9.6.40 任何 1 字符) **或** 按 §9.6.40 5 段 1:1 同步 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 
§9.6.41 任何 1 字符).

**与 §9.6.18 关系** — §9.6.18 (T273 #192 落地) 关注 "16 件套 verb ability base (5 verb ability 共享 base)" (16 件套 = 5 字段 + 1 
@onready + 2 @export + `_ready` + `_process_cooldown` + `_consume_verb_cost` + `_setup_windup_state` + `_exit_tree` + 5 
verb 各自 `trigger` 镜像, 16 件套覆盖 verb ability base 字节码 0 涉及 6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约). §9.6.41 关注 "6 
verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约" (6 verb = Pulse 1 verb + Bind 1 verb + Cut 1 verb + Echo 1 verb + Wave 1 
verb + Whisper 1 verb, 6 verb 覆盖 6 verb `_ready()` super 调用顺序 6 verb 0 涉及 verb ability base 16 件套). §9.6.18 与 §9.6.41 0 
互混 0 复用 0 共享: §9.6.18 关注 "16 件套 verb ability base" (与 6 verb `_ready()` super 调用顺序 0 相关, 16 件套仅 verb ability base 字节码 0 
涉及 Pulse 1 verb / Bind 1 verb / Cut 1 verb / Echo 1 verb / Wave 1 verb / Whisper 1 verb 6 verb `_ready()` super 调用顺序 自身 
1:1 严格分离), §9.6.41 关注 "6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约 6 verb 1:1 视觉组连贯" (与 16 件套 verb ability base 0 相关,
 6 verb 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 6 verb = 6 元素 1:1 严格). §9.6.41 与 §9.6.18 是 "姊妹段" (1 套 
polish 模式 × 6 verb = 6 元素 1:1 严格 镜像 1 套 polish 模式 × 16 件套 = 16 元素 1:1 严格, 一是 16 件套 verb ability base, 一是 6 verb 
`_ready()` super 调用顺序 6 verb; 1 套 polish 模式 × 16 件套 = 16 元素 1:1 严格 + 1 套 polish 模式 × 6 verb = 6 元素 1:1 严格). 任何"加新 1 
verb"必须**先**按 §9.6.41 6 verb 1:1 复制 (6 verb 1:1 严格, 0 漏 1 verb 0 改 1 字段 0 反向) **或** 按 §9.6.18 16 件套 1:1 复制 (16 件套 1:1 
严格, 0 漏 1 件套 0 改 1 字段 0 反向), **再**按 §9.6.41 6 verb 1:1 同步 (6 verb 1:1 严格, 0 漏 1 verb 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 
触碰既有 §9.6.18 任何 1 字符) **或** 按 §9.6.18 16 件套 1:1 同步 (16 件套 1:1 严格, 0 漏 1 件套 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 
§9.6.41 任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 
0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 33 轮 0 后续 持续收敛), §9.6.41 是"6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约" (6 verb 1:1 
严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约). T162 与 §9.6.41 0 互混 0 复用 0 共享: T162 关注 "brittle 修复流程 5 步骤" (1 expect 反转 + 1 
docblock 说明 + 1 段 find 反转 + 0 触碰既有 + 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 步骤 1:1 严格), §9.6.41 关注 "6 verb 
1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约" (与 5 步骤 0 相关, 1 套 polish 模式 × 6 verb = 6 元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离). 任何"加新 
1 brittle 修复"必须**先**按 T162 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), **再**按 §9.6.41 
6 verb 1:1 镜像 (6 verb 1:1 严格, 0 漏 1 verb 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.41 6 verb 走 §9.1 9 步落地的 1 步 (Stage 2 ability 子类, 跨 6 verb 各 1 verb), 0 触碰 8 步 
(1 `verb` 枚举 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 
冷光勾边` 渲染). 任何"加新 1 verb"必须**先**按 §9.6.41 6 verb 1:1 镜像 (6 verb 1:1 严格, 0 漏 1 verb 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 
8 步 (`verb` 枚举 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `vfx` 视觉组 + `audio` preset 字段 + `HUD 冷光勾边` 渲染)). 
任何"加新 1 §9.1 步"必须**先**在 §9.6.41 6 verb 加 1 边 (1 Pulse 1 verb entry 或 1 Bind 1 verb 边 或 1 Cut 1 verb 边 或 1 Echo 1 verb 边 
或 1 Wave 1 verb 边 或 1 Whisper 1 verb 边), 0 触碰既有 8 步 (1 `verb` 枚举 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 
`icon` PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).
