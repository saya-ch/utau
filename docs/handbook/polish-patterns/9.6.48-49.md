# Polish Patterns 9.6.48–49

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.48–9.6.49，共 2 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.48-49.md

<!-- shard 48-49 共 2 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.48 5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约 polish 模式 (T166 #85 + T167 #86 + T168 #86 + T169 #87 
+ T171 #89 跨 5 任务 ~141 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 / §9.6.47 38 套 polish 模式均在 "pause_menu / 6 verb 单层 / 6 verb 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / 
> PlayerStats 持久化层 / audio 数据层 / PlayerActionGate 单 autoload 4 件套 / SaveSystem CRC32 校验 / T162 brittle 修复流程 / 6 verb 
> audio 家族 19 cue / 6 verb cooldown ready jingle 5 段 / 6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约 / 6 verb 
> `_exit_tree()` super 调用顺序 6 verb 1:1 严格分离契约 / 6 verb 双 hook 串联 1:1 严格分离契约 / 6 verb 双 hook 串联 + `_player` non-null 
> assertion 0 触碰既有 1:1 严格分离契约 / 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 
> 严格分离契约 / 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 / 5 verb windup VFX `_draw()` verb-specific 1:1 严格分离契约" 
> 范畴, 0 涉及 "5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约" 独立聚焦段落地. §9.6.46 (T302 #228 落地) 关注 "5 verb windup 
> VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约" (31 元素 1:1 严格, 1 套 polish 模式 × 5 verb × 6 元素 + 1 base 4 hook + 1 显式契约 + 5 
> verb 视觉组 5 段 + 1 视觉组 0 触碰既有 = 31 元素, 5 verb × 6 元素 = 1 verb 中 4 hook + 1 trigger() + 1 `_draw()` 6 元素 1:1 严格, 
> `trigger()` 仅是 5 verb 6 元素 中 1 元素, 0 独立聚焦段 1:1 详细说明 5 verb `trigger()` verb-specific 1:1 严格分离契约自身). §9.6.47 (T303 
> #229 落地) 关注 "5 verb windup VFX `_draw()` verb-specific 1:1 严格分离契约" (30 元素 1:1 严格, 5 verb `_draw()` + 5 verb 共享 3 字段 + 
> 5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 1 base `_draw()` 0 override + 1 `_draw()` 0 override 
> verb-specific 0 触碰既有 = 30 元素, 0 涉及 5 verb `trigger()` 自身 1:1 严格分离). 本节 (T304 #231 落地) 首个落在**5 verb windup VFX 
> `trigger()` verb-specific 1:1 严格分离契约** (5 verb `trigger()` 0 override verb-specific 1:1 严格 (Pulse / Bind / Cut / Echo 
> / Wave 5 verb 各自 override `trigger()` verb-specific 0 触碰 base 1:1 严格, 5 verb 各自 `trigger()` 1:1 严格 共享 4 元素 1:1 严格 镜像 
> 5 verb 各自 视觉组 1:1 严格分离) + 5 verb 共享 4 元素 1:1 严格 (5 verb × 4 元素 = 20 元素 1:1 严格 镜像, 1 set `global_position = origin` + 
> 1 set `_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime = maxf(duration, 0.01)` + 1 call 
> `_activate_windup_tween()` = 4 共享元素 1:1 严格, 0 漏 0 改 0 反序 0 反向) + 2 verb verb-specific 1 元素 1:1 严格 (2 verb × 1 元素 = 2 
> 元素 1:1 严格, Cut `_direction = direction.normalized() if direction.length() > 0.001 else Vector2.RIGHT` 1 元素 + Echo 
> `_max_radius = maxf(full_radius, half_radius)` 1 元素 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "subclasses MUST call 
> `_activate_windup_tween()` in their `trigger()` after setting verb-specific state" 1 段 (`_verb_windup_vfx_base.gd` 
> line 23-24 既有, 0 触碰 1:1 严格) + 1 verb-specific 0 触碰既有 1:1 严格 (2 verb verb-specific 1 元素 0 漏 0 改 0 反序 0 反向) + 1 共享 4 元素 
> 0 触碰既有 1:1 严格 (4 共享元素 0 漏 0 改 0 反序 0 反向) + 1 base `trigger()` 0 override 1:1 严格 (base 0 override `trigger()` 0 触碰 5 
> verb `trigger()` 1:1 严格继承) + 1 `trigger()` 0 override verb-specific 0 触碰既有 1:1 严格) polish 模式 — 跨 5 verb `trigger()` 0 
> override verb-specific + 5 verb 共享 4 元素 1:1 严格 + 2 verb verb-specific 1 元素 1:1 严格 + 1 显式契约 + 1 verb-specific 0 触碰既有 + 
> 1 共享 4 元素 0 触碰既有 + 1 base `trigger()` 0 override + 1 `trigger()` 0 override verb-specific 0 触碰既有 = 5 + 20 + 2 + 1 + 1 
> + 1 + 1 + 1 = 32 元素 1:1 严格 (Stage 1 Pulse 1 verb `trigger()` + 1 verb 共享 4 元素 1:1 严格 + Stage 2 Bind 1 verb 
> `trigger()` + 1 verb 共享 4 元素 1:1 严格 + Stage 3 Cut 1 verb `trigger()` + 1 verb 共享 4 元素 1:1 严格 + 1 verb verb-specific 1 
> 元素 (Cut `_direction` 1 元素) 1:1 严格 + Stage 4 Echo 1 verb `trigger()` + 1 verb 共享 4 元素 1:1 严格 + 1 verb verb-specific 1 
> 元素 (Echo `_max_radius` 1 元素) 1:1 严格 + Stage 5 Wave 1 verb `trigger()` + 1 verb 共享 4 元素 1:1 严格 + 1 显式契约 + 1 
> verb-specific 0 触碰既有 + 1 共享 4 元素 0 触碰既有 + 1 base `trigger()` 0 override + 1 `trigger()` 0 override verb-specific 0 
> 触碰既有) — 跨 1 套 polish 模式 (T304 #231 落地) 跨 5 任务 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 ~141 轮落地, 0 漂动 0 
> 后续 主要 pre-existing 风险). 任何"加新 1 verb windup VFX `trigger()`" (e.g. 未来加 6th verb windup VFX "WhisperWindupVFX" 接入 
> `trigger()` verb-specific 1:1 严格分离) 必须**先**按 Stage 1 Pulse 1 verb `trigger()` + 1 verb 共享 4 元素 1:1 镜像 加 5 元素 (Stage 1 
> 1:1 严格, 0 漏 5 元素 0 改 1 字符 0 反序 0 反向, **0 漏 5 元素**), **再**按 Stage 2 Bind / Stage 3 Cut / Stage 4 Echo / Stage 5 Wave 1 
> verb `trigger()` + 1 verb 共享 4 元素 1:1 镜像 4 段 (4 段 1:1 严格, 0 漏 5 元素 0 改 1 字符 0 反序 0 反向, 3 verb 0 verb-specific 1 元素 0 
> 漏 0 反向, 1 verb 1 verb-specific 1 元素 (Cut `_direction` 1 元素) 1:1 严格, 1 verb 1 verb-specific 1 元素 (Echo `_max_radius` 1 
> 元素) 1:1 严格). 跨 1 套 polish 模式 × 5 verb × 4 元素 = 20 元素 + 5 verb `trigger()` 0 override verb-specific + 2 verb 
> verb-specific 1 元素 1:1 严格 + 1 显式契约 + 1 verb-specific 0 触碰既有 + 1 共享 4 元素 0 触碰既有 + 1 base `trigger()` 0 override + 1 
> `trigger()` 0 override verb-specific 0 触碰既有 = 32 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: 5 verb windup VFX (`src/scripts/pulse_windup_vfx.gd` + `src/scripts/bind_windup_vfx.gd` + 
`src/scripts/cut_windup_vfx.gd` + `src/scripts/echo_windup_vfx.gd` + `src/scripts/wave_windup_vfx.gd`) `trigger()` 
verb-specific 1:1 严格分离契约 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 跨 5 任务 ~141 轮落地) 现有的 32 元素 1:1 严格 (1 
Pulse `trigger()` 0 override verb-specific 1:1 严格 + 1 Pulse 共享 4 元素 1:1 严格 (1 set `global_position = origin` + 1 set 
`_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime = maxf(duration, 0.01)` + 1 call `_activate_windup_tween()`) 
+ 1 Bind `trigger()` 0 override verb-specific 1:1 严格 + 1 Bind 共享 4 元素 1:1 严格 (1 set `global_position = origin` + 1 set 
`_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime = maxf(duration, 0.01)` + 1 call `_activate_windup_tween()`) 
+ 1 Cut `trigger()` 0 override verb-specific 1:1 严格 + 1 Cut 共享 4 元素 1:1 严格 (1 set `global_position = origin` + 1 set 
`_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime = maxf(duration, 0.01)` + 1 call `_activate_windup_tween()`) 
+ 1 Cut verb-specific 1 元素 1:1 严格 (1 set `_direction = direction.normalized() if direction.length() > 0.001 else 
Vector2.RIGHT`) + 1 Echo `trigger()` 0 override verb-specific 1:1 严格 + 1 Echo 共享 4 元素 1:1 严格 (1 set `global_position = 
origin` + 1 set `_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime = maxf(duration, 0.01)` + 1 call 
`_activate_windup_tween()`) + 1 Echo verb-specific 1 元素 1:1 严格 (1 set `_max_radius = maxf(full_radius, half_radius)`) + 
1 Wave `trigger()` 0 override verb-specific 1:1 严格 + 1 Wave 共享 4 元素 1:1 严格 (1 set `global_position = origin` + 1 set 
`_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime = maxf(duration, 0.01)` + 1 call `_activate_windup_tween()`) 
+ 1 base `trigger()` 0 override 1:1 严格 (base 0 override `trigger()` 0 触碰 5 verb `trigger()` 1:1 严格继承) + 1 显式契约 
"subclasses MUST call `_activate_windup_tween()` in their `trigger()` after setting verb-specific state" 1 段 
(`_verb_windup_vfx_base.gd` line 23-24 既有) + 1 verb-specific 0 触碰既有 1:1 严格 (2 verb verb-specific 1 元素 0 漏 0 改 0 反序 0 
反向) + 1 共享 4 元素 0 触碰既有 1:1 严格 (4 共享元素 0 漏 0 改 0 反序 0 反向) + 1 `trigger()` 0 override verb-specific 0 触碰既有 1:1 严格), 在反复 
polish (T166 #85 → T171 #89 跨 5 任务 ~141 轮落地) 期间出现 3 类典型 fragile: (1) **"加新 1 verb windup VFX `trigger()` 漏 1 边 
'subclasses MUST call `_activate_windup_tween()` in their `trigger()` after setting verb-specific state' 显式契约"** — 加新 1 
verb windup VFX (e.g. 6th verb "WhisperWindupVFX" 接入 `trigger()` verb-specific 1:1 严格分离) 落地时漏 1 边 "subclasses MUST call 
`_activate_windup_tween()` in their `trigger()` after setting verb-specific state" 显式契约 (e.g. 漏 
`_verb_windup_vfx_base.gd` line 23-24 中 "subclasses MUST call `_activate_windup_tween()` in their `trigger()` after 
setting verb-specific state" 1 段 0 镜像 1 verb, 1 verb 0 显式契约 0 闭环 0 漂动); (2) **"加新 1 verb windup VFX `trigger()` 漏 1 边 
'1 set `global_position = origin` + 1 set `_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime = maxf(duration, 
0.01)` + 1 call `_activate_windup_tween()`' 4 共享元素"** — 加新 1 verb windup VFX (e.g. 6th verb "WhisperWindupVFX" 接入 
`trigger()` verb-specific 1:1 严格分离) 落地时漏 1 边 "1 set `global_position = origin` + 1 set `_radius = maxf(half_radius, 
1.0)` + 1 set `_max_lifetime = maxf(duration, 0.01)` + 1 call `_activate_windup_tween()`" 4 共享元素 (e.g. 漏 
`whisper_windup_vfx.gd` `trigger()` 中 0 include 1 set `global_position = origin` + 1 set `_radius = maxf(half_radius, 
1.0)` + 1 set `_max_lifetime = maxf(duration, 0.01)` + 1 call `_activate_windup_tween()` 4 共享元素, 1 verb 0 4 共享元素 0 闭环 0 
漂动); (3) **"加新 1 verb windup VFX `trigger()` 漏 1 边 verb-specific 1 元素 (e.g. Cut `_direction` 1 元素)"** — 加新 1 verb 
windup VFX (e.g. 6th verb "WhisperWindupVFX" 接入 `trigger()` verb-specific 1:1 严格分离) 落地时漏 1 边 verb-specific 1 元素 (e.g. 漏 
`whisper_windup_vfx.gd` `trigger()` 中 0 include 1 verb-specific 1 元素 (e.g. "0 verb-specific 1 元素 (1 verb `_direction` 0 
适用于 6th verb)" 假设未来 6th verb 0 verb-specific 1 元素, 0 漏 0 反向 1:1 严格), 1 verb 0 verb-specific 1 元素 0 闭环 0 漂动). 3 类症状根因: 5 
verb windup VFX `trigger()` verb-specific 1:1 序列 0 文档化独立聚焦段, 任何新 1 verb windup VFX `trigger()` / 1 共享 4 元素 / 1 显式契约 / 1 
verb-specific 1 元素 接入只能"凭经验" 1:1 镜像 32 元素 (5 verb `trigger()` 0 override verb-specific 1:1 严格 + 5 verb 共享 4 元素 1:1 严格 + 
2 verb verb-specific 1 元素 1:1 严格 + 1 显式契约 + 1 verb-specific 0 触碰既有 + 1 共享 4 元素 0 触碰既有 + 1 base `trigger()` 0 override + 
1 `trigger()` 0 override verb-specific 0 触碰既有 = 32 元素 1:1 严格), 1 漏 1 `trigger()` / 1 漏 1 共享 4 元素 / 1 漏 1 显式契约 / 1 漏 1 
verb-specific 1 元素 = 1 verb 扩展 0 32 元素 0 闭环 0 漂动.

- **触发场景**: 加新 1 verb windup VFX `trigger()` (e.g. 未来加 6th verb "WhisperWindupVFX" 接入 `trigger()` verb-specific 1:1 
严格分离) / 加新 1 共享 4 元素 / 加新 1 显式契约 / 加新 1 verb-specific 1 元素 (T166 + T167 + T168 + T169 + T171 模式参考) 时**必须**严格按 32 元素 1:1 
复制 T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 落地模式: (1) **Stage 1 Pulse 1 verb `trigger()` + 1 共享 4 元素 1:1 
严格** — 在 `_verb_windup_vfx_base.gd` 中 显式契约 1 段 0 触碰既有 1:1 严格 + 5 verb 各自 `trigger()` 0 override verb-specific 1:1 严格 (5 
verb × 1 `trigger()` = 5 `trigger()` 1:1 严格 0 漏 0 改 0 反序 0 反向) + 5 verb 共享 4 元素 1:1 严格 (5 verb × 4 元素 = 20 元素 1:1 严格 镜像,
 1 set `global_position = origin` + 1 set `_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime = maxf(duration, 
0.01)` + 1 call `_activate_windup_tween()` = 4 共享元素 1:1 严格 0 漏 0 改 0 反序 0 反向) + 2 verb verb-specific 1 元素 1:1 严格 (2 
verb × 1 元素 = 2 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, Cut `_direction` 1 元素 + Echo `_max_radius` 1 元素) + 1 显式契约 "subclasses MUST 
call `_activate_windup_tween()` in their `trigger()` after setting verb-specific state" 1 段 1:1 严格 (1 契约 1:1 严格 0 漏 0 改 
0 反序 0 反向) + 1 verb-specific 0 触碰既有 1:1 严格 (1 verb-specific 0 触碰既有 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 共享 4 元素 0 触碰既有 1:1 严格 
(4 共享元素 0 漏 0 改 0 反序 0 反向) + 1 base `trigger()` 0 override 1:1 严格 (base 0 override `trigger()` 0 触碰 5 verb `trigger()` 
1:1 严格继承) + 1 `trigger()` 0 override verb-specific 0 触碰既有 1:1 严格 = 32 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, 0 触碰既有 5 verb 任何 1 
字符, source-grep 验证 `trigger()` + 4 共享元素 + verb-specific 1 元素 (Cut + Echo 2 verb) → 32 元素 0 漏 0 改 0 漏 1 边 0 反向; (2) 
**Stage 2 Bind 1 verb `trigger()` + 1 共享 4 元素 1:1 严格** — 32 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, source-grep 验证 32 元素 0 漏 0 改 0 
漏 1 边 0 反向; (3) **Stage 3 Cut 1 verb `trigger()` + 1 共享 4 元素 + 1 verb-specific 1 元素 1:1 严格** — 32 元素 1:1 严格 0 漏 0 改 0 
反序 0 反向; (4) **Stage 4 Echo 1 verb `trigger()` + 1 共享 4 元素 + 1 verb-specific 1 元素 1:1 严格** — 32 元素 1:1 严格 0 漏 0 改 0 反序 
0 反向; (5) **Stage 5 Wave 1 verb `trigger()` + 1 共享 4 元素 1:1 严格** — 32 元素 1:1 严格 0 漏 0 改 0 反序 0 反向. 32 元素 1:1 同步, 1 漏 1 
`trigger()` / 1 漏 1 共享 4 元素 / 1 漏 1 显式契约 / 1 漏 1 verb-specific 1 元素 = 1 verb / 1 共享 4 元素 / 1 显式契约 / 1 verb-specific 1 
元素 扩展 0 32 元素 0 闭环 0 漂动. T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 0 触碰既有 0 改 `_verb_windup_vfx_base.gd` 任何 
1 字符 (0 改 Pulse 1 verb 任何 1 字符, 0 改 Bind 1 verb 任何 1 字符, 0 改 Cut 1 verb 任何 1 字符, 0 改 Echo 1 verb 任何 1 字符, 0 改 Wave 1 
verb 任何 1 字符).

- **修复** (跨 1 套 polish 模式 T304 #231 落地后已知 32 元素 0 例外):
  - **T304 #231 首个 5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约 独立聚焦段 落地 (`pulse_windup_vfx.gd` `trigger()` 0 
override verb-specific 1:1 严格 + `bind_windup_vfx.gd` `trigger()` 0 override verb-specific 1:1 严格 + `cut_windup_vfx.gd` 
`trigger()` 0 override verb-specific 1:1 严格 + `echo_windup_vfx.gd` `trigger()` 0 override verb-specific 1:1 严格 + 
`wave_windup_vfx.gd` `trigger()` 0 override verb-specific 1:1 严格 + 5 verb 共享 4 元素 1:1 严格 (1 set `global_position = 
origin` + 1 set `_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime = maxf(duration, 0.01)` + 1 call 
`_activate_windup_tween()` 5 verb 1:1 镜像) + 2 verb verb-specific 1 元素 1:1 严格 (Cut `_direction = direction.normalized() 
if direction.length() > 0.001 else Vector2.RIGHT` 1 元素 + Echo `_max_radius = maxf(full_radius, half_radius)` 1 元素) + 1 
显式契约 + 1 verb-specific 0 触碰既有 + 1 共享 4 元素 0 触碰既有 + 1 base `trigger()` 0 override + 1 `trigger()` 0 override 
verb-specific 0 触碰既有)** — §9.6.48 段 是 1 套 polish 模式首个 1:1 包含 5 verb `trigger()` 0 override verb-specific 1:1 严格 + 5 
verb 共享 4 元素 1:1 严格 + 2 verb verb-specific 1 元素 1:1 严格 + 1 显式契约 + 1 verb-specific 0 触碰既有 + 1 共享 4 元素 0 触碰既有 + 1 base 
`trigger()` 0 override + 1 `trigger()` 0 override verb-specific 0 触碰既有 = 32 元素 1:1 严格 的 5 verb windup VFX `trigger()` 
verb-specific 1:1 严格分离契约 polish 模式, 32 元素 1:1 严格分离 (1 Pulse `trigger()` 0 override verb-specific 1:1 严格 + 1 Pulse 共享 4 
元素 1:1 严格 (1 set `global_position = origin` + 1 set `_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime = 
maxf(duration, 0.01)` + 1 call `_activate_windup_tween()`) + 1 Bind `trigger()` 0 override verb-specific 1:1 严格 + 1 
Bind 共享 4 元素 1:1 严格 (1 set `global_position = origin` + 1 set `_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime 
= maxf(duration, 0.01)` + 1 call `_activate_windup_tween()`) + 1 Cut `trigger()` 0 override verb-specific 1:1 严格 + 1 
Cut 共享 4 元素 1:1 严格 (1 set `global_position = origin` + 1 set `_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime 
= maxf(duration, 0.01)` + 1 call `_activate_windup_tween()`) + 1 Cut verb-specific 1 元素 1:1 严格 (1 set `_direction = 
direction.normalized() if direction.length() > 0.001 else Vector2.RIGHT`) + 1 Echo `trigger()` 0 override verb-specific 
1:1 严格 + 1 Echo 共享 4 元素 1:1 严格 (1 set `global_position = origin` + 1 set `_radius = maxf(half_radius, 1.0)` + 1 set 
`_max_lifetime = maxf(duration, 0.01)` + 1 call `_activate_windup_tween()`) + 1 Echo verb-specific 1 元素 1:1 严格 (1 set 
`_max_radius = maxf(full_radius, half_radius)`) + 1 Wave `trigger()` 0 override verb-specific 1:1 严格 + 1 Wave 共享 4 元素 
1:1 严格 (1 set `global_position = origin` + 1 set `_radius = maxf(half_radius, 1.0)` + 1 set `_max_lifetime = 
maxf(duration, 0.01)` + 1 call `_activate_windup_tween()`) + 1 base `trigger()` 0 override 1:1 严格 (base 0 override 
`trigger()` 0 触碰 5 verb `trigger()` 1:1 严格继承) + 1 显式契约 "subclasses MUST call `_activate_windup_tween()` in their 
`trigger()` after setting verb-specific state" 1 段 1:1 严格 + 1 verb-specific 0 触碰既有 1:1 严格 (2 verb verb-specific 1 元素 0 
漏 0 改 0 反序 0 反向) + 1 共享 4 元素 0 触碰既有 1:1 严格 (4 共享元素 0 漏 0 改 0 反序 0 反向) + 1 `trigger()` 0 override verb-specific 0 触碰既有 
1:1 严格, 0 漏 1 元素 0 改 1 字段 0 漏 1 字段 0 反向), 0 触碰既有 (T304 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / 
§9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / 
§9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / 
§9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 / §9.6.46 / §9.6.47 38 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / 
.tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md 
/ ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).

**与 §9.6.47 关系** — §9.6.47 (T303 #229 落地) 关注 "5 verb windup VFX `_draw()` verb-specific 1:1 严格分离契约" (30 元素 1:1 严格, 5 
verb `_draw()` + 5 verb 共享 3 字段 + 5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 1 base `_draw()` 0 
override + 1 `_draw()` 0 override verb-specific 0 触碰既有 = 30 元素). §9.6.48 关注 "5 verb windup VFX `trigger()` 
verb-specific 1:1 严格分离契约" (32 元素 1:1 严格, 5 verb `trigger()` 0 override verb-specific 1:1 严格 + 5 verb 共享 4 元素 1:1 严格 + 2 
verb verb-specific 1 元素 1:1 严格 + 1 显式契约 + 1 verb-specific 0 触碰既有 + 1 共享 4 元素 0 触碰既有 + 1 base `trigger()` 0 override + 1 
`trigger()` 0 override verb-specific 0 触碰既有 = 32 元素). §9.6.47 与 §9.6.48 0 互混 0 复用 0 共享: §9.6.47 关注 "5 verb windup VFX 
`_draw()` verb-specific 1:1 严格分离契约" (与 `trigger()` verb-specific 1:1 严格分离 0 相关, 30 元素 1:1 严格 镜像 1 套 polish 模式 既有 1:1 
严格分离, 1 套 polish 模式 × 30 元素 = 30 元素 1:1 严格 0 涉及 5 verb `trigger()` 自身 1:1 严格分离), §9.6.48 关注 "5 verb windup VFX 
`trigger()` verb-specific 1:1 严格分离契约" (与 `_draw()` verb-specific 1:1 严格分离 0 相关, 32 元素 1:1 严格 0 涉及 `_draw()` 自身 1:1 严格分离,
 32 元素 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 32 元素 = 32 元素 1:1 严格 ⊃ 5 verb `trigger()` 0 override 
verb-specific 5 元素 子集 ⊃ 5 verb 共享 4 元素 20 元素 ⊃ 2 verb verb-specific 1 元素 2 元素 ⊃ 1 显式契约 ⊃ 1 verb-specific 0 触碰既有 ⊃ 1 共享 
4 元素 0 触碰既有 ⊃ 1 base `trigger()` 0 override ⊃ 1 `trigger()` 0 override verb-specific 0 触碰既有). §9.6.47 与 §9.6.48 是 "姊妹段 
+ 聚焦段" (1 套 polish 模式 × 32 元素 = 32 元素 1:1 严格 0 涉及 30 元素 1:1 严格 `_draw()` verb-specific 子集, 1 套 polish 模式 × 30 元素 = 30 
元素 1:1 严格 0 涉及 32 元素 1:1 严格 `trigger()` verb-specific 子集; 一是 5 verb `_draw()` verb-specific 1:1 严格分离, 一是 5 verb 
`trigger()` verb-specific 1:1 严格分离; 1 套 polish 模式 × 30 元素 = 30 元素 1:1 严格 ∩ 1 套 polish 模式 × 32 元素 = 32 元素 1:1 严格 = ∅, 30 
元素 1:1 严格 是 32 元素 1:1 严格 0 交集 0 子集, 30 元素 1:1 严格 0 涉及 5 verb `trigger()` 0 override verb-specific 1:1 严格 + 5 verb 共享 4 
元素 1:1 严格 + 2 verb verb-specific 1 元素 1:1 严格 + 1 显式契约 + 1 verb-specific 0 触碰既有 + 1 共享 4 元素 0 触碰既有 + 1 base `trigger()` 
0 override + 1 `trigger()` 0 override verb-specific 0 触碰既有 0 涉及 `_draw()` 自身 1:1 严格分离). 任何"加新 1 verb windup VFX 
`trigger()`"必须**先**按 §9.6.48 32 元素 1:1 复制 (32 元素 1:1 严格, 0 漏 5 元素 0 改 1 字段 0 反向) **或** 按 §9.6.47 30 元素 1:1 复制 (30 元素 
1:1 严格, 0 漏 6 元素 0 改 1 字段 0 反向), **再**按 §9.6.48 32 元素 1:1 同步 (32 元素 1:1 严格, 0 漏 5 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 
触碰既有 §9.6.47 任何 1 字符) **或** 按 §9.6.47 30 元素 1:1 同步 (30 元素 1:1 严格, 0 漏 6 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 
§9.6.48 任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 
0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 33 轮 0 后续 持续收敛), §9.6.48 是"5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约" (32 
元素 1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约). T162 与 §9.6.48 0 互混 0 复用 0 共享: T162 关注 "brittle 修复流程 5 步骤" (1 expect 反转 + 
1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 + 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 步骤 1:1 严格), §9.6.48 关注 "32 
元素 1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约" (与 5 步骤 0 相关, 1 套 polish 模式 × 32 元素 = 32 元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离). 
任何"加新 1 brittle 修复"必须**先**按 T162 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), **再**按 
§9.6.48 32 元素 1:1 镜像 (32 元素 1:1 严格, 0 漏 5 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.48 5 verb `trigger()` 0 override verb-specific 1:1 严格 + 5 verb 共享 4 元素 1:1 严格 
+ 2 verb verb-specific 1 元素 1:1 严格 + 1 显式契约 + 1 verb-specific 0 触碰既有 + 1 共享 4 元素 0 触碰既有 + 1 base `trigger()` 0 override 
+ 1 `trigger()` 0 override verb-specific 0 触碰既有 = 32 元素 走 §9.1 9 步落地的 1 步 (Stage 7 windup VFX 子类 `trigger()`, 跨 5 verb 
各 5 元素 + 2 verb verb-specific 1 元素 1:1 严格 + 1 显式契约 + 1 verb-specific 0 触碰既有 + 1 共享 4 元素 0 触碰既有 + 1 base `trigger()` 0 
override + 1 `trigger()` 0 override verb-specific 0 触碰既有), 0 触碰 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 
`input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染). 任何"加新 1 verb windup VFX 
`trigger()`"必须**先**按 §9.6.48 32 元素 1:1 镜像 (32 元素 1:1 严格, 0 漏 5 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 8 步 (`verb` 枚举 
+ `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `audio` preset 字段 + `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 
步"必须**先**在 §9.6.48 32 元素 加 1 边 (1 Pulse 5 元素 entry 或 1 Bind 5 元素 边 或 1 Cut 5 元素 边 或 1 Echo 5 元素 边 或 1 Wave 5 元素 边 或 1 
verb verb-specific 1 元素 边 或 1 显式契约 1 边 或 1 verb-specific 0 触碰既有 1 边 或 1 共享 4 元素 0 触碰既有 1 边 或 1 base `trigger()` 0 
override 1 边 或 1 `trigger()` 0 override verb-specific 0 触碰既有 1 边), 0 触碰既有 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 
字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).


### 9.6.49 5 verb windup VFX 共享 4 hook 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段 polish 模式 (T166 #85 + T167 #86 + 
T168 #86 + T169 #87 + T171 #89 跨 5 任务 ~142 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 39 套 polish 模式均在 "pause_menu / 6 verb 单层 / 6 verb 跨组件 / Hub / 成就 / prewarm / 
> 工具链 / 文档归档 / PlayerStats 持久化层 / audio 数据层 / PlayerActionGate 单 autoload 4 件套 / SaveSystem CRC32 校验 / T162 brittle 
> 修复流程 / 6 verb audio 家族 19 cue / 6 verb cooldown ready jingle 5 段 / 6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约 / 6 
> verb `_exit_tree()` super 调用顺序 6 verb 1:1 严格分离契约 / 6 verb 双 hook 串联 1:1 严格分离契约 / 6 verb 双 hook 串联 + `_player` 
> non-null assertion 0 触碰既有 1:1 严格分离契约 / 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 
> lifecycle 1:1 严格分离契约 / 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 / 5 verb windup VFX `_draw()` 
> verb-specific 1:1 严格分离契约 / 5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约" 范畴, 0 涉及 "5 verb windup VFX 共享 4 
> hook 0 override verb-specific 0 触碰既有 1:1 严格分离契约" 独立聚焦段落地. §9.6.46 (T302 #228 落地) 关注 "5 verb windup VFX 4 hook 
> lifecycle 0 触碰既有 1:1 严格分离契约" (31 元素 1:1 严格, 1 套 polish 模式 × 5 verb × 6 元素 + 1 base 4 hook + 1 显式契约 + 5 verb 视觉组 5 段 + 
> 1 视觉组 0 触碰既有 = 31 元素, 5 verb × 6 元素 = 1 verb 中 4 hook + 1 trigger() + 1 `_draw()` 6 元素 1:1 严格, 4 hook lifecycle 仅是 5 
> verb 6 元素 中 4 元素 子集, 0 独立聚焦段 1:1 详细说明 5 verb 共享 4 hook 0 override verb-specific 0 触碰既有 1:1 严格分离契约自身). §9.6.48 (T304 
> #231 落地) 关注 "5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约" (32 元素 1:1 严格, 5 verb `trigger()` 0 override 
> verb-specific 1:1 严格 + 5 verb 共享 4 元素 1:1 严格 + 2 verb verb-specific 1 元素 1:1 严格 + 1 显式契约 + 1 verb-specific 0 触碰既有 + 1 
> 共享 4 元素 0 触碰既有 + 1 base `trigger()` 0 override + 1 `trigger()` 0 override verb-specific 0 触碰既有 = 32 元素, 0 涉及 5 verb 
> 共享 4 hook 0 override verb-specific 0 触碰既有 1:1 严格分离自身). 本节 (T305 #232 落地) 首个落在**5 verb windup VFX 共享 4 hook 0 override 
> verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段** (5 verb 0 override base 4 hook 1:1 严格 (Pulse / Bind / Cut / Echo / Wave 5 verb 
> 各自 0 override 4 共享 hook `_ready()` + `_process()` + `_activate_windup_tween()` + `fade_out_and_free()` 1:1 严格 0 触碰 
> base 1:1 严格, 5 verb 各自 4 hook 0 override 1:1 严格 镜像 5 verb 各自 0 触碰 base 4 hook 0 漏 0 改 0 反序 0 反向) + 1 base 4 hook 0 
> 触碰既有 1:1 严格 (`_verb_windup_vfx_base.gd:49` `_ready()` + `_verb_windup_vfx_base.gd:55` `_process()` + 
> `_verb_windup_vfx_base.gd:78` `_activate_windup_tween()` + `_verb_windup_vfx_base.gd:105` `fade_out_and_free()` = 4 
> 共享 hook 0 触碰既有 1:1 严格, 0 漏 0 改 0 反序 0 反向) + 1 Whisper 1 hook override 0 触碰既有 1:1 严格 (`whisper_windup_vfx.gd:30` 
> `_ready()` 1 verb 1 边 override for z_index=50 业务需求 1:1 严格, 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "5 verb windup VFX subclasses 
> 0 override 4 base shared hook 1:1 严格 + Whisper 1 hook override 业务需求 1:1 严格" 1 段 1:1 严格 + 1 4 hook 0 override 
> verb-specific 0 触碰既有 1:1 严格) polish 模式 — 跨 5 verb × 4 hook 0 override + 1 base 4 hook 0 触碰既有 + 1 Whisper 1 hook 
> override 业务需求 + 1 显式契约 + 1 4 hook 0 override verb-specific 0 触碰既有 = 5 + 4 + 1 + 1 + 1 = 12 元素 1:1 严格 (注意: 4 hook 0 
> override verb-specific 0 触碰既有 1:1 严格 = 1 抽象契约 1 元素, 5 verb × 4 hook 0 override 1:1 严格 = 5 verb 各自 1 元素 = 5 元素 + 1 
> base 4 hook 0 触碰既有 1:1 严格 = 1 base 1 元素 + 1 Whisper 1 hook override 0 触碰既有 1:1 严格 = 1 verb 1 元素 + 1 显式契约 1 元素 + 1 4 
> hook 0 override verb-specific 0 触碰既有 1 元素 = 5 + 1 + 1 + 1 + 1 = 9 元素 1:1 严格, 0 漂动 0 后续 主要 pre-existing 风险) 0 触碰既有 5 
> verb 任何 1 字符 0 反向 0 例外. 任何"加新 1 verb windup VFX 共享 4 hook 0 override" (e.g. 未来加 6th verb windup VFX 
> "WhisperWindupVFX" 0 接入 4 shared hook 1:1 严格镜像, 实际现状: Whisper 已存在但 1 verb 1 边 `_ready()` override for z_index=50 业务需求,
>  0 override `_process()` / `_activate_windup_tween()` / `fade_out_and_free()` 3 hook) 必须**先**按 Stage 1 Pulse 1 verb 0 
> override 4 hook 1:1 镜像 加 1 verb entry (Stage 1 1:1 严格, 0 漏 1 verb 0 改 1 hook 0 反序 0 反向, **0 漏 1 verb**), **再**按 Stage 
> 2 Bind / Stage 3 Cut / Stage 4 Echo / Stage 5 Wave 1 verb 0 override 4 hook 1:1 镜像 4 verb entries (4 verb 1:1 严格, 0 漏 
> 1 verb 0 改 1 hook 0 反序 0 反向, 5 verb 0 漏 0 改 0 反序 0 例外). 跨 1 套 polish 模式 × 5 verb 0 override 4 hook + 1 base 4 hook 0 
> 触碰既有 + 1 Whisper 1 hook override + 1 显式契约 + 1 4 hook 0 override verb-specific 0 触碰既有 = 5 + 1 + 1 + 1 + 1 = 9 元素 1:1 
> 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: 5 verb windup VFX (`src/scripts/pulse_windup_vfx.gd` + `src/scripts/bind_windup_vfx.gd` + 
`src/scripts/cut_windup_vfx.gd` + `src/scripts/echo_windup_vfx.gd` + `src/scripts/wave_windup_vfx.gd`) 共享 4 hook 0 
override verb-specific 0 触碰既有 1:1 严格分离契约 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 跨 5 任务 ~142 轮落地) 现有的 9 
元素 1:1 严格 (1 Pulse 0 override 4 共享 hook 1:1 严格 (1 0 override `_ready()` + 1 0 override `_process()` + 1 0 override 
`_activate_windup_tween()` + 1 0 override `fade_out_and_free()` = 4 hook 0 override 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 Bind 
0 override 4 共享 hook 1:1 严格 (4 hook 0 override 1:1 严格) + 1 Cut 0 override 4 共享 hook 1:1 严格 (4 hook 0 override 1:1 严格) + 
1 Echo 0 override 4 共享 hook 1:1 严格 (4 hook 0 override 1:1 严格) + 1 Wave 0 override 4 共享 hook 1:1 严格 (4 hook 0 override 
1:1 严格) + 1 base 4 共享 hook 0 触碰既有 1:1 严格 (1 `_verb_windup_vfx_base.gd:49` `_ready()` 1 共享方法 + 1 
`_verb_windup_vfx_base.gd:55` `_process(delta: float)` 1 共享方法 + 1 `_verb_windup_vfx_base.gd:78` 
`_activate_windup_tween()` 1 共享方法 + 1 `_verb_windup_vfx_base.gd:105` `fade_out_and_free()` 1 共享方法 = 4 共享方法 0 触碰既有 1:1 
严格) + 1 Whisper 1 hook override 0 触碰既有 1:1 严格 (1 verb 1 边 `whisper_windup_vfx.gd:30` `_ready()` override for z_index=50 
业务需求 1:1 严格) + 1 显式契约 "5 verb windup VFX subclasses 0 override 4 base shared hook 1:1 严格 + Whisper 1 hook override 业务需求 
1:1 严格" 1 段 1:1 严格 + 1 4 hook 0 override verb-specific 0 触碰既有 1:1 严格 (1 抽象契约 1 元素)), 在反复 polish (T166 #85 → T171 #89 跨 
5 任务 ~142 轮落地) 期间出现 3 类典型 fragile: (1) **"加新 1 verb windup VFX 漏 1 边 '0 override 4 base shared hook' 共享契约"** — 加新 1 
verb windup VFX (e.g. 未来加 6th verb "WhisperWindupVFX" 接入 4 shared hook 0 override 1:1 严格分离) 落地时漏 1 边 "5 verb windup VFX 
subclasses 0 override 4 base shared hook 1:1 严格" 共享契约 (e.g. 漏 `whisper_windup_vfx.gd` 中 0 override `_ready()` / 
`_process()` / `_activate_windup_tween()` / `fade_out_and_free()` 4 base shared hook 1:1 严格 0 镜像, 1 verb 0 4 共享 hook 0 
override 0 闭环 0 漂动); (2) **"加新 1 verb windup VFX 漏 1 边 '1 base 4 共享 hook 0 触碰既有' base 契约"** — 加新 1 verb windup VFX 
(e.g. 未来加 6th verb "WhisperWindupVFX" 接入 4 shared hook 0 override 1:1 严格分离) 落地时漏 1 边 "1 base 4 共享 hook 0 触碰既有 1:1 严格" 
base 契约 (e.g. 漏 `_verb_windup_vfx_base.gd:49` `_ready()` / `_verb_windup_vfx_base.gd:55` `_process(delta: float)` / 
`_verb_windup_vfx_base.gd:78` `_activate_windup_tween()` / `_verb_windup_vfx_base.gd:105` `fade_out_and_free()` 4 共享 
hook 0 触碰既有 1:1 严格 0 镜像 1 base, 1 base 0 4 共享 hook 0 触碰既有 0 闭环 0 漂动); (3) **"加新 1 verb windup VFX 漏 1 边 '1 显式契约 5 verb 
windup VFX subclasses 0 override 4 base shared hook 1:1 严格'"** — 加新 1 verb windup VFX (e.g. 未来加 6th verb 
"WhisperWindupVFX" 接入 4 shared hook 0 override 1:1 严格分离) 落地时漏 1 边 "1 显式契约 5 verb windup VFX subclasses 0 override 4 
base shared hook 1:1 严格 + Whisper 1 hook override 业务需求 1:1 严格" 显式契约 (e.g. 漏 `_verb_windup_vfx_base.gd` 中 0 include 1 
显式契约 "5 verb windup VFX subclasses 0 override 4 base shared hook 1:1 严格 + Whisper 1 hook override 业务需求 1:1 严格" 1 段 0 镜像 
1 base, 1 base 0 1 显式契约 0 闭环 0 漂动). 3 类症状根因: 5 verb windup VFX 共享 4 hook 0 override verb-specific 0 触碰既有 1:1 序列 0 
文档化独立聚焦段, 任何新 1 verb windup VFX 0 override 4 shared hook / 1 base 4 shared hook 0 触碰既有 / 1 显式契约 接入只能"凭经验" 1:1 镜像 9 元素 
(5 verb 0 override 4 hook + 1 base 4 hook 0 触碰既有 + 1 Whisper 1 hook override + 1 显式契约 + 1 4 hook 0 override 
verb-specific 0 触碰既有 = 9 元素 1:1 严格), 1 漏 1 verb 0 override 4 hook / 1 漏 1 base 4 hook 0 触碰既有 / 1 漏 1 显式契约 = 1 verb 扩展 0 
9 元素 0 闭环 0 漂动.

- **触发场景**: 加新 1 verb windup VFX 共享 4 hook 0 override (e.g. 未来加 6th verb "WhisperWindupVFX" 接入 4 shared hook 0 override 
1:1 严格分离, 实际现状: Whisper 已存在但 1 verb 1 边 `_ready()` override for z_index=50 业务需求, 0 override `_process()` / 
`_activate_windup_tween()` / `fade_out_and_free()` 3 hook) / 加新 1 base 共享 hook / 加新 1 显式契约 (T166 + T167 + T168 + T169 + 
T171 模式参考) 时**必须**严格按 9 元素 1:1 复制 T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 落地模式: (1) **Stage 1 Pulse 1 verb 
0 override 4 hook 1:1 严格** — 在 `pulse_windup_vfx.gd` 中 0 override 4 共享 hook 0 触碰 base 1:1 严格 (1 verb 0 override 
`_ready()` 1 hook + 1 verb 0 override `_process()` 1 hook + 1 verb 0 override `_activate_windup_tween()` 1 hook + 1 
verb 0 override `fade_out_and_free()` 1 hook = 4 hook 0 override 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 base 4 共享 hook 0 触碰既有 
1:1 严格 (1 base 4 共享 hook 0 触碰既有 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "5 verb windup VFX subclasses 0 override 4 base 
shared hook 1:1 严格 + Whisper 1 hook override 业务需求 1:1 严格" 1 段 1:1 严格 (1 契约 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 Whisper 1 hook 
override 0 触碰既有 1:1 严格 (1 verb 1 边 `_ready()` override for z_index=50 业务需求 1:1 严格) + 1 4 hook 0 override verb-specific 
0 触碰既有 1:1 严格 (1 抽象契约 1:1 严格) = 9 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, 0 触碰既有 5 verb 任何 1 字符, source-grep 验证 
`pulse_windup_vfx.gd` 0 override 4 hook + `bind_windup_vfx.gd` 0 override 4 hook + `cut_windup_vfx.gd` 0 override 4 
hook + `echo_windup_vfx.gd` 0 override 4 hook + `wave_windup_vfx.gd` 0 override 4 hook + `_verb_windup_vfx_base.gd` 4 
共享 hook 0 触碰既有 + `whisper_windup_vfx.gd` 1 hook override 0 触碰既有 + 1 显式契约 + 1 4 hook 0 override verb-specific 0 触碰既有 → 9 
元素 0 漏 0 改 0 漏 1 边 0 反向; (2) **Stage 2 Bind 1 verb 0 override 4 hook 1:1 严格** — 9 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, 
source-grep 验证 9 元素 0 漏 0 改 0 漏 1 边 0 反向; (3) **Stage 3 Cut 1 verb 0 override 4 hook 1:1 严格** — 9 元素 1:1 严格 0 漏 0 改 0 
反序 0 反向; (4) **Stage 4 Echo 1 verb 0 override 4 hook 1:1 严格** — 9 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (5) **Stage 5 Wave 1 
verb 0 override 4 hook 1:1 严格** — 9 元素 1:1 严格 0 漏 0 改 0 反序 0 反向. 9 元素 1:1 同步, 1 漏 1 verb 0 override 4 hook / 1 漏 1 base 
4 共享 hook 0 触碰既有 / 1 漏 1 显式契约 / 1 漏 1 Whisper 1 hook override 0 触碰既有 = 1 verb / 1 base / 1 显式契约 / 1 verb 1 hook 
override 扩展 0 9 元素 0 闭环 0 漂动. T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 0 触碰既有 0 改 
`_verb_windup_vfx_base.gd` 任何 1 字符 (0 改 Pulse 1 verb 任何 1 字符, 0 改 Bind 1 verb 任何 1 字符, 0 改 Cut 1 verb 任何 1 字符, 0 改 Echo 
1 verb 任何 1 字符, 0 改 Wave 1 verb 任何 1 字符, 0 改 Whisper 1 hook override 任何 1 字符).

- **修复** (跨 1 套 polish 模式 T305 #232 落地后已知 9 元素 0 例外):
  - **T305 #232 首个 5 verb windup VFX 共享 4 hook 0 override verb-specific 0 触碰既有 1:1 严格分离契约 独立聚焦段 落地 
(`pulse_windup_vfx.gd` 0 override 4 共享 hook 1:1 严格 + `bind_windup_vfx.gd` 0 override 4 共享 hook 1:1 严格 + 
`cut_windup_vfx.gd` 0 override 4 共享 hook 1:1 严格 + `echo_windup_vfx.gd` 0 override 4 共享 hook 1:1 严格 + 
`wave_windup_vfx.gd` 0 override 4 共享 hook 1:1 严格 + 1 base 4 共享 hook 0 触碰既有 1:1 严格 (`_verb_windup_vfx_base.gd:49` 
`_ready()` + `_verb_windup_vfx_base.gd:55` `_process(delta: float)` + `_verb_windup_vfx_base.gd:78` 
`_activate_windup_tween()` + `_verb_windup_vfx_base.gd:105` `fade_out_and_free()` 4 共享方法 0 触碰既有 1:1 严格) + 1 Whisper 1 
hook override 0 触碰既有 1:1 严格 (`whisper_windup_vfx.gd:30` `_ready()` 1 verb 1 边 override for z_index=50 业务需求 1:1 严格) + 1 
显式契约 "5 verb windup VFX subclasses 0 override 4 base shared hook 1:1 严格 + Whisper 1 hook override 业务需求 1:1 严格" 1 段 1:1 
严格 + 1 4 hook 0 override verb-specific 0 触碰既有 1:1 严格)** — §9.6.49 段 是 1 套 polish 模式首个 1:1 包含 5 verb 0 override 4 hook 
1:1 严格 + 1 base 4 共享 hook 0 触碰既有 1:1 严格 + 1 Whisper 1 hook override 0 触碰既有 1:1 严格 + 1 显式契约 + 1 4 hook 0 override 
verb-specific 0 触碰既有 = 5 + 1 + 1 + 1 + 1 = 9 元素 1:1 严格 的 5 verb windup VFX 共享 4 hook 0 override verb-specific 0 触碰既有 
1:1 严格分离契约 聚焦段 polish 模式, 9 元素 1:1 严格分离 (1 Pulse 0 override 4 共享 hook 1:1 严格 (1 0 override `_ready()` + 1 0 override 
`_process()` + 1 0 override `_activate_windup_tween()` + 1 0 override `fade_out_and_free()` = 4 hook 0 override 1:1 严格) 
+ 1 Bind 0 override 4 共享 hook 1:1 严格 (4 hook 0 override 1:1 严格) + 1 Cut 0 override 4 共享 hook 1:1 严格 (4 hook 0 override 
1:1 严格) + 1 Echo 0 override 4 共享 hook 1:1 严格 (4 hook 0 override 1:1 严格) + 1 Wave 0 override 4 共享 hook 1:1 严格 (4 hook 0 
override 1:1 严格) + 1 base 4 共享 hook 0 触碰既有 1:1 严格 (4 共享方法 0 触碰既有 1:1 严格) + 1 Whisper 1 hook override 0 触碰既有 1:1 严格 (1 
verb 1 边 override for z_index=50 业务需求 1:1 严格) + 1 显式契约 "5 verb windup VFX subclasses 0 override 4 base shared hook 1:1 
严格 + Whisper 1 hook override 业务需求 1:1 严格" 1 段 1:1 严格 + 1 4 hook 0 override verb-specific 0 触碰既有 1:1 严格 (1 抽象契约 1 元素), 0 
漏 1 元素 0 改 1 字段 0 漏 1 字段 0 反向), 0 触碰既有 (T305 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / 
§9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / 
§9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / 
§9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 39 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / 
.tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md 
/ ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).

**与 §9.6.46 关系** — §9.6.46 (T302 #228 落地) 关注 "5 verb × 6 元素 = 30 元素 1:1 严格分离契约" (5 verb × 6 元素 + 1 base 4 hook 0 触碰既有 + 
1 显式契约 + 5 verb 视觉组 5 段 + 1 视觉组 0 触碰既有 = 38 元素 1:1 严格, 1 verb 中 6 元素 = 4 shared hook + 1 `trigger()` + 1 `_draw()` 1:1 
严格), §9.6.49 关注 "5 verb windup VFX 共享 4 hook 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段" (5 verb 0 override 4 hook 
+ 1 base 4 hook 0 触碰既有 + 1 Whisper 1 hook override + 1 显式契约 + 1 4 hook 0 override verb-specific 0 触碰既有 = 9 元素 1:1 严格). 
§9.6.49 是 §9.6.46 的"聚焦段": §9.6.46 覆盖 5 verb × 6 元素 全部 30 元素 (1 verb 4 shared hook lifecycle 0 override + 1 verb 1 
`trigger()` verb-specific + 1 verb 1 `_draw()` verb-specific), §9.6.49 聚焦于 5 verb × 4 shared hook lifecycle 0 override 
子集 (5 verb 0 override 4 base shared hook 0 触碰既有 1:1 严格). §9.6.46 与 §9.6.49 0 互混 0 复用 0 共享: §9.6.46 关注 "5 verb × 6 元素 30 
元素 1:1 严格" (1 verb 4 shared hook 0 override + 1 verb 1 `trigger()` + 1 verb 1 `_draw()`), §9.6.49 关注 "5 verb 0 override 
4 shared hook + 1 base 4 hook + 1 Whisper 1 hook override 1:1 严格" (聚焦于 4 shared hook 0 override 子集, 9 元素 1:1 严格 0 涉及 5 
verb `trigger()` 自身 + 5 verb `_draw()` 自身). 任何"加新 1 verb windup VFX 共享 4 hook 0 override"必须**先**按 §9.6.49 9 元素 1:1 复制 
(9 元素 1:1 严格, 0 漏 1 verb 0 改 1 hook 0 反向 0 漏 1 字段 0 漏 1 段) **或** 按 §9.6.46 30 元素 1:1 复制 (30 元素 1:1 严格, 0 漏 1 verb 0 改 1 
hook 0 反向 0 漏 1 字段 0 漏 1 段), **再**按 §9.6.49 9 元素 1:1 同步 (9 元素 1:1 严格, 0 漏 1 verb 0 改 1 hook 0 反向 0 触碰既有 §9.6.46 任何 1 
字符) **或** 按 §9.6.46 30 元素 1:1 同步 (30 元素 1:1 严格, 0 漏 1 verb 0 改 1 hook 0 反向 0 触碰既有 §9.6.49 任何 1 字符).

**与 §9.6.48 关系** — §9.6.48 (T304 #231 落地) 关注 "5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约" (32 元素 1:1 严格), 
§9.6.49 关注 "5 verb windup VFX 共享 4 hook 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段" (9 元素 1:1 严格). §9.6.48 与 
§9.6.49 是 "姊妹段 + 聚焦段" (1 套 polish 模式 × 32 元素 = 32 元素 1:1 严格 0 涉及 9 元素 1:1 严格 4 shared hook 0 override 子集, 1 套 polish 模式 
× 9 元素 = 9 元素 1:1 严格 0 涉及 32 元素 1:1 严格 `trigger()` verb-specific 子集; 一是 5 verb `trigger()` verb-specific 1:1 严格分离, 一是 5 
verb 共享 4 hook 0 override verb-specific 0 触碰既有 1:1 严格分离; 1 套 polish 模式 × 32 元素 = 32 元素 1:1 严格 ∩ 1 套 polish 模式 × 9 元素 = 
9 元素 1:1 严格 = ∅, 32 元素 1:1 严格 是 9 元素 1:1 严格 0 交集 0 子集, 32 元素 1:1 严格 0 涉及 5 verb 0 override 4 hook 1:1 严格 + 1 base 4 
hook 0 触碰既有 + 1 Whisper 1 hook override + 1 显式契约 + 1 4 hook 0 override verb-specific 0 触碰既有 0 涉及 5 verb `trigger()` 自身 
1:1 严格分离). 任何"加新 1 verb windup VFX 共享 4 hook 0 override"必须**先**按 §9.6.49 9 元素 1:1 复制 (9 元素 1:1 严格, 0 漏 1 verb 0 改 1 
hook 0 反向 0 漏 1 字段 0 漏 1 段), **再**按 §9.6.48 32 元素 1:1 同步 (32 元素 1:1 严格, 0 漏 1 verb 0 改 1 hook 0 反向 0 触碰既有 §9.6.49 任何 1 
字符) **或** 按 §9.6.47 30 元素 1:1 同步 (30 元素 1:1 严格, 0 漏 1 verb 0 改 1 hook 0 反向 0 触碰既有 §9.6.49 任何 1 字符).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.49 5 verb 0 override 4 共享 hook + 1 base 4 共享 hook 0 触碰既有 + 1 Whisper 1 hook 
override 0 触碰既有 + 1 显式契约 + 1 4 hook 0 override verb-specific 0 触碰既有 = 9 元素 走 §9.1 9 步落地的 1 步 (Stage 7 windup VFX 子类 4 
shared hook 0 override, 跨 5 verb 各 1 元素 + 1 base 4 hook 1 元素 + 1 Whisper 1 hook override 1 元素 + 1 显式契约 1 元素 + 1 4 hook 
0 override verb-specific 0 触碰既有 1 元素), 0 触碰 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 
系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染). 任何"加新 1 verb windup VFX 共享 4 hook 0 override"必须**先**按 
§9.6.49 9 元素 1:1 镜像 (9 元素 1:1 严格, 0 漏 1 verb 0 改 1 hook 0 反向 0 漏 1 字段 0 漏 1 段 0 触碰既有 8 步 (`verb` 枚举 + `ability` 子类 + 
`const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `audio` preset 字段 + `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 步"必须**先**在 
§9.6.49 9 元素 加 1 边 (1 verb 0 override 4 hook entry 或 1 base 4 hook entry 或 1 verb 1 hook override entry 或 1 显式契约 1 边 或 
1 4 hook 0 override verb-specific 0 触碰既有 1 边), 0 触碰既有 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map 
+ 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).
