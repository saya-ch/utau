# Polish Patterns 9.6.34–34

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.34–9.6.34，共 1 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.34-34.md

<!-- shard 34-34 共 1 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.34 PlayerStats `_best_stats` + `_run_history` 字段扩展 6 段 canonical 1:1 严格分离契约 polish 模式 (T127 #67 + T131 #83 + 
T150 #100 + T167 #115 + T209 #128 + F018.1 #130 跨 6 任务 ~140 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> 24 套 polish 模式均在 "pause_menu / 6 verb / Hub / 成就 / prewarm / 工具链 / 文档归档" 范畴, 0 涉及 "持久化层 (`_best_stats` 字典 + 
> `_run_history` 数组) 字段扩展" 落地. §9.6.27 (T283 #198 落地) 覆盖 PlayerProfilePanel 1 panel × 3 组件 (ProfileQuickStats + 
> ProfileRecentList + ProfileAudit) × 1:1 视觉组连贯 5 段, 但**未**覆盖"数据层 (PlayerStats 持久化) 加新 1 字段" 必须同时 1:1 镜像 6 段 (Stage 1 
> dict default + Stage 2 单调更新 + Stage 3 snapshot + Stage 4 UI 显示 + Stage 5 accessor docblock + Stage 6 老存档兼容) 的契约. 本节 
> (T290 #213 落地) 首个落在**持久化层字段扩展 6 段 1:1 严格分离契约** (`_best_stats` dict 5 字段 + `_run_history` snapshot 7 字段 + UI 4 段 + 
> accessor 1 行 + 老存档 fallback 0.0) polish 模式 — 跨 6 段 (Stage 1 dict default 1:1 严格 0 漏 1 字段 + Stage 2 单调更新 1:1 严格 0 漏 1 
> 比较 + Stage 3 snapshot 1:1 严格 0 漏 1 字段 + Stage 4 UI 显示 1:1 严格 0 漏 1 段 + Stage 5 accessor docblock 1:1 严格 0 漏 1 字段说明 + 
> Stage 6 老存档兼容 1:1 严格 0 漏 1 fallback) — 跨 1 套 polish 模式 (T290 #213 落地) 跨 0 任务 (~213 轮落地, 0 漂动 0 后续 主要 pre-existing 
> 风险). 任何"加新 1 best 字段"或"加新 1 history 字段"或"加新 1 派生 UI 字段"必须**先**按 Stage 1 dict default 1:1 严格 加 1 entry (0 漏 1 字段 0 改 1 
> 字段 0 反序 0 反向), **再**按 Stage 2 单调更新 1:1 镜像 (1:1 严格, 0 漏 1 比较 0 改 1 比较 0 反序 0 反向), **再**按 Stage 3 snapshot 1:1 镜像 (1:1 
> 严格, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), **再**按 Stage 4 UI 显示 1:1 镜像 (1:1 严格, 0 漏 1 段 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), **再**按 
> Stage 5 accessor docblock 1:1 镜像 (1:1 严格, 0 漏 1 字段说明 0 改 1 字符 0 反向), **再**按 Stage 6 老存档兼容 1:1 镜像 (1:1 严格, 0 漏 1 
> fallback 0 改 1 fallback 0 反向). 跨 1 套 polish 模式 × 6 段 = 6 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: PlayerStats `_best_stats` 字典 (从 1 字段 `longest_run_seconds` T127 #67 → 2 字段 + `most_rooms_cleared` T150 #100 → 
3 字段 + `most_shards_collected` T150 #100 → 4 字段 + `most_enemies_purified` T167 #115 → 5 字段 + `longest_room_seconds` 
T209 #128) + `_run_history` snapshot (从 0 字段 T127 → 6 字段 T131 #83 → 7 字段 T209 #128) + PlayerProfilePanel UI 
(ProfileQuickStats 1 段 → 2 段 → 3 段 → 4 段 + ProfileRecentList 5 字段 → 6 字段 → 7 字段 T249 #167 + ProfileAudit 1 字段 → 4 字段 
T229 #150) + accessor `get_best_stats()` docblock + `_load_best_stats` 老存档 fallback 0.0/0 共 6 段, 在反复 polish (T127 #67 → 
T131 #83 → T150 #100 → T167 #115 → T209 #128 → T249 #167 → F018.1 #130 修复链) 期间出现 4 类典型 fragile: (1) **"加新 1 best 字段 漏 1 
段"** — 加新 1 best 字段 (e.g. T209 加 `longest_room_seconds`) 落地时漏 1 段 (e.g. 漏 Stage 2 单调更新, 或漏 Stage 3 snapshot, 或漏 Stage 4 
UI 顶级行, 或漏 Stage 5 accessor docblock, 或漏 Stage 6 老存档 fallback), 1 best 字段扩展 0 6 段 0 闭环 0 漂动 (1 字段 0 单调更新 = 1 字段 0 跨 run 
比较 0 漂动; 1 字段 0 snapshot = 1 字段 0 跨 history 0 漂动; 1 字段 0 UI = 1 字段 0 玩家可见 0 漂动; 1 字段 0 accessor docblock = 1 字段 0 文档说明 
0 漂动); (2) **"加新 1 字段 漏 GameState 引用 守卫"** — 加新 1 best 字段 (T209 `longest_room_seconds` 模式) 落地时需要从 GameState autoload 读 
`_longest_room_seconds` 累加器, 0 用 `_read_longest_room_from_gamestate()` 动态 `get_node + has_method` 守卫 (F018.1 #130 修复), 
SceneTree 模式 (smoke test `--script` 启动) GameState autoload 不被加载, 静态引用 `GameState.x` 抛 "Identifier not found: GameState" 
把 T127/T130/T131 3 个 smoke test 整废; (3) **"修 1 best 字段 0 触碰既有 4 字段"** — 修 1 best 字段比较 (e.g. 改 `longest_run_seconds` 比较 
`>` → `>=`) 时改硬编码 0 改 既有 4 字段 (1 `most_rooms_cleared` + 1 `most_shards_collected` + 1 `most_enemies_purified` + 1 
`longest_room_seconds`), 跨字段 1:1 漂动 (1 修 1 字段 0 改既有 4 字段 = 1 字段 0 5 字段 0 闭环 0 漂动); (4) **"加新 1 history 字段 0 同步 best 字段 
0 同步 UI"** — 加新 1 history snapshot 字段 (e.g. T209 加 `longest_room_seconds` 进 snapshot) 0 同步 加新 1 best 字段 (Stage 2 0 镜像) 
+ 0 同步 加新 1 UI 顶级行 (Stage 4 0 镜像), 1 history 字段 0 3 段 0 闭环 0 漂动. 4 类症状根因: 持久化层字段扩展 6 段 canonical 1:1 序列 0 文档化, 任何新 1 
best 字段扩展只能"凭经验" 1:1 镜像 6 段 (1 dict default 1:1 严格 + 1 单调更新 1:1 严格 + 1 snapshot 1:1 严格 + 1 UI 显示 1:1 严格 + 1 accessor 
docblock 1:1 严格 + 1 老存档兼容 1:1 严格), 1 漏 1 段 = 1 best 字段 0 6 段 0 闭环 0 漂动 (1 漏 1 段 0 6 段 0 1:1 严格分离契约 0 漂动 / 1 漏 1 边 0 字节码 
0 1:1 严格 0 漂动).

- **触发场景**: 加新 1 best 字段 (T209 `longest_room_seconds` 模式参考) 时**必须**严格按 6 段 1:1 复制 T209 落地模式: (1) **Stage 1 dict default 
1:1 严格** — 在 `_best_stats` dict literal 加 1 entry (e.g. T209 落地时加 `"longest_room_seconds": 0.0` 1 entry, 与既有 4 字段 0 漏 0 
改 0 反序 0 反向, 0 触碰既有 4 字段任何 1 字段), source-grep 验证 `_best_stats` dict 5 字段 0 漏 0 改 0 漏 1 entry 0 反向; (2) **Stage 2 单调更新 
1:1 严格** — 在 `_update_best_stats_from_current_run()` 末尾加 1 比较 (e.g. T209 落地时加 `var longest_room: float = 
_read_longest_room_from_gamestate()` + `if longest_room > float(_best_stats.get("longest_room_seconds", 0.0))` + 
`_best_stats["longest_room_seconds"] = longest_room` 3 行, 0 触碰既有 4 字段任何 1 字段 0 改 0 反向), source-grep 验证 5 字段 0 漏 0 改 0 漏 
1 比较 0 反向; (3) **Stage 3 snapshot 1:1 严格** — 在 `_capture_run_into_history()` snapshot dict literal 加 1 entry (e.g. T209 
落地时加 `"longest_room_seconds": _read_longest_room_from_gamestate()` 1 entry, 与既有 6 字段 0 漏 0 改 0 反序 0 反向), source-grep 验证 
7 字段 0 漏 0 改 0 漏 1 entry 0 反向; (4) **Stage 4 UI 显示 1:1 严格** — 在 `pause_menu.gd` `_refresh_top_aggregate_rows` 加 1 顶级行 
(e.g. T209 落地时加 `ProfileLongestRoom` 节点 + `_profile_longest_room` 字段 + 3 early-return 路径 + n>0 主路径 8 行 mm:ss format, 0 
触碰既有 4 段 (Achievements / 最佳 / Run # / 段间 ` · ` 分隔符)), source-grep 验证 4 段 0 漏 0 改 0 漏 1 段 0 反向; (5) **Stage 5 accessor 
docblock 1:1 严格** — 在 `get_best_stats()` docblock 注释加 1 字段说明 (e.g. T209 落地时 docblock 加 `longest_room_seconds` 字段说明, 与既有 
4 字段 0 漏 0 改 0 反向), source-grep 验证 docblock 0 漏 0 改 0 漏 1 字段说明 0 反向; (6) **Stage 6 老存档兼容 1:1 严格** — 在 
`_load_best_stats()` 加载 fallback 加 1 fallback (e.g. T209 落地时 `_best_stats.get(key, 0.0)` 循环遍历现有 5 字段 keys, 老存档缺 1 字段自动 
fallback 0.0, 0 抛错 0 反向), source-grep 验证 fallback 0 漏 0 改 0 漏 1 fallback 0 反向. 6 段 1:1 同步, 1 漏 1 段 / 1 漏 1 边 = 1 best 
字段 0 6 段 0 闭环 0 漂动. T209 0 触碰既有 4 字段任何 1 字段 (0 改 dict default 任何 1 entry, 0 改 4 字段单调更新 任何 1 比较, 0 改 snapshot 任何 1 entry,
 0 改 UI 任何 1 顶级行, 0 改 docblock 任何 1 字符, 0 改 fallback 任何 1 字符).

- **修复** (跨 1 套 polish 模式 T290 #213 落地后已知 6 段 0 例外):
  - **T290 #213 首个 PlayerStats `_best_stats` + `_run_history` 字段扩展 6 段 canonical 1:1 严格分离契约 落地** — §9.6.34 段 是 1 套 
polish 模式首个 1:1 包含 6 段 (1 dict default 1:1 严格 + 1 单调更新 1:1 严格 + 1 snapshot 1:1 严格 + 1 UI 显示 1:1 严格 + 1 accessor 
docblock 1:1 严格 + 1 老存档兼容 1:1 严格) 的 PlayerStats 持久化层字段扩展 polish 模式, 6 段 1:1 严格分离 (1 dict default 5 字段 1:1 严格 0 漏 0 改 + 
1 单调更新 5 比较 1:1 严格 0 漏 0 改 + 1 snapshot 7 字段 1:1 严格 0 漏 0 改 + 1 UI 4 段 1:1 严格 0 漏 0 改 + 1 accessor docblock 5 字段说明 1:1 
严格 0 漏 0 改 + 1 老存档 fallback 5 字段 1:1 严格 0 漏 0 改, 0 漏 1 段 0 改 1 字段 0 反序 0 反向), 0 触碰既有 (T290 0 触碰 §9.6.6 / §9.6.7 / 
§9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / 
§9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 24 套 polish 模式任何 1 字符,
 0 触碰游戏代码 .gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容, 0 触碰 
ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).
  - **T290 #213 Stage 1 dict default 1:1 严格分离契约 落地 (`_best_stats` 5 字段)** — §9.6.34 段 1:1 显式 dict default (T290 落地时 
`_best_stats` dict 5 字段 (longest_run_seconds / most_rooms_cleared / most_shards_collected / most_enemies_purified / 
longest_room_seconds) 1:1 严格, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), dict default 1:1 严格 (1 `_best_stats` 5 字段 1:1 严格 0 漏 0 改 + 
1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), 0 触碰既有 (T290 0 改 `_best_stats` dict 任何 1 字符).
  - **T290 #213 Stage 2 单调更新 1:1 严格分离契约 落地 (`_update_best_stats_from_current_run` 5 比较)** — §9.6.34 段 1:1 显式单调更新 (T290 
落地时 `_update_best_stats_from_current_run()` 5 比较 (longest_run_seconds / most_rooms_cleared / most_shards_collected / 
most_enemies_purified / longest_room_seconds) 1:1 严格, 0 漏 1 比较 0 改 1 比较 0 反序 0 反向), 单调更新 1:1 严格 (1 5 比较 1:1 严格 0 漏 0 改 
+ 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 比较 0 改 1 比较 0 反序 0 反向), 0 触碰既有 (T290 0 改 `_update_best_stats_from_current_run` 
任何 1 字符).
  - **T290 #213 Stage 3 snapshot 1:1 严格分离契约 落地 (`_capture_run_into_history` 7 字段)** — §9.6.34 段 1:1 显式 snapshot (T290 
落地时 `_capture_run_into_history()` snapshot 7 字段 (run_number / run_time_seconds / rooms_cleared / enemies_purified / 
shards_collected / deaths / longest_room_seconds) 1:1 严格, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), snapshot 1:1 严格 (1 7 字段 1:1 严格 
0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), 0 触碰既有 (T290 0 改 `_capture_run_into_history` 
任何 1 字符).
  - **T290 #213 Stage 4 UI 显示 1:1 严格分离契约 落地 (`pause_menu.gd` 4 段顶级行)** — §9.6.34 段 1:1 显式 UI 显示 (T290 落地时 
`pause_menu.gd` 4 段顶级行 (ProfileQuickStats Achievements / 最佳 / 最长单房 / Run #) 1:1 严格, 0 漏 1 段 0 改 1 段 0 反序 0 反向), UI 显示 
1:1 严格 (1 4 段顶级行 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 段 0 改 1 段 0 反序 0 反向), 0 触碰既有 (T290 0 改 
`pause_menu.gd` 任何 1 字符, 0 改 `pause_menu.tscn` 任何 1 节点).
  - **T290 #213 Stage 5 accessor docblock 1:1 严格分离契约 落地 (`get_best_stats` 5 字段说明)** — §9.6.34 段 1:1 显式 accessor 
docblock (T290 落地时 `get_best_stats()` docblock 5 字段说明 (longest_run_seconds / most_rooms_cleared / most_shards_collected 
/ most_enemies_purified / longest_room_seconds) 1:1 严格, 0 漏 1 字段说明 0 改 1 字符 0 反序 0 反向), accessor docblock 1:1 严格 (1 5 
字段说明 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 字段说明 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T290 0 改 
`get_best_stats` docblock 任何 1 字符).
  - **T290 #213 Stage 6 老存档兼容 1:1 严格分离契约 落地 (`_load_best_stats` 5 字段 fallback 0.0)** — §9.6.34 段 1:1 显式老存档兼容 (T290 落地时 
`_load_best_stats()` 5 字段 fallback 0.0 (`_best_stats.get(key, 0.0)` 循环遍历现有 5 字段 keys, 老存档缺 1 字段自动 fallback 0.0) 1:1 严格, 
0 漏 1 fallback 0 改 1 fallback 0 反序 0 反向), 老存档兼容 1:1 严格 (1 5 字段 fallback 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 
改, 0 漏 1 fallback 0 改 1 fallback 0 反序 0 反向), 0 触碰既有 (T290 0 改 `_load_best_stats` 任何 1 字符).
  - 跨 1 套 polish 模式 T290 #213 落地后 0 漂动, §9.6.34 段 1:1 严格 (6 段 1:1 严格分离 + dict default 1:1 严格 + 单调更新 1:1 严格 + snapshot 
1:1 严格 + UI 显示 1:1 严格 + accessor docblock 1:1 严格 + 老存档兼容 1:1 严格) 100% 闭环, 6 段 1:1 严格分离 (1 Stage 1 dict default 1:1 严格 0 
漏 0 改 0 反序 + 1 Stage 2 单调更新 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 3 snapshot 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 4 UI 显示 1:1 严格 0 漏 0 
改 0 反序 + 1 Stage 5 accessor docblock 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 6 老存档兼容 1:1 严格 0 漏 0 改 0 反序). 1 套 polish 模式 0 后续 主要 
pre-existing 风险.

- **预防**:
  1. 任何 polish 期给 "加新 1 best 字段" 或 "加新 1 history 字段" 或 "加新 1 派生 UI 字段" 任务加新 best 字段 / 加新 history 字段 / 加新派生 UI 
字段时**必须**严格按 6 段 1:1 复制, 0 反序 0 漏 0 改 1 字段. 6 段 0 触碰边界: **Stage 1 0 改 dict default 1:1 严格分离契约** (T290 落地后 `_best_stats` 
5 字段已切 1:1 strict 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反序 0 反向, 改 1 字段 / 漏 1 字段 = 1 best 字段扩展 0 6 段 0 闭环 0 漂动), **Stage 2 0 
改单调更新 1:1 严格分离契约** (T290 落地后 `_update_best_stats_from_current_run` 5 比较已切 1:1 strict 0 漏 0 改 0 改 1 比较 0 漏 1 比较 0 反序 0 
反向, 改 1 比较 / 漏 1 比较 = 1 best 字段扩展 0 单调更新 0 闭环 0 漂动), **Stage 3 0 改 snapshot 1:1 严格分离契约** (T290 落地后 
`_capture_run_into_history` 7 字段已切 1:1 strict 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反序 0 反向, 改 1 字段 / 漏 1 字段 = 1 history 字段扩展 0 
snapshot 0 闭环 0 漂动), **Stage 4 0 改 UI 显示 1:1 严格分离契约** (T290 落地后 `pause_menu.gd` 4 段顶级行已切 1:1 strict 0 漏 0 改 0 改 1 段 0 漏 
1 段 0 反序 0 反向, 改 1 段 / 漏 1 段 = 1 best 字段扩展 0 UI 显示 0 闭环 0 漂动), **Stage 5 0 改 accessor docblock 1:1 严格分离契约** (T290 落地后 
`get_best_stats` docblock 5 字段说明已切 1:1 strict 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 0 反向, 改 1 字符 / 漏 1 字符 = 1 best 字段扩展 0 
accessor 0 闭环 0 漂动), **Stage 6 0 改老存档兼容 1:1 严格分离契约** (T290 落地后 `_load_best_stats` 5 字段 fallback 已切 1:1 strict 0 漏 0 改 0 
改 1 fallback 0 漏 1 fallback 0 反序 0 反向, 改 1 fallback / 漏 1 fallback = 1 best 字段扩展 0 老存档兼容 0 闭环 0 漂动). 任何"加新 1 best 
字段"必须**先**按 Stage 1 dict default 1:1 加 1 entry (Stage 1 1:1 严格, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), **再**按 Stage 2 单调更新 1:1 加 
1 比较 (Stage 2 1:1 严格, 0 漏 1 比较 0 改 1 比较 0 反序 0 反向), **再**按 Stage 3 snapshot 1:1 加 1 entry (Stage 3 1:1 严格, 0 漏 1 字段 0 改 
1 字段 0 反序 0 反向), **再**按 Stage 4 UI 显示 1:1 加 1 段 (Stage 4 1:1 严格, 0 漏 1 段 0 改 1 字段 0 反序 0 反向), **再**按 Stage 5 accessor 
docblock 1:1 加 1 字段说明 (Stage 5 1:1 严格, 0 漏 1 字符 0 改 1 字符 0 反序 0 反向), **再**按 Stage 6 老存档兼容 1:1 加 1 fallback (Stage 6 1:1 
严格, 0 漏 1 fallback 0 改 1 fallback 0 反序 0 反向).
  2. **6 段 0 触碰边界** — `_best_stats` dict + `_update_best_stats_from_current_run` + `_capture_run_into_history` + 
`pause_menu.gd` + `get_best_stats` docblock + `_load_best_stats` fallback 任何 1 件 0 改 0 删 0 重排 (6 段 0 触碰边界), 任何"修 1 
件"polish 走 Stage 1 1:1 严格 (改 件既有 6 段时**必须**保持 6 段 0 改 0 漏 0 反向 0 反序 0 触碰既有 1 件); 任何"加新 1 边"polish 走 Stage 2 1:1 严格 (改 
边既有 单调更新 时**必须**保持 单调更新 0 改 0 漏 0 反向 0 反序 0 漏 1 边); 任何"加新 1 块"polish 走 Stage 3 1:1 严格 (改 块既有 snapshot 时**必须**保持 
snapshot 0 改 0 漏 0 反向 0 反序 0 漏 1 块); 任何"加新 1 段"polish 走 Stage 4 1:1 严格 (改 段既有 UI 显示 时**必须**保持 UI 显示 0 改 0 漏 0 反向 0 反序 0 
漏 1 段); 任何"加新 1 字符"polish 走 Stage 5 1:1 严格 (改 字符既有 accessor docblock 时**必须**保持 accessor docblock 0 改 0 漏 0 反向 0 反序 0 漏 
1 字符); 任何"加新 1 fallback"polish 走 Stage 6 1:1 严格 (改 fallback 既有 老存档兼容 时**必须**保持 老存档兼容 0 改 0 漏 0 反向 0 反序 0 漏 1 fallback).
  3. **0 改 1 字段 0 漏 1 字段 0 反向** (T290 落地后 0 触碰边界) — PlayerStats 持久化层字段扩展 6 段 (1 `_best_stats` 5 字段 + 1 
`_update_best_stats_from_current_run` 5 比较 + 1 `_capture_run_into_history` 7 字段 + 1 `pause_menu.gd` 4 段顶级行 + 1 
`get_best_stats` docblock 5 字段说明 + 1 `_load_best_stats` 5 字段 fallback) 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反向, 改 1 字段 
/ 漏 1 字段 / 反向 1 字段 = 1 best 字段扩展 0 6 段 0 漂动, source-grep 验证 6 段 0 漏 0 改 1 字段 0 漏 1 字段 0 改 1 字符 0 反向.
  4. **0 改 1 边 0 漏 1 边 0 反向** (T290 落地后 0 触碰边界) — 字段扩展契约 (1 `_best_stats` 5 字段 1:1 严格 + 1 
`_update_best_stats_from_current_run` 5 比较 1:1 严格 + 1 `_capture_run_into_history` 7 字段 1:1 严格 + 1 `pause_menu.gd` 4 
段顶级行 1:1 严格 + 1 `get_best_stats` docblock 5 字段说明 1:1 严格 + 1 `_load_best_stats` 5 字段 fallback 1:1 严格) 1:1 严格分离 0 漏 0 改 0 
改 1 边 0 漏 1 边 0 反向, 改 1 边 / 漏 1 边 / 反向 1 边 = 1 best 字段扩展 0 字段扩展契约 0 漂动, source-grep 验证 6 段 0 触碰字段扩展契约 0 漏 0 改 1 边 0 漏 1 
边 0 改 1 字符 0 反向.
  5. **T162 brittle 修复流程 0 触碰边界** — PlayerStats 持久化层字段扩展 0 副作用 子段 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 
触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构 (T162 brittle 修复流程 = 
1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), 改 1 字段 / 漏 1 字段 / 
反向 1 字段 = 1 best 字段扩展 0 0 副作用 0 漂动 0 闭环, source-grep 验证 6 段 0 副作用 子段 0 漏 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 触碰既有 0 改 1 段 
0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构.
  6. **1 套 polish 模式 0 互混 0 复用 0 共享 唯一性 标注 0 改 1 段 0 漏 1 段 0 反向** — §9.6.34 段 唯一性 标注 1:1 严格分离 0 漏 0 改 0 改 1 套 polish 模式 
0 漏 1 套 polish 模式 0 反向 0 触碰既有 0 改 1 段 (本节是 25 套 polish 模式**唯一**关注 PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约 的 1 套, 
0 互混 0 复用 0 共享), 改 1 套 polish 模式 / 漏 1 套 polish 模式 / 反向 1 套 polish 模式 = 1 §9.6.34 段 0 唯一性 标注 0 漂动 0 闭环, source-grep 验证 
§9.6.34 段 唯一性 标注 0 漏 0 改 1 套 polish 模式 0 漏 1 套 polish 模式 0 改 1 字符 0 反向.
  7. **§9.6.34 是 25 套 polish 模式**唯一**关注 "PlayerStats `_best_stats` + `_run_history` 字段扩展 6 段 canonical 1:1 严格分离契约 
(`_best_stats` 5 字段 + `_update_best_stats_from_current_run` 5 比较 + `_capture_run_into_history` 7 字段 + `pause_menu.gd` 4 
段顶级行 + `get_best_stats` docblock 5 字段说明 + `_load_best_stats` 5 字段 fallback) + dict default 1:1 严格 (5 字段 1:1 严格 0 漏 0 改) 
+ 单调更新 1:1 严格 (5 比较 1:1 严格 0 漏 0 改) + snapshot 1:1 严格 (7 字段 1:1 严格 0 漏 0 改) + UI 显示 1:1 严格 (4 段顶级行 1:1 严格 0 漏 0 改) + 
accessor docblock 1:1 严格 (5 字段说明 1:1 严格 0 漏 0 改) + 老存档兼容 1:1 严格 (5 字段 fallback 1:1 严格 0 漏 0 改) + 0 触碰游戏代码 + 0 触碰 §10 
决策记录流程 0 触碰既有" 的 1 套** — 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / 
§9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / 
§9.6.31 / §9.6.32 / §9.6.33 24 套 polish 模式 0 互混 0 复用 0 共享. §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / 
§9.6.16 / §9.6.17 8 套关注 "具体 game / pause_menu polish 模式 (1 panel × 3 组件 × 1:1 视觉组连贯 / 6 verb hit handler / 6 verb 
signal 桥接 / SaveSystem audit_save_slots / PauseMenu 跨面板 `_alpha_base` / ProfileQuickStats 4 段独立 hover 联动 / 
ProfileRecentList 5 行 row 文本 5→7 字段 / ProfileRecentList 5 行 row hover feedback 三件套)" (与 PlayerStats 持久化层字段扩展 6 段 0 相关, 
8 套具体 polish 模式 0 涉及 6 段 1:1 严格分离契约 自身 1:1 严格分离), §9.6.18 / §9.6.19 / §9.6.22 / §9.6.24 4 套关注 "6 verb 接入 单层 字节码 (16 件套 
ability base / 7 件套 windup VFX base / 5 段 VFX 视觉组 / 5 段 HUD 36 通道)" (与 PlayerStats 持久化层字段扩展 6 段 0 相关, 4 套 6 verb 单层 字节码 
0 涉及 PlayerStats 持久化层 6 段 自身 1:1 严格分离), §9.6.20 / §9.6.21 / §9.6.23 / §9.6.25 / §9.6.26 5 套关注 "6 verb 跨组件 / 跨房间 / 跨段 
视觉组 (Hub ↔ archive 跨房间 transition / 6 verb 成就系统 闭环 8 段 / 7 桶 prewarm / 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 / 6 verb icon 视觉组 
5 段)" (与 PlayerStats 持久化层字段扩展 6 段 0 相关, 5 套跨组件 0 涉及 6 段**之间**字段扩展契约 自身 1:1 严格分离), §9.6.27 关注 "PlayerProfilePanel 1 
panel × 3 组件 × 1:1 视觉组连贯 5 段 canonical 1:1 序列" (与 PlayerStats 持久化层字段扩展 6 段 **Stage 4 UI 显示 1:1 严格** 1:1 镜像, 1 套 polish 
模式覆盖 UI 视觉组连贯 0 涉及 PlayerStats 持久化层 6 段 自身 1:1 严格分离), §9.6.28 / §9.6.29 / §9.6.30 3 套关注 "工具链 / smoke test / docs polish 
(smoke test ITERATION_COUNT 跨迭代 + 段边界 find 5 段 / polish 文档化 5 段 canonical 1:1 序列 模板 / 工具链 3 件套 1:1 严格分离契约)" (与 
PlayerStats 持久化层字段扩展 6 段 0 相关, 3 套工具链 0 涉及 6 段**之间**字段扩展契约 自身 1:1 严格分离), §9.6.31 / §9.6.32 2 套关注 "文档归档 3 件套 1:1 严格分离契约 
(CHANGELOG 归档 3 件套 1:1 严格分离契约 / REVIEW_LOG 归档 3 件套 1:1 严格分离契约)" (与 PlayerStats 持久化层字段扩展 6 段 0 相关, 2 套文档归档 0 涉及 6 
段**之间**字段扩展契约 自身 1:1 严格分离), §9.6.33 关注 "6 verb 接入 4 件套 字节码一致性 1:1 严格分离契约 4 件套 1:1 视觉组连贯" (与 PlayerStats 持久化层字段扩展 6 段 0 
相关, 4 件套 1:1 严格 0 涉及 PlayerStats 持久化层 6 段 自身 1:1 严格分离), §9.6.34 关注 "PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约 6 段 
1:1 视觉组连贯" (与具体 game / pause_menu / 6 verb 接入 单层 / 6 verb 跨组件 / 工具链 / 文档归档 / 6 verb 接入 4 件套 polish 0 相关, 6 段 1:1 严格 镜像 
1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 6 段 = 6 元素 1:1 严格). §9.6.34 是 25 套 polish 模式**唯一**关注 "PlayerStats 
`_best_stats` + `_run_history` 字段扩展 6 段 canonical 1:1 严格分离契约 (`_best_stats` 5 字段 + 
`_update_best_stats_from_current_run` 5 比较 + `_capture_run_into_history` 7 字段 + `pause_menu.gd` 4 段顶级行 + 
`get_best_stats` docblock 5 字段说明 + `_load_best_stats` 5 字段 fallback) + dict default 1:1 严格 (5 字段 1:1 严格 0 漏 0 改) + 单调更新 
1:1 严格 (5 比较 1:1 严格 0 漏 0 改) + snapshot 1:1 严格 (7 字段 1:1 严格 0 漏 0 改) + UI 显示 1:1 严格 (4 段顶级行 1:1 严格 0 漏 0 改) + accessor 
docblock 1:1 严格 (5 字段说明 1:1 严格 0 漏 0 改) + 老存档兼容 1:1 严格 (5 字段 fallback 1:1 严格 0 漏 0 改) + 0 触碰游戏代码 + 0 触碰 §10 决策记录流程 0 
触碰既有" 的 1 套, 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 
/ §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 
/ §9.6.33 24 套 ProfileRecentList 5 行 / ProfileQuickStats 4 段 / AchievementGrid / 6 verb 单层 字节码 / 6 verb 跨组件 / 
SaveSystem / 跨房间 transition / PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 / smoke test ITERATION_COUNT 跨迭代 + 段边界 find 
/ polish 文档化 5 段 canonical 1:1 序列 模板 / 工具链 3 件套 1:1 严格分离契约 / CHANGELOG 归档 3 件套 1:1 严格分离契约 / REVIEW_LOG 归档 3 件套 1:1 
严格分离契约 / 6 verb 接入 4 件套 字节码一致性 0 互混 0 复用 0 共享. 任何"加新 1 best 字段"或"加新 1 history 字段"或"加新 1 派生 UI 字段"必须**先**按 §9.6.34 Stage 
1 dict default 1:1 严格 加 1 entry (Stage 1 1:1 严格, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), **再**按 §9.6.34 Stage 2 单调更新 1:1 严格 加 1 
比较 (Stage 2 1:1 严格, 0 漏 1 比较 0 改 1 比较 0 反序 0 反向), **再**按 §9.6.34 Stage 3 snapshot 1:1 严格 镜像 (Stage 3 1:1 严格, 0 触碰既有 0 漏 
1 entry 0 改 1 字段 0 反向), **再**按 §9.6.34 Stage 4 UI 显示 1:1 严格 镜像 (Stage 4 1:1 严格, 0 触碰既有 0 漏 1 段 0 改 1 字段 0 反向), **再**按 
§9.6.34 Stage 5 accessor docblock 1:1 严格 镜像 (Stage 5 1:1 严格, 0 触碰既有 0 漏 1 字符 0 改 1 字符 0 反向), **再**按 §9.6.34 Stage 6 
老存档兼容 1:1 严格 镜像 (Stage 6 1:1 严格, 0 触碰既有 0 漏 1 fallback 0 改 1 fallback 0 反向).
  8. 已知 drift risk: PlayerStats 持久化层字段扩展 **可能**漏 1 段 / 1 边 1:1 镜像 (e.g. 加新 1 best 字段时漏 Stage 1 dict default 0 镜像, 1 
best 字段扩展 0 6 段 0 闭环; 加新 1 best 字段时漏 Stage 2 单调更新 中 5 比较 0 镜像, 1 best 字段扩展 0 单调更新 0 闭环; 加新 1 best 字段时漏 Stage 3 snapshot 
中 7 字段 0 镜像, 1 best 字段扩展 0 snapshot 0 闭环; 加新 1 best 字段时漏 Stage 4 UI 顶级行 中 4 段 0 镜像, 1 best 字段扩展 0 UI 0 闭环; 加新 1 best 
字段时漏 Stage 5 accessor docblock 中 5 字段说明 0 镜像, 1 best 字段扩展 0 accessor 0 闭环; 加新 1 best 字段时漏 Stage 6 老存档 fallback 中 5 字段 0 
镜像, 1 best 字段扩展 0 老存档兼容 0 闭环). 监控建议: 任何"未来给 PlayerStats 持久化层加新 1 best 字段 / 加新 1 history 字段 / 加新 1 派生 UI 字段"必须**先**在 6 段 
(Stage 1 dict default 1:1 严格 + Stage 2 单调更新 1:1 严格 + Stage 3 snapshot 1:1 严格 + Stage 4 UI 显示 1:1 严格 + Stage 5 accessor 
docblock 1:1 严格 + Stage 6 老存档兼容 1:1 严格) 1:1 镜像 0 漏 1 段 / 0 漏 1 边 / 0 漏 1 字段. T290 (#213) 6 段 1:1 镜像落地后任何新 best 字段 / 新 
history 字段 / 新 派生 UI 字段 / 新 6 段同步 是 §9.6.34 6 段的"母集" (1 best 字段扩展 × 6 段 = 6 元素 1:1 严格, §9.6.34 6 段 1 套 polish 模式 完整闭环, 
1 套 polish 模式 × 6 段 = 6 元素 1:1 严格, 含 Stage 1 dict default 1:1 严格 1 套 polish 模式 6 段 0 漏 0 改 + Stage 2 单调更新 1:1 严格 1 套 
polish 模式 单调更新 0 漏 0 改 + Stage 3 snapshot 1:1 严格 1 套 polish 模式 snapshot 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 段 + Stage 4 UI 
显示 1:1 严格 1 套 polish 模式 UI 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 段 + Stage 5 accessor docblock 1:1 严格 1 套 polish 模式 accessor 
0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 段 + Stage 6 老存档兼容 1:1 严格 1 套 polish 模式 老存档兼容 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 段). 另 1 
类已知 drift: Stage 4 UI 显示 1:1 严格 0 改 1 段 0 漏 1 段 0 反向 (e.g. 后续 polish 期加新 1 best 字段时漏 0 镜像 `pause_menu.gd` 4 段顶级行任何 1 段, 
1 best 字段扩展 0 UI 显示 0 闭环; 或漏 0 镜像 §9.6.27 5 段任何 1 段, 1 best 字段扩展 0 UI 显示 0 闭环; 或改 1 段 0 改 1 段 0 反向 0 触碰既有), 后续 polish 
期若加新 1 best 字段必须**先**用 1:1 镜像 6 段 模板, 0 改既有 0 漏 1 段 0 漏 1 边 0 反向.

**0 副作用**: T290 (#213) 任何 const / var 0 触碰 (`_best_stats` 0 改 0 删 0 重排 5 字段任何 1 entry; 
`_update_best_stats_from_current_run` 0 改 0 删 0 重排 5 比较任何 1 字符; `_capture_run_into_history` 0 改 0 删 0 重排 7 字段任何 1 entry;
 `pause_menu.gd` 0 改 0 删 0 重排 4 段顶级行任何 1 字段; `get_best_stats` docblock 0 改 0 删 0 重排 5 字段说明任何 1 字符; `_load_best_stats` 0 
改 0 删 0 重排 5 字段 fallback 任何 1 字符; CONTRIBUTING.md §9.6.34 段 0 改 0 删 0 重排, 仅新加 1 段 (~50 行)); 0 触碰 §9.6.6 
ProfileRecentList 5 行 row 文本 5→7 字段 format 字符串扩展模式 (T290 0 改 §9.6.6 任何 1 字符); 0 触碰 §9.6.7 ProfileRecentList 5 行 row 
hover feedback 三件套 (T290 0 改 §9.6.7 任何 1 字符); 0 触碰 §9.6.8 ProfileQuickStats 4 段独立 hover 联动 (T290 0 改 §9.6.8 任何 1 字符); 0 
触碰 §9.6.9 AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` Dictionary 双源 (T290 0 改 §9.6.9 任何 1 字符); 0 触碰 §9.6.10 
「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (T290 0 改 §9.6.10 任何 1 字符); 0 触碰 §9.6.15 SaveSystem audit_save_slots (T290 0 改 
§9.6.15 任何 1 字符); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T290 0 改 §9.6.16 任何 1 字符); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 
(T290 0 改 §9.6.17 任何 1 字符); 0 触碰 §9.6.18 16 件套 verb ability base (T290 0 改 §9.6.18 任何 1 字符); 0 触碰 §9.6.19 7 件套 windup 
VFX base (T290 0 改 §9.6.19 任何 1 字符); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 transition flow (T290 0 改 §9.6.20 任何 1 字符); 0 触碰 
§9.6.21 8 段成就序列 (T290 0 改 §9.6.21 任何 1 字符); 0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T290 0 改 §9.6.22 任何 1 字符); 0 触碰 §9.6.23 7 
桶 prewarm 5+1+1 (T290 0 改 §9.6.23 任何 1字符); 0 触碰 §9.6.24 HUD 6 verb × 6 通道 = 36 通道 5 段序列 (T290 0 改 §9.6.24 任何 1 字符); 0 
触碰 §9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 5 段序列 (T290 0 改 §9.6.25 任何 1 字符); 0 触碰 §9.6.26 6 verb icon 视觉组 5 段 canonical 1:1 
序列 (T290 0 改 §9.6.26 任何 1 字符); 0 触碰 §9.6.27 PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 5 段序列 (T290 0 改 §9.6.27 任何 1 
字符); 0 触碰 §9.6.28 smoke test ITERATION_COUNT 跨迭代 + 段边界 find 5 段序列 (T290 0 改 §9.6.28 任何 1 字符); 0 触碰 §9.6.29 polish 文档化 5 
段 canonical 1:1 序列 模板 + 4 段关系段 模板 (T290 0 改 §9.6.29 任何 1 字符); 0 触碰 §9.6.30 工具链 3 件套 1:1 严格分离契约 (T290 0 改 §9.6.30 任何 1 
字符); 0 触碰 §9.6.31 CHANGELOG 归档 3 件套 1:1 严格分离契约 (T290 0 改 §9.6.31 任何 1 字符); 0 触碰 §9.6.32 REVIEW_LOG 归档 3 件套 1:1 严格分离契约 
(T290 0 改 §9.6.32 任何 1 字符); 0 触碰 §9.6.33 6 verb 接入 4 件套 字节码一致性 (T290 0 改 §9.6.33 任何 1 字符); 0 触碰 
check_smoke_consistency.sh (T290 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh 
(T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, 0 触碰 `player_stats.gd` 
`_best_stats` / `_update_best_stats_from_current_run` / `_capture_run_into_history` / `get_best_stats` / 
`_load_best_stats` 任何 1 字段, 0 触碰 `pause_menu.gd` 4 段顶级行任何 1 字段, 0 触碰 `pause_menu.tscn` 
PlayerProfilePanel/ProfileMargin/ProfileVBox 4 段任何 1 节点); 0 行为变化, 1 文档 1 段 (§9.6.34 ~50 行) + 0 字节码修改 (T290 仅文档化既有 
PlayerStats 持久化层字段扩展 6 段 canonical polish 模式, 0 改 `_best_stats` 5 字段 0 改 `_update_best_stats_from_current_run` 5 比较 0 改 
`_capture_run_into_history` 7 字段 0 改 `pause_menu.gd` 4 段顶级行 0 改 `get_best_stats` docblock 5 字段说明 0 改 `_load_best_stats` 
5 字段 fallback 任何 1 字段 0 触碰既有) + 1 smoke test (T290 30+ 断言).

**与 §9.6.27 关系** — §9.6.27 (T283 #198 落地) 关注 "PlayerProfilePanel 1 panel × 3 组件 (ProfileQuickStats 4 段 + 
ProfileRecentList 5 行 × 7 字段 + ProfileAudit 4 字段) (1 panel × 3 组件 × 1:1 视觉组连贯) 5 段 canonical 1:1 序列 polish 模式" (UI 
视觉组连贯 0 涉及数据层). §9.6.34 关注 "PlayerStats `_best_stats` + `_run_history` 字段扩展 6 段 canonical 1:1 严格分离契约" (数据层 0 涉及 UI 
视觉组连贯, Stage 4 UI 显示 1:1 严格 镜像 `pause_menu.gd` 4 段顶级行). §9.6.27 与 §9.6.34 0 互混 0 复用 0 共享: §9.6.27 关注 "UI 视觉组连贯 5 段 
canonical 1:1 序列 (3 组件 1:1 + 中点分隔符 + 7pt 字号 + _COLOR_* canonical 颜色 token + 派生字段)" (与 PlayerStats 持久化层字段扩展 6 段 0 相关, UI 
视觉组连贯 0 涉及 dict default / 单调更新 / snapshot / accessor docblock / 老存档 fallback 5 段), §9.6.34 关注 "持久化层字段扩展 6 段 canonical 
1:1 严格分离契约 (dict default + 单调更新 + snapshot + UI 显示 + accessor docblock + 老存档兼容)" (与 UI 视觉组连贯 0 相关, 6 段 1:1 严格 镜像 1 套 
polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 6 段 = 6 元素 1:1 严格). §9.6.34 是 §9.6.27 的"上游" — §9.6.27 Stage 4 关注 "UI 视觉组连贯 0 漂动 
0 闭环", §9.6.34 Stage 4 关注 "UI 显示 1:1 严格 0 漂动 0 闭环" (即 "加新 1 顶级行" 必须**先**按 §9.6.34 Stage 4 UI 显示 1:1 严格 加 1 段 (Stage 4 
1:1 严格, 0 漏 1 段 0 改 1 字段), **再**按 §9.6.27 Stage 1 3 组件 1:1 严格 加 1 段 (Stage 1 1:1 严格, 0 漏 1 段 0 改 1 字段)). 任何"加新 1 
顶级行"必须**先**按 §9.6.34 Stage 4 UI 显示 1:1 严格 加 1 段 (Stage 4 1:1 严格, 0 漏 1 段 0 改 1 字段 0 反向), **再**按 §9.6.27 Stage 1 3 组件 
1:1 严格 加 1 段 (Stage 1 1:1 严格, 0 漏 1 段 0 改 1 字段 0 反向).

**与 §9.6.31 / §9.6.32 关系** — §9.6.31 (T287 #209 落地) 关注 "CHANGELOG 归档 3 件套 1:1 严格分离契约" 模式; §9.6.32 (T288 #211 落地) 关注 
"REVIEW_LOG 归档 3 件套 1:1 严格分离契约" 模式. §9.6.34 关注 "PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约" 模式. §9.6.31 / §9.6.32 与 
§9.6.34 0 互混 0 复用 0 共享: §9.6.31 / §9.6.32 关注 "文档归档 3 件套 1:1 严格分离契约 (CHANGELOG / REVIEW_LOG)" (与 PlayerStats 持久化层字段扩展 6 
段 0 相关, 文档归档 3 件套仅是 `wc -l` 阈值 + 活跃保留 N + 归档完整性 3 件套, 0 涉及 dict default / 单调更新 / snapshot / UI 显示 / accessor docblock / 
老存档 fallback 6 段), §9.6.34 关注 "PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约 6 段 1:1 视觉组连贯" (与文档归档 0 相关, 6 段 1:1 严格 镜像 
1 套 polish 模式 既有 1:1 严格分离). 任何"加新 1 文档归档类型"必须**先**按 §9.6.31 / §9.6.32 3 件套 1:1 复制 (3 件套 1:1 严格, 0 漏 1 件 0 改 1 字段 0 反向), 
**再**按 §9.6.34 6 段 1:1 同步 (6 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.31 / §9.6.32 任何 1 字符).

**与 §9.6.33 关系** — §9.6.33 (T289 #212 落地) 关注 "6 verb 接入 4 件套 字节码一致性 1:1 严格分离契约" 模式 (4 件套 1:1 严格分离契约: Stage 1 ability 
字节码 1:1 严格 + Stage 2 windup VFX 字节码 1:1 严格 + Stage 3 VFX 视觉组 字节码 1:1 严格 + Stage 4 HUD row 字节码 1:1 严格). §9.6.34 关注 
"PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约" 模式 (6 段 1:1 严格分离契约: Stage 1 dict default 1:1 严格 + Stage 2 单调更新 1:1 严格 + 
Stage 3 snapshot 1:1 严格 + Stage 4 UI 显示 1:1 严格 + Stage 5 accessor docblock 1:1 严格 + Stage 6 老存档兼容 1:1 严格). §9.6.33 与 
§9.6.34 0 互混 0 复用 0 共享: §9.6.33 关注 "6 verb 接入 4 件套 字节码一致性 1:1 严格分离契约 (`_verb_ability_base.gd` 16 件套 + 
`_verb_windup_vfx_base.gd` 7 件套 + 6 verb VFX 视觉组 5 段 + HUD 6 verb × 6 通道 = 36 通道)" (与 PlayerStats 持久化层字段扩展 6 段 0 相关, 4 
件套 1:1 严格 0 涉及 dict default / 单调更新 / snapshot / UI 显示 / accessor docblock / 老存档 fallback 6 段), §9.6.34 关注 "PlayerStats 
持久化层字段扩展 6 段 canonical 1:1 严格分离契约 (`_best_stats` 5 字段 + `_update_best_stats_from_current_run` 5 比较 + 
`_capture_run_into_history` 7 字段 + `pause_menu.gd` 4 段顶级行 + `get_best_stats` docblock 5 字段说明 + `_load_best_stats` 5 字段 
fallback)" (与 6 verb 接入 4 件套 字节码一致性 0 相关, 6 段 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离). §9.6.33 是 §9.6.34 6 段的"姊妹段" (2 套 
polish 模式 0 互混 0 复用 0 共享, 一是 6 verb 接入 4 件套 字节码, 一是 PlayerStats 持久化层字段扩展 6 段; 1 套 polish 模式 × 4 件套 = 4 元素 1:1 严格 + 1 套 
polish 模式 × 6 段 = 6 元素 1:1 严格). 任何"加新 1 6 verb"必须**先**按 §9.6.33 4 件套 1:1 复制 (4 件套 1:1 严格, 0 漏 1 件 0 改 1 字段 0 反向), 
**再**按 §9.6.34 6 段 1:1 同步 (6 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.33 任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 
0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 轮 0 后续 持续收敛), §9.6.34 是"PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约" (6 段 1:1 
严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约). T162 与 §9.6.34 0 互混 0 复用 0 共享: T162 关注 "brittle 修复流程 5 步骤" (1 expect 反转 + 1 
docblock 说明 + 1 段 find 反转 + 0 触碰既有 + 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 步骤 1:1 严格), §9.6.34 关注 "6 段 
1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约" (与 5 步骤 0 相关, 1 套 polish 模式 × 6 段 = 6 元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离). 任何"加新 1 
brittle 修复"必须**先**按 T162 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), **再**按 §9.6.34 6 
段 1:1 镜像 (6 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点).

**与 §9.6.9 关系** — §9.6.9 (T222 #144 + T219 #141 + T226 #145 落地) 关注 "AchievementGrid locked slot 解锁进度 alpha lerp + 
ProfileRecentList 5 行 alpha 渐变 base + 跨面板 `_alpha_base` Dictionary 双源 polish 模式" (跨面板 1:1 alpha lerp 模板, 0 涉及 
PlayerStats 持久化层字段扩展). §9.6.34 关注 "PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约" (PlayerStats 数据层字段扩展, 0 涉及 alpha 
lerp). §9.6.9 与 §9.6.34 0 互混 0 复用 0 共享: §9.6.9 关注 "跨面板 alpha lerp 双源 (`_alpha_base` Dictionary)" (与 PlayerStats 
持久化层字段扩展 6 段 0 相关, alpha lerp 0 涉及 dict default / 单调更新 / snapshot / accessor docblock / 老存档 fallback 5 段), §9.6.34 关注 
"PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约" (与 alpha lerp 0 相关, 6 段 1:1 严格 0 涉及 `_alpha_base` Dictionary). 任何"加新 1 
alpha lerp 跨面板"必须**先**按 §9.6.9 跨面板 alpha lerp 1:1 复制 (1:1 严格, 0 漏 1 边 0 改 1 字段 0 反向), **再**按 §9.6.34 6 段 1:1 同步 (6 段 
1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.9 任何 1 字符).
