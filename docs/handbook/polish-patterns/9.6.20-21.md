# Polish Patterns 9.6.20–21

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.20–9.6.21，共 2 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.20-21.md

<!-- shard 20-21 共 2 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.20 Hub ↔ archive 跨房间 transition flow polish 模式 (跨 12 任务 ~196 轮 落地)

> T053 (#38) Hub ↔ archive 双向闭环奠基 → T063 (#43) BGM 场景集成 → T070 (#49) 存档系统持久化磁盘版 → T078 (#55) Boss 音乐 override 引用计数 → 
> T079 (#56) 玩家死亡后重生点修复 (关键: `_ready` 顺序 bug 修, 0 漏 `GameState._is_transitioning` 检查在 `is_hub_mode` 之前) → T081 (#57) 
> atmosphere bell 修复 0.8s 暖光回流 → T107 (#76) 第 7 主题 BGM `archive_storm` → T114 (#82) `silence_void` BGM preset → T123 
> (#88) `whisper_hollow` 路由 → T156 (#112) ArchiveStorm 主摄像机 shake 触发 (1f skybox rotate) → T223 (#163) `archive_05` Wave 
> 0.5× 教学房间 → T269 (#192) `archive_05` 灰盒 + 内容扩展. 跨 12 任务 158 轮迭代落地, 5 段 canonical 1:1 序列, 0 漏 0 改 1 字段, 任何"加新 1 
> room"必须**先**按 §9.6.20 5 段序列 1:1 复制, **再**按 §9.1 9 步走 "node" 步落地 (0 反序 0 漏 0 改 1 字段). 跨 5 个 room (`hub` / `archive_01` 
> ~ `archive_05`) + 5 个 BGM preset (`silence_void` / `whisper_hollow` / `archive_storm` / ... 0 触碰) 全部走同一套 5 段 1:1 序列 0 
> 例外.

1. **症状** — polish 期任意 1 个 room 切换 (Hub → archive / archive → Hub / archive_01 → archive_02) 出现 4 类症状之一: (a) **玩家位置丢失** 
— 跨 scene 后玩家回到旧 scene 的 `last_known_position` 0 走 `_pending_spawn_point` (T079 修, `_ready` 顺序 bug 漏 
`GameState._is_transitioning` 检查在 `is_hub_mode` 之前), 或 `_pending_spawn_point` 0 写入就 `change_scene_to_file` (字段传递契约破坏), 
或 `respawn_at` 0 调直接走默认 `Vector2(60, 180)` (T273.b #191 收回完成, T079 #56 修复后 `respawn_at(_pending_spawn_point)` 1:1 调用); 
(b) **持久化状态丢失** — `change_scene_to_file` 之前 0 调 `GameState.save_persistent_state()` (T070 #49 持久化磁盘版落地, 跨 scene 后 
health / max_health / coins / current_verb 等 0 还原), 或 `save_persistent_state` 在 `change_scene_to_file` 之后调 (T070 落地后顺序 
1:1 严格, 跨 scene 持久化契约 0 反序); (c) **fade_in 黑屏闪 1 帧** — `_room_transition.fade_in(0.5)` 之前 0 调 `await 
get_tree().process_frame` (T052 #37 `call_deferred` 修复: `_room_transition` 通过 `call_deferred("add_child", ...)` 在 
`_ready` 之后 1 帧才落地, `ColorRect.modulate` 属性 0 存在直接 `tween_property` 报 "Required object 'rp_target' is null"), 或 
`process_frame` 在 `fade_in` 之后调 (T052 落地后顺序 1:1 严格); (d) **BGM 错乱** — `change_scene_to_file` 之前 0 调 
`_play_music_for_state(PLAYING)` (T107 #76 archive_storm 落地时 0 改), 或新 scene `_ready` 之前就播 boss BGM 干扰 hub BGM (T078 #55 
Boss override 引用计数修). 4 类症状的根因**全部**是 5 段序列任意 1 段 0 走或反序或漏 1 字段 (`_pending_room_path` / `_pending_spawn_point` / 
`GameState._is_transitioning` / `save_persistent_state` / `process_frame await`).

2. **触发场景** — 任何 1 个 polish 任务涉及"加新 1 room 切换"或"修 1 跨 scene 状态"时触发. 跨 5 个典型 polish 任务类别: (i) **新加 1 archive 房间** (e.g. 
T223 #163 `archive_05` 教学房间, 走 Hub → archive_05 transition, 需在 `room_door` 配 `target_room_path = 
"res://src/scenes/archive_05.tscn"` + `target_spawn_point = Vector2(...)` + GFC `_ready` 走 5 段 1:1 序列); (ii) **新加 1 BGM 
preset** (e.g. T107 #76 `archive_storm`, 需在 `audio_manager_enhanced` 注册 preset + `room_scene.gd._on_room_entered` 路由 
trigger 调 `_play_music_for_state(PLAYING)`); (iii) **修 1 玩家死亡回 Hub 路径** (e.g. T079 #56 死亡回 safe room, 需 `player_dead` 
signal 桥接 GFC `_on_player_died` → `change_scene_to_file(hub_path)` + Hub `_ready` 走 5 段 1:1 序列, T079 修 `_ready` 顺序 bug);
 (iv) **修 1 跨 scene 状态传递** (e.g. T070 #49 持久化磁盘版, 需 `GameState.save_persistent_state` + `GameState._is_transitioning` 跨 
scene 持久化, 0 漏 0 反序); (v) **修 1 transition 时序问题** (e.g. T052 #37 `call_deferred` 修, 需 `await get_tree().process_frame` 
在 `fade_in` 之前, 0 触碰 `_room_transition` 自身 `_ready`). 5 类触发场景根因**全部**是 5 段序列任意 1 段 0 走或漏 1 字段.

3. **修复** — 严格按 5 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段序列 1:1 (T053 #38 + T070 #49 + T079 #56 + T107 #76 + 
T223 #163 12 任务 158 轮落地后定稿, 0 例外): **Stage 1 触发检测** — `room_door.gd._on_body_entered(body)` 检测 
`body.is_in_group("player")` → emit `player_entered(target_room_path)` signal (T053 落地, `room_door` 是 Hub ↔ archive 
唯一入口, 0 走 player.gd 直接 transition, 0 走单例 Manager). **Stage 2 GFC 状态转换** — 
`game_flow_controller.gd._on_door_entered(path)` 或 `_on_door_with_spawn_entered(path, spawn_point)` → 存 
`GameState._pending_room_path = path` + `GameState._pending_spawn_point = spawn_point` (T053 + T079 跨 scene 字段传递契约, 2 
字段名 0 改 0 重命名) → `_enter_state(State.ROOM_TRANSITION)`. **Stage 3 fade_out** — `await _room_transition.fade_out(0.4)` 
(T053 落地, 0.4s 短于玩家感知延迟, 长于 1 frame jitter) → emit `transition_finished` signal 触发 `Stage 4`. **Stage 4 场景切换** — 
`GameState.save_persistent_state()` (T070 #49 持久化磁盘版, 写 `user://save_data.json`, 0 走 in-memory 缓存, 跨 scene 后 `_ready` 0 
还原) → `GameState._is_transitioning = true` (T079 跨 scene 持久化状态, 新 scene `_ready` 检查此字段才走 `_recover_from_transition`, 0 
走 is_hub_mode 单点判断 0 区分 hub/archive) → `get_tree().change_scene_to_file(_pending_room_path)` (Godot 4.6 SceneTree 原子切换, 
0 走 `change_scene` 旧 API). **Stage 5 新 scene 恢复** — 新 scene `GFC._ready` 调用 `_recover_from_transition()` (T079 #56 修复后, 
关键: 检查 `GameState._is_transitioning` 在 `is_hub_mode` 之前, 0 漏 0 反序) → 
`player.respawn_at(GameState._pending_spawn_point)` (T053 落地, `respawn_at` 是 player.gd 唯一入口, 0 走 `position = ...` 直接赋值, 
跨 `set_speed_multiplier(1.0)` 1:1 调用) → `GameState.restore_persistent_state()` (T070 1:1 镜像 `save_persistent_state`, 0 
走 `_ready` 顶部 0 走 `change_scene` 之前) → `await get_tree().process_frame` (T052 #37 `call_deferred` 修, 让 
`_room_transition` 通过 `call_deferred("add_child", ...)` 在 `_ready` 之后 1 帧落地, `ColorRect.modulate` 属性存在性保证) → 
`_room_transition.fade_in(0.5)` (T053 落地, 0.5s 长于 0.4s fade_out, 视觉连贯 0 闪 1 帧) → `_enter_state(State.PLAYING)` (T053 落地,
 状态机恢复 0 停留在 ROOM_TRANSITION). 5 段 1:1 严格, 跨 5 个 room + 5 个 BGM preset 0 例外.

4. **预防** — 任何 polish 期给"加新 1 room 切换"或"修 1 跨 scene 状态"任务加新字段 / 加新方法 / 加新 BGM preset 时**必须**严格按 5 段 canonical 1:1 序列 
1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 漏 `is_in_group("player")` 守卫** (0 误触发 non-player body), **Stage 2 0 
改 `_pending_room_path` / `_pending_spawn_point` 2 字段名** (T053 + T079 跨 scene 字段传递契约, 改 1 字符破坏 `change_scene_to_file` 跨 
scene 持久化, 0 走 `set_meta` / `set("path", ...)` Godot 反射, 0 走 player.gd 内部 `position`), **Stage 3 0 触碰 0.4s fade_out 
持续时间** (T053 落地时 0.4s 短于玩家感知, 改 0.2s 闪 1 帧改 0.6s 玩家感知卡顿), **Stage 4 0 反序 `save_persistent_state` / `_is_transitioning = 
true` / `change_scene_to_file` 3 行** (T070 + T079 落地后顺序 1:1 严格, 0 漏 `save_persistent_state` 跨 scene 持久化 0 还原, 0 漏 
`_is_transitioning = true` 新 scene `_ready` 0 走 `_recover_from_transition`), **Stage 5 0 漏 `await 
get_tree().process_frame` 在 `fade_in` 之前** (T052 `call_deferred` 修复 0 触碰, 0 触碰 `_room_transition` 自身 `_ready`, 0 走 
`is_inside_tree` 单点判断 0 走 `await get_tree().physics_frame` 物理帧). 任何"加新 1 BGM preset"必须**先**在 
`audio_manager_enhanced.gd` 注册 preset, **再**在 `room_scene.gd._on_room_entered` 路由 trigger 调 
`_play_music_for_state(PLAYING)`, 0 触碰 GFC 5 段 1:1 序列 (BGM 切换是 5 段序列的"Stage 4 副作用", 0 走 GFC 内部 `_play_music_for_state` 
0 走 scene.tscn autoplay). 任何"加新 1 room"必须**先**创建 `src/scenes/<room_name>.tscn` (含 player + door + enemy + ...), **再**在 
`room_door.target_room_path` 配 `<room_name>.tscn` 资源路径, **再**在 GFC 5 段序列 1:1 复制 (0 改 Stage 1-5 任何字段任何方法任何顺序). T053 + 
T070 + T079 + T107 + T223 12 任务 158 轮落地后 0 漂移, 跨 5 个 room + 5 个 BGM preset 100% 1:1 严格. F013.E 0 触碰本段 (本段聚焦 cross-room 
transition, 不重叠 §9.6.19 verb windup VFX 类层级共享契约).

**5 段 canonical 1:1 序列总表** (T053 + T070 + T079 + T107 + T223 12 任务 158 轮落地后定稿, 5 段 1:1 严格, 跨 5 room + 5 BGM preset 0 
例外):

| 段 | 1:1 序列 | 落地任务 | 关键字段 / 方法 | 0 触碰边界 | 0 反序依赖 |
|------|------|------|------|------|------|
| Stage 1 触发检测 | `room_door._on_body_entered` → `is_in_group("player")` 守卫 → `emit_signal("player_entered", path)` | 
| T053 #38 | `target_room_path: String` (export var, 0 走 hardcode) | 0 漏 player 守卫, 0 走 player.gd 直接 transition | 0 触碰 
| Stage 2-5 |
| Stage 2 GFC 状态转换 | `_on_door_with_spawn_entered(path, spawn)` → `GameState._pending_room_path = path` + 
| `GameState._pending_spawn_point = spawn` → `_enter_state(ROOM_TRANSITION)` | T053 #38 + T079 #56 | 2 字段名 0 改 0 重命名 
| (T053 + T079 跨 scene 字段传递契约) | 0 漏 2 字段写入, 0 走 `set_meta` / `set("path", ...)` | `_enter_state(ROOM_TRANSITION)` 必须在 
| 2 字段写入之后 |
| Stage 3 fade_out | `await _room_transition.fade_out(0.4)` → emit `transition_finished` signal | T053 #38 | 0.4s 持续时间 
| 0 改 (T053 落地值, 短于玩家感知长于 1 frame jitter) | 0 触碰 `_room_transition._ready` / 0 走 `modulate.a = 0.0` 直接赋值 | `await` 阻塞必须,
|  0 走 `fade_out(0.4); emit_signal(...)` 非阻塞 |
| Stage 4 场景切换 | `GameState.save_persistent_state()` → `GameState._is_transitioning = true` → 
| `get_tree().change_scene_to_file(path)` | T070 #49 + T079 #56 + T107 #76 | `user://save_data.json` 持久化磁盘版 (T070 0 走 
| in-memory) | 0 漏 `save_persistent_state` 持久化, 0 漏 `_is_transitioning` 跨 scene 标志 | 3 行顺序 1:1 严格, 0 反序 0 漏 |
| Stage 5 新 scene 恢复 | `if GameState._is_transitioning: _recover_from_transition()` → 
| `player.respawn_at(_pending_spawn_point)` → `GameState.restore_persistent_state()` → `await get_tree().process_frame` 
| → `_room_transition.fade_in(0.5)` → `_enter_state(PLAYING)` | T052 #37 + T053 #38 + T070 #49 + T079 #56 | 
| `_recover_from_transition` 是新 scene `_ready` 唯一入口 (T079 修 `_ready` 顺序 bug) | 0 漏 `process_frame await` 在 `fade_in` 之前 
| (T052 `call_deferred` 修), 0 走 `is_hub_mode` 单点判断 0 区分 hub/archive | `_recover_from_transition` 内 5 行顺序 1:1 严格 |

**5 段序列跨任务历史落地表** (T053 → T269 12 任务 158 轮, 5 段 1:1 0 例外 0 漂移):

| 任务 | 轮次 | 落地段 | 关键修复 / 扩展 | 0 触碰边界 |
|------|------|------|------|------|
| T053 Hub ↔ archive 双向闭环 | #38 | Stage 1-3 + Stage 5 基线 | 5 段序列 0→1 落地, `room_door` + GFC + `_room_transition` 0→1 创建 |
|  0 触碰 player.gd 内部 `position`, 0 走单例 Manager |
| T063 BGM 场景集成 | #43 | Stage 4 副作用扩展 | `_play_music_for_state(PLAYING)` 调点 (Stage 4 之后) | 0 触碰 5 段序列, BGM 是 Stage 4 
| 副作用 |
| T070 存档系统持久化磁盘版 | #49 | Stage 4 持久化 | `GameState.save_persistent_state` + `restore_persistent_state` 1:1 镜像 | 0 走 
| in-memory 缓存, 0 走 user:// 之外路径 |
| T078 Boss 音乐 override 引用计数 | #55 | Stage 4 副作用 | Boss BGM override 引用计数 (0 走 SceneTree autoplay) | 0 触碰 5 段序列, Boss 是 
| Stage 4 副作用 + 引用计数 |
| T079 玩家死亡后重生点 | #56 | Stage 4 + Stage 5 关键修 | **`_ready` 顺序 bug 修**: 检查 `GameState._is_transitioning` 在 `is_hub_mode` 
| 之前, 0 漏 0 反序 | 0 漏 `process_frame await` 在 `fade_in` 之前 (T052 已修) |
| T081 atmosphere bell 修复 | #57 | Stage 5 副作用扩展 | 0.8s 暖光回流 (Hub → archive transition 1:1 镜像 `fade_in` 0.5s) | 0 触碰 5 
| 段序列 |
| T107 第 7 主题 BGM `archive_storm` | #76 | Stage 4 副作用扩展 | `archive_storm` BGM preset 注册 + `room_scene._on_room_entered` 
| 路由 trigger | 0 触碰 5 段序列 |
| T114 `silence_void` BGM preset | #82 | Stage 4 副作用扩展 | `silence_void` BGM preset (0 触碰 5 段序列) | 0 触碰 5 段序列 |
| T123 `whisper_hollow` 路由 | #88 | Stage 4 副作用扩展 | `whisper_hollow` BGM preset 路由 trigger | 0 触碰 5 段序列 |
| T156 ArchiveStorm 主摄像机 shake | #112 | Stage 5 副作用扩展 | 1f skybox rotate trigger (Stage 5 `_recover_from_transition` 之后 
| 1f) | 0 触碰 5 段序列, shake 是 Stage 5 副作用 |
| T223 `archive_05` Wave 0.5× 教学房间 | #163 | Stage 1-5 1:1 复制 | `archive_05.tscn` 0→1 创建, `room_door.target_room_path` 配 
| `archive_05.tscn` | 0 触碰 5 段序列任何字段任何方法任何顺序 |
| T269 `archive_05` 灰盒 + 内容扩展 | #192 | Stage 5 副作用扩展 | `archive_05` 灰盒 + 0.5× Wave + content 扩展 | 0 触碰 5 段序列 |

**5 段序列常见 anti-pattern 表** (4 类症状根因, 12 任务 158 轮 0 触碰, 任何"加新 1 room 切换"或"修 1 跨 scene 状态"必须 0 触碰 0 反序 0 漏 0 改 1 字段):

| anti-pattern | 症状 | 根因 | T053-T269 落地后 1:1 严格 | 0 触碰边界 |
|------|------|------|------|------|
| 漏 Stage 2 `_pending_spawn_point` 写入 | 跨 scene 后玩家回旧 scene `last_known_position` 0 走 `_pending_spawn_point` | Stage 2 
| `_on_door_with_spawn_entered` 0 调, 改用 `_on_door_entered` 0 传 spawn | T079 #56 修后 `_pending_spawn_point` 1:1 写入, 跨 5 
| room 0 例外 | 0 改 2 字段名, 0 走 `set_meta` / `set("path", ...)` |
| Stage 4 漏 `save_persistent_state` | 跨 scene 后 health / max_health / coins / current_verb 0 还原 | 
| `change_scene_to_file` 之前 0 调 `GameState.save_persistent_state()` | T070 #49 落地下 1:1 严格 3 行顺序, 跨 5 room 0 例外 | 0 反序 
| `save_persistent_state` / `_is_transitioning` / `change_scene_to_file` |
| Stage 4 漏 `_is_transitioning = true` | 新 scene `_ready` 0 走 `_recover_from_transition`, 玩家在旧 scene 
| `last_known_position` 0 还原 | `change_scene_to_file` 之前 0 设 `GameState._is_transitioning = true` | T079 #56 修后 1:1 严格, 
| 跨 5 room 0 例外 | 0 走 `is_hub_mode` 单点判断 0 区分 hub/archive |
| Stage 5 漏 `await get_tree().process_frame` | `_room_transition.fade_in(0.5)` 报 "Required object 'rp_target' is null", 
| 黑屏闪 1 帧 | `_room_transition` 通过 `call_deferred("add_child", ...)` 在 `_ready` 之后 1 帧落地, `ColorRect.modulate` 属性 0 存在 | 
| T052 #37 修后 1:1 严格, 跨 5 room 0 例外 | 0 触碰 `_room_transition._ready`, 0 走 `await get_tree().physics_frame` 物理帧 |

**与 §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 关系** — §9.6.16 覆盖 player.gd 6 verb hit handler 5 件套 1:1 严格分离 (verb ability 
`.trigger_player_hit` → 5 件套 `_on_verb_hit_pulse` / `_on_verb_hit_bind` / `_on_verb_hit_cut` / `_on_verb_hit_echo` / 
`_on_verb_hit_wave`), §9.6.17 覆盖 player.gd `_ready` 5+1 件套 signal 桥接 1:1 严格分离 (5 verb `verb_triggered` signal + 1 verb 
Whisper 业务特例 `whisper_ability_executed` signal), §9.6.18 覆盖 `_verb_ability_base.gd` 6 verb ability 子类共享契约 (6 字段 + 1 
`@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 兜底 + 1 `is_globally_blocking` 扩展 = 16 件 1:1 严格分离), §9.6.19 覆盖 
`_verb_windup_vfx_base.gd` 6 verb windup VFX 子类共享契约 (3 字段 + 1 `_ready()` + 1 `_process(delta)` + 1 
`_activate_windup_tween()` + 1 `fade_out_and_free()` = 7 件 1:1 严格分离). §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow 5 段 
canonical 1:1 序列 (Stage 1 触发检测 + Stage 2 GFC 状态转换 + Stage 3 fade_out + Stage 4 场景切换 + Stage 5 新 scene 恢复). §9.6.16 / 
§9.6.17 / §9.6.18 / §9.6.19 关注"verb ability + windup VFX 类层级共享契约 + signal 桥接", §9.6.20 关注"跨 scene transition flow 
时序契约". 5 套 polish 模式 0 反序 0 漏 0 改 1 字符. 任何"加新 1 room"必须**先**按 §9.6.20 Stage 1-5 1:1 复制 (0 反序 0 漏 0 改 1 字段), **再**按 §9.1 
9 步走 "node" 步落地 (含 `room_scene` / `room_door` / `hub` / `archive_xx` 节点), 0 触碰 §9.6.16-§9.6.19 任何 verb ability / windup 
VFX 类层级共享契约 (verb 子类 lifecycle 在本 scene 内 0 跨 scene, transition 是 1:1 时序契约 0 触碰 verb 内部 lifecycle). §9.6.20 是 5 套 
polish 模式**唯一**关注"跨 scene 持久化 + 状态机恢复 + fade 时序"的 1 套, 与 §9.6.16-§9.6.19 4 套 verb / VFX 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.20 5 段序列走 §9.1 9 步落地的"node" 步 (`src/scenes/room_door.gd` + 
`src/scripts/game_flow_controller.gd` + `src/scripts/room_transition.gd` + `src/scenes/room_scene.gd` + 
`src/scenes/hub.tscn` + `src/scenes/archive_xx.tscn` + `src/scripts/audio_manager_enhanced.gd`), 0 触碰 verb ability / 
windup VFX 类层级共享契约 (verb 子类 lifecycle 在本 scene 内 0 跨 scene, 5 段序列 0 触碰 verb 内部 lifecycle). 任何"加新 1 room"必须**先**按 §9.1 9 
步落地 (含 `room_door` + `hub` / `archive_xx` 节点), **再**按 §9.6.20 5 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 任何"加新 1 BGM 
preset"必须**先**在 `audio_manager_enhanced.gd` 注册 preset (走 §9.1 9 步 "audio" 步), **再**在 `room_scene.gd._on_room_entered` 
路由 trigger 调 `_play_music_for_state(PLAYING)`, 0 触碰 GFC 5 段 1:1 序列 (BGM 切换是 5 段序列的"Stage 4 副作用", 0 走 GFC 内部 
`_play_music_for_state` 0 走 scene.tscn autoplay). 0 触碰 §9.1 9 步任何 const / var / 节点 (5 段序列仅在 GFC + `room_transition` + 
`room_door` 3 文件, 6 verb 子类 0 改 base 0 改, 含 1 verb Whisper T275 #194 收回完成).


### 9.6.21 6 verb 成就系统 闭环 polish 模式 (跨 5 任务 ~146 轮 落地)

> T094 (#51) `quadruple_voice` 4/6 成就 (3 verb → 4 verb closure) → T103 (#74) `quintuple_voice` 5/6 成就 (4 verb → 5 verb 
> closure) → T241 (#159) F013.E 6 verb 接入路径 `Whisper` 静默场 + `sextuple_voice` 6/6 成就 (5 verb → 6 verb closure) → T245 
> (#162) `whisper_icon` 落地 (Muted Mauve #C8A4D8) + `ICON_COLORS` 6 verb 调色六元组登记 `sextuple_voice` → T246 (#163) 5 verb 
> 旧成就 PNG 双路径补全 (`quadruple_voice=echo_icon` + `quintuple_voice=wave_icon` 走 `assets/ui/<verb>_icon/` + 
> `assets/ui/achievements/<verb>_icon/` 双路径, 1:1 严格). 跨 5 任务 146 轮迭代落地, 8 段 canonical 1:1 序列, 0 漏 0 改 1 字段, 任何"加新 1 
> verb"必须**先**按 §9.1 9 步落地, **再**按 §9.6.21 8 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 跨 6 verb (`pulse` / `bind` / `cut` / 
> `echo` / `wave` / `whisper`) + 4 tier 成就 (`triple_voice` / `quadruple_voice` / `quintuple_voice` / `sextuple_voice`) 
> + 3 verb icon 关联 (`echo_icon` / `wave_icon` / `whisper_icon`) + 1 个共享 `all_abilities_used` 条件类型 + 1 调色六元组 (`Pulse 
> Coral` / `Bind Violet` / `Cut Amber` / `Echo Cyan` / `Wave Pale` / `Whisper Mauve`) 全部走同一套 8 段 1:1 序列 0 例外.

1. **症状** — polish 期任意 1 个新 verb 接入成就系统 (e.g. T241 #159 `whisper` 第 6 verb) 出现 6 类症状之一: (a) **成就触发计数错** — 
`record_ability_used("whisper")` 0 调, `whisper_used` 永远 0, `sextuple_voice` 永不触发 (T241 #159 落地下 1:1 严格, 6 branch 0 漏, 
`whisper` 分支走 `record_stat("whisper_used", 1)`), 或 `record_ability_used` 调了但分支名 0 改 `"whisper"` (字段名 0 改, 改 
`"whisper_ability"` 等同破坏 §9.1 9 步 "const" 步 verb 名字段契约, 0 走 `set_meta("verb_name", ...)` 反射); (b) **成就 UI 颜色 fallback** 
— `sextuple_voice.icon_hint = "whisper_icon"` 但 `ICON_COLORS` 0 登记 `whisper_icon` entry (T245 #162 落地下 1:1 严格, 6 verb 
调色六元组 0 漏 0 改, fallback `Color(0.949, 0.714, 0.431, 1.0)` 6 verb 关联成就 0 走), 或 `ICON_COLORS` 登记了 `whisper_icon` 但 
`Color` 值 0 改 `Color(0.784, 0.643, 0.847, 1.0)` (T245 #162 落地 Muted Mauve #C8A4D8, 改 1 通道破坏 §9.6.4 调色六元组 + 0 走 §9.6.4 
HUD 6 行 6 色色域分工); (c) **成就 PNG 加载失败** — `quadruple_voice.icon_hint = "echo_icon"` 但 
`assets/ui/achievements/echo_icon.png` 0 创建 (T246 #163 落地下 1:1 严格, 3 verb 关联成就 PNG 双路径 0 漏, 
`assets/ui/echo_icon/echo_icon.png` + `assets/ui/achievements/echo_icon/echo_icon.png` 1:1 双路径), 或 PNG 路径走 
`assets/ui/echo_icon/icon.png` 0 走 `<verb>_icon/<verb>_icon.png` (T246 #163 落地路径 1:1 严格, 改路径名破坏 §9.1 9 步 "icon" 步 verb 
icon 资源契约); (d) **成就条件类型漂移** — `all_abilities_used` 条件从 5 verb 扩到 6 verb 漏 `whisper_used >= 1` 检查 (T241 #159 落地下 1:1 严格,
 `pulse_used >= 1 and bind_used >= 1 and cut_used >= 1 and echo_used >= 1 and wave_used >= 1 and whisper_used >= 1` 6 项 
0 漏 0 反序), 或条件从 `and` 改 `or` (破坏"全 6 verb 至少用 1 次"语义, 0 走 `sum >= 6` 累积阈值 0 走 `count >= 1` 计数 0 区分); (e) **新 run 
状态不重置** — `reset_run()` 漏 `whisper_used = 0` (T241 #159 落地下 1:1 严格, 6 verb 0 漏 0 反序, reset 顺序 1:1 严格), 或 `reset_run` 
调了但调点 0 走 `player.gd._ready` 调走 `_on_run_started` signal (T094 #51 落地后 1:1 严格, `player.gd._ready` 调 
`PlayerStats.reset_run()` 是唯一入口); (f) **成就 tier 命名漂移** — `sextuple_voice` 改 `whisper_master` (T241 #159 落地下 1:1 严格, 4 
tier 成就名 0 改 0 重命名, `triple_voice` / `quadruple_voice` / `quintuple_voice` / `sextuple_voice` 1:1 严格, 改 1 字符破坏 
`achievements.json` ID 契约 + 0 走 i18n 翻译键). 6 类症状的根因**全部**是 8 段序列任意 1 段 0 走或反序或漏 1 字段 (`<verb>_used` / 
`record_ability_used` / `get_stat` / `set_stat` / `reset_run` / `all_abilities_used` / `icon_hint` / `ICON_COLORS` + 
PNG 双路径).

2. **触发场景** — 任何 1 个 polish 任务涉及"加新 1 verb 接入成就系统"或"修 1 成就条件类型"或"修 1 成就 UI 颜色"时触发. 跨 5 个典型 polish 任务类别: (i) **新加 1 verb 
接入成就系统** (e.g. T241 #159 `whisper` 第 6 verb, 需在 `PlayerStats` 加 1 字段 `whisper_used` + `record_ability_used` 加 1 分支 
`whisper` + `get_stat` 加 1 entry `whisper_used` + `set_stat` 加 1 entry `whisper_used` + `reset_run` 加 1 行 `whisper_used 
= 0` + `all_abilities_used` 条件加 1 项 `whisper_used >= 1` + `achievements.json` 加 1 entry `sextuple_voice` + 
`ICON_COLORS` 加 1 entry `whisper_icon` + `assets/ui/whisper_icon/` + `assets/ui/achievements/whisper_icon/` 1:1 双路径, 跨 
8 段 0 漏 0 反序); (ii) **新加 1 tier 成就** (e.g. T094 #51 `quadruple_voice` 4 verb tier, 需在 `achievements.json` 加 1 entry + 1 
个 verb icon 关联 + 1 个 PNG 双路径, 0 触碰既有 5 verb `all_abilities_used` 6 项检查); (iii) **修 1 成就 UI 颜色** (e.g. T245 #162 
`whisper_icon` Muted Mauve #C8A4D8, 需在 `ICON_COLORS` 加 1 entry + `Color` 通道值 0 改 §9.6.4 调色六元组, 0 触碰既有 5 verb entry); 
(iv) **修 1 成就 PNG 路径** (e.g. T246 #163 5 verb 旧成就 PNG 补全, 需 `assets/ui/<verb>_icon/<verb>_icon.png` + 
`assets/ui/achievements/<verb>_icon/<verb>_icon.png` 1:1 双路径 0 漏, 0 触碰 §9.1 9 步 "icon" 步 verb icon 资源契约); (v) **修 1 
成就条件** (e.g. T241 #159 6 verb `all_abilities_used` 6 项检查 0 漏 0 反序, 需 `pulse_used >= 1 and ... and whisper_used >= 1` 6 
项 0 漏 0 改 1 字段, 0 走 `sum >= 6` 累积阈值 0 走 `count >= 1` 计数 0 区分). 5 类触发场景根因**全部**是 8 段序列任意 1 段 0 走或漏 1 字段.

3. **修复** — 严格按 8 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 8 段序列 1:1 (T094 #51 + T103 #74 + T241 #159 + T245 #162 
+ T246 #163 5 任务 146 轮落地后定稿, 0 例外): **Stage 1 PlayerStats 字段声明** — `var whisper_used: int = 0` (T241 #159 落地, 与 5 verb 
`pulse_used` / `bind_used` / `cut_used` / `echo_used` / `wave_used` 1:1 同构, 6 字段名 0 改 0 重命名, 0 走 `set_meta("verb_used", 
...)` 反射). **Stage 2 `record_ability_used` 分支** — `match verb_name:` 加 1 分支 `"whisper": record_stat("whisper_used", 1)` 
(T241 #159 落地, 6 branch 0 漏, 分支名 0 改 `"whisper"` 0 改 `"whisper_ability"`, 0 走 default 分支 fallthrough). **Stage 3 
`get_stat` entry** — `match stat_name:` 加 1 entry `"whisper_used": return whisper_used` (T241 #159 落地, 6 entry 0 漏, 0 改 
`return pulse_used` 等既有 entry). **Stage 4 `set_stat` entry** — `match stat_name:` 加 1 entry `"whisper_used": 
whisper_used = value` (T241 #159 落地, 6 entry 0 漏, 0 改 `pulse_used = value` 等既有 entry). **Stage 5 `reset_run` 重置** — 加 1 
行 `whisper_used = 0` (T241 #159 落地, 6 行 0 漏 0 反序, reset 顺序 1:1 严格, 0 触碰 `health` / `coins` 等非 verb 字段). **Stage 6 
`all_abilities_used` 条件** — `return pulse_used >= 1 and bind_used >= 1 and cut_used >= 1 and echo_used >= 1 and 
wave_used >= 1 and whisper_used >= 1` 6 项检查 0 漏 0 反序 (T241 #159 落地, 6 项 `and` 1:1 严格, 0 走 `sum >= 6` 累积阈值 0 走 `count >= 
1` 计数 0 区分, 0 改 `>= 1` 为 `>= 2` 破坏"至少 1 次"语义). **Stage 7 `achievements.json` tier entry** — 加 1 entry `{ "id": 
"sextuple_voice", "condition_type": "all_abilities_used", "icon_hint": "whisper_icon", "title_key": 
"achievement_sextuple_voice", "description_key": "achievement_sextuple_voice_desc" }` (T241 #159 落地, 4 tier 成就 ID 0 改 0 
重命名, `triple_voice` / `quadruple_voice` / `quintuple_voice` / `sextuple_voice` 1:1 严格, 改 1 字符破坏 ID 契约 + 0 走 i18n 翻译键). 
**Stage 8 `ICON_COLORS` entry + PNG 双路径** — `ICON_COLORS` 加 1 entry `"whisper_icon": Color(0.784, 0.643, 0.847, 1.0)` 
Muted Mauve #C8A4D8 (T245 #162 落地, 6 verb 调色六元组 0 漏 0 改 1 通道值, 0 走 fallback `Color(0.949, 0.714, 0.431, 1.0)` 6 verb 
关联成就 0 走) + 创建 `assets/ui/whisper_icon/whisper_icon.png` + `assets/ui/achievements/whisper_icon/whisper_icon.png` 1:1 
双路径 (T246 #163 落地, 3 verb 关联成就 PNG 双路径 0 漏, 0 改 `<verb>_icon/<verb>_icon.png` 路径名, 0 走 `assets/ui/echo_icon/icon.png` 0 
区分 verb). 8 段 1:1 严格, 跨 6 verb + 4 tier 成就 + 3 verb icon 关联 0 例外.

4. **预防** — 任何 polish 期给"加新 1 verb 接入成就系统"或"修 1 成就条件"或"修 1 成就 UI 颜色"任务加新字段 / 加新方法 / 加新 tier 成就时**必须**严格按 8 段 canonical 
1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 8 段 0 触碰边界: **Stage 1 0 改 6 verb `<verb>_used` 字段名** (T241 #159 落地后字段名 1:1 严格, 
`pulse_used` / `bind_used` / `cut_used` / `echo_used` / `wave_used` / `whisper_used` 6 字段名 0 改 0 重命名, 改 1 字符破坏 
`record_ability_used` 6 branch + `get_stat` 6 entry + `set_stat` 6 entry + `reset_run` 6 行 + `all_abilities_used` 6 项检查 
+ `achievements.json` `icon_hint` 字段引用), **Stage 2 0 改 `"whisper"` 分支名** (T241 #159 落地后分支名 1:1 严格, 改 
`"whisper_ability"` 等同破坏 `WhisperAbility._execute_whisper()` 调点 + 0 走 default 分支 fallthrough 静默失败), **Stage 3-4 0 改 
`"whisper_used"` entry key** (T241 #159 落地后 entry key 1:1 严格, 改 `"whisper_count"` 等同破坏 `record_stat("whisper_used", 1)` 
调点), **Stage 5 0 反序 6 verb reset 顺序** (T241 #159 落地后顺序 1:1 严格, `pulse_used` / `bind_used` / `cut_used` / `echo_used` / 
`wave_used` / `whisper_used` 6 行顺序 1:1, 0 漏 0 反序, 0 触碰 `health` / `coins` 等非 verb 字段), **Stage 6 0 改 `>= 1` 阈值** (T241 
#159 落地后阈值 1:1 严格, 改 `>= 2` 破坏"至少 1 次"语义, 0 走 `sum >= 6` 累积阈值 0 走 `count >= 1` 计数 0 区分), **Stage 7 0 改 4 tier 成就 ID** 
(T094 + T103 + T241 落地后 ID 1:1 严格, `triple_voice` / `quadruple_voice` / `quintuple_voice` / `sextuple_voice` 4 ID 0 改 0 
重命名, 改 1 字符破坏 ID 契约 + 0 走 i18n 翻译键), **Stage 8 0 改 `Color` 通道值** (T245 #162 落地后 Muted Mauve #C8A4D8 `Color(0.784, 0.643,
 0.847, 1.0)` 0 改 1 通道, 0 触碰 §9.6.4 调色六元组 + §9.6.4 HUD 6 行 6 色色域分工) + 0 改 `<verb>_icon/<verb>_icon.png` PNG 路径名 (T246 
#163 落地后路径 1:1 严格, `assets/ui/<verb>_icon/<verb>_icon.png` + `assets/ui/achievements/<verb>_icon/<verb>_icon.png` 1:1 
双路径, 改路径名破坏 §9.1 9 步 "icon" 步 verb icon 资源契约). 任何"加新 1 verb 接入成就系统"必须**先**按 §9.1 9 步落地 (含 `verb` (1) + `ability` (2) + 
`const` (3) + `icon` (6) 步), **再**按 §9.6.21 8 段序列 1:1 复制 (Stage 1-8 0 反序 0 漏 0 改 1 字段). 任何"加新 1 tier 成就"必须**先**在 
`achievements.json` 加 1 entry (Stage 7 1:1 严格), **再**在 `ICON_COLORS` 加 1 verb icon 关联 (Stage 8 0 改 1 通道值) + 1:1 双路径 PNG 
(Stage 8 0 改路径名), 0 触碰既有 5 verb `all_abilities_used` 6 项检查 (Stage 6 0 反序 0 漏 0 改 1 阈值). 任何"修 1 成就 UI 颜色"必须**先**在 §9.6.4 
调色六元组加 1 entry, **再**在 `ICON_COLORS` 加 1 entry (Stage 8 0 改 1 通道值), 0 触碰既有 5 verb entry. 任何"修 1 成就 PNG 路径"必须**先**创建 
`assets/ui/<verb>_icon/<verb>_icon.png`, **再**创建 `assets/ui/achievements/<verb>_icon/<verb>_icon.png` 1:1 双路径 (Stage 8 
0 改路径名), 0 触碰 §9.1 9 步 "icon" 步 verb icon 资源契约. 任何"修 1 成就条件"必须**先**在 `all_abilities_used` 6 项检查加 1 项 (Stage 6 0 反序 0 漏 
0 改 1 阈值), **再**在 `achievements.json` 引用 1 个 verb icon 关联 (Stage 7 0 改 ID), 0 触碰既有 `and` 语义. 已知 drift risk: 
`_stat_names()` 数组 (T190 落地, 用于 `get_all_stats` debug 接口) **可能**漏 `whisper_used` entry (T241 #159 落地下 `_stat_names()` 0 
触碰, `_stat_names()` 是 debug 接口 0 影响游戏逻辑, 但若未来给 `_stat_names()` 加 1 个消费方 (e.g. debug UI 列表) 必须**先**检查 6 verb entry 0 漏 0 
反序, 0 漏 1 边). T094 + T103 + T241 + T245 + T246 5 任务 146 轮落地后 0 漂移, 跨 6 verb + 4 tier 成就 + 3 verb icon 关联 100% 1:1 严格. 
F013.E (#159) 6 verb 接入路径是 §9.6.21 8 段序列的"母集" (1 verb 6 字段, §9.6.21 8 段 1 verb 完整闭环, 6 verb × 8 段 = 48 件 1:1 严格).

**8 段 canonical 1:1 序列总表** (T094 + T103 + T241 + T245 + T246 5 任务 146 轮落地后定稿, 8 段 1:1 严格, 跨 6 verb + 4 tier 成就 + 3 verb 
icon 关联 0 例外):

| 段 | 1:1 序列 | 落地任务 | 关键字段 / 方法 | 0 触碰边界 | 0 反序依赖 |
|------|------|------|------|------|------|
| Stage 1 PlayerStats 字段声明 | `var <verb>_used: int = 0` (6 verb × 1 字段 = 6 字段) | T241 #159 | `<verb>_used` 字段名 0 改 0 
| 重命名 | 0 走 `set_meta("verb_used", ...)` 反射, 0 走 enum | Stage 2-6 全部依赖 6 字段存在 |
| Stage 2 `record_ability_used` 分支 | `match verb_name:` 加 1 分支 `"<verb>": record_stat("<verb>_used", 1)` (6 branch) | 
| T241 #159 | 分支名 0 改 `<verb>` 0 改 `<verb>_ability` | 0 漏 1 分支, 0 走 default fallthrough | 必须在 Stage 1 字段声明之后 |
| Stage 3 `get_stat` entry | `match stat_name:` 加 1 entry `"<verb>_used": return <verb>_used` (6 entry) | T241 #159 | 
| entry key 0 改 `<verb>_used` 0 改 `<verb>_count` | 0 漏 1 entry, 0 改既有 5 verb entry | 必须在 Stage 1 字段声明之后 |
| Stage 4 `set_stat` entry | `match stat_name:` 加 1 entry `"<verb>_used": <verb>_used = value` (6 entry) | T241 #159 | 
| entry key 0 改 `<verb>_used` 0 改 `<verb>_count` | 0 漏 1 entry, 0 改既有 5 verb entry | 必须在 Stage 1 字段声明之后 |
| Stage 5 `reset_run` 重置 | 加 1 行 `<verb>_used = 0` (6 行) | T241 #159 | reset 顺序 1:1 严格 (pulse → bind → cut → echo → 
| wave → whisper) | 0 反序 6 行, 0 漏 1 行, 0 触碰 `health` / `coins` 等非 verb 字段 | 必须在 Stage 1 字段声明之后 |
| Stage 6 `all_abilities_used` 条件 | `return pulse_used >= 1 and bind_used >= 1 and cut_used >= 1 and echo_used >= 1 and 
| wave_used >= 1 and whisper_used >= 1` (6 项 `and`) | T241 #159 | `>= 1` 阈值 0 改, 0 走 `sum >= 6` 累积阈值 | 0 反序 6 项, 0 漏 1 
| 项, 0 改 `and` 为 `or` | 必须在 Stage 1 字段声明之后 |
| Stage 7 `achievements.json` tier entry | 加 1 entry `{ "id": "sextuple_voice", "condition_type": "all_abilities_used", 
| "icon_hint": "whisper_icon", "title_key": ..., "description_key": ... }` (4 tier 成就) | T094 #51 + T103 #74 + T241 
| #159 | 4 tier ID 0 改 0 重命名, `triple_voice` / `quadruple_voice` / `quintuple_voice` / `sextuple_voice` | 0 改 ID 破坏 
| i18n 翻译键, 0 走 `condition_type` 自定义类型 | 必须在 Stage 6 条件之后 |
| Stage 8 `ICON_COLORS` entry + PNG 双路径 | `ICON_COLORS` 加 1 entry `"<verb>_icon": Color(...)` (6 verb 调色六元组) + 
| `assets/ui/<verb>_icon/<verb>_icon.png` + `assets/ui/achievements/<verb>_icon/<verb>_icon.png` 1:1 双路径 | T245 #162 + 
| T246 #163 | `Color` 通道值 0 改 §9.6.4 调色六元组, 路径名 0 改 `<verb>_icon/<verb>_icon.png` | 0 改 1 通道值破坏 §9.6.4 调色六元组 + 0 改路径名破坏 
| §9.1 9 步 "icon" 步 | 必须在 Stage 7 tier entry 之后 |

**8 段序列跨任务历史落地表** (T094 → T246 5 任务 146 轮, 8 段 1:1 0 例外 0 漂移):

| 任务 | 轮次 | 落地段 | 关键修复 / 扩展 | 0 触碰边界 |
|------|------|------|------|------|
| T094 `quadruple_voice` 4/6 成就 | #51 | Stage 1-7 4 verb baseline | 4 verb 闭环奠基, `all_abilities_used` 4 项检查 + 
| `quadruple_voice` 4 tier 成就 + `echo_icon` icon 关联 | 0 触碰 `triple_voice` 既有 3 verb entry |
| T103 `quintuple_voice` 5/6 成就 | #74 | Stage 1-7 5 verb 扩展 | 5 verb 闭环扩展, `all_abilities_used` 5 项检查 + 
| `quintuple_voice` 5 tier 成就 + `wave_icon` icon 关联 | 0 触碰 `quadruple_voice` 既有 4 verb entry |
| T241 F013.E 6 verb 接入路径 + `sextuple_voice` | #159 | Stage 1-7 6 verb 完整闭环 | 6 verb 完整闭环落地, `all_abilities_used` 6 项检查 
| 0 漏 + `sextuple_voice` 6 tier 成就 + `whisper_used` 6 字段全部 1:1 镜像 5 verb | 0 触碰既有 5 verb 6 字段任何 entry |
| T245 `whisper_icon` 落地 + `ICON_COLORS` 6 verb 调色六元组 | #162 | Stage 8 6 verb 调色六元组 | `ICON_COLORS` 加 `whisper_icon` 
| entry (Muted Mauve #C8A4D8) + 6 verb 调色六元组登记 `sextuple_voice` fallback 颜色 | 0 改 1 通道值破坏 §9.6.4 调色六元组, 0 触碰既有 5 verb 
| entry |
| T246 5 verb 旧成就 PNG 双路径补全 | #163 | Stage 8 PNG 双路径 0 漏 | 3 verb 关联成就 PNG 双路径补全 (`quadruple_voice=echo_icon` + 
| `quintuple_voice=wave_icon` + `sextuple_voice=whisper_icon` 走 `assets/ui/<verb>_icon/<verb>_icon.png` + 
| `assets/ui/achievements/<verb>_icon/<verb>_icon.png` 1:1 双路径, 0 漏 0 改路径名) | 0 触碰 §9.1 9 步 "icon" 步 verb icon 资源契约, 0 
| 改路径名 |

**8 段序列常见 anti-pattern 表** (6 类症状根因, 5 任务 146 轮 0 触碰, 任何"加新 1 verb 接入成就系统"或"修 1 成就条件"或"修 1 成就 UI 颜色"必须 0 触碰 0 反序 0 漏 0 
改 1 字段):

| anti-pattern | 症状 | 根因 | T094-T246 落地后 1:1 严格 | 0 触碰边界 |
|------|------|------|------|------|
| 漏 Stage 1 `whisper_used` 字段声明 | `record_ability_used("whisper")` 报 "Undefined variable 'whisper_used'" | Stage 1 0 调, 
| 改用 `set_meta("whisper_used", 0)` 反射存 | T241 #159 落地下 1:1 严格, 6 字段 0 漏, 跨 6 verb 0 例外 | 0 走 `set_meta` / 
| `set("whisper_used", ...)` 反射, 0 走 enum |
| 漏 Stage 2 `whisper` 分支 | `whisper_used` 永远 0, `sextuple_voice` 永不触发 | Stage 2 `match verb_name:` 0 加 `whisper` 分支, 改用 
| default fallthrough 静默失败 | T241 #159 落地下 1:1 严格, 6 branch 0 漏, 跨 6 verb 0 例外 | 0 改分支名 `<verb>` 0 改 `<verb>_ability`, 
| 0 走 default fallthrough |
| 漏 Stage 6 `whisper_used >= 1` 检查 | `sextuple_voice` 5/6 检查通过即触发, 即使从未用 `whisper` | Stage 6 0 加第 6 项 `whisper_used >= 
| 1`, 改用 `sum >= 6` 累积阈值 | T241 #159 落地下 1:1 严格, 6 项 `and` 0 漏, 跨 6 verb 0 例外 | 0 反序 6 项, 0 改 `>= 1` 阈值, 0 改 `and` 为 
| `or` |
| 漏 Stage 5 `whisper_used = 0` reset | 新 run `whisper_used` 保留旧 run 值, `sextuple_voice` 立即触发 | Stage 5 0 加 
| `whisper_used = 0`, 改用 `reset_all_stats()` 通用 reset | T241 #159 落地下 1:1 严格, 6 行 reset 0 漏, 跨 6 verb 0 例外 | 0 反序 6 行, 
| 0 漏 1 行, 0 触碰 `health` / `coins` 等非 verb 字段 |
| 漏 Stage 8 `whisper_icon` ICON_COLORS entry | `sextuple_voice` 弹窗颜色走 fallback `Color(0.949, 0.714, 0.431, 1.0)`, 视觉组连贯 
| tooltip 颜色错 | Stage 8 0 加 `whisper_icon` entry, 改用 fallback 默认颜色 | T245 #162 落地下 1:1 严格, 6 verb 调色六元组 0 漏, 跨 6 verb 0 
| 例外 | 0 改 1 通道值破坏 §9.6.4 调色六元组, 0 走 fallback |
| 漏 Stage 8 PNG 双路径 | `sextuple_voice` 弹窗 PNG 加载失败, 弹窗空白 | Stage 8 0 创建 `assets/ui/whisper_icon/whisper_icon.png` + 
| `assets/ui/achievements/whisper_icon/whisper_icon.png` 双路径 | T246 #163 落地下 1:1 严格, 3 verb 关联成就 PNG 双路径 0 漏, 跨 3 verb 
| 0 例外 | 0 改路径名 `<verb>_icon/<verb>_icon.png` 0 改 `<verb>_icon/icon.png`, 0 触碰 §9.1 9 步 "icon" 步 verb icon 资源契约 |

**已知 drift risk 表** (5 任务 146 轮落地后已知 drift, 0 触碰边界但需持续监控):

| drift 位置 | 现状 | 风险 | 0 触碰边界 | 监控建议 |
|------|------|------|------|------|
| `PlayerStats._stat_names()` 数组 | T190 落地, 5 verb entry, `whisper_used` **可能**漏 (T241 #159 落地下 0 触碰) | `_stat_names()` 
| 是 debug 接口 `get_all_stats` 列表, 0 影响游戏逻辑 | 0 改 entry key `<verb>_used` 0 改 `<verb>_count`, 0 触碰 `_stat_names()` 既有 5 
| verb entry | 任何"未来给 `_stat_names()` 加消费方" (e.g. debug UI 列表) 必须**先**检查 6 verb entry 0 漏 0 反序, 0 漏 1 边 |

**与 §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 关系** — §9.6.16 覆盖 player.gd 6 verb hit handler 5 件套 1:1 严格分离 (verb 
ability `.trigger_player_hit` → 5 件套 `_on_verb_hit_pulse` / `_on_verb_hit_bind` / `_on_verb_hit_cut` / 
`_on_verb_hit_echo` / `_on_verb_hit_wave`), §9.6.17 覆盖 player.gd `_ready` 5+1 件套 signal 桥接 1:1 严格分离 (5 verb 
`verb_triggered` signal + 1 verb Whisper 业务特例 `whisper_ability_executed` signal), §9.6.18 覆盖 `_verb_ability_base.gd` 6 
verb ability 子类共享契约 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 兜底 + 1 `is_globally_blocking` 扩展 = 16 
件 1:1 严格分离), §9.6.19 覆盖 `_verb_windup_vfx_base.gd` 6 verb windup VFX 子类共享契约 (3 字段 + 1 `_ready()` + 1 `_process(delta)` 
+ 1 `_activate_windup_tween()` + 1 `fade_out_and_free()` = 7 件 1:1 严格分离), §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow 
5 段 canonical 1:1 序列 (Stage 1 触发检测 + Stage 2 GFC 状态转换 + Stage 3 fade_out + Stage 4 场景切换 + Stage 5 新 scene 恢复), §9.6.21 
覆盖 6 verb 成就系统 闭环 8 段 canonical 1:1 序列 (Stage 1 PlayerStats 字段声明 + Stage 2 `record_ability_used` 分支 + Stage 3 
`get_stat` entry + Stage 4 `set_stat` entry + Stage 5 `reset_run` 重置 + Stage 6 `all_abilities_used` 条件 + Stage 7 
`achievements.json` tier entry + Stage 8 `ICON_COLORS` entry + PNG 双路径). §9.6.16 / §9.6.17 关注"verb ability + signal 桥接",
 §9.6.18 关注"verb ability 类层级共享契约", §9.6.19 关注"verb windup VFX 类层级共享契约", §9.6.20 关注"跨 scene transition flow 时序契约", 
§9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格)". 6 套 polish 模式 0 反序 0 漏 0 改 1 字符. 任何"加新 1 verb 接入成就系统"必须**先**按 §9.6.16 5 件套 
hit handler 1:1 复制 (0 反序 0 漏 0 改 1 字段), **再**按 §9.6.17 5+1 件套 signal 桥接 1:1 复制 (0 反序 0 漏 0 改 1 字段), **再**按 §9.6.18 16 
件套 共享契约 1:1 复制 (0 反序 0 漏 0 改 1 字符), **再**按 §9.6.19 7 件套 windup VFX 共享契约 1:1 复制 (0 反序 0 漏 0 改 1 字符), **再**按 §9.6.21 8 
段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). §9.6.20 与 §9.6.21 0 互混 0 复用 0 共享: §9.6.20 关注"跨 scene transition flow 时序契约" (与 verb 
lifecycle 0 相关), §9.6.21 关注"6 verb 成就系统 闭环契约" (与跨 scene transition 0 相关). §9.6.21 是 6 套 polish 模式**唯一**关注"6 verb 关联成就 8 
段 1:1 闭环"的 1 套, 与 §9.6.16-§9.6.20 5 套 verb / VFX / transition 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.21 8 段序列走 §9.1 9 步落地的"verb" (1) + "ability" (2) + "const" (3) + "icon" (6) 4 步 
(`src/autoload/player_stats.gd` 6 verb 字段 + `src/data/achievements.json` 4 tier 成就 + 
`src/scripts/achievement_notification.gd` ICON_COLORS + `assets/ui/<verb>_icon/` + 
`assets/ui/achievements/<verb>_icon/` PNG 双路径), 0 触碰 §9.1 9 步任何 "input" (4) + "shop" (5) + "vfx" (7) + "audio" (8) + 
"HUD 冷光勾边" (9) 5 步 (成就系统 0 走 input map + 0 走 shop 系统 + 0 走 windup VFX 共享契约 + 0 走 audio 副作用 + 0 走 HUD 渲染). 任何"加新 1 verb 
接入成就系统"必须**先**按 §9.1 9 步落地 (含 `verb` (1) + `ability` (2) + `const` (3) + `icon` (6) 步), **再**按 §9.6.21 8 段序列 1:1 复制 (0 
反序 0 漏 0 改 1 字段). 任何"加新 1 tier 成就"必须**先**在 `achievements.json` 加 1 entry (Stage 7 1:1 严格), **再**在 `ICON_COLORS` 加 1 
verb icon 关联 (Stage 8 0 改 1 通道值) + 1:1 双路径 PNG (Stage 8 0 改路径名), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "const" 
(3) 步 (成就系统 0 触碰 verb enum + ability 子类 + const 字段). 任何"修 1 成就 UI 颜色"必须**先**在 §9.6.4 调色六元组加 1 entry, **再**在 
`ICON_COLORS` 加 1 entry (Stage 8 0 改 1 通道值), 0 触碰 §9.1 9 步任何 "HUD 冷光勾边" (9) 步 (成就弹窗颜色 0 走 HUD 6 行 6 色色域分工). 0 触碰 §9.1 9 
步任何 const / var / 节点 (8 段序列仅在 `PlayerStats` + `achievements.json` + `achievement_notification` + PNG 双路径 4 文件, 6 verb 
子类 0 改 base 0 改, 含 1 verb Whisper T275 #194 收回完成).
