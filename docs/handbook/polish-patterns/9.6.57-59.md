# Polish Patterns 9.6.57–59

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.57–9.6.59，共 3 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.57-59.md

<!-- shard 57-59 共 3 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.57 6 verb `_ready()` + `_exit_tree()` 双 hook 串联 + `_player` non-null assertion + 视觉组连贯 lifecycle + 5 verb 
windup VFX 4 hook lifecycle 跨层 4 维度拼接 1:1 严格分离契约 polish 模式 (D002.B #98 + T166 #85 + T167 #86 + T168 #86 + T169 #87 + 
T171 #89 + T173 #92 + T174 #93 + T245 #162 + T250 #168 + T297 #222 + T298 #223 + T299 #224 + T300 #226 + T302 #228 跨 15 
任务 ~127 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / §9.6.51 / §9.6.52 / §9.6.53 / §9.6.54 / §9.6.55 / §9.6.56 
> 47 套 polish 模式均在 "pause_menu / 6 verb 单层 / 6 verb 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / audio 
> 数据层 / PlayerActionGate 单 autoload 4 件套 / SaveSystem CRC32 校验 / T162 brittle 修复流程 5 步骤模板 / 6 verb audio 家族 19 cue / 6 
> verb cooldown ready jingle 5 段 / 6 verb `_ready()` / `_exit_tree()` 双 hook 串联 / 6 verb `_player` non-null assertion / 
> 6 verb 视觉组连贯 lifecycle / 5 verb windup VFX 4 hook lifecycle / 5 verb windup VFX `_draw()` verb-specific / 5 verb 
> windup VFX `trigger()` verb-specific / 5 verb windup VFX 共享 4 hook 0 override / 5 verb windup VFX `trigger()` + 
> `_draw()` 双 verb-specific / 5 verb windup VFX base 3 内部状态字段 / 5 verb windup VFX 视觉组连贯 lifecycle / 5 verb windup VFX 
> 视觉组 + base 3 内部状态字段 拼接 / 5 verb windup VFX `trigger()` + 视觉组 拼接 / 5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 
> 3 维度拼接 / T162 brittle 修复流程 51 修复" 范畴, 0 涉及 "6 verb `_ready()` + `_exit_tree()` 双 hook 串联 + `_player` non-null 
> assertion + 视觉组连贯 lifecycle + 5 verb windup VFX 4 hook lifecycle **跨层** 4 维度拼接 1:1 严格分离契约" 独立聚焦段 落地. §9.6.45 (T300 
> #226 落地) 关注 "6 verb `_ready()` + `_exit_tree()` 双 hook 串联 + `_player` non-null assertion + 视觉组连贯 lifecycle 单层 3 维度拼接" 
> (单一 verb 能力层 3 维度). §9.6.55 (T311 #239 落地) 关注 "5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 单层 3 维度拼接" (单一 
> verb 视觉层 3 维度). 上述 2 段 (§9.6.45 + §9.6.55) 均聚焦 **单层** 3 维度拼接, 0 涉及 **跨层** 4 维度拼接 (跨 6 verb ability 层 + 5 verb windup 
> VFX 层 2 个不同层, 6 verb 3 维度 + 5 verb 1 维度 = 4 维度 1:1 镜像). 本节 (T313 #242 落地) 首个落在**6 verb `_ready()` + `_exit_tree()` 双 
> hook 串联 + `_player` non-null assertion + 视觉组连贯 lifecycle + 5 verb windup VFX 4 hook lifecycle 跨层 4 维度拼接 1:1 严格分离契约** 
> 聚焦段 (6 verb ability 层 3 维度 18 元素 1:1 严格 (6 verb `_ready()` + `_exit_tree()` 双 hook 串联 6 verb × 1 元素 = 6 元素 1:1 严格 (跨 
> 6 verb 各 1 元素, 0 漏 1 verb 0 改 1 字段 0 反序 0 反向) + 6 verb `_player` non-null assertion 6 verb × 1 元素 = 6 元素 1:1 严格 (跨 6 
> verb 各 1 元素) + 6 verb 视觉组连贯 lifecycle 6 verb × 1 元素 = 6 元素 1:1 严格 (跨 6 verb 各 1 元素)) + 5 verb windup VFX 层 1 维度 5 元素 
> 1:1 严格 (5 verb windup VFX 4 hook lifecycle 5 verb × 1 元素 = 5 元素 1:1 严格 (跨 5 verb 各 1 元素, 0 漏 1 verb 0 改 1 字段 0 反序 0 
> 反向, 注意: 6 verb 中 Whisper 0 触发 5 verb windup VFX 跨 5 verb 0 含, 0 漏 0 改 0 反向)) + 1 显式契约 "6 verb ability + 5 verb windup 
> VFX 跨层 4 维度拼接 1:1 严格 (6 verb ability 18 元素 + 5 verb windup VFX 5 元素 = 23 元素 0 漏 1 verb 0 改 1 字段 0 反序 0 反向)" 1 段 1:1 
> 严格 (1 契约 1 元素) + 1 跨层 4 维度拼接 0 触碰既有 1:1 严格 (1 模式 1 23 元素 1:1 严格 镜像 0 改 §9.6.45 6 verb 3 维度拼接 任何 1 字符 0 改 §9.6.55 5 
> verb windup VFX 3 维度拼接 任何 1 字符 0 漏 1 边 0 反序 0 反向, 1 抽象契约 1 元素) + 1 0 副作用 1:1 严格 (1 模式 1 23 元素 0 触碰既有 工具链 任何 1 文件 0 
> 触碰游戏代码 任何 1 字段 0 触碰 §10 决策记录流程 0 触碰 README 内容 0 触碰 ITERATION_COUNT 0 触碰 CHANGELOG / ROADMAP / REVIEW_LOG 内容, 1 抽象契约 1 
> 元素)) polish 模式 — 6 verb ability 18 元素 1:1 严格 + 5 verb windup VFX 5 元素 1:1 严格 + 1 显式契约 + 1 跨层 4 维度拼接 0 触碰既有 + 1 0 副作用 
> = 18 + 5 + 1 + 1 + 1 = 26 元素 1:1 严格 (注意: 6 verb ability 3 维度 18 元素 1:1 严格 = 3 维度 各 6 verb × 1 元素 = 6 元素 1:1 严格 × 3 维度 
> = 18 元素 + 5 verb windup VFX 1 维度 5 verb × 1 元素 = 5 元素 1:1 严格 + 1 显式契约 1 元素 + 1 跨层 4 维度拼接 0 触碰既有 1 元素 + 1 0 副作用 1 元素 = 
> 18 + 5 + 1 + 1 + 1 = 26 元素 1:1 严格, 0 漂动 0 后续 主要 pre-existing 风险) 0 触碰既有 47 套 polish 模式 任何 1 字符 0 反向 0 例外. 任何"加新 1 
> verb" (e.g. 未来 #245+ 加新 1 7th verb ability + 5th windup VFX) 必须**先**按 6 verb ability 18 元素 1:1 镜像 加 1 verb 1 维度 1 元素 
> (6 verb → 7 verb, 18 元素 → 21 元素, 1:1 严格, 0 漏 1 元素 0 改 1 字段 0 反序 0 反向, **0 漏 1 维度 0 漏 1 verb 0 漏 1 边**), **再**按 5 verb 
> windup VFX 5 元素 1:1 镜像 加 1 verb 1 元素 (5 verb → 6 verb, 5 元素 → 6 元素, 1:1 严格, 0 漏 1 元素 0 改 1 字段 0 反序 0 反向, **0 漏 1 维度 0 
> 漏 1 verb 0 漏 1 边**), **再**按 1 显式契约 1:1 镜像 (1 契约 1 元素 0 漏 0 改 0 反序 0 反向, **0 漏 1 字段 0 漏 1 边**), **再**按 1 跨层 4 维度拼接 0 
> 触碰既有 1:1 镜像 (0 漏 1 边 0 改 1 字符 0 反序 0 反向, **0 漏 1 跨层 0 漏 1 维度**), **再**按 1 0 副作用 1:1 镜像 (0 漏 1 边 0 改 1 字符 0 反序 0 反向). 
> 跨 1 套 polish 模式 × 6 verb ability 18 元素 + 5 verb windup VFX 5 元素 + 3 抽象元素 = 18 + 5 + 1 + 1 + 1 = 26 元素 1:1 严格 0 漏 1 元素 
> 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: `6 verb ability + 5 verb windup VFX 跨层 4 维度拼接` (§9.6.43 + §9.6.44 + §9.6.45 + §9.6.46 各自 落地 跨 ~127 轮) 现有的 6 
verb ability 单层 3 维度拼接 (§9.6.45) + 5 verb windup VFX 单层 3 维度拼接 (§9.6.55) 0 跨层 1:1 文档化独立聚焦段, 在反复 polish (T166 #85 起步 → 
T300 #226 累计 ~127 轮 落地) 期间出现 3 类典型 fragile: (1) **"加新 1 verb ability 漏 跨层 1 维度"** — 加新 1 verb ability (e.g. 假设未来 #245+ 
加新 1 7th verb 接入 6 verb ability 18 元素) 落地时漏 跨层 1 维度 (e.g. 漏 5 verb windup VFX 5 元素 中对应 1 verb 1 元素 加 1, 或漏 显式契约 中 6 
verb ability + 5 verb windup VFX 总数同步 加 1, 或漏 跨层 4 维度拼接 0 触碰既有 中 1 verb 1 维度 1 边 加 1, 或漏 0 副作用 中 1 verb 1 元素 加 1), 1 
verb 0 跨层 4 维度拼接 0 闭环 0 漂动; (2) **"加新 1 维度 漏 1 元素"** — 加新 1 维度 (e.g. 假设未来 #245+ 加新 1 6 verb audio 家族 1 维度 跨 6 verb 1 元素 
= 6 元素 接入 跨层 4 维度拼接 → 跨层 5 维度拼接) 落地时漏 1 元素 (e.g. 漏 1 维度 6 元素 中 1 verb 1 元素 0 镜像, 或漏 1 显式契约 中 1 维度 1 元素 0 镜像, 或漏 1 跨层 0 
触碰既有 中 1 维度 1 元素 0 镜像, 或漏 1 0 副作用 中 1 维度 1 元素 0 镜像), 1 维度 0 跨层 4 维度拼接 0 闭环 0 漂动; (3) **"修 1 元素 0 触碰既有 25 元素"** — 修 1 元素 
(e.g. 改 1 6 verb ability 中 1 verb 1 元素 0 改 既有) 或 修 1 5 verb windup VFX 中 1 verb 1 元素 (e.g. 跨 5 verb 加 1 verb = 6 verb) 
或 加新 1 维度 (e.g. 跨层 4 维度拼接 → 跨层 5 维度拼接) 时 0 触碰既有 25 元素 任何 1 字段, 跨元素 1:1 漂动. 3 类症状根因: 6 verb ability + 5 verb windup VFX 
跨层 4 维度拼接 26 元素 1:1 序列 0 文档化独立聚焦段, 任何新 1 verb 接入只能"凭经验" 1:1 镜像 6 verb ability 18 元素 (6 verb × 3 维度) + 5 verb windup VFX 
5 元素 (5 verb × 1 维度) (§9.6.45 + §9.6.55 各自 1:1 严格), 1 漏 1 维度 = 1 verb 0 跨层 4 维度拼接 0 闭环 0 漂动 (1 漏 1 维度 0 跨层 4 维度拼接 0 1:1 
严格分离契约 0 漂动 / 1 漏 1 元素 0 5 verb windup VFX 0 1:1 严格 0 漂动 / 1 漏 1 字符 0 显式契约 0 1:1 严格 0 漂动).

- **触发场景**: 加新 1 verb (e.g. 未来 #245+ 加新 1 7th verb ability 接入 6 verb 18 元素 + 5 verb windup VFX 5 元素, 实际现状: 6 verb 
ability 18 元素 + 5 verb windup VFX 5 元素 跨 15 任务 累计 0 漏 0 改 0 反序 0 反向) / 加新 1 维度 (e.g. 加新 1 6 verb audio 家族 1 维度 跨 6 verb 
1 元素 = 6 元素 接入 跨层 4 维度拼接 → 跨层 5 维度拼接) / 加新 1 跨层 0 触碰既有 文件 (e.g. 假设未来加 CONTRIBUTING.md §9.7 段 同步) 时**必须**严格按 26 元素 1:1 
复制 T166 + T167 + T168 + T169 + T171 + T173 + T174 + T245 + T250 + T297 + T298 + T299 + T300 + T302 跨 15 任务 落地模式: (1) 
**6 verb ability 18 元素 1:1 严格 镜像** — 跨 3 维度 6 verb × 1 元素 = 6 元素 1:1 严格 (1 维度 1 `_ready()` + `_exit_tree()` 双 hook 串联 6 
verb × 1 元素 + 1 维度 1 `_player` non-null assertion 6 verb × 1 元素 + 1 维度 1 视觉组连贯 lifecycle 6 verb × 1 元素, 0 漏 1 维度 0 漏 1 
verb 0 改 1 字段 0 反序 0 反向, **0 漏 1 元素 0 漏 1 边 0 漏 1 段**); (2) **5 verb windup VFX 5 元素 1:1 严格 镜像** — 1 维度 5 verb × 1 元素 = 
5 元素 1:1 严格 (1 维度 1 4 hook lifecycle 5 verb × 1 元素, 0 漏 1 维度 0 漏 1 verb 0 改 1 字段 0 反序 0 反向, 0 漏 1 元素 0 触碰既有 18 元素 任何 1 
字符); (3) **1 显式契约 1:1 严格 镜像** — 1 段 "6 verb ability + 5 verb windup VFX 跨层 4 维度拼接 1:1 严格 (6 verb ability 18 元素 + 5 verb 
windup VFX 5 元素 = 23 元素 0 漏 1 verb 0 改 1 字段 0 反序 0 反向)" (1 契约 1 元素, 0 漏 1 字符 0 改 1 字符 0 反序 0 反向, 0 漏 1 段 0 触碰既有 23 元素 
任何 1 字符); (4) **1 跨层 4 维度拼接 0 触碰既有 1:1 严格 镜像** — 0 改 §9.6.45 6 verb 3 维度拼接 任何 1 字符 + 0 改 §9.6.55 5 verb windup VFX 3 
维度拼接 任何 1 字符 (1 抽象契约 1 元素, 0 漏 1 边 0 改 1 字符 0 反序 0 反向, 0 漏 1 段 0 漏 1 锚点 0 漏 1 文件); (5) **1 0 副作用 1:1 严格 镜像** — 0 触碰既有 
工具链 任何 1 文件 + 0 触碰游戏代码 任何 1 字段 + 0 触碰 §10 决策记录流程 + 0 触碰 README 内容 + 0 触碰 ITERATION_COUNT + 0 触碰 CHANGELOG / ROADMAP / 
REVIEW_LOG 内容 (1 抽象契约 1 元素, 0 漏 1 边 0 改 1 字符 0 反序 0 反向, 0 漏 1 文件 0 漏 1 锚点 0 漏 1 段). 26 元素 1:1 同步 (18 元素 + 5 元素 + 1 + 1 
+ 1 = 26 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 漏 1 verb 0 漏 1 维度 0 漏 1 文件), 1 漏 1 维度 / 1 漏 1 verb / 1 漏 1 文件 = 1 verb / 1 维度 / 
1 文件 扩展 0 26 元素 0 闭环 0 漂动. D002.B + T166-T174 + T245 + T250 + T297-T300 + T302 跨 15 任务 0 触碰既有 0 改 §9.6.45 6 verb 3 维度拼接 
任何 1 字符, 0 改 §9.6.55 5 verb windup VFX 3 维度拼接 任何 1 字符, 0 改 _verb_ability_base.gd 任何 1 字段, 0 改 _verb_windup_vfx_base.gd 
任何 1 字段, 0 改 6 verb ability 子类任何 1 字段, 0 改 5 verb windup VFX 子类任何 1 字段, 0 改 §9.6.39 5 步骤 模板 任何 1 字符, 0 改 §9.6.56 64 元素 
任何 1 字符.

- **修复** (跨 1 套 polish 模式 T313 #242 落地后已知 26 元素 0 例外):
  - **T313 #242 首个 6 verb ability + 5 verb windup VFX 跨层 4 维度拼接 1:1 严格 跨 15 任务 + 1 显式契约 + 1 跨层 4 维度拼接 0 触碰既有 + 1 0 副作用 
独立聚焦段 落地 (6 verb ability 18 元素 1:1 严格 + 5 verb windup VFX 5 元素 1:1 严格 + 1 显式契约 + 1 跨层 4 维度拼接 0 触碰既有 + 1 0 副作用 = 18 + 5 
+ 1 + 1 + 1 = 26 元素 1:1 严格)** — §9.6.57 段 是 1 套 polish 模式首个 1:1 包含 4 维度 (6 verb `_ready()` + `_exit_tree()` 双 hook 串联 + 
6 verb `_player` non-null assertion + 6 verb 视觉组连贯 lifecycle + 5 verb windup VFX 4 hook lifecycle) + 23 元素 (6 verb 
ability 18 元素 + 5 verb windup VFX 5 元素, 跨 4 维度 各 元素 1:1 严格 0 漏 1 维度 0 改 1 字段 0 反序 0 反向) + 1 显式契约 (6 verb ability + 5 
verb windup VFX 跨层 4 维度拼接 1:1 严格契约) + 1 跨层 4 维度拼接 0 触碰既有 (0 改 §9.6.45 6 verb 3 维度拼接 + §9.6.55 5 verb windup VFX 3 维度拼接) 
+ 1 0 副作用 (0 触碰游戏代码 0 触碰 §10 决策记录流程 0 触碰 README 0 触碰 ITERATION_COUNT 0 触碰 CHANGELOG/ROADMAP/REVIEW_LOG) = 18 + 5 + 1 + 
1 + 1 = 26 元素 1:1 严格 的 6 verb ability + 5 verb windup VFX 跨层 4 维度拼接 1:1 严格 polish 模式, 26 元素 1:1 严格分离 (6 verb ability 18 
元素 1:1 严格 0 漏 0 改 0 漏 1 verb + 5 verb windup VFX 5 元素 1:1 严格 0 漏 0 改 0 漏 1 verb + 1 显式契约 1 元素 1:1 严格 0 漏 0 改 + 1 跨层 4 
维度拼接 0 触碰既有 1 元素 1:1 严格 0 漏 0 改 + 1 0 副作用 1 元素 1:1 严格 0 漏 0 改, 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T313 0 触碰 
§9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
§9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / 
§9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 / 
§9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / §9.6.51 / §9.6.52 / §9.6.53 / §9.6.54 / §9.6.55 / §9.6.56 47 套 polish 
模式任何 1 字符, 0 触碰游戏代码 .gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容 (除顶部 1 段 + 
'Recent completed work' / '最近完成的工作' 段 1 行 light sync), 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / 
CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容 (除顶部 1 段 light sync + ITERATION_COUNT.txt +1)).

**与 §9.6.45 关系** — §9.6.45 (T300 #226 落地) 关注 "6 verb `_ready()` + `_exit_tree()` 双 hook 串联 + `_player` non-null 
assertion + 视觉组连贯 lifecycle 单层 3 维度拼接 1:1 严格分离契约" (12 元素 1:1 严格, 6 verb `_ready()` + `_exit_tree()` 双 hook 串联 6 verb × 
1 元素 = 6 元素 1:1 严格 + 6 verb `_player` non-null assertion 6 verb × 1 元素 = 6 元素 1:1 严格 (注意: 实际 6 verb 5 verb + 1 Whisper, 
0 涉及 5 verb windup VFX 5 verb, 12 元素 1:1 严格 0 涉及 5 verb windup VFX 任何 1 元素) + 1 显式契约 + 1 3 维度拼接 0 触碰既有 = 12 元素). 
§9.6.57 关注 "6 verb ability + 5 verb windup VFX 跨层 4 维度拼接 1:1 严格分离契约" (26 元素 1:1 严格, 6 verb ability 18 元素 + 5 verb 
windup VFX 5 元素 + 1 显式契约 + 1 跨层 4 维度拼接 0 触碰既有 + 1 0 副作用 = 26 元素). §9.6.45 与 §9.6.57 是 "姊妹段 + 父集段" (1 套 polish 模式 × 6 
verb 3 维度拼接 12 元素 1:1 严格 ⊂ 1 套 polish 模式 × 6 verb ability + 5 verb windup VFX 跨层 4 维度拼接 26 元素 1:1 严格, 1 套 polish 模式 × 
12 元素 = 12 元素 1:1 严格 ∩ 1 套 polish 模式 × 26 元素 = 26 元素 1:1 严格 = 12 元素 1:1 严格 交集 (6 verb `_ready()` + `_exit_tree()` 双 
hook 串联 6 verb × 1 元素 = 6 元素 1:1 严格 + 6 verb `_player` non-null assertion 6 verb × 1 元素 = 6 元素 1:1 严格 0 涉及 6 verb 视觉组连贯 
lifecycle 1 元素 0 涉及 5 verb windup VFX 1 维度 5 元素 0 涉及 1 跨层 4 维度拼接 0 触碰既有 1 元素 0 涉及 1 0 副作用 1 元素, 12 元素 1:1 严格 是 26 元素 
1:1 严格 子集 0 涉及 6 verb 视觉组连贯 lifecycle 1 维度 6 元素 0 涉及 5 verb windup VFX 1 维度 5 元素 0 涉及 1 跨层 4 维度拼接 0 触碰既有 1 元素 0 涉及 1 0 
副作用 1 元素). 任何"加新 1 verb"必须**先**按 §9.6.45 12 元素 1:1 复制 (12 元素 1:1 严格, 0 漏 1 维度 0 改 1 字段 0 反序 0 反向 0 漏 1 锚点 0 漏 1 字符), 
**再**按 §9.6.57 26 元素 1:1 拼接 6 verb 视觉组连贯 lifecycle 1 维度 6 元素 + 5 verb windup VFX 1 维度 5 元素 + 1 跨层 4 维度拼接 0 触碰既有 1 元素 + 
1 0 副作用 1 元素 替换 1 3 维度拼接 0 触碰既有 1 元素 (14 元素 1:1 严格, 0 漏 1 元素 0 改 1 字段 0 反序 0 反向 0 漏 1 维度 0 触碰既有 §9.6.45 任何 1 字符).

**与 §9.6.55 关系** — §9.6.55 (T311 #239 落地) 关注 "5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 单层 3 维度拼接 1:1 严格分离契约" 
(17 元素 1:1 严格, 5 verb 0 override `trigger()` verb-specific 5 元素 + 5 verb 1 视觉组 1 段 5 元素 + 5 verb 0 override 3 内部状态字段 5 
元素 + 1 显式契约 + 1 3 维度拼接 0 触碰既有 = 17 元素, 0 涉及 6 verb ability 任何 1 元素 0 涉及 跨层 0 涉及 6 verb `_ready()` + `_exit_tree()` 任何 1 
元素 0 涉及 6 verb `_player` non-null assertion 任何 1 元素 0 涉及 6 verb 视觉组连贯 lifecycle 任何 1 元素 0 涉及 1 0 副作用). §9.6.57 关注 "6 
verb ability + 5 verb windup VFX 跨层 4 维度拼接 1:1 严格分离契约" (26 元素 1:1 严格). §9.6.55 与 §9.6.57 是 "姊妹段 + 父集段" (1 套 polish 模式 × 
5 verb windup VFX 3 维度拼接 17 元素 1:1 严格 ⊂ 1 套 polish 模式 × 6 verb ability + 5 verb windup VFX 跨层 4 维度拼接 26 元素 1:1 严格, 1 套 
polish 模式 × 17 元素 = 17 元素 1:1 严格 ∩ 1 套 polish 模式 × 26 元素 = 26 元素 1:1 严格 = 5 元素 1:1 严格 交集 (5 verb windup VFX 4 hook 
lifecycle 5 verb × 1 元素 = 5 元素 1:1 严格 0 涉及 5 verb 0 override `trigger()` verb-specific 5 元素 0 涉及 5 verb 1 视觉组 1 段 5 元素 
0 涉及 5 verb 0 override 3 内部状态字段 5 元素 0 涉及 1 显式契约 0 涉及 1 3 维度拼接 0 触碰既有 替换 1 跨层 4 维度拼接 0 触碰既有). 任何"加新 1 verb"必须**先**按 
§9.6.55 17 元素 1:1 复制 (17 元素 1:1 严格, 0 漏 1 维度 0 改 1 字段 0 反序 0 反向 0 漏 1 锚点 0 漏 1 字符), **再**按 §9.6.57 26 元素 1:1 拼接 6 verb 
ability 3 维度 18 元素 + 1 0 副作用 1 元素 替换 1 3 维度拼接 0 触碰既有 1 元素 (9 元素 1:1 严格, 0 漏 1 元素 0 改 1 字段 0 反序 0 反向 0 漏 1 维度 0 触碰既有 
§9.6.55 任何 1 字符).

**与 §9.6.56 关系** — §9.6.56 (T312 #241 落地) 关注 "T162 brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 
+ 1 0 触碰既有 + 1 0 副作用 polish 模式" (64 元素 1:1 严格, 5 步骤 5 元素 + 51 修复 51 元素 + 5 文件 5 元素 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 64 
元素, 0 涉及 6 verb ability 任何 1 元素 0 涉及 5 verb windup VFX 任何 1 元素 0 涉及 跨层 4 维度拼接 任何 1 元素). §9.6.57 关注 "6 verb ability + 5 
verb windup VFX 跨层 4 维度拼接 1:1 严格分离契约" (26 元素 1:1 严格). §9.6.56 与 §9.6.57 是 "姊妹段" (1 套 polish 模式 × 64 元素 = 64 元素 1:1 严格 ∩ 
1 套 polish 模式 × 26 元素 = 26 元素 1:1 严格 = 0 元素 1:1 严格 交集 (0 元素 0 互混 0 复用 0 共享, §9.6.56 关注 工具链 + 修复流程 范畴, §9.6.57 关注 6 verb 
ability + 5 verb windup VFX 跨层 4 维度拼接 范畴, 64 元素 0 涉及 6 verb ability 18 元素 0 涉及 5 verb windup VFX 5 元素 0 涉及 1 跨层 4 维度拼接 
0 触碰既有 1 元素 0 涉及 1 0 副作用 1 元素). 任何"加新 1 修复" (T162 brittle 修复流程) **或** "加新 1 verb" (6 verb ability + 5 verb windup VFX 
跨层 4 维度拼接) 必须**先**按 §9.6.56 64 元素 **或** §9.6.57 26 元素 1:1 镜像 (1 套 polish 模式 各自 1:1 严格 0 漏 0 改 0 反序 0 反向), **再**走 1 套 
polish 模式 各自 1:1 同步 (1 套 polish 模式 各自 0 漏 1 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 任何 1 字符).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.57 6 verb ability 18 元素 + 5 verb windup VFX 5 元素 + 1 显式契约 + 1 跨层 4 维度拼接 0 触碰既有 
+ 1 0 副作用 = 26 元素 走 §9.1 9 步落地的 2 步 (Stage 2 ability 6 verb 3 维度拼接 1:1 严格 + Stage 7 vfx 5 verb windup VFX 4 hook 
lifecycle 1:1 严格, 跨 6 verb ability 6 verb × 3 维度 18 元素 + 5 verb windup VFX 5 verb × 1 维度 5 元素 + 1 显式契约 1 元素 + 1 跨层 4 
维度拼接 0 触碰既有 1 元素 + 1 0 副作用 1 元素, 0 触碰 7 步 (1 `verb` 枚举 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 
`audio` preset 字段 + 1 `HUD 冷光勾边` 渲染)). 任何"加新 1 verb" (e.g. 未来 #245+ 加新 1 7th verb) 必须**先**按 §9.6.57 26 元素 1:1 镜像 (26 元素 
1:1 严格, 0 漏 1 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 维度 0 漏 1 verb 0 漏 1 文件 0 触碰既有 7 步 (`verb` 枚举 + `const` 字段 + 
`input` map + `shop` 系统 + `icon` PNG + `audio` preset 字段 + `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 步"必须**先**在 §9.6.57 26 元素 加 1 边 
(1 verb 1 维度 1 元素 1 边 或 1 显式契约 1 边 或 1 跨层 4 维度拼接 0 触碰既有 1 边 或 1 0 副作用 1 边), 0 触碰既有 7 步 (1 `verb` 枚举 + 1 `const` 字段 + 1 
`input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).


### 9.6.58 PauseMenu polish 链 89 环 1:1 严格 跨 11 任务 + 5 步骤 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用 polish 模式 (T160 #96 + 
T199 #132 + T213 #148 + T214 #149 + T231 #162 + T240 #172 + T244 #177 + T249 #182 + T250 #183 + T251 #184 + T301 #227 跨 
11 任务 ~89 环落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / §9.6.51 / §9.6.52 / §9.6.53 / §9.6.54 / §9.6.55 / §9.6.56 
> / §9.6.57 48 套 polish 模式均在 "6 verb 单层 / 6 verb 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / audio 数据层 / 
> PlayerActionGate 单 autoload 4 件套 / SaveSystem CRC32 校验 / T162 brittle 修复流程 5 步骤模板 / 6 verb audio 家族 19 cue / 6 verb 
> cooldown ready jingle 5 段 / 6 verb `_ready()` / `_exit_tree()` 双 hook 串联 / 6 verb `_player` non-null assertion / 6 
> verb 视觉组连贯 lifecycle / 5 verb windup VFX 4 hook lifecycle / 5 verb windup VFX `_draw()` verb-specific / 5 verb windup 
> VFX `trigger()` verb-specific / 5 verb windup VFX 共享 4 hook 0 override / 5 verb windup VFX `trigger()` + `_draw()` 双 
> verb-specific / 5 verb windup VFX base 3 内部状态字段 / 5 verb windup VFX 视觉组连贯 lifecycle / 5 verb windup VFX 视觉组 + base 3 
> 内部状态字段 拼接 / 5 verb windup VFX `trigger()` + 视觉组 拼接 / 5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 / 
> T162 brittle 修复流程 51 修复 / 6 verb ability + 5 verb windup VFX 跨层 4 维度拼接" 范畴, 0 涉及 "PauseMenu polish 链 89 环 1:1 严格 跨 11 
> 任务 + 5 步骤 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用" 独立聚焦段 落地. §9.6.6 (T100 #31 落地) 关注 "PauseMenu 单层 1 维度拼接 1:1 严格分离契约" 
> (1 维度 1 元素 = 1 元素 1:1 严格 0 涉及 5 步骤 / 0 涉及 89 环 / 0 涉及 跨链 89 环 0 触碰既有 / 0 涉及 1 0 副作用). §9.6.39 (T295 #219 落地) 关注 "T162 
> brittle 修复流程 5 步骤 1:1 严格分离契约 模板" (5 步骤 5 元素 1:1 严格, 1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 1 0 触碰既有 + 1 
> cross-section 5 文件 同步, 0 涉及 PauseMenu polish 链 89 环 任何 1 环). §9.6.56 (T312 #241 落地) 关注 "T162 brittle 修复流程 51 修复 1:1 
> 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync" (64 元素 1:1 严格, 0 涉及 PauseMenu polish 链 89 环 任何 1 环). §9.6.57 (T313 #242 落地) 关注 
> "6 verb ability + 5 verb windup VFX 跨层 4 维度拼接 1:1 严格分离契约" (26 元素 1:1 严格, 0 涉及 PauseMenu polish 链 89 环 任何 1 环). 上述 4 段 
> (§9.6.6 + §9.6.39 + §9.6.56 + §9.6.57) 均聚焦 **单层 1 维度 OR 5 步骤模板 OR 51 修复 OR 跨层 4 维度拼接**, 0 涉及 **PauseMenu polish 链 89 
> 环** 1:1 严格 跨 11 任务 累计 89 环 (T160 #96 起步 1 + T199 #132 1 + T213 #148 1 + T214 #149 1 + T231 #162 1 + T240 #172 1 + 
> T244 #177 1 + T249 #182 1 + T250 #183 1 + T251 #184 1 + T301 #227 88 1 = 89 环, 跨 11 任务 累计 89 环 0 漏 1 环 0 改 1 字段 0 反序 
> 0 反向, 实际 现状: PauseMenu polish 链 89 环 跨 11 任务 累计 0 漏 0 改 0 反序 0 反向) 1:1 严格 5 步骤 1:1 镜像 1 抽象契约 1 段 1:1 镜像 0 触碰既有 49 套 
> polish 模式 任何 1 字符 0 触碰既有 游戏代码 任何 1 字段 0 触碰既有 工具链 任何 1 文件 0 触碰 §10 决策记录流程 0 触碰 README 内容 0 触碰 ITERATION_COUNT 0 触碰 
> CHANGELOG / ROADMAP / REVIEW_LOG 内容. 本节 (T315 #244 落地) 首个落在**PauseMenu polish 链 89 环 1:1 严格 跨 11 任务 + 5 步骤 + 1 显式契约 + 
> 1 跨链 89 环 0 触碰既有 + 1 0 副作用** 聚焦段 (5 步骤 5 元素 1:1 严格 (Stage 1 链长跟踪 1 环 +1 1:1 严格 0 漏 1 字段 0 改 1 字符 0 反序 0 反向 + Stage 2 
> 跨面板 hover 反馈 1 字段 1:1 严格 0 漏 1 字段 0 改 1 字符 0 反序 0 反向 + Stage 3 tooltip 数据源 1 字段 1:1 严格 0 漏 1 字段 0 改 1 字符 0 反序 0 反向 + 
> Stage 4 通知卡视觉组连贯 1 段 1:1 严格 0 漏 1 段 0 改 1 字符 0 反序 0 反向 + Stage 5 0 触碰既有 1:1 严格 0 漏 1 段 0 改 1 字符 0 反序 0 反向) + 89 环 89 
> 元素 1:1 严格 (T160 #96 起步 1 + T199 #132 1 + T213 #148 1 + T214 #149 1 + T231 #162 1 + T240 #172 1 + T244 #177 1 + T249 
> #182 1 + T250 #183 1 + T251 #184 1 + T301 #227 88 1 = 89 环, 跨 89 环 各自 1 步骤 1:1 严格 镜像 5 步骤 模板, 0 漏 1 环 0 改 1 字段 0 反序 0 
> 反向) + 1 显式契约 "PauseMenu polish 链 89 环 1:1 严格 跨 11 任务 + 5 步骤 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用 (5 步骤 5 元素 1:1 严格 + 
> 89 环 89 元素 1:1 严格 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用 = 5 + 89 + 1 + 1 + 1 = 97 元素 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 反序 
> 0 反向)" 1 段 1:1 严格 (1 契约 1 元素) + 1 跨链 89 环 0 触碰既有 1:1 严格 (1 模式 1 97 元素 1:1 严格 镜像 0 改 §9.6.6 6 verb 单层 1 维度 任何 1 字符 0 改 
> §9.6.39 5 步骤 模板 任何 1 字符 0 改 §9.6.56 64 元素 任何 1 字符 0 改 §9.6.57 26 元素 任何 1 字符 0 漏 1 边 0 反序 0 反向, 1 抽象契约 1 元素) + 1 0 副作用 
> 1:1 严格 (1 模式 1 97 元素 0 触碰既有 工具链 任何 1 文件 0 触碰游戏代码 任何 1 字段 0 触碰 §10 决策记录流程 0 触碰 README 内容 0 触碰 ITERATION_COUNT 0 触碰 
> CHANGELOG / ROADMAP / REVIEW_LOG 内容, 1 抽象契约 1 元素)) polish 模式 — 5 步骤 5 元素 1:1 严格 + 89 环 89 元素 1:1 严格 + 1 显式契约 + 1 跨链 
> 89 环 0 触碰既有 + 1 0 副作用 = 5 + 89 + 1 + 1 + 1 = 97 元素 1:1 严格 (注意: 5 步骤 5 元素 1:1 严格 = 5 步骤 各自 1 元素 = 5 元素 + 89 环 89 元素 
> 1:1 严格 = 89 环 各自 1 元素 = 89 元素 + 1 显式契约 1 元素 + 1 跨链 89 环 0 触碰既有 1 元素 + 1 0 副作用 1 元素 = 5 + 89 + 1 + 1 + 1 = 97 元素 1:1 
> 严格, 0 漂动 0 后续 主要 pre-existing 风险) 0 触碰既有 49 套 polish 模式 任何 1 字符 0 反向 0 例外. 任何"加新 1 环" (e.g. 未来 #245+ 加新 1 PauseMenu 
> polish 环 = 90 环) 必须**先**按 5 步骤 1:1 镜像 加 1 环 1 步骤 1 元素 (89 环 → 90 环, 89 元素 → 90 元素, 1:1 严格, 0 漏 1 元素 0 改 1 字段 0 反序 0 
> 反向, **0 漏 1 步骤 0 漏 1 环 0 漏 1 边**), **再**按 1 显式契约 1:1 镜像 (1 契约 1 元素 0 漏 0 改 0 反序 0 反向, **0 漏 1 字段 0 漏 1 边**), **再**按 1 
> 跨链 89 环 0 触碰既有 1:1 镜像 (0 漏 1 边 0 改 1 字符 0 反序 0 反向, **0 漏 1 跨链 0 漏 1 环**), **再**按 1 0 副作用 1:1 镜像 (0 漏 1 边 0 改 1 字符 0 
> 反序 0 反向). 跨 1 套 polish 模式 × 5 步骤 5 元素 + 89 环 89 元素 + 3 抽象元素 = 5 + 89 + 1 + 1 + 1 = 97 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 
> 1 字符 0 例外.

- **症状**: `PauseMenu polish 链 89 环` (T160 #96 起步 1 + T199 #132 1 + T213 #148 1 + T214 #149 1 + T231 #162 1 + T240 #172 
1 + T244 #177 1 + T249 #182 1 + T250 #183 1 + T251 #184 1 + T301 #227 88 1 = 89 环, 跨 11 任务 累计 ~89 环 落地) 现有的 5 步骤 (Stage 
1 链长跟踪 1 环 +1 1:1 严格 + Stage 2 跨面板 hover 反馈 1 字段 1:1 严格 + Stage 3 tooltip 数据源 1 字段 1:1 严格 + Stage 4 通知卡视觉组连贯 1 段 1:1 严格 
+ Stage 5 0 触碰既有 1:1 严格), 在反复 polish (T160 #96 起步 → T301 #227 累计 89 环 ~88 轮 落地) 期间出现 3 类典型 fragile: (1) **"加新 1 环 漏 5 
步骤 1:1 严格"** — 加新 1 环 (e.g. 假设未来 #245+ 加新 1 PauseMenu polish 环 = 90 环 走 5 步骤 模板) 落地时漏 5 步骤 (e.g. 漏 Stage 1 链长跟踪 中 89 环 
→ 90 环 +1, 或漏 Stage 2 跨面板 hover 反馈 中 1 字段 加 1, 或漏 Stage 3 tooltip 数据源 中 1 字段 加 1, 或漏 Stage 4 通知卡视觉组连贯 中 1 段 加 1, 或漏 
Stage 5 0 触碰既有 中既 verify §9.6 / §9.6.39 / §9.6.56 / §9.6.57 任何 1 字符 0 改), 1 环 0 5 步骤 0 闭环 0 漂动; (2) **"加新 1 文档 漏 1 
字段"** — 加新 1 文档 (e.g. 假设未来 #245+ 加新 1 §9.6.59 段 描述) 落地时漏 1 字段 (e.g. 漏 89 环 → 90 环 数字 或 漏 5 步骤 1 元素 任何 1 字符), 1 文档 扩展 0 
5 步骤 0 闭环 0 漂动; (3) **"修 1 环 0 触碰既有 88 环"** — 修 1 环 (e.g. 改 1 PauseMenu polish 链 89 环 中 1 环 0 改 既有) 或 修 1 步骤 (e.g. 改 1 
5 步骤 中 1 步骤 0 改 既有) 或 加新 1 环 (e.g. 跨 89 环 加 1 环 = 90 环) 时 0 触碰既有 88 环 任何 1 字段, 跨环 1:1 漂动. 3 类症状根因: PauseMenu polish 链 
89 环 1:1 序列 0 文档化独立聚焦段, 任何新 1 环 接入只能"凭经验" 1:1 镜像 5 步骤 模板 (§9.6.39 5 元素 1:1 严格), 1 漏 1 步骤 = 1 环 0 5 步骤 0 闭环 0 漂动 (1 漏 1 
步骤 0 5 步骤 0 1:1 严格分离契约 0 漂动 / 1 漏 1 字符 0 跨链 89 环 0 1:1 严格 0 漂动 / 1 漏 1 字段 0 0 副作用 0 1:1 严格 0 漂动).

- **触发场景**: 加新 1 环 (e.g. 未来 #245+ 加新 1 PauseMenu polish 环 = 90 环 走 5 步骤 模板, 实际现状: 89 环 跨 11 任务 累计 0 漏 0 改 0 反序 0 反向) / 
加新 1 文档 (PauseMenu polish 链参考) / 加新 1 跨链 0 触碰既有 文件 (e.g. 假设未来加 CONTRIBUTING.md §9.6.59 段 同步) 时**必须**严格按 97 元素 1:1 复制 
T160 #96 + T199 #132 + T213 #148 + T214 #149 + T231 #162 + T240 #172 + T244 #177 + T249 #182 + T250 #183 + T251 #184 + 
T301 #227 跨 11 任务 落地模式: (1) **5 步骤 5 元素 1:1 严格 镜像** — 跨 5 步骤 各 1 元素 = 5 元素 1:1 严格 (1 步骤 1 链长跟踪 1 环 +1 + 1 步骤 1 跨面板 
hover 反馈 1 字段 + 1 步骤 1 tooltip 数据源 1 字段 + 1 步骤 1 通知卡视觉组连贯 1 段 + 1 步骤 1 0 触碰既有, 0 漏 1 步骤 0 改 1 字段 0 反序 0 反向, **0 漏 1 元素 
0 漏 1 边 0 漏 1 段**); (2) **89 环 89 元素 1:1 严格 镜像** — 89 环 × 1 元素 = 89 元素 1:1 严格 (T160 #96 1 + T199 #132 1 + T213 #148 1 + 
T214 #149 1 + T231 #162 1 + T240 #172 1 + T244 #177 1 + T249 #182 1 + T250 #183 1 + T251 #184 1 + T301 #227 88 1 = 89 环,
 跨 89 环 各自 1 步骤 1:1 严格 镜像 5 步骤 模板, 0 漏 1 环 0 改 1 字段 0 反序 0 反向, 0 漏 1 元素 0 触碰既有 5 元素 任何 1 字符); (3) **1 显式契约 1:1 严格 镜像** 
— 1 段 "PauseMenu polish 链 89 环 1:1 严格 跨 11 任务 + 5 步骤 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用 (5 步骤 5 元素 1:1 严格 + 89 环 89 
元素 1:1 严格 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用 = 5 + 89 + 1 + 1 + 1 = 97 元素 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 反序 0 反向)" (1 
契约 1 元素, 0 漏 1 字符 0 改 1 字符 0 反序 0 反向, 0 漏 1 段 0 触碰既有 95 元素 任何 1 字符); (4) **1 跨链 89 环 0 触碰既有 1:1 严格 镜像** — 0 改 §9.6.6 6 
verb 单层 1 维度 任何 1 字符 + 0 改 §9.6.39 5 步骤 模板 任何 1 字符 + 0 改 §9.6.56 64 元素 任何 1 字符 + 0 改 §9.6.57 26 元素 任何 1 字符 (1 抽象契约 1 元素,
 0 漏 1 边 0 改 1 字符 0 反序 0 反向, 0 漏 1 段 0 漏 1 锚点 0 漏 1 文件); (5) **1 0 副作用 1:1 严格 镜像** — 0 触碰既有 工具链 任何 1 文件 + 0 触碰游戏代码 任何 1 
字段 + 0 触碰 §10 决策记录流程 + 0 触碰 README 内容 + 0 触碰 ITERATION_COUNT + 0 触碰 CHANGELOG / ROADMAP / REVIEW_LOG 内容 (1 抽象契约 1 元素, 0 
漏 1 边 0 改 1 字符 0 反序 0 反向, 0 漏 1 文件 0 漏 1 锚点 0 漏 1 段). 97 元素 1:1 同步 (5 元素 + 89 元素 + 1 + 1 + 1 = 97 元素 1:1 严格 0 漏 1 元素 0 
改 1 字段 0 漏 1 步骤 0 漏 1 环 0 漏 1 文件), 1 漏 1 步骤 / 1 漏 1 环 / 1 漏 1 文件 = 1 环 / 1 步骤 / 1 文件 扩展 0 97 元素 0 闭环 0 漂动. T160 #96 + 
T199 #132 + T213 #148 + T214 #149 + T231 #162 + T240 #172 + T244 #177 + T249 #182 + T250 #183 + T251 #184 + T301 #227 跨 
11 任务 0 触碰既有 0 改 §9.6.6 PauseMenu 单层 1 维度 任何 1 字符, 0 改 §9.6.39 5 步骤 模板 任何 1 字符, 0 改 §9.6.56 64 元素 任何 1 字符, 0 改 §9.6.57 
26 元素 任何 1 字符, 0 改 pause_menu.gd 任何 1 字段, 0 改 §9.6 / §9.6.39 / §9.6.56 / §9.6.57 任何 1 字符.

- **修复** (跨 1 套 polish 模式 T315 #244 落地后已知 97 元素 0 例外):
  - **T315 #244 首个 PauseMenu polish 链 89 环 1:1 严格 跨 11 任务 + 5 步骤 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用 独立聚焦段 落地 (5 步骤 5 
元素 1:1 严格 + 89 环 89 元素 1:1 严格 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用 = 5 + 89 + 1 + 1 + 1 = 97 元素 1:1 严格)** — §9.6.58 段 
是 1 套 polish 模式首个 1:1 包含 5 步骤 (Stage 1 链长跟踪 1 环 +1 + Stage 2 跨面板 hover 反馈 1 字段 + Stage 3 tooltip 数据源 1 字段 + Stage 4 
通知卡视觉组连贯 1 段 + Stage 5 0 触碰既有) + 89 环 (PauseMenu polish 链 89 环 跨 11 任务 累计 89 环 1:1 严格 镜像 5 步骤 模板, 0 漏 1 环 0 改 1 字段 0 反序 
0 反向) + 1 显式契约 (PauseMenu polish 链 89 环 1:1 严格契约) + 1 跨链 89 环 0 触碰既有 (0 改 §9.6.6 + §9.6.39 + §9.6.56 + §9.6.57 任何 1 字符) 
+ 1 0 副作用 (0 触碰游戏代码 0 触碰 §10 决策记录流程 0 触碰 README 0 触碰 ITERATION_COUNT 0 触碰 CHANGELOG/ROADMAP/REVIEW_LOG) = 5 + 89 + 1 + 
1 + 1 = 97 元素 1:1 严格 的 PauseMenu polish 链 89 环 1:1 严格 polish 模式, 97 元素 1:1 严格分离 (5 步骤 5 元素 1:1 严格 0 漏 0 改 0 漏 1 步骤 + 89 
环 89 元素 1:1 严格 0 漏 0 改 0 漏 1 环 + 1 显式契约 1 元素 1:1 严格 0 漏 0 改 + 1 跨链 89 环 0 触碰既有 1 元素 1:1 严格 0 漏 0 改 + 1 0 副作用 1 元素 1:1 
严格 0 漏 0 改, 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T315 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / 
§9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / 
§9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / 
§9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / 
§9.6.51 / §9.6.52 / §9.6.53 / §9.6.54 / §9.6.55 / §9.6.56 / §9.6.57 49 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / .tscn / 任何 
gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容 (除顶部 1 段 + 'Recent completed work' / '最近完成的工作' 段 1 
行 light sync), 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / 
REVIEW_LOG_ARCHIVE.md 内容 (除顶部 1 段 light sync + ITERATION_COUNT.txt +1)).

**与 §9.6.6 关系** — §9.6.6 (T100 #31 落地) 关注 "PauseMenu 单层 1 维度拼接 1:1 严格分离契约" (1 元素 1:1 严格, 1 维度 1 元素, 0 涉及 5 步骤 0 涉及 89 环 
0 涉及 跨链 89 环 0 触碰既有 0 涉及 1 0 副作用). §9.6.58 关注 "PauseMenu polish 链 89 环 1:1 严格 跨 11 任务 + 5 步骤 + 1 显式契约 + 1 跨链 89 环 0 
触碰既有 + 1 0 副作用" (97 元素 1:1 严格, 5 步骤 5 元素 + 89 环 89 元素 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用 = 97 元素). §9.6.6 与 §9.6.58 
是 "姊妹段 + 父集段" (1 套 polish 模式 × 1 维度 1 元素 1:1 严格 ⊂ 1 套 polish 模式 × 5 步骤 5 元素 + 89 环 89 元素 + 3 抽象元素 = 97 元素 1:1 严格, 1 套 
polish 模式 × 1 元素 = 1 元素 1:1 严格 ∩ 1 套 polish 模式 × 97 元素 = 97 元素 1:1 严格 = 1 元素 1:1 严格 交集 (1 维度 1 元素 1:1 严格 0 涉及 5 步骤 5 元素 
0 涉及 89 环 89 元素 0 涉及 1 显式契约 0 涉及 1 跨链 89 环 0 触碰既有 0 涉及 1 0 副作用, 1 元素 1:1 严格 是 97 元素 1:1 严格 子集 0 涉及 5 步骤 5 元素 0 涉及 89 环 
89 元素 0 涉及 1 显式契约 0 涉及 1 跨链 89 环 0 触碰既有 0 涉及 1 0 副作用). 任何"加新 1 环"必须**先**按 §9.6.6 1 元素 1:1 复制 (1 元素 1:1 严格, 0 漏 1 维度 0 改 
1 字段 0 反序 0 反向 0 漏 1 锚点 0 漏 1 字符), **再**按 §9.6.58 97 元素 1:1 拼接 5 步骤 5 元素 + 89 环 89 元素 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 
副作用 替换 1 抽象契约 1 元素 (96 元素 1:1 严格, 0 漏 1 元素 0 改 1 字段 0 反序 0 反向 0 漏 1 步骤 0 漏 1 环 0 触碰既有 §9.6.6 任何 1 字符).

**与 §9.6.39 关系** — §9.6.39 (T295 #219 落地) 关注 "T162 brittle 修复流程 5 步骤 接入 1:1 严格分离契约 模板" (5 元素 1:1 严格, 1 expect 反转 + 1 
docblock 说明 + 1 段 find 反转 + 1 0 触碰既有 + 1 cross-section 5 文件 同步, 跨 5 步骤 各 1 元素 = 5 元素 1:1 严格). §9.6.58 关注 "PauseMenu 
polish 链 89 环 1:1 严格 跨 11 任务 + 5 步骤 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用" (97 元素 1:1 严格). §9.6.39 与 §9.6.58 是 "姊妹段" (1 
套 polish 模式 × 5 元素 1:1 严格 ∩ 1 套 polish 模式 × 97 元素 1:1 严格 = 5 元素 1:1 严格 交集 (5 步骤 5 元素 1:1 严格 0 涉及 1 expect 反转 0 涉及 1 
docblock 说明 0 涉及 1 段 find 反转 0 涉及 1 0 触碰既有 0 涉及 1 cross-section 5 文件 同步, 5 元素 1:1 严格 是 97 元素 1:1 严格 子集 0 涉及 1 expect 反转 
0 涉及 1 docblock 说明 0 涉及 1 段 find 反转 0 涉及 1 0 触碰既有 0 涉及 1 cross-section 5 文件 同步 替换 1 0 副作用 1 元素). 任何"加新 1 环" (PauseMenu 
polish 链) **或** "加新 1 修复" (T162 brittle 修复流程) 必须**先**按 §9.6.39 5 元素 1:1 复制 (5 元素 1:1 严格, 0 漏 1 步 0 改 1 字段 0 反序 0 反向 0 漏 
1 锚点 0 漏 1 字符), **再**按 §9.6.58 97 元素 1:1 拼接 89 环 89 元素 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 替换 1 cross-section 5 文件 同步 1 元素 (92 
元素 1:1 严格, 0 漏 1 元素 0 改 1 字段 0 反序 0 反向 0 漏 1 环 0 触碰既有 §9.6.39 任何 1 字符).

**与 §9.6.56 关系** — §9.6.56 (T312 #241 落地) 关注 "T162 brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 
+ 1 0 触碰既有 + 1 0 副作用 polish 模式" (64 元素 1:1 严格, 5 步骤 5 元素 + 51 修复 51 元素 + 5 文件 5 元素 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 64 
元素, 0 涉及 PauseMenu polish 链 任何 1 环 0 涉及 跨链 89 环 任何 1 元素). §9.6.58 关注 "PauseMenu polish 链 89 环 1:1 严格 跨 11 任务 + 5 步骤 + 1 
显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用" (97 元素 1:1 严格). §9.6.56 与 §9.6.58 是 "姊妹段" (1 套 polish 模式 × 64 元素 = 64 元素 1:1 严格 ∩ 1 
套 polish 模式 × 97 元素 = 97 元素 1:1 严格 = 1 元素 1:1 严格 交集 (1 0 副作用 1 元素 0 涉及 5 步骤 5 元素 0 涉及 89 环 89 元素 0 涉及 1 显式契约 0 涉及 1 跨链 
89 环 0 触碰既有 0 涉及 1 0 副作用 替换 1 0 副作用 1 元素 1 元素, 1 元素 1:1 严格 是 97 元素 1:1 严格 子集 0 涉及 5 步骤 5 元素 0 涉及 89 环 89 元素 0 涉及 1 显式契约 
0 涉及 1 跨链 89 环 0 触碰既有 0 涉及 1 0 副作用 替换 1 0 副作用 1 元素). 任何"加新 1 修复" (T162 brittle 修复流程) **或** "加新 1 环" (PauseMenu polish 
链) 必须**先**按 §9.6.56 64 元素 **或** §9.6.58 97 元素 1:1 镜像 (1 套 polish 模式 各自 1:1 严格 0 漏 0 改 0 反序 0 反向), **再**走 1 套 polish 模式 
各自 1:1 同步 (1 套 polish 模式 各自 0 漏 1 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 任何 1 字符).

**与 §9.6.57 关系** — §9.6.57 (T313 #242 落地) 关注 "6 verb ability + 5 verb windup VFX 跨层 4 维度拼接 1:1 严格分离契约" (26 元素 1:1 严格, 6 
verb ability 18 元素 + 5 verb windup VFX 5 元素 + 1 显式契约 + 1 跨层 4 维度拼接 0 触碰既有 + 1 0 副作用 = 26 元素, 0 涉及 PauseMenu polish 链 任何 
1 环 0 涉及 5 步骤 任何 1 元素 0 涉及 89 环 任何 1 元素). §9.6.58 关注 "PauseMenu polish 链 89 环 1:1 严格 跨 11 任务 + 5 步骤 + 1 显式契约 + 1 跨链 89 
环 0 触碰既有 + 1 0 副作用" (97 元素 1:1 严格). §9.6.57 与 §9.6.58 是 "姊妹段" (1 套 polish 模式 × 26 元素 = 26 元素 1:1 严格 ∩ 1 套 polish 模式 × 
97 元素 = 97 元素 1:1 严格 = 1 元素 1:1 严格 交集 (1 0 副作用 1 元素 0 涉及 6 verb ability 18 元素 0 涉及 5 verb windup VFX 5 元素 0 涉及 1 显式契约 0 
涉及 1 跨层 4 维度拼接 0 触碰既有 0 涉及 1 0 副作用 替换 1 0 副作用 1 元素 1 元素, 1 元素 1:1 严格 是 97 元素 1:1 严格 子集 0 涉及 6 verb ability 18 元素 0 涉及 5 
verb windup VFX 5 元素 0 涉及 1 显式契约 0 涉及 1 跨层 4 维度拼接 0 触碰既有 0 涉及 1 0 副作用 替换 1 0 副作用 1 元素). 任何"加新 1 verb" (6 verb ability + 
5 verb windup VFX 跨层 4 维度拼接) **或** "加新 1 环" (PauseMenu polish 链) 必须**先**按 §9.6.57 26 元素 **或** §9.6.58 97 元素 1:1 镜像 (1 套 
polish 模式 各自 1:1 严格 0 漏 0 改 0 反序 0 反向), **再**走 1 套 polish 模式 各自 1:1 同步 (1 套 polish 模式 各自 0 漏 1 元素 0 漏 1 边 0 改 1 字段 0 改 
1 字符 0 反向 0 触碰既有 任何 1 字符).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.58 5 步骤 5 元素 + 89 环 89 元素 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用 = 97 元素 走 §9.1 
9 步落地的 0 步 (PauseMenu polish 链 89 环 跨 11 任务 5 步骤 + 89 环 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用 是 pause_menu.gd polish 
工具链 + 文档化 范畴, 0 走 9 步任何 1 步), 0 触碰 9 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 
`icon` PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染). 任何"加新 1 环" (e.g. 未来 #245+ 加新 1 PauseMenu polish 环 = 
90 环) 必须**先**按 §9.6.58 97 元素 1:1 镜像 (97 元素 1:1 严格, 0 漏 1 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 步骤 0 漏 1 环 0 漏 1 文件 0 
触碰既有 9 步 (`verb` 枚举 + `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `vfx` 视觉组 + `audio` preset 字段 
+ `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 步"必须**先**在 §9.6.58 97 元素 加 1 边 (1 环 1 步骤 1 元素 1 边 或 1 显式契约 1 边 或 1 跨链 89 环 0 触碰既有 1 边 或 
1 0 副作用 1 边), 0 触碰既有 9 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 
`vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).


### 9.6.59 T162 brittle 修复流程 53 修复 1:1 严格 跨 14 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 polish 模式 
(T312 #241 + T316 #246 跨 14 审查轮 累计 53 修复 ~54 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / §9.6.51 / §9.6.52 / §9.6.53 / §9.6.54 / §9.6.55 / §9.6.56 
> / §9.6.57 / §9.6.58 49 套 polish 模式均在 "pause_menu / 6 verb 单层 / 6 verb 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / 
> PlayerStats 持久化层 / audio 数据层 / PlayerActionGate 单 autoload 4 件套 / SaveSystem CRC32 校验 / T162 brittle 修复流程 5 步骤模板 / 6 
> verb audio 家族 19 cue / 6 verb cooldown ready jingle 5 段 / 6 verb `_ready()` / `_exit_tree()` 双 hook 串联 / 5 verb 
> windup VFX 4 hook lifecycle / 5 verb windup VFX `_draw()` verb-specific / 5 verb windup VFX `trigger()` verb-specific 
> / 5 verb windup VFX 共享 4 hook 0 override / 5 verb windup VFX `trigger()` + `_draw()` 双 verb-specific / 5 verb windup 
> VFX base 3 内部状态字段 / 5 verb windup VFX 视觉组连贯 lifecycle / 5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 / 5 verb windup VFX 
> `trigger()` + 视觉组 拼接 / 5 verb windup VFX `trigger()` + 视觉组 + base 3 内部状态字段 3 维度拼接 / T162 brittle 修复流程 51 修复 / 6 verb 
> ability + 5 verb windup VFX 跨层 4 维度拼接 / PauseMenu polish 链 89 环" 范畴, 0 涉及 "T162 brittle 修复流程 53 修复 1:1 严格 跨 14 审查轮 + 
> 5 步骤 + 5 文件 light sync 1:1 严格 0 漏 0 改 0 反向" 独立聚焦段 落地. §9.6.39 (T295 #219 落地) 关注 "T162 brittle 修复流程 5 步骤 1:1 严格分离契约 
> 模板" (5 元素 1:1 严格, 1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 1 0 触碰既有 + 1 cross-section 5 文件 同步, 跨 5 步骤 各 1 元素 = 5 
> 元素 1:1 严格). §9.6.56 (T312 #241 落地) 关注 "T162 brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 + 1 
> 0 触碰既有 + 1 0 副作用 polish 模式" (64 元素 1:1 严格). §9.6.58 (T315 #244 落地) 关注 "PauseMenu polish 链 89 环 1:1 严格 跨 11 任务 + 5 步骤 
> + 1 显式契约 + 1 跨链 89 环 0 触碰既有 + 1 0 副作用 polish 模式" (97 元素 1:1 严格). 上述 3 段 (§9.6.39 + §9.6.56 + §9.6.58) 均聚焦 5 步骤 模板 OR 
> 51 修复 OR PauseMenu 89 环 单范畴, 0 涉及 "T162 brittle 修复流程 53 修复 1:1 严格 跨 14 审查轮" (即 14 审查轮 × 53 修复 × 5 步骤 1:1 严格 镜像, 跨 53 
> 修复 各自 5 步骤 1:1 严格 0 漏 0 改 0 反序 0 反向). 本节 (T316 #246 落地) 首个落在**T162 brittle 修复流程 53 修复 1:1 严格 跨 14 审查轮 + 5 步骤 + 5 文件 
> light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用** 聚焦段 (5 步骤 5 元素 1:1 严格 (Stage 1 expect 反转 1:1 严格 0 漏 1 字段 + Stage 2 
> docblock 说明 1:1 严格 0 漏 1 字符 + Stage 3 段 find 反转 1:1 严格 0 漏 1 锚点 + Stage 4 0 触碰既有 1:1 严格 0 漏 1 段 + Stage 5 
> cross-section 5 文件 同步 1:1 严格 0 漏 1 文件, 跨 5 步骤 各 1 元素 = 5 元素 1:1 严格 0 漏 0 改 0 反序 0 反向) + 53 修复 53 元素 1:1 严格 (T162 #185 
> 起步 1 + #187 1 + #190 1 + #195 3 + #200 4 + #205 3 + #210 14 + #215 4 + #220 3 + #225 6 + #230 7 + #235 2 + #240 2 + 
> #245 2 = 53 修复, 跨 53 修复 各自 5 步骤 1:1 严格 镜像, 0 漏 1 修复 0 改 1 字段 0 改 1 字符 0 反序 0 反向) + 5 文件 light sync 5 元素 1:1 严格 
> (CHANGELOG.md 顶部 1 段 + ROADMAP.md 顶部 1 时间戳 + README.md 'Recent completed work' 1 段 + README.zh-CN.md '最近完成的工作' 1 段 + 
> ITERATION_COUNT.txt 1 计数, 跨 5 文件 各 1 元素 = 5 元素 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "T162 brittle 修复流程 53 修复 1:1 严格 跨 
> 14 审查轮 + 5 步骤 + 5 文件 light sync 1:1 严格 (1 修复 必 5 步骤 1:1 严格 必 5 文件 1:1 严格, 53 修复 0 漏 1 步 0 漏 1 文件 0 改 1 字段 0 反序 0 反向)" 
> 1 段 1:1 严格 (1 契约 1 元素) + 1 0 触碰既有 1:1 严格 (1 修复 1 5 步骤 1:1 严格 镜像 1 5 文件 1:1 严格 拼接 0 改 §9.6.39 5 步骤 模板 任何 1 字符 0 改 
> §9.6.56 64 元素 任何 1 字符 0 改 §9.6.58 97 元素 任何 1 字符 0 改 跨 52 修复 任何 1 字符 0 漏 1 边 0 反序 0 反向, 1 抽象契约 1 元素) + 1 0 副作用 1:1 严格 
> (1 修复 1 5 步骤 0 触碰既有 工具链 任何 1 文件 0 触碰游戏代码 任何 1 字段 0 触碰 §10 决策记录流程 0 触碰 README 内容 0 触碰 ITERATION_COUNT 0 触碰 CHANGELOG / 
> ROADMAP / REVIEW_LOG 内容, 1 抽象契约 1 元素)) polish 模式 — 跨 5 步骤 × 53 修复 × 5 文件 = 5 + 53 + 5 = 63 元素 1:1 严格 + 1 显式契约 + 1 0 
> 触碰既有 + 1 0 副作用 = 3 元素 1:1 严格 = 5 + 53 + 5 + 1 + 1 + 1 = 66 元素 1:1 严格 (注意: 5 步骤 5 元素 1:1 严格 = 5 步骤 各自 1 元素 = 5 元素 + 53 
> 修复 53 元素 1:1 严格 = 53 修复 各自 1 元素 = 53 元素 + 5 文件 5 元素 1:1 严格 = 5 文件 各自 1 元素 = 5 元素 + 1 显式契约 1 元素 + 1 0 触碰既有 1 元素 + 1 0 
> 副作用 1 元素 = 5 + 53 + 5 + 1 + 1 + 1 = 66 元素 1:1 严格, 0 漂动 0 后续 主要 pre-existing 风险) 0 触碰既有 52 修复 任何 1 字符 0 反向 0 例外. 任何"加新 
> 1 修复" (e.g. 未来 #250+ 加新 1 brittle 修复) 必须**先**按 Stage 1 expect 反转 1:1 严格 加 1 修复 entry (Stage 1 1:1 严格, 0 漏 1 字段 0 改 1 
> 字符 0 反序 0 反向, **0 漏 1 步 0 漏 1 文件 0 漏 1 修复**), **再**按 Stage 2 docblock 说明 1:1 严格 加 1 修复 entry, **再**按 Stage 3 段 find 
> 反转 1:1 严格 加 1 修复 entry, **再**按 Stage 4 0 触碰既有 1:1 严格 镜像 (0 漏 1 段 0 改 1 字符 0 反序 0 反向), **再**按 Stage 5 cross-section 5 
> 文件 同步 1:1 严格 镜像 (0 漏 1 文件 0 改 1 字符 0 反序 0 反向). 跨 1 套 polish 模式 × 5 步骤 × 53 修复 × 5 文件 + 3 元素 = 5 + 53 + 5 + 1 + 1 + 1 
> = 66 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: `T162 brittle 修复流程 53 修复` (#185 1 + #187 1 + #190 1 + #195 3 + #200 4 + #205 3 + #210 14 + #215 4 + #220 3 + 
#225 6 + #230 7 + #235 2 + #240 2 + #245 2 = 53 修复, 跨 14 审查轮 累计 ~54 轮 落地) 现有的 5 步骤 (Stage 1 expect 反转 1:1 严格 + Stage 2 
docblock 说明 1:1 严格 + Stage 3 段 find 反转 1:1 严格 + Stage 4 0 触碰既有 1:1 严格 + Stage 5 cross-section 5 文件 同步 1:1 严格) + 5 文件 
light sync (CHANGELOG.md + ROADMAP.md + README.md + README.zh-CN.md + ITERATION_COUNT.txt), 在反复 polish (T162 #185 起步 → 
#245 累计 53 修复 ~54 轮 落地) 期间出现 4 类典型 fragile: (1) **"加新 1 修复 漏 5 步骤 1:1 严格"** — 加新 1 修复 (e.g. 假设未来 #250+ 加新 1 §9.6.60 段 
polish 触发 1 修复) 落地时漏 5 步骤 (e.g. 漏 Stage 1 expect 反转 中 expect 改 反向, 或漏 Stage 2 docblock 说明 中 docblock 解释为什么反向, 或漏 Stage 
3 段 find 反转 中 find 字符串 改 反向, 或漏 Stage 4 0 触碰既有 中既 verify test_*.gd 任何其他 expect / docblock / find 0 改, 或漏 Stage 5 
cross-section 5 文件 同步 中 5 文件 light sync 缺 1), 1 修复 0 5 步骤 0 闭环 0 漂动; (2) **"加新 1 docblock 漏 1 行"** — 加新 1 docblock 
(e.g. 假设未来 #250+ 加新 1 修复 的 docblock 行 解释) 落地时漏 1 行 (e.g. 漏 docblock 上方 `#` 注释行 或 docblock 自身行 1 行), 1 docblock 扩展 0 5 
步骤 0 闭环 0 漂动; (3) **"修 1 expect 0 触碰既有 52 修复"** — 修 1 expect (e.g. 改 1 test_*.gd 中 expect 行 0 改 既有) 或 修 1 段 find 字符串 
(e.g. 改 1 test_*.gd 中 find 字符串 0 改 既有) 或 加新 1 修复 (e.g. 跨 53 修复 加 1 修复 = 54 修复) 时 0 触碰既有 52 修复 任何 1 字段, 跨修复 1:1 漂动; (4) 
**"§9.6.56 51 修复 文档 漂 53 修复"** — §9.6.56 段 锚定 51 修复 / 13 审查轮 0 同步 53 修复 / 14 审查轮 (e.g. 假设未来 polish 改 §9.6.56 段 51→53 但 
0 改 13→14 审查轮, 或 0 改 64 元素 → 66 元素, 1 元素 漂动 0 闭环), 1 文档 漂动 0 53 修复 0 闭环 0 漂动. 4 类症状根因: T162 brittle 修复流程 53 修复 1:1 序列 0 
文档化独立聚焦段, 任何新 1 修复 接入只能"凭经验" 1:1 镜像 5 步骤 模板 (§9.6.39 5 元素 1:1 严格) 或 51 修复 锚定 (§9.6.56 64 元素 1:1 严格), 1 漏 1 步 = 1 修复 0 5 
步骤 0 闭环 0 漂动 (1 漏 1 步 0 5 步骤 0 1:1 严格分离契约 0 漂动 / 1 漏 1 行 0 docblock 0 1:1 严格 0 漂动 / 1 漏 1 字符 0 cross-section 0 1:1 严格 0 
漂动 / 1 漏 1 字符 0 §9.6.56 0 同步 0 漂动).

- **触发场景**: 加新 1 修复 (e.g. 未来 #250+ 加新 1 brittle 修复 走 T162 流程, 实际现状: 53 修复 跨 14 审查轮 累计 0 漏 0 改 0 反序 0 反向) / 加新 1 
docblock (T162 流程参考) / 加新 1 cross-section 文件 (e.g. 假设未来加 REVIEW_LOG.md 顶部 1 段 同步) 时**必须**严格按 66 元素 1:1 复制 T312 #241 + 
T316 #246 跨 53 修复 落地模式: (1) **Stage 1 expect 反转 1:1 严格** — 在 test_*.gd 中定位 1 expect 行, 改 反向 期望 == / != / ≥ / ≤ 1 字符 
(Stage 1 1:1 严格, 0 漏 1 字段 0 改 1 字符 0 反序 0 反向, **0 漏 1 修复 0 漏 1 步**); (2) **Stage 2 docblock 说明 1:1 严格** — 在 expect 上方加 
1 行 docblock 解释为什么反向 (Stage 2 1:1 严格, 0 漏 1 字符 0 改 1 字符 0 反序 0 反向, 0 漏 1 行 0 触碰既有 4 步骤); (3) **Stage 3 段 find 反转 1:1 
严格** — 在 test_*.gd 中定位 1 段 find 字符串, 改 反向 字符串 (Stage 3 1:1 严格, 0 漏 1 锚点 0 改 1 锚点 0 反序 0 反向, 0 漏 1 段 0 触碰既有 4 步骤); (4) 
**Stage 4 0 触碰既有 1:1 严格 镜像** — 0 改 test_*.gd 任何其他 expect / docblock / find / 段 (Stage 4 1:1 严格, 0 漏 1 段 0 改 1 字符 0 反序 0 
反向, 0 漏 1 段 0 漏 1 文件 0 漏 1 锚点); (5) **Stage 5 cross-section 5 文件 同步 1:1 严格 镜像** — CHANGELOG.md 顶部 1 段 + ROADMAP.md 顶部 1 
时间戳 + README.md 'Recent completed work' 1 段 + README.zh-CN.md '最近完成的工作' 1 段 + ITERATION_COUNT.txt 1 计数 (Stage 5 1:1 严格, 
0 漏 1 文件 0 改 1 字符 0 反序 0 反向, 0 漏 1 字段 0 漏 1 锚点 0 漏 1 边 0 漏 1 修复). 66 元素 1:1 同步 (5 步骤 5 元素 1:1 严格 + 53 修复 53 元素 1:1 严格 + 
5 文件 5 元素 1:1 严格 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 66 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 漏 1 修复 0 漏 1 步 0 漏 1 文件), 1 漏 1 步 / 1 
漏 1 修复 / 1 漏 1 文件 = 1 修复 / 1 步 / 1 文件 扩展 0 66 元素 0 闭环 0 漂动. T312 #241 + T316 #246 跨 53 修复 0 触碰既有 0 改 test_*.gd 任何其他 
expect 任何 1 字符, 0 改 test_*.gd 任何其他 docblock 任何 1 字符, 0 改 test_*.gd 任何其他 段 find 任何 1 字符, 0 改 既有 52 修复 任何 1 字段, 0 改 
§9.6.39 5 步骤 模板 任何 1 字符, 0 改 §9.6.55 17 元素 任何 1 字符, 0 改 §9.6.56 64 元素 任何 1 字符, 0 改 §9.6.57 26 元素 任何 1 字符, 0 改 §9.6.58 
97 元素 任何 1 字符.

- **修复** (跨 1 套 polish 模式 T316 #246 落地后已知 66 元素 0 例外):
  - **T316 #246 首个 T162 brittle 修复流程 53 修复 1:1 严格 跨 14 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 独立聚焦段 
落地 (5 步骤 5 元素 1:1 严格 + 53 修复 53 元素 1:1 严格 + 5 文件 5 元素 1:1 严格 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 5 + 53 + 5 + 1 + 1 + 1 = 
66 元素 1:1 严格)** — §9.6.59 段 是 1 套 polish 模式首个 1:1 包含 5 步骤 (Stage 1 expect 反转 1:1 严格 + Stage 2 docblock 说明 1:1 严格 + 
Stage 3 段 find 反转 1:1 严格 + Stage 4 0 触碰既有 1:1 严格 + Stage 5 cross-section 5 文件 同步 1:1 严格) + 53 修复 (T162 跨 #185-#245 累计 
53 修复 1:1 严格 镜像 5 步骤 模板, 0 漏 1 修复 0 改 1 字段 0 反序 0 反向) + 5 文件 light sync (CHANGELOG.md + ROADMAP.md + README.md + 
README.zh-CN.md + ITERATION_COUNT.txt) + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 5 + 53 + 5 + 1 + 1 + 1 = 66 元素 1:1 严格 的 T162 
brittle 修复流程 53 修复 1:1 严格 polish 模式, 66 元素 1:1 严格分离 (5 步骤 5 元素 1:1 严格 0 漏 0 改 0 漏 1 步 + 53 修复 53 元素 1:1 严格 0 漏 0 改 0 漏 
1 修复 + 5 文件 5 元素 1:1 严格 0 漏 0 改 0 漏 1 文件 + 1 显式契约 1 元素 1:1 严格 0 漏 0 改 + 1 0 触碰既有 1 元素 1:1 严格 0 漏 0 改 + 1 0 副作用 1 元素 1:1 
严格 0 漏 0 改, 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T316 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / 
§9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / 
§9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / 
§9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / 
§9.6.51 / §9.6.52 / §9.6.53 / §9.6.54 / §9.6.55 / §9.6.56 / §9.6.57 / §9.6.58 49 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / 
.tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容 (除顶部 1 段 + 'Recent completed work' / 
'最近完成的工作' 段 1 行 light sync), 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / 
REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容 (除顶部 1 段 light sync + ITERATION_COUNT.txt +1)).

**与 §9.6.39 关系** — §9.6.39 (T295 #219 落地) 关注 "T162 brittle 修复流程 5 步骤 接入 1:1 严格分离契约 模板" (5 元素 1:1 严格, 1 expect 反转 + 1 
docblock 说明 + 1 段 find 反转 + 1 0 触碰既有 + 1 cross-section 5 文件 同步, 跨 5 步骤 各 1 元素 = 5 元素 1:1 严格). §9.6.59 关注 "T162 brittle 
修复流程 53 修复 1:1 严格 跨 14 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用" (66 元素 1:1 严格, 5 步骤 5 元素 + 53 修复 53 
元素 + 5 文件 5 元素 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 66 元素). §9.6.39 与 §9.6.59 是 "姊妹段 + 母集段" (1 套 polish 模式 × 5 步骤 5 元素 1:1 
严格 ⊂ 1 套 polish 模式 × 66 元素 1:1 严格, 1 套 polish 模式 × 5 元素 = 5 元素 1:1 严格 ∩ 1 套 polish 模式 × 66 元素 = 66 元素 1:1 严格 = 5 元素 1:1 
严格 交集 (5 步骤 5 元素 + 5 文件 5 元素 0 涉及 53 修复 任何 1 修复 0 涉及 1 显式契约 0 涉及 1 0 触碰既有 0 涉及 1 0 副作用, 5 元素 1:1 严格 是 66 元素 1:1 严格 子集 0 
涉及 53 修复 任何 1 修复 0 涉及 1 显式契约 0 涉及 1 0 触碰既有 0 涉及 1 0 副作用). 任何"加新 1 修复"必须**先**按 §9.6.39 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步 
0 改 1 字段 0 反序 0 反向 0 漏 1 锚点 0 漏 1 字符 0 漏 1 文件), **再**按 §9.6.59 66 元素 1:1 拼接 53 修复 53 元素 替换 1 抽象契约 1 元素 (53 元素 1:1 严格, 0 
漏 1 修复 0 改 1 字段 0 反序 0 反向 0 漏 1 段 0 触碰既有 §9.6.39 任何 1 字符 0 触碰既有 52 修复 任何 1 字符).

**与 §9.6.56 关系** — §9.6.56 (T312 #241 落地) 关注 "T162 brittle 修复流程 51 修复 1:1 严格 跨 13 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 
+ 1 0 触碰既有 + 1 0 副作用 polish 模式" (64 元素 1:1 严格, 5 步骤 5 元素 + 51 修复 51 元素 + 5 文件 5 元素 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 64 
元素, 0 涉及 53 修复 任何 1 修复 0 涉及 #245 2 修复 任何 1 修复 0 涉及 14 审查轮 任何 1 轮 0 涉及 66 元素 任何 1 元素). §9.6.59 关注 "T162 brittle 修复流程 53 
修复 1:1 严格 跨 14 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用" (66 元素 1:1 严格, 5 步骤 5 元素 + 53 修复 53 元素 + 5 
文件 5 元素 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 66 元素). §9.6.56 与 §9.6.59 是 "姊妹段 + 母集段" (1 套 polish 模式 × 64 元素 1:1 严格 ⊂ 1 套 
polish 模式 × 66 元素 1:1 严格, 1 套 polish 模式 × 64 元素 = 64 元素 1:1 严格 ∩ 1 套 polish 模式 × 66 元素 = 66 元素 1:1 严格 = 64 元素 1:1 严格 交集 
(5 步骤 5 元素 + 51 修复 51 元素 + 5 文件 5 元素 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 64 元素, 64 元素 1:1 严格 是 66 元素 1:1 严格 子集 0 涉及 #245 2 
修复 任何 1 修复 0 涉及 14 审查轮 中 #245 1 轮 0 涉及 66 元素 → 64 元素 +2 修复 0 涉及 1 显式契约 0 涉及 1 0 触碰既有 0 涉及 1 0 副作用). 任何"加新 1 修复" 
必须**先**按 §9.6.56 64 元素 1:1 复制 (64 元素 1:1 严格, 0 漏 1 元素 0 改 1 字段 0 反序 0 反向 0 漏 1 锚点 0 漏 1 字符 0 漏 1 文件), **再**按 §9.6.59 66 
元素 1:1 拼接 #245 2 修复 2 元素 替换 1 抽象契约 1 元素 (2 元素 1:1 严格, 0 漏 1 修复 0 改 1 字段 0 反序 0 反向 0 漏 1 段 0 触碰既有 §9.6.56 任何 1 字符 0 触碰既有 
51 修复 任何 1 字符 0 漏 1 修复 0 漏 1 边 0 漏 1 锚点).

**与 §9.6.58 关系** — §9.6.58 (T315 #244 落地) 关注 "PauseMenu polish 链 89 环 1:1 严格 跨 11 任务 + 5 步骤 + 1 显式契约 + 1 跨链 89 环 0 触碰既有 
+ 1 0 副作用 polish 模式" (97 元素 1:1 严格, 0 涉及 T162 brittle 修复流程 任何 1 修复 0 涉及 14 审查轮 任何 1 轮 0 涉及 53 修复 任何 1 修复). §9.6.59 关注 
"T162 brittle 修复流程 53 修复 1:1 严格 跨 14 审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用" (66 元素 1:1 严格). 
§9.6.58 与 §9.6.59 是 "姊妹段" (1 套 polish 模式 × 97 元素 = 97 元素 1:1 严格 ∩ 1 套 polish 模式 × 66 元素 = 66 元素 1:1 严格 = 1 元素 1:1 严格 交集 
(1 0 副作用 1 元素 0 涉及 5 步骤 5 元素 0 涉及 53 修复 53 元素 0 涉及 5 文件 5 元素 0 涉及 1 显式契约 0 涉及 1 0 触碰既有 0 涉及 1 0 副作用 替换 1 0 副作用 1 元素 1 
元素, 1 元素 1:1 严格 是 66 元素 1:1 严格 子集 0 涉及 5 步骤 5 元素 0 涉及 53 修复 53 元素 0 涉及 5 文件 5 元素 0 涉及 1 显式契约 0 涉及 1 0 触碰既有 0 涉及 1 0 副作用 
替换 1 0 副作用 1 元素 1 元素). 任何"加新 1 修复" (T162 brittle 修复流程) **或** "加新 1 环" (PauseMenu polish 链) 必须**先**按 §9.6.58 97 元素 **或** 
§9.6.59 66 元素 1:1 镜像 (1 套 polish 模式 各自 1:1 严格 0 漏 0 改 0 反序 0 反向), **再**走 1 套 polish 模式 各自 1:1 同步 (1 套 polish 模式 各自 0 漏 
1 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 任何 1 字符).

**与 §9.6.57 关系** — §9.6.57 (T313 #242 落地) 关注 "6 verb ability + 5 verb windup VFX 跨层 4 维度拼接 1:1 严格分离契约" (26 元素 1:1 严格, 0 
涉及 T162 brittle 修复流程 任何 1 修复 0 涉及 14 审查轮 任何 1 轮 0 涉及 53 修复 任何 1 修复). §9.6.59 关注 "T162 brittle 修复流程 53 修复 1:1 严格 跨 14 
审查轮 + 5 步骤 + 5 文件 light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用" (66 元素 1:1 严格). §9.6.57 与 §9.6.59 是 "姊妹段" (1 套 polish 模式 × 
26 元素 = 26 元素 1:1 严格 ∩ 1 套 polish 模式 × 66 元素 = 66 元素 1:1 严格 = 0 元素 1:1 严格 交集 (0 元素 0 互混 0 复用 0 共享, §9.6.57 关注 6 verb 
ability + 5 verb windup VFX 跨层 4 维度拼接 范畴, §9.6.59 关注 T162 brittle 修复流程 53 修复 跨 14 审查轮 范畴, 26 元素 0 涉及 5 步骤 5 元素 0 涉及 53 
修复 53 元素 0 涉及 5 文件 5 元素 0 涉及 1 显式契约 0 涉及 1 0 触碰既有 0 涉及 1 0 副作用). 任何"加新 1 修复" (T162 brittle 修复流程) **或** "加新 1 verb" (6 
verb ability + 5 verb windup VFX 跨层 4 维度拼接) 必须**先**按 §9.6.57 26 元素 **或** §9.6.59 66 元素 1:1 镜像 (1 套 polish 模式 各自 1:1 严格 
0 漏 0 改 0 反序 0 反向), **再**走 1 套 polish 模式 各自 1:1 同步 (1 套 polish 模式 各自 0 漏 1 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 任何 
1 字符).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 
0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 53 修复 累计 0 后续 持续收敛), §9.6.59 是 "T162 brittle 修复流程 53 修复 1:1 严格 跨 14 审查轮 + 5 步骤 + 5 文件 
light sync + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用" (66 元素 1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约). T162 与 §9.6.59 0 互混 0 复用 0 
共享: T162 关注 "brittle 修复流程 5 步骤" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 + 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 
漏 1 段 0 漏 1 锚点, 5 步骤 1:1 严格), §9.6.59 关注 "5 步骤 + 53 修复 + 5 文件 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 1:1 严格分离契约 + 1 套 polish 模式 
既有 1:1 严格分离契约" (与 5 步骤 0 相关, 1 套 polish 模式 × 66 元素 = 66 元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离, 1 套 polish 模式 × 66 元素 = 66 元素 1:1 
严格). 任何"加新 1 修复"必须**先**按 T162 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), **再**按 
§9.6.59 66 元素 1:1 镜像 (66 元素 1:1 严格, 0 漏 1 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点 0 漏 1 文件).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.59 5 步骤 + 53 修复 + 5 文件 + 1 显式契约 + 1 0 触碰既有 + 1 0 副作用 = 66 元素 走 §9.1 9 步落地的 0 步 
(T162 brittle 修复流程 53 修复 跨 14 审查轮 5 步骤 + 5 文件 light sync 接入 是 polish 工具链 范畴, 0 走 9 步任何 1 步), 0 触碰 9 步 (1 `verb` 枚举 + 1 
`ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 
冷光勾边` 渲染). 任何"加新 1 修复"必须**先**按 §9.6.59 66 元素 1:1 镜像 (66 元素 1:1 严格, 0 漏 1 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 修复 0 漏 
1 步 0 漏 1 文件 0 触碰既有 9 步 (`verb` 枚举 + `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `vfx` 视觉组 + 
`audio` preset 字段 + `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 步"必须**先**在 §9.6.59 66 元素 加 1 边 (1 修复 1 5 步骤 1 边 或 1 显式契约 1 边 或 1 0 
触碰既有 1 边 或 1 0 副作用 1 边), 0 触碰既有 9 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 
`icon` PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).
