# Polish Patterns 9.6.50–51

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.50–9.6.51，共 2 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.50-51.md

<!-- shard 50-51 共 2 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.50 5 verb windup VFX `trigger()` + `_draw()` 双 verb-specific 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段 
polish 模式 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 + T304 #231 + T305 #232 跨 9 任务 
~150 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 40 套 polish 模式均在 "pause_menu / 6 verb 单层 / 6 verb 跨组件 / Hub / 成就 / 
> prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / audio 数据层 / PlayerActionGate 单 autoload 4 件套 / SaveSystem CRC32 校验 / T162 
> brittle 修复流程 / 6 verb audio 家族 19 cue / 6 verb cooldown ready jingle 5 段 / 6 verb `_ready()` super 调用顺序 6 verb 1:1 
> 严格分离契约 / 6 verb `_exit_tree()` super 调用顺序 6 verb 1:1 严格分离契约 / 6 verb 双 hook 串联 1:1 严格分离契约 / 6 verb 双 hook 串联 + 
> `_player` non-null assertion 0 触碰既有 1:1 严格分离契约 / 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 
> 视觉组连贯 lifecycle 1:1 严格分离契约 / 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 / 5 verb windup VFX `_draw()` 
> verb-specific 1:1 严格分离契约 / 5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约 / 5 verb windup VFX 共享 4 hook 0 
> override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段" 范畴, 0 涉及 "5 verb windup VFX `trigger()` + `_draw()` 双 verb-specific 0 
> override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段" 独立聚焦段落地. §9.6.47 (T303 #229 落地) 关注 "5 verb windup VFX `_draw()` 
> verb-specific 1:1 严格分离契约" (30 元素 1:1 严格, 5 verb `_draw()` + 5 verb 共享 3 字段 + 5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 + 
> 1 共享 3 字段 0 触碰既有 + 1 base `_draw()` 0 override + 1 `_draw()` 0 override verb-specific 0 触碰既有 = 30 元素). §9.6.48 (T304 
> #231 落地) 关注 "5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约" (32 元素 1:1 严格, 5 verb `trigger()` 0 override 
> verb-specific 1:1 严格 + 5 verb 共享 4 元素 1:1 严格 + 2 verb verb-specific 1 元素 1:1 严格 + 1 显式契约 + 1 verb-specific 0 触碰既有 + 1 
> 共享 4 元素 0 触碰既有 + 1 base `trigger()` 0 override + 1 `trigger()` 0 override verb-specific 0 触碰既有 = 32 元素). §9.6.49 
> (T305 #232 落地) 关注 "5 verb windup VFX 共享 4 hook 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段" (9 元素 1:1 严格, 5 verb 0 
> override 4 hook + 1 base 4 hook 0 触碰既有 + 1 Whisper 1 hook override + 1 显式契约 + 1 4 hook 0 override verb-specific 0 
> 触碰既有 = 9 元素, 0 涉及 5 verb `trigger()` + 5 verb `_draw()` 自身 1:1 严格分离). 本节 (T306 #233 落地) 首个落在**5 verb windup VFX 
> `trigger()` + `_draw()` 双 verb-specific 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段** (5 verb 0 override 4 共享 hook 
> 1:1 严格 (5 verb 各自 0 override `_ready()` + 0 override `_process()` + 0 override `_activate_windup_tween()` + 0 
> override `fade_out_and_free()` 1:1 严格 0 触碰 base 1:1 严格 0 漏 0 改 0 反序 0 反向) + 5 verb 0 override `trigger()` 
> verb-specific 1:1 严格 (5 verb 各自 `trigger()` 0 override verb-specific 仅设 verb-specific 状态 + call 
> `_activate_windup_tween()` 后 0 触碰 base 1:1 严格 0 漏 0 改 0 反序 0 反向) + 5 verb 0 override `_draw()` verb-specific 1:1 严格 
> (5 verb 各自 `_draw()` 0 override verb-specific 仅设 verb-specific 视觉组 + draw 后 0 触碰 base 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 
> 显式契约 "5 verb windup VFX 双 verb-specific (`trigger()` + `_draw()`) 0 override verb-specific 0 触碰既有 1:1 严格 + 5 verb 共享 
> 4 hook 0 override 1:1 严格" 1 段 1:1 严格 + 1 双 verb-specific 0 触碰既有 1:1 严格) polish 模式 — 跨 5 verb × 4 hook 0 override + 5 
> verb × 1 `trigger()` 0 override + 5 verb × 1 `_draw()` 0 override + 1 显式契约 + 1 双 verb-specific 0 触碰既有 = 5 + 5 + 5 + 1 
> + 1 = 17 元素 1:1 严格 (注意: 5 verb 0 override 4 shared hook 1:1 严格 = 5 verb 各自 1 元素 = 5 元素 + 5 verb 0 override 
> `trigger()` 1:1 严格 = 5 verb 各自 1 元素 = 5 元素 + 5 verb 0 override `_draw()` 1:1 严格 = 5 verb 各自 1 元素 = 5 元素 + 1 显式契约 1 元素 
> + 1 双 verb-specific 0 触碰既有 1 元素 = 5 + 5 + 5 + 1 + 1 = 17 元素 1:1 严格, 0 漂动 0 后续 主要 pre-existing 风险) 0 触碰既有 5 verb 任何 1 
> 字符 0 反向 0 例外. 任何"加新 1 verb windup VFX 双 verb-specific 0 override" (e.g. 未来加 6th verb windup VFX "WhisperWindupVFX" 0 
> 接入 双 verb-specific 1:1 严格镜像, 实际现状: Whisper 已存在但 0 override 双 verb-specific 1:1 严格, 0 override `trigger()` + 0 
> override `_draw()` 镜像 base 0 漏 0 改 0 反向) 必须**先**按 Stage 1 Pulse 1 verb 0 override 双 verb-specific 1:1 镜像 加 1 verb 
> entry (Stage 1 1:1 严格, 0 漏 1 verb 0 改 1 method 0 反序 0 反向, **0 漏 1 verb**), **再**按 Stage 2 Bind / Stage 3 Cut / Stage 
> 4 Echo / Stage 5 Wave 1 verb 0 override 双 verb-specific 1:1 镜像 4 verb entries (4 verb 1:1 严格, 0 漏 1 verb 0 改 1 method 
> 0 反序 0 反向, 5 verb 0 漏 0 改 0 反序 0 例外). 跨 1 套 polish 模式 × 5 verb 0 override 4 shared hook + 5 verb 0 override 
> `trigger()` + 5 verb 0 override `_draw()` + 1 显式契约 + 1 双 verb-specific 0 触碰既有 = 5 + 5 + 5 + 1 + 1 = 17 元素 1:1 严格 0 漏 
> 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: 5 verb windup VFX (`src/scripts/pulse_windup_vfx.gd` + `src/scripts/bind_windup_vfx.gd` + 
`src/scripts/cut_windup_vfx.gd` + `src/scripts/echo_windup_vfx.gd` + `src/scripts/wave_windup_vfx.gd`) 双 verb-specific 
(`trigger()` + `_draw()`) 0 override verb-specific 0 触碰既有 1:1 严格分离契约 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 
#89 + T302 #228 + T303 #229 + T304 #231 + T305 #232 跨 9 任务 ~150 轮落地) 现有的 17 元素 1:1 严格 (1 Pulse 0 override 4 共享 hook 1:1 
严格 (1 0 override `_ready()` + 1 0 override `_process()` + 1 0 override `_activate_windup_tween()` + 1 0 override 
`fade_out_and_free()` = 4 hook 0 override 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 Bind 0 override 4 共享 hook 1:1 严格 (4 hook 0 
override 1:1 严格) + 1 Cut 0 override 4 共享 hook 1:1 严格 (4 hook 0 override 1:1 严格) + 1 Echo 0 override 4 共享 hook 1:1 严格 (4 
hook 0 override 1:1 严格) + 1 Wave 0 override 4 共享 hook 1:1 严格 (4 hook 0 override 1:1 严格) + 1 Pulse 0 override 
`trigger()` verb-specific 1:1 严格 (1 verb 0 override `trigger()` verb-specific 仅设 verb-specific 状态 + call 
`_activate_windup_tween()` 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 Bind 0 override `trigger()` verb-specific 1:1 严格 (1 verb 0 
override `trigger()` verb-specific 1:1 严格) + 1 Cut 0 override `trigger()` verb-specific 1:1 严格 (1 verb 0 override 
`trigger()` verb-specific 1:1 严格) + 1 Echo 0 override `trigger()` verb-specific 1:1 严格 (1 verb 0 override `trigger()` 
verb-specific 1:1 严格) + 1 Wave 0 override `trigger()` verb-specific 1:1 严格 (1 verb 0 override `trigger()` verb-specific 
1:1 严格) + 1 Pulse 0 override `_draw()` verb-specific 1:1 严格 (1 verb 0 override `_draw()` verb-specific 仅设 verb-specific 
视觉组 + draw 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 Bind 0 override `_draw()` verb-specific 1:1 严格 (1 verb 0 override `_draw()` 
verb-specific 1:1 严格) + 1 Cut 0 override `_draw()` verb-specific 1:1 严格 (1 verb 0 override `_draw()` verb-specific 1:1 
严格) + 1 Echo 0 override `_draw()` verb-specific 1:1 严格 (1 verb 0 override `_draw()` verb-specific 1:1 严格) + 1 Wave 0 
override `_draw()` verb-specific 1:1 严格 (1 verb 0 override `_draw()` verb-specific 1:1 严格) + 1 显式契约 "5 verb windup VFX 
双 verb-specific (`trigger()` + `_draw()`) 0 override verb-specific 0 触碰既有 1:1 严格 + 5 verb 共享 4 hook 0 override 1:1 严格" 
1 段 1:1 严格 + 1 双 verb-specific 0 触碰既有 1:1 严格 (1 抽象契约 1 元素)), 在反复 polish (T166 #85 + T167 #86 + T168 #86 + T169 #87 + 
T171 #89 + T302 #228 + T303 #229 + T304 #231 + T305 #232 跨 9 任务 ~150 轮落地) 期间出现 3 类典型 fragile: (1) **"加新 1 verb windup 
VFX 漏 1 边 '5 verb 双 verb-specific 0 override 1:1 严格' 共享契约"** — 加新 1 verb windup VFX (e.g. 未来加 6th verb 
"WhisperWindupVFX" 接入 双 verb-specific 0 override 1:1 严格分离) 落地时漏 1 边 "5 verb windup VFX 双 verb-specific (`trigger()` + 
`_draw()`) 0 override verb-specific 0 触碰既有 1:1 严格" 共享契约 (e.g. 漏 `whisper_windup_vfx.gd` 中 0 override `trigger()` + 0 
override `_draw()` 2 verb-specific 0 override 1:1 严格 0 镜像, 1 verb 0 双 verb-specific 0 override 0 闭环 0 漂动); (2) **"加新 1 
verb windup VFX 漏 1 边 '5 verb 共享 4 hook 0 override 0 触碰既有' base 契约"** — 加新 1 verb windup VFX (e.g. 未来加 6th verb 
"WhisperWindupVFX" 接入 双 verb-specific 0 override 1:1 严格分离) 落地时漏 1 边 "5 verb 共享 4 hook 0 override 0 触碰既有 1:1 严格" base 契约 
(e.g. 漏 `_verb_windup_vfx_base.gd:49` `_ready()` / `_verb_windup_vfx_base.gd:55` `_process(delta: float)` / 
`_verb_windup_vfx_base.gd:78` `_activate_windup_tween()` / `_verb_windup_vfx_base.gd:105` `fade_out_and_free()` 4 共享 
hook 0 触碰既有 1:1 严格 0 镜像 1 base, 1 base 0 4 共享 hook 0 override 0 闭环 0 漂动); (3) **"加新 1 verb windup VFX 漏 1 边 '1 显式契约 5 
verb windup VFX 双 verb-specific 0 override 1:1 严格'"** — 加新 1 verb windup VFX (e.g. 未来加 6th verb "WhisperWindupVFX" 接入 双 
verb-specific 0 override 1:1 严格分离) 落地时漏 1 边 "1 显式契约 5 verb windup VFX 双 verb-specific (`trigger()` + `_draw()`) 0 
override verb-specific 0 触碰既有 1:1 严格 + 5 verb 共享 4 hook 0 override 1:1 严格" 显式契约 (e.g. 漏 `_verb_windup_vfx_base.gd` 中 0 
include 1 显式契约 "5 verb windup VFX 双 verb-specific 0 override 1:1 严格 + 5 verb 共享 4 hook 0 override 1:1 严格" 1 段 0 镜像 1 
base, 1 base 0 1 显式契约 0 闭环 0 漂动). 3 类症状根因: 5 verb windup VFX 双 verb-specific 0 override verb-specific 0 触碰既有 1:1 序列 0 
文档化独立聚焦段, 任何新 1 verb windup VFX 0 override 双 verb-specific / 1 base 4 shared hook 0 触碰既有 / 1 显式契约 接入只能"凭经验" 1:1 镜像 17 
元素 (5 verb 0 override 4 hook + 5 verb 0 override `trigger()` + 5 verb 0 override `_draw()` + 1 显式契约 + 1 双 verb-specific 
0 触碰既有 = 17 元素 1:1 严格), 1 漏 1 verb 0 override 双 verb-specific / 1 漏 1 base 4 shared hook 0 触碰既有 / 1 漏 1 显式契约 = 1 verb 
扩展 0 17 元素 0 闭环 0 漂动.

- **触发场景**: 加新 1 verb windup VFX 双 verb-specific 0 override (e.g. 未来加 6th verb "WhisperWindupVFX" 接入 双 verb-specific 0 
override 1:1 严格分离, 实际现状: Whisper 已存在但 0 override 双 verb-specific 1:1 严格, 0 override `trigger()` + 0 override `_draw()` 
镜像 base 0 漏 0 改 0 反向) / 加新 1 共享 base 4 hook / 加新 1 显式契约 (T166 + T167 + T168 + T169 + T171 + T302 + T303 + T304 + T305 
模式参考) 时**必须**严格按 17 元素 1:1 复制 T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 + T304 #231 
+ T305 #232 落地模式: (1) **Stage 1 Pulse 1 verb 0 override 双 verb-specific 1:1 严格** — 在 `pulse_windup_vfx.gd` 中 0 override 
4 共享 hook 0 触碰 base 1:1 严格 (1 verb 0 override `_ready()` 1 hook + 1 verb 0 override `_process()` 1 hook + 1 verb 0 
override `_activate_windup_tween()` 1 hook + 1 verb 0 override `fade_out_and_free()` 1 hook = 4 hook 0 override 1:1 严格 
0 漏 0 改 0 反序 0 反向) + 1 verb 0 override `trigger()` verb-specific 1:1 严格 (1 verb `trigger()` 仅设 verb-specific 状态 + call 
`_activate_windup_tween()` 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 verb 0 override `_draw()` verb-specific 1:1 严格 (1 verb 
`_draw()` 仅设 verb-specific 视觉组 + draw 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "5 verb windup VFX 双 verb-specific 
(`trigger()` + `_draw()`) 0 override verb-specific 0 触碰既有 1:1 严格 + 5 verb 共享 4 hook 0 override 1:1 严格" 1 段 1:1 严格 (1 契约 
1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 双 verb-specific 0 触碰既有 1:1 严格 (1 抽象契约 1:1 严格) = 17 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, 0 触碰既有 5 
verb 任何 1 字符, source-grep 验证 `pulse_windup_vfx.gd` 0 override 4 hook + 0 override `trigger()` + 0 override `_draw()` + 
`bind_windup_vfx.gd` 0 override 4 hook + 0 override `trigger()` + 0 override `_draw()` + `cut_windup_vfx.gd` 0 override 
4 hook + 0 override `trigger()` + 0 override `_draw()` + `echo_windup_vfx.gd` 0 override 4 hook + 0 override 
`trigger()` + 0 override `_draw()` + `wave_windup_vfx.gd` 0 override 4 hook + 0 override `trigger()` + 0 override 
`_draw()` + `_verb_windup_vfx_base.gd` 4 共享 hook 0 触碰既有 + `_verb_windup_vfx_base.gd` `trigger()` 0 override + 
`_verb_windup_vfx_base.gd` `_draw()` 0 override + 1 显式契约 + 1 双 verb-specific 0 触碰既有 → 17 元素 0 漏 0 改 0 漏 1 边 0 反向; (2) 
**Stage 2 Bind 1 verb 0 override 双 verb-specific 1:1 严格** — 17 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, source-grep 验证 17 元素 0 漏 0 
改 0 漏 1 边 0 反向; (3) **Stage 3 Cut 1 verb 0 override 双 verb-specific 1:1 严格** — 17 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (4) 
**Stage 4 Echo 1 verb 0 override 双 verb-specific 1:1 严格** — 17 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (5) **Stage 5 Wave 1 verb 0 
override 双 verb-specific 1:1 严格** — 17 元素 1:1 严格 0 漏 0 改 0 反序 0 反向. 17 元素 1:1 同步, 1 漏 1 verb 0 override 双 verb-specific 
/ 1 漏 1 base 4 共享 hook 0 触碰既有 / 1 漏 1 显式契约 / 1 漏 1 双 verb-specific 0 触碰既有 = 1 verb / 1 base / 1 显式契约 / 1 双 
verb-specific 扩展 0 17 元素 0 闭环 0 漂动. T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 + T304 
#231 + T305 #232 0 触碰既有 0 改 `_verb_windup_vfx_base.gd` 任何 1 字符 (0 改 Pulse 1 verb 任何 1 字符, 0 改 Bind 1 verb 任何 1 字符, 0 改 
Cut 1 verb 任何 1 字符, 0 改 Echo 1 verb 任何 1 字符, 0 改 Wave 1 verb 任何 1 字符, 0 改 Whisper 1 hook override 任何 1 字符).

- **修复** (跨 1 套 polish 模式 T306 #233 落地后已知 17 元素 0 例外):
  - **T306 #233 首个 5 verb windup VFX `trigger()` + `_draw()` 双 verb-specific 0 override verb-specific 0 触碰既有 1:1 严格分离契约 
独立聚焦段 落地 (`pulse_windup_vfx.gd` 0 override 4 共享 hook 1:1 严格 + 0 override `trigger()` verb-specific 1:1 严格 + 0 override 
`_draw()` verb-specific 1:1 严格 + `bind_windup_vfx.gd` 0 override 4 共享 hook 1:1 严格 + 0 override `trigger()` 
verb-specific 1:1 严格 + 0 override `_draw()` verb-specific 1:1 严格 + `cut_windup_vfx.gd` 0 override 4 共享 hook 1:1 严格 + 0 
override `trigger()` verb-specific 1:1 严格 + 0 override `_draw()` verb-specific 1:1 严格 + `echo_windup_vfx.gd` 0 override 
4 共享 hook 1:1 严格 + 0 override `trigger()` verb-specific 1:1 严格 + 0 override `_draw()` verb-specific 1:1 严格 + 
`wave_windup_vfx.gd` 0 override 4 共享 hook 1:1 严格 + 0 override `trigger()` verb-specific 1:1 严格 + 0 override `_draw()` 
verb-specific 1:1 严格 + 1 显式契约 "5 verb windup VFX 双 verb-specific (`trigger()` + `_draw()`) 0 override verb-specific 0 
触碰既有 1:1 严格 + 5 verb 共享 4 hook 0 override 1:1 严格" 1 段 1:1 严格 + 1 双 verb-specific 0 触碰既有 1:1 严格)** — §9.6.50 段 是 1 套 
polish 模式首个 1:1 包含 5 verb 0 override 4 hook 1:1 严格 + 5 verb 0 override `trigger()` verb-specific 1:1 严格 + 5 verb 0 
override `_draw()` verb-specific 1:1 严格 + 1 显式契约 + 1 双 verb-specific 0 触碰既有 = 5 + 5 + 5 + 1 + 1 = 17 元素 1:1 严格 的 5 verb 
windup VFX `trigger()` + `_draw()` 双 verb-specific 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段 polish 模式, 17 元素 1:1 
严格分离 (1 Pulse 0 override 4 共享 hook 1:1 严格 (1 0 override `_ready()` + 1 0 override `_process()` + 1 0 override 
`_activate_windup_tween()` + 1 0 override `fade_out_and_free()` = 4 hook 0 override 1:1 严格) + 1 Pulse 0 override 
`trigger()` verb-specific 1:1 严格 (1 verb 0 override `trigger()` verb-specific 仅设 verb-specific 状态 + call 
`_activate_windup_tween()` 1:1 严格) + 1 Pulse 0 override `_draw()` verb-specific 1:1 严格 (1 verb 0 override `_draw()` 
verb-specific 仅设 verb-specific 视觉组 + draw 1:1 严格) + 1 Bind 0 override 4 共享 hook 1:1 严格 (4 hook 0 override 1:1 严格) + 1 
Bind 0 override `trigger()` verb-specific 1:1 严格 (1 verb 0 override `trigger()` verb-specific 1:1 严格) + 1 Bind 0 
override `_draw()` verb-specific 1:1 严格 (1 verb 0 override `_draw()` verb-specific 1:1 严格) + 1 Cut 0 override 4 共享 hook 
1:1 严格 (4 hook 0 override 1:1 严格) + 1 Cut 0 override `trigger()` verb-specific 1:1 严格 (1 verb 0 override `trigger()` 
verb-specific 1:1 严格) + 1 Cut 0 override `_draw()` verb-specific 1:1 严格 (1 verb 0 override `_draw()` verb-specific 1:1 
严格) + 1 Echo 0 override 4 共享 hook 1:1 严格 (4 hook 0 override 1:1 严格) + 1 Echo 0 override `trigger()` verb-specific 1:1 
严格 (1 verb 0 override `trigger()` verb-specific 1:1 严格) + 1 Echo 0 override `_draw()` verb-specific 1:1 严格 (1 verb 0 
override `_draw()` verb-specific 1:1 严格) + 1 Wave 0 override 4 共享 hook 1:1 严格 (4 hook 0 override 1:1 严格) + 1 Wave 0 
override `trigger()` verb-specific 1:1 严格 (1 verb 0 override `trigger()` verb-specific 1:1 严格) + 1 Wave 0 override 
`_draw()` verb-specific 1:1 严格 (1 verb 0 override `_draw()` verb-specific 1:1 严格) + 1 显式契约 "5 verb windup VFX 双 
verb-specific (`trigger()` + `_draw()`) 0 override verb-specific 0 触碰既有 1:1 严格 + 5 verb 共享 4 hook 0 override 1:1 严格" 1 
段 1:1 严格 + 1 双 verb-specific 0 触碰既有 1:1 严格 (1 抽象契约 1 元素), 0 漏 1 元素 0 改 1 字段 0 漏 1 字段 0 反向), 0 触碰既有 (T306 0 触碰 §9.6.6 / 
§9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / 
§9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / 
§9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 / §9.6.46 / 
§9.6.47 / §9.6.48 / §9.6.49 40 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 
README.md / README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / 
REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).

**与 §9.6.47 关系** — §9.6.47 (T303 #229 落地) 关注 "5 verb windup VFX `_draw()` verb-specific 1:1 严格分离契约" (30 元素 1:1 严格, 5 
verb `_draw()` + 5 verb 共享 3 字段 + 5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 1 base `_draw()` 0 
override + 1 `_draw()` 0 override verb-specific 0 触碰既有 = 30 元素). §9.6.50 关注 "5 verb windup VFX `trigger()` + `_draw()` 
双 verb-specific 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段" (17 元素 1:1 严格, 5 verb 0 override 4 hook + 5 verb 0 
override `trigger()` verb-specific + 5 verb 0 override `_draw()` verb-specific + 1 显式契约 + 1 双 verb-specific 0 触碰既有 = 17 
元素). §9.6.47 与 §9.6.50 0 互混 0 复用 0 共享: §9.6.47 关注 "5 verb `_draw()` verb-specific 1:1 严格分离契约" (与 `trigger()` 0 相关, 30 
元素 1:1 严格 0 涉及 `trigger()` 自身 1:1 严格分离), §9.6.50 关注 "5 verb 双 verb-specific (`trigger()` + `_draw()`) 0 override 
verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段" (与 `_draw()` 1:1 严格分离 0 相关, 17 元素 1:1 严格 0 涉及 `_draw()` 共享 3 字段 0 涉及 5 verb 视觉组 5 
段 0 涉及 视觉组 0 触碰既有 0 涉及 共享 3 字段 0 触碰既有 0 涉及 5 verb `_draw()` verb-specific 1:1 严格自身, 17 元素 1:1 严格 0 涉及 `_draw()` 视觉组 + 
共享 3 字段 子集, 1 套 polish 模式 × 17 元素 = 17 元素 1:1 严格 0 涉及 30 元素 1:1 严格 `_draw()` 视觉组 + 共享 3 字段 子集, 17 元素 1:1 严格 是 30 元素 1:1 
严格 子集 0 涉及 视觉组 5 段 0 涉及 共享 3 字段 0 涉及 视觉组 0 触碰既有 0 涉及 共享 3 字段 0 触碰既有). 任何"加新 1 verb windup VFX 双 verb-specific 0 
override"必须**先**按 §9.6.50 17 元素 1:1 复制 (17 元素 1:1 严格, 0 漏 1 verb 0 改 1 method 0 反向 0 漏 1 字段 0 漏 1 段) **或** 按 §9.6.47 30 
元素 1:1 复制 (30 元素 1:1 严格, 0 漏 1 verb 0 改 1 method 0 反向 0 漏 1 字段 0 漏 1 段), **再**按 §9.6.50 17 元素 1:1 同步 (17 元素 1:1 严格, 0 漏 
1 verb 0 改 1 method 0 反向 0 触碰既有 §9.6.47 任何 1 字符) **或** 按 §9.6.47 30 元素 1:1 同步 (30 元素 1:1 严格, 0 漏 1 verb 0 改 1 method 0 
反向 0 触碰既有 §9.6.50 任何 1 字符).

**与 §9.6.48 关系** — §9.6.48 (T304 #231 落地) 关注 "5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约" (32 元素 1:1 严格, 5 
verb `trigger()` 0 override verb-specific 1:1 严格 + 5 verb 共享 4 元素 1:1 严格 + 2 verb verb-specific 1 元素 1:1 严格 + 1 显式契约 + 
1 verb-specific 0 触碰既有 + 1 共享 4 元素 0 触碰既有 + 1 base `trigger()` 0 override + 1 `trigger()` 0 override verb-specific 0 
触碰既有 = 32 元素), §9.6.50 关注 "5 verb windup VFX `trigger()` + `_draw()` 双 verb-specific 0 override verb-specific 0 触碰既有 
1:1 严格分离契约 聚焦段" (17 元素 1:1 严格). §9.6.48 与 §9.6.50 是 "姊妹段 + 聚焦段" (1 套 polish 模式 × 32 元素 = 32 元素 1:1 严格 0 涉及 17 元素 1:1 严格 
双 verb-specific 0 override 子集, 1 套 polish 模式 × 17 元素 = 17 元素 1:1 严格 0 涉及 32 元素 1:1 严格 `trigger()` verb-specific 子集; 一是 
5 verb `trigger()` verb-specific 1:1 严格分离, 一是 5 verb 双 verb-specific 0 override 1:1 严格分离; 1 套 polish 模式 × 32 元素 = 32 元素 
1:1 严格 ∩ 1 套 polish 模式 × 17 元素 = 17 元素 1:1 严格 = 5 元素 1:1 严格 交集 (5 verb 0 override `trigger()` verb-specific 5 元素 0 漏 0 
改 0 反序 0 反向), 32 元素 1:1 严格 17 元素 1:1 严格 子集 1 套 polish 模式 × 17 元素 = 17 元素 1:1 严格 0 涉及 32 元素 1:1 严格 `trigger()` 共享 4 元素 0 
涉及 32 元素 1:1 严格 2 verb verb-specific 1 元素 子集 0 涉及 32 元素 1:1 严格 verb-specific 0 触碰既有 0 涉及 32 元素 1:1 严格 共享 4 元素 0 触碰既有 0 
涉及 32 元素 1:1 严格 base `trigger()` 0 override 0 涉及 32 元素 1:1 严格 `trigger()` 0 override verb-specific 0 触碰既有 0 涉及 
`_draw()` 自身 1:1 严格分离). 任何"加新 1 verb windup VFX 双 verb-specific 0 override"必须**先**按 §9.6.50 17 元素 1:1 复制 (17 元素 1:1 严格, 
0 漏 1 verb 0 改 1 method 0 反向 0 漏 1 字段 0 漏 1 段), **再**按 §9.6.48 32 元素 1:1 同步 (32 元素 1:1 严格, 0 漏 1 verb 0 改 1 method 0 反向 
0 触碰既有 §9.6.50 任何 1 字符) **或** 按 §9.6.47 30 元素 1:1 同步 (30 元素 1:1 严格, 0 漏 1 verb 0 改 1 method 0 反向 0 触碰既有 §9.6.50 任何 1 
字符).

**与 §9.6.49 关系** — §9.6.49 (T305 #232 落地) 关注 "5 verb windup VFX 共享 4 hook 0 override verb-specific 0 触碰既有 1:1 严格分离契约 
聚焦段" (9 元素 1:1 严格, 5 verb 0 override 4 hook + 1 base 4 hook 0 触碰既有 + 1 Whisper 1 hook override + 1 显式契约 + 1 4 hook 0 
override verb-specific 0 触碰既有 = 9 元素), §9.6.50 关注 "5 verb windup VFX `trigger()` + `_draw()` 双 verb-specific 0 override 
verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段" (17 元素 1:1 严格). §9.6.49 与 §9.6.50 是 "姊妹段 + 聚焦段" (1 套 polish 模式 × 9 元素 = 9 元素 1:1 
严格 0 涉及 17 元素 1:1 严格 双 verb-specific 0 override 子集, 1 套 polish 模式 × 17 元素 = 17 元素 1:1 严格 0 涉及 9 元素 1:1 严格 4 shared hook 
0 override 子集; 一是 5 verb 共享 4 hook 0 override 1:1 严格分离, 一是 5 verb 双 verb-specific 0 override 1:1 严格分离; 1 套 polish 模式 × 
9 元素 = 9 元素 1:1 严格 ∩ 1 套 polish 模式 × 17 元素 = 17 元素 1:1 严格 = 5 元素 1:1 严格 交集 (5 verb 0 override 4 shared hook 5 元素 0 漏 0 
改 0 反序 0 反向), 9 元素 1:1 严格 17 元素 1:1 严格 子集 1 套 polish 模式 × 17 元素 = 17 元素 1:1 严格 0 涉及 9 元素 1:1 严格 base 4 hook 0 触碰既有 0 涉及 
9 元素 1:1 严格 Whisper 1 hook override 0 涉及 9 元素 1:1 严格 4 hook 0 override verb-specific 0 触碰既有 0 涉及 双 verb-specific 自身 1:1 
严格分离). 任何"加新 1 verb windup VFX 双 verb-specific 0 override"必须**先**按 §9.6.50 17 元素 1:1 复制 (17 元素 1:1 严格, 0 漏 1 verb 0 改 1 
method 0 反向 0 漏 1 字段 0 漏 1 段), **再**按 §9.6.49 9 元素 1:1 同步 (9 元素 1:1 严格, 0 漏 1 verb 0 改 1 hook 0 反向 0 触碰既有 §9.6.50 任何 1 
字符).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.50 5 verb 0 override 4 共享 hook + 5 verb 0 override `trigger()` verb-specific + 
5 verb 0 override `_draw()` verb-specific + 1 显式契约 + 1 双 verb-specific 0 触碰既有 = 17 元素 走 §9.1 9 步落地的 1 步 (Stage 7 windup 
VFX 子类 双 verb-specific 0 override, 跨 5 verb 各 3 元素 + 1 显式契约 1 元素 + 1 双 verb-specific 0 触碰既有 1 元素), 0 触碰 8 步 (1 `verb` 
枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 
渲染). 任何"加新 1 verb windup VFX 双 verb-specific 0 override"必须**先**按 §9.6.50 17 元素 1:1 镜像 (17 元素 1:1 严格, 0 漏 1 verb 0 改 1 
method 0 反向 0 漏 1 字段 0 漏 1 段 0 触碰既有 8 步 (`verb` 枚举 + `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + 
`audio` preset 字段 + `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 步"必须**先**在 §9.6.50 17 元素 加 1 边 (1 verb 0 override 4 hook entry 或 1 
verb 0 override `trigger()` entry 或 1 verb 0 override `_draw()` entry 或 1 显式契约 1 边 或 1 双 verb-specific 0 触碰既有 1 边), 0 
触碰既有 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 
字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).


### 9.6.51 5 verb windup VFX base 3 内部状态字段 (`_max_lifetime` + `_lifetime` + `_active`) 0 override verb-specific 0 触碰既有 
1:1 严格分离契约 polish 模式 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T174.B #94 + T275 #194 跨 7 任务 ~150 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 41 套 polish 模式均在 "pause_menu / 6 verb 单层 / 6 verb 跨组件 / Hub 
> / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / audio 数据层 / PlayerActionGate 单 autoload 4 件套 / SaveSystem CRC32 校验 / 
> T162 brittle 修复流程 / 6 verb audio 家族 19 cue / 6 verb cooldown ready jingle 5 段 / 6 verb `_ready()` super 调用顺序 6 verb 
> 1:1 严格分离契约 / 6 verb `_exit_tree()` super 调用顺序 6 verb 1:1 严格分离契约 / 6 verb 双 hook 串联 1:1 严格分离契约 / 6 verb 双 hook 串联 + 
> `_player` non-null assertion 0 触碰既有 1:1 严格分离契约 / 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 
> 视觉组连贯 lifecycle 1:1 严格分离契约 / 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 / 5 verb windup VFX `_draw()` 
> verb-specific 1:1 严格分离契约 / 5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约 / 5 verb windup VFX 共享 4 hook 0 
> override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段 / 5 verb windup VFX `trigger()` + `_draw()` 双 verb-specific 0 override 
> verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段" 范畴, 0 涉及 "5 verb windup VFX base 3 内部状态字段 (`_max_lifetime` + `_lifetime` + 
> `_active`) 0 override verb-specific 0 触碰既有 1:1 严格分离契约" 独立聚焦段落地. §9.6.46 (T302 #228 落地) 关注 "5 verb windup VFX 4 hook 
> lifecycle 0 触碰既有 1:1 严格分离契约" (31 元素 1:1 严格, 5 verb × 6 元素 = 30 元素 + 1 base 4 hook + 1 显式契约 + 1 视觉组 0 触碰既有). §9.6.49 
> (T305 #232 落地) 关注 "5 verb windup VFX 共享 4 hook 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段" (9 元素 1:1 严格). §9.6.50 
> (T306 #233 落地) 关注 "5 verb windup VFX `trigger()` + `_draw()` 双 verb-specific 0 override verb-specific 0 触碰既有 1:1 
> 严格分离契约 聚焦段" (17 元素 1:1 严格). 上述 3 段均聚焦 5 verb windup VFX base 4 共享 hook (`_ready()` + `_process()` + 
> `_activate_windup_tween()` + `fade_out_and_free()`) lifecycle + `trigger()` + `_draw()` 维度的 1:1 严格分离契约, 0 涉及 "5 verb 
> windup VFX base 3 内部状态字段" 1:1 严格分离契约 独立聚焦段 自身. 本节 (T307 #234 落地) 首个落在**5 verb windup VFX base 3 内部状态字段 
> (`_max_lifetime` + `_lifetime` + `_active`) 0 override verb-specific 0 触碰既有 1:1 严格分离契约** 聚焦段 (5 verb 0 override 3 
> 内部状态字段 1:1 严格 (Pulse + Bind + Cut + Echo + Wave 5 verb 各自 0 override `_max_lifetime: float = 0.10` + 0 override 
> `_lifetime: float = 0.0` + 0 override `_active: bool = false` 3 内部状态字段 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 base 3 内部状态字段 0 
> 触碰既有 1:1 严格 (`_verb_windup_vfx_base.gd:45` `_max_lifetime: float = 0.10` + `:46` `_lifetime: float = 0.0` + `:47` 
> `_active: bool = false` 3 字段 0 触碰既有 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 Whisper 0 override 3 内部状态字段 1:1 严格 rename 镜像 (T275 
> #194 落地 `whisper_windup_vfx.gd` `_is_active` → `_active` 1:1 rename + 0 override `_max_lifetime` + 0 override 
> `_lifetime` 0 override `_active` 3 字段 0 触碰 base 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "5 verb windup VFX subclasses 0 
> override base 3 内部状态字段 (`_max_lifetime` + `_lifetime` + `_active`) 1:1 严格 + 6 verb 0 override 1:1 严格 (Whisper after 
> T275 #194 rename 镜像)" 1 段 1:1 严格 + 1 3 内部状态字段 0 override verb-specific 0 触碰既有 1:1 严格) polish 模式 — 跨 5 verb × 3 内部状态字段 
> 0 override + 1 base 3 字段 0 触碰既有 + 1 Whisper 0 override 3 字段 rename 镜像 + 1 显式契约 + 1 3 字段 0 override verb-specific 0 
> 触碰既有 = 5 + 1 + 1 + 1 + 1 = 9 元素 1:1 严格 (注意: 5 verb 0 override 3 内部状态字段 1:1 严格 = 5 verb 各自 1 元素 = 5 元素 + 1 base 3 字段 0 
> 触碰既有 1 元素 + 1 Whisper 0 override 3 字段 rename 镜像 1 元素 + 1 显式契约 1 元素 + 1 3 字段 0 override verb-specific 0 触碰既有 1 元素 = 5 
> + 1 + 1 + 1 + 1 = 9 元素 1:1 严格, 0 漂动 0 后续 主要 pre-existing 风险) 0 触碰既有 6 verb 任何 1 字符 0 反向 0 例外. 任何"加新 1 verb windup VFX 
> 3 内部状态字段 0 override" (e.g. 未来加 7th verb windup VFX 0 接入 base 3 内部状态字段 1:1 严格镜像, 实际现状: 6 verb 全部 0 override base 3 
> 内部状态字段 1:1 严格, 0 override `_max_lifetime` + 0 override `_lifetime` + 0 override `_active` 镜像 base 0 漏 0 改 0 反向) 
> 必须**先**按 Stage 1 Pulse 1 verb 0 override 3 内部状态字段 1:1 镜像 加 1 verb entry (Stage 1 1:1 严格, 0 漏 1 verb 0 改 1 字段 0 反序 0 
> 反向, **0 漏 1 verb**), **再**按 Stage 2 Bind / Stage 3 Cut / Stage 4 Echo / Stage 5 Wave 1 verb 0 override 3 内部状态字段 1:1 
> 镜像 4 verb entries (4 verb 1:1 严格, 0 漏 1 verb 0 改 1 字段 0 反序 0 反向, 5 verb 0 漏 0 改 0 反序 0 例外). 跨 1 套 polish 模式 × 5 verb 
> 0 override 3 内部状态字段 + 1 base 3 字段 0 触碰既有 + 1 Whisper 0 override 3 字段 rename 镜像 + 1 显式契约 + 1 3 字段 0 override 
> verb-specific 0 触碰既有 = 5 + 1 + 1 + 1 + 1 = 9 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: 5 verb windup VFX (`src/scripts/pulse_windup_vfx.gd` + `src/scripts/bind_windup_vfx.gd` + 
`src/scripts/cut_windup_vfx.gd` + `src/scripts/echo_windup_vfx.gd` + `src/scripts/wave_windup_vfx.gd`) + 1 verb Whisper 
windup VFX (`src/scripts/whisper_windup_vfx.gd`) 共享 base 3 内部状态字段 (`_max_lifetime` + `_lifetime` + `_active`) 0 
override verb-specific 0 触碰既有 1:1 严格分离契约 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T174.B #94 + T275 #194 
跨 7 任务 ~150 轮落地) 现有的 9 元素 1:1 严格 (1 Pulse 0 override 3 内部状态字段 1:1 严格 (1 0 override `_max_lifetime: float = 0.10` + 1 0 
override `_lifetime: float = 0.0` + 1 0 override `_active: bool = false` = 3 内部状态字段 0 override 1:1 严格 0 漏 0 改 0 反序 0 
反向) + 1 Bind 0 override 3 内部状态字段 1:1 严格 (3 内部状态字段 0 override 1:1 严格) + 1 Cut 0 override 3 内部状态字段 1:1 严格 (3 内部状态字段 0 
override 1:1 严格) + 1 Echo 0 override 3 内部状态字段 1:1 严格 (3 内部状态字段 0 override 1:1 严格) + 1 Wave 0 override 3 内部状态字段 1:1 严格 
(3 内部状态字段 0 override 1:1 严格) + 1 base 3 内部状态字段 0 触碰既有 1:1 严格 (`_verb_windup_vfx_base.gd:45` `_max_lifetime: float = 
0.10` 0 触碰既有 1:1 严格 + `:46` `_lifetime: float = 0.0` 0 触碰既有 1:1 严格 + `:47` `_active: bool = false` 0 触碰既有 1:1 严格 = 3 字段 
0 触碰既有 1:1 严格) + 1 Whisper 0 override 3 内部状态字段 1:1 严格 rename 镜像 (T275 #194 落地 1 verb Whisper 0 override 3 字段 rename 镜像 
base `_is_active` → `_active` 1:1 rename 0 触碰 base 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "5 verb windup VFX subclasses 0 
override base 3 内部状态字段 (`_max_lifetime` + `_lifetime` + `_active`) 1:1 严格 + 6 verb 0 override 1:1 严格 (Whisper after 
T275 #194 rename 镜像)" 1 段 1:1 严格 + 1 3 内部状态字段 0 override verb-specific 0 触碰既有 1:1 严格 (1 抽象契约 1 元素)), 在反复 polish (T166 
#85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T174.B #94 + T275 #194 跨 7 任务 ~150 轮落地) 期间出现 3 类典型 fragile: (1) **"加新 
1 verb windup VFX 漏 1 边 '5 verb 3 内部状态字段 0 override 1:1 严格' 共享契约"** — 加新 1 verb windup VFX (e.g. 未来加 7th verb windup 
VFX 接入 base 3 内部状态字段 0 override 1:1 严格分离) 落地时漏 1 边 "5 verb windup VFX 3 内部状态字段 (`_max_lifetime` + `_lifetime` + 
`_active`) 0 override verb-specific 0 触碰既有 1:1 严格" 共享契约 (e.g. 漏 7th verb 0 override `_max_lifetime` + 0 override 
`_lifetime` + 0 override `_active` 3 内部状态字段 0 override 1:1 严格 0 镜像 1 verb, 1 verb 0 3 内部状态字段 0 override 0 闭环 0 漂动); (2) 
**"加新 1 verb windup VFX 漏 1 边 '1 base 3 内部状态字段 0 触碰既有 1:1 严格' base 契约"** — 加新 1 verb windup VFX (e.g. 未来加 7th verb 
windup VFX 接入 3 内部状态字段 0 override 1:1 严格分离) 落地时漏 1 边 "1 base 3 内部状态字段 (`_max_lifetime` + `_lifetime` + `_active`) 0 
触碰既有 1:1 严格" base 契约 (e.g. 漏 `_verb_windup_vfx_base.gd:45` `_max_lifetime: float = 0.10` + `:46` `_lifetime: float = 
0.0` + `:47` `_active: bool = false` 3 内部状态字段 0 触碰既有 1:1 严格 0 镜像 1 base, 1 base 0 3 内部状态字段 0 触碰既有 0 闭环 0 漂动); (3) **"加新 
1 verb windup VFX 漏 1 边 '1 显式契约 5 verb 3 内部状态字段 0 override 1:1 严格'"** — 加新 1 verb windup VFX (e.g. 未来加 7th verb windup 
VFX 接入 3 内部状态字段 0 override 1:1 严格分离) 落地时漏 1 边 "1 显式契约 5 verb windup VFX 3 内部状态字段 (`_max_lifetime` + `_lifetime` + 
`_active`) 0 override 1:1 严格 + 6 verb 0 override 1:1 严格 (Whisper after T275 #194 rename 镜像)" 显式契约 (e.g. 漏 
`_verb_windup_vfx_base.gd` 中 0 include 1 显式契约 "5 verb windup VFX 3 内部状态字段 0 override 1:1 严格 + 6 verb 0 override 1:1 严格" 
1 段 0 镜像 1 base, 1 base 0 1 显式契约 0 闭环 0 漂动). 3 类症状根因: 5 verb windup VFX 3 内部状态字段 0 override verb-specific 0 触碰既有 1:1 序列 
0 文档化独立聚焦段, 任何新 1 verb windup VFX 0 override 3 内部状态字段 / 1 base 3 内部状态字段 0 触碰既有 / 1 显式契约 接入只能"凭经验" 1:1 镜像 9 元素 (5 verb 0 
override 3 内部状态字段 + 1 base 3 内部状态字段 0 触碰既有 + 1 Whisper 0 override 3 内部状态字段 rename 镜像 + 1 显式契约 + 1 3 内部状态字段 0 override 
verb-specific 0 触碰既有 = 9 元素 1:1 严格), 1 漏 1 verb 0 override 3 内部状态字段 / 1 漏 1 base 3 内部状态字段 0 触碰既有 / 1 漏 1 显式契约 = 1 verb 
扩展 0 9 元素 0 闭环 0 漂动.

- **触发场景**: 加新 1 verb windup VFX 3 内部状态字段 0 override (e.g. 未来加 7th verb windup VFX 接入 base 3 内部状态字段 0 override 1:1 严格分离,
 实际现状: 6 verb 全部 0 override base 3 内部状态字段 1:1 严格, 0 override `_max_lifetime` + 0 override `_lifetime` + 0 override 
`_active` 镜像 base 0 漏 0 改 0 反向) / 加新 1 base 3 内部状态字段 / 加新 1 显式契约 (T166 + T167 + T168 + T169 + T171 + T174.B + T275 
模式参考) 时**必须**严格按 9 元素 1:1 复制 T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T174.B #94 + T275 #194 落地模式: (1) 
**Stage 1 Pulse 1 verb 0 override 3 内部状态字段 1:1 严格** — 在 `pulse_windup_vfx.gd` 中 0 override 3 内部状态字段 0 触碰 base 1:1 严格 (1 
verb 0 override `_max_lifetime: float = 0.10` 1 字段 + 1 verb 0 override `_lifetime: float = 0.0` 1 字段 + 1 verb 0 
override `_active: bool = false` 1 字段 = 3 内部状态字段 0 override 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 base 3 内部状态字段 0 触碰既有 1:1 严格 
(`_verb_windup_vfx_base.gd:45` `_max_lifetime: float = 0.10` + `:46` `_lifetime: float = 0.0` + `:47` `_active: bool = 
false` 3 内部状态字段 0 触碰既有 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "5 verb windup VFX subclasses 0 override base 3 内部状态字段 
(`_max_lifetime` + `_lifetime` + `_active`) 1:1 严格 + 6 verb 0 override 1:1 严格 (Whisper after T275 #194 rename 镜像)" 1 段 
1:1 严格 (1 契约 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 3 内部状态字段 0 触碰既有 1:1 严格 (1 抽象契约 1 元素) = 9 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, 0 触碰既有 
6 verb 任何 1 字符, source-grep 验证 `pulse_windup_vfx.gd` 0 override 3 内部状态字段 + `bind_windup_vfx.gd` 0 override 3 内部状态字段 + 
`cut_windup_vfx.gd` 0 override 3 内部状态字段 + `echo_windup_vfx.gd` 0 override 3 内部状态字段 + `wave_windup_vfx.gd` 0 override 3 
内部状态字段 + `whisper_windup_vfx.gd` 0 override 3 内部状态字段 (after T275 #194 rename `_is_active` → `_active` 1:1 rename) + 
`_verb_windup_vfx_base.gd` 3 内部状态字段 0 触碰既有 + 1 显式契约 + 1 3 内部状态字段 0 触碰既有 → 9 元素 0 漏 0 改 0 漏 1 边 0 反向; (2) **Stage 2 Bind 
1 verb 0 override 3 内部状态字段 1:1 严格** — 9 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, source-grep 验证 9 元素 0 漏 0 改 0 漏 1 边 0 反向; (3) 
**Stage 3 Cut 1 verb 0 override 3 内部状态字段 1:1 严格** — 9 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (4) **Stage 4 Echo 1 verb 0 override 
3 内部状态字段 1:1 严格** — 9 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (5) **Stage 5 Wave 1 verb 0 override 3 内部状态字段 1:1 严格** — 9 元素 1:1 严格 
0 漏 0 改 0 反序 0 反向. 9 元素 1:1 同步, 1 漏 1 verb 0 override 3 内部状态字段 / 1 漏 1 base 3 内部状态字段 0 触碰既有 / 1 漏 1 显式契约 / 1 漏 1 3 
内部状态字段 0 触碰既有 = 1 verb / 1 base / 1 显式契约 / 1 3 内部状态字段 扩展 0 9 元素 0 闭环 0 漂动. T166 #85 + T167 #86 + T168 #86 + T169 #87 + 
T171 #89 + T174.B #94 + T275 #194 0 触碰既有 0 改 `_verb_windup_vfx_base.gd` 任何 1 字符 (0 改 `_max_lifetime: float = 0.10` 任何 1 
字符, 0 改 `_lifetime: float = 0.0` 任何 1 字符, 0 改 `_active: bool = false` 任何 1 字符, 0 改 Pulse 1 verb 任何 1 字符, 0 改 Bind 1 
verb 任何 1 字符, 0 改 Cut 1 verb 任何 1 字符, 0 改 Echo 1 verb 任何 1 字符, 0 改 Wave 1 verb 任何 1 字符, 0 改 Whisper 1 verb rename 
`_is_active` → `_active` 任何 1 字符).

- **修复** (跨 1 套 polish 模式 T307 #234 落地后已知 9 元素 0 例外):
  - **T307 #234 首个 5 verb windup VFX base 3 内部状态字段 (`_max_lifetime` + `_lifetime` + `_active`) 0 override verb-specific 
0 触碰既有 1:1 严格分离契约 独立聚焦段 落地 (`pulse_windup_vfx.gd` 0 override 3 内部状态字段 1:1 严格 + `bind_windup_vfx.gd` 0 override 3 内部状态字段 
1:1 严格 + `cut_windup_vfx.gd` 0 override 3 内部状态字段 1:1 严格 + `echo_windup_vfx.gd` 0 override 3 内部状态字段 1:1 严格 + 
`wave_windup_vfx.gd` 0 override 3 内部状态字段 1:1 严格 + 1 base 3 内部状态字段 0 触碰既有 1:1 严格 + 1 Whisper 0 override 3 内部状态字段 1:1 严格 
rename 镜像 (T275 #194 落地 `_is_active` → `_active` 1:1 rename) + 1 显式契约 "5 verb windup VFX subclasses 0 override base 3 
内部状态字段 (`_max_lifetime` + `_lifetime` + `_active`) 1:1 严格 + 6 verb 0 override 1:1 严格 (Whisper after T275 #194 rename 
镜像)" 1 段 1:1 严格 + 1 3 内部状态字段 0 override verb-specific 0 触碰既有 1:1 严格)** — §9.6.51 段 是 1 套 polish 模式首个 1:1 包含 5 verb 0 
override 3 内部状态字段 1:1 严格 + 1 base 3 内部状态字段 0 触碰既有 + 1 Whisper 0 override 3 内部状态字段 1:1 严格 rename 镜像 + 1 显式契约 + 1 3 
内部状态字段 0 override verb-specific 0 触碰既有 = 5 + 1 + 1 + 1 + 1 = 9 元素 1:1 严格 的 5 verb windup VFX base 3 内部状态字段 
(`_max_lifetime` + `_lifetime` + `_active`) 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段 polish 模式, 9 元素 1:1 严格分离 (1 
Pulse 0 override 3 内部状态字段 1:1 严格 (1 0 override `_max_lifetime: float = 0.10` + 1 0 override `_lifetime: float = 0.0` + 
1 0 override `_active: bool = false` = 3 内部状态字段 0 override 1:1 严格) + 1 Bind 0 override 3 内部状态字段 1:1 严格 (3 内部状态字段 0 
override 1:1 严格) + 1 Cut 0 override 3 内部状态字段 1:1 严格 (3 内部状态字段 0 override 1:1 严格) + 1 Echo 0 override 3 内部状态字段 1:1 严格 (3 
内部状态字段 0 override 1:1 严格) + 1 Wave 0 override 3 内部状态字段 1:1 严格 (3 内部状态字段 0 override 1:1 严格) + 1 base 3 内部状态字段 0 触碰既有 1:1 
严格 (1 `_max_lifetime: float = 0.10` 0 触碰既有 1:1 严格 + 1 `_lifetime: float = 0.0` 0 触碰既有 1:1 严格 + 1 `_active: bool = 
false` 0 触碰既有 1:1 严格 = 3 内部状态字段 0 触碰既有 1:1 严格) + 1 Whisper 0 override 3 内部状态字段 1:1 严格 rename 镜像 (1 0 override 
`_max_lifetime` 1 字段 + 1 0 override `_lifetime` 1 字段 + 1 0 override `_active` 1 字段 after T275 #194 rename `_is_active` 
→ `_active` 1:1 rename = 3 内部状态字段 0 override 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "5 verb windup VFX subclasses 0 
override base 3 内部状态字段 (`_max_lifetime` + `_lifetime` + `_active`) 1:1 严格 + 6 verb 0 override 1:1 严格 (Whisper after 
T275 #194 rename 镜像)" 1 段 1:1 严格 + 1 3 内部状态字段 0 override verb-specific 0 触碰既有 1:1 严格 (1 抽象契约 1 元素), 0 漏 1 元素 0 改 1 字段 0 
漏 1 字段 0 反向), 0 触碰既有 (T307 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / 
§9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / 
§9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / 
§9.6.43 / §9.6.44 / §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 41 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / 
.tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md 
/ ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).

**与 §9.6.46 关系** — §9.6.46 (T302 #228 落地) 关注 "5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约" (31 元素 1:1 严格, 5 
verb × 6 元素 = 30 元素 + 1 base 4 hook + 1 显式契约 + 1 视觉组 0 触碰既有 = 31 元素). §9.6.51 关注 "5 verb windup VFX base 3 内部状态字段 
(`_max_lifetime` + `_lifetime` + `_active`) 0 override verb-specific 0 触碰既有 1:1 严格分离契约" (9 元素 1:1 严格, 5 verb 0 override 
3 内部状态字段 + 1 base 3 内部状态字段 0 触碰既有 + 1 Whisper 0 override 3 内部状态字段 1:1 严格 rename 镜像 + 1 显式契约 + 1 3 内部状态字段 0 override 
verb-specific 0 触碰既有 = 9 元素). §9.6.46 与 §9.6.51 0 互混 0 复用 0 共享: §9.6.46 关注 "5 verb 4 hook lifecycle 0 触碰既有 1:1 严格分离契约" 
(与 3 内部状态字段 0 相关, 31 元素 1:1 严格 0 涉及 3 内部状态字段 0 override verb-specific 1:1 严格分离), §9.6.51 关注 "5 verb base 3 内部状态字段 0 
override verb-specific 0 触碰既有 1:1 严格分离契约" (与 4 hook lifecycle 0 相关, 9 元素 1:1 严格 0 涉及 4 hook lifecycle 0 触碰既有 1:1 严格分离 
自身). 任何"加新 1 verb windup VFX 3 内部状态字段 0 override"必须**先**按 §9.6.51 9 元素 1:1 复制 (9 元素 1:1 严格, 0 漏 1 verb 0 改 1 字段 0 反向 0 
漏 1 段), **再**按 §9.6.46 31 元素 1:1 同步 (31 元素 1:1 严格, 0 漏 1 verb 0 改 1 method 0 反向 0 触碰既有 §9.6.51 任何 1 字符) **或** 按 §9.6.47 
30 元素 1:1 同步 (30 元素 1:1 严格, 0 漏 1 verb 0 改 1 method 0 反向 0 触碰既有 §9.6.51 任何 1 字符) **或** 按 §9.6.48 32 元素 1:1 同步 (32 元素 
1:1 严格, 0 漏 1 verb 0 改 1 method 0 反向 0 触碰既有 §9.6.51 任何 1 字符) **或** 按 §9.6.49 9 元素 1:1 同步 (9 元素 1:1 严格, 0 漏 1 verb 0 改 1 
hook 0 反向 0 触碰既有 §9.6.51 任何 1 字符) **或** 按 §9.6.50 17 元素 1:1 同步 (17 元素 1:1 严格, 0 漏 1 verb 0 改 1 method 0 反向 0 触碰既有 
§9.6.51 任何 1 字符).

**与 §9.6.19 关系** — §9.6.19 (T273 #192 落地) 关注 "6 verb windup VFX 7 件套 base 1:1 严格分离契约" (7 件套 1:1 严格, 7 共享 hook / 字段 / 方法 
跨 6 verb 0 override 1:1 严格, 0 涉及 base 3 内部状态字段 0 override verb-specific 1:1 严格分离 自身, 1 套 × 7 件套 = 7 件套 1:1 严格 ∩ 1 套 × 9 
元素 = 9 元素 1:1 严格 = 3 元素 1:1 严格 交集 (3 内部状态字段 (`_max_lifetime` + `_lifetime` + `_active`) 跨 6 verb 0 override 1:1 严格 0 漏 
0 改 0 反序 0 反向), 7 件套 1:1 严格 9 元素 1:1 严格 子集). 任何"加新 1 verb windup VFX 3 内部状态字段 0 override"必须**先**按 §9.6.51 9 元素 1:1 复制 
(9 元素 1:1 严格, 0 漏 1 verb 0 改 1 字段 0 反向 0 漏 1 段), **再**按 §9.6.19 7 件套 1:1 同步 (7 件套 1:1 严格, 0 漏 1 verb 0 改 1 件套 0 反向 0 
触碰既有 §9.6.51 任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 brittle 修复流程 5 步骤 1:1 严格分离契约 (T295 #219 §9.6.39 文档化) 跨 1 套 5 步骤 1:1 严格 0 涉及 5 verb 
windup VFX base 3 内部状态字段 0 override verb-specific 0 触碰既有 1:1 严格分离契约 自身, 1 套 5 步骤 = 5 步骤 1:1 严格 ∩ 1 套 9 元素 = 9 元素 1:1 严格 
= 0 元素 1:1 严格 交集 (T162 5 步骤关注 "复查根因 / 加新测试 / 修复 / 验证 / 跟踪" 5 步骤, 0 涉及 3 内部状态字段 0 override verb-specific 0 触碰既有 1:1 严格分离 
自身, 5 步骤 1:1 严格 0 涉及 9 元素 1:1 严格). 任何"加新 1 verb windup VFX 3 内部状态字段 0 override" 必须**先**按 §9.6.51 9 元素 1:1 复制 (9 元素 1:1 
严格, 0 漏 1 verb 0 改 1 字段 0 反向 0 漏 1 段), **再**走 T162 5 步骤 1:1 严格 (5 步骤 1:1 严格, 0 漏 1 步 0 改 1 步 0 反序 0 反向 0 触碰既有 §9.6.51 
任何 1 字符).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.51 5 verb 0 override 3 内部状态字段 + 1 base 3 内部状态字段 0 触碰既有 + 1 Whisper 0 override 
3 内部状态字段 1:1 严格 rename 镜像 + 1 显式契约 + 1 3 内部状态字段 0 override verb-specific 0 触碰既有 = 9 元素 走 §9.1 9 步落地的 1 步 (Stage 7 
windup VFX base 3 内部状态字段 0 override verb-specific 0 触碰既有, 跨 5 verb 各 1 元素 + 1 base 3 内部状态字段 0 触碰既有 1 元素 + 1 Whisper 0 
override 3 内部状态字段 1:1 严格 rename 镜像 1 元素 + 1 显式契约 1 元素 + 1 3 内部状态字段 0 override verb-specific 0 触碰既有 1 元素), 0 触碰 8 步 (1 
`verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 
冷光勾边` 渲染). 任何"加新 1 verb windup VFX 3 内部状态字段 0 override"必须**先**按 §9.6.51 9 元素 1:1 镜像 (9 元素 1:1 严格, 0 漏 1 verb 0 改 1 字段 0 
反向 0 漏 1 段 0 触碰既有 8 步 (`verb` 枚举 + `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `audio` preset 字段 
+ `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 步"必须**先**在 §9.6.51 9 元素 加 1 边 (1 verb 0 override 3 内部状态字段 entry 或 1 base 3 内部状态字段 0 
触碰既有 entry 或 1 Whisper 0 override 3 内部状态字段 1:1 严格 rename 镜像 entry 或 1 显式契约 1 边 或 1 3 内部状态字段 0 override verb-specific 0 
触碰既有 1 边), 0 触碰既有 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 
`audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).
