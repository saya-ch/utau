# Polish Patterns 9.6.46–47

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.46–9.6.47，共 2 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.46-47.md

<!-- shard 46-47 共 2 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.46 5 verb windup VFX `_ready()` + `_process()` + `_activate_windup_tween()` + `fade_out_and_free()` 4 hook 
lifecycle 0 触碰既有 1:1 严格分离契约 polish 模式 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 跨 5 任务 ~140 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 36 套 polish 模式均在 "pause_menu / 6 verb 单层 / 6 verb 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / 
> audio 数据层 / PlayerActionGate 单 autoload 4 件套 / SaveSystem CRC32 校验 / T162 brittle 修复流程 / 6 verb audio 家族 19 cue / 6 
> verb cooldown ready jingle 5 段 / 6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约 / 6 verb `_exit_tree()` super 调用顺序 6 
> verb 1:1 严格分离契约 / 6 verb 双 hook 串联 1:1 严格分离契约 / 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 1:1 严格分离契约 / 6 
> verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约" 范畴, 0 涉及 "5 verb windup VFX 
> 4 hook lifecycle 0 触碰既有 1:1 严格分离契约" 落地. §9.6.19 (T273 #192 落地) 关注 "7 件套 windup VFX base (1 base + 5 verb subclasses + 
> 1 显式契约 字节码 1:1 严格)" (与 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 0 相关, 7 件套仅 字节码 1:1 严格 0 涉及 4 hook 自身 1:1 严格分离). §9.6.33 
> (T289 #218 落地) 关注 "6 verb 接入 4 件套 字节码一致性" (与 4 hook lifecycle 0 触碰既有 0 相关, 4 件套 字节码 0 涉及 4 hook 自身 1:1 严格分离). 本节 
> (T302 #228 落地) 首个落在**5 verb windup VFX `_ready()` + `_process()` + `_activate_windup_tween()` + `fade_out_and_free()` 
> 4 hook lifecycle 0 触碰既有 1:1 严格分离契约** (`src/scripts/_verb_windup_vfx_base.gd` `_ready()` 1 共享方法 + `_process()` 1 共享方法 
> + `_activate_windup_tween()` 1 共享方法 + `fade_out_and_free()` 1 共享方法 + 显式契约 1 段 "subclasses MUST call 
> _activate_windup_tween() in their trigger()" + 5 verb 子类 0 override 4 hook (Pulse / Bind / Cut / Echo / Wave 5 verb 0 
> 触碰 base 1:1 严格继承, 5 verb 0 override `_ready()` + 0 override `_process()` + 0 override `_activate_windup_tween()` + 0 
> override `fade_out_and_free()` 字节码 1:1 严格一致) + 5 verb trigger() 0 override verb-specific 1:1 严格 (Pulse / Bind / Cut / 
> Echo / Wave 各自 trigger() 调用 `_activate_windup_tween()` 0 触碰 base 1:1 严格, 5 verb 0 override 
> `_activate_windup_tween()`) + 5 verb `_draw()` 0 override verb-specific 1:1 严格 (Pulse 同心圆环 + Coral Pulse 核 / Bind 
> 向内螺旋 + Muted Violet 核 / Cut 4 三角碎片 + Amber Voice 核 / Echo 8 棱镜折射 + Glass Cyan 核 / Wave 3 同心圆环 + Pale Resonance 核 0 触碰 
> base 1:1 严格) + 1 视觉组 0 触碰既有 1:1 严格 (5 verb 视觉组 5 段 0 漏 0 改 0 反序 0 反向)) polish 模式 — 跨 5 verb × 4 hook 0 override = 20 
> hook + 1 base 4 hook + 1 显式契约 + 5 verb 视觉组 5 段 + 1 视觉组 0 触碰既有 = 31 元素 1:1 严格 (Stage 1 Pulse 1 verb 4 hook + 1 视觉组 1:1 
> 严格 + Stage 2 Bind 1 verb 4 hook + 1 视觉组 1:1 严格 + Stage 3 Cut 1 verb 4 hook + 1 视觉组 1:1 严格 + Stage 4 Echo 1 verb 4 
> hook + 1 视觉组 1:1 严格 + Stage 5 Wave 1 verb 4 hook + 1 视觉组 1:1 严格 + 1 base 4 hook + 1 显式契约 + 1 视觉组 0 触碰既有) — 跨 1 套 
> polish 模式 (T302 #228 落地) 跨 5 任务 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 ~140 轮落地, 0 漂动 0 后续 主要 
> pre-existing 风险). 任何"加新 1 verb windup VFX" (e.g. 未来加 6th verb windup VFX "WhisperWindupVFX" 接入 4 hook lifecycle 0 
> 触碰既有 1:1 严格分离) 必须**先**按 Stage 1 Pulse 1 verb 4 hook + 1 视觉组 1:1 镜像 加 5 元素 (Stage 1 1:1 严格, 0 漏 5 元素 0 改 1 字符 0 反序 0 
> 反向, **0 漏 5 元素**), **再**按 Stage 2 Bind / Stage 3 Cut / Stage 4 Echo / Stage 5 Wave 1 verb 4 hook + 1 视觉组 1:1 镜像 4 段 
> (4 段 1:1 严格, 0 漏 5 元素 0 改 1 字符 0 反序 0 反向). 跨 1 套 polish 模式 × 5 verb × 4 hook = 20 hook + 1 base 4 hook = 24 hook 1:1 
> 严格 + 1 显式契约 + 5 verb 视觉组 5 段 + 1 视觉组 0 触碰既有 = 31 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: 5 verb windup VFX (`src/scripts/pulse_windup_vfx.gd` + `src/scripts/bind_windup_vfx.gd` + 
`src/scripts/cut_windup_vfx.gd` + `src/scripts/echo_windup_vfx.gd` + `src/scripts/wave_windup_vfx.gd`) 4 hook 
(`_ready()` + `_process()` + `_activate_windup_tween()` + `fade_out_and_free()`) lifecycle 0 触碰既有 1:1 严格分离契约 (T166 #85 
+ T167 #86 + T168 #86 + T169 #87 + T171 #89 跨 5 任务 ~140 轮落地) 现有的 31 元素 1:1 严格 (1 Pulse `_ready()` 0 override 0 触碰 base 
1:1 严格继承 + 1 Pulse `_process()` 0 override 0 触碰 base 1:1 严格继承 + 1 Pulse `_activate_windup_tween()` 0 override 0 触碰 base 
1:1 严格继承 + 1 Pulse `fade_out_and_free()` 0 override 0 触碰 base 1:1 严格继承 + 1 Pulse trigger() 0 override verb-specific 1:1 
严格 + 1 Pulse `_draw()` 0 override verb-specific 1:1 严格 + 1 Bind `_ready()` 0 override 0 触碰 base 1:1 严格继承 + 1 Bind 
`_process()` 0 override 0 触碰 base 1:1 严格继承 + 1 Bind `_activate_windup_tween()` 0 override 0 触碰 base 1:1 严格继承 + 1 Bind 
`fade_out_and_free()` 0 override 0 触碰 base 1:1 严格继承 + 1 Bind trigger() 0 override verb-specific 1:1 严格 + 1 Bind 
`_draw()` 0 override verb-specific 1:1 严格 + 1 Cut `_ready()` 0 override 0 触碰 base 1:1 严格继承 + 1 Cut `_process()` 0 
override 0 触碰 base 1:1 严格继承 + 1 Cut `_activate_windup_tween()` 0 override 0 触碰 base 1:1 严格继承 + 1 Cut 
`fade_out_and_free()` 0 override 0 触碰 base 1:1 严格继承 + 1 Cut trigger() 0 override verb-specific 1:1 严格 + 1 Cut `_draw()` 
0 override verb-specific 1:1 严格 + 1 Echo `_ready()` 0 override 0 触碰 base 1:1 严格继承 + 1 Echo `_process()` 0 override 0 触碰 
base 1:1 严格继承 + 1 Echo `_activate_windup_tween()` 0 override 0 触碰 base 1:1 严格继承 + 1 Echo `fade_out_and_free()` 0 
override 0 触碰 base 1:1 严格继承 + 1 Echo trigger() 0 override verb-specific 1:1 严格 + 1 Echo `_draw()` 0 override 
verb-specific 1:1 严格 + 1 Wave `_ready()` 0 override 0 触碰 base 1:1 严格继承 + 1 Wave `_process()` 0 override 0 触碰 base 1:1 
严格继承 + 1 Wave `_activate_windup_tween()` 0 override 0 触碰 base 1:1 严格继承 + 1 Wave `fade_out_and_free()` 0 override 0 触碰 
base 1:1 严格继承 + 1 Wave trigger() 0 override verb-specific 1:1 严格 + 1 Wave `_draw()` 0 override verb-specific 1:1 严格 + 1 
base `_ready()` 1 共享方法 (z_index = 10) + 1 base `_process()` 1 共享方法 (lifetime tracker + auto-free) + 1 base 
`_activate_windup_tween()` 1 共享方法 (ramp-in tween) + 1 base `fade_out_and_free()` 1 共享方法 (0.05s fade-out tween + 
queue_free) + 1 显式契约 "subclasses MUST call _activate_windup_tween() in their trigger()" 1 段 + 5 verb 视觉组 5 段 1:1 严格 
(Pulse 同心圆环 + Coral Pulse 核 / Bind 向内螺旋 + Muted Violet 核 / Cut 4 三角碎片 + Amber Voice 核 / Echo 8 棱镜折射 + Glass Cyan 核 / 
Wave 3 同心圆环 + Pale Resonance 核) + 1 视觉组 0 触碰既有 1:1 严格 (5 verb 视觉组 5 段 0 漏 0 改 0 反序 0 反向)), 在反复 polish (T166 #85 → T171 
#89 跨 5 任务 ~140 轮落地) 期间出现 3 类典型 fragile: (1) **"加新 1 verb windup VFX 漏 5 verb × 4 hook = 20 hook 1:1 严格"** — 加新 1 verb 
windup VFX (e.g. 假设未来加 6th verb "WhisperWindupVFX" 接入 4 hook lifecycle 0 触碰既有 1:1 严格分离) 落地时漏 5 verb × 4 hook = 20 hook 
1:1 严格 (e.g. 漏 Stage 1 Pulse 1 verb 中 4 hook (1 `_ready()` 0 override + 1 `_process()` 0 override + 1 
`_activate_windup_tween()` 0 override + 1 `fade_out_and_free()` 0 override) → ... → Stage 5 Wave 1 verb 中 4 hook, 0 镜像 
1 verb 20 hook), 1 verb 扩展 0 20 hook 0 闭环 0 漂动; (2) **"加新 1 verb windup VFX 漏 1 边 'subclasses MUST call 
_activate_windup_tween()' 显式契约"** — 加新 1 verb windup VFX (e.g. 6th verb "WhisperWindupVFX" 接入 4 hook lifecycle) 落地时漏 1 
边 "subclasses MUST call _activate_windup_tween()" 显式契约 (e.g. 漏 `_verb_windup_vfx_base.gd` 中 "subclasses MUST call 
_activate_windup_tween() in their trigger()" 1 段 0 镜像 1 verb, 1 verb 0 显式契约 0 闭环 0 漂动); (3) **"加新 1 verb windup VFX 漏 1 
边 trigger() 调用 `_activate_windup_tween()` 0 触碰 base"** — 加新 1 verb windup VFX (e.g. 6th verb "WhisperWindupVFX" 接入 4 
hook lifecycle) 落地时漏 1 边 trigger() 调用 `_activate_windup_tween()` 0 触碰 base (e.g. 漏 `whisper_windup_vfx.gd` 中 trigger() 
0 调用 `_activate_windup_tween()` 0 镜像 1 verb, 1 verb 0 trigger() 0 闭环 0 漂动). 3 类症状根因: 5 verb windup VFX 4 hook lifecycle 
1:1 序列 0 文档化, 任何新 1 verb windup VFX / 1 trigger() / 1 显式契约 接入只能"凭经验" 1:1 镜像 31 元素 (5 verb × 4 hook = 20 hook 0 override 
0 触碰 base 1:1 严格继承 + 5 verb trigger() 0 override verb-specific 1:1 严格 + 5 verb `_draw()` 0 override verb-specific 1:1 
严格 + 1 base 4 hook = 4 hook 1:1 严格 + 1 显式契约 + 5 verb 视觉组 5 段 + 1 视觉组 0 触碰既有 = 31 元素 1:1 严格), 1 漏 1 hook / 1 漏 1 
trigger() / 1 漏 1 显式契约 = 1 verb 扩展 0 31 元素 0 闭环 0 漂动.

- **触发场景**: 加新 1 verb windup VFX (e.g. 未来加 6th verb "WhisperWindupVFX" 接入 4 hook lifecycle 0 触碰既有 1:1 严格分离) / 加新 1 
trigger() / 加新 1 显式契约 (T166 + T167 + T168 + T169 + T171 模式参考) 时**必须**严格按 31 元素 1:1 复制 T166 #85 + T167 #86 + T168 #86 + 
T169 #87 + T171 #89 落地模式: (1) **Stage 1 Pulse 1 verb 4 hook + 1 视觉组 1:1 严格** — 在 `_verb_windup_vfx_base.gd` 中 显式契约 1 段 
0 触碰既有 1:1 严格 + 5 verb 各自 4 hook 0 override 0 触碰 base 1:1 严格继承 (5 verb × 4 hook = 20 hook 1:1 严格 0 漏 0 改 0 反序 0 反向) + 5 
verb trigger() 0 override verb-specific 1:1 严格 (5 verb trigger() 0 触碰 base 1:1 严格, 5 verb 各自 trigger() 调用 
`_activate_windup_tween()` 0 触碰 base 1:1 严格) + 5 verb `_draw()` 0 override verb-specific 1:1 严格 (5 verb 0 触碰 base 1:1 
严格) + 1 base 4 hook = 4 hook 1:1 严格 (1 base `_ready()` 0 触碰既有 1:1 严格 + 1 base `_process()` 0 触碰既有 1:1 严格 + 1 base 
`_activate_windup_tween()` 0 触碰既有 1:1 严格 + 1 base `fade_out_and_free()` 0 触碰既有 1:1 严格) + 1 显式契约 "subclasses MUST call 
_activate_windup_tween() in their trigger()" 1:1 严格 (1 契约 1:1 严格 0 漏 0 改 0 反序 0 反向) + 5 verb 视觉组 5 段 1:1 严格 (5 verb × 1 
视觉组 = 5 视觉组 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 视觉组 0 触碰既有 1:1 严格 (1 视觉组 0 触碰既有 1:1 严格 0 漏 0 改 0 反序 0 反向) = 31 元素 1:1 严格 0 漏 
0 改 0 反序 0 反向, 0 触碰既有 5 verb 任何 1 字符, source-grep 验证 4 hook + trigger() + 显式契约 + 视觉组连贯 5 verb → 31 元素 0 漏 0 改 0 漏 1 边 0 
反向; (2) **Stage 2 Bind 1 verb 4 hook + 1 视觉组 1:1 严格** — 31 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, source-grep 验证 31 元素 0 漏 0 改 0 
漏 1 边 0 反向; (3) **Stage 3 Cut 1 verb 4 hook + 1 视觉组 1:1 严格** — 31 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (4) **Stage 4 Echo 1 
verb 4 hook + 1 视觉组 1:1 严格** — 31 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (5) **Stage 5 Wave 1 verb 4 hook + 1 视觉组 1:1 严格** — 31 
元素 1:1 严格 0 漏 0 改 0 反序 0 反向. 31 元素 1:1 同步, 1 漏 1 hook / 1 漏 1 trigger() / 1 漏 1 显式契约 / 1 漏 1 视觉组 = 1 verb / 1 trigger() 
/ 1 显式契约 / 1 视觉组 扩展 0 31 元素 0 闭环 0 漂动. T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 0 触碰既有 0 改 
`_verb_windup_vfx_base.gd` 任何 1 字符 (0 改 Pulse 1 verb 任何 1 字符, 0 改 Bind 1 verb 任何 1 字符, 0 改 Cut 1 verb 任何 1 字符, 0 改 Echo 
1 verb 任何 1 字符, 0 改 Wave 1 verb 任何 1 字符).

- **修复** (跨 1 套 polish 模式 T302 #228 落地后已知 31 元素 0 例外):
  - **T302 #228 首个 5 verb windup VFX `_ready()` + `_process()` + `_activate_windup_tween()` + `fade_out_and_free()` 4 
hook lifecycle 0 触碰既有 1:1 严格分离契约 落地 (`_verb_windup_vfx_base.gd` `_ready()` 1 共享方法 + `_process()` 1 共享方法 + 
`_activate_windup_tween()` 1 共享方法 + `fade_out_and_free()` 1 共享方法 + 显式契约 1 段 "subclasses MUST call 
_activate_windup_tween() in their trigger()" + 5 verb 各自 4 hook 0 override 0 触碰 base 1:1 严格继承 + 5 verb trigger() 0 
override verb-specific 1:1 严格 + 5 verb `_draw()` 0 override verb-specific 1:1 严格 + 5 verb 视觉组 5 段 1:1 严格 + 1 视觉组 0 触碰既有 
1:1 严格)** — §9.6.46 段 是 1 套 polish 模式首个 1:1 包含 5 verb × 4 hook = 20 hook + 5 verb trigger() + 5 verb `_draw()` + 1 base 
4 hook + 1 显式契约 + 5 verb 视觉组 5 段 + 1 视觉组 0 触碰既有 = 31 元素 1:1 严格 的 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 
polish 模式, 31 元素 1:1 严格分离 (1 Pulse 4 hook 0 override 0 触碰 base 1:1 严格 + 1 Pulse trigger() 0 override 1:1 严格 + 1 Pulse 
`_draw()` 0 override 1:1 严格 + 1 Pulse 视觉组 1 段 1:1 严格 + 1 Bind 4 hook 0 override 0 触碰 base 1:1 严格 + 1 Bind trigger() 0 
override 1:1 严格 + 1 Bind `_draw()` 0 override 1:1 严格 + 1 Bind 视觉组 1 段 1:1 严格 + 1 Cut 4 hook 0 override 0 触碰 base 1:1 严格 
+ 1 Cut trigger() 0 override 1:1 严格 + 1 Cut `_draw()` 0 override 1:1 严格 + 1 Cut 视觉组 1 段 1:1 严格 + 1 Echo 4 hook 0 
override 0 触碰 base 1:1 严格 + 1 Echo trigger() 0 override 1:1 严格 + 1 Echo `_draw()` 0 override 1:1 严格 + 1 Echo 视觉组 1 段 
1:1 严格 + 1 Wave 4 hook 0 override 0 触碰 base 1:1 严格 + 1 Wave trigger() 0 override 1:1 严格 + 1 Wave `_draw()` 0 override 
1:1 严格 + 1 Wave 视觉组 1 段 1:1 严格 + 1 base `_ready()` 1 共享方法 0 触碰既有 1:1 严格 + 1 base `_process()` 1 共享方法 0 触碰既有 1:1 严格 + 1 
base `_activate_windup_tween()` 1 共享方法 0 触碰既有 1:1 严格 + 1 base `fade_out_and_free()` 1 共享方法 0 触碰既有 1:1 严格 + 1 显式契约 
"subclasses MUST call _activate_windup_tween() in their trigger()" 1 段 1:1 严格 + 1 视觉组 0 触碰既有 1:1 严格 (5 verb 视觉组 5 段 0 漏 
0 改 0 反序 0 反向), 0 漏 1 元素 0 改 1 字段 0 漏 1 字段 0 反向), 0 触碰既有 (T302 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / 
§9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / 
§9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / 
§9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 36 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / .tscn / 任何 
gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md / 
ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).
  - 跨 1 套 polish 模式 T302 #228 落地后 0 漂动, §9.6.46 段 1:1 严格 (31 元素 1:1 严格分离 + Pulse 1 verb 6 元素 1:1 严格 + Bind 1 verb 6 元素 
1:1 严格 + Cut 1 verb 6 元素 1:1 严格 + Echo 1 verb 6 元素 1:1 严格 + Wave 1 verb 6 元素 1:1 严格 + base 4 hook 4 元素 1:1 严格 + 显式契约 1 
元素 1:1 严格 + 视觉组 0 触碰既有 1 元素 1:1 严格) 100% 闭环, 31 元素 1:1 严格分离. 1 套 polish 模式 0 后续 主要 pre-existing 风险.

- **预防**:
  1. 任何 polish 期给 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 加新 1 verb windup VFX (e.g. 未来加 6th verb 
"WhisperWindupVFX" 接入 4 hook lifecycle 0 触碰既有 系统) 或加新 1 trigger() 或加新 1 显式契约 任务**必须**严格按 31 元素 1:1 复制, 0 反序 0 漏 0 改 1 
字段. 5 verb 0 触碰边界: **Stage 1 0 改 Pulse 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 严格分离契约** (T166 #85 落地后 
`pulse_windup_vfx.gd` 0 override `_ready()` 0 触碰 base 1:1 严格继承 + 0 override `_process()` 0 触碰 base 1:1 严格继承 + 0 
override `_activate_windup_tween()` 0 触碰 base 1:1 严格继承 + 0 override `fade_out_and_free()` 0 触碰 base 1:1 严格继承 + 0 
override trigger() verb-specific 1:1 严格 + 0 override `_draw()` verb-specific 1:1 严格 + 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 0 
反向, 改 1 字符 / 漏 1 字符 = 1 verb 扩展 0 31 元素 0 闭环 0 漂动), **Stage 2 0 改 Bind 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 
严格分离契约** (T167 #86 落地后 `bind_windup_vfx.gd` 4 hook + trigger() + `_draw()` 0 触碰既有 1:1 严格 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 
0 反向, 改 1 字符 / 漏 1 字符 = 1 verb 扩展 0 31 元素 0 闭环 0 漂动), **Stage 3 0 改 Cut 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 
严格分离契约** (T168 #86 落地后 `cut_windup_vfx.gd` 4 hook + trigger() + `_draw()` 0 触碰既有 1:1 严格 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 
0 反向, 改 1 字符 / 漏 1 字符 = 1 verb 扩展 0 31 元素 0 闭环 0 漂动), **Stage 4 0 改 Echo 1 verb 4 hook + trigger() + `_draw()` + 视觉组 
1:1 严格分离契约** (T169 #87 落地后 `echo_windup_vfx.gd` 4 hook + trigger() + `_draw()` 0 触碰既有 1:1 严格 0 漏 0 改 0 改 1 字符 0 漏 1 字符 
0 反序 0 反向, 改 1 字符 / 漏 1 字符 = 1 verb 扩展 0 31 元素 0 闭环 0 漂动), **Stage 5 0 改 Wave 1 verb 4 hook + trigger() + `_draw()` + 
视觉组 1:1 严格分离契约** (T171 #89 落地后 `wave_windup_vfx.gd` 4 hook + trigger() + `_draw()` 0 触碰既有 1:1 严格 0 漏 0 改 0 改 1 字符 0 漏 1 
字符 0 反序 0 反向, 改 1 字符 / 漏 1 字符 = 1 verb 扩展 0 31 元素 0 闭环 0 漂动), **base 4 hook 0 触碰既有 1:1 严格分离契约** (T166 #85 + T167 #86 + 
T168 #86 + T169 #87 + T171 #89 落地后 `_verb_windup_vfx_base.gd` `_ready()` 1 共享方法 + `_process()` 1 共享方法 + 
`_activate_windup_tween()` 1 共享方法 + `fade_out_and_free()` 1 共享方法 0 触碰既有 1:1 严格 0 漏 0 改 0 改 1 字符 0 漏 1 字符 0 反序 0 反向, 改 1 
字符 / 漏 1 字符 = 1 verb 扩展 0 1 base 4 hook 0 闭环 0 漂动). 任何"加新 1 verb windup VFX"必须**先**按 Stage 1 Pulse 1 verb 4 hook + 
trigger() + `_draw()` + 视觉组 1:1 严格 加 6 元素 (Stage 1 1:1 严格, 0 漏 6 元素 0 改 1 字符 0 反序 0 反向, **0 漏 6 元素**), **再**按 Stage 2 
Bind 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 严格 加 6 元素 (Stage 2 1:1 严格, 0 漏 6 元素 0 改 1 字符 0 反序 0 反向), **再**按 
Stage 3 Cut 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 严格 加 6 元素 (Stage 3 1:1 严格, 0 漏 6 元素 0 改 1 字符 0 反序 0 反向), 
**再**按 Stage 4 Echo 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 严格 加 6 元素 (Stage 4 1:1 严格, 0 漏 6 元素 0 改 1 字符 0 反序 0 
反向), **再**按 Stage 5 Wave 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 严格 加 6 元素 (Stage 5 1:1 严格, 0 漏 6 元素 0 改 1 字符 0 
反序 0 反向), **再**按 base 4 hook 0 触碰既有 1:1 严格 加 4 base hook 0 触碰既有 4 元素 (4 base hook 0 触碰既有 1:1 严格, 0 漏 4 元素 0 改 1 字符 0 反序 
0 反向), **再**按 1 显式契约 "subclasses MUST call _activate_windup_tween() in their trigger()" 加 1 显式契约 1 元素 (1 显式契约 1:1 严格, 0 
漏 1 元素 0 改 1 字符 0 反序 0 反向), **再**按 1 视觉组 0 触碰既有 1:1 严格 加 1 视觉组 0 触碰既有 1 元素 (1 视觉组 0 触碰既有 1:1 严格, 0 漏 1 元素 0 改 1 字符 0 反序 
0 反向).
  2. **31 元素 0 触碰边界** — 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 (5 verb × 4 hook = 20 hook 0 触碰 + 5 verb 
trigger() 0 override verb-specific 1:1 严格 + 5 verb `_draw()` 0 override verb-specific 1:1 严格 + 1 base 4 hook = 4 hook 0 
触碰 + 1 显式契约 + 5 verb 视觉组 5 段 + 1 视觉组 0 触碰既有 = 31 元素) 任何 1 元素 0 改 0 删 0 重排 (31 元素 0 触碰边界), 任何"修 1 verb windup VFX"polish 
走 Stage 1 1:1 严格 (改 verb windup VFX 既有 31 元素 时**必须**保持 31 元素 0 改 0 漏 0 反向 0 反序 0 触碰既有 1 元素); 任何"加新 1 trigger()"polish 走 
Stage 1 1:1 严格 (改 trigger() 既有 5 verb 时**必须**保持 5 verb 0 改 0 漏 0 反向 0 反序 0 漏 1 trigger()); 任何"加新 1 `_draw()`"polish 走 
Stage 1 1:1 严格 (改 `_draw()` 既有 5 verb 时**必须**保持 5 verb 0 改 0 漏 0 反向 0 反序 0 漏 1 `_draw()`); 任何"加新 1 显式契约"polish 走 Stage 
1 1:1 严格 (改 显式契约 既有 1 段 时**必须**保持 1 段 0 改 0 漏 0 反向 0 反序 0 漏 1 契约).
  3. **0 改 1 字段 0 漏 1 字段 0 反向** (T302 落地后 0 触碰边界) — 31 元素 1:1 严格分离 (5 verb × 4 hook = 20 hook 1:1 严格 镜像 1 套 polish 模式 
既有 1:1 严格分离 + 5 verb trigger() 0 override verb-specific 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离 + 5 verb `_draw()` 0 
override verb-specific 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离 + 1 base 4 hook 0 触碰既有 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离 + 
1 显式契约 0 触碰既有 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离 + 5 verb 视觉组 5 段 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离 + 1 视觉组 0 触碰既有 
1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离) 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反向, 改 1 字段 / 漏 1 字段 / 反向 1 字段 = 1 verb / 1 trigger() / 
1 `_draw()` / 1 显式契约 扩展 0 31 元素 0 漂动, source-grep 验证 31 元素 0 漏 0 改 1 字段 0 漏 1 字段 0 改 1 字符 0 反向.
  4. **0 改 1 边 0 漏 1 边 0 反向** (T302 落地后 0 触碰边界) — 接入契约 (1 Pulse 1 verb 1:1 严格 + 1 Bind 1 verb 1:1 严格 + 1 Cut 1 verb 1:1 
严格 + 1 Echo 1 verb 1:1 严格 + 1 Wave 1 verb 1:1 严格 + 1 base 4 hook 0 触碰既有 1:1 严格 + 1 显式契约 0 触碰既有 1:1 严格 + 1 视觉组 0 触碰既有 
1:1 严格) 1:1 严格分离 0 漏 0 改 0 改 1 边 0 漏 1 边 0 反向, 改 1 边 / 漏 1 边 / 反向 1 边 = 1 verb / 1 trigger() / 1 `_draw()` / 1 显式契约 扩展 
0 接入契约 0 漂动, source-grep 验证 31 元素 0 触碰接入契约 0 漏 0 改 1 边 0 漏 1 边 0 改 1 字符 0 反向.
  5. **T162 brittle 修复流程 0 触碰边界** — 31 元素 0 副作用 子段 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 触碰既有 0 改 1 段 0 
触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构 (T162 brittle 修复流程 = 1 expect 反转 + 1 
docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), 改 1 字段 / 漏 1 字段 / 反向 1 字段 = 1 
verb / 1 trigger() / 1 `_draw()` / 1 显式契约 扩展 0 0 副作用 0 漂动 0 闭环, source-grep 验证 31 元素 0 副作用 子段 0 漏 0 改 1 字段 0 改 1 字符 0 
反向 0 漏 1 字段 0 触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构.
  6. **1 套 polish 模式 0 互混 0 复用 0 共享 唯一性 标注 0 改 1 段 0 漏 1 段 0 反向** — §9.6.46 段 唯一性 标注 1:1 严格分离 0 漏 0 改 0 改 1 套 polish 模式 
0 漏 1 套 polish 模式 0 反向 0 触碰既有 0 改 1 段 (本节是 37 套 polish 模式**唯一**关注 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 
的 1 套, 0 互混 0 复用 0 共享), 改 1 套 polish 模式 / 漏 1 套 polish 模式 / 反向 1 套 polish 模式 = 1 §9.6.46 段 0 唯一性 标注 0 漂动 0 闭环, 
source-grep 验证 §9.6.46 段 唯一性 标注 0 漏 0 改 1 套 polish 模式 0 漏 1 套 polish 模式 0 改 1 字符 0 反向.
  7. **§9.6.46 是 37 套 polish 模式**唯一**关注 "5 verb windup VFX `_ready()` + `_process()` + `_activate_windup_tween()` + 
`fade_out_and_free()` 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 (`_verb_windup_vfx_base.gd` `_ready()` 1 共享方法 + `_process()` 1 
共享方法 + `_activate_windup_tween()` 1 共享方法 + `fade_out_and_free()` 1 共享方法 + 显式契约 1 段 "subclasses MUST call 
_activate_windup_tween() in their trigger()" + 5 verb 各自 4 hook 0 override 0 触碰 base 1:1 严格继承 + 5 verb trigger() 0 
override verb-specific 1:1 严格 + 5 verb `_draw()` 0 override verb-specific 1:1 严格 + 5 verb 视觉组 5 段 1:1 严格 + 1 视觉组 0 触碰既有 
1:1 严格) + Pulse 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 严格 (1 0 override `_ready()` 0 触碰 base 1:1 严格继承 + 1 0 
override `_process()` 0 触碰 base 1:1 严格继承 + 1 0 override `_activate_windup_tween()` 0 触碰 base 1:1 严格继承 + 1 0 override 
`fade_out_and_free()` 0 触碰 base 1:1 严格继承 + 1 0 override trigger() verb-specific 1:1 严格 + 1 0 override `_draw()` 
verb-specific 1:1 严格 + 1 视觉组 1 段 1:1 严格 0 漏 0 改) + Bind 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 严格 + Cut 1 verb 
4 hook + trigger() + `_draw()` + 视觉组 1:1 严格 + Echo 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 严格 + Wave 1 verb 4 
hook + trigger() + `_draw()` + 视觉组 1:1 严格 + 1 base 4 hook 0 触碰既有 1:1 严格 + 1 显式契约 0 触碰既有 1:1 严格 + 1 视觉组 0 触碰既有 1:1 严格 + 
0 触碰游戏代码 + 0 触碰 §10 决策记录流程 0 触碰既有" 的 1 套** — 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / 
§9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / 
§9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / 
§9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 36 套 polish 模式 0 互混 0 复用 0 共享. §9.6.19 (T273 #192 落地) 关注 "7 件套 windup 
VFX base (1 base + 5 verb subclasses + 1 显式契约 字节码 1:1 严格)" (与 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 0 相关,
 7 件套仅 字节码 1:1 严格 0 涉及 4 hook 自身 1:1 严格分离, 0 涉及 trigger() verb-specific 1:1 严格分离, 0 涉及 `_draw()` verb-specific 1:1 
严格分离), §9.6.33 (T289 #218 落地) 关注 "6 verb 接入 4 件套 字节码一致性" (与 5 verb windup VFX 4 hook lifecycle 0 触碰既有 0 相关, 4 件套 字节码 0 
涉及 4 hook 自身 1:1 严格分离). 任何"加新 1 verb windup VFX"或"加新 1 trigger()"或"加新 1 `_draw()`"或"加新 1 显式契约"必须**先**按 §9.6.46 Stage 1 
Pulse 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 严格 加 6 元素 (Stage 1 1:1 严格, 0 漏 6 元素 0 改 1 字符 0 反序 0 反向, **0 漏 6 
元素**), **再**按 §9.6.46 Stage 2 Bind 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 严格 加 6 元素 (Stage 2 1:1 严格, 0 漏 6 元素 
0 改 1 字符 0 反序 0 反向), **再**按 §9.6.46 Stage 3 Cut 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 严格 加 6 元素 (Stage 3 1:1 
严格, 0 漏 6 元素 0 改 1 字符 0 反序 0 反向), **再**按 §9.6.46 Stage 4 Echo 1 verb 4 hook + trigger() + `_draw()` + 视觉组 1:1 严格 加 6 元素 
(Stage 4 1:1 严格, 0 漏 6 元素 0 改 1 字符 0 反序 0 反向), **再**按 §9.6.46 Stage 5 Wave 1 verb 4 hook + trigger() + `_draw()` + 视觉组 
1:1 严格 加 6 元素 (Stage 5 1:1 严格, 0 漏 6 元素 0 改 1 字符 0 反序 0 反向), **再**按 base 4 hook 0 触碰既有 1:1 严格 加 4 base hook 0 触碰既有 4 元素 
(4 base hook 0 触碰既有 1:1 严格, 0 漏 4 元素 0 改 1 字符 0 反序 0 反向), **再**按 1 显式契约 0 触碰既有 1:1 严格 加 1 显式契约 1 元素 (1 显式契约 1:1 严格, 0 漏 
1 元素 0 改 1 字符 0 反序 0 反向), **再**按 1 视觉组 0 触碰既有 1:1 严格 加 1 视觉组 0 触碰既有 1 元素 (1 视觉组 0 触碰既有 1:1 严格, 0 漏 1 元素 0 改 1 字符 0 反序 0 
反向).
  8. 已知 drift risk: 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 31 元素 接入 **可能**漏 5 元素 / 1 边 1:1 镜像 (e.g. 加新 1 
verb windup VFX 时漏 Stage 1 Pulse 1 verb 中 6 元素 0 镜像, 1 verb 0 31 元素 0 闭环; 加新 1 verb windup VFX 时漏 Stage 2 Bind 1 verb 中 
6 元素 0 镜像, 1 verb 0 Bind 0 闭环; 加新 1 verb windup VFX 时漏 Stage 3 Cut 1 verb 中 6 元素 0 镜像, 1 verb 0 Cut 0 闭环; 加新 1 verb 
windup VFX 时漏 Stage 4 Echo 1 verb 中 6 元素 0 镜像, 1 verb 0 Echo 0 闭环; 加新 1 verb windup VFX 时漏 Stage 5 Wave 1 verb 中 6 元素 0 
镜像, 1 verb 0 Wave 0 闭环; 加新 1 verb windup VFX 时漏 base 4 hook 中 4 元素 0 镜像, 1 verb 0 base 4 hook 0 闭环; 加新 1 verb windup 
VFX 时漏 1 显式契约 中 1 元素 0 镜像, 1 verb 0 显式契约 0 闭环; 加新 1 verb windup VFX 时漏 1 视觉组 0 触碰既有 中 1 元素 0 镜像, 1 verb 0 视觉组 0 触碰既有 0 
闭环). 监控建议: 任何"未来给 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 加新 1 verb windup VFX / 加新 1 trigger() / 加新 1 
`_draw()` / 加新 1 显式契约"必须**先**在 31 元素 (Stage 1 Pulse 1 verb 6 元素 1:1 严格 + Stage 2 Bind 1 verb 6 元素 1:1 严格 + Stage 3 Cut 
1 verb 6 元素 1:1 严格 + Stage 4 Echo 1 verb 6 元素 1:1 严格 + Stage 5 Wave 1 verb 6 元素 1:1 严格 + 1 base 4 hook 0 触碰既有 1:1 严格 + 
1 显式契约 0 触碰既有 1:1 严格 + 1 视觉组 0 触碰既有 1:1 严格) 1:1 镜像 0 漏 6 元素 / 0 漏 1 边 / 0 漏 1 字符 / **0 漏 1 行**. T302 (#228) 31 元素 1:1 
镜像落地后任何新 verb windup VFX / 新 trigger() / 新 `_draw()` / 新 显式契约 是 §9.6.46 31 元素的"母集" (1 verb windup VFX / 1 trigger() / 1 
`_draw()` / 1 显式契约 扩展 × 5 verb × 6 元素 + 1 base 4 hook + 1 显式契约 + 1 视觉组 0 触碰既有 = 31 元素 1:1 严格, §9.6.46 31 元素 1 套 polish 
模式 完整闭环, 1 套 polish 模式 × 31 元素 = 31 元素 1:1 严格). 另 1 类已知 drift: Stage 1 Pulse 6 元素 1:1 严格 0 改 1 字符 0 漏 1 字符 0 反向, 后续 
polish 期若加新 1 verb windup VFX 必须**先**用 1:1 镜像 31 元素 模板, 0 改既有 0 漏 6 元素 0 漏 1 边 0 反向 **0 漏 1 字符**.

**0 副作用**: T302 (#228) 任何 const / var 0 触碰 (`_verb_windup_vfx_base.gd` 0 改 0 删 0 重排 `_ready()` / `_process()` / 
`_activate_windup_tween()` / `fade_out_and_free()` 方法任何 1 字符; `pulse_windup_vfx.gd` 0 改 0 删 0 重排 任何 1 字符; 
`bind_windup_vfx.gd` 0 改 0 删 0 重排 任何 1 字符; `cut_windup_vfx.gd` 0 改 0 删 0 重排 任何 1 字符; `echo_windup_vfx.gd` 0 改 0 删 0 重排 
任何 1 字符; `wave_windup_vfx.gd` 0 改 0 删 0 重排 任何 1 字符; CONTRIBUTING.md §9.6.46 段 0 改 0 删 0 重排, 仅新加 1 段); 0 触碰 §9.6.6 
ProfileRecentList 5 行 row 文本 5→7 字段 format 字符串扩展模式 (T302 0 改 §9.6.6 任何 1 字符); 0 触碰 §9.6.7 ProfileRecentList 5 行 row 
hover feedback 三件套 (T302 0 改 §9.6.7 任何 1 字符); 0 触碰 §9.6.8 ProfileQuickStats 4 段独立 hover 联动 (T302 0 改 §9.6.8 任何 1 字符); 0 
触碰 §9.6.9 AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` Dictionary 双源 (T302 0 改 §9.6.9 任何 1 字符); 0 触碰 §9.6.10 
「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (T302 0 改 §9.6.10 任何 1 字符); 0 触碰 §9.6.15 SaveSystem audit_save_slots (T302 0 改 
§9.6.15 任何 1 字符); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T302 0 改 §9.6.16 任何 1 字符); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 
(T302 0 改 §9.6.17 任何 1 字符); 0 触碰 §9.6.18 16 件套 verb ability base (T302 0 改 §9.6.18 任何 1 字符); 0 触碰 §9.6.19 7 件套 windup 
VFX base (T302 0 改 §9.6.19 任何 1 字符); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 transition flow (T302 0 改 §9.6.20 任何 1 字符); 0 触碰 
§9.6.21 8 段成就序列 (T302 0 改 §9.6.21 任何 1 字符); 0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T302 0 改 §9.6.22 任何 1 字符); 0 触碰 §9.6.23 7 
桶 prewarm 5+1+1 (T302 0 改 §9.6.23 任何 1 字符); 0 触碰 §9.6.24 HUD 6 verb × 6 通道 = 36 通道 5 段序列 (T302 0 改 §9.6.24 任何 1 字符); 0 
触碰 §9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 5 段序列 (T302 0 改 §9.6.25 任何 1 字符); 0 触碰 §9.6.26 6 verb icon 视觉组 5 段 canonical 1:1 
序列 (T302 0 改 §9.6.26 任何 1 字符); 0 触碰 §9.6.27 PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 5 段序列 (T302 0 改 §9.6.27 任何 1 
字符); 0 触碰 §9.6.28 smoke test ITERATION_COUNT 跨迭代 + 段边界 find 5 段序列 (T302 0 改 §9.6.28 任何 1 字符); 0 触碰 §9.6.29 polish 文档化 5 
段 canonical 1:1 序列 模板 + 4 段关系段 模板 (T302 0 改 §9.6.29 任何 1 字符); 0 触碰 §9.6.30 工具链 3 件套 1:1 严格分离契约 (T302 0 改 §9.6.30 任何 1 
字符); 0 触碰 §9.6.31 CHANGELOG 归档 3 件套 1:1 严格分离契约 (T302 0 改 §9.6.31 任何 1 字符); 0 触碰 §9.6.32 REVIEW_LOG 归档 3 件套 1:1 严格分离契约 
(T302 0 改 §9.6.32 任何 1 字符); 0 触碰 §9.6.33 6 verb 接入 4 件套 字节码一致性 (T302 0 改 §9.6.33 任何 1 字符); 0 触碰 §9.6.34 PlayerStats 
持久化层字段扩展 6 段 canonical 1:1 严格分离契约 (T302 0 改 §9.6.34 任何 1 字符); 0 触碰 §9.6.35 AudioPresets `MUSIC_PRESETS` preset 字段扩展 5 段 
canonical 1:1 严格分离契约 (T302 0 改 §9.6.35 任何 1 字符); 0 触碰 §9.6.36 PlayerActionGate 单 autoload 4 件套 接入 1:1 严格分离契约 (T302 0 改 
§9.6.36 任何 1 字符); 0 触碰 §9.6.37 SaveSystem save data CRC32 校验 5 段 接入 1:1 严格分离契约 (T302 0 改 §9.6.37 任何 1 字符); 0 触碰 §9.6.38 
6 verb audio 家族 19 cue 字段扩展 5 段 接入 1:1 严格分离契约 (T302 0 改 §9.6.38 任何 1 字符); 0 触碰 §9.6.39 T162 brittle 修复流程 5 步骤 接入 1:1 
严格分离契约 (T302 0 改 §9.6.39 任何 1 字符); 0 触碰 §9.6.40 6 verb cooldown ready jingle 5 段 1:1 严格分离契约 (T302 0 改 §9.6.40 任何 1 字符); 
0 触碰 §9.6.41 6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约 (T302 0 改 §9.6.41 任何 1 字符); 0 触碰 §9.6.42 6 verb 
`_exit_tree()` super 调用顺序 6 verb 1:1 严格分离契约 (T302 0 改 §9.6.42 任何 1 字符); 0 触碰 §9.6.43 6 verb `_ready()` + `_exit_tree()` 
双 hook 串联 1:1 严格分离契约 (T302 0 改 §9.6.43 任何 1 字符); 0 触碰 §9.6.44 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 
1:1 严格分离契约 (T302 0 改 §9.6.44 任何 1 字符); 0 触碰 §9.6.45 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 
视觉组连贯 lifecycle 1:1 严格分离契约 (T302 0 改 §9.6.45 任何 1 字符); 0 触碰 check_smoke_consistency.sh (T302 0 改任何规则); 0 触碰 
_parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓);
 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, 0 触碰 `_verb_windup_vfx_base.gd` `_ready()` / `_process()` / 
`_activate_windup_tween()` / `fade_out_and_free()` 方法任何 1 字符, 0 触碰 5 verb 各自 4 hook 任何 1 字符, 0 触碰 5 verb trigger() 任何 1 
字符, 0 触碰 5 verb `_draw()` 任何 1 字符); 0 行为变化, 1 文档 1 段 (§9.6.46) + 0 字节码修改 (T302 仅文档化既有 5 verb windup VFX 4 hook 
lifecycle 0 触碰既有 1:1 严格分离契约, 0 改 `_verb_windup_vfx_base.gd` 任何 1 字符 0 改 5 verb 各自 4 hook 任何 1 字符 0 触碰既有) + 1 smoke test 
(T302 80+ 断言).

**与 §9.6.19 关系** — §9.6.19 (T273 #192 落地) 关注 "7 件套 windup VFX base (1 base + 5 verb subclasses + 1 显式契约 字节码 1:1 严格)" (7 
件套覆盖 5 verb windup VFX base 字节码 1:1 严格 0 涉及 4 hook 自身 1:1 严格分离, 0 涉及 trigger() verb-specific 1:1 严格分离, 0 涉及 `_draw()` 
verb-specific 1:1 严格分离, 0 涉及 5 verb 视觉组 5 段 1:1 严格 + 1 视觉组 0 触碰既有 1:1 严格). §9.6.46 关注 "5 verb windup VFX 4 hook 
lifecycle 0 触碰既有 1:1 严格分离契约" (31 元素 1:1 严格, 31 元素覆盖 5 verb 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 0 涉及 5 verb windup VFX 
base 字节码 1:1 严格 自身 1:1 严格分离). §9.6.19 与 §9.6.46 0 互混 0 复用 0 共享: §9.6.19 关注 "7 件套 windup VFX base 字节码 1:1 严格" (与 4 hook 
lifecycle 0 触碰既有 0 相关, 7 件套字节码 0 涉及 4 hook 自身 1:1 严格分离, 0 涉及 trigger() verb-specific 1:1 严格分离, 0 涉及 `_draw()` 
verb-specific 1:1 严格分离, 0 涉及 5 verb 视觉组 5 段 1:1 严格 + 1 视觉组 0 触碰既有 1:1 严格), §9.6.46 关注 "5 verb windup VFX 4 hook 
lifecycle 0 触碰既有 1:1 严格分离契约" (与 7 件套 字节码 0 相关, 31 元素 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 31 元素 = 31 元素 
1:1 严格). §9.6.46 与 §9.6.19 是 "姊妹段 + 拼接段" (1 套 polish 模式 × 31 元素 = 31 元素 1:1 严格 包含 1 套 polish 模式 × 7 件套 = 7 件套 1:1 严格, 
一是 7 件套 windup VFX base 字节码, 一是 5 verb windup VFX 4 hook lifecycle 0 触碰既有). 任何"加新 1 verb windup VFX"必须**先**按 §9.6.46 31 
元素 1:1 复制 (31 元素 1:1 严格, 0 漏 6 元素 0 改 1 字段 0 反向) **或** 按 §9.6.19 7 件套 1:1 复制 (7 件套 1:1 严格, 0 漏 1 件 0 改 1 字段 0 反向), 
**再**按 §9.6.46 31 元素 1:1 同步 (31 元素 1:1 严格, 0 漏 6 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.19 任何 1 字符) **或** 按 
§9.6.19 7 件套 1:1 同步 (7 件套 1:1 严格, 0 漏 1 件 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.46 任何 1 字符).

**与 §9.6.33 关系** — §9.6.33 (T289 #218 落地) 关注 "6 verb 接入 4 件套 字节码一致性" (4 件套覆盖 6 verb 接入 字节码 0 涉及 5 verb windup VFX 4 
hook lifecycle 0 触碰既有 1:1 严格分离, 0 涉及 trigger() verb-specific 1:1 严格分离, 0 涉及 `_draw()` verb-specific 1:1 严格分离, 0 涉及 5 
verb 视觉组 5 段 1:1 严格 + 1 视觉组 0 触碰既有 1:1 严格). §9.6.46 关注 "5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约" (31 元素 
1:1 严格, 31 元素覆盖 5 verb 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 0 涉及 6 verb 接入 4 件套 字节码 自身 1:1 严格分离). §9.6.33 与 §9.6.46 0 互混 
0 复用 0 共享: §9.6.33 关注 "6 verb 接入 4 件套 字节码一致性" (与 4 hook lifecycle 0 触碰既有 0 相关, 4 件套 字节码 0 涉及 4 hook 自身 1:1 严格分离, 0 涉及 
trigger() verb-specific 1:1 严格分离, 0 涉及 `_draw()` verb-specific 1:1 严格分离, 0 涉及 5 verb 视觉组 5 段 1:1 严格 + 1 视觉组 0 触碰既有 1:1 
严格), §9.6.46 关注 "5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约" (与 4 件套 字节码 0 相关, 31 元素 1:1 严格 镜像 1 套 polish 模式 
既有 1:1 严格分离, 1 套 polish 模式 × 31 元素 = 31 元素 1:1 严格). §9.6.46 与 §9.6.33 是 "姊妹段 + 拼接段" (1 套 polish 模式 × 31 元素 = 31 元素 1:1 
严格 包含 1 套 polish 模式 × 4 件套 = 4 件套 1:1 严格, 一是 6 verb 接入 4 件套 字节码, 一是 5 verb windup VFX 4 hook lifecycle 0 触碰既有; 1 套 
polish 模式 × 31 元素 = 31 元素 1:1 严格 ⊃ 1 套 polish 模式 × 4 件套 = 4 件套 1:1 严格). 任何"加新 1 verb windup VFX"必须**先**按 §9.6.46 31 元素 
1:1 复制 (31 元素 1:1 严格, 0 漏 6 元素 0 改 1 字段 0 反向) **或** 按 §9.6.33 4 件套 1:1 复制 (4 件套 1:1 严格, 0 漏 1 件 0 改 1 字段 0 反向), **再**按 
§9.6.46 31 元素 1:1 同步 (31 元素 1:1 严格, 0 漏 6 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.33 任何 1 字符) **或** 按 §9.6.33 4 
件套 1:1 同步 (4 件套 1:1 严格, 0 漏 1 件 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.46 任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 
0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 33 轮 0 后续 持续收敛), §9.6.46 是"5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约" (31 元素 
1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约). T162 与 §9.6.46 0 互混 0 复用 0 共享: T162 关注 "brittle 修复流程 5 步骤" (1 expect 反转 + 1 
docblock 说明 + 1 段 find 反转 + 0 触碰既有 + 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 步骤 1:1 严格), §9.6.46 关注 "31 元素 
1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约" (与 5 步骤 0 相关, 1 套 polish 模式 × 31 元素 = 31 元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离). 任何"加新 
1 brittle 修复"必须**先**按 T162 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), **再**按 §9.6.46 
31 元素 1:1 镜像 (31 元素 1:1 严格, 0 漏 6 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.46 5 verb × 6 元素 + 1 base 4 hook + 1 显式契约 + 1 视觉组 0 触碰既有 = 31 元素 走 §9.1 9 步落地的 
1 步 (Stage 7 windup VFX 子类, 跨 5 verb 各 6 元素 + 1 base 4 hook + 1 显式契约 + 1 视觉组 0 触碰既有), 0 触碰 8 步 (1 `verb` 枚举 + 1 
`ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染). 
任何"加新 1 verb"必须**先**按 §9.6.46 31 元素 1:1 镜像 (31 元素 1:1 严格, 0 漏 6 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 8 步 (`verb` 枚举 
+ `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `audio` preset 字段 + `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 
步"必须**先**在 §9.6.46 31 元素 加 1 边 (1 Pulse 6 元素 entry 或 1 Bind 6 元素 边 或 1 Cut 6 元素 边 或 1 Echo 6 元素 边 或 1 Wave 6 元素 边 或 1 
base 4 hook 0 触碰既有 4 边 或 1 显式契约 1 边 或 1 视觉组 0 触碰既有 1 边), 0 触碰既有 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 
`input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).

---


### 9.6.47 5 verb windup VFX `_draw()` verb-specific 1:1 严格分离契约 polish 模式 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + 
T171 #89 跨 5 任务 ~140 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 37 套 polish 模式均在 "pause_menu / 6 verb 单层 / 6 verb 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / 
> PlayerStats 持久化层 / audio 数据层 / PlayerActionGate 单 autoload 4 件套 / SaveSystem CRC32 校验 / T162 brittle 修复流程 / 6 verb 
> audio 家族 19 cue / 6 verb cooldown ready jingle 5 段 / 6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约 / 6 verb 
> `_exit_tree()` super 调用顺序 6 verb 1:1 严格分离契约 / 6 verb 双 hook 串联 1:1 严格分离契约 / 6 verb 双 hook 串联 + `_player` non-null 
> assertion 0 触碰既有 1:1 严格分离契约 / 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 
> 严格分离契约 / 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约" 范畴, 0 涉及 "5 verb windup VFX `_draw()` verb-specific 
> 1:1 严格分离契约" 独立聚焦段落地. §9.6.46 (T302 #228 落地) 关注 "5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约" (31 元素 1:1 严格, 
> 1 套 polish 模式 × 5 verb × 6 元素 + 1 base 4 hook + 1 显式契约 + 5 verb 视觉组 5 段 + 1 视觉组 0 触碰既有 = 31 元素, 5 verb × 6 元素 = 1 
> verb 中 4 hook + 1 trigger() + 1 `_draw()` 6 元素 1:1 严格, `_draw()` 仅是 5 verb 6 元素 中 1 元素, 0 独立聚焦段 1:1 详细说明 5 verb 
> `_draw()` verb-specific 1:1 严格分离契约自身). 本节 (T303 #229 落地) 首个落在**5 verb windup VFX `_draw()` verb-specific 1:1 严格分离契约** 
> (5 verb `_draw()` 0 override verb-specific 1:1 严格 (Pulse / Bind / Cut / Echo / Wave 5 verb 0 override `_draw()` 0 触碰 
> base 1:1 严格, 5 verb 各自 `_draw()` 1:1 严格 镜像 5 verb 视觉组 5 段 1:1 严格分离) + 5 verb 共享 3 字段 1:1 严格 (5 verb × 3 字段 = 15 字段 
> 1:1 严格 镜像, 1 guard + 1 progress + 1 alpha = 3 共享字段 1:1 严格, 0 漏 0 改 0 反序 0 反向) + 5 verb 视觉组 5 段 1:1 严格 (Pulse 同心圆环 + 
> Coral Pulse 核 / Bind 向内螺旋 + Muted Violet 核 / Cut 4 三角碎片 + Amber Voice 核 / Echo 8 棱镜折射 + Glass Cyan 核 / Wave 3 同心圆环 + 
> Pale Resonance 核 0 触碰 base 1:1 严格) + 1 显式契约 "subclasses MUST implement verb-specific `_draw()` with 1 guard + 1 
> progress + 1 alpha + verb-specific 视觉组" 1 段 + 1 视觉组 0 触碰既有 1:1 严格 (5 verb 视觉组 5 段 0 漏 0 改 0 反序 0 反向) + 1 共享 3 字段 0 
> 触碰既有 1:1 严格 (3 共享字段 0 漏 0 改 0 反序 0 反向) + 1 base `_draw()` 0 override 1:1 严格 (base 0 override `_draw()` 0 触碰 5 verb 
> `_draw()` 1:1 严格继承) + 1 `_draw()` 0 override verb-specific 0 触碰既有 1:1 严格) polish 模式 — 跨 5 verb `_draw()` + 5 verb 共享 
> 3 字段 + 5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 1 base `_draw()` 0 override + 1 `_draw()` 0 
> override verb-specific 0 触碰既有 = 5 + 15 + 5 + 1 + 1 + 1 + 1 + 1 = 30 元素 1:1 严格 (Stage 1 Pulse 1 verb `_draw()` + 1 
> verb 共享 3 字段 + 1 verb 视觉组 1:1 严格 + Stage 2 Bind 1 verb `_draw()` + 1 verb 共享 3 字段 + 1 verb 视觉组 1:1 严格 + Stage 3 Cut 1 
> verb `_draw()` + 1 verb 共享 3 字段 + 1 verb 视觉组 1:1 严格 + Stage 4 Echo 1 verb `_draw()` + 1 verb 共享 3 字段 + 1 verb 视觉组 1:1 
> 严格 + Stage 5 Wave 1 verb `_draw()` + 1 verb 共享 3 字段 + 1 verb 视觉组 1:1 严格 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 
> 1 base `_draw()` 0 override + 1 `_draw()` 0 override verb-specific 0 触碰既有) — 跨 1 套 polish 模式 (T303 #229 落地) 跨 5 任务 
> (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 ~140 轮落地, 0 漂动 0 后续 主要 pre-existing 风险). 任何"加新 1 verb windup 
> VFX `_draw()`" (e.g. 未来加 6th verb windup VFX "WhisperWindupVFX" 接入 `_draw()` verb-specific 1:1 严格分离) 必须**先**按 Stage 1 
> Pulse 1 verb `_draw()` + 1 verb 共享 3 字段 + 1 verb 视觉组 1:1 镜像 加 3 元素 (Stage 1 1:1 严格, 0 漏 3 元素 0 改 1 字符 0 反序 0 反向, **0 
> 漏 3 元素**), **再**按 Stage 2 Bind / Stage 3 Cut / Stage 4 Echo / Stage 5 Wave 1 verb `_draw()` + 1 verb 共享 3 字段 + 1 verb 
> 视觉组 1:1 镜像 4 段 (4 段 1:1 严格, 0 漏 3 元素 0 改 1 字符 0 反序 0 反向). 跨 1 套 polish 模式 × 5 verb × 3 元素 = 15 元素 + 5 verb `_draw()` 
> 0 override + 5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 1 base `_draw()` 0 override + 1 `_draw()` 0 
> override verb-specific 0 触碰既有 = 30 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: 5 verb windup VFX (`src/scripts/pulse_windup_vfx.gd` + `src/scripts/bind_windup_vfx.gd` + 
`src/scripts/cut_windup_vfx.gd` + `src/scripts/echo_windup_vfx.gd` + `src/scripts/wave_windup_vfx.gd`) `_draw()` 
verb-specific 1:1 严格分离契约 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 跨 5 任务 ~140 轮落地) 现有的 30 元素 1:1 严格 (1 
Pulse `_draw()` 0 override verb-specific 1:1 严格 + 1 Pulse 共享 3 字段 1:1 严格 (1 guard + 1 progress + 1 alpha) + 1 Pulse 视觉组 
1 段 1:1 严格 (同心圆环 + Coral Pulse 核) + 1 Bind `_draw()` 0 override verb-specific 1:1 严格 + 1 Bind 共享 3 字段 1:1 严格 (1 guard + 
1 progress + 1 alpha) + 1 Bind 视觉组 1 段 1:1 严格 (向内螺旋 + Muted Violet 核) + 1 Cut `_draw()` 0 override verb-specific 1:1 严格 
+ 1 Cut 共享 3 字段 1:1 严格 (1 guard + 1 progress + 1 alpha) + 1 Cut 视觉组 1 段 1:1 严格 (4 三角碎片 + Amber Voice 核) + 1 Echo 
`_draw()` 0 override verb-specific 1:1 严格 + 1 Echo 共享 3 字段 1:1 严格 (1 guard + 1 progress + 1 alpha) + 1 Echo 视觉组 1 段 1:1 
严格 (8 棱镜折射 + Glass Cyan 核) + 1 Wave `_draw()` 0 override verb-specific 1:1 严格 + 1 Wave 共享 3 字段 1:1 严格 (1 guard + 1 
progress + 1 alpha) + 1 Wave 视觉组 1 段 1:1 严格 (3 同心圆环 + Pale Resonance 核) + 1 base `_draw()` 0 override 1:1 严格 (base 0 
override `_draw()` 0 触碰 5 verb `_draw()` 1:1 严格继承) + 1 显式契约 "subclasses MUST implement verb-specific `_draw()` with 1 
guard + 1 progress + 1 alpha + verb-specific 视觉组" 1 段 + 1 视觉组 0 触碰既有 1:1 严格 (5 verb 视觉组 5 段 0 漏 0 改 0 反序 0 反向) + 1 共享 3 
字段 0 触碰既有 1:1 严格 (3 共享字段 0 漏 0 改 0 反序 0 反向) + 1 `_draw()` 0 override verb-specific 0 触碰既有 1:1 严格), 在反复 polish (T166 #85 
→ T171 #89 跨 5 任务 ~140 轮落地) 期间出现 3 类典型 fragile: (1) **"加新 1 verb windup VFX `_draw()` 漏 1 边 'subclasses MUST implement 
verb-specific `_draw()` with 1 guard + 1 progress + 1 alpha' 显式契约"** — 加新 1 verb windup VFX (e.g. 6th verb 
"WhisperWindupVFX" 接入 `_draw()` verb-specific 1:1 严格分离) 落地时漏 1 边 "subclasses MUST implement verb-specific `_draw()` 
with 1 guard + 1 progress + 1 alpha + verb-specific 视觉组" 显式契约 (e.g. 漏 `_verb_windup_vfx_base.gd` 中 "subclasses MUST 
implement verb-specific `_draw()` with 1 guard + 1 progress + 1 alpha + verb-specific 视觉组" 1 段 0 镜像 1 verb, 1 verb 0 
显式契约 0 闭环 0 漂动); (2) **"加新 1 verb windup VFX `_draw()` 漏 1 边 '1 guard + 1 progress + 1 alpha' 3 共享字段"** — 加新 1 verb 
windup VFX (e.g. 6th verb "WhisperWindupVFX" 接入 `_draw()` verb-specific 1:1 严格分离) 落地时漏 1 边 "1 guard + 1 progress + 1 
alpha" 3 共享字段 (e.g. 漏 `whisper_windup_vfx.gd` `_draw()` 中 0 include 1 guard `if not _active: return` + 1 progress `var 
t := clampf(_lifetime / _max_lifetime, 0.0, 1.0)` + 1 alpha `var col := whisper_color; col.a = <peak_alpha>` 3 共享字段, 1 
verb 0 3 共享字段 0 闭环 0 漂动); (3) **"加新 1 verb windup VFX `_draw()` 漏 1 边 verb-specific 视觉组"** — 加新 1 verb windup VFX (e.g. 
6th verb "WhisperWindupVFX" 接入 `_draw()` verb-specific 1:1 严格分离) 落地时漏 1 边 verb-specific 视觉组 (e.g. 漏 
`whisper_windup_vfx.gd` `_draw()` 中 0 include 1 verb-specific 视觉组 (e.g. "8 棱镜折射 + 极光核" 假设未来 6th verb 视觉组), 1 verb 0 视觉组 
0 闭环 0 漂动). 3 类症状根因: 5 verb windup VFX `_draw()` verb-specific 1:1 序列 0 文档化独立聚焦段, 任何新 1 verb windup VFX `_draw()` / 1 
共享 3 字段 / 1 显式契约 / 1 verb-specific 视觉组 接入只能"凭经验" 1:1 镜像 30 元素 (5 verb `_draw()` 0 override verb-specific 1:1 严格 + 5 
verb 共享 3 字段 1:1 严格 + 5 verb 视觉组 5 段 1:1 严格 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 1 base `_draw()` 0 override + 
1 `_draw()` 0 override verb-specific 0 触碰既有 = 30 元素 1:1 严格), 1 漏 1 `_draw()` / 1 漏 1 共享 3 字段 / 1 漏 1 显式契约 / 1 漏 1 
verb-specific 视觉组 = 1 verb 扩展 0 30 元素 0 闭环 0 漂动.

- **触发场景**: 加新 1 verb windup VFX `_draw()` (e.g. 未来加 6th verb "WhisperWindupVFX" 接入 `_draw()` verb-specific 1:1 严格分离) / 
加新 1 共享 3 字段 / 加新 1 显式契约 / 加新 1 verb-specific 视觉组 (T166 + T167 + T168 + T169 + T171 模式参考) 时**必须**严格按 30 元素 1:1 复制 T166 
#85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 落地模式: (1) **Stage 1 Pulse 1 verb `_draw()` + 1 共享 3 字段 + 1 视觉组 1:1 严格** 
— 在 `_verb_windup_vfx_base.gd` 中 显式契约 1 段 0 触碰既有 1:1 严格 + 5 verb 各自 `_draw()` 0 override verb-specific 1:1 严格 (5 verb × 
1 `_draw()` = 5 `_draw()` 1:1 严格 0 漏 0 改 0 反序 0 反向) + 5 verb 共享 3 字段 1:1 严格 (5 verb × 3 字段 = 15 字段 1:1 严格 镜像, 1 guard + 
1 progress + 1 alpha = 3 共享字段 1:1 严格 0 漏 0 改 0 反序 0 反向) + 5 verb 视觉组 5 段 1:1 严格 (5 verb × 1 视觉组 = 5 视觉组 1:1 严格 0 漏 0 改 
0 反序 0 反向, Pulse 同心圆环 + Coral Pulse 核 / Bind 向内螺旋 + Muted Violet 核 / Cut 4 三角碎片 + Amber Voice 核 / Echo 8 棱镜折射 + Glass 
Cyan 核 / Wave 3 同心圆环 + Pale Resonance 核) + 1 显式契约 "subclasses MUST implement verb-specific `_draw()` with 1 guard + 1 
progress + 1 alpha + verb-specific 视觉组" 1 段 1:1 严格 (1 契约 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 视觉组 0 触碰既有 1:1 严格 (1 视觉组 0 触碰既有 
1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 共享 3 字段 0 触碰既有 1:1 严格 (3 共享字段 0 漏 0 改 0 反序 0 反向) + 1 base `_draw()` 0 override 1:1 严格 
(base 0 override `_draw()` 0 触碰 5 verb `_draw()` 1:1 严格继承) + 1 `_draw()` 0 override verb-specific 0 触碰既有 1:1 严格 = 30 元素 
1:1 严格 0 漏 0 改 0 反序 0 反向, 0 触碰既有 5 verb 任何 1 字符, source-grep 验证 `_draw()` + 3 共享字段 + 视觉组连贯 5 verb → 30 元素 0 漏 0 改 0 漏 1 
边 0 反向; (2) **Stage 2 Bind 1 verb `_draw()` + 1 共享 3 字段 + 1 视觉组 1:1 严格** — 30 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, source-grep 
验证 30 元素 0 漏 0 改 0 漏 1 边 0 反向; (3) **Stage 3 Cut 1 verb `_draw()` + 1 共享 3 字段 + 1 视觉组 1:1 严格** — 30 元素 1:1 严格 0 漏 0 改 0 
反序 0 反向; (4) **Stage 4 Echo 1 verb `_draw()` + 1 共享 3 字段 + 1 视觉组 1:1 严格** — 30 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (5) **Stage 
5 Wave 1 verb `_draw()` + 1 共享 3 字段 + 1 视觉组 1:1 严格** — 30 元素 1:1 严格 0 漏 0 改 0 反序 0 反向. 30 元素 1:1 同步, 1 漏 1 `_draw()` / 
1 漏 1 共享 3 字段 / 1 漏 1 显式契约 / 1 漏 1 verb-specific 视觉组 = 1 verb / 1 共享 3 字段 / 1 显式契约 / 1 verb-specific 视觉组 扩展 0 30 元素 0 
闭环 0 漂动. T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 0 触碰既有 0 改 `_verb_windup_vfx_base.gd` 任何 1 字符 (0 改 Pulse 
1 verb 任何 1 字符, 0 改 Bind 1 verb 任何 1 字符, 0 改 Cut 1 verb 任何 1 字符, 0 改 Echo 1 verb 任何 1 字符, 0 改 Wave 1 verb 任何 1 字符).

- **修复** (跨 1 套 polish 模式 T303 #229 落地后已知 30 元素 0 例外):
  - **T303 #229 首个 5 verb windup VFX `_draw()` verb-specific 1:1 严格分离契约 独立聚焦段 落地 (`pulse_windup_vfx.gd` `_draw()` 0 
override verb-specific 1:1 严格 + `bind_windup_vfx.gd` `_draw()` 0 override verb-specific 1:1 严格 + `cut_windup_vfx.gd` 
`_draw()` 0 override verb-specific 1:1 严格 + `echo_windup_vfx.gd` `_draw()` 0 override verb-specific 1:1 严格 + 
`wave_windup_vfx.gd` `_draw()` 0 override verb-specific 1:1 严格 + 5 verb 共享 3 字段 1:1 严格 (1 guard `if not _active: 
return` + 1 progress `var t := clampf(_lifetime / _max_lifetime, 0.0, 1.0)` + 1 alpha `var col := <verb>_color; col.a = 
<peak_alpha>` 5 verb 1:1 镜像) + 5 verb 视觉组 5 段 1:1 严格 (Pulse 同心圆环 + Coral Pulse 核 / Bind 向内螺旋 + Muted Violet 核 / Cut 4 
三角碎片 + Amber Voice 核 / Echo 8 棱镜折射 + Glass Cyan 核 / Wave 3 同心圆环 + Pale Resonance 核) + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 
0 触碰既有 + 1 base `_draw()` 0 override + 1 `_draw()` 0 override verb-specific 0 触碰既有)** — §9.6.47 段 是 1 套 polish 模式首个 1:1 
包含 5 verb `_draw()` + 5 verb 共享 3 字段 + 5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 1 base `_draw()` 0 
override + 1 `_draw()` 0 override verb-specific 0 触碰既有 = 30 元素 1:1 严格 的 5 verb windup VFX `_draw()` verb-specific 1:1 
严格分离契约 polish 模式, 30 元素 1:1 严格分离 (1 Pulse `_draw()` 0 override verb-specific 1:1 严格 + 1 Pulse 共享 3 字段 1:1 严格 (1 guard + 
1 progress + 1 alpha) + 1 Pulse 视觉组 1 段 1:1 严格 (同心圆环 + Coral Pulse 核) + 1 Bind `_draw()` 0 override verb-specific 1:1 
严格 + 1 Bind 共享 3 字段 1:1 严格 (1 guard + 1 progress + 1 alpha) + 1 Bind 视觉组 1 段 1:1 严格 (向内螺旋 + Muted Violet 核) + 1 Cut 
`_draw()` 0 override verb-specific 1:1 严格 + 1 Cut 共享 3 字段 1:1 严格 (1 guard + 1 progress + 1 alpha) + 1 Cut 视觉组 1 段 1:1 
严格 (4 三角碎片 + Amber Voice 核) + 1 Echo `_draw()` 0 override verb-specific 1:1 严格 + 1 Echo 共享 3 字段 1:1 严格 (1 guard + 1 
progress + 1 alpha) + 1 Echo 视觉组 1 段 1:1 严格 (8 棱镜折射 + Glass Cyan 核) + 1 Wave `_draw()` 0 override verb-specific 1:1 严格 
+ 1 Wave 共享 3 字段 1:1 严格 (1 guard + 1 progress + 1 alpha) + 1 Wave 视觉组 1 段 1:1 严格 (3 同心圆环 + Pale Resonance 核) + 1 base 
`_draw()` 0 override 1:1 严格 (base 0 override `_draw()` 0 触碰 5 verb `_draw()` 1:1 严格继承) + 1 显式契约 "subclasses MUST 
implement verb-specific `_draw()` with 1 guard + 1 progress + 1 alpha + verb-specific 视觉组" 1 段 1:1 严格 + 1 视觉组 0 触碰既有 
1:1 严格 (5 verb 视觉组 5 段 0 漏 0 改 0 反序 0 反向) + 1 共享 3 字段 0 触碰既有 1:1 严格 (3 共享字段 0 漏 0 改 0 反序 0 反向) + 1 `_draw()` 0 override 
verb-specific 0 触碰既有 1:1 严格, 0 漏 1 元素 0 改 1 字段 0 漏 1 字段 0 反向), 0 触碰既有 (T303 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / 
§9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / 
§9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / 
§9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 / §9.6.46 37 套 polish 模式任何 1 字符, 0 触碰游戏代码 
.gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / 
CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).

**与 §9.6.46 关系** — §9.6.46 (T302 #228 落地) 关注 "5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约" (31 元素 1:1 严格, 5 
verb × 6 元素 + 1 base 4 hook + 1 显式契约 + 5 verb 视觉组 5 段 + 1 视觉组 0 触碰既有 = 31 元素, 1 verb 6 元素 = 4 hook + 1 trigger() + 1 
`_draw()`, 5 verb `_draw()` 仅是 5 verb × 6 元素 中 5 元素, 0 独立聚焦段 1:1 详细说明 5 verb `_draw()` verb-specific 1:1 严格分离契约自身). 
§9.6.47 关注 "5 verb windup VFX `_draw()` verb-specific 1:1 严格分离契约" (30 元素 1:1 严格, 5 verb `_draw()` + 5 verb 共享 3 字段 + 5 
verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 1 base `_draw()` 0 override + 1 `_draw()` 0 override 
verb-specific 0 触碰既有 = 30 元素). §9.6.46 与 §9.6.47 0 互混 0 复用 0 共享: §9.6.46 关注 "5 verb windup VFX 4 hook lifecycle 0 触碰既有 
1:1 严格分离契约" (与 `_draw()` verb-specific 1:1 严格分离 0 相关, 31 元素 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 31 元素 
= 31 元素 1:1 严格 ⊃ 5 verb `_draw()` verb-specific 5 元素 子集 0 独立聚焦段), §9.6.47 关注 "5 verb windup VFX `_draw()` verb-specific 
1:1 严格分离契约" (与 4 hook lifecycle 0 触碰既有 0 相关, 30 元素 1:1 严格 0 涉及 4 hook 自身 1:1 严格分离, 0 涉及 trigger() verb-specific 1:1 
严格分离, 30 元素 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 30 元素 = 30 元素 1:1 严格 ⊃ 5 verb `_draw()` verb-specific 
5 元素 ⊃ 5 verb 共享 3 字段 15 字段 ⊃ 5 verb 视觉组 5 段 ⊃ 1 显式契约 ⊃ 1 视觉组 0 触碰既有 ⊃ 1 共享 3 字段 0 触碰既有 ⊃ 1 base `_draw()` 0 override ⊃ 
1 `_draw()` 0 override verb-specific 0 触碰既有). §9.6.46 与 §9.6.47 是 "姊妹段 + 聚焦段" (1 套 polish 模式 × 31 元素 = 31 元素 1:1 严格 ⊃ 1 
套 polish 模式 × 30 元素 = 30 元素 1:1 严格, 一是 4 hook lifecycle 0 触碰既有, 一是 `_draw()` verb-specific 1:1 严格分离; 1 套 polish 模式 × 30 
元素 = 30 元素 1:1 严格 ⊂ 1 套 polish 模式 × 31 元素 = 31 元素 1:1 严格, 30 元素 1:1 严格 是 31 元素 1:1 严格 中 5 verb `_draw()` verb-specific 
子集 + 5 verb 共享 3 字段 子集 + 5 verb 视觉组 5 段 子集 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 1 base `_draw()` 0 override + 1 
`_draw()` 0 override verb-specific 0 触碰既有 子集 0 涉及 4 hook + trigger() 自身 1:1 严格分离). 任何"加新 1 verb windup VFX 
`_draw()`"必须**先**按 §9.6.47 30 元素 1:1 复制 (30 元素 1:1 严格, 0 漏 3 元素 0 改 1 字段 0 反向) **或** 按 §9.6.46 31 元素 1:1 复制 (31 元素 1:1 
严格, 0 漏 6 元素 0 改 1 字段 0 反向), **再**按 §9.6.47 30 元素 1:1 同步 (30 元素 1:1 严格, 0 漏 3 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 
§9.6.46 任何 1 字符) **或** 按 §9.6.46 31 元素 1:1 同步 (31 元素 1:1 严格, 0 漏 6 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.47 任何 
1 字符).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 
0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 33 轮 0 后续 持续收敛), §9.6.47 是"5 verb windup VFX `_draw()` verb-specific 1:1 严格分离契约" (30 元素 
1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约). T162 与 §9.6.47 0 互混 0 复用 0 共享: T162 关注 "brittle 修复流程 5 步骤" (1 expect 反转 + 1 
docblock 说明 + 1 段 find 反转 + 0 触碰既有 + 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 步骤 1:1 严格), §9.6.47 关注 "30 元素 
1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约" (与 5 步骤 0 相关, 1 套 polish 模式 × 30 元素 = 30 元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离). 任何"加新 
1 brittle 修复"必须**先**按 T162 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), **再**按 §9.6.47 
30 元素 1:1 镜像 (30 元素 1:1 严格, 0 漏 3 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.47 5 verb × 3 元素 + 5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 
1 base `_draw()` 0 override + 1 `_draw()` 0 override verb-specific 0 触碰既有 = 30 元素 走 §9.1 9 步落地的 1 步 (Stage 7 windup VFX 
子类 `_draw()`, 跨 5 verb 各 3 元素 + 5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 1 base `_draw()` 0 override 
+ 1 `_draw()` 0 override verb-specific 0 触碰既有), 0 触碰 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 
1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染). 任何"加新 1 verb windup VFX `_draw()`"必须**先**按 §9.6.47 
30 元素 1:1 镜像 (30 元素 1:1 严格, 0 漏 3 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 8 步 (`verb` 枚举 + `ability` 子类 + `const` 字段 + 
`input` map + `shop` 系统 + `icon` PNG + `audio` preset 字段 + `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 步"必须**先**在 §9.6.47 30 元素 加 1 边 
(1 Pulse 3 元素 entry 或 1 Bind 3 元素 边 或 1 Cut 3 元素 边 或 1 Echo 3 元素 边 或 1 Wave 3 元素 边 或 1 verb 视觉组 1 段 边 或 1 显式契约 1 边 或 1 
视觉组 0 触碰既有 1 边 或 1 共享 3 字段 0 触碰既有 1 边 或 1 base `_draw()` 0 override 1 边 或 1 `_draw()` 0 override verb-specific 0 触碰既有 1 
边), 0 触碰既有 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` 
preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).
