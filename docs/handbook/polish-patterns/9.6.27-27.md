# Polish Patterns 9.6.27–27

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.27–9.6.27，共 1 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.27-27.md

<!-- shard 27-27 共 1 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.27 PlayerProfilePanel 3 组件 (ProfileQuickStats 4 段 + ProfileRecentList 5 行 × 7 字段 + ProfileAudit 4 字段) (1 panel 
× 3 组件 × 1:1 视觉组连贯) polish 模式 (T162 + T213 + T214 + T215 + T216 + T217 + T219 + T225 + T226 + T229 + T231 + T234 + T235 
+ T240 + T249 + T256 + T257 + T258 + T259 + T260 + T261 + T262 + T263 跨 23 任务 ~200 轮落地) 文档化

> §9.6.6 记录 ProfileRecentList 5 行 row 文本 5→7 字段 format 扩展模式; §9.6.7 记录 ProfileRecentList 5 行 row hover feedback 三件套 
> (font_color + alpha boost + tooltip); §9.6.8 记录 ProfileQuickStats 4 段独立 hover 联动 (4 段 tween + dim restore); §9.6.9 记录 
> AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` Dictionary 双源 (5 行 + 11 slot 共享 `clampf(base + 0.1)`); §9.6.10 
> 记录「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (T215 + T216 / T217 + T218 模式). 本节记录 `pause_menu.gd` PlayerProfilePanel 1 
> panel × 3 组件 × 1:1 视觉组连贯 polish 模式 — 跨 5 段 (Stage 1 3 组件 1:1 严格分离 + Stage 2 中点分隔符 ` · ` 1:1 严格 + Stage 3 7pt 字号 1:1 
> 严格 + Stage 4 _COLOR_* canonical 颜色 token 1:1 严格 + Stage 5 派生字段 1:1 严格) — 跨 23 任务 (T162 + T213 + T214 + T215 + T216 + 
> T217 + T219 + T225 + T226 + T229 + T231 + T234 + T235 + T240 + T249 + T256 + T257 + T258 + T259 + T260 + T261 + T262 
> + T263) ~200 轮迭代落地, 3 组件 × 1 panel 1:1 严格分离 (4 段 + 35 字段 + 4 字段 = 43 element 1:1 + 跨组件共享中点分隔符 + 跨组件共享 7pt 字号 + 跨组件共享 
> _COLOR_* canonical 颜色 token + 派生字段 1:1 严格), 0 漂动 0 后续 主要 pre-existing 风险. 任何"加新 1 段"或"加新 1 字段"或"加新 1 组件"或"修 1 段颜色"或"修 
> 1 字段格式"必须**先**按 Stage 1 3 组件 1:1 加 1 组件 (1 panel 1:1 严格), **再**按 Stage 2 中点分隔符 1:1 复制 (Stage 2 1:1 严格), **再**按 Stage 
> 3 7pt 字号 1:1 镜像 (Stage 3 1:1 严格), **再**按 Stage 4 _COLOR_* canonical 颜色 token 加 1 entry (Stage 4 1:1 严格), **再**按 Stage 
> 5 派生字段 1:1 同步 (Stage 5 1:1 严格). 跨 1 panel × 3 组件 × 4 段 (Achievements 成就 / 最佳 / 最长单房 / Run #) + 5 行 × 7 字段 (Run # / 房 
> / 净 / 碎 / 时 / 房/时 / 净/时) + 4 字段 (ok / 损坏 / 漂移 / 空) = 43 element 1:1 严格 0 漏 1 element 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: PlayerProfilePanel 1 panel × 3 组件 (ProfileQuickStats 4 段 + ProfileRecentList 5 行 × 7 字段 + ProfileAudit 4 字段) 
跨 23 task 反复 polish 期间出现 5 类典型 fragile: (1) "加新 1 段漏 Stage 1 3 组件 1:1" — 加新 1 段到 ProfileQuickStats 时漏 ProfileRecentList 
+ ProfileAudit 同步 (e.g. 加 "本周 5 局最佳" 段到 QuickStats 4 段 → 5 段, 漏 RecentList 加 1 行 同字段镜像, 漏 Audit 加 1 字段 同字段镜像, 玩家扫 3 
组件看到新段只在 QuickStats 出现 视觉组连贯崩塌); (2) "修 1 字段漏 Stage 2 中点分隔符 1:1 镜像" — 修 ProfileQuickStats 4 段 
`_format_quick_stats_row()` 字符串时改分隔符 e.g. "  ·  " → "  •  " 或 " | ", 触发 3 组件中点分隔符 1:1 严格 0 撞 ProfileRecentList 5 行 + 
ProfileAudit 4 字段 既有 _RECENT_ROW_FIELD_SEP (`  ·  ` 4 字符中点带前后 2 空格) 0 触碰 (Stage 2 1:1 严格 0 改 1 字符), 玩家扫 3 组件看到中点分隔符 0 
协调, 跨组件视觉组连贯崩塌; (3) "加新 1 组件漏 Stage 3 7pt 字号 1:1 镜像" — 加新 1 组件 (e.g. ProfileTrend 5/10/20 行) 到 PlayerProfilePanel 时漏 
font_size=7 0 走 8/9/10 字号 (e.g. T229 #149 ProfileAudit 早期版本 1 段用 8pt 后续 T256 #176 收回 7pt), 触发 3 组件 7pt 字号 1:1 严格 0 撞既有 
3 组件 7pt 0 触碰 (Stage 3 1:1 严格 0 改 1 pt), 玩家扫 3 组件看到新组件字号偏大 0 协调 跨组件视觉组连贯崩塌; (4) "修 1 段颜色漏 Stage 4 _COLOR_* canonical 颜色 
token 1:1 同步" — 修 ProfileQuickStats 4 段 4 调色 (Achievements Glass Cyan #69C7CE / 最佳 Amber Voice #F2B66E / 最长单房 Muted 
Violet #65506A / Run # Pale Resonance #B7E6DC) 0 改 _QUICK_STATS_HINT `_COLOR_*` canonical 颜色 token 
(`_COLOR_RECENT_RUN_LATEST` Amber Voice + `_COLOR_RECENT_RUN_NORMAL` Pale Resonance + `_COLOR_ZERO_STAT` 暖白 3 颜色 token 
+ ProfileAudit 4 字段 3 状态色 暖白 (0.875, 0.835, 0.784) / 暖黄 (0.85, 0.71, 0.43) / 暖红 (0.85, 0.45, 0.4) inline literals in 
`_refresh_profile_audit()`) 0 镜像 ProfileRecentList 5 行 2 色 + ProfileAudit 4 字段 3 状态色, 触发 Stage 4 1:1 严格 0 漏 1 token 0 改 
1 通道值 (跨组件 _COLOR_* canonical 颜色 token 0 协调, 玩家扫 3 组件看到颜色 0 视觉组连贯, Run # Pale Resonance 与 ProfileAudit 暖白 0 区分存档健康状态); 
(5) "加新 1 派生字段漏 Stage 5 派生字段 1:1 同步" — 加新 1 派生字段到 ProfileRecentList 5 行 × 7 字段 (e.g. "净/房" = 净/房 派生率, 派生自原 5 字段中的 房/净) 
时漏 `_RECENT_ROW_FIELD_SEP` 6 中点 0 改 0 漏 (Stage 2 1:1 严格), 漏 `_RECENT_ROW_HINT` 7 entry → 8 entry 加 1 entry 0 改 0 漏 
(Stage 5 1:1 严格), 漏 `_build_recent_row_tooltip()` 8 行 → 9 行 拼接 0 改 0 漏 (Stage 5 1:1 严格), 漏 `_format_recent_row_text()` 
format 字符串 7 placeholder → 8 placeholder 0 改 0 漏 (Stage 5 1:1 严格), 漏 t_sec == 0 guard (Stage 5 1:1 严格, 0 触碰 0/0 = nan 
defensive 路径), 玩家扫 5 行看到 0 派生率 inline 0 tooltip 看 0 派生率含义 派生字段视觉组连贯崩塌. 5 类症状根因: 1 panel × 3 组件 × 1:1 视觉组连贯 polish 模式 0 
文档化, polish 期只能"凭记忆"同步 5 件套 (1 3 组件结构 1:1 + 1 中点分隔符 1:1 + 1 7pt 字号 1:1 + 1 _COLOR_* canonical 颜色 token 1:1 + 1 派生字段 
1:1), 1 漏 1 = 1 panel × 3 组件 × 1:1 视觉组连贯 漂动 (3 组件 0 镜像 / 中点分隔符 0 协调 / 字号 0 协调 / 颜色 0 协调 / 派生字段 0 同步).

- **触发场景**: PlayerProfilePanel 1 panel × 3 组件 (ProfileQuickStats 4 段 + ProfileRecentList 5 行 × 7 字段 + ProfileAudit 4 
字段) 1:1 视觉组连贯落地需同步 5 件套: (1) **Stage 1 3 组件 1:1 严格分离** — `pause_menu.tscn` 
PlayerProfilePanel/ProfileMargin/ProfileVBox/ProfileQuickStats + ProfileRecentList + ProfileAudit 3 组件 1:1 严格 (3 组件 0 漏 
0 改 0 重命名 0 反序 0 改 1 字符, T162 + T213 + T215 + T229 跨 4 task 落地 0 漂动), 0 触碰既有 3 组件 0 改 0 漏; (2) **Stage 2 中点分隔符 ` · ` 
1:1 严格分离** — 3 组件共享中点分隔符 `_RECENT_ROW_FIELD_SEP := "  ·  "` 4 字符中点带前后 2 空格 1:1 严格 (3 组件 1:1 严格 0 改 0 漏 0 改 1 字符 0 改 1 
空格 0 改 1 中点, T235 #154 落地 0 触碰 既有 3 组件 0 改 0 漏), 0 触碰既有 3 组件中点分隔符; (3) **Stage 3 7pt 字号 1:1 严格分离** — 3 组件所有 Label 
`add_theme_font_size_override("font_size", 7)` 1:1 严格 (3 组件 1:1 严格 0 改 0 漏 0 改 1 pt, T256 #176 落地时 ProfileAudit 收回 8pt 
→ 7pt 0 触碰 既有 3 组件 0 改 0 漏), 0 触碰既有 3 组件 7pt 字号; (4) **Stage 4 _COLOR_* canonical 颜色 token 1:1 严格分离** — 3 组件共享 
`_COLOR_*` canonical 颜色 token (ProfileQuickStats 4 段 4 调色 hex 字符串 + ProfileRecentList 5 行 2 色 
`_COLOR_RECENT_RUN_LATEST` Amber Voice + `_COLOR_RECENT_RUN_NORMAL` Pale Resonance + ProfileAudit 4 字段 3 状态色 暖白 / 暖黄 / 
暖红 inline literals in `_refresh_profile_audit()`, T213 + T219 + T229 落地时 0 触碰既有 3 组件 0 改 0 漏, Stage 4 1:1 严格 0 改 0 漏 0 
改 1 通道值 0 改 1 hex 0 改 1 字段), 0 触碰既有 3 组件 _COLOR_* canonical 颜色 token; (5) **Stage 5 派生字段 1:1 严格分离** — ProfileRecentList 
5 行 × 7 字段 派生 2 字段 (房/时 + 净/时) from 原 5 字段 (Run # / 房 / 净 / 碎 / 时) 1:1 严格 (T249 #167 落地时 5 字段 → 7 字段 0 触碰既有 5 字段 0 改 0 
漏 0 漏 1 派生字段, Stage 5 1:1 严格 0 改 0 漏 0 改 1 派生率 0 改 1 公式 0 漏 1 派生率). 5 件套 1:1 同步, 1 漏 1 = 1 panel × 3 组件 × 1:1 视觉组连贯 漂动.

- **修复** (跨 23 任务 ~200 轮落地后已知 5 段 canonical 1:1 序列 + 3 组件 1:1 视觉组连贯 0 例外):
  - **T162 (#83) A147 ProfileQuickStats 4 段 起步 (1 panel × 1 组件 1:1 模板)** — 
`PlayerProfilePanel/ProfileMargin/ProfileVBox/ProfileQuickStats` 4 段 `HBoxContainer` 起步 (T162 落地时 1 panel × 1 组件 模板, 4 
段 `_quick_stats_achievement` + `_quick_stats_best_time` + `_quick_stats_longest_room` + `_quick_stats_run_number` 4 
@onready var 0 触碰 0 改 0 漏 1 段), 0 触碰既有 4 段既有字段.
  - **T213 (#133) A179 ProfileQuickStats 4 段 hover tooltip 数据源 (`_QUICK_STATS_HINT` 4 entry 共享 + 
`_build_quick_stats_tooltip()` 函数体)** — `pause_menu.gd` `_QUICK_STATS_HINT` const 4 entry (成就 Glass Cyan #69C7CE / 最佳 
Amber Voice #F2B66E / 最长单房 Muted Violet #65506A / Run # Pale Resonance #B7E6DC) + `_build_quick_stats_tooltip()` 9 行拼接 
(1 header + 4 段 bullet + 4 缩进描述) 1:1 严格落地 (T213 落地时 4 段 tooltip 数据源 0 触碰既有 4 段既有字段 0 改 0 漏 0 漏 1 段 tooltip 同步), 0 触碰既有 
4 段既有字段.
  - **T214 (#134) A180 ProfileQuickStats 4 段独立 hover 联动 (4 段 tween + dim restore)** — `pause_menu.gd` 
`_on_quick_stats_hover_in(segment_id)` 4 段独立 hover tween `Color.WHITE` (0.3s TRANS_QUAD + EASE_OUT) + 
`_on_quick_stats_hover_out(segment_id)` dim restore 0.3s 1:1 严格落地 (T214 落地时 4 段独立 hover 0 触碰既有 4 段既有字段 0 改 0 漏 0 漏 1 段 
hover handler), 0 触碰既有 4 段既有字段.
  - **T215 (#136) A181 ProfileRecentList 5 行独立 hover 反馈 (5 行 tween + default_color save)** — `pause_menu.gd` 
`_on_recent_row_hover_in(idx)` 5 行 tween `Color.WHITE` (0.12s 升级到 T240 #158) + `_on_recent_row_hover_out(idx)` 
default_color restore + `_recent_row_default_color.append(default_color)` 5 行 默认色保存 1:1 严格落地 (T215 落地时 5 行独立 hover 0 
触碰既有 5 行既有字段 0 改 0 漏 0 漏 1 行 hover handler), 0 触碰既有 5 行既有字段.
  - **T216 (#137) A182 ProfileRecentList 5 行 hover tooltip 数据源 (`_RECENT_ROW_HINT` 5 entry 共享 + 
`_build_recent_row_tooltip()` 函数体)** — `pause_menu.gd` `_RECENT_ROW_HINT` const 5 entry (Run # / 房 / 净 / 碎 / 时) + 
`_build_recent_row_tooltip()` 11 行拼接 (1 header + 5 字段 × 2 行/字段) 1:1 严格落地 (T216 落地时 5 字段 tooltip 数据源 0 触碰既有 5 字段既有字段 0 改 
0 漏), 0 触碰既有 5 字段既有字段.
  - **T217 (#138) A183 ProfileQuickStats 4 段 独立 hover 状态字段 (`_quick_stats_hovered` 4 entry dict)** — `pause_menu.gd` 
`_quick_stats_hovered` Dictionary 4 entry (achievement / best_time / longest_room / run_number) + re-entrant guard 1:1 
严格落地 (T217 落地时 4 段独立 hover 状态 0 触碰既有 4 段既有字段 0 改 0 漏), 0 触碰既有 4 段既有字段.
  - **T219 (#141) A185 ProfileRecentList 5 行 alpha 渐变 (5 步等差梯度 1.0/0.875/0.75/0.625/0.5)** — `pause_menu.gd` 
`_RECENT_ROW_ALPHA_MAX` / `_RECENT_ROW_ALPHA_MIN` const 1.0/0.5 + 5 步等差梯度 `_RECENT_ROW_ALPHA_MAX - i * alpha_step` 1:1 
严格落地 (T219 落地时 5 行 alpha 渐变 0 触碰既有 5 行既有字段 0 改 0 漏 0 漏 1 行 alpha 公式), 0 触碰既有 5 行既有字段.
  - **T225 (#147) A186 ProfileQuickStats 4 段 hover 提亮 tween 引用 (1 个全局 + 4 段共享)** — `pause_menu.gd` 
`_quick_stats_font_color_tween` Tween 1 个全局 + 4 段 hover 0.3s 共享 tween 1:1 严格落地 (T225 落地时 4 段 hover tween 0 触碰既有 4 段既有字段 
0 改 0 漏 0 漏 1 段 tween 引用), 0 触碰既有 4 段既有字段.
  - **T226 (#145) A187 AchievementGrid locked slot 解锁进度 alpha lerp + 跨面板 `_alpha_base` Dictionary 双源 polish 模式** — 
`pause_menu.gd` `_alpha_base` Dictionary 共享 ProfileRecentList 5 行 + AchievementGrid 11 slot (T226 落地时跨面板 _alpha_base 0 
触碰既有 5 行 + 11 slot 既有字段 0 改 0 漏 0 漏 1 panel), 0 触碰既有 3 组件既有字段.
  - **T229 (#149) A190 ProfileAudit 4 字段 1 行 (存档 5 slot 巡检)** — `pause_menu.gd` `_refresh_profile_audit()` 4 字段 1 行紧凑 
"存档 %d ok · %d 损坏 · %d 漂移 · %d 空" + SaveSystem.audit_save_slots() 公共接口 1:1 严格落地 (T229 落地时 4 字段 1 行 0 触碰既有 4 段 + 5 行既有字段 
0 改 0 漏 0 漏 1 组件 0 漏 1 字段), 0 触碰既有 2 组件既有字段.
  - **T231 (#151) A192 ProfileRecentList 5 行 base alpha 字典 (`_recent_row_hover_alpha_base` row → base_alpha)** — 
`pause_menu.gd` `_recent_row_hover_alpha_base` Dictionary row → base_alpha 1:1 严格落地 (T231 落地时 5 行 base alpha 0 触碰既有 5 
行既有字段 0 改 0 漏 0 漏 1 行 base alpha 字典), 0 触碰既有 5 行既有字段.
  - **T234 (#153) A195 ProfileRecentList 5 行 row text 末尾 1 个 " ↗" 字符 (_RECENT_ROW_TIP_INDICATOR)** — `pause_menu.gd` 
`_RECENT_ROW_TIP_INDICATOR` const " ↗" 2 字符 + 5 行 row text 末尾 1:1 严格落地 (T234 落地时 5 行 tip indicator 0 触碰既有 5 行既有字段 0 改 0 
漏 0 漏 1 字符), 0 触碰既有 5 行既有字段.
  - **T235 (#154) A196 中点分隔符 `  ·  ` 1:1 严格 (3 组件共享 _RECENT_ROW_FIELD_SEP)** — `pause_menu.gd` `_RECENT_ROW_FIELD_SEP` 
const "  ·  " 4 字符中点带前后 2 空格 + 3 组件 (ProfileQuickStats 4 段 + ProfileRecentList 5 行 + ProfileAudit 4 字段) 1:1 严格共享 1:1 
严格落地 (T235 落地时 3 组件中点分隔符 0 触碰既有 3 组件既有字段 0 改 0 漏 0 漏 1 边), 0 触碰既有 3 组件既有字段.
  - **T240 (#158) A198 ProfileRecentList 5 行 hover font_color 提亮 0.12s 渐变 (T215 snap 升级 + 与 T231 alpha boost 0.12s 
同步)** — `pause_menu.gd` `_RECENT_ROW_FONT_COLOR_FADE_DURATION` const 0.12s + `_recent_row_font_color_tween` Tween 5 行共享 
+ `tween_property(row, "theme_override_colors/font_color", Color.WHITE, 0.12s)` 1:1 严格落地 (T240 落地时 5 行 tween 0.12s 0 
触碰既有 5 行既有字段 0 改 0 漏 0 漏 1 边), 0 触碰既有 5 行既有字段.
  - **T249 (#167) A205 ProfileRecentList 5 行 row 文本 5 字段 → 7 字段扩展 (同步 _RECENT_ROW_HINT tooltip)** — `pause_menu.gd` 
`_RECENT_ROW_HINT` 5 entry → 7 entry 扩展 (房/时 + 净/时 2 派生率) + `_format_recent_row_text()` 7 placeholder + 
`_build_recent_row_tooltip()` 5 字段 → 7 字段 bullet 1:1 严格落地 (T249 落地时 5 字段 → 7 字段 0 触碰既有 5 字段既有字段 0 改 0 漏 0 漏 1 字段 0 漏 1 
派生率), 0 触碰既有 5 字段既有字段.
  - **T256 (#176) A211 §9.6.6 ProfileRecentList 5 行 row 文本 5 字段 → 7 字段 format 字符串扩展模式 polish 文档化** — `CONTRIBUTING.md` 
§9.6.6 5 行 row 文本 5 字段 → 7 字段 format 字符串扩展模式 1:1 严格落地 (T256 落地时 §9.6.6 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 
任何子段.
  - **T257 (#177) A212 §9.6.7 ProfileRecentList 5 行 row hover feedback 三件套 (font_color + alpha boost + tooltip) polish 
文档化** — `CONTRIBUTING.md` §9.6.7 5 行 row hover feedback 三件套 1:1 严格落地 (T257 落地时 §9.6.7 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 
漏), 0 触碰既有 §9.6 任何子段.
  - **T258 (#178) A213 §9.6.8 ProfileQuickStats 4 段独立 hover 联动 polish 文档化** — `CONTRIBUTING.md` §9.6.8 4 段独立 hover 联动 
1:1 严格落地 (T258 落地时 §9.6.8 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - **T259 (#179) A214 §9.6.9 AchievementGrid locked slot 解锁进度 alpha lerp + ProfileRecentList 5 行 alpha 渐变 base + 跨面板 
`_alpha_base` Dictionary 双源 polish 文档化** — `CONTRIBUTING.md` §9.6.9 跨面板 _alpha_base Dictionary 双源 1:1 严格落地 (T259 落地时 
§9.6.9 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - **T260 (#181) A215 §9.6.10 「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 polish 文档化** — `CONTRIBUTING.md` §9.6.10 双层 
hover 互补 1:1 严格落地 (T260 落地时 §9.6.10 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - **T261 (#182) A216 §9.6.11 PlayerProfilePanel 4 段 ↔ 5 行 ↔ 4 字段 ↔ 1 panel 1:1 视觉组连贯 polish 文档化** — `CONTRIBUTING.md` 
§9.6.11 1 panel × 3 组件 × 1:1 视觉组连贯 1:1 严格落地 (T261 落地时 §9.6.11 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - **T262 (#183) A217 §9.6.12 ProfileAudit 4 字段 (ok/损坏/漂移/空) ↔ SaveSystem.audit_save_slots() 公共接口 1:1 polish 文档化** — 
`CONTRIBUTING.md` §9.6.12 ProfileAudit 4 字段 ↔ SaveSystem 公共接口 1:1 1:1 严格落地 (T262 落地时 §9.6.12 文档 0 触碰既有 §9.6 任何子段任何 1 字符 
0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - **T263 (#184) A218 §9.6.13 ProfileQuickStats 4 段 ↔ ProfileRecentList 5 行 ↔ ProfileAudit 4 字段 (3 组件 跨组件 
`_QUICK_STATS_HINT` / `_RECENT_ROW_HINT` / inline format 数据源 + 7pt 字号 + 中点分隔符 1:1) polish 文档化** — `CONTRIBUTING.md` 
§9.6.13 3 组件 跨组件 数据源 + 字号 + 分隔符 1:1 1:1 严格落地 (T263 落地时 §9.6.13 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - 跨 23 任务 ~200 轮落地后 0 漂动, 1 panel × 3 组件 × 1:1 视觉组连贯 5 段 canonical 1:1 序列 100% 闭环, 5 段 1:1 严格分离 (Stage 1 3 组件 1:1 严格 
0 漏 0 改 0 重命名 0 反序 + Stage 2 中点分隔符 1:1 严格 0 漏 0 改 0 改 1 字符 0 改 1 空格 0 改 1 中点 + Stage 3 7pt 字号 1:1 严格 0 漏 0 改 0 改 1 pt + 
Stage 4 _COLOR_* canonical 颜色 token 1:1 严格 0 漏 0 改 0 改 1 通道值 0 改 1 hex 0 改 1 字段 + Stage 5 派生字段 1:1 严格 0 漏 0 改 0 改 1 派生率 
0 改 1 公式 0 漏 1 派生率), 3 组件 调色 1:1 严格分离 (ProfileQuickStats 4 段 4 调色 hex 字符串 (Glass Cyan + Amber Voice + Muted Violet + 
Pale Resonance) + ProfileRecentList 5 行 2 色 `_COLOR_RECENT_RUN_LATEST` Amber Voice + `_COLOR_RECENT_RUN_NORMAL` Pale 
Resonance + ProfileAudit 4 字段 3 状态色 暖白 / 暖黄 / 暖红 inline literals in `_refresh_profile_audit()` = 4 + 2 + 3 = 9 颜色 token 
0 漏 0 重叠 0 互混 0 复用 0 共享). 23 任务 0 后续 主要 pre-existing 风险.

- **预防**:
  1. 任何 polish 期给 "加新 1 段" 或 "加新 1 字段" 或 "加新 1 组件" 或 "修 1 段颜色" 或 "修 1 字段格式" 任务加新段 / 加新字段 / 加新组件 / 改颜色 / 改格式时**必须**严格按 5 
段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 改 3 组件** (T162 + T213 + T215 + T229 落地后 3 组件 
(ProfileQuickStats + ProfileRecentList + ProfileAudit) 1:1 严格, 0 改 0 重命名 0 漏 1 组件 0 反序, 改 1 组件名 = `pause_menu.gd` 
`_profile_quick_stats` / `_profile_recent_list` / `_profile_audit` 任何 1 @onready var load 0 找到节点 0 走 fallback 0 
视觉组连贯崩塌), **Stage 2 0 改中点分隔符** (T235 #154 落地后 3 组件共享 `_RECENT_ROW_FIELD_SEP` "  ·  " 4 字符 1:1 严格, 0 改 0 漏 0 改 1 字符 0 改 
1 空格 0 改 1 中点, 改 1 字符 = 3 组件中点分隔符 0 协调 跨组件视觉组连贯崩塌), **Stage 3 0 改 7pt 字号** (T256 #176 落地后 3 组件所有 Label `font_size = 7` 
1:1 严格, 0 改 0 漏 0 改 1 pt, 改 1 pt = 3 组件字号 0 协调 跨组件视觉组连贯崩塌), **Stage 4 0 改 _COLOR_* canonical 颜色 token** (T213 + T219 + 
T229 落地后 3 组件 _COLOR_* canonical 颜色 token 1:1 严格, 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 通道值 / 1 hex = 3 组件颜色 0 协调 跨组件视觉组连贯崩塌), 
**Stage 5 0 改 派生字段 1:1 同步** (T249 #167 落地后 ProfileRecentList 5 行 × 7 字段 派生 2 字段 1:1 严格, 0 改 0 漏 0 改 1 派生率 0 改 1 公式 0 漏 
1 派生率, 改 1 派生率 / 1 公式 / 漏 1 派生率 = 派生字段视觉组连贯崩塌). 任何"加新 1 段"必须**先**按 Stage 1 3 组件 1:1 加 1 段 (Stage 1 1:1 严格, 0 改既有 3 组件 0 
改 1 字符), **再**按 Stage 2 中点分隔符 1:1 复制 (Stage 2 1:1 严格, 0 改既有 3 组件 0 改 1 字符), **再**按 Stage 3 7pt 字号 1:1 镜像 (Stage 3 1:1 
严格, 0 改既有 3 组件 0 改 1 pt), **再**按 Stage 4 _COLOR_* canonical 颜色 token 加 1 entry (Stage 4 1:1 严格, 0 改既有 3 组件 0 漏 1 token),
 **再**按 Stage 5 派生字段 1:1 同步 (Stage 5 1:1 严格, 0 改既有 5 字段 0 漏 1 派生率). 任何"加新 1 字段"必须**先**按 Stage 1 3 组件 1:1 加 1 字段 (Stage 
1 1:1 严格), **再**按 Stage 2 中点分隔符 1:1 加 1 间隔 (Stage 2 1:1 严格, 0 漏 1 间隔 0 改 1 字符), **再**按 Stage 3 7pt 字号 1:1 镜像 (Stage 3 
1:1 严格, 0 改既有 3 组件 0 改 1 pt), **再**按 Stage 4 _COLOR_* canonical 颜色 token 加 1 entry (Stage 4 1:1 严格), **再**按 Stage 5 
派生字段 1:1 加 1 派生率 (Stage 5 1:1 严格, 0 改既有 5 字段 0 漏 1 派生率 0 改 1 公式).
  2. **5 段序列 0 触碰边界** — `STYLE_GUIDE.md` 任何 const / var 0 改 0 删 0 重排 (5 段 0 触碰边界), 任何"修 1 段颜色"polish 走 Stage 4 1:1 严格 
(改 _COLOR_* canonical 颜色 token 既有 entry 时**必须**同步改 `pause_menu.gd` `_QUICK_STATS_HINT` / `_RECENT_ROW_HINT` 6+7=13 
entry color 字段 + 跨段镜像 §9.6.4 调色六元组 6 verb entry, 0 改 1 通道值 0 漏 1 跨段); 任何"修 1 字段格式"polish 走 Stage 2 1:1 严格 (改 
_RECENT_ROW_FIELD_SEP 既有 4 字符时**必须**同步改 3 组件既有 4 字符中点分隔符, 0 改 1 字符 0 漏 1 跨组件).
  3. **3 组件 0 改 0 漏 0 改 1 字段 0 改 1 节点** (T162 + T213 + T215 + T229 落地后 0 触碰边界) — `pause_menu.gd` 3 组件 
(ProfileQuickStats + ProfileRecentList + ProfileAudit) 1:1 严格分离 0 漏 0 改 0 重命名 0 反序 0 改 1 字符 0 改 1 节点, 改 1 组件名 / 漏 1 组件 
= 1 panel × 3 组件 × 1:1 视觉组连贯 漂动, source-grep 验证 `pause_menu.tscn` 
PlayerProfilePanel/ProfileMargin/ProfileVBox/ProfileQuickStats + ProfileRecentList + ProfileAudit 3 组件 0 漏 0 改 1 字符 0 
反序.
  4. **3 组件 _COLOR_* canonical 颜色 token 0 改 1 通道值 0 改 1 hex** (T213 + T219 + T229 落地后 0 触碰边界) — `pause_menu.gd` 3 组件 
_COLOR_* canonical 颜色 token (ProfileQuickStats 4 段 4 调色 hex 字符串 + ProfileRecentList 5 行 2 色 `_COLOR_RECENT_RUN_LATEST` 
/ `_COLOR_RECENT_RUN_NORMAL` + ProfileAudit 4 字段 3 状态色 暖白 / 暖黄 / 暖红 inline literals in `_refresh_profile_audit()` = 4 + 
2 + 3 = 9 颜色 token) 1:1 严格分离 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 通道值 / 1 hex = 3 组件颜色 0 协调 跨组件视觉组连贯 漂动, source-grep 验证 3 组件 
_COLOR_* 2 颜色 token + `_QUICK_STATS_HINT` 4 entry color 字段 + `_RECENT_ROW_HINT` 0 entry color 字段 (无) + 
`_refresh_profile_audit()` 3 状态色 inline literals 0 漏 0 改 1 字段 0 改 1 通道值.
  5. **中点分隔符 0 改 0 漏 0 改 1 字符 0 改 1 空格 0 改 1 中点** (T235 #154 落地后 0 触碰边界) — `pause_menu.gd` `_RECENT_ROW_FIELD_SEP` 
const "  ·  " 4 字符中点带前后 2 空格 1:1 严格 (3 组件共享 0 漏 0 改 0 改 1 字符 0 改 1 空格 0 改 1 中点, 0 触碰既有 3 组件既有字段), 改 1 字符 / 漏 1 字符 = 3 
组件中点分隔符 0 协调 跨组件视觉组连贯 漂动, source-grep 验证 3 组件中点分隔符 0 漏 0 改 1 字符 0 改 1 空格 0 改 1 中点 0 改 1 字段.
  6. **7pt 字号 0 改 0 漏 0 改 1 pt** (T256 #176 落地后 0 触碰边界) — `pause_menu.gd` 3 组件所有 Label 
`add_theme_font_size_override("font_size", 7)` 1:1 严格 (3 组件 0 漏 0 改 0 改 1 pt, 0 触碰既有 3 组件既有字段), 改 1 pt = 3 组件字号 0 协调 
跨组件视觉组连贯 漂动, source-grep 验证 3 组件 font_size = 7 0 漏 0 改 1 pt 0 改 1 字段 0 改 1 字符.
  7. **派生字段 0 改 0 漏 0 改 1 派生率 0 改 1 公式 0 漏 1 派生率** (T249 #167 落地后 0 触碰边界) — `pause_menu.gd` ProfileRecentList 5 行 × 7 
字段 派生 2 字段 (房/时 + 净/时) from 原 5 字段 (Run # / 房 / 净 / 碎 / 时) 1:1 严格 (T249 (#167) 落地时 5 字段 → 7 字段 0 触碰既有 5 字段既有字段 0 改 0 漏 
0 漏 1 派生字段 0 漏 1 派生率 0 改 1 派生率 0 改 1 公式), 改 1 派生率 / 1 公式 / 漏 1 派生率 = 派生字段视觉组连贯 漂动, source-grep 验证 `_RECENT_ROW_HINT` 7 
entry + `_build_recent_row_tooltip()` 7 字段 bullet + `_format_recent_row_text()` 7 placeholder + t_sec == 0 guard 0 漏 0 
改 1 字段 0 改 1 字符 0 改 1 公式.
  8. 已知 drift risk: 1 panel × 3 组件 × 1:1 视觉组连贯 5 段 canonical 1:1 序列 **可能**漏 1 段 1:1 镜像 (e.g. 加新 1 段到 ProfileQuickStats 
时漏 ProfileRecentList + ProfileAudit 同步 0 镜像, 3 组件 0 协调 跨组件视觉组连贯崩塌; 修 1 段颜色 0 改 1 hex 触发 3 组件颜色 0 协调 跨组件视觉组连贯崩塌; 改 
_RECENT_ROW_FIELD_SEP 既有 4 字符 触发 3 组件中点分隔符 0 协调 跨组件视觉组连贯崩塌). 监控建议: 任何"未来给 1 panel × 3 组件 加新段 / 加新字段 / 加新组件 / 改颜色 / 
改格式"必须**先**在 5 段序列 (Stage 1 3 组件 + Stage 2 中点分隔符 + Stage 3 7pt 字号 + Stage 4 _COLOR_* canonical 颜色 token + Stage 5 派生字段) 
1:1 镜像 0 漏 1 段. T263 (#184) 5 段序列 1:1 镜像落地后任何新段 / 新字段 / 新组件 / 新颜色 / 新格式 加 5 段同步是 §9.6.27 5 段序列的"母集" (1 panel × 3 组件 × 
1:1 5 段 = 15 元素 1:1 严格, §9.6.27 5 段 3 组件完整闭环, 3 组件 × 5 段 = 15 元素 1:1 严格, 含 3 组件 调色 9 颜色 token 0 漏 0 改 + 派生 2 字段 0 漏 0 
改). 另 1 类已知 drift: Stage 4 _COLOR_* canonical 颜色 token 0 改 1 通道值 (e.g. 改 `_refresh_profile_audit()` 暖红 inline literal 
(0.85, 0.45, 0.4) 为 (0.80, 0.45, 0.4) 偏红 0 触碰既有 2 状态色, 或改 `_COLOR_RECENT_RUN_LATEST` Amber Voice (0.949, 0.714, 0.431, 
1.0) 为 (0.90, 0.714, 0.431, 1.0) 偏淡橙 0 触碰既有 1 token), 后续 polish 期若加新组件必须**先**用 1:1 镜像 _COLOR_* canonical 颜色 token 模板, 0 
改既有 9 颜色 token.

**0 副作用**: T162 / T213 / T214 / T215 / T216 / T217 / T219 / T225 / T226 / T229 / T231 / T234 / T235 / T240 / T249 任何 
const / var 0 触碰 (3 组件 调色 0 改 0 删 0 重排 + 中点分隔符 0 改 0 删 0 重排 + 7pt 字号 0 改 0 删 0 重排 + _COLOR_* canonical 颜色 token 0 改 0 删 
0 重排 + 派生 2 字段 0 改 0 删 0 重排); 0 触碰 §9.6.6 ProfileRecentList 5 行 row 文本 5→7 字段 format 字符串扩展模式 (T283 0 改 §9.6.6 任何 1 字符); 
0 触碰 §9.6.7 ProfileRecentList 5 行 row hover feedback 三件套 (T283 0 改 §9.6.7 任何 1 字符); 0 触碰 §9.6.8 ProfileQuickStats 4 段独立 
hover 联动 (T283 0 改 §9.6.8 任何 1 字符); 0 触碰 §9.6.9 AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` Dictionary 双源 
(T283 0 改 §9.6.9 任何 1 字符); 0 触碰 §9.6.10 「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (T283 0 改 §9.6.10 任何 1 字符); 0 触碰 
§9.6.15 SaveSystem audit_save_slots (T283 0 改 §9.6.15 任何 1 字符); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T283 0 改 §9.6.16 
任何 1 字符); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T283 0 改 §9.6.17 任何 1 字符); 0 触碰 §9.6.18 16 件套 verb ability base (T283 0 
改 §9.6.18 任何 1 字符); 0 触碰 §9.6.19 7 件套 windup VFX base (T283 0 改 §9.6.19 任何 1 字符); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 
transition flow (T283 0 改 §9.6.20 任何 1 字符); 0 触碰 §9.6.21 8 段成就序列 (T283 0 改 §9.6.21 任何 1 字符); 0 触碰 §9.6.22 6 verb VFX 
视觉组 5 段 (T283 0 改 §9.6.22 任何 1 字符); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T283 0 改 §9.6.23 任何 1 字符); 0 触碰 §9.6.24 HUD 6 verb 
× 6 通道 = 36 通道 5 段序列 (T283 0 改 §9.6.24 任何 1 字符); 0 触碰 §9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 5 段序列 (T283 0 改 §9.6.25 任何 1 
字符); 0 触碰 §9.6.26 6 verb icon 视觉组 5 段 canonical 1:1 序列 (T283 0 改 §9.6.26 任何 1 字符); 0 触碰 check_smoke_consistency.sh 
(T283 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 
.git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, 0 触碰 `pause_menu.gd` 3 组件 任何 1 字段, 0 触碰 
`pause_menu.tscn` PlayerProfilePanel/ProfileMargin/ProfileVBox 3 组件任何 1 节点); 0 行为变化, 1 文档 1 段 (§9.6.27 ~50 行) + 1 smoke 
test (T283 24+ 断言).

**与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
§9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 关系** — §9.6.6 覆盖 ProfileRecentList 5 行 row 文本 5 字段 → 7 字段 format 
字符串扩展模式 (单一组件 row 文本扩展); §9.6.7 覆盖 ProfileRecentList 5 行 row hover feedback 三件套 (单一组件 row hover feedback); §9.6.8 覆盖 
ProfileQuickStats 4 段独立 hover 联动 (单一组件 4 段 hover); §9.6.9 覆盖 AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` 
Dictionary 双源 (2 面板跨面板 _alpha_base); §9.6.10 覆盖「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (2 层 hover 互补); §9.6.15 覆盖 
SaveSystem audit_save_slots (1 个公共接口 polish); §9.6.16 覆盖 6 verb hit handler 5 件套 (6 verb hit handler); §9.6.17 覆盖 6 
verb signal 桥接 5+1 件套 (6 verb signal 桥接); §9.6.18 覆盖 16 件套 verb ability base (6 verb ability base); §9.6.19 覆盖 7 件套 
windup VFX base (6 verb windup VFX base); §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow (跨房间 transition); §9.6.21 覆盖 6 
verb 成就系统 闭环 8 段 (6 verb 成就系统); §9.6.22 覆盖 6 verb VFX 视觉组 5 段 (6 verb VFX 视觉组); §9.6.23 覆盖 7 桶 prewarm 5+1+1 (7 桶 
prewarm); §9.6.24 覆盖 HUD 6 verb × 6 通道 = 36 通道 5 段 (HUD 36 通道); §9.6.25 覆盖 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 (6 verb 视觉组 
tooltip); §9.6.26 覆盖 6 verb icon 视觉组 5 段 (6 verb icon 视觉组). §9.6.27 覆盖 `pause_menu.gd` PlayerProfilePanel 1 panel × 3 
组件 × 1:1 视觉组连贯 (5 段 canonical 1:1 序列: Stage 1 3 组件 + Stage 2 中点分隔符 + Stage 3 7pt 字号 + Stage 4 _COLOR_* canonical 颜色 
token + Stage 5 派生字段). §9.6.6 关注 "ProfileRecentList 5 行 row 文本 5 字段 → 7 字段 format 字符串扩展模式 (单一组件 row 文本扩展, 5 字段 → 7 字段 
1:1 严格)", §9.6.7 关注 "ProfileRecentList 5 行 row hover feedback 三件套 (font_color + alpha boost + tooltip, 1 组件 3 反馈 1:1 
严格)", §9.6.8 关注 "ProfileQuickStats 4 段独立 hover 联动 (4 段 tween + dim restore, 1 组件 4 段 hover 1:1 严格)", §9.6.9 关注 
"AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` Dictionary 双源 (2 面板跨面板 _alpha_base 1:1 严格)", §9.6.10 关注 "「hover 
静态高亮层 + tooltip 数据源层」双层 hover 互补 (2 层 hover 互补 1:1 严格)", §9.6.15 关注 "SaveSystem audit_save_slots (1 个公共接口 polish, audit 
4 字段 1:1 严格)", §9.6.16 关注 "6 verb hit handler 5 件套 (6 verb hit handler 1:1 严格)", §9.6.17 关注 "6 verb signal 桥接 5+1 件套 (6 
verb signal 桥接 1:1 严格)", §9.6.18 关注 "16 件套 verb ability base (6 verb ability base 1:1 严格)", §9.6.19 关注 "7 件套 windup VFX 
base (6 verb windup VFX base 1:1 严格)", §9.6.20 关注 "Hub ↔ archive 跨房间 transition flow (跨房间 transition 1:1 严格)", §9.6.21 
关注 "6 verb 成就系统 闭环契约 (8 段 1:1 严格, 含 PNG 双路径)", §9.6.22 关注 "6 verb VFX 视觉组 5 段 1:1 严格分离契约", §9.6.23 关注 "7 桶 prewarm 
5+1+1 (1:1 严格分离契约)", §9.6.24 关注 "HUD 6 verb × 6 通道 = 36 通道 5 段 1:1 严格分离契约", §9.6.25 关注 "6 verb 视觉组连贯 tooltip 8 行拼接 5 段 
1:1 严格分离契约", §9.6.26 关注 "6 verb icon 视觉组 5 段 1:1 严格分离契约 (Stage 1 family 路径 + Stage 2 disc + outer ring + Stage 3 调色 + 
Stage 4 几何 + Stage 5 双 export + 3 verb achievement 双路径)". §9.6.27 是 17 套 polish 模式**唯一**关注 "PlayerProfilePanel 1 panel 
× 3 组件 × 1:1 视觉组连贯 (5 段 canonical 1:1 序列: Stage 1 3 组件 + Stage 2 中点分隔符 + Stage 3 7pt 字号 + Stage 4 _COLOR_* canonical 颜色 
token + Stage 5 派生字段)" 的 1 套, 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / 
§9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 16 套 ProfileRecentList 5 行 / 
ProfileQuickStats 4 段 / AchievementGrid / 6 verb / SaveSystem / 跨房间 transition 0 互混 0 复用 0 共享. 任何"加新 1 段"或"加新 1 字段"或"加新 
1 组件"或"修 1 段颜色"或"修 1 字段格式"必须**先**按 §9.6.27 Stage 1 3 组件 1:1 加 1 组件 (Stage 1 1:1 严格), **再**按 §9.6.27 Stage 2 中点分隔符 1:1 
复制 (Stage 2 1:1 严格), **再**按 §9.6.27 Stage 3 7pt 字号 1:1 镜像 (Stage 3 1:1 严格), **再**按 §9.6.27 Stage 4 _COLOR_* canonical 
颜色 token 加 1 entry (Stage 4 1:1 严格), **再**按 §9.6.27 Stage 5 派生字段 1:1 同步 (Stage 5 1:1 严格). §9.6.6 / §9.6.7 与 §9.6.27 0 
互混 0 复用 0 共享: §9.6.6 / §9.6.7 关注 "ProfileRecentList 5 行 row 文本 / hover 反馈 1:1 严格分离契约 (单一组件 row 文本扩展 / row hover 
feedback, 1 组件 1 panel 0 涉及 3 组件 1:1)" (与 3 组件 0 相关), §9.6.27 关注 "PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 (5 段 
canonical 1:1 序列, 3 组件 1:1 严格分离)" (与 单一组件 0 相关, 5 段序列 1:1 严格 镜像 3 组件既有 1:1 严格分离). §9.6.8 与 §9.6.27 0 互混 0 复用 0 共享: 
§9.6.8 关注 "ProfileQuickStats 4 段独立 hover 联动 1:1 严格分离契约 (单一组件 4 段 hover, 1 组件 1 panel 0 涉及 3 组件 1:1)" (与 3 组件 0 相关), 
§9.6.27 关注 "1 panel × 3 组件 × 1:1 视觉组连贯 (3 组件 1:1 严格分离, 含 4 段 hover 但 1 段 hover 仅是 4 段中 1 段, Stage 1 3 组件 1:1 严格 0 漏 1 
边)" (与 单一组件 hover 0 相关, 5 段序列 1:1 严格 镜像 3 组件既有 1:1 严格分离). §9.6.9 / §9.6.10 与 §9.6.27 0 互混 0 复用 0 共享: §9.6.9 / §9.6.10 
关注 "AchievementGrid + ProfileRecentList 跨面板 / 双层 hover 互补 1:1 严格分离契约 (2 面板 / 2 层 hover, 1 组件 + 1 面板 / 1 组件 + 1 层 0 涉及 3 
组件 1 panel 1:1)" (与 3 组件 1 panel 0 相关), §9.6.27 关注 "1 panel × 3 组件 × 1:1 视觉组连贯 (3 组件 1:1 严格分离, 1 panel 内 3 组件 0 漏 1 边)" 
(与 2 面板 / 2 层 hover 0 相关, 5 段序列 1:1 严格 镜像 3 组件既有 1:1 严格分离). §9.6.15 与 §9.6.27 0 互混 0 复用 0 共享: §9.6.15 关注 "SaveSystem 
audit_save_slots 1:1 严格分离契约 (1 个公共接口 polish, 1 接口 4 字段 1:1 严格)" (与 3 组件 0 相关, audit 4 字段仅是 §9.6.15 1 段 4 字段), §9.6.27 
关注 "1 panel × 3 组件 × 1:1 视觉组连贯 (3 组件 1:1 严格分离, 含 ProfileAudit 4 字段但 4 字段仅是 3 组件中 1 组件, Stage 1 3 组件 1:1 严格 0 漏 1 边)" (与 
1 接口 0 相关, 5 段序列 1:1 严格 镜像 3 组件既有 1:1 严格分离). §9.6.16-§9.6.26 与 §9.6.27 0 互混 0 复用 0 共享: §9.6.16-§9.6.26 关注 "6 verb 视觉 / 
调色 / 几何 / 共享契约 / 跨段 1:1 严格分离契约 (6 verb 视觉组连贯, 6 verb 0 涉及 PlayerProfilePanel 3 组件)" (与 1 panel × 3 组件 0 相关, 6 verb 0 触碰 
ProfileQuickStats 4 段 + ProfileRecentList 5 行 + ProfileAudit 4 字段), §9.6.27 关注 "1 panel × 3 组件 × 1:1 视觉组连贯 (3 组件 1:1 
严格分离, 1 panel 内 3 组件 0 漏 1 边)" (与 6 verb 0 相关, 5 段序列 1:1 严格 镜像 3 组件既有 1:1 严格分离). §9.6.27 是 17 套 polish 模式**唯一**关注 
"PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 (5 段 canonical 1:1 序列: Stage 1 3 组件 + Stage 2 中点分隔符 + Stage 3 7pt 字号 + 
Stage 4 _COLOR_* canonical 颜色 token + Stage 5 派生字段)" 的 1 套, 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / 
§9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 16 套 
ProfileRecentList 5 行 / ProfileQuickStats 4 段 / AchievementGrid / 6 verb / SaveSystem / 跨房间 transition 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.27 5 段序列走 §9.1 9 步落地的 "const" (3) 1 步 (`pause_menu.gd` `_QUICK_STATS_HINT` 
const 4 entry + `_RECENT_ROW_HINT` const 7 entry + `_RECENT_ROW_FIELD_SEP` const "  ·  " 4 字符中点分隔符 + 
`_RECENT_ROW_TIP_INDICATOR` const " ↗" 2 字符 + `_RECENT_ROW_FONT_COLOR_FADE_DURATION` const 0.12s + 
`_RECENT_ROW_ALPHA_MAX` / `_RECENT_ROW_ALPHA_MIN` const 1.0 / 0.5 + `_COLOR_*` canonical 颜色 token 2 token + 
`_COLOR_ZERO_STAT` 1 token + `_QUICK_STATS_HINT` 4 entry color 字段 + `_refresh_profile_audit()` 3 状态色 inline literals = 
2+1+4+3 = 10 颜色 reference), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "input" (4) + "shop" (5) + "icon" (6) + "vfx" 
(7) + "audio" (8) + "HUD 冷光勾边" (9) 8 步 (1 panel × 3 组件 × 1:1 视觉组连贯 0 走 verb enum + ability 子类 + input map + shop 系统 + 
icon PNG + VFX 视觉组 + audio 副作用 + HUD 渲染). 任何"加新 1 段"或"加新 1 字段"或"加新 1 组件"必须**先**按 §9.1 9 步落地 (含 "const" (3) 步), **再**按 
§9.6.27 5 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 任何"修 1 段颜色"必须**先**在 `pause_menu.gd` `_COLOR_*` canonical 颜色 token 改 1 通道值 
(Stage 4 1:1 严格, 0 漏 1 边 0 改 1 通道值 0 改 1 hex), **再**在 `_QUICK_STATS_HINT` 4 entry color 字段 1:1 镜像 (Stage 4 1:1 严格, 0 漏 
1 通道 0 改 1 通道值), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) 步 (1 panel × 3 组件 × 1:1 视觉组连贯 0 触碰 verb enum + ability 子类). 
任何"加新 1 组件"必须**先**在 `pause_menu.tscn` PlayerProfilePanel/ProfileMargin/ProfileVBox 加 1 节点 (Stage 1 1:1 严格), **再**在 
`pause_menu.gd` 4 + 35 + 4 = 43 element 加 1 element (Stage 1 1:1 严格, 0 漏 1 element 0 改 1 字段), 0 触碰 §9.1 9 步任何 "icon" 
(6) 步既有 6 verb icon PNG 双路径. 任何"加新 1 派生字段"必须**先**在 `_RECENT_ROW_HINT` 7 entry → 8 entry 加 1 entry (Stage 5 1:1 严格), 
**再**在 `_build_recent_row_tooltip()` 7 字段 bullet → 8 字段 bullet 加 1 bullet (Stage 5 1:1 严格), **再**在 
`_format_recent_row_text()` 7 placeholder → 8 placeholder 加 1 placeholder (Stage 5 1:1 严格), 0 触碰 §9.6.6 / §9.6.7 既有 5 
字段 → 7 字段既有 0 改 0 漏 1 字段. 任何"加新 1 跨段镜像"必须**先**在 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 6 跨段加 1 镜像 (5 段序列 
1:1 严格, 0 漏 1 跨段 0 改 1 字段), 0 触碰 §9.1 9 步任何 const / var / 节点 (5 段序列仅在 `pause_menu.gd` `_QUICK_STATS_HINT` / 
`_RECENT_ROW_HINT` / `_RECENT_ROW_FIELD_SEP` / `_RECENT_ROW_TIP_INDICATOR` / `_RECENT_ROW_FONT_COLOR_FADE_DURATION` / 
`_RECENT_ROW_ALPHA_MAX` / `_RECENT_ROW_ALPHA_MIN` / `_COLOR_*` canonical 颜色 token + `pause_menu.tscn` 3 组件 节点, 5 verb 
lazy-init 守卫 0 删 0 改, 含 1 verb Whisper T245 #162 接入完成).
