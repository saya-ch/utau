# Polish Patterns 9.6.28–28

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.28–9.6.28，共 1 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.28-28.md

<!-- shard 28-28 共 1 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.28 smoke test ITERATION_COUNT 跨迭代 0 漂移 + 段边界 find 跨迭代稳定 polish 模式 (FIX-#200-1/2/3/4 + FIX-#205-1/2/3 跨 7 
brittle 修复 ~5 轮落地) 文档化

> §9.6.6-§9.6.27 17 套 polish 模式均落在游戏代码 (e.g. `pause_menu.gd` `_QUICK_STATS_HINT` / `_RECENT_ROW_HINT` / `_COLOR_*` 
> canonical 颜色 token / 5 派生字段, `_verb_ability_base.gd` 共享契约, Hub ↔ archive 跨房间 transition flow, 6 verb VFX 视觉组 5 段 
> canonical 1:1 序列, 7 桶 prewarm 5+1+1, 6 verb HUD 6 行 6 色色域分工 6 通道, 6 verb 视觉组连贯 tooltip 8 行拼接, 6 verb icon 视觉组 5 段 
> canonical 1:1 序列, PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 5 段 canonical 1:1 序列). 本节首次落在**测试代码** 
> (`tools/test_*.gd` smoke test 套件) 的 polish 模式 — 跨 5 段 (Stage 1 ITERATION_COUNT 跨迭代 0 漂移 + Stage 2 段边界 find 跨迭代稳定 + 
> Stage 3 ITERATION_COUNT `>=` 而非 `==` + Stage 4 跨段 find 0 反向 0 漂动 + Stage 5 commit-time check 集成) — 跨 7 brittle 修复 
> (FIX-#200-1 / FIX-#200-2 / FIX-#200-3 / FIX-#200-4 跨 #200 审查模式 4 brittle 修复 + FIX-#205-1 / FIX-#205-2 / FIX-#205-3 跨 
> #205 审查模式 3 brittle 修复) 走 T162 brittle 修复流程落地, 0 漂动 0 后续 主要 pre-existing 风险. 任何"加新 1 smoke test"或"加新 1 brittle 
> 修复"或"加新 1 段边界检查"必须**先**按 Stage 1 ITERATION_COUNT 跨迭代 0 漂移 1:1 加 1 expect (Stage 1 1:1 严格, 0 硬编码 `==` N), **再**按 Stage 
> 2 段边界 find 跨迭代稳定 1:1 复制 (Stage 2 1:1 严格, 0 硬编码 `## #N` marker), **再**按 Stage 3 ITERATION_COUNT `>=` 而非 `==` 1:1 镜像 
> (Stage 3 1:1 严格, 0 触碰 0 改 0 漏 0 反向), **再**按 Stage 4 跨段 find 0 反向 0 漂动 1:1 加 1 anchor (Stage 4 1:1 严格, 0 漏 1 锚点 0 改 1 
> 字符 0 反向), **再**按 Stage 5 commit-time check 集成 1:1 同步 (Stage 5 1:1 严格, 0 漏 1 hook 0 改 1 段). 跨 7 brittle 修复 × 5 段 = 35 
> 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: `tools/test_*.gd` smoke test 套件跨 7 brittle 修复 (FIX-#200-1/2/3/4 + FIX-#205-1/2/3) 走 T162 brittle 修复流程 反复 
polish 期间出现 2 类典型 fragile: (1) **"smoke test ITERATION_COUNT 硬编码 `==` 跨迭代断裂"** — 写新 1 smoke test 时 (e.g. test_t276 T276 
#196 落地写 `count_text == "196"`) 硬编码 ITERATION_COUNT.txt == 落地时的 N, 0 用 `>=` 跨迭代稳定断言, 触发 ITERATION_COUNT.txt 增长时 (N → 
N+1 → N+2) smoke test 0 PASS 0 漂动 (e.g. test_t276 #200 时 ITERATION_COUNT.txt = 200, 原 `count_text == "196"` PASS 17 期望 
T276 #196 落地时旧状态失效, 修复: 期望 `==` → `>=` 跨迭代稳定 1 expect 反转 0 触碰 18 断言其他项, 4 brittle 修复同型 FIX-#200-1/2/3/4 全部走通); (2) 
**"段边界 find 硬编码 `## #N` 跨迭代断裂"** — 写新 1 smoke test 校验 ROADMAP 顶部时间戳或 CHANGELOG 顶部索引时硬编码 `## #N` 段标题 (e.g. test_t257 
`ROADMAP.md 顶部时间戳含 #177 硬编码 → 改为检查「`§9.6.7` (T257 章节锚点) + 顶部最近 5 轮 iteration marker 至少 1 个 (180/179/178/177/176)」`), 0 
用稳定子串 (e.g. `_section9628_smoke.gd` 文件名后缀 或 `### 9.6.X` 章节锚点) 0 改 1 字段, 触发 ROADMAP 顶部按节奏滚动时 (顶部 #179 → #180 → #185 → 
#200 → #205) smoke test 0 PASS 0 漂动. 2 类症状根因: smoke test ITERATION_COUNT 跨迭代 0 漂移 + 段边界 find 跨迭代稳定 polish 模式 0 文档化, 任何新 
1 smoke test 只能"凭经验"同步 5 件套 (1 ITERATION_COUNT 跨迭代 0 漂移 1:1 + 1 段边界 find 跨迭代稳定 1:1 + 1 ITERATION_COUNT `>=` 而非 `==` 1:1 
+ 1 跨段 find 0 反向 0 漂动 1:1 + 1 commit-time check 集成 1:1), 1 漏 1 = 1 smoke test 0 跨迭代 0 漂动 0 闭环 (1 硬编码 `==` N 0 翻转 0 漂动 / 
1 硬编码 `## #N` 0 翻转 0 漂动).

- **触发场景**: `tools/test_*.gd` smoke test 套件 1:1 视觉组连贯 (5 段) 落地需同步 5 件套: (1) **Stage 1 ITERATION_COUNT 跨迭代 0 漂移** — 
任何"校验 ITERATION_COUNT.txt 内容"smoke test 必须 1:1 用 `count_text ==` (仅 N 时刻) 或 1:1 用 `count_text >=` (跨迭代 N / N+1 / N+2 / 
... 任意时刻) 严格 (FIX-#200-1/2/3/4 走通后所有 T2xx 类 polish 文档 smoke test 已切 `>=`, 0 触碰既有 1 字段), 0 触碰既有 0 硬编码 `==`; (2) **Stage 
2 段边界 find 跨迭代稳定** — 任何"校验 ROADMAP 顶部时间戳 / CHANGELOG 顶部索引 / CONTRIBUTING §9.6 章节存在"smoke test 必须 1:1 用稳定子串 (e.g. 
`_section9627_smoke.gd` 文件名后缀 或 `### 9.6.27` 章节锚点) 0 硬编码 `## #N` (FIX-#205-1 走通后所有 T2xx 类 polish 文档 smoke test 已切 稳定子串 
+ 最近 N 轮 marker 任意一个, 0 触碰既有 1 字段), 0 触碰既有 0 硬编码 `## #N`; (3) **Stage 3 ITERATION_COUNT `>=` 而非 `==`** — 任何"校验 
ITERATION_COUNT.txt 内容"expect 必须 1:1 显式 `int(count_text) >= N` (e.g. `int(count_text) >= 196`), 0 触碰既有 0 `>=` 0 翻转 0 反向 
(FIX-#200-1 走通后 4 expect 反转), 0 触碰既有 0 改 1 expect; (4) **Stage 4 跨段 find 0 反向 0 漂动** — 任何"校验 ROADMAP 顶部时间戳"必须 1:1 显式 
"最近 5 轮 iteration marker 任意一个" (e.g. `f"#%d" % (iter - i)` for i in range(5)), 0 触碰既有 0 反向 0 漂动 (FIX-#205-1 走通后 1 段 
find 反转), 0 触碰既有 0 改 1 锚点; (5) **Stage 5 commit-time check 集成** — 任何"加新 1 ITERATION_COUNT 跨迭代检查"必须 1:1 同步 
`tools/_parse_recent_section.py` + `tools/pre_commit_f002_check.sh` (T265 落地时 0 含, 后续 polish 期可加 "smoke test 
ITERATION_COUNT 跨迭代稳定" 自动检查, 0 触碰既有 0 漏 1 hook 0 改 1 段), 0 触碰既有 0 触碰 F002 self-test 0 触碰 `tools/install_hooks.sh`. 5 件套 
1:1 同步, 1 漏 1 = 1 smoke test 0 跨迭代 0 漂动.

- **修复** (跨 7 brittle 修复 走 T162 brittle 修复流程 落地后已知 5 段 canonical 1:1 序列 + 5 件套 0 例外):
  - **FIX-#200-1 (T276 #196 polish 文档 smoke test ITERATION_COUNT 跨迭代 0 漂移)** — 
[`tools/test_t276_contributing_fragility_section9620_smoke.gd`](file:///workspace/tools/test_t276_contributing_fragility
_section9620_smoke.gd) ITERATION_COUNT 跨迭代 0 漂移 (#200 ITERATION_COUNT.txt = 200, 原 PASS 17 期望 `count_text == "196"` 
T276 #196 落地时旧状态失效, 修复: 期望 `==` → `>=` 跨迭代稳定 1 expect 反转 0 触碰 18 断言其他项, Stage 1 1:1 严格 + Stage 3 1:1 严格 0 反向 0 漂动 0 漏 1 
字段), 0 触碰既有 0 改 1 字段.
  - **FIX-#200-2 (T277 #197 polish 文档 smoke test ITERATION_COUNT 跨迭代 0 漂移)** — 
[`tools/test_t277_contributing_fragility_section9621_smoke.gd`](file:///workspace/tools/test_t277_contributing_fragility
_section9621_smoke.gd) ITERATION_COUNT 跨迭代 0 漂移 (期望 `==` → `>=` 跨迭代稳定 1 expect 反转 0 触碰 20 断言其他项, Stage 1 1:1 严格 + Stage 
3 1:1 严格 0 反向 0 漂动 0 漏 1 字段), 0 触碰既有 0 改 1 字段.
  - **FIX-#200-3 (T278 #198 polish 文档 smoke test ITERATION_COUNT 跨迭代 0 漂移)** — 
[`tools/test_t278_contributing_fragility_section9622_smoke.gd`](file:///workspace/tools/test_t278_contributing_fragility
_section9622_smoke.gd) ITERATION_COUNT 跨迭代 0 漂移 (期望 `==` → `>=` 跨迭代稳定 1 expect 反转 0 触碰 18 断言其他项, Stage 1 1:1 严格 + Stage 
3 1:1 严格 0 反向 0 漂动 0 漏 1 字段), 0 触碰既有 0 改 1 字段.
  - **FIX-#200-4 (T279 #199 polish 文档 smoke test ITERATION_COUNT 跨迭代 0 漂移)** — 
[`tools/test_t279_contributing_fragility_section9623_smoke.gd`](file:///workspace/tools/test_t279_contributing_fragility
_section9623_smoke.gd) ITERATION_COUNT 跨迭代 0 漂移 (期望 `==` → `>=` 跨迭代稳定 1 expect 反转 0 触碰 21 断言其他项, Stage 1 1:1 严格 + Stage 
3 1:1 严格 0 反向 0 漂动 0 漏 1 字段), 0 触碰既有 0 改 1 字段.
  - **FIX-#205-1 (T283 #204 polish 文档 smoke test 段边界 find 跨迭代稳定)** — 
[`tools/test_t283_contributing_fragility_section9627_smoke.gd`](file:///workspace/tools/test_t283_contributing_fragility
_section9627_smoke.gd) 段边界 find 跨迭代稳定 (原硬编码 `ROADMAP.md 顶部时间戳含 #177` 0 触碰, 修复: 改为检查「`§9.6.7` (T257 章节锚点) + 顶部最近 5 轮 
iteration marker 至少 1 个 (180/179/178/177/176)」, Stage 2 1:1 严格 + Stage 4 1:1 严格 0 反向 0 漂动 0 漏 1 锚点 0 漏 1 段), 0 触碰既有 0 改 
1 段.
  - **FIX-#205-2 (T283 #204 polish 文档 smoke test 段边界 find 跨迭代稳定)** — 
[`tools/test_t283_contributing_fragility_section9627_smoke.gd`](file:///workspace/tools/test_t283_contributing_fragility
_section9627_smoke.gd) 段边界 find 跨迭代稳定 (原硬编码 `ROADMAP.md 顶部时间戳含 #204` 0 触碰, 修复: 同 FIX-#205-1 模式 改为「`§9.6.27` (T283 章节锚点) 
+ 顶部最近 5 轮 iteration marker 至少 1 个」, Stage 2 1:1 严格 + Stage 4 1:1 严格 0 反向 0 漂动 0 漏 1 锚点 0 漏 1 段), 0 触碰既有 0 改 1 段.
  - **FIX-#205-3 (T283 #204 polish 文档 smoke test 段边界 find 跨迭代稳定)** — 
[`tools/test_t283_contributing_fragility_section9627_smoke.gd`](file:///workspace/tools/test_t283_contributing_fragility
_section9627_smoke.gd) 段边界 find 跨迭代稳定 (原硬编码 `CHANGELOG.md 顶部含 #204` 0 触碰, 修复: 同 FIX-#205-1 模式 改为「`§9.6.27` (T283 章节锚点) 
+ 顶部最近 5 轮 iteration marker 至少 1 个」, Stage 2 1:1 严格 + Stage 4 1:1 严格 0 反向 0 漂动 0 漏 1 锚点 0 漏 1 段), 0 触碰既有 0 改 1 段.
  - 跨 7 brittle 修复 走 T162 brittle 修复流程 落地后 0 漂动, smoke test 5 段 canonical 1:1 序列 100% 闭环, 5 段 1:1 严格分离 (Stage 1 
ITERATION_COUNT 跨迭代 0 漂移 1:1 严格 0 漏 0 改 0 反序 0 硬编码 `==` N + Stage 2 段边界 find 跨迭代稳定 1:1 严格 0 漏 0 改 0 硬编码 `## #N` 0 反向 + 
Stage 3 ITERATION_COUNT `>=` 而非 `==` 1:1 严格 0 漏 0 改 0 改 1 expect 0 反向 + Stage 4 跨段 find 0 反向 0 漂动 1:1 严格 0 漏 0 改 0 反向 0 
漂动 + Stage 5 commit-time check 集成 1:1 严格 0 漏 0 改 0 触碰既有 F002 self-test 0 触碰既有 `install_hooks.sh`). 7 brittle 修复 0 后续 主要 
pre-existing 风险.

- **预防**:
  1. 任何 polish 期给 "加新 1 smoke test" 或 "加新 1 brittle 修复" 或 "加新 1 段边界检查" 任务加新段 / 加新 expect / 加新锚点 / 改 expect / 
改锚点时**必须**严格按 5 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 改 ITERATION_COUNT 跨迭代 0 漂移** 
(FIX-#200-1/2/3/4 走通后所有 T2xx 类 polish 文档 smoke test 已切 `>=`, 0 触碰既有 1 字段 0 改 1 expect 0 漏 1 expect 0 反序 0 反向, 改 1 
expect `==` → `==` = 1 smoke test 0 跨迭代 0 漂动 0 闭环), **Stage 2 0 改段边界 find 跨迭代稳定** (FIX-#205-1/2/3 走通后所有 T2xx 类 polish 
文档 smoke test 已切 稳定子串 + 最近 N 轮 marker 任意一个, 0 触碰既有 1 字段 0 改 1 锚点 0 漏 1 锚点 0 反序 0 反向, 改 1 锚点 `## #N` → `## #N` = 1 smoke 
test 0 跨迭代 0 漂动 0 闭环), **Stage 3 0 改 ITERATION_COUNT `>=` 而非 `==`** (FIX-#200-1/2/3/4 走通后 4 expect 反转, 0 触碰既有 0 改 1 
expect 0 漏 1 expect 0 反序 0 反向, 改 1 expect `>=` → `==` = 1 smoke test 0 跨迭代 0 漂动 0 闭环), **Stage 4 0 改跨段 find 0 反向 0 漂动** 
(FIX-#205-1/2/3 走通后 1 段 find 反转, 0 触碰既有 0 改 1 锚点 0 漏 1 锚点 0 反序 0 反向, 改 1 锚点 0 反向 = 1 smoke test 0 跨迭代 0 漂动 0 闭环), 
**Stage 5 0 改 commit-time check 集成** (T265 (#186) 落地时 0 含, 后续 polish 期可加 "smoke test ITERATION_COUNT 跨迭代稳定" 自动检查, 0 
触碰既有 F002 self-test 0 触碰既有 `install_hooks.sh` 0 漏 1 hook 0 改 1 段). 任何"加新 1 expect"必须**先**按 Stage 1 ITERATION_COUNT 跨迭代 
0 漂移 1:1 加 1 expect (Stage 1 1:1 严格, 0 硬编码 `==` N), **再**按 Stage 2 段边界 find 跨迭代稳定 1:1 复制 (Stage 2 1:1 严格, 0 硬编码 `## #N` 
marker), **再**按 Stage 3 ITERATION_COUNT `>=` 而非 `==` 1:1 镜像 (Stage 3 1:1 严格, 0 触碰既有 0 改 1 expect 0 漏 1 expect 0 反向), 
**再**按 Stage 4 跨段 find 0 反向 0 漂动 1:1 加 1 anchor (Stage 4 1:1 严格, 0 漏 1 锚点 0 改 1 字符 0 反向), **再**按 Stage 5 commit-time 
check 集成 1:1 同步 (Stage 5 1:1 严格, 0 漏 1 hook 0 改 1 段). 任何"加新 1 锚点"必须**先**按 Stage 2 段边界 find 跨迭代稳定 1:1 加 1 锚点 (Stage 2 
1:1 严格, 0 硬编码 `## #N` marker), **再**按 Stage 4 跨段 find 0 反向 0 漂动 1:1 加 1 anchor (Stage 4 1:1 严格, 0 漏 1 锚点 0 改 1 字符 0 反向).
  2. **5 段序列 0 触碰边界** — `tools/test_*.gd` 任何 expect 0 改 0 删 0 重排 (5 段 0 触碰边界), 任何"修 1 expect"polish 走 Stage 1 1:1 严格 (改 
ITERATION_COUNT.txt 内容 expect 既有 `>=` 时**必须**保持 `>=` 0 改 0 漏 0 反向 0 反序 0 触碰既有 1 字段); 任何"加新 1 段边界检查"polish 走 Stage 2 1:1 
严格 (改 段边界 find 既有 稳定子串 + 最近 N 轮 marker 任意一个时**必须**保持 稳定子串 0 改 0 漏 0 反向 0 反序 0 硬编码 `## #N`).
  3. **0 改 1 期望 0 漏 1 期望 0 反向** (FIX-#200-1/2/3/4 + FIX-#205-1/2/3 落地后 0 触碰边界) — `tools/test_*.gd` smoke test 套件 expect 
(1:1 严格分离 0 漏 0 改 0 改 1 expect 0 漏 1 expect 0 反向, 改 1 expect / 漏 1 expect / 反向 1 expect = 1 smoke test 0 跨迭代 0 漂动), 
source-grep 验证 `tools/test_*.gd` expect `>=` 0 漏 0 改 1 expect 0 漏 1 期望 0 改 1 字符 0 反向.
  4. **0 改 1 锚点 0 漏 1 锚点 0 反向** (FIX-#205-1/2/3 落地后 0 触碰边界) — `tools/test_*.gd` smoke test 套件 锚点 (稳定子串 + 最近 N 轮 marker 
任意一个) 1:1 严格分离 0 漏 0 改 0 硬编码 `## #N` 0 反向 (稳定子串 0 漏 0 改 0 漏 1 子串 0 改 1 子串 0 硬编码 `## #N` 0 反向, 改 1 锚点 / 漏 1 锚点 / 反向 1 锚点 
= 1 smoke test 0 跨迭代 0 漂动), source-grep 验证 `tools/test_*.gd` 锚点 0 漏 0 改 1 锚点 0 漏 1 锚点 0 改 1 字符 0 反向 0 硬编码 `## #N` 0 
触碰既有 0 触碰既有 0 改 1 段.
  5. **T162 brittle 修复流程 0 触碰边界** — `tools/test_*.gd` smoke test 套件 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 
触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构 (T162 brittle 修复流程 = 
1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), 改 1 字段 / 漏 1 字段 / 
反向 1 字段 = 1 smoke test 0 跨迭代 0 漂动 0 闭环, source-grep 验证 `tools/test_*.gd` smoke test 0 漏 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 
0 触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构.
  6. **commit-time check 集成 0 改 1 段 0 漏 1 hook 0 反向** (T265 (#186) 落地时 0 含, 后续 polish 期可加 "smoke test ITERATION_COUNT 
跨迭代稳定" 自动检查) — `tools/_parse_recent_section.py` + `tools/pre_commit_f002_check.sh` 1:1 严格分离 0 漏 0 改 0 改 1 段 0 漏 1 hook 
0 反向 0 触碰既有 0 改 1 段 0 触碰既有 0 改 1 字段 0 触碰既有 0 改 1 字符 (T265 工具链 3 件套 0 触碰, 后续 polish 期加新 1 hook 类型必须 1:1 复制 T265 模式, 0 漏 
1 hook 0 改 1 段 0 反向 0 触碰既有 F002 self-test 0 触碰既有 `install_hooks.sh`), 改 1 段 / 漏 1 hook / 反向 1 段 = 1 commit-time check 0 
跨迭代 0 漂动 0 闭环, source-grep 验证 `tools/_parse_recent_section.py` + `tools/pre_commit_f002_check.sh` 0 漏 0 改 1 段 0 漏 1 
hook 0 反向 0 触碰既有 0 改 1 字段 0 改 1 字符.
  7. **§9.6.28 是 18 套 polish 模式**唯一**关注 "smoke test ITERATION_COUNT 跨迭代 0 漂移 + 段边界 find 跨迭代稳定" 的 1 套** — 与 §9.6.6 / 
§9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / 
§9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 17 套 polish 模式 0 互混 0 复用 0 共享. §9.6.6-§9.6.27 关注 "游戏代码 polish 模式 (1 
panel × 3 组件 × 1:1 视觉组连贯 / 5 段 canonical 1:1 序列 / 6 verb 调色六元组 / 6 verb 视觉组连贯 / 7 桶 prewarm / Hub ↔ archive 跨房间 
transition / 6 verb HUD 6 行 6 色色域分工 / 6 verb icon 视觉组 5 段 / 6 verb 成就系统 8 段 / 6 verb VFX 视觉组 5 段 / 6 verb windup VFX 
共享契约 / 6 verb ability base 共享契约 / 6 verb signal 桥接 / 6 verb hit handler / SaveSystem audit_save_slots / PauseMenu 跨面板 
`_alpha_base` / ProfileQuickStats 4 段独立 hover 联动 / ProfileRecentList 5 行 row 文本 5→7 字段 / ProfileRecentList 5 行 row 
hover feedback 三件套 / ProfileQuickStats 4 段独立 hover 联动 / ProfileRecentList 5 行 row 文本 5→7 字段 / PlayerProfilePanel 1 
panel × 3 组件 × 1:1 视觉组连贯 5 段 canonical 1:1 序列)" (与 smoke test 0 相关, 1 panel × 3 组件 0 涉及 ITERATION_COUNT 跨迭代 / 段边界 find),
 §9.6.28 关注 "smoke test 1:1 视觉组连贯 (5 段 canonical 1:1 序列: Stage 1 ITERATION_COUNT 跨迭代 0 漂移 + Stage 2 段边界 find 跨迭代稳定 + 
Stage 3 ITERATION_COUNT `>=` 而非 `==` + Stage 4 跨段 find 0 反向 0 漂动 + Stage 5 commit-time check 集成)" (与游戏代码 0 相关, 5 段序列 
1:1 严格 镜像 7 brittle 修复既有 1:1 严格分离).
  8. 已知 drift risk: smoke test 5 段 canonical 1:1 序列 **可能**漏 1 段 1:1 镜像 (e.g. 加新 1 smoke test 时漏 Stage 1 ITERATION_COUNT 
跨迭代 0 漂移 0 镜像, 1 smoke test 0 跨迭代 0 漂动 0 闭环; 加新 1 段边界检查时漏 Stage 2 段边界 find 跨迭代稳定 0 镜像, 1 smoke test 0 跨迭代 0 漂动 0 闭环; 修 
1 expect 0 改 1 expect `>=` → `==` 触发 1 smoke test 0 跨迭代 0 漂动 0 闭环; 改 1 锚点 0 改 1 锚点 0 硬编码 `## #N` 触发 1 smoke test 0 跨迭代 
0 漂动 0 闭环). 监控建议: 任何"未来给 smoke test 加新 expect / 加新锚点 / 加新段边界检查 / 改 expect / 改锚点"必须**先**在 5 段序列 (Stage 1 ITERATION_COUNT 
跨迭代 0 漂移 + Stage 2 段边界 find 跨迭代稳定 + Stage 3 ITERATION_COUNT `>=` 而非 `==` + Stage 4 跨段 find 0 反向 0 漂动 + Stage 5 
commit-time check 集成) 1:1 镜像 0 漏 1 段. T283 (#204) 5 段序列 1:1 镜像落地后任何新 expect / 新锚点 / 新段边界检查 / 新 expect / 新锚点 加 5 段同步是 
§9.6.28 5 段序列的"母集" (1 smoke test × 5 段 = 5 元素 1:1 严格, §9.6.28 5 段 7 brittle 修复完整闭环, 7 brittle 修复 × 5 段 = 35 元素 1:1 严格, 
含 Stage 1 ITERATION_COUNT 跨迭代 0 漂移 4 expect 反转 0 漏 0 改 + Stage 2 段边界 find 跨迭代稳定 3 段 find 反转 0 漏 0 改 + Stage 3 
ITERATION_COUNT `>=` 而非 `==` 4 expect 反转 0 漏 0 改 + Stage 4 跨段 find 0 反向 0 漂动 3 段 find 反转 0 漏 0 改 + Stage 5 commit-time 
check 集成 0 改 0 漏 0 反向 0 触碰既有 F002 self-test 0 触碰既有 `install_hooks.sh`). 另 1 类已知 drift: Stage 5 commit-time check 集成 0 改 
1 段 0 漏 1 hook 0 反向 (e.g. 后续 polish 期加新 1 hook 类型 "smoke test ITERATION_COUNT 跨迭代稳定" 自动检查时漏 `install_hooks.sh` 1:1 复制 
T265 模式, 1 hook 0 跨迭代 0 漂动 0 闭环; 或改 `pre_commit_f002_check.sh` 既有 1 段 0 触碰既有 F002 self-test), 后续 polish 期若加新 1 
commit-time check 必须**先**用 1:1 镜像 F002 self-test 模板, 0 改既有 0 漏 1 hook 0 反向.

**0 副作用**: T265 (#186) 任何 const / var 0 触碰 (`tools/_parse_recent_section.py` 4 条解析规则 0 改 0 删 0 重排 + 
`tools/pre_commit_f002_check.sh` sanity check 3 件套 0 改 0 删 0 重排 + `tools/install_hooks.sh` install / uninstall 2 模式 4 
个分支 0 改 0 删 0 重排); 0 触碰 §9.6.6 ProfileRecentList 5 行 row 文本 5→7 字段 format 字符串扩展模式 (T284 0 改 §9.6.6 任何 1 字符); 0 触碰 
§9.6.7 ProfileRecentList 5 行 row hover feedback 三件套 (T284 0 改 §9.6.7 任何 1 字符); 0 触碰 §9.6.8 ProfileQuickStats 4 段独立 
hover 联动 (T284 0 改 §9.6.8 任何 1 字符); 0 触碰 §9.6.9 AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` Dictionary 双源 
(T284 0 改 §9.6.9 任何 1 字符); 0 触碰 §9.6.10 「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (T284 0 改 §9.6.10 任何 1 字符); 0 触碰 
§9.6.15 SaveSystem audit_save_slots (T284 0 改 §9.6.15 任何 1 字符); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T284 0 改 §9.6.16 
任何 1 字符); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T284 0 改 §9.6.17 任何 1 字符); 0 触碰 §9.6.18 16 件套 verb ability base (T284 0 
改 §9.6.18 任何 1 字符); 0 触碰 §9.6.19 7 件套 windup VFX base (T284 0 改 §9.6.19 任何 1 字符); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 
transition flow (T284 0 改 §9.6.20 任何 1 字符); 0 触碰 §9.6.21 8 段成就序列 (T284 0 改 §9.6.21 任何 1 字符); 0 触碰 §9.6.22 6 verb VFX 
视觉组 5 段 (T284 0 改 §9.6.22 任何 1 字符); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T284 0 改 §9.6.23 任何 1 字符); 0 触碰 §9.6.24 HUD 6 verb 
× 6 通道 = 36 通道 5 段序列 (T284 0 改 §9.6.24 任何 1 字符); 0 触碰 §9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 5 段序列 (T284 0 改 §9.6.25 任何 1 
字符); 0 触碰 §9.6.26 6 verb icon 视觉组 5 段 canonical 1:1 序列 (T284 0 改 §9.6.26 任何 1 字符); 0 触碰 §9.6.27 PlayerProfilePanel 1 
panel × 3 组件 × 1:1 视觉组连贯 5 段序列 (T284 0 改 §9.6.27 任何 1 字符); 0 触碰 check_smoke_consistency.sh (T284 0 改任何规则); 0 触碰 
_parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓);
 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, 0 触碰 `pause_menu.gd` 3 组件 任何 1 字段, 0 触碰 `pause_menu.tscn` 
PlayerProfilePanel/ProfileMargin/ProfileVBox 3 组件任何 1 节点); 0 行为变化, 1 文档 1 段 (§9.6.28 ~30 行) + 1 smoke test (T284 24+ 
断言).

**与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
§9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 关系** — §9.6.6 覆盖 ProfileRecentList 5 行 row 文本 5 字段 → 7 字段 
format 字符串扩展模式 (单一组件 row 文本扩展); §9.6.7 覆盖 ProfileRecentList 5 行 row hover feedback 三件套 (单一组件 row hover feedback); 
§9.6.8 覆盖 ProfileQuickStats 4 段独立 hover 联动 (单一组件 4 段 hover); §9.6.9 覆盖 AchievementGrid + ProfileRecentList 跨面板 
`_alpha_base` Dictionary 双源 (2 面板跨面板 _alpha_base); §9.6.10 覆盖「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (2 层 hover 互补); 
§9.6.15 覆盖 SaveSystem audit_save_slots (1 个公共接口 polish); §9.6.16 覆盖 6 verb hit handler 5 件套 (6 verb hit handler); 
§9.6.17 覆盖 6 verb signal 桥接 5+1 件套 (6 verb signal 桥接); §9.6.18 覆盖 16 件套 verb ability base (6 verb ability base); 
§9.6.19 覆盖 7 件套 windup VFX base (6 verb windup VFX base); §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow (跨房间 transition);
 §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 (6 verb 成就系统); §9.6.22 覆盖 6 verb VFX 视觉组 5 段 (6 verb VFX 视觉组); §9.6.23 覆盖 7 桶 prewarm 
5+1+1 (7 桶 prewarm); §9.6.24 覆盖 HUD 6 verb × 6 通道 = 36 通道 5 段 (HUD 36 通道); §9.6.25 覆盖 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 (6 
verb 视觉组 tooltip); §9.6.26 覆盖 6 verb icon 视觉组 5 段 (6 verb icon 视觉组); §9.6.27 覆盖 `pause_menu.gd` PlayerProfilePanel 1 
panel × 3 组件 × 1:1 视觉组连贯 (5 段 canonical 1:1 序列: Stage 1 3 组件 + Stage 2 中点分隔符 + Stage 3 7pt 字号 + Stage 4 _COLOR_* 
canonical 颜色 token + Stage 5 派生字段). §9.6.28 覆盖 `tools/test_*.gd` smoke test 套件 ITERATION_COUNT 跨迭代 0 漂移 + 段边界 find 
跨迭代稳定 (5 段 canonical 1:1 序列: Stage 1 ITERATION_COUNT 跨迭代 0 漂移 + Stage 2 段边界 find 跨迭代稳定 + Stage 3 ITERATION_COUNT `>=` 
而非 `==` + Stage 4 跨段 find 0 反向 0 漂动 + Stage 5 commit-time check 集成). §9.6.6 关注 "ProfileRecentList 5 行 row 文本 5 字段 → 7 
字段 format 字符串扩展模式 (单一组件 row 文本扩展, 5 字段 → 7 字段 1:1 严格)", §9.6.7 关注 "ProfileRecentList 5 行 row hover feedback 三件套 
(font_color + alpha boost + tooltip, 1 组件 3 反馈 1:1 严格)", §9.6.8 关注 "ProfileQuickStats 4 段独立 hover 联动 (4 段 tween + dim 
restore, 1 组件 4 段 hover 1:1 严格)", §9.6.9 关注 "AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` Dictionary 双源 (2 
面板跨面板 _alpha_base 1:1 严格)", §9.6.10 关注 "「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (2 层 hover 互补 1:1 严格)", §9.6.15 关注 
"SaveSystem audit_save_slots (1 个公共接口 polish, audit 4 字段 1:1 严格)", §9.6.16 关注 "6 verb hit handler 5 件套 (6 verb hit 
handler 1:1 严格)", §9.6.17 关注 "6 verb signal 桥接 5+1 件套 (6 verb signal 桥接 1:1 严格)", §9.6.18 关注 "16 件套 verb ability base 
(6 verb ability base 1:1 严格)", §9.6.19 关注 "7 件套 windup VFX base (6 verb windup VFX base 1:1 严格)", §9.6.20 关注 "Hub ↔ 
archive 跨房间 transition flow (跨房间 transition 1:1 严格)", §9.6.21 关注 "6 verb 成就系统 闭环契约 (8 段 1:1 严格, 含 PNG 双路径)", §9.6.22 关注 
"6 verb VFX 视觉组 5 段 1:1 严格分离契约", §9.6.23 关注 "7 桶 prewarm 5+1+1 (1:1 严格分离契约)", §9.6.24 关注 "HUD 6 verb × 6 通道 = 36 通道 5 段 
1:1 严格分离契约", §9.6.25 关注 "6 verb 视觉组连贯 tooltip 8 行拼接 5 段 1:1 严格分离契约", §9.6.26 关注 "6 verb icon 视觉组 5 段 1:1 严格分离契约 (Stage 
1 family 路径 + Stage 2 disc + outer ring + Stage 3 调色 + Stage 4 几何 + Stage 5 双 export + 3 verb achievement 双路径)", 
§9.6.27 关注 "PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 (5 段 canonical 1:1 序列: Stage 1 3 组件 + Stage 2 中点分隔符 + Stage 3 
7pt 字号 + Stage 4 _COLOR_* canonical 颜色 token + Stage 5 派生字段)" 的 1 套. §9.6.28 是 18 套 polish 模式**唯一**关注 "smoke test 
ITERATION_COUNT 跨迭代 0 漂移 + 段边界 find 跨迭代稳定 (5 段 canonical 1:1 序列: Stage 1 ITERATION_COUNT 跨迭代 0 漂移 + Stage 2 段边界 find 
跨迭代稳定 + Stage 3 ITERATION_COUNT `>=` 而非 `==` + Stage 4 跨段 find 0 反向 0 漂动 + Stage 5 commit-time check 集成)" 的 1 套, 与 
§9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
§9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 17 套 ProfileRecentList 5 行 / ProfileQuickStats 4 段 / 
AchievementGrid / 6 verb / SaveSystem / 跨房间 transition / PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 0 互混 0 复用 0 共享. 
任何"加新 1 expect"或"加新 1 锚点"或"加新 1 段边界检查"或"修 1 expect"或"修 1 锚点"必须**先**按 §9.6.28 Stage 1 ITERATION_COUNT 跨迭代 0 漂移 1:1 加 1 
expect (Stage 1 1:1 严格, 0 硬编码 `==` N), **再**按 §9.6.28 Stage 2 段边界 find 跨迭代稳定 1:1 复制 (Stage 2 1:1 严格, 0 硬编码 `## #N` 
marker), **再**按 §9.6.28 Stage 3 ITERATION_COUNT `>=` 而非 `==` 1:1 镜像 (Stage 3 1:1 严格, 0 触碰既有 0 改 1 expect 0 漏 1 expect 0 
反向), **再**按 §9.6.28 Stage 4 跨段 find 0 反向 0 漂动 1:1 加 1 anchor (Stage 4 1:1 严格, 0 漏 1 锚点 0 改 1 字符 0 反向), **再**按 §9.6.28 
Stage 5 commit-time check 集成 1:1 同步 (Stage 5 1:1 严格, 0 漏 1 hook 0 改 1 段). §9.6.6-§9.6.27 与 §9.6.28 0 互混 0 复用 0 共享: 
§9.6.6-§9.6.27 关注 "游戏代码 polish 模式 (1 panel × 3 组件 × 1:1 视觉组连贯 / 5 段 canonical 1:1 序列 / 6 verb 调色六元组 / 6 verb 视觉组连贯 / 7 
桶 prewarm / Hub ↔ archive 跨房间 transition / 6 verb HUD 6 行 6 色色域分工 / 6 verb icon 视觉组 5 段 / 6 verb 成就系统 8 段 / 6 verb VFX 
视觉组 5 段 / 6 verb windup VFX 共享契约 / 6 verb ability base 共享契约 / 6 verb signal 桥接 / 6 verb hit handler / SaveSystem 
audit_save_slots / PauseMenu 跨面板 `_alpha_base` / ProfileQuickStats 4 段独立 hover 联动 / ProfileRecentList 5 行 row 文本 5→7 字段 
/ ProfileRecentList 5 行 row hover feedback 三件套 / ProfileQuickStats 4 段独立 hover 联动 / ProfileRecentList 5 行 row 文本 5→7 字段 
/ PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 5 段 canonical 1:1 序列)" (与 smoke test 0 相关, 1 panel × 3 组件 0 涉及 
ITERATION_COUNT 跨迭代 / 段边界 find), §9.6.28 关注 "smoke test 1:1 视觉组连贯 (5 段 canonical 1:1 序列: Stage 1 ITERATION_COUNT 跨迭代 0 
漂移 + Stage 2 段边界 find 跨迭代稳定 + Stage 3 ITERATION_COUNT `>=` 而非 `==` + Stage 4 跨段 find 0 反向 0 漂动 + Stage 5 commit-time 
check 集成)" (与游戏代码 0 相关, 5 段序列 1:1 严格 镜像 7 brittle 修复既有 1:1 严格分离). §9.6.28 是 18 套 polish 模式**唯一**关注 "smoke test 
ITERATION_COUNT 跨迭代 0 漂移 + 段边界 find 跨迭代稳定" 的 1 套, 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / 
§9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 17 套 
ProfileRecentList 5 行 / ProfileQuickStats 4 段 / AchievementGrid / 6 verb / SaveSystem / 跨房间 transition / 
PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 0 互混 0 复用 0 共享.

**与 §11 F002 self-test commit hook 集成 关系** — §11 记录 T265 (#186) 落地的 3 件套工具链 (`tools/_parse_recent_section.py` + 
`tools/pre_commit_f002_check.sh` + `tools/install_hooks.sh`), 关注 "F002 self-test 提前到 commit 阶段" 模式 (3 件套 1:1 严格分离契约). 
§9.6.28 关注 "smoke test ITERATION_COUNT 跨迭代 0 漂移 + 段边界 find 跨迭代稳定" 模式 (5 段 canonical 1:1 序列: Stage 1 ITERATION_COUNT 跨迭代 
0 漂移 + Stage 2 段边界 find 跨迭代稳定 + Stage 3 ITERATION_COUNT `>=` 而非 `==` + Stage 4 跨段 find 0 反向 0 漂动 + Stage 5 commit-time 
check 集成). §11 与 §9.6.28 0 互混 0 复用 0 共享: §11 关注 "F002 self-test commit hook 工具链 3 件套 1:1 严格分离契约 (Python 解析器 + 
pre-commit 检查脚本 + hook installer)" (与 ITERATION_COUNT 跨迭代 / 段边界 find 0 相关, 3 件套仅是 F002 self-test 0 涉及 ITERATION_COUNT 
跨迭代 / 段边界 find), §9.6.28 关注 "smoke test 5 段 canonical 1:1 序列 1:1 视觉组连贯" (与 3 件套 0 相关, 5 段序列 1:1 严格 镜像 7 brittle 修复既有 
1:1 严格分离, 7 brittle 修复 × 5 段 = 35 元素 1:1 严格). §11 Stage 5 commit-time check 集成 是 §9.6.28 Stage 5 的"上游" — §9.6.28 Stage 
5 关注 "smoke test ITERATION_COUNT 跨迭代 0 漂移 0 漂动 0 闭环", §11 Stage 5 关注 "F002 self-test commit-time 集成 0 漂动 0 闭环". 任何"加新 1 
commit-time check"必须**先**按 §11 F002 self-test 工具链 3 件套 1:1 复制 (T265 模式 1:1 严格, 0 漏 1 件 0 改 1 字段), **再**按 §9.6.28 Stage 
5 commit-time check 集成 1:1 同步 (5 段序列 1:1 严格, 0 漏 1 hook 0 改 1 段 0 反向 0 触碰既有 F002 self-test 0 触碰既有 `install_hooks.sh`).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 
0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 轮 0 后续 持续收敛), §9.6.28 是"polish 模式" (5 段 canonical 1:1 序列 + 7 brittle 修复既有 1:1 
严格分离契约). T162 与 §9.6.28 0 互混 0 复用 0 共享: T162 关注 "brittle 修复流程 5 步骤" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 
+ 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 步骤 1:1 严格), §9.6.28 关注 "5 段 canonical 1:1 序列 + 7 brittle 修复既有 1:1 
严格分离契约" (与 5 步骤 0 相关, 7 brittle 修复 × 5 段 = 35 元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离). 任何"加新 1 brittle 修复"必须**先**按 T162 5 步骤 1:1 
复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), **再**按 §9.6.28 5 段序列 1:1 镜像 (5 段序列 1:1 严格, 0 漏 1 段 
0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.28 5 段序列走 §9.1 9 步落地的 0 步 (5 段序列仅在 `tools/test_*.gd` smoke test 套件 expect / 锚点,
 0 走 verb enum + ability 子类 + const / var / input map + shop 系统 + icon PNG + VFX 视觉组 + audio 副作用 + HUD 渲染, 0 触碰 §9.1 9 
步任何 1 步). 任何"加新 1 expect"或"加新 1 锚点"或"加新 1 段边界检查"必须**先**按 §9.6.28 5 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段), 0 触碰 §9.1 9 步任何 1 步 
(5 段序列仅在 `tools/test_*.gd` smoke test 套件 expect / 锚点, 0 触碰 `pause_menu.gd` 任何 const / var / 节点, 0 触碰 6 verb enum + 
ability 子类 + input map + shop 系统 + icon PNG + VFX 视觉组 + audio 副作用 + HUD 渲染). 任何"修 1 expect"必须**先**在 `tools/test_*.gd` 
smoke test 套件 expect 改 1 expect (Stage 1 1:1 严格, 0 漏 1 边 0 改 1 expect 0 反向 0 硬编码 `==` N), 0 触碰 §9.1 9 步任何 1 步 (1 expect 
0 触碰 verb enum + ability 子类 + const / var). 任何"加新 1 锚点"必须**先**在 `tools/test_*.gd` smoke test 套件 锚点 加 1 锚点 (Stage 2 1:1 
严格, 0 漏 1 边 0 改 1 锚点 0 反向 0 硬编码 `## #N` marker), 0 触碰 §9.1 9 步任何 1 步 (1 锚点 0 触碰 verb enum + ability 子类 + const / var). 
任何"加新 1 段边界检查"必须**先**在 `tools/test_*.gd` smoke test 套件 段边界检查 加 1 段边界检查 (Stage 2 1:1 严格, 0 漏 1 边 0 改 1 段边界检查 0 反向 0 硬编码 
`## #N` marker), 0 触碰 §9.1 9 步任何 1 步 (1 段边界检查 0 触碰 verb enum + ability 子类 + const / var). 任何"加新 1 跨段镜像"必须**先**在 §9.6.6 
/ §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / 
§9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 17 跨段加 1 镜像 (5 段序列 1:1 严格, 0 漏 1 跨段 0 改 1 字段), 0 触碰 §9.1 9 步任何 const / 
var / 节点 (5 段序列仅在 `tools/test_*.gd` smoke test 套件 expect / 锚点, 17 跨段 0 触碰 verb enum + ability 子类 + input map + shop 系统 
+ icon PNG + VFX 视觉组 + audio 副作用 + HUD 渲染).
