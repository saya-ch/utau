# Polish Patterns 9.6.35–35

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.35–9.6.35，共 1 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.35-35.md

<!-- shard 35-35 共 1 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.35 AudioPresets `MUSIC_PRESETS` preset 字段扩展 5 段 canonical 1:1 严格分离契约 polish 模式 (T062 #2 + T080 #39 + T087 #48 + 
T107 #59 + T114 #63 + T118 #63 跨 6 任务 ~211 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 25 套 polish 模式均在 "pause_menu / 6 verb / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层" 范畴, 0 涉及 "audio 
> 数据层 (`MUSIC_PRESETS` 字典) preset 字段扩展" 落地. §9.6.22 (T278 #197 落地) 覆盖 6 verb VFX 视觉组 5 段, §9.6.23 (T279 #198 落地) 覆盖 7 桶 
> prewarm 5+1+1, §9.6.24 (T280 #199 落地) 覆盖 HUD 6 verb × 6 通道 36 通道 5 段 — 这 3 段分别覆盖"VFX 视觉组" + "prewarm 桶聚合" + "HUD 通道", 
> 但**未**覆盖 "audio 预热 (`prewarm_music_streams()` 遍历 `AudioPresets.MUSIC_PRESETS.keys()`) 加新 1 preset 字段" 必须同时 1:1 镜像 5 段 
> (Stage 1 dict default + Stage 2 家族传播 + Stage 3 prewarm 桶 cache key + Stage 4 UI 显示 + Stage 5 老存档兼容 preload 0 触碰) 的契约. 
> 本节 (T291 #214 落地) 首个落在**audio 数据层 preset 字段扩展 5 段 1:1 严格分离契约** (`MUSIC_PRESETS` dict 9 preset × 13 字段 + 
> `audio_manager_enhanced.gd` 家族传播 1 字段引用 + `prewarm_music_streams()` MUSIC_PRESETS 桶 cache key + `settings_menu.gd` 
> BGM 主题提示 + audio_presets.gd preload 0 触碰) polish 模式 — 跨 5 段 (Stage 1 dict default 1:1 严格 0 漏 1 字段 + Stage 2 家族传播 1:1 
> 严格 0 漏 1 字段引用 + Stage 3 prewarm 桶 cache key 1:1 严格 0 漏 1 cache key 字段 + Stage 4 UI 显示 1:1 严格 0 漏 1 字段显示 + Stage 5 
> 老存档兼容 preload 0 触碰 1:1 严格 0 漏 1 preload 守护) — 跨 1 套 polish 模式 (T291 #214 落地) 跨 0 任务 (~214 轮落地, 0 漂动 0 后续 主要 
> pre-existing 风险). 任何"加新 1 preset 字段" (e.g. 加 `pad_attack_ms` 控制 pad 启动包络) 或"加新 1 BGM preset" (e.g. 未来 10th preset) 
> 必须**先**按 Stage 1 dict default 1:1 严格 加 1 entry (0 漏 1 字段 0 改 1 字段 0 反序 0 反向), **再**按 Stage 2 家族传播 1:1 镜像 (1:1 严格, 0 漏 
> 1 字段引用 0 改 1 字段 0 反序 0 反向), **再**按 Stage 3 prewarm 桶 cache key 1:1 镜像 (1:1 严格, 0 漏 1 cache key 字段 0 改 1 cache key 字段 
> 0 反序 0 反向), **再**按 Stage 4 UI 显示 1:1 镜像 (1:1 严格, 0 漏 1 字段显示 0 改 1 字段 0 反序 0 反向), **再**按 Stage 5 老存档兼容 preload 0 触碰 
> 1:1 镜像 (1:1 严格, 0 漏 1 preload 守护 0 改 1 字符 0 反向). 跨 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: `AudioPresets.MUSIC_PRESETS` 字典 (9 BGM preset: title_intro / hub_warm / archive_exploration / archive_boss / 
archive_boss_dual / archive_dawn / archive_storm / silence_void / whisper_hollow, T062 #2 → T118 #63 跨 6 任务 ~211 轮落地) 
每个 preset 13 字段 (bpm / duration / root_midi / chord_midi / arp_midi / shimmer_midi / lfo_freq / lfo_depth / shimmer_mod 
/ arp_volume / pad_volume / bass_volume / shimmer_volume), 在反复 polish (T062 #2 → T080 #39 +5 字段 → T087 #48 +4 字段 → T107 
#59 6 → 7 → 8 preset → T114 #63 8 preset → T118 #63 +whisper_hollow 9th preset) 期间出现 4 类典型 fragile: (1) **"加新 1 preset 
字段 漏 1 段"** — 加新 1 preset 字段 (e.g. T080 模式参考, 假设未来加 `pad_attack_ms` 控制 pad 启动包络) 落地时漏 1 段 (e.g. 漏 Stage 2 家族传播 中 
`audio_manager_enhanced.gd._ensure_music_stream()` 0 引用 `pad_attack_ms` 字段, 或漏 Stage 3 prewarm 桶 cache key 中 
MUSIC_PRESETS 桶 cache key 0 包含 `pad_attack_ms`, 或漏 Stage 4 UI 显示 中 `settings_menu.gd` BGM 主题提示 0 显示 `pad_attack_ms`), 1 
preset 字段扩展 0 5 段 0 闭环 0 漂动 (1 字段 0 家族传播 = 1 字段 0 合成应用 0 漂动; 1 字段 0 prewarm cache key = 1 字段 0 prewarm 后回放 0 漂动; 1 字段 0 
UI = 1 字段 0 玩家可见 0 漂动); (2) **"加新 1 preset 字段 漏 preset 镜像"** — 加新 1 preset 字段 (T118 whisper_hollow 9th preset 模式参考, 
假设未来加 `silence_decay_ms` 控制 silence 衰减时间) 落地时 9 preset 字典 加 1 entry 镜像漏 1 preset (e.g. 漏 title_intro, 或漏 whisper_hollow 
9th), 1 preset 字段 0 9 preset 镜像 0 闭环 0 漂动 (1 字段 0 1/9 preset 镜像 = 8/9 preset 缺 1 字段 = 1 字段 0 9 preset 0 漂动); (3) **"修 1 
preset 字段 0 触碰既有 12 字段"** — 修 1 preset 字段 (e.g. 改 `bpm` 值 60 → 72) 时改硬编码 0 改 既有 12 字段 (12 字段任何 1 字段), 跨字段 1:1 漂动 (1 修 1 
字段 0 改既有 12 字段 = 1 字段 0 13 字段 0 闭环 0 漂动); (4) **"加新 1 BGM preset 漏 13 字段镜像"** — 加新 1 BGM preset (e.g. 未来 10th preset 
"coda_dawn") 漏 13 字段任何 1 字段 (漏 `bpm` 或 `duration` 或 `root_midi` 或 ...), 1 preset 0 13 字段 0 闭环 0 漂动 (1 preset 0 13 字段 = 
1 preset 0 1 字段 0 漂动). 4 类症状根因: audio 数据层 preset 字段扩展 5 段 canonical 1:1 序列 0 文档化, 任何新 1 preset 字段扩展只能"凭经验" 1:1 镜像 5 段 
(1 dict default 1:1 严格 + 1 家族传播 1:1 严格 + 1 prewarm 桶 cache key 1:1 严格 + 1 UI 显示 1:1 严格 + 1 老存档兼容 preload 0 触碰 1:1 严格), 
1 漏 1 段 = 1 preset 字段 0 5 段 0 闭环 0 漂动 (1 漏 1 段 0 5 段 0 1:1 严格分离契约 0 漂动 / 1 漏 1 边 0 字段引用 0 1:1 严格 0 漂动).

- **触发场景**: 加新 1 preset 字段 (T118 13 字段模式参考) 时**必须**严格按 5 段 1:1 复制 T118 落地模式: (1) **Stage 1 dict default 1:1 严格** — 在 
`audio_presets.gd` `MUSIC_PRESETS` 9 preset 字典 literal 末尾**每个 preset 镜像**加 1 entry (e.g. T118 落地时 9 preset × 13 字段 1:1 
严格, 假设未来加 `pad_attack_ms` 字段, 9 preset 字典末尾**每个**都加 `"pad_attack_ms": 0.05` 1 entry, 与既有 12 字段 0 漏 0 改 0 反序 0 反向, 0 
触碰既有 12 字段任何 1 字段, 0 漏 1 preset), source-grep 验证 `MUSIC_PRESETS` 9 preset × 13 字段 0 漏 0 改 0 漏 1 entry 0 反向; (2) **Stage 
2 家族传播 1:1 严格** — 在 `audio_manager_enhanced.gd` `_ensure_music_stream()` / `_synth_preset_from_dict()` / 等家族传播函数 加 1 
字段引用 (e.g. T118 落地时 9 preset × 13 字段 1:1 严格, 假设未来加 `pad_attack_ms` → `_synth_preset_from_dict()` 中读取 
`preset.get("pad_attack_ms", 0.05)` 1 行, 0 触碰既有 12 字段任何 1 字段 0 改 0 反向), source-grep 验证 9 preset × 13 字段 0 漏 0 改 0 漏 1 
字段引用 0 反向; (3) **Stage 3 prewarm 桶 cache key 1:1 严格** — 在 `audio_manager_enhanced.gd` `prewarm_music_streams()` 中 
`AudioPresets.MUSIC_PRESETS.keys()` 桶 cache key 包含新 1 字段 (e.g. T118 落地时 9 preset × 13 字段 1:1 严格, 假设未来加 `pad_attack_ms` 
→ prewarm 桶 cache key 包含 `pad_attack_ms`, 保证 prewarm 后回放 一致, 0 触碰既有 12 字段任何 1 字段 0 改 0 反向), source-grep 验证 prewarm 桶 
cache key 0 漏 0 改 0 漏 1 cache key 字段 0 反向; (4) **Stage 4 UI 显示 1:1 严格** — 在 `settings_menu.gd` 
`_on_music_preview_pressed()` + BGM 主题提示 加 1 字段显示 (e.g. T239 落地时 9 theme 主题 1:1 严格, 假设未来加 `pad_attack_ms` → tooltip 提示 
1 行, 0 触碰既有 9 theme 任何 1 theme 0 改 0 反向), source-grep 验证 9 theme 0 漏 0 改 0 漏 1 字段显示 0 反向; (5) **Stage 5 老存档兼容 preload 0 
触碰 1:1 严格** — `audio_presets.gd` 永远以 `const AudioPresets = preload("res://src/scripts/audio_presets.gd")` 加载, 0 在 
SaveSystem save dict (T128 `SLOT_DATA_KEYS` / T135 share / T136 autosave) 中, 加新 1 preset 字段 0 触发 SaveSystem 0 触碰 0 
migration, 0 漏 0 改 0 反向. 5 段 1:1 同步, 1 漏 1 段 / 1 漏 1 边 = 1 preset 字段 0 5 段 0 闭环 0 漂动. T118 0 触碰既有 12 字段任何 1 字段 (0 改 
dict default 任何 1 entry, 0 改 家族传播 任何 1 字段引用, 0 改 prewarm 桶 cache key 任何 1 cache key 字段, 0 改 UI 任何 1 theme, 0 触碰 
SaveSystem save dict 任何 1 字符).

- **修复** (跨 1 套 polish 模式 T291 #214 落地后已知 5 段 0 例外):
  - **T291 #214 首个 audio 数据层 preset 字段扩展 5 段 canonical 1:1 严格分离契约 落地 (`MUSIC_PRESETS` 9 preset × 13 字段 + 
`audio_manager_enhanced.gd` 家族传播 + `prewarm_music_streams()` MUSIC_PRESETS 桶 cache key + `settings_menu.gd` BGM 主题提示 + 
`audio_presets.gd` preload 0 触碰)** — §9.6.35 段 是 1 套 polish 模式首个 1:1 包含 5 段 (`audio_presets.gd` `MUSIC_PRESETS` 9 
preset × 13 字段 dict default + `audio_manager_enhanced.gd` 家族传播 1 字段引用 + `prewarm_music_streams()` MUSIC_PRESETS 桶 cache 
key + `settings_menu.gd` BGM 主题提示 1 字段显示 + `audio_presets.gd` preload 0 触碰) 的 audio 数据层 preset 字段扩展 polish 模式, 5 段 1:1 
严格分离 (1 `MUSIC_PRESETS` 9 preset × 13 字段 1:1 严格 0 漏 0 改 + 1 `audio_manager_enhanced.gd` 家族传播 1:1 严格 0 漏 0 改 + 1 
`prewarm_music_streams()` MUSIC_PRESETS 桶 cache key 1:1 严格 0 漏 0 改 + 1 `settings_menu.gd` BGM 主题提示 1:1 严格 0 漏 0 改 + 1 
`audio_presets.gd` preload 0 触碰 1:1 严格 0 漏 0 改, 0 漏 1 段 0 改 1 字段 0 反序 0 反向), 0 触碰既有 (T291 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 
/ §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 
/ §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 25 套 polish 模式任何 1 
字符, 0 触碰游戏代码 .gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容, 0 触碰 
ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).
  - **T291 #214 Stage 1 dict default 1:1 严格分离契约 落地 (`MUSIC_PRESETS` 9 preset × 13 字段)** — §9.6.35 段 1:1 显式 dict default 
(T291 落地时 `audio_presets.gd` `MUSIC_PRESETS` 9 preset × 13 字段 1:1 严格 (title_intro / hub_warm / archive_exploration / 
archive_boss / archive_boss_dual / archive_dawn / archive_storm / silence_void / whisper_hollow, 每 preset bpm / 
duration / root_midi / chord_midi / arp_midi / shimmer_midi / lfo_freq / lfo_depth / shimmer_mod / arp_volume / 
pad_volume / bass_volume / shimmer_volume), 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), dict default 1:1 严格 (1 `MUSIC_PRESETS` 9 
preset × 13 字段 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向), 0 触碰既有 (T291 0 改 
`audio_presets.gd` `MUSIC_PRESETS` 任何 1 字符).
  - **T291 #214 Stage 2 家族传播 1:1 严格分离契约 落地 (`audio_manager_enhanced.gd` 13 字段引用)** — §9.6.35 段 1:1 显式家族传播 (T291 落地时 
`audio_manager_enhanced.gd` `_ensure_music_stream()` / `_synth_preset_from_dict()` / `_midi_to_hz()` / 等函数 13 字段 1:1 引用 
(bpm → tempo calc, duration → loop length, root_midi / chord_midi / arp_midi / shimmer_midi → freq synth, lfo_freq / 
lfo_depth / shimmer_mod → modulation, arp_volume / pad_volume / bass_volume / shimmer_volume → AudioStreamPlayer gain), 
0 漏 1 字段引用 0 改 1 字段 0 反序 0 反向), 家族传播 1:1 严格 (1 13 字段引用 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 字段引用 0 改 
1 字段 0 反序 0 反向), 0 触碰既有 (T291 0 改 `audio_manager_enhanced.gd` 任何 1 字符).
  - **T291 #214 Stage 3 prewarm 桶 cache key 1:1 严格分离契约 落地 (`prewarm_music_streams()` MUSIC_PRESETS 桶 cache key)** — 
§9.6.35 段 1:1 显式 prewarm 桶 cache key (T291 落地时 `audio_manager_enhanced.gd` `prewarm_music_streams()` 遍历 
`AudioPresets.MUSIC_PRESETS.keys()` (9 preset key) → `_ensure_music_stream(key)` 1:1 严格, 0 漏 1 cache key 字段 0 改 1 cache 
key 字段 0 反序 0 反向), prewarm 桶 cache key 1:1 严格 (1 9 preset key 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 
cache key 字段 0 改 1 cache key 字段 0 反序 0 反向), 0 触碰既有 (T291 0 改 `prewarm_music_streams()` 任何 1 字符).
  - **T291 #214 Stage 4 UI 显示 1:1 严格分离契约 落地 (`settings_menu.gd` BGM 主题提示 9 theme)** — §9.6.35 段 1:1 显式 UI 显示 (T291 落地时 
`settings_menu.gd` `_on_music_preview_pressed()` 9 theme (title_intro / hub_warm / archive_exploration / archive_boss / 
archive_boss_dual / archive_dawn / archive_storm / silence_void / whisper_hollow) 1:1 严格 + BGM 主题提示 tooltip 13 字段 (T239 
落地时 `_on_music_preview_pressed` 1:1 严格, 0 漏 1 theme 0 改 1 字段 0 反序 0 反向), UI 显示 1:1 严格 (1 9 theme 1:1 严格 0 漏 0 改 + 1 
source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 字段显示 0 改 1 字段 0 反序 0 反向), 0 触碰既有 (T291 0 改 `settings_menu.gd` 任何 1 字符).
  - **T291 #214 Stage 5 老存档兼容 preload 0 触碰 1:1 严格分离契约 落地 (`audio_presets.gd` preload 0 触碰 SaveSystem save dict)** — 
§9.6.35 段 1:1 显式老存档兼容 (T291 落地时 `audio_presets.gd` 永远以 `const AudioPresets = preload(...)` 加载, 0 在 SaveSystem save dict 
(T128 `SLOT_DATA_KEYS` / T135 share / T136 autosave) 中, 9 preset × 13 字段 = 117 entry 0 触碰 SaveSystem 任何 1 字段 0 抛错 0 反向, 
任何"加新 1 preset 字段" 0 触发 SaveSystem migration 0 触碰 0 抛错), 老存档兼容 preload 0 触碰 1:1 严格 (1 `audio_presets.gd` 0 在 SaveSystem 
save dict 中 1:1 严格 0 漏 0 改 + 1 source-grep 验证 1:1 严格 0 漏 0 改, 0 漏 1 preload 守护 0 改 1 字符 0 反序 0 反向), 0 触碰既有 (T291 0 改 
`audio_presets.gd` preload 任何 1 字符, 0 改 SaveSystem save dict 任何 1 字符).
  - 跨 1 套 polish 模式 T291 #214 落地后 0 漂动, §9.6.35 段 1:1 严格 (5 段 1:1 严格分离 + dict default 1:1 严格 + 家族传播 1:1 严格 + prewarm 桶 
cache key 1:1 严格 + UI 显示 1:1 严格 + 老存档兼容 preload 0 触碰 1:1 严格) 100% 闭环, 5 段 1:1 严格分离 (1 Stage 1 dict default 1:1 严格 0 漏 0 
改 0 反序 + 1 Stage 2 家族传播 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 3 prewarm 桶 cache key 1:1 严格 0 漏 0 改 0 反序 + 1 Stage 4 UI 显示 1:1 
严格 0 漏 0 改 0 反序 + 1 Stage 5 老存档兼容 preload 0 触碰 1:1 严格 0 漏 0 改 0 反序). 1 套 polish 模式 0 后续 主要 pre-existing 风险.

- **预防**:
  1. 任何 polish 期给 "加新 1 preset 字段" (e.g. `pad_attack_ms` / `silence_decay_ms` / `reverb_send_db` 等) 或 "加新 1 BGM preset" 
(e.g. 未来 10th preset "coda_dawn") 任务加新 preset 字段 / 加新 BGM preset 时**必须**严格按 5 段 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: 
**Stage 1 0 改 dict default 1:1 严格分离契约** (T291 落地后 `MUSIC_PRESETS` 9 preset × 13 字段已切 1:1 strict 0 漏 0 改 0 改 1 字段 0 漏 1 
字段 0 反序 0 反向, 改 1 字段 / 漏 1 字段 = 1 preset 字段扩展 0 5 段 0 闭环 0 漂动), **Stage 2 0 改家族传播 1:1 严格分离契约** (T291 落地后 
`audio_manager_enhanced.gd` 13 字段引用已切 1:1 strict 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反序 0 反向, 改 1 字段 / 漏 1 字段 = 1 preset 字段扩展 0 
家族传播 0 闭环 0 漂动), **Stage 3 0 改 prewarm 桶 cache key 1:1 严格分离契约** (T291 落地后 `prewarm_music_streams()` 9 preset key 已切 1:1 
strict 0 漏 0 改 0 改 1 cache key 字段 0 漏 1 cache key 字段 0 反序 0 反向, 改 1 cache key 字段 / 漏 1 cache key 字段 = 1 preset 字段扩展 0 
prewarm 桶 cache key 0 闭环 0 漂动), **Stage 4 0 改 UI 显示 1:1 严格分离契约** (T291 落地后 `settings_menu.gd` 9 theme + BGM 主题提示 13 
字段已切 1:1 strict 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反序 0 反向, 改 1 字段 / 漏 1 字段 = 1 preset 字段扩展 0 UI 显示 0 闭环 0 漂动), **Stage 5 0 
改老存档兼容 preload 0 触碰 1:1 严格分离契约** (T291 落地后 `audio_presets.gd` preload 0 触碰 SaveSystem save dict 已切 1:1 strict 0 漏 0 改 0 
改 1 字符 0 漏 1 字符 0 反序 0 反向, 改 1 字符 / 漏 1 字符 = 1 preset 字段扩展 0 老存档兼容 0 闭环 0 漂动). 任何"加新 1 preset 字段"必须**先**按 Stage 1 dict 
default 1:1 加 1 entry × 9 preset (Stage 1 1:1 严格, 0 漏 1 字段 0 改 1 字段 0 反序 0 反向, **0 漏 1 preset**), **再**按 Stage 2 家族传播 
1:1 加 1 字段引用 (Stage 2 1:1 严格, 0 漏 1 字段引用 0 改 1 字段 0 反序 0 反向), **再**按 Stage 3 prewarm 桶 cache key 1:1 加 1 cache key 字段 
(Stage 3 1:1 严格, 0 漏 1 cache key 字段 0 改 1 cache key 字段 0 反序 0 反向), **再**按 Stage 4 UI 显示 1:1 加 1 字段显示 (Stage 4 1:1 严格, 0 
漏 1 字段显示 0 改 1 字段 0 反序 0 反向), **再**按 Stage 5 老存档兼容 preload 0 触碰 1:1 镜像 (Stage 5 1:1 严格, 0 漏 1 preload 守护 0 改 1 字符 0 反序 
0 反向).
  2. **5 段 0 触碰边界** — `audio_presets.gd` `MUSIC_PRESETS` dict + `audio_manager_enhanced.gd` 家族传播 + 
`prewarm_music_streams()` + `settings_menu.gd` BGM 主题提示 + `audio_presets.gd` preload 任何 1 件 0 改 0 删 0 重排 (5 段 0 触碰边界), 
任何"修 1 件"polish 走 Stage 1 1:1 严格 (改 件既有 5 段时**必须**保持 5 段 0 改 0 漏 0 反向 0 反序 0 触碰既有 1 件); 任何"加新 1 边"polish 走 Stage 2 1:1 
严格 (改 边既有 家族传播 时**必须**保持 家族传播 0 改 0 漏 0 反向 0 反序 0 漏 1 边); 任何"加新 1 块"polish 走 Stage 3 1:1 严格 (改 块既有 prewarm 桶 cache key 
时**必须**保持 prewarm 桶 cache key 0 改 0 漏 0 反向 0 反序 0 漏 1 块); 任何"加新 1 段"polish 走 Stage 4 1:1 严格 (改 段既有 UI 显示 时**必须**保持 UI 
显示 0 改 0 漏 0 反向 0 反序 0 漏 1 段); 任何"加新 1 字符"polish 走 Stage 5 1:1 严格 (改 字符既有 老存档兼容 preload 0 触碰 时**必须**保持 老存档兼容 preload 0 
触碰 0 改 0 漏 0 反向 0 反序 0 漏 1 字符).
  3. **0 改 1 字段 0 漏 1 字段 0 反向** (T291 落地后 0 触碰边界) — audio 数据层 preset 字段扩展 5 段 (1 `MUSIC_PRESETS` 9 preset × 13 字段 + 1 
`audio_manager_enhanced.gd` 13 字段引用 + 1 `prewarm_music_streams()` 9 preset key + 1 `settings_menu.gd` 9 theme + BGM 
主题提示 13 字段 + 1 `audio_presets.gd` preload 0 触碰 SaveSystem save dict) 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反向, 改 1 字段 / 
漏 1 字段 / 反向 1 字段 = 1 preset 字段扩展 0 5 段 0 漂动, source-grep 验证 5 段 0 漏 0 改 1 字段 0 漏 1 字段 0 改 1 字符 0 反向.
  4. **0 改 1 边 0 漏 1 边 0 反向** (T291 落地后 0 触碰边界) — 字段扩展契约 (1 `MUSIC_PRESETS` 9 preset × 13 字段 1:1 严格 + 1 
`audio_manager_enhanced.gd` 13 字段引用 1:1 严格 + 1 `prewarm_music_streams()` 9 preset key 1:1 严格 + 1 `settings_menu.gd` 9 
theme + BGM 主题提示 13 字段 1:1 严格 + 1 `audio_presets.gd` preload 0 触碰 SaveSystem save dict 1:1 严格) 1:1 严格分离 0 漏 0 改 0 改 1 边 
0 漏 1 边 0 反向, 改 1 边 / 漏 1 边 / 反向 1 边 = 1 preset 字段扩展 0 字段扩展契约 0 漂动, source-grep 验证 5 段 0 触碰字段扩展契约 0 漏 0 改 1 边 0 漏 1 边 0 
改 1 字符 0 反向.
  5. **T162 brittle 修复流程 0 触碰边界** — audio 数据层 preset 字段扩展 0 副作用 子段 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 
触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构 (T162 brittle 修复流程 = 
1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), 改 1 字段 / 漏 1 字段 / 
反向 1 字段 = 1 preset 字段扩展 0 0 副作用 0 漂动 0 闭环, source-grep 验证 audio 数据层 preset 字段扩展 0 副作用 子段 0 漏 0 改 1 字段 0 改 1 字符 0 反向 0 漏 
1 字段 0 触碰既有 0 改 1 段 0 触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构.
  6. **1 套 polish 模式 0 互混 0 复用 0 共享 唯一性 标注 0 改 1 段 0 漏 1 段 0 反向** — §9.6.35 段 唯一性 标注 1:1 严格分离 0 漏 0 改 0 改 1 套 polish 模式 
0 漏 1 套 polish 模式 0 反向 0 触碰既有 0 改 1 段 (本节是 26 套 polish 模式**唯一**关注 audio 数据层 preset 字段扩展 5 段 canonical 1:1 严格分离契约 的 1 套, 
0 互混 0 复用 0 共享), 改 1 套 polish 模式 / 漏 1 套 polish 模式 / 反向 1 套 polish 模式 = 1 §9.6.35 段 0 唯一性 标注 0 漂动 0 闭环, source-grep 验证 
§9.6.35 段 唯一性 标注 0 漏 0 改 1 套 polish 模式 0 漏 1 套 polish 模式 0 改 1 字符 0 反向.
  7. **§9.6.35 是 26 套 polish 模式**唯一**关注 "audio 数据层 (`MUSIC_PRESETS` 字典) preset 字段扩展 5 段 canonical 1:1 严格分离契约 
(`MUSIC_PRESETS` 9 preset × 13 字段 + `audio_manager_enhanced.gd` 13 字段引用 + `prewarm_music_streams()` 9 preset key + 
`settings_menu.gd` 9 theme + BGM 主题提示 13 字段 + `audio_presets.gd` preload 0 触碰 SaveSystem save dict) + dict default 1:1 
严格 (9 preset × 13 字段 1:1 严格 0 漏 0 改) + 家族传播 1:1 严格 (13 字段引用 1:1 严格 0 漏 0 改) + prewarm 桶 cache key 1:1 严格 (9 preset key 
1:1 严格 0 漏 0 改) + UI 显示 1:1 严格 (9 theme + 13 字段 1:1 严格 0 漏 0 改) + 老存档兼容 preload 0 触碰 1:1 严格 (`audio_presets.gd` 0 在 
SaveSystem save dict 中 1:1 严格 0 漏 0 改) + 0 触碰游戏代码 + 0 触碰 §10 决策记录流程 0 触碰既有" 的 1 套** — 与 §9.6.6 / §9.6.7 / §9.6.8 / 
§9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / 
§9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 25 套 polish 模式 0 互混 0 
复用 0 共享. §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 8 套关注 "具体 game / pause_menu polish 
模式 (1 panel × 3 组件 × 1:1 视觉组连贯 / 6 verb hit handler / 6 verb signal 桥接 / SaveSystem audit_save_slots / PauseMenu 跨面板 
`_alpha_base` / ProfileQuickStats 4 段独立 hover 联动 / ProfileRecentList 5 行 row 文本 5→7 字段 / ProfileRecentList 5 行 row 
hover feedback 三件套)" (与 audio 数据层 preset 字段扩展 5 段 0 相关, 8 套具体 polish 模式 0 涉及 5 段 1:1 严格分离契约 自身 1:1 严格分离), §9.6.18 / 
§9.6.19 / §9.6.22 / §9.6.24 4 套关注 "6 verb 接入 单层 字节码 (16 件套 ability base / 7 件套 windup VFX base / 5 段 VFX 视觉组 / 5 段 HUD 
36 通道)" (与 audio 数据层 preset 字段扩展 5 段 0 相关, 4 套 6 verb 单层 字节码 0 涉及 audio 5 段 自身 1:1 严格分离), §9.6.20 / §9.6.21 / §9.6.25 / 
§9.6.26 / §9.6.27 5 套关注 "6 verb 跨组件 / 跨房间 / 跨段 视觉组 (Hub ↔ archive 跨房间 transition / 6 verb 成就系统 闭环 8 段 / 6 verb 视觉组连贯 
tooltip 8 行拼接 5 段 / 6 verb icon 视觉组 5 段 / PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯)" (与 audio 数据层 preset 字段扩展 5 段 
0 相关, 5 套跨组件 0 涉及 5 段**之间**字段扩展契约 自身 1:1 严格分离), §9.6.23 关注 "7 桶 prewarm 5+1+1 (prewarm_music_streams 第 1 桶 / 
prewarm_hit_sfx 第 2 桶 / prewarm_shop_sfx 第 3 桶 / prewarm_misc_sfx 第 4 桶 / prewarm_verb_cooldown_tails 第 5 桶 / 
prewarm_verb_fire_sfx 第 6 桶 / prewarm_verb_cooldown_readys 第 7 桶)" (与 audio 数据层 preset 字段扩展 5 段 **Stage 3 prewarm 桶 
cache key 1:1 严格** 1:1 镜像, 1 套 polish 模式覆盖 prewarm 桶聚合 0 涉及 audio 5 段 自身 1:1 严格分离), §9.6.28 / §9.6.29 / §9.6.30 3 套关注 
"工具链 / smoke test / docs polish (smoke test ITERATION_COUNT 跨迭代 + 段边界 find 5 段 / polish 文档化 5 段 canonical 1:1 序列 模板 / 
工具链 3 件套 1:1 严格分离契约)" (与 audio 数据层 preset 字段扩展 5 段 0 相关, 3 套工具链 0 涉及 5 段**之间**字段扩展契约 自身 1:1 严格分离), §9.6.31 / §9.6.32 2 
套关注 "文档归档 3 件套 1:1 严格分离契约 (CHANGELOG 归档 3 件套 1:1 严格分离契约 / REVIEW_LOG 归档 3 件套 1:1 严格分离契约)" (与 audio 数据层 preset 字段扩展 5 段 
0 相关, 2 套文档归档 0 涉及 5 段**之间**字段扩展契约 自身 1:1 严格分离), §9.6.33 关注 "6 verb 接入 4 件套 字节码一致性 1:1 严格分离契约" (与 audio 数据层 preset 字段扩展 
5 段 0 相关, 4 件套 0 涉及 audio 5 段 自身 1:1 严格分离), §9.6.34 关注 "PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约" (与 audio 数据层 
preset 字段扩展 5 段 0 相关, §9.6.34 6 段是数据层字段扩展但 0 涉及 audio 数据层 5 段**之间**字段扩展契约 自身 1:1 严格分离). §9.6.35 关注 "audio 数据层 preset 
字段扩展 5 段 canonical 1:1 严格分离契约 5 段 1:1 视觉组连贯" (与具体 game / pause_menu / 6 verb 接入 单层 / 6 verb 跨组件 / 工具链 / 文档归档 / 
PlayerStats 持久化层 polish 0 相关, 5 段 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格). §9.6.35 是 26 
套 polish 模式**唯一**关注 "audio 数据层 preset 字段扩展 5 段 canonical 1:1 严格分离契约 (`MUSIC_PRESETS` 9 preset × 13 字段 + 
`audio_manager_enhanced.gd` 13 字段引用 + `prewarm_music_streams()` 9 preset key + `settings_menu.gd` 9 theme + BGM 主题提示 13 
字段 + `audio_presets.gd` preload 0 触碰 SaveSystem save dict) + dict default 1:1 严格 (9 preset × 13 字段 1:1 严格 0 漏 0 改) + 
家族传播 1:1 严格 (13 字段引用 1:1 严格 0 漏 0 改) + prewarm 桶 cache key 1:1 严格 (9 preset key 1:1 严格 0 漏 0 改) + UI 显示 1:1 严格 (9 theme 
+ 13 字段 1:1 严格 0 漏 0 改) + 老存档兼容 preload 0 触碰 1:1 严格 (`audio_presets.gd` 0 在 SaveSystem save dict 中 1:1 严格 0 漏 0 改) + 0 
触碰游戏代码 + 0 触碰 §10 决策记录流程 0 触碰既有" 的 1 套, 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / 
§9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / 
§9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 25 套 ProfileRecentList 5 行 / ProfileQuickStats 4 段 / AchievementGrid / 
6 verb 单层 字节码 / 6 verb 跨组件 / SaveSystem / 跨房间 transition / PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 / smoke test 
ITERATION_COUNT 跨迭代 + 段边界 find / polish 文档化 5 段 canonical 1:1 序列 模板 / 工具链 3 件套 1:1 严格分离契约 / CHANGELOG 归档 3 件套 1:1 
严格分离契约 / REVIEW_LOG 归档 3 件套 1:1 严格分离契约 / 6 verb 接入 4 件套 字节码一致性 / PlayerStats 持久化层字段扩展 6 段 0 互混 0 复用 0 共享. 任何"加新 1 
preset 字段"或"加新 1 BGM preset"必须**先**按 §9.6.35 Stage 1 dict default 1:1 严格 加 1 entry × 9 preset (Stage 1 1:1 严格, 0 漏 1 字段 
0 改 1 字段 0 反序 0 反向, **0 漏 1 preset**), **再**按 §9.6.35 Stage 2 家族传播 1:1 严格 加 1 字段引用 (Stage 2 1:1 严格, 0 漏 1 字段引用 0 改 1 字段 
0 反序 0 反向), **再**按 §9.6.35 Stage 3 prewarm 桶 cache key 1:1 严格 镜像 (Stage 3 1:1 严格, 0 触碰既有 0 漏 1 cache key 字段 0 改 1 cache 
key 字段 0 反向), **再**按 §9.6.35 Stage 4 UI 显示 1:1 严格 镜像 (Stage 4 1:1 严格, 0 触碰既有 0 漏 1 字段显示 0 改 1 字段 0 反向), **再**按 §9.6.35 
Stage 5 老存档兼容 preload 0 触碰 1:1 严格 镜像 (Stage 5 1:1 严格, 0 触碰既有 0 漏 1 preload 守护 0 改 1 字符 0 反向).
  8. 已知 drift risk: audio 数据层 preset 字段扩展 **可能**漏 1 段 / 1 边 1:1 镜像 (e.g. 加新 1 preset 字段时漏 Stage 1 dict default 中 9 
preset 镜像中 1 preset 0 镜像, 1 preset 字段 0 5 段 0 闭环; 加新 1 preset 字段时漏 Stage 2 家族传播 中 13 字段引用 0 镜像, 1 preset 字段 0 家族传播 0 闭环;
 加新 1 preset 字段时漏 Stage 3 prewarm 桶 cache key 中 9 preset key 0 镜像, 1 preset 字段 0 prewarm 桶 cache key 0 闭环; 加新 1 preset 
字段时漏 Stage 4 UI 显示 中 9 theme 0 镜像, 1 preset 字段 0 UI 0 闭环; 加新 1 preset 字段时漏 Stage 5 老存档兼容 preload 0 触碰 0 守护, 1 preset 字段 
0 老存档兼容 0 闭环). 监控建议: 任何"未来给 audio 数据层加新 1 preset 字段 / 加新 1 BGM preset / 加新 1 字段引用 / 加新 1 字段显示"必须**先**在 5 段 (Stage 1 
dict default 1:1 严格 + Stage 2 家族传播 1:1 严格 + Stage 3 prewarm 桶 cache key 1:1 严格 + Stage 4 UI 显示 1:1 严格 + Stage 5 老存档兼容 
preload 0 触碰 1:1 严格) 1:1 镜像 0 漏 1 段 / 0 漏 1 边 / 0 漏 1 字段 / **0 漏 1 preset**. T291 (#214) 5 段 1:1 镜像落地后任何新 preset 字段 / 新 
BGM preset / 新字段引用 / 新字段显示 / 新 5 段同步 是 §9.6.35 5 段的"母集" (1 preset 字段扩展 × 5 段 = 5 元素 1:1 严格, §9.6.35 5 段 1 套 polish 模式 
完整闭环, 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格, 含 Stage 1 dict default 1:1 严格 1 套 polish 模式 5 段 0 漏 0 改 + Stage 2 家族传播 1:1 严格 
1 套 polish 模式 家族传播 0 漏 0 改 + Stage 3 prewarm 桶 cache key 1:1 严格 1 套 polish 模式 prewarm 桶 cache key 0 漏 0 改 0 反向 0 触碰既有 0 
触碰既有 0 改 1 段 + Stage 4 UI 显示 1:1 严格 1 套 polish 模式 UI 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 段 + Stage 5 老存档兼容 preload 0 触碰 
1:1 严格 1 套 polish 模式 老存档兼容 0 漏 0 改 0 反向 0 触碰既有 0 触碰既有 0 改 1 段). 另 1 类已知 drift: Stage 1 dict default 1:1 严格 0 改 1 字段 0 漏 
1 字段 0 反向 (e.g. 后续 polish 期加新 1 preset 字段时漏 0 镜像 9 preset 任何 1 preset, 1 preset 字段 0 dict default 0 闭环; 或漏 0 镜像 13 字段任何 
1 字段, 1 preset 字段 0 dict default 0 闭环; 或改 1 字段 0 改 1 字段 0 反向 0 触碰既有), 后续 polish 期若加新 1 preset 字段必须**先**用 1:1 镜像 5 段 模板, 
0 改既有 0 漏 1 段 0 漏 1 边 0 反向 **0 漏 1 preset**.

**0 副作用**: T291 (#214) 任何 const / var 0 触碰 (`audio_presets.gd` `MUSIC_PRESETS` 0 改 0 删 0 重排 9 preset × 13 字段任何 1 entry; 
`audio_presets.gd` `BOSS_MUSIC_TIER` 0 改 0 删 0 重排任何 1 entry; `audio_manager_enhanced.gd` 0 改 0 删 0 重排 13 字段引用任何 1 字符; 
`prewarm_music_streams()` 0 改 0 删 0 重排 9 preset key 任何 1 字符; `settings_menu.gd` 0 改 0 删 0 重排 9 theme + BGM 主题提示 13 字段任何 
1 字符; CONTRIBUTING.md §9.6.35 段 0 改 0 删 0 重排, 仅新加 1 段 (~40 行)); 0 触碰 §9.6.6 ProfileRecentList 5 行 row 文本 5→7 字段 format 
字符串扩展模式 (T291 0 改 §9.6.6 任何 1 字符); 0 触碰 §9.6.7 ProfileRecentList 5 行 row hover feedback 三件套 (T291 0 改 §9.6.7 任何 1 字符); 
0 触碰 §9.6.8 ProfileQuickStats 4 段独立 hover 联动 (T291 0 改 §9.6.8 任何 1 字符); 0 触碰 §9.6.9 AchievementGrid + ProfileRecentList 
跨面板 `_alpha_base` Dictionary 双源 (T291 0 改 §9.6.9 任何 1 字符); 0 触碰 §9.6.10 「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (T291 0 
改 §9.6.10 任何 1 字符); 0 触碰 §9.6.15 SaveSystem audit_save_slots (T291 0 改 §9.6.15 任何 1 字符); 0 触碰 §9.6.16 6 verb hit 
handler 5 件套 (T291 0 改 §9.6.16 任何 1 字符); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T291 0 改 §9.6.17 任何 1 字符); 0 触碰 §9.6.18 
16 件套 verb ability base (T291 0 改 §9.6.18 任何 1 字符); 0 触碰 §9.6.19 7 件套 windup VFX base (T291 0 改 §9.6.19 任何 1 字符); 0 触碰 
§9.6.20 Hub ↔ archive 跨房间 transition flow (T291 0 改 §9.6.20 任何 1 字符); 0 触碰 §9.6.21 8 段成就序列 (T291 0 改 §9.6.21 任何 1 字符); 
0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T291 0 改 §9.6.22 任何 1 字符); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T291 0 改 §9.6.23 任何 1 字符); 
0 触碰 §9.6.24 HUD 6 verb × 6 通道 = 36 通道 5 段序列 (T291 0 改 §9.6.24 任何 1 字符); 0 触碰 §9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 5 段序列 
(T291 0 改 §9.6.25 任何 1 字符); 0 触碰 §9.6.26 6 verb icon 视觉组 5 段 canonical 1:1 序列 (T291 0 改 §9.6.26 任何 1 字符); 0 触碰 §9.6.27 
PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 5 段序列 (T291 0 改 §9.6.27 任何 1 字符); 0 触碰 §9.6.28 smoke test ITERATION_COUNT 
跨迭代 + 段边界 find 5 段序列 (T291 0 改 §9.6.28 任何 1 字符); 0 触碰 §9.6.29 polish 文档化 5 段 canonical 1:1 序列 模板 + 4 段关系段 模板 (T291 0 改 
§9.6.29 任何 1 字符); 0 触碰 §9.6.30 工具链 3 件套 1:1 严格分离契约 (T291 0 改 §9.6.30 任何 1 字符); 0 触碰 §9.6.31 CHANGELOG 归档 3 件套 1:1 
严格分离契约 (T291 0 改 §9.6.31 任何 1 字符); 0 触碰 §9.6.32 REVIEW_LOG 归档 3 件套 1:1 严格分离契约 (T291 0 改 §9.6.32 任何 1 字符); 0 触碰 §9.6.33 
6 verb 接入 4 件套 字节码一致性 (T291 0 改 §9.6.33 任何 1 字符); 0 触碰 §9.6.34 PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约 (T291 0 改 
§9.6.34 任何 1 字符); 0 触碰 check_smoke_consistency.sh (T291 0 改任何规则); 0 触碰 _parse_recent_section.py + 
pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 
任何 gameplay code 0 改, 0 触碰 `audio_presets.gd` 任何 1 字段, 0 触碰 `audio_manager_enhanced.gd` 任何 1 字符, 0 触碰 
`settings_menu.gd` 任何 1 字符); 0 行为变化, 1 文档 1 段 (§9.6.35 ~40 行) + 0 字节码修改 (T291 仅文档化既有 audio 数据层 preset 字段扩展 5 段 
canonical polish 模式, 0 改 `MUSIC_PRESETS` 9 preset × 13 字段 0 改 `audio_manager_enhanced.gd` 13 字段引用 0 改 
`prewarm_music_streams()` 9 preset key 0 改 `settings_menu.gd` 9 theme 0 触碰既有) + 1 smoke test (T291 30+ 断言).

**与 §9.6.23 关系** — §9.6.23 (T279 #198 落地) 关注 "7 桶 prewarm 5+1+1 (prewarm_music_streams 第 1 桶 / prewarm_hit_sfx 第 2 桶 / 
prewarm_shop_sfx 第 3 桶 / prewarm_misc_sfx 第 4 桶 / prewarm_verb_cooldown_tails 第 5 桶 / prewarm_verb_fire_sfx 第 6 桶 / 
prewarm_verb_cooldown_readys 第 7 桶)" (prewarm 桶聚合 0 涉及 audio 数据层 preset 字段扩展). §9.6.35 关注 "audio 数据层 preset 字段扩展 5 段 
canonical 1:1 严格分离契约" (audio 数据层字段扩展, Stage 3 prewarm 桶 cache key 1:1 严格 镜像 `prewarm_music_streams()` 9 preset key). 
§9.6.23 与 §9.6.35 0 互混 0 复用 0 共享: §9.6.23 关注 "7 桶 prewarm 5+1+1 桶聚合" (与 audio 数据层 preset 字段扩展 5 段 0 相关, 7 桶聚合 0 涉及 dict 
default / 家族传播 / prewarm 桶 cache key / UI 显示 / 老存档兼容 preload 0 触碰 5 段), §9.6.35 关注 "audio 数据层 preset 字段扩展 5 段 canonical 
1:1 严格分离契约 5 段 1:1 视觉组连贯" (与 7 桶 prewarm 0 相关, 5 段 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 5 段 = 5 元素 1:1 
严格). §9.6.35 是 §9.6.23 7 桶的"上游" — §9.6.23 第 1 桶关注 "prewarm_music_streams 遍历 `MUSIC_PRESETS.keys()` 9 preset 1:1 严格 0 漂动 
0 闭环", §9.6.35 Stage 3 关注 "prewarm 桶 cache key 1:1 严格 0 漂动 0 闭环". 任何"加新 1 preset 字段"必须**先**按 §9.6.23 7 桶 1:1 复制 7 桶 (7 
桶 1:1 严格, 0 漏 1 桶 0 改 1 桶), **再**按 §9.6.35 5 段 1:1 同步 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 
§9.6.23 任何 1 字符).

**与 §9.6.34 关系** — §9.6.34 (T290 #213 落地) 关注 "PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约" (持久化层字段扩展). §9.6.35 关注 
"audio 数据层 preset 字段扩展 5 段 canonical 1:1 严格分离契约" (audio 数据层字段扩展). §9.6.34 与 §9.6.35 0 互混 0 复用 0 共享: §9.6.34 关注 
"PlayerStats 持久化层字段扩展 6 段 canonical 1:1 严格分离契约 (`_best_stats` dict 5 字段 + `_update_best_stats_from_current_run` 5 比较 + 
`_capture_run_into_history` 7 字段 + `pause_menu.gd` 4 段顶级行 + `get_best_stats` docblock 5 字段说明 + `_load_best_stats` 5 字段 
fallback)" (与 audio 数据层 preset 字段扩展 5 段 0 相关, 6 段 0 涉及 MUSIC_PRESETS / audio_manager_enhanced.gd / 
prewarm_music_streams / settings_menu.gd 5 段), §9.6.35 关注 "audio 数据层 preset 字段扩展 5 段 canonical 1:1 严格分离契约 5 段 1:1 
视觉组连贯" (与 PlayerStats 持久化层 0 相关, 5 段 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离). §9.6.34 与 §9.6.35 是 "姊妹段" (1 套 polish 模式 × 6 
段 = 6 元素 1:1 严格 镜像 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格, 一是 PlayerStats 持久化层 字段扩展 6 段, 一是 audio 数据层 字段扩展 5 段; 1 套 polish 
模式 × 6 段 = 6 元素 1:1 严格 + 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格). 任何"加新 1 字段"必须**先**按 §9.6.34 6 段 1:1 复制 (6 段 1:1 严格, 0 漏 1 
段 0 改 1 字段 0 反向) **或** 按 §9.6.35 5 段 1:1 复制 (5 段 1:1 严格, 0 漏 1 段 0 改 1 字段 0 反向), **再**按 §9.6.35 5 段 1:1 同步 (5 段 1:1 严格, 
0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.34 任何 1 字符) **或** 按 §9.6.34 6 段 1:1 同步 (6 段 1:1 严格, 0 漏 1 段 0 漏 1 边 
0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.35 任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 
0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 轮 0 后续 持续收敛), §9.6.35 是"audio 数据层 preset 字段扩展 5 段 canonical 1:1 严格分离契约" (5 段 1:1 
严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约). T162 与 §9.6.35 0 互混 0 复用 0 共享: T162 关注 "brittle 修复流程 5 步骤" (1 expect 反转 + 1 
docblock 说明 + 1 段 find 反转 + 0 触碰既有 + 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 5 步骤 1:1 严格), §9.6.35 关注 "5 段 
1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约" (与 5 步骤 0 相关, 1 套 polish 模式 × 5 段 = 5 元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离). 任何"加新 1 
brittle 修复"必须**先**按 T162 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点), **再**按 §9.6.35 5 
段 1:1 镜像 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.35 5 段走 §9.1 9 步落地的 1 步 (`audio` 步), 0 走 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 
`const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `vfx` 视觉组 + 1 `HUD 冷光勾边` 渲染). 任何"加新 1 preset 字段"必须**先**按 
§9.1 9 步落地的 1 步 1:1 复制 (1 `audio` preset 字段 entry), **再**按 §9.6.35 5 段 1:1 同步 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 
1 字符 0 反向 0 触碰既有 8 步 (`verb` 枚举 + `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `vfx` 视觉组 + `HUD 
冷光勾边` 渲染)). 任何"加新 1 audio 步"必须**先**在 §9.6.35 5 段加 1 步 (1 `audio` preset 字段 entry), 0 触碰既有 8 步 (1 `verb` 枚举 + 1 
`ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `vfx` 视觉组 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).
