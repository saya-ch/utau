# Polish Patterns 9.6.24–24

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.24–9.6.24，共 1 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.24-24.md

<!-- shard 24-24 共 1 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.24 6 verb HUD 6 行 6 色色域分工 6 通道 (5 段 canonical 1:1 序列) polish 模式 (T200 + T202 + T204 + T206 + T233 + T247 跨 6 任务 
~48 轮落地) 文档化

> §9.6.3 记录 HUD 6 verb 5+1 verb 7 UI 通道同步扩 verb 模式 (加新 1 verb 时 6 处 1:1 复制, T247 #164 落地模式文档化); §9.6.4 记录 6 verb 调色六元组 
> + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 2 4 通道 + 2 派生 = 6 通道 per verb); §9.6.5 记录 6 verb 视觉组连贯 tooltip 8 行拼接扩展器; 
> §9.6.16 记录 player.gd 6 verb hit handler 5 件套 1:1; §9.6.22 记录 6 verb VFX 视觉组 5 段 canonical 1:1 序列. 本节记录 
> `src/scripts/hud.gd` 6 verb HUD 6 行 6 色色域分工 6 通道 (5 段 canonical 1:1 序列) polish 模式 — 跨 5 段 (Stage 1 6 verb row 结构 1:1 
> + Stage 2 6 verb 调色六元组 1:1 + Stage 3 6 verb 6 通道 per verb 1:1 = 6 verb × 6 通道 = 36 通道 1:1 + Stage 4 6 verb glow state 
> 6 key + stylebox 6 instance 1:1 + Stage 5 6 verb reduce_flash iteration list 8 element 1:1 + 3 闭环宪法同步) — 跨 6 任务 (T200 
> #117 + T202 #118 + T204 #119 + T206 #123 + T233 #152 + T247 #164) ~48 轮迭代落地, 36 通道 1:1 严格分离 (6 verb × 6 通道 = 36 通道, 0 
> 漏 0 重叠 0 互混 0 复用 0 共享), 0 漂动 0 后续 主要 pre-existing 风险. 任何"加新 1 verb HUD 接入"或"修 1 verb HUD 6 通道"或"加新 1 HUD UI 元素 
> (reduce_flash 范围扩展)"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格), **再**按 §9.6.3 5+1 verb 7 UI 通道 6 处 1:1 复制 (Stage 
> 1 + 3 1:1 严格, 0 反序 0 漏 0 改 1 字段), **再**按 Stage 4 glow 1:1 严格, **再**按 Stage 5 reduce_flash 1:1 严格, **再**按 §9.6.22 6 
> verb VFX 视觉组 1:1 镜像 (Stage 2 调色 + Stage 3 6 通道 1:1 严格, 0 漏 0 改 1 hex 0 改 1 通道值), **再**按 §9.6.24 5 段序列 1:1 复制 (polish 
> 模式 5 段 1:1 严格, 0 反序 0 漏 0 改 1 字段). 跨 6 verb (Pulse Coral Pulse #E86D5A / Bind Muted Violet #65506A / Cut Amber Voice 
> #F2B66E / Echo Glass Cyan #69C7CE / Wave Pale Resonance #B7E6DC / Whisper Muted Mauve #C8A4D8) + 6 verb 6 通道 (icon 
> sprite modulate / name label font_color / ProgressBar fill bg_color / cooldown label text color / cold glow border 
> 1px bg_color / reduce_flash modulate overlay) + 6 verb reduce_flash iteration list 8 element (5 verb bar + 1 whisper 
> + 1 resonance + 1 health) 0 漏 1 verb 全部走同一套 5 段 1:1 序列 0 例外.

- **症状**: `src/scripts/hud.gd` 6 verb HUD 6 行 6 色色域分工 6 通道 跨 6 verb 反复 polish 期间出现 6 类典型 fragile: (1) "加新 1 verb HUD 漏 
5 段序列同步" — 新 verb 接入 HUD 时仅加 @onready var + 主题色 const + 调 _create_verb_glow_stylebox 3 件 (T247 #164 6 步 1:1 复制), 但漏 
Stage 4 `_verb_glow_state` dict 6 key 加 1 key (T247 落地时漏 dict 加 `"whisper": false` 触发 Whisper 冷光勾边 0 在 _process 跑 
state-change tween 0 触发) / 漏 Stage 5 reduce_flash iteration list 8 element 加 1 element (T247 落地时漏 `_whisper_cooldown` 
加进 list 触发 Whisper 0 走 reduce_flash 灰化, accessibility 玩家勾选 "减弱屏幕闪烁" 时 Whisper bar 仍 100% 饱和度, 与 5 verb 不一致); (2) "修 1 
verb 主题色漏 §9.6.4 调色六元组同步" — 修 Pulse `_PULSE_GLOW_COLOR` 时改硬编码 hex 0 改 §9.6.4 调色六元组 entry, 跨段 §9.6.22 6 verb VFX 视觉组 5 段 
+ §9.6.5 tooltip 视觉组连贯 8 行拼接 + §9.6.3 5+1 verb 7 UI 通道全部漂动 (Pulse icon / Pulse fill / Pulse glow border / Pulse name 
label / Pulse cooldown label / Pulse tooltip color 6 通道全 漂动, 玩家扫 HUD 时 Pulse 主色与既有 6 verb 调色六元组 0 一致); (3) "Stage 3 6 
通道 1 verb 漏 1 通道" — 新 verb 接入时只画 icon + fill + glow 3 通道, 漏 name label 主题色 (T204 #119 5 verb 模式) / 漏 cooldown label 主题色 
(T202 #118 5 verb 模式) / 漏 reduce_flash 灰化 (T200 #117 5 verb 模式) 3 派生通道, 玩家 hover 该 verb 时看不到 verb 名称 (0 name label) / 
冷却时看不到 "冷却中" 文字 (0 cooldown label) / accessibility 模式下 bar 仍 100% 饱和度 (0 reduce_flash) = 6 通道 缺 3 通道 视觉组连贯破坏; (4) 
"Stage 4 glow 0 改 1 verb 调色 hex 漏 `_verb_glow_state` dict key 同步" — 加新 verb 时加 `_whisper_glow_bg = 
_create_verb_glow_stylebox(_WHISPER_GLOW_COLOR)` 1 行 + `_whisper_cooldown.add_theme_stylebox_override("background", 
_whisper_glow_bg)` 1 行, 0 在 `_verb_glow_state` dict 加 `"whisper": false` 1 key, 触发 _process 跑 
`_update_verb_glow_state("whisper", ...)` 时 0 在 dict 找到 key 抛 KeyError 或 silent skip 0 冷光勾边, 玩家看不到 verb 主题色 glow 反馈 (与 
5 verb 1:1 视觉组连贯崩塌); (5) "Stage 5 reduce_flash 范围扩展漏 1 element" — 修 accessibility reduce_flash 范围时 (e.g. T206 #123 把 
range 从 5 verb bar 扩到 5 verb + resonance + health) 漏 1 element (e.g. 漏 `_health_container` 加进 list 触发 HUD 顶部生命条仍 100% 
饱和度, 与 5 verb bar + resonance 不一致, accessibility 玩家勾 "减弱屏幕闪烁" 时仍看到生命条高饱和小铃铛), iteration list 元素 0 触碰 既有 N element 顺序 (0 
反序 0 漏); (6) "Stage 1 6 verb row tscn 漏 4 子节点" — tscn 加 `WhisperRow` HBoxContainer 时漏 4 子节点 (`WhisperIcon` TextureRect 
/ `WhisperNameLabel` Label / `WhisperCooldown` ProgressBar / `WhisperCooldownLabel` Label) 1 段, 触发 @onready var path 
`$MarginContainer/VBoxContainer/WhisperRow/Whisper<Elem>` 多层 path 缺 1 段 runtime "Node not found" 错. 6 类症状根因: 6 verb HUD 
6 行 6 色色域分工 6 通道 5 段 canonical 1:1 序列 0 文档化, polish 期只能"凭记忆"同步 N 处 (1 Stage 1 tscn + 1 Stage 2 调色 const + 1 Stage 3 6 
通道 @onready var / var / ability ref + 1 Stage 4 glow dict + 1 Stage 5 iteration list 1 元素 + 1 §9.6.22 跨段镜像 0 漏 1 边), 1 
漏 1 = 6 verb HUD 6 行 6 色色域分工 6 通道 漂动.
- **触发场景**: `src/scripts/hud.gd` 6 verb HUD 6 行 6 色色域分工 6 通道 5 段 canonical 1:1 序列落地需同步 5 件套: (1) **`hud.gd` 6 verb 
`@onready var` 6 通道 per verb (4 主 + 2 派生) 1:1 严格分离** — 6 verb 6 row × 4 UI 元素 = 24 `@onready var` 1:1 严格 (icon / name 
label / cooldown bar / cooldown label) + 6 verb 6 ability reference `var _<verb>_ability` 1:1 严格 + 6 verb 6 topic-color 
const `_PULSE_GLOW_COLOR` / `_BIND_GLOW_COLOR` / `_CUT_GLOW_COLOR` / `_ECHO_GLOW_COLOR` / `_WAVE_GLOW_COLOR` / 
`_WHISPER_GLOW_COLOR` 6 entry 1:1 严格 (T247 #164 落地时 Whisper Muted Mauve #C8A4D8 1 hex 0 撞 5 verb 调色, `STYLE_GUIDE.md` 
§9.6.4 调色六元组 6 entry 1:1 对齐); (2) **Stage 4 6 verb glow state 6 key + stylebox 6 instance 1:1 严格** — `_verb_glow_state: 
Dictionary` 6 key (pulse / bind / cut / echo / wave / whisper) 0 漏 1 verb + 6 verb glow stylebox 6 instance 
(`_pulse_glow_bg` / `_bind_glow_bg` / `_cut_glow_bg` / `_echo_glow_bg` / `_wave_glow_bg` / `_whisper_glow_bg`) 0 漏 1 
verb + 6 verb `_create_verb_glow_stylebox` 6 调点 0 漏 + 6 verb `add_theme_stylebox_override("background", 
_<verb>_glow_bg)` 6 调点 0 漏 (T233 #152 落地 5 verb 冷光勾边 + T247 #164 加 Whisper 第 6 verb 1:1 复制, 0 改 5 verb 既有字段); (3) 
**Stage 5 reduce_flash iteration list 1:1 严格** — `_apply_reduced_flash_modulate` 8 element list (5 verb bar + 1 whisper 
+ 1 resonance + 1 health, T200 #117 落地 5 element + T206 #123 扩 7 element + T247 #164 扩 8 element 跨 3 任务迭代沉淀) 0 漏 1 
element 0 反序 0 改 1 element, 1 次 modulate 写 = 6 verb 6 bar + 1 resonance + 1 health = 8 element 全灰化 (T200 5 element 起步 → 
T206 7 element 范围扩展 → T247 8 element 加 Whisper 第 6 verb, 跨 3 任务 0 漂动); (4) **Stage 1 6 verb row tscn 4 子节点 1:1 严格** — 
`hud.tscn` 6 verb 6 row (PulseRow / BindRow / CutRow / EchoRow / WaveRow / WhisperRow) 0 漏 1 row + 6 verb 6 row 4 子节点 
(Icon / NameLabel / Cooldown / CooldownLabel) 0 漏 1 节点 0 改 1 节点类型 (TextureRect / Label / ProgressBar / Label) 0 改 0 漏 1 
path 段; (5) **5 段序列 1:1 同步跨段镜像 §9.6.22 6 verb VFX 视觉组 + §9.6.5 tooltip 视觉组连贯 8 行拼接** — 1 verb 1 跨段 3 边 (与 §9.6.22 6 
verb `@export` color 字段 1:1 镜像 Stage 2 调色六元组 0 漏 1 verb + 与 §9.6.5 `_VERB_ACHV_INFO` 6 字段 (achv_id / verb_index / color 
/ color_name / geometry_zh / visual_group) 1:1 镜像 Stage 2 调色 + 视觉组连贯 0 漏 1 verb 0 漏 1 字段). 5 件套 1:1 同步, 1 漏 1 = 6 verb 
HUD 6 行 6 色色域分工 6 通道 漂动 (Stage 1 tscn 漏 1 row / Stage 2 调色 撞色 / Stage 3 6 通道 漏 1 通道 / Stage 4 glow 0 冷光勾边 / Stage 5 
reduce_flash iteration list 漏 1 element / 跨段镜像漏 1 边).
- **修复** (跨 6 任务 ~48 轮落地后已知 5 段 canonical 1:1 序列 + 36 通道 0 例外):
  - **T200 (#117) Stage 5 reduce_flash 5 verb 起步** — `src/scripts/hud.gd` 加 `_REDUCED_COLOR_MODULATE := Color(0.55, 
0.55, 0.6, 0.75)` + `_NORMAL_COLOR_MODULATE := Color(1.0, 1.0, 1.0, 1.0)` 2 const + `_reduced_flash_applied: bool = 
false` 1 var + `_apply_reduced_flash_modulate()` 函数 5 element iteration list (5 verb bar) + 
`GameState._on_reduce_flash_changed(reduce: bool)` 信号连接 0.12s tween, 5 verb bar 0 触碰既有 verb 调色 (饱和度降 30% + 透明度 75%, 玩家勾 
"减弱屏幕闪烁" 时 5 verb bar 1 次 modulate 写 = 5 bar 全灰化, 6 verb 调色六元组 0 改 1 hex 0 改 1 通道值), 0 触碰 GameState.health / 
GameState.resonance 既有 铃铛 + ResonanceBar (T200 rationale "常驻/瞬时二分 = constant state 不灰化", 后续 T206 #123 扩展才覆盖).
  - **T202 (#118) Stage 3 派生通道 1/2 cooldown label 主题色 5 verb** — `src/scripts/hud.gd` 加 5 verb `_pulse_cooldown_label` 
/ `_bind_cooldown_label` / `_cut_cooldown_label` / `_echo_cooldown_label` / `_wave_cooldown_label` 5 `@onready var` + 5 
verb 主题色文本 (cooldown > 0 时 modulate alpha 0.6 显示 "冷却中" 给玩家即时反馈) + 6 通道 per verb 第 4 通道 1:1 严格分离 (与 T204 name label 主题色 
0 互混 0 复用, 1 通道 1 派生), 0 触碰 T200 reduce_flash 5 element 既有顺序 0 反序 0 漏.
  - **T204 (#119) Stage 3 派生通道 2/2 name label 主题色 5 verb** — `src/scripts/hud.gd` 加 5 verb `_pulse_name_label` / 
`_bind_name_label` / `_cut_name_label` / `_echo_name_label` / `_wave_name_label` 5 `@onready var` + 5 verb 主题色 
always-visible 7pt 小字 (位置 Icon 后、Cooldown 前, 玩家一眼看出 "这一行是哪个 verb") + 6 通道 per verb 第 2 通道 1:1 严格分离 (与 T202 cooldown 
label 主题色 1:1 对齐, 0 互混 0 复用 1 字段), 6 verb 调色六元组 0 改 1 hex 0 改 1 通道值 (Pulse Amber Voice #F2B66E / Bind Muted Violet 
#65506A / Cut Coral Pulse #E86D5A / Echo Glass Cyan #69C7CE / Wave Pale Resonance #B7E6DC, 5 verb 5 主色 0 撞). T202 + 
T204 跨 2 任务沉淀 6 通道 per verb 4 主 + 2 派生 = 6 通道 100% 闭环 (4 主: icon / name label / fill / cooldown label + 2 派生: 
reduce_flash / glow border, 后者 T233 #152 落地).
  - **T206 (#123) Stage 5 reduce_flash 范围扩展 5 verb + resonance + health = 7 element** — `src/scripts/hud.gd` 修 
`_apply_reduced_flash_modulate` iteration list 从 5 element → 7 element (加 `_resonance_bar` ResonanceBar 主色 Glass Cyan 
#69C7CE + `_health_container` 4 bell Glass Cyan/Muted Violet, 1 次 modulate 写 = 7 element 全灰化, T200 rationale "常驻/瞬时二分" 
升级为 "accessibility 偏好应当覆盖玩家感知的全部高饱和度 UI blob", 14x16 满血 bell 每帧 0.5px 玻璃青高光与 5 verb 冷却条同等刺激源, 累加效应 = "我刚勾 reduce_flash 
但屏幕还是闪"), 5 verb + 2 派生 = 7 element 0 反序 0 漏 0 改 1 element 1 顺序.
  - **T233 (#152) Stage 4 cold glow border 5 verb 1:1 严格** — `src/scripts/hud.gd` 加 5 verb `_PULSE_GLOW_COLOR` / 
`_BIND_GLOW_COLOR` / `_CUT_GLOW_COLOR` / `_ECHO_GLOW_COLOR` / `_WAVE_GLOW_COLOR` 5 const (5 verb 5 主色 hex 0 撞 6 verb 
调色六元组) + 5 verb glow stylebox 5 instance (`_pulse_glow_bg` / `_bind_glow_bg` / `_cut_glow_bg` / `_echo_glow_bg` / 
`_wave_glow_bg`, 每 verb 1 instance 让 `_tween_verb_glow` 可独立 tween border_color) + `_verb_glow_state: Dictionary` 5 key 
(pulse / bind / cut / echo / wave) → bool + `_create_verb_glow_stylebox(color: Color)` 函数 + 
`_update_verb_glow_state(verb_name: String, ...)` 函数 + 0.12s 双向 tween quad-ease-out 同步 T231 / T226 / T111 节奏 (verb 
ready 状态时 1px border 显示 verb 主题色, cooling 状态时 border alpha fade out 到 0, 玩家一眼看出 "verb 已就绪" 不必看 progress bar 数值) + 5 
verb `add_theme_stylebox_override("background", _<verb>_glow_bg)` 5 调点 (替换 tscn 共用 StyleBoxFlat 引用, 5 verb 独立持有自己 
stylebox), 5 verb 0 改既有 @onready var / var / 5 verb 调色六元组 0 改 1 hex 0 改 1 通道值.
  - **T247 (#164) Stage 1-3 6 verb Whisper 接入 + Stage 4 dict 6 key + Stage 5 iteration list 8 element** — 
`src/scripts/hud.gd` 加 `_WHISPER_GLOW_COLOR := Color(0.784, 0.643, 0.847, 1.0)` (Muted Mauve #C8A4D8, 6 verb 调色六元组第 6 行 
0 撞 5 verb 调色) + 6 verb 4 `@onready var` (`_whisper_cooldown` / `_whisper_cooldown_label` / `_whisper_name_label` + 隐含 
`_whisper_icon` path) + 6 verb 1 ability reference `var _whisper_ability` + 6 verb 1 glow stylebox `_whisper_glow_bg` + 
6 verb 1 dict key `"whisper": false` + 6 verb 1 iteration list 8 element (T200 5 element → T206 7 element → T247 8 
element) + 6 verb 1 `_create_verb_glow_stylebox(_WHISPER_GLOW_COLOR)` 调点 + 6 verb 1 
`add_theme_stylebox_override("background", _whisper_glow_bg)` 调点 + 6 verb 1 `_update_verb_glow_state("whisper", ...)` 
调点 + 6 verb 1 `get_node_or_null("WhisperAbility")` 守卫 + `hud.tscn` 加 `WhisperRow` (HBoxContainer) + 4 子节点 
(`WhisperIcon` TextureRect / `WhisperNameLabel` Label / `WhisperCooldown` ProgressBar / `WhisperCooldownLabel` Label) + 
`StyleBoxFlat_whisper_fill` (Muted Mauve #C8A4D8 主题色) + ext_resource `whisper_icon.png` 引用, 5 段序列 1:1 复制 0 反序 0 漏 0 改 1 
字段 0 改 1 hex 0 改 1 通道值. 5 verb 0 改既有 @onready var / var / const / 5 verb 调色六元组 0 改 1 hex 0 改 1 通道值 / 5 verb glow 
stylebox 0 改 1 字段 / 5 verb dict key 0 改 1 key / 7 element iteration list 0 反序 0 漏 0 改 1 element.
  - 跨 6 任务 ~48 轮落地后 0 漂动, 6 verb HUD 6 行 6 色色域分工 6 通道 5 段 canonical 1:1 序列 100% 闭环, 5 段 1:1 严格分离 (Stage 1 tscn 6 verb 6 
row 4 子节点 1:1 严格 0 漏 + Stage 2 6 verb 调色六元组 1:1 严格 0 漏 0 重叠 + Stage 3 6 verb × 6 通道 = 36 通道 1:1 严格 0 漏 1 通道 0 互混 0 复用 0 
共享 + Stage 4 6 verb dict 6 key 0 漏 1 verb + 6 verb glow stylebox 6 instance 0 漏 1 verb + Stage 5 reduce_flash iteration 
list 8 element 0 漏 1 element 0 反序 0 改 1 element 1 顺序). 36 通道 0 漂动 (6 verb × 6 通道, 含 4 主通道 + 2 派生通道, 1 verb 1 6 通道 0 
例外). 6 任务 0 后续 主要 pre-existing 风险.
- **预防**:
  1. 任何 polish 期给"加新 1 verb HUD 接入"或"修 1 verb HUD 6 通道"或"加新 1 HUD UI 元素"任务加新字段 / 加新方法 / 加新 verb 时**必须**严格按 5 段 
canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 改 `hud.tscn` 6 verb row 4 子节点模板** (T247 #164 落地后 6 
verb row 1:1 严格, PulseRow / BindRow / CutRow / EchoRow / WaveRow / WhisperRow 6 row 0 改 0 重命名 0 漏 1 row, 4 子节点 (Icon / 
NameLabel / Cooldown / CooldownLabel) 0 改 0 漏 1 节点 0 改 1 节点类型, 改 1 字段名破坏 @onready var path 多层 path 触发 "Node not found" 
runtime 错), **Stage 2 0 改 6 verb 调色六元组 0 改 1 hex 0 改 1 通道值** (T200 + T233 + F013.E + T241 + T247 落地后 6 verb 调色六元组 1:1 
严格, Pulse Coral Pulse #E86D5A (0.91, 0.43, 0.35, 1.0) / Bind Muted Violet #65506A (0.40, 0.31, 0.42, 1.0) / Cut Amber 
Voice #F2B66E (0.95, 0.71, 0.43, 1.0) / Echo Glass Cyan #69C7CE (0.41, 0.78, 0.81, 1.0) / Wave Pale Resonance #B7E6DC 
(0.72, 0.91, 0.87, 1.0) / Whisper Muted Mauve #C8A4D8 (0.78, 0.64, 0.85, 1.0) 6 verb 调色 1:1 严格分离 0 漏 0 重叠 0 互混 0 复用 0 
共享, 改 1 hex / 1 通道值 = 6 verb HUD 6 行 6 通道 漂动 + §9.6.4 调色六元组 entry 漂动 + §9.6.22 6 verb VFX 视觉组 漂动 + §9.6.5 tooltip 视觉组连贯 
8 行拼接 漂动), **Stage 3 0 改 6 verb 6 通道 per verb 0 反序 0 漏 0 改 1 通道** (T200 + T202 + T204 + T233 + F013.E + T247 落地后 6 verb 
6 通道 1:1 严格, 4 主通道 (icon / name label / fill / cooldown label) + 2 派生通道 (reduce_flash modulate / glow border) 6 通道 per 
verb 0 漏 0 互混 0 复用 0 共享, 6 verb × 6 通道 = 36 通道 0 漂动), **Stage 4 0 改 6 verb `_verb_glow_state` dict 6 key 0 漏 1 verb + 6 
verb glow stylebox 6 instance 0 漏 1 verb** (T233 + T247 落地后 6 verb dict 6 key 1:1 严格, pulse / bind / cut / echo / wave 
/ whisper 6 key 0 漏 0 改 0 重命名 0 反序, 6 verb glow stylebox 6 instance (`_pulse_glow_bg` / `_bind_glow_bg` / 
`_cut_glow_bg` / `_echo_glow_bg` / `_wave_glow_bg` / `_whisper_glow_bg`) 0 漏 0 改 0 重命名, 改 1 dict key / 改 1 stylebox var 
名 = `_update_verb_glow_state` 0 触发 冷光勾边, 0 触碰既有 5 verb dict key 0 漏), **Stage 5 0 改 reduce_flash iteration list 8 
element 0 漏 1 element 0 反序 0 改 1 顺序** (T200 + T206 + T247 落地后 8 element 1:1 严格, 5 verb bar + 1 whisper + 1 resonance + 
1 health = 8 element 0 漏 0 改 0 漏 1 顺序, 改 1 顺序 = accessibility 灰化范围错乱 / 漏 1 element = HUD 顶部某元素 0 灰化). 任何"加新 1 verb HUD 
接入"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格), **再**在 `hud.tscn` 加 1 row + 4 子节点 (Stage 1 1:1 严格, 0 漏 1 row 0 改 1 
节点类型), **再**在 `hud.gd` 加 6 通道 1 verb 4 `@onready var` + 1 ability ref + 1 主题色 const (Stage 2 + 3 1:1 严格, 0 漏 0 改 1 hex 
0 改 1 通道值), **再**加 1 glow stylebox + 1 dict key + 1 `_create_verb_glow_stylebox` 调点 + 1 `add_theme_stylebox_override` 
调点 + 1 `_update_verb_glow_state` 调点 (Stage 4 1:1 严格), **再**加 1 iteration list element (Stage 5 1:1 严格), **再**按 §9.6.22 
5 段序列 1:1 复制 (VFX 视觉组 调色 + 几何 + layer 1:1 严格), **再**按 §9.6.5 `_VERB_ACHV_INFO` 6 字段 1:1 复制 (tooltip 视觉组连贯 0 漏 1 verb), 
0 触碰 §9.6.3 既有 5+1 verb 7 UI 通道 任何 1 字段 0 改既有 6 verb row 0 改既有 6 verb 主题色 0 改既有 36 通道 0 改既有 8 element iteration list 0 
改既有 6 verb dict 6 key.
  2. **5 段序列 0 触碰边界** — `src/scripts/hud.gd` 任何 const / var / `@onready var` / `@export` / `@onready` 0 改 0 删 0 重排 
(Stage 1-5 既有 1 字段 1 调点 0 改), 任何"修 1 verb 主题色"polish 走 Stage 2 1:1 严格 (改 `_PULSE_GLOW_COLOR` 时同步改 §9.6.4 调色六元组 Pulse 
entry 0 改 1 hex 0 改 1 通道值); 任何"加新 1 verb HUD 6 通道"polish 走 Stage 3 1:1 严格 (在 @onready var / var / const 1:1 复制 0 漏 1 通道 
0 反序 0 改 1 字段 0 改 1 节点类型); 任何"修 1 verb glow 主题色"polish 走 Stage 4 1:1 严格 (改 `_PULSE_GLOW_COLOR` + `_pulse_glow_bg` 同步, 0 
漏 1 stylebox 0 改 1 dict key 0 漏 1 verb); 任何"修 reduce_flash 范围"polish 走 Stage 5 1:1 严格 (在 
`_apply_reduced_flash_modulate` iteration list 末尾/中间加 1 element 0 漏 0 改既有 8 element 0 改 1 顺序).
  3. **6 verb 调色 0 改 1 hex 0 改 1 通道值** (T200 + T233 + F013.E + T241 + T247 落地后 0 触碰边界) — Pulse Coral Pulse #E86D5A / 
Bind Muted Violet #65506A / Cut Amber Voice #F2B66E / Echo Glass Cyan #69C7CE / Wave Pale Resonance #B7E6DC / Whisper 
Muted Mauve #C8A4D8 6 verb 调色 1:1 严格分离 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 hex / 1 通道值 = 6 verb HUD 6 行 6 通道 漂动 (Stage 2 漂动) + 
§9.6.4 调色六元组 entry 漂动 + §9.6.22 ICON_COLORS 漂动 + §9.6.3 5+1 verb 7 UI 通道 漂动 + §9.6.5 tooltip 视觉组连贯 8 行拼接 漂动, 
source-grep 验证 6 verb 主题色 const + §9.6.4 调色六元组 6 entry + §9.6.22 6 verb VFX `@export` color 字段 0 漏 0 改 1 字段 0 改 1 通道值.
  4. **36 通道 (6 verb × 6 通道) 0 反序 0 漏 0 改 1 通道** (T200 + T202 + T204 + T233 + F013.E + T247 落地后 0 触碰边界) — 6 verb × 6 通道 
= 36 通道 1:1 严格分离, 4 主通道 (icon sprite modulate / name label font_color / ProgressBar fill bg_color / cooldown label text 
color) + 2 派生通道 (reduce_flash modulate overlay / cold glow border 1px bg_color), 0 反序 0 漏 0 改 1 通道 0 改 1 节点 0 改 1 字段 0 
改 1 节点类型, 任何"加新 1 通道"polish 必须在 6 verb × 6 通道 = 36 通道范围内 1:1 同步, source-grep 验证 24 @onready var (6 verb × 4 UI 元素) + 6 
verb ability ref + 6 verb 主题色 const + 6 verb glow stylebox 0 漏 0 改 1 字段 0 改 1 节点类型.
  5. **Stage 4 6 verb `_verb_glow_state` dict 6 key 0 改 1 字符 0 重命名 0 反序 0 漏 1 key** (T233 + T247 落地后 0 触碰边界) — 
`_verb_glow_state: Dictionary` 6 key (pulse / bind / cut / echo / wave / whisper) 0 改 0 删 0 重命名 0 反序 0 漏 1 key, 任何"加新 1 
verb glow"polish 必须**先**在 dict 字面量末尾加 1 key (e.g. `"<verb>": false`), **再**在 `_ready()` 加 1 行 
`_create_verb_glow_stylebox(_<VERB>_GLOW_COLOR)` 调点 + 1 行 `_<verb>_cooldown.add_theme_stylebox_override("background", 
_<verb>_glow_bg)` 调点 + 1 行 `_update_verb_glow_state("<verb>", _<verb>_glow_bg, _<VERB>_GLOW_COLOR, _<verb>_ability)` 调点,
 改 1 dict key / 改 1 stylebox var 名 / 改 1 调点顺序 = 6 verb 冷光勾边 0 触发 0 视觉组连贯崩塌, source-grep 验证 6 dict key + 6 stylebox var 
+ 6 `_update_verb_glow_state` 调点 0 漏 0 改 1 字段.
  6. **Stage 5 reduce_flash iteration list 8 element 0 改 1 element 0 漏 1 element 0 反序** (T200 + T206 + T247 落地后 0 触碰边界) 
— `_apply_reduced_flash_modulate` 8 element list (5 verb bar + 1 whisper + 1 resonance + 1 health) 0 改 0 漏 0 反序 0 改 1 
element 1 顺序, 任何"加新 1 reduce_flash element"polish 必须在 list 末尾/中间加 1 element (e.g. `_shop_panel` / `_pause_menu` / 
`_save_load_menu` / `_settings_menu`), 改 1 element = accessibility 灰化范围错乱 / 漏 1 element = HUD 顶部某元素 0 灰化 (e.g. T206 
#123 落地时漏 `_health_container` 触发 HUD 顶部生命条仍 100% 饱和度), source-grep 验证 8 element 0 漏 0 改 1 element 1 顺序.
  7. 已知 drift risk: 6 verb HUD 6 行 6 色色域分工 6 通道 (5 段 canonical 1:1 序列) **可能**漏 1 段 1:1 镜像 (e.g. T247 #164 落地时漏 Stage 4 
`_verb_glow_state` dict 加 `"whisper": false` 1 key, 0 冷光勾边; 漏 Stage 5 iteration list 加 `_whisper_cooldown` 1 element, 
accessibility 灰化 0 覆盖 Whisper bar). 监控建议: 任何"未来给 6 verb HUD 加新 verb 或加新 UI 元素"必须**先**在 5 段序列 (Stage 1 tscn + Stage 2 调色 
const + Stage 3 6 通道 @onready var / var / ability ref + Stage 4 dict + stylebox + 调点 + Stage 5 iteration list) 1:1 镜像 0 
漏 1 段. T247 (#164) 5 段序列 1:1 镜像落地后任何新 verb / 新 UI 元素 加 5 段同步是 §9.6.24 5 段序列的"母集" (1 verb 5 段 1:1 严格, §9.6.24 5 段 1 verb 
完整闭环, 6 verb × 5 段 = 30 件 1:1 严格, 含 6 通道 × 6 verb = 36 通道 1:1 严格, 8 element iteration list 0 漏 1 element, 6 verb dict 6 
key 0 漏 1 key). 另 1 类已知 drift: Stage 1 tscn row 4 子节点 path 0 改 1 字段 触发 @onready var path 多层 path "Node not found" 错 
(T247 #164 落地时 `WhisperRow` 4 子节点 0 漏 0 改 path 段, 后续 polish 期若加新 verb 必须**先**在 tscn 加 row + 4 子节点, **再**在 `hud.gd` 加 4 
`@onready var` path 与 tscn 1:1 严格对齐).

**0 副作用**: T200 / T202 / T204 / T206 / T233 / T247 任何 const / var 0 触碰 (6 verb 主题色 const 0 改 0 删 0 重排 + 6 verb 
`@onready var` 0 改 0 删 0 重排 + 6 verb `var _<verb>_ability` 0 改 0 删 0 重排 + `_REDUCED_COLOR_MODULATE` / 
`_NORMAL_COLOR_MODULATE` 0 改 + `_verb_glow_state` dict 6 key 0 改 0 删 0 重排 + 6 verb glow stylebox var 0 改 0 删 0 重排 + 
reduce_flash iteration list 8 element 0 改 0 删 0 重排); 0 触碰 §9.6.1 6 verb VFX 跨类 handler `is_instance_valid + has_method` 
双守卫 (T280 0 改 6 verb `_current_<verb>_vfx` 任何字段); 0 触碰 §9.6.2 5 layer 视觉组既有 5 verb 任何 1 字段 1 alpha 公式 1 layer 顺序; 0 触碰 
§9.6.3 5+1 verb 7 UI 通道 既有 6 verb 任何 1 字段 1 调点 (T280 0 改 6 verb 4 @onready var + 1 ability ref + 1 主题色 const + 1 风格 
stylebox + 1 dict key + 1 iteration list element 0 触碰既有 5 步 1:1 复制 模板); 0 触碰 §9.6.4 调色六元组 + HUD 6 行 6 通道 + tooltip 
三闭环宪法 (T280 0 改 6 verb 调色 1 hex 0 改 1 通道值 + 0 改 HUD 6 行 6 通道 + 0 改 tooltip 6 字段); 0 触碰 §9.6.5 6 verb 视觉组连贯 tooltip 8 
行拼接 (T280 0 改 `_VERB_ACHV_INFO` 6 字段 任何 1 字段); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T280 0 改 6 verb `_on_<verb>_hit` 
函数体任何 const / var / 屏染 / 屏抖 / VFX flash / 音频); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T280 0 改 6 verb 主信号 + 命中信号 + 结束信号 
+ 扩展信号 + PlayerActionGate register / unregister); 0 触碰 §9.6.18 16 件套 verb ability base (T280 0 改 6 verb ability 子类任何 
const / var / `@export` / `@onready` / 5 共享方法 / `_exit_tree` 兜底 / `is_globally_blocking` 扩展); 0 触碰 §9.6.19 7 件套 windup 
VFX base (T280 0 改 6 verb windup VFX 子类任何 const / var / `_ready()` / `_process(delta)` / `_activate_windup_tween()` / 
`fade_out_and_free()`); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 transition flow (T280 0 改 GFC + room_transition + room_door + 
GameState 5 段序列); 0 触碰 §9.6.21 8 段成就序列 (T280 0 改 PlayerStats 6 verb 字段 + record_ability_used 6 branch + get_stat 6 
entry + set_stat 6 entry + reset_run 6 行 + all_abilities_used 6 项 + achievements.json 4 tier ID + ICON_COLORS 6 entry + 
PNG 双路径); 0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T280 0 改 6 verb VFX 文件 任何 const / var / `@export` color 字段 / 5+ layer 视觉组 / 
几何 / 跨段镜像); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T280 0 改 audio_manager_enhanced.gd 7 桶 `prewarm_*` 函数体 0 改 0 删 0 重排 + 
prewarm_all_sfx() aggregator 7 行 0 改 0 删 0 重排 + 3 跨调用方 0 改 0 删 0 重排); 0 触碰 check_smoke_consistency.sh (T280 0 改任何规则); 0 
触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 
不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, hud.gd 6 verb @onready var / var / const / dict key / stylebox / 
iteration list 任何 1 字段 0 改 0 删 0 重排); 0 行为变化, 1 文档 1 段 (§9.6.24 ~50 行) + 1 smoke test (T280 19+ 断言).

**与 §9.6.3 / §9.6.4 / §9.6.5 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 关系** — 
§9.6.3 覆盖 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb 模式 (加新 1 verb 时 6 处 1:1 复制: tscn + 4 @onready var + 1 ability ref + 1 
const + 1 stylebox + dict key + iteration list), §9.6.4 覆盖 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 1 + 
2 + 3, 宪法 2 = 6 verb HUD 6 行 4 主通道 + 2 派生通道 = 6 通道 per verb), §9.6.5 覆盖 6 verb 视觉组连贯 tooltip 
`_build_verb_achievement_tooltip` 8 行拼接扩展器, §9.6.16 覆盖 player.gd 6 verb hit handler 5 件套 1:1 严格分离 (verb ability 
`.trigger_player_hit` → 5 件套 `_on_verb_hit_<verb>` + 1 verb Whisper), §9.6.17 覆盖 player.gd `_ready` 6 verb signal 桥接 
5+1 件套 1:1 严格分离, §9.6.18 覆盖 `_verb_ability_base.gd` 6 verb ability 子类共享契约 16 件 1:1 严格分离, §9.6.19 覆盖 
`_verb_windup_vfx_base.gd` 6 verb windup VFX 子类共享契约 7 件 1:1 严格分离, §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow 5 段 
canonical 1:1 序列, §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 canonical 1:1 序列, §9.6.22 覆盖 6 verb VFX 视觉组 5 段 canonical 1:1 序列 (Stage 
1 class_name + extends Node2D 模板 + Stage 2 6 verb 调色六元组 1:1 + Stage 3 5+ layer 视觉组 1:1 + Stage 4 6 verb 几何 1:1 + Stage 
5 6 verb 跨段 1:1 镜像), §9.6.23 覆盖 `audio_manager_enhanced.gd` prewarm aggregator 7 桶 5+1+1 canonical 1:1 序列 + 3 跨调用方 1:1 
镜像. §9.6.24 覆盖 `src/scripts/hud.gd` 6 verb HUD 6 行 6 色色域分工 6 通道 5 段 canonical 1:1 序列 (Stage 1 6 verb row 4 子节点 1:1 + 
Stage 2 6 verb 调色六元组 1:1 + Stage 3 6 verb × 6 通道 = 36 通道 1:1 + Stage 4 6 verb dict 6 key + stylebox 6 instance 1:1 + 
Stage 5 reduce_flash iteration list 8 element 1:1 + 3 闭环宪法同步). §9.6.3 关注"HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb (加新 1 
verb 时 6 处 1:1 复制 structural sync)", §9.6.4 关注"6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法)", §9.6.5 关注"6 
verb 视觉组连贯 tooltip 8 行拼接扩展器", §9.6.16-§9.6.19 关注"verb ability + signal + hit handler + windup VFX 共享契约", §9.6.20 关注"跨 
scene transition flow 时序契约", §9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格)", §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约 (调色 + 
几何 + layer + 跨段镜像)", §9.6.23 关注"prewarm aggregator 5+1+1 序列 1:1 严格分离契约 + 3 跨调用方 1:1 镜像", §9.6.24 关注"HUD 6 verb × 6 通道 = 
36 通道 1:1 严格分离契约 (5 段 1:1 严格分离)". 12 套 polish 模式 0 反序 0 漏 0 改 1 字符. 任何"加新 1 verb HUD 接入"必须**先**按 §9.6.4 调色六元组加 1 entry 
(Stage 2 1:1 严格), **再**按 §9.6.3 5+1 verb 7 UI 通道 6 处 1:1 复制 (Stage 1 + 3 1:1 严格), **再**按 §9.6.24 5 段序列 1:1 复制 (Stage 4 
+ 5 1:1 严格), **再**按 §9.6.22 5 段序列 1:1 镜像 (VFX 视觉组 调色 + 几何 + layer 1:1 严格), **再**按 §9.6.5 `_VERB_ACHV_INFO` 6 字段 1:1 复制 
(tooltip 视觉组连贯 0 漏 1 verb). §9.6.3 与 §9.6.24 0 互混 0 复用 0 共享: §9.6.3 关注"HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb 
(structural sync, 加新 1 verb 时 6 处 1:1 复制: tscn 4 子节点 + 4 @onready var + 1 ability ref + 1 const + 1 stylebox + dict key 
+ iteration list element)" (与 5 段序列 1:1 镜像的 36 通道 canonical 1:1 序列 0 相关), §9.6.24 关注"HUD 6 verb × 6 通道 = 36 通道 1:1 
严格分离契约 (5 段 canonical 1:1 序列: Stage 1 row 4 子节点 + Stage 2 调色 + Stage 3 36 通道 + Stage 4 dict + stylebox + Stage 5 
iteration list)" (与 structural sync 6 处 1:1 复制 0 相关). §9.6.4 与 §9.6.24 0 互混 0 复用 0 共享: §9.6.4 关注"6 verb 调色六元组 + HUD 6 行 
6 通道 + tooltip 三闭环宪法 (abstract 宪法 1+2+3, 1 句宪法 0 触及 5 段序列 0 触及 36 通道 canonical 1:1 序列)" (与 5 段序列 1:1 镜像的 36 通道 0 相关), 
§9.6.24 关注"HUD 6 verb × 6 通道 = 36 通道 1:1 严格分离契约 (5 段 canonical 1:1 序列)" (与 abstract 宪法 0 相关). §9.6.22 与 §9.6.24 0 互混 0 
复用 0 共享: §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约 (Stage 1 class_name + Stage 2 调色六元组 + Stage 3 5+ layer 视觉组 + Stage 4 
6 verb 几何 + Stage 5 6 verb 跨段 1:1 镜像, 5 段全在 `<verb>_vfx.gd` 文件)" (与 HUD `hud.gd` 36 通道 0 相关), §9.6.24 关注"HUD 6 verb × 6 
通道 = 36 通道 1:1 严格分离契约 (5 段 canonical 1:1 序列, 5 段全在 `hud.gd` 文件 + 1 段 tscn 6 verb row 4 子节点)" (与 VFX 视觉组 0 相关). §9.6.24 
是 12 套 polish 模式**唯一**关注"HUD 6 verb × 6 通道 = 36 通道 1:1 严格分离 (5 段 canonical 1:1 序列: Stage 1 row + Stage 2 调色 + Stage 3 
36 通道 + Stage 4 dict + stylebox + Stage 5 iteration list)"的 1 套, 与 §9.6.3 / §9.6.4 / §9.6.5 / §9.6.16-§9.6.23 11 套 
structural sync / abstract 宪法 / tooltip / verb ability / signal / hit handler / windup VFX / transition / 成就 / VFX 视觉组 
/ prewarm 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.24 5 段序列走 §9.1 9 步落地的 "verb" (1) + "ability" (2) + "const" (3) + "icon" (6) + 
"HUD 冷光勾边" (9) 5 步 (`src/scripts/hud.gd` 6 verb `@onready var` 4 主通道 + 6 verb `var _<verb>_ability` + 6 verb 主题色 const 
`_PULSE_GLOW_COLOR` / ... / `_WHISPER_GLOW_COLOR` + `src/scenes/hud.tscn` 6 verb row 4 子节点 + 6 verb glow stylebox 6 
instance + 6 verb `_verb_glow_state` dict 6 key + `_apply_reduced_flash_modulate` iteration list 8 element), 0 触碰 §9.1 
9 步任何 "input" (4) + "shop" (5) + "vfx" (7) + "audio" (8) 4 步 (HUD 6 verb × 6 通道 0 走 input map + 0 走 shop 系统 + 0 走 VFX 
视觉组 + 0 走 audio 副作用). 任何"加新 1 verb HUD 接入"必须**先**按 §9.1 9 步落地 (含 "verb" (1) + "ability" (2) + "const" (3) + "icon" (6) 
+ "HUD 冷光勾边" (9) 5 步), **再**按 §9.6.24 5 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 任何"加新 1 verb 调色"必须**先**在 `STYLE_GUIDE.md` 
§9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格, 0 漏 1 entry 0 改 1 hex), **再**在 `hud.gd` 加 `_<VERB>_GLOW_COLOR := Color(<hex>)` 
1:1 镜像 (Stage 2 1:1 严格, 0 漏 1 字段 0 改 1 通道值), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "const" (3) 步 (HUD 6 verb × 6 
通道 0 触碰 verb enum + ability 子类 + const 字段). 任何"加新 1 verb 6 通道"必须**先**在 `hud.tscn` 加 1 row + 4 子节点 (Stage 1 1:1 严格), 
**再**在 `hud.gd` 加 4 `@onready var` + 1 ability ref + 1 const + 1 stylebox (Stage 3 + 4 1:1 严格, 0 复用 L1-L5 既有 5 verb 
`@onready var` 0 改 1 字段 0 改 1 节点类型), 0 触碰 §9.1 9 步任何 "icon" (6) 步既有 6 verb icon PNG 双路径. 任何"加新 1 reduce_flash 
element"必须**先**在 `_apply_reduced_flash_modulate` iteration list 末尾/中间加 1 element (Stage 5 1:1 严格, 0 漏 1 element 0 改既有 8 
element 顺序), **再**确认该 element 走 `_apply_reduced_flash_modulate` 1:1 镜像 (Stage 5 1:1 严格, 0 漏 1 element 0 改 1 顺序), 0 触碰 
§9.1 9 步任何 "HUD 冷光勾边" (9) 步既有 6 verb 6 bar 任何 1 字段. 任何"加新 1 跨段镜像"必须**先**在 §9.6.3 / §9.6.4 / §9.6.5 / §9.6.22 4 跨段加 1 镜像 
(5 段序列 1:1 严格, 0 漏 1 跨段 0 改 1 字段), 0 触碰 §9.1 9 步任何 const / var / 节点 (5 段序列仅在 `hud.gd` 6 verb × 6 通道 = 36 通道 + 6 verb 
dict 6 key + 6 verb glow stylebox 6 instance + 8 element iteration list, 6 verb 子类 0 改 base 0 改, 5 verb lazy-init 守卫 0 
删 0 改, 含 1 verb Whisper T247 #164 接入完成).

---
