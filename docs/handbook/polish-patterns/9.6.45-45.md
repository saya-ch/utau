# Polish Patterns 9.6.45–45

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.45–9.6.45，共 1 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.45-45.md

<!-- shard 45-45 共 1 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.45 6 verb `_ready()` + `_exit_tree()` 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 
1:1 严格分离契约 polish 模式 (D002.B #98 + T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T173 #92 + T174 #93 + T245 
#162 + T250 #168 + T297 #222 + T298 #223 + T299 #224 + T300 #226 跨 14 任务 ~127 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 35 套 
> polish 模式均在 "pause_menu / 6 verb 单层 / 6 verb 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / audio 数据层 / 
> PlayerActionGate 单 autoload 4 件套 / SaveSystem CRC32 校验 / T162 brittle 修复流程 / 6 verb audio 家族 19 cue / 6 verb cooldown 
> ready jingle 5 段 / 6 verb `_ready()` super 调用顺序 6 verb 1:1 严格分离契约 / 6 verb `_exit_tree()` super 调用顺序 6 verb 1:1 
> 严格分离契约 / 6 verb 双 hook 串联 1:1 严格分离契约 / 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 1:1 严格分离契约" 范畴, 0 涉及 "6 
> verb `_ready()` + `_exit_tree()` 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约" 
> 落地. §9.6.44 (T300 #226 落地) 关注 "6 verb `_ready()` + `_exit_tree()` 双 hook 串联 + `_player` non-null assertion 0 触碰既有 1:1 
> 严格分离契约" (19 元素 1:1 严格, 19 元素覆盖 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 0 涉及 6 verb 视觉组连贯 lifecycle 1:1 
> 严格分离契约, 0 涉及 5 verb 视觉组 5 段 1:1 严格 + 1 verb 视觉组 1 段 1:1 严格 + 1 显式契约 "6 verb 视觉组连贯 lifecycle 共享视觉语法 (深海军蓝背景圆盘 + Glass 
> Cyan 外环 + verb 主色 core)" 1 段 + 1 视觉组 0 触碰既有 1:1 严格). 本节 (T301 #227 落地) 首个落在**6 verb `_ready()` + `_exit_tree()` 双 
> hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约** 
> (`src/scripts/_verb_ability_base.gd` `_ready()` 1 共享方法 + `_exit_tree()` 1 共享方法 + `_player` non-null assertion 1 边 0 
> 触碰既有 1:1 严格 + 显式契约 1 段 "Lifecycle contract" + 6 verb 子类 各自 `_ready()` 第 1 行 `super._ready()` (此 1 行 隐式 触发 base 
> `_player` non-null assertion, 6 verb 0 自己加 `assert(_player != null, ...)`) + 6 verb 子类 各自 `_exit_tree()` 0 触碰既有 1:1 
> 严格 — 5 verb 0 override `_exit_tree()` (Pulse / Bind / Cut / Echo / Whisper 0 触碰 base 1:1 严格继承) + 1 verb 1 override 
> `_exit_tree()` (Wave 0 显式 `super._exit_tree()` 调用但 1:1 严格 byte-identical cleanup 镜像 base — fade_out_and_free + null 与 
> base 字节码 1:1 严格一致) + 6 verb 0 触碰 `_player` non-null assertion 0 触碰既有 1:1 严格 (6 verb 0 自己加 `assert(_player != null, 
> "VerbAbilityBase subclass must be child of CharacterBody2D")` 0 触碰 base 1:1 严格继承) + 5 verb 视觉组 5 段 1:1 严格 (Pulse 
> 同心圆环扩散波 + Coral Pulse 核 / Bind 向内螺旋涡 + Muted Violet 核 / Cut 水平锋利斩 + 4 三角碎片 + Amber Voice 核 / Echo 玻璃护盾 + 8 棱镜折射 + 
> 双向反弹 + Glass Cyan 核 / Wave 双环扩散 + 8 棱镜光线 + amber 中心 + Pale Resonance 核) + 1 verb 视觉组 1 段 1:1 严格 (Whisper constant 球 + 
> 2px 描边 + 球心亮点 + Muted Mauve 核, T245 #162 落地) + 1 显式契约 "6 verb 视觉组连贯 lifecycle 共享视觉语法 (深海军蓝背景圆盘 + Glass Cyan 外环 + verb 
> 主色 core)" 1 段 + 1 视觉组 0 触碰既有 1:1 严格 (6 verb 视觉组连贯 5 段 + 1 段 0 触碰既有, 任何 "加新 1 verb" 必 1:1 镜像 5 段 + 1 段 7 元素 0 漏 0 改 0 
> 反向)) polish 模式 — 跨 19 元素 (源自 §9.6.44) + 7 元素 拼接段 = 26 元素 1:1 严格 (Stage 1 Pulse 1 verb 2 hook + 1 assertion 1:1 严格 + 
> Stage 2 Bind 1 verb 2 hook + 1 assertion 1:1 严格 + Stage 3 Cut 1 verb 2 hook + 1 assertion 1:1 严格 + Stage 4 Echo 1 
> verb 2 hook + 1 assertion 1:1 严格 + Stage 5 Wave 1 verb 2 hook + 1 assertion 1:1 严格 + Stage 6 Whisper 1 verb 2 hook + 
> 1 assertion 1:1 严格 + 1 base assertion 0 触碰既有 + 5 verb 视觉组 5 段 1:1 严格 + 1 verb 视觉组 1 段 1:1 严格 + 1 显式契约 "6 verb 视觉组连贯 
> lifecycle 共享视觉语法" + 1 视觉组 0 触碰既有 1:1 严格) — 跨 1 套 polish 模式 (T301 #227 落地) 跨 14 任务 (D002.B #98 + T166 #85 + T167 #86 + 
> T168 #86 + T169 #87 + T171 #89 + T173 #92 + T174 #93 + T245 #162 + T250 #168 + T297 #222 + T298 #223 + T299 #224 + 
> T300 #226 ~127 轮落地, 0 漂动 0 后续 主要 pre-existing 风险). 任何"加新 1 verb" (e.g. 未来加 7th verb "Drain" 接入 `_ready()` + 
> `_exit_tree()` 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离) 或"加新 1 perk" 或"修 1 
> verb 视觉组" 或"修 1 verb 视觉组连贯" 必须**先**按 Stage 1 Pulse 1 verb 2 hook + 1 assertion + 1 视觉组 1:1 严格 加 4 元素 (Stage 1 1:1 严格, 
> 0 漏 4 元素 0 改 1 字符 0 反序 0 反向, **0 漏 4 元素**), **再**按 Stage 2 Bind 1 verb 2 hook + 1 assertion + 1 视觉组 1:1 镜像 (1:1 严格, 0 
> 漏 4 元素 0 改 1 字符 0 反序 0 反向), **再**按 Stage 3 Cut 1 verb 2 hook + 1 assertion + 1 视觉组 1:1 镜像 (1:1 严格, 0 漏 4 元素 0 改 1 字符 
> 0 反序 0 反向), **再**按 Stage 4 Echo 1 verb 2 hook + 1 assertion + 1 视觉组 1:1 镜像 (1:1 严格, 0 漏 4 元素 0 改 1 字符 0 反序 0 反向), 
> **再**按 Stage 5 Wave 1 verb 2 hook + 1 assertion + 1 视觉组 1:1 镜像 (1:1 严格, 0 漏 4 元素 0 改 1 字符 0 反序 0 反向), **再**按 Stage 6 
> Whisper 1 verb 2 hook + 1 assertion + 1 视觉组 1:1 镜像 (1:1 严格, 0 漏 4 元素 0 改 1 字符 0 反序 0 反向), **再**按 base assertion 0 
> 触碰既有 1:1 严格 加 1 base assertion 0 触碰既有 1 元素 (1 base assertion 0 触碰既有 1:1 严格, 0 漏 1 元素 0 改 1 字符 0 反序 0 反向), **再**按 1 
> 显式契约 "6 verb 视觉组连贯 lifecycle 共享视觉语法" 加 1 显式契约 1 元素 (1 显式契约 1:1 严格, 0 漏 1 元素 0 改 1 字符 0 反序 0 反向), **再**按 1 视觉组 0 触碰既有 
> 1:1 严格 加 1 视觉组 0 触碰既有 1 元素 (1 视觉组 0 触碰既有 1:1 严格, 0 漏 1 元素 0 改 1 字符 0 反序 0 反向). 跨 1 套 polish 模式 × 6 verb × 3 元素 = 18 
> 元素 + 1 base assertion = 19 元素 1:1 严格 (源自 §9.6.44) + 1 套 polish 模式 × 7 元素 = 7 元素 1:1 严格 拼接段 = 26 元素 1:1 严格 0 漏 1 元素 0 
> 改 1 字段 0 改 1 字符 0 例外.

- **症状**: 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 (D002.B #98 + T166 
#85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T173 #92 + T174 #93 + T245 #162 + T250 #168 + T297 #222 + T298 #223 + 
T299 #224 + T300 #226 跨 14 任务 ~127 轮落地) 现有的 26 元素 1:1 严格 (1 Pulse `_ready()` 第 1 行 `super._ready()` + 1 Pulse 
`_exit_tree()` 0 override 0 触碰 base 1:1 严格继承 + 1 Pulse 0 override `_player` non-null assertion 0 触碰 base 1:1 严格继承 + 1 
Pulse 视觉组 1 段 1:1 严格 + 1 Bind `_ready()` 第 1 行 `super._ready()` + 1 Bind `_exit_tree()` 0 override 0 触碰 base 1:1 严格继承 + 
1 Bind 0 override `_player` non-null assertion 0 触碰 base 1:1 严格继承 + 1 Bind 视觉组 1 段 1:1 严格 + 1 Cut `_ready()` 第 1 行 
`super._ready()` + 1 Cut `_exit_tree()` 0 override 0 触碰 base 1:1 严格继承 + 1 Cut 0 override `_player` non-null assertion 0 
触碰 base 1:1 严格继承 + 1 Cut 视觉组 1 段 1:1 严格 + 1 Echo `_ready()` 第 1 行 `super._ready()` + 1 Echo `_exit_tree()` 0 override 0 
触碰 base 1:1 严格继承 + 1 Echo 0 override `_player` non-null assertion 0 触碰 base 1:1 严格继承 + 1 Echo 视觉组 1 段 1:1 严格 + 1 Wave 
`_ready()` 第 1 行 `super._ready()` + 1 Wave `_exit_tree()` 1 override 0 显式 `super._exit_tree()` 调用但 1:1 严格 
byte-identical cleanup 镜像 base + 1 Wave 0 override `_player` non-null assertion 0 触碰 base 1:1 严格继承 + 1 Wave 视觉组 1 段 1:1 
严格 + 1 Whisper `_ready()` 第 1 行 `super._ready()` + 1 Whisper `_exit_tree()` 0 override 0 触碰 base 1:1 严格继承 + 1 Whisper 0 
override `_player` non-null assertion 0 触碰 base 1:1 严格继承 + 1 Whisper 视觉组 1 段 1:1 严格 (T245 #162 落地, 唯一 constant 球 + 2px 
描边 + 球心亮点 + Muted Mauve 核) + 1 base `_ready()` 1 共享方法 含 `_player` non-null assertion 1 边 + 1 base `_exit_tree()` 1 共享方法 
+ 1 显式契约 "Lifecycle contract" 1 段 + 1 显式契约 "6 verb 视觉组连贯 lifecycle 共享视觉语法 (深海军蓝背景圆盘 + Glass Cyan 外环 + verb 主色 core)" 1 
段 + 1 视觉组 0 触碰既有 1:1 严格 (5 verb 视觉组 5 段 + 1 verb 视觉组 1 段 0 漏 0 改 0 反序 0 反向)), 在反复 polish (D002.B #98 → T300 #226 跨 12 
任务 ~126 轮落地, T245 #162 落地 Whisper 6 verb 视觉组连贯 1 段 → T250 #168 落地 6 verb 视觉组连贯 tooltip 8 行 → T301 #227 文档化 §9.6.45 拼接段) 
期间出现 3 类典型 fragile: (1) **"加新 1 verb 漏 6 verb × 4 元素 = 24 元素 1:1 严格"** — 加新 1 verb (e.g. 假设未来加 7th verb "Drain" 接入双 
hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离) 落地时漏 6 verb × 4 元素 = 24 元素 1:1 严格 
(e.g. 漏 Stage 1 Pulse 1 verb 中 4 元素 (1 `_ready()` 第 1 行 + 1 `_exit_tree()` 0 override + 1 `_player` assertion 0 
override + 1 视觉组) → ... → Stage 6 Whisper 1 verb 中 4 元素, 0 镜像 1 verb 24 元素), 1 verb 扩展 0 24 元素 0 闭环 0 漂动; (2) **"修 1 
verb 视觉组连贯 0 触碰既有 1 元素 0 触碰边界"** — 修 1 verb 视觉组连贯 (e.g. 改 Pulse 1 verb 视觉组 中 "同心圆环扩散波" 顺序 或 改 Wave 1 verb 视觉组 中 "双环扩散 + 
8 棱镜光线" 顺序) 时 0 触碰既有 1 元素 (1 视觉组 0 触碰既有 1:1 严格), 跨 verb 1:1 漂动; (3) **"加新 1 verb 漏 1 边 '6 verb 视觉组连贯 lifecycle 共享视觉语法' 
显式契约"** — 加新 1 verb (e.g. 7th verb "Drain" 接入双 hook 串联 + 6 verb 视觉组连贯) 落地时漏 1 边 "6 verb 视觉组连贯 lifecycle 共享视觉语法" 显式契约 
(e.g. 漏 §9.6.45 段 中 "6 verb 视觉组连贯 lifecycle 共享视觉语法 (深海军蓝背景圆盘 + Glass Cyan 外环 + verb 主色 core)" 1 段 0 镜像 1 verb, 1 verb 0 
视觉组连贯 0 闭环 0 漂动). 3 类症状根因: 6 verb 双 hook 串联 + `_player` non-null assertion + 6 verb 视觉组连贯 lifecycle 1:1 序列 0 文档化, 任何新 1 
verb / 1 perk / 1 视觉组 / 1 视觉组连贯契约 接入只能"凭经验" 1:1 镜像 26 元素 (6 verb × 3 元素 = 18 元素 1:1 严格 + 1 base assertion = 1 元素 1:1 严格 
+ 5 verb 视觉组 5 段 + 1 verb 视觉组 1 段 + 1 显式契约 + 1 视觉组 0 触碰既有 1:1 严格 = 26 元素 1:1 严格), 1 漏 1 元素 / 1 漏 1 视觉组 / 1 漏 1 视觉组连贯契约 
= 1 verb / 1 perk / 1 视觉组 / 1 视觉组连贯契约 扩展 0 26 元素 0 闭环 0 漂动.

- **触发场景**: 加新 1 verb (e.g. 未来加 7th verb "Drain" 接入 `_ready()` + `_exit_tree()` 双 hook 串联 + `_player` non-null 
assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离) / 加新 1 perk / 加新 1 hook 顺序 / 加新 1 `_player` non-null assertion / 加新 
1 视觉组 / 加新 1 视觉组连贯契约 (T245 + T250 + T300 模式参考) 时**必须**严格按 26 元素 1:1 复制 D002.B #98 + T245 #162 + T250 #168 + T297 #222 + 
T298 #223 + T299 #224 + T300 #226 落地模式: (1) **Stage 1 Pulse 1 verb 3 元素 + 1 视觉组 1:1 严格** — 在 `_verb_ability_base.gd` 
`_ready()` 方法 中 显式契约 + `_player` non-null assertion 1 边 0 触碰既有 1:1 严格 + 6 verb 各自 `_ready()` 第 1 行 `super._ready()` 1:1 
严格 (6 verb × 1 hook = 6 hook 1:1 严格 0 漏 0 改 0 反序 0 反向) + 6 verb 0 自己加 `assert(_player != null, ...)` 1:1 严格 0 漏 0 改 0 
反序 0 反向 + 6 verb 各自 `_exit_tree()` 1:1 严格 — 5 verb 0 override `_exit_tree()` 0 触碰 base 1:1 严格继承 (6 verb × 1 hook = 5 
hook 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 verb 1 override `_exit_tree()` 0 显式 `super._exit_tree()` 调用但 1:1 严格 byte-identical 
cleanup 镜像 base (1 hook 1:1 严格 0 漏 0 改 0 反序 0 反向) + 6 verb 0 override `_player` non-null assertion 0 触碰 base 1:1 严格继承 
(6 verb × 1 assertion = 6 assertion 1:1 严格 0 漏 0 改 0 反序 0 反向) + 6 verb 各自 1 视觉组 1:1 严格 (6 verb × 1 视觉组 = 6 视觉组 1:1 严格 0 
漏 0 改 0 反序 0 反向) + 1 base assertion 0 触碰既有 1:1 严格 (1 assertion 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "6 verb 视觉组连贯 
lifecycle 共享视觉语法" 1:1 严格 (1 契约 1:1 严格 0 漏 0 改 0 反序 0 反向) + 1 视觉组 0 触碰既有 1:1 严格 (1 视觉组 0 触碰既有 1:1 严格 0 漏 0 改 0 反序 0 反向) 
= 26 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, 0 触碰既有 6 verb 任何 1 字符, source-grep 验证 双 hook + `_player` non-null assertion + 视觉组连贯 6 
verb → 26 元素 0 漏 0 改 0 漏 1 边 0 反向; (2) **Stage 2 Bind 1 verb 3 元素 + 1 视觉组 1:1 严格** — 26 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, 
source-grep 验证 26 元素 0 漏 0 改 0 漏 1 边 0 反向; (3) **Stage 3 Cut 1 verb 3 元素 + 1 视觉组 1:1 严格** — 26 元素 1:1 严格 0 漏 0 改 0 反序 0 
反向; (4) **Stage 4 Echo 1 verb 3 元素 + 1 视觉组 1:1 严格** — 26 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (5) **Stage 5 Wave 1 verb 3 元素 + 
1 视觉组 1:1 严格** — 26 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (6) **Stage 6 Whisper 1 verb 3 元素 + 1 视觉组 1:1 严格** (T245 #162 落地 
Whisper 1 视觉组 constant 球 + 2px 描边 + 球心亮点 + Muted Mauve 核, 6 verb 唯一"不扩散" 几何与 5 verb 动态几何 视觉组连贯) — 26 元素 1:1 严格 0 漏 0 改 
0 反序 0 反向. 26 元素 1:1 同步, 1 漏 1 hook / 1 漏 1 assertion / 1 漏 1 视觉组 / 1 漏 1 视觉组连贯契约 = 1 verb / 1 perk / 1 hook 顺序 / 1 
`_player` non-null assertion / 1 视觉组 / 1 视觉组连贯契约 扩展 0 26 元素 0 闭环 0 漂动. T245 #162 + T250 #168 + T300 #226 0 触碰既有 0 改 
`_verb_ability_base.gd` `_ready()` 任何 1 字符 (0 改 Pulse 1 verb 任何 1 字符, 0 改 Bind 1 verb 任何 1 字符, 0 改 Cut 1 verb 任何 1 字符, 
0 改 Echo 1 verb 任何 1 字符, 0 改 Wave 1 verb 任何 1 字符, 0 改 Whisper 1 verb 任何 1 字符).

- **修复** (跨 1 套 polish 模式 T301 #227 落地后已知 26 元素 0 例外):
  - **T301 #227 首个 6 verb `_ready()` + `_exit_tree()` 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 
lifecycle 1:1 严格分离契约 落地 (`_verb_ability_base.gd` `_ready()` 1 共享方法 + `_exit_tree()` 1 共享方法 + `_player` non-null 
assertion 1 边 0 触碰既有 1:1 严格 + 显式契约 1 段 "Lifecycle contract" + 6 verb 各自双 hook 1:1 严格 — 5 verb `_exit_tree()` 0 override 
0 触碰既有 1:1 严格继承 + 1 verb `_exit_tree()` 1 override 1:1 严格 byte-identical cleanup 镜像 base + 6 verb 0 自己加 `assert(_player 
!= null, ...)` 0 触碰既有 1:1 严格继承 + 5 verb 视觉组 5 段 1:1 严格 + 1 verb 视觉组 1 段 1:1 严格 (Whisper 唯一 constant 球 + 2px 描边 + 球心亮点 + 
Muted Mauve 核) + 1 显式契约 "6 verb 视觉组连贯 lifecycle 共享视觉语法" 1 段 + 1 视觉组 0 触碰既有 1:1 严格)** — §9.6.45 段 是 1 套 polish 模式首个 1:1 
包含 6 verb × 3 元素 + 1 base assertion + 5 verb 视觉组 5 段 + 1 verb 视觉组 1 段 + 1 显式契约 + 1 视觉组 0 触碰既有 = 26 元素 1:1 严格 的 6 verb 双 
hook 串联 + `_player` non-null assertion + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 polish 模式, 26 元素 1:1 严格分离 (1 Pulse 3 元素 1:1 
严格 0 漏 0 改 + 1 Pulse 1 视觉组 1:1 严格 0 漏 0 改 + 1 Bind 3 元素 1:1 严格 0 漏 0 改 + 1 Bind 1 视觉组 1:1 严格 0 漏 0 改 + 1 Cut 3 元素 1:1 
严格 0 漏 0 改 + 1 Cut 1 视觉组 1:1 严格 0 漏 0 改 + 1 Echo 3 元素 1:1 严格 0 漏 0 改 + 1 Echo 1 视觉组 1:1 严格 0 漏 0 改 + 1 Wave 3 元素 1:1 严格 
0 漏 0 改 + 1 Wave 1 视觉组 1:1 严格 0 漏 0 改 + 1 Whisper 3 元素 1:1 严格 0 漏 0 改 + 1 Whisper 1 视觉组 1:1 严格 0 漏 0 改 (T245 #162 落地) + 
1 base assertion 0 触碰既有 1:1 严格 + 1 显式契约 "6 verb 视觉组连贯 lifecycle 共享视觉语法" 1:1 严格 + 1 视觉组 0 触碰既有 1:1 严格, 0 漏 1 元素 0 改 1 字段 
0 漏 1 字段 0 反向), 0 触碰既有 (T301 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / 
§9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / 
§9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / 
§9.6.43 / §9.6.44 35 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / 
README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / 
REVIEW_LOG_ARCHIVE.md 内容).
  - 跨 1 套 polish 模式 T301 #227 落地后 0 漂动, §9.6.45 段 1:1 严格 (26 元素 1:1 严格分离 + Pulse 1 verb 4 元素 1:1 严格 + Bind 1 verb 4 元素 
1:1 严格 + Cut 1 verb 4 元素 1:1 严格 + Echo 1 verb 4 元素 1:1 严格 + Wave 1 verb 4 元素 1:1 严格 + Whisper 1 verb 4 元素 1:1 严格 + base 
assertion 1 元素 1:1 严格 + 显式契约 1 元素 1:1 严格 + 视觉组 0 触碰既有 1 元素 1:1 严格) 100% 闭环, 26 元素 1:1 严格分离. 1 套 polish 模式 0 后续 主要 
pre-existing 风险.

- **预防**:
  1. 任何 polish 期给 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 加新 1 verb 
(e.g. 未来加 7th verb "Drain" 接入双 hook 串联 + `_player` non-null assertion + 视觉组连贯 lifecycle 系统) 或加新 1 perk 或加新 1 hook 顺序 
或加新 1 `_player` non-null assertion 或加新 1 视觉组 或加新 1 视觉组连贯契约 任务**必须**严格按 26 元素 1:1 复制, 0 反序 0 漏 0 改 1 字段.
  2. **26 元素 0 触碰边界** — 6 verb 双 hook 串联 + `_player` non-null assertion + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 (6 verb × 3 
元素 = 18 元素 0 触碰 + 1 base assertion 0 触碰既有 + 5 verb 视觉组 5 段 + 1 verb 视觉组 1 段 + 1 显式契约 + 1 视觉组 0 触碰既有 = 26 元素) 任何 1 元素 0 
改 0 删 0 重排 (26 元素 0 触碰边界), 任何"修 1 verb"polish 走 Stage 1 1:1 严格 (改 verb 既有 26 元素 时**必须**保持 26 元素 0 改 0 漏 0 反向 0 反序 0 
触碰既有 1 元素).
  3. **0 改 1 字段 0 漏 1 字段 0 反向** (T301 落地后 0 触碰边界) — 26 元素 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 漏 1 字段 0 反向, 改 1 字段 / 漏 1 字段 / 反向 
1 字段 = 1 verb / 1 perk / 1 hook 顺序 / 1 `_player` non-null assertion / 1 视觉组 / 1 视觉组连贯契约 扩展 0 26 元素 0 漂动, source-grep 验证 
26 元素 0 漏 0 改 1 字段 0 漏 1 字段 0 改 1 字符 0 反向.
  4. **0 改 1 边 0 漏 1 边 0 反向** (T301 落地后 0 触碰边界) — 接入契约 (1 Pulse 1 verb 4 元素 1:1 严格 + 1 Bind 1 verb 4 元素 1:1 严格 + 1 Cut 
1 verb 4 元素 1:1 严格 + 1 Echo 1 verb 4 元素 1:1 严格 + 1 Wave 1 verb 4 元素 1:1 严格 + 1 Whisper 1 verb 4 元素 1:1 严格 + 1 base 
assertion 0 触碰既有 1:1 严格 + 1 显式契约 + 1 视觉组 0 触碰既有) 1:1 严格分离 0 漏 0 改 0 改 1 边 0 漏 1 边 0 反向.
  5. **T162 brittle 修复流程 0 触碰边界** — 26 元素 0 副作用 子段 1:1 严格分离 0 漏 0 改 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 触碰既有 0 改 1 段 0 
触碰既有 0 改 1 文档段 0 触碰既有 0 改 1 章节段 0 触碰既有 0 改 1 段标题 0 触碰既有 0 改 1 段内容 0 触碰既有 0 改 1 段结构.
  6. **1 套 polish 模式 0 互混 0 复用 0 共享 唯一性 标注 0 改 1 段 0 漏 1 段 0 反向** — §9.6.45 段 唯一性 标注 1:1 严格分离 0 漏 0 改 0 改 1 套 polish 模式 
0 漏 1 套 polish 模式 0 反向 0 触碰既有 0 改 1 段 (本节是 36 套 polish 模式**唯一**关注 6 verb 双 hook 串联 + `_player` non-null assertion + 6 
verb 视觉组连贯 lifecycle 1:1 严格分离契约 的 1 套, 0 互混 0 复用 0 共享).
  7. **§9.6.45 是 36 套 polish 模式**唯一**关注 "6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 
lifecycle 1:1 严格分离契约 (`_verb_ability_base.gd` `_ready()` 1 共享方法 + `_exit_tree()` 1 共享方法 + `_player` non-null assertion 
1 边 0 触碰既有 1:1 严格 + 显式契约 1 段 "Lifecycle contract" + 6 verb 双 hook 1:1 严格 — 5 verb `_exit_tree()` 0 override + 1 verb 
`_exit_tree()` 1 override 1:1 严格 byte-identical cleanup 镜像 base + 6 verb 0 自己加 `assert(_player != null, ...)` 0 触碰既有 
1:1 严格继承 + 5 verb 视觉组 5 段 1:1 严格 (Pulse 同心圆环扩散波 + Coral Pulse 核 / Bind 向内螺旋涡 + Muted Violet 核 / Cut 水平锋利斩 + 4 三角碎片 + 
Amber Voice 核 / Echo 玻璃护盾 + 8 棱镜折射 + 双向反弹 + Glass Cyan 核 / Wave 双环扩散 + 8 棱镜光线 + amber 中心 + Pale Resonance 核) + 1 verb 
视觉组 1 段 1:1 严格 (Whisper constant 球 + 2px 描边 + 球心亮点 + Muted Mauve 核, T245 #162 落地) + 1 显式契约 "6 verb 视觉组连贯 lifecycle 
共享视觉语法 (深海军蓝背景圆盘 + Glass Cyan 外环 + verb 主色 core)" 1 段 + 1 视觉组 0 触碰既有 1:1 严格 + 0 触碰游戏代码 + 0 触碰 §10 决策记录流程 0 触碰既有" 的 1 
套** — 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / 
§9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / 
§9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 
35 套 polish 模式 0 互混 0 复用 0 共享. §9.6.26 (T275 #194 落地) 关注 "6 verb icon 视觉组 5 段 canonical 1:1 序列" (与 6 verb 双 hook 串联 + 
`_player` non-null assertion + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 0 相关, 5 段仅 6 verb icon 视觉组 0 涉及 6 verb 双 hook 串联 + 
`_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 0 涉及), §9.6.22 (T268 #186 落地) 关注 "6 verb VFX 
视觉组 5 段" (与 6 verb 视觉组连贯 lifecycle 0 相关, 5 段仅 6 verb VFX 视觉组 0 涉及 6 verb 双 hook 串联 + `_player` non-null assertion 0 
触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 0 涉及), §9.6.44 (T300 #226 落地) 关注 "6 verb 双 hook 串联 + `_player` non-null 
assertion 0 触碰既有 1:1 严格分离契约" (与 6 verb 视觉组连贯 lifecycle 0 相关, 19 元素 1:1 严格 0 涉及 6 verb 视觉组连贯 lifecycle 1:1 严格分离, §9.6.45 
是 §9.6.44 + 6 verb 视觉组连贯 lifecycle 7 元素 拼接段, 1 套 polish 模式 × 26 元素 = 26 元素 1:1 严格 镜像 1 套 polish 模式 × 19 元素 = 19 元素 1:1 
严格, 一是 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有, 一是 6 verb 双 hook 串联 + `_player` non-null assertion 0 
触碰既有 + 6 verb 视觉组连贯 lifecycle; 1 套 polish 模式 × 26 元素 = 26 元素 1:1 严格 ⊃ 1 套 polish 模式 × 19 元素 = 19 元素 1:1 严格). 任何"加新 1 
verb"或"加新 1 perk"或"加新 1 hook 顺序"或"加新 1 `_player` non-null assertion"或"加新 1 视觉组"或"加新 1 视觉组连贯契约"必须**先**按 §9.6.45 26 元素 
1:1 镜像 (26 元素 1:1 严格, 0 漏 4 元素 0 改 1 字符 0 反序 0 反向, **0 漏 4 元素**).
  8. 已知 drift risk: 6 verb 双 hook 串联 + `_player` non-null assertion + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 26 元素 接入 
**可能**漏 4 元素 / 1 边 1:1 镜像 (e.g. 加新 1 verb 时漏 Stage 1 Pulse 1 verb 中 4 元素 0 镜像, 1 verb 0 26 元素 0 闭环). 监控建议: 任何"未来给 6 
verb 双 hook 串联 + `_player` non-null assertion + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 加新 1 verb / 加新 1 perk / 加新 1 hook 顺序 
/ 加新 1 `_player` non-null assertion / 加新 1 视觉组 / 加新 1 视觉组连贯契约"必须**先**在 26 元素 (Stage 1 Pulse 1 verb 4 元素 1:1 严格 + Stage 
2 Bind 1 verb 4 元素 1:1 严格 + Stage 3 Cut 1 verb 4 元素 1:1 严格 + Stage 4 Echo 1 verb 4 元素 1:1 严格 + Stage 5 Wave 1 verb 4 元素 
1:1 严格 + Stage 6 Whisper 1 verb 4 元素 1:1 严格 + 1 base assertion 0 触碰既有 1:1 严格 + 1 显式契约 "6 verb 视觉组连贯 lifecycle 共享视觉语法" 
1:1 严格 + 1 视觉组 0 触碰既有 1:1 严格) 1:1 镜像 0 漏 4 元素 / 0 漏 1 边 / 0 漏 1 字符 / **0 漏 1 行**.

**0 副作用**: T301 (#227) 任何 const / var 0 触碰 (`_verb_ability_base.gd` 0 改 0 删 0 重排 `_ready()` 方法任何 1 字符 (含 
`assert(_player != null, "VerbAbilityBase subclass must be child of CharacterBody2D")` 1 边 0 触碰既有 1:1 严格); 
`_verb_ability_base.gd` 0 改 0 删 0 重排 `_exit_tree()` 方法任何 1 字符; `pulse_ability.gd` 0 改 0 删 0 重排 任何 1 字符; 
`bind_ability.gd` 0 改 0 删 0 重排 任何 1 字符; `cut_ability.gd` 0 改 0 删 0 重排 任何 1 字符; `echo_ability.gd` 0 改 0 删 0 重排 任何 1 字符; 
`resonance_wave_ability.gd` 0 改 0 删 0 重排 `_ready()` / `_exit_tree()` 方法任何 1 字符; `whisper_ability.gd` 0 改 0 删 0 重排 任何 1 
字符; `assets/ui/pulse_icon/` 0 改 0 删 0 重排 任何 1 像素 (T245 #162 落地后 1 视觉组 1:1 严格); `assets/ui/bind_icon/` 0 改 0 删 0 重排 任何 1 
像素; `assets/ui/cut_icon/` 0 改 0 删 0 重排 任何 1 像素; `assets/ui/echo_icon/` 0 改 0 删 0 重排 任何 1 像素; `assets/ui/wave_icon/` 0 改 
0 删 0 重排 任何 1 像素; `assets/ui/whisper_icon/` 0 改 0 删 0 重排 任何 1 像素; CONTRIBUTING.md §9.6.45 段 0 改 0 删 0 重排, 仅新加 1 段 (~50 
行)); 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / 
§9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / 
§9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 
35 套 polish 模式 任何 1 字符; 0 触碰 check_smoke_consistency.sh (T301 0 改任何规则); 0 触碰 _parse_recent_section.py + 
pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 
任何 gameplay code 0 改, 0 触碰 `_verb_ability_base.gd` `_ready()` / `_exit_tree()` 方法任何 1 字符 (含 `assert(_player != null, 
"VerbAbilityBase subclass must be child of CharacterBody2D")` 1 边 0 触碰既有 1:1 严格), 0 触碰 6 verb 各自双 hook 任何 1 字符, 0 触碰 6 
verb 0 自己加 `assert(_player != null, ...)` 任何 1 字符, 0 触碰 6 verb 视觉组任何 1 像素); 0 行为变化, 1 文档 1 段 (§9.6.45 ~50 行) + 0 字节码修改 
(T301 仅文档化既有 6 verb 双 hook 串联 + `_player` non-null assertion + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约, 0 改 
`_verb_ability_base.gd` 任何 1 字符 0 改 6 verb 各自双 hook 任何 1 字符 0 触碰既有 0 改 6 verb 视觉组任何 1 像素) + 1 smoke test (T301 80+ 断言).

**与 §9.6.44 关系** — §9.6.44 (T300 #226 落地) 关注 "6 verb `_ready()` + `_exit_tree()` 双 hook 串联 + `_player` non-null 
assertion 0 触碰既有 1:1 严格分离契约" (19 元素 1:1 严格, 19 元素覆盖 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 1:1 严格分离契约 0 
涉及 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约). §9.6.45 关注 "6 verb `_ready()` + `_exit_tree()` 双 hook 串联 + `_player` non-null 
assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约" (26 元素 1:1 严格, 26 元素覆盖 6 verb 双 hook 串联 + `_player` non-null 
assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 0 涉及 6 verb 单 hook 1:1 严格分离契约). §9.6.44 与 §9.6.45 0 互混 0 复用 0 共享: 
§9.6.44 关注 "6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 1:1 严格分离契约" (与 6 verb 视觉组连贯 lifecycle 0 相关, 19 元素 
1:1 严格 0 涉及 6 verb 视觉组连贯 lifecycle 1:1 严格分离), §9.6.45 关注 "6 verb 双 hook 串联 + `_player` non-null assertion + 6 verb 
视觉组连贯 lifecycle 1:1 严格分离契约" (与 6 verb 单 hook 0 相关, 26 元素 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 26 元素 = 
26 元素 1:1 严格). §9.6.45 与 §9.6.44 是 "姊妹段 + 拼接段" (1 套 polish 模式 × 26 元素 = 26 元素 1:1 严格 包含 1 套 polish 模式 × 19 元素 = 19 元素 
1:1 严格, 一是 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有, 一是 6 verb 双 hook 串联 + `_player` non-null assertion 0 
触碰既有 + 6 verb 视觉组连贯 lifecycle; 1 套 polish 模式 × 26 元素 = 26 元素 1:1 严格 ⊃ 1 套 polish 模式 × 19 元素 = 19 元素 1:1 严格).

**与 §9.6.26 关系** — §9.6.26 (T275 #194 落地) 关注 "6 verb icon 视觉组 5 段 canonical 1:1 序列" (5 段 = 1 icon visual 段 1:1 严格 + 1 
视觉组连贯 段 1:1 严格 + 1 canonical 1:1 序列 段 1:1 严格 + 1 0 触碰既有 段 1:1 严格 + 1 5 段 5 段关系 段 1:1 严格). §9.6.45 关注 "6 verb 双 hook 串联 
+ `_player` non-null assertion + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约" (26 元素 1:1 严格, 26 元素覆盖 6 verb 视觉组连贯 lifecycle 1:1 
严格分离契约 0 涉及 6 verb icon 视觉组 5 段 canonical 1:1 序列). §9.6.26 与 §9.6.45 0 互混 0 复用 0 共享: §9.6.26 关注 "6 verb icon 视觉组 5 段 
canonical 1:1 序列" (与 6 verb 双 hook 串联 + `_player` non-null assertion + 6 verb 视觉组连贯 lifecycle 0 相关, 5 段仅 6 verb icon 
视觉组 canonical 1:1 序列 0 涉及 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 0 
涉及), §9.6.45 关注 "6 verb 双 hook 串联 + `_player` non-null assertion + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约" (与 6 verb icon 
视觉组 0 相关, 26 元素 1:1 严格 镜像 1 套 polish 模式 既有 1:1 严格分离, 1 套 polish 模式 × 26 元素 = 26 元素 1:1 严格). 任何"加新 1 verb"必须**先**按 
§9.6.45 26 元素 1:1 复制 (26 元素 1:1 严格, 0 漏 4 元素 0 改 1 字段 0 反向) **或** 按 §9.6.26 5 段 1:1 复制 (5 段 1:1 严格, 0 漏 1 段 0 改 1 字段 0 
反向), **再**按 §9.6.45 26 元素 1:1 同步 (26 元素 1:1 严格, 0 漏 4 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.26 任何 1 字符) **或** 按 
§9.6.26 5 段 1:1 同步 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.45 任何 1 字符).

**与 §9.6.22 关系** — §9.6.22 (T268 #186 落地) 关注 "6 verb VFX 视觉组 5 段" (5 段 = 1 VFX visual 段 1:1 严格 + 1 视觉组连贯 段 1:1 严格 + 1 5 
段序列 段 1:1 严格 + 1 0 触碰既有 段 1:1 严格 + 1 5 段关系 段 1:1 严格). §9.6.45 关注 "6 verb 双 hook 串联 + `_player` non-null assertion + 6 
verb 视觉组连贯 lifecycle 1:1 严格分离契约" (26 元素 1:1 严格). §9.6.22 与 §9.6.45 0 互混 0 复用 0 共享: §9.6.22 关注 "6 verb VFX 视觉组 5 段" (与 6 
verb 双 hook 串联 + `_player` non-null assertion + 6 verb 视觉组连贯 lifecycle 0 相关, 5 段仅 6 verb VFX 视觉组 0 涉及 6 verb 双 hook 串联 
+ `_player` non-null assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 0 涉及), §9.6.45 关注 "6 verb 双 hook 串联 + 
`_player` non-null assertion + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约" (与 6 verb VFX 视觉组 0 相关, 26 元素 1:1 严格). 任何"加新 1 
verb"必须**先**按 §9.6.45 26 元素 1:1 复制 (26 元素 1:1 严格, 0 漏 4 元素 0 改 1 字段 0 反向) **或** 按 §9.6.22 5 段 1:1 复制 (5 段 1:1 严格, 0 漏 1 
段 0 改 1 字段 0 反向), **再**按 §9.6.45 26 元素 1:1 同步 (26 元素 1:1 严格, 0 漏 4 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.22 任何 
1 字符) **或** 按 §9.6.22 5 段 1:1 同步 (5 段 1:1 严格, 0 漏 1 段 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 §9.6.45 任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 是"brittle 修复流程" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 0 改 1 字段 0 改 1 字符 
0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 锚点, 33 轮 0 后续 持续收敛), §9.6.45 是"6 verb 双 hook 串联 + `_player` non-null assertion + 6 verb 
视觉组连贯 lifecycle 1:1 严格分离契约" (26 元素 1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约). T162 与 §9.6.45 0 互混 0 复用 0 共享: T162 关注 
"brittle 修复流程 5 步骤" (1 expect 反转 + 1 docblock 说明 + 1 段 find 反转 + 0 触碰既有 + 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 段 0 漏 1 
锚点, 5 步骤 1:1 严格), §9.6.45 关注 "26 元素 1:1 严格分离契约 + 1 套 polish 模式 既有 1:1 严格分离契约" (与 5 步骤 0 相关, 1 套 polish 模式 × 26 元素 = 26 
元素 1:1 严格 镜像 5 步骤既有 1:1 严格分离). 任何"加新 1 brittle 修复"必须**先**按 T162 5 步骤 1:1 复制 (5 步骤 1:1 严格, 0 漏 1 步骤 0 改 1 字段 0 反向 0 漏 1 
字段 0 漏 1 段 0 漏 1 锚点), **再**按 §9.6.45 26 元素 1:1 镜像 (26 元素 1:1 严格, 0 漏 4 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 漏 1 字段 0 漏 1 
段 0 漏 1 锚点).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.45 6 verb × 4 元素 + 1 base assertion + 1 显式契约 + 1 视觉组 0 触碰既有 = 26 元素 走 §9.1 9 
步落地的 1 步 (Stage 2 ability 子类, 跨 6 verb 各 4 元素 + 1 base assertion + 1 显式契约 + 1 视觉组 0 触碰既有), 0 触碰 8 步 (1 `verb` 枚举 + 1 
`const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染). 任何"加新 1 
verb"必须**先**按 §9.6.45 26 元素 1:1 镜像 (26 元素 1:1 严格, 0 漏 4 元素 0 漏 1 边 0 改 1 字段 0 改 1 字符 0 反向 0 触碰既有 8 步 (`verb` 枚举 + 
`const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `vfx` 视觉组 + `audio` preset 字段 + `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 
步"必须**先**在 §9.6.45 26 元素 加 1 边 (1 Pulse 4 元素 entry 或 1 Bind 4 元素 边 或 1 Cut 4 元素 边 或 1 Echo 4 元素 边 或 1 Wave 4 元素 边 或 1 
Whisper 4 元素 边 或 1 base assertion 0 触碰既有 1 边 或 1 显式契约 1 边 或 1 视觉组 0 触碰既有 1 边), 0 触碰既有 8 步 (1 `verb` 枚举 + 1 `const` 字段 + 
1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `vfx` 视觉组 + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).
