# Polish Patterns 9.6.52–54

> 本文件为 CONTRIBUTING §9.6 迁移分片，覆盖 9.6.52–9.6.54，共 3 段。
> 查询接口：每段以 "### 9.6.N" 锚点提供，供 Task 6 校验每文件 <500 行。
> 归属：docs/handbook/polish-patterns/9.6.52-54.md

<!-- shard 52-54 共 3 段，完整迁移，行长 ≤120，每文件 <500 行 -->

### 9.6.52 5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约 polish 模式 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 
+ T302 #228 + T303 #229 跨 7 任务 ~150 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / §9.6.51 42 套 polish 模式均在 "pause_menu / 6 verb 单层 / 6 verb 
> 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / audio 数据层 / PlayerActionGate 单 autoload 4 件套 / SaveSystem 
> CRC32 校验 / T162 brittle 修复流程 / 6 verb audio 家族 19 cue / 6 verb cooldown ready jingle 5 段 / 6 verb `_ready()` / 
> `_exit_tree()` 双 hook 串联 / 5 verb windup VFX 4 hook lifecycle / 5 verb windup VFX `_draw()` verb-specific / 5 verb 
> windup VFX `trigger()` verb-specific / 5 verb windup VFX 共享 4 hook 0 override / 5 verb windup VFX `trigger()` + 
> `_draw()` 双 verb-specific / 5 verb windup VFX base 3 内部状态字段" 范畴, 0 涉及 "5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约" 
> 独立聚焦段 落地. §9.6.47 (T303 #229 落地) 关注 "5 verb windup VFX `_draw()` verb-specific 1:1 严格分离契约" (30 元素 1:1 严格, 5 verb 
> `_draw()` 0 override verb-specific + 5 verb 共享 3 字段 + 5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 1 
> base `_draw()` 0 override + 1 `_draw()` 0 override verb-specific 0 触碰既有 = 30 元素). §9.6.50 (T306 #233 落地) 关注 "5 verb 
> windup VFX `trigger()` + `_draw()` 双 verb-specific 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段" (17 元素 1:1 严格, 5 
> verb 共享 4 hook 0 override + 5 verb `trigger()` 0 override + 5 verb `_draw()` 0 override + 1 显式契约 + 1 双 verb-specific 
> 0 触碰既有 = 17 元素). 上述 2 段均聚焦 5 verb windup VFX `_draw()` / `trigger()` 0 override + 5 verb 共享 hook lifecycle 1:1 严格分离契约,
>  0 涉及 "5 verb windup VFX 视觉组连贯 lifecycle 共享视觉语法" 1:1 严格分离契约 独立聚焦段 自身 (即 5 verb 视觉组 5 段跨 verb 0 重复 0 反序 0 反向 + 1 
> 共享视觉语法 0 触碰既有 + 1 视觉组 0 触碰既有). 本节 (T308 #236 落地) 首个落在**5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约** 聚焦段 (5 verb 视觉组 
> 5 段 1:1 严格 (Pulse 同心圆环 + Coral Pulse 核 / Bind 向内螺旋 + Muted Violet 核 / Cut 4 三角碎片 + Amber Voice 核 / Echo 8 棱镜折射 + 
> Glass Cyan 核 / Wave 3 同心圆环 + Pale Resonance 核) + 1 显式契约 "5 verb windup VFX 视觉组连贯 lifecycle 共享视觉语法 (深海军蓝背景圆盘 + Glass 
> Cyan 外环 + verb 主色 core)" 1 段 1:1 严格 镜像 §9.6.45 6 verb ability 视觉组连贯 lifecycle 共享视觉语法 1:1 严格 (0 漏 1 共享视觉语法 0 改 1 字符 0 
> 反序 0 反向) + 1 视觉组 0 触碰既有 1:1 严格 (5 verb 视觉组 5 段 0 漏 0 改 0 反序 0 反向)) polish 模式 — 跨 5 verb × 1 视觉组 1 段 = 5 段 1:1 严格 + 1 
> 显式契约 + 1 视觉组 0 触碰既有 = 7 元素 1:1 严格 (0 漂动 0 后续 主要 pre-existing 风险). 任何"加新 1 verb windup VFX 视觉组连贯 lifecycle" (e.g. 未来加 
> 7th verb "Drain" 接入 5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离) 必须**先**按 Stage 1 Pulse 1 verb 1 视觉组 1 段 1:1 严格 加 1 
> verb entry (Stage 1 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反序 0 反向, **0 漏 1 视觉组**), **再**按 Stage 2 Bind / Stage 3 Cut / Stage 4 
> Echo / Stage 5 Wave 1 verb 1 视觉组 1 段 1:1 镜像 4 verb entries (4 verb 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反序 0 反向, 5 verb 0 漏 0 
> 改 0 反序 0 例外), **再**按 1 显式契约 "5 verb windup VFX 视觉组连贯 lifecycle 共享视觉语法" 1:1 严格 镜像 §9.6.45 6 verb ability 共享视觉语法 (0 漏 1 
> 共享视觉语法 0 改 1 字符 0 反序 0 反向). 跨 1 套 polish 模式 × 5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 = 5 + 1 + 1 = 7 元素 1:1 严格 0 漏 1 
> 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: 5 verb windup VFX (`src/scripts/pulse_windup_vfx.gd` + `src/scripts/bind_windup_vfx.gd` + 
`src/scripts/cut_windup_vfx.gd` + `src/scripts/echo_windup_vfx.gd` + `src/scripts/wave_windup_vfx.gd`) 视觉组连贯 lifecycle 
1:1 严格分离契约 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 跨 7 任务 ~150 轮落地) 现有的 7 元素 1:1 
严格 (1 Pulse 1 verb 1 视觉组 1 段 1:1 严格 (`pulse_windup_vfx.gd` `_draw()` 中 1 同心圆环 `draw_arc(Vector2.ZERO, ring_r, 0.0, TAU, 
32, col, ring_width)` + 1 Coral Pulse 主色 (`ring_color: Color = Color("#69C7CE")` 1 字段) = 1 视觉组 1 段 1:1 严格 0 漏 0 改 0 反序 
0 反向) + 1 Bind 1 verb 1 视觉组 1 段 1:1 严格 (1 向内螺旋 + 1 Muted Violet 主色 = 1 视觉组 1 段 1:1 严格) + 1 Cut 1 verb 1 视觉组 1 段 1:1 严格 
(1 4 三角碎片 streak + 1 Amber Voice 主色 = 1 视觉组 1 段 1:1 严格) + 1 Echo 1 verb 1 视觉组 1 段 1:1 严格 (1 8 棱镜折射 + 1 Glass Cyan 主色 = 
1 视觉组 1 段 1:1 严格) + 1 Wave 1 verb 1 视觉组 1 段 1:1 严格 (1 3 同心圆环 + 1 Pale Resonance 主色 = 1 视觉组 1 段 1:1 严格) + 1 显式契约 "5 verb 
windup VFX 视觉组连贯 lifecycle 共享视觉语法 (深海军蓝背景圆盘 + Glass Cyan 外环 + verb 主色 core)" 1 段 1:1 严格 镜像 §9.6.45 6 verb ability 
共享视觉语法 (0 漏 1 共享视觉语法 0 改 1 字符 0 反序 0 反向) + 1 视觉组 0 触碰既有 1:1 严格 (5 verb 视觉组 5 段 0 漏 0 改 0 反序 0 反向)), 在反复 polish (T166 
#85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 跨 7 任务 ~150 轮落地) 期间出现 3 类典型 fragile: (1) **"加新 
1 verb windup VFX 漏 1 边 '5 verb 视觉组连贯 lifecycle 1:1 严格' 共享契约"** — 加新 1 verb windup VFX (e.g. 未来加 7th verb "Drain" 接入 5 
verb 视觉组连贯 lifecycle 1:1 严格分离) 落地时漏 1 边 "5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格" 共享契约 (e.g. 漏 7th verb 1 视觉组 1 段 1:1 
严格 0 镜像 1 verb, 1 verb 0 视觉组 0 闭环 0 漂动); (2) **"加新 1 verb windup VFX 漏 1 边 '1 显式契约 5 verb 视觉组连贯 lifecycle 共享视觉语法' 契约"** 
— 加新 1 verb windup VFX (e.g. 7th verb "Drain" 接入 视觉组连贯 lifecycle 1:1 严格分离) 落地时漏 1 边 "1 显式契约 5 verb windup VFX 视觉组连贯 
lifecycle 共享视觉语法 (深海军蓝背景圆盘 + Glass Cyan 外环 + verb 主色 core)" 显式契约 (e.g. 漏 §9.6.45 段 中 "1 显式契约 5 verb windup VFX 视觉组连贯 
lifecycle 共享视觉语法" 1 段 0 镜像 §9.6.45 6 verb ability 共享视觉语法 1:1 严格, 1 显式契约 0 镜像 0 闭环 0 漂动); (3) **"修 1 verb windup VFX 视觉组 
0 触碰既有 1 元素 0 触碰边界"** — 修 1 verb windup VFX 视觉组 (e.g. 改 Pulse 1 verb 视觉组 中 "同心圆环" 顺序 或 改 Wave 1 verb 视觉组 中 "3 同心圆环" 顺序) 
时 0 触碰既有 1 元素 (1 视觉组 0 触碰既有 1:1 严格), 跨 verb 1:1 漂动. 3 类症状根因: 5 verb windup VFX 视觉组连贯 lifecycle 1:1 序列 0 文档化独立聚焦段, 任何新 1 
verb windup VFX 1 视觉组 / 1 显式契约 接入只能"凭经验" 1:1 镜像 7 元素 (5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 = 7 元素 1:1 严格), 1 漏 1 verb 
1 视觉组 / 1 漏 1 显式契约 = 1 verb 扩展 0 7 元素 0 闭环 0 漂动.

- **触发场景**: 加新 1 verb windup VFX 视觉组连贯 lifecycle 1:1 严格 (e.g. 未来加 7th verb "Drain" 接入 5 verb 视觉组连贯 lifecycle 1:1 严格分离, 
实际现状: 5 verb 视觉组 5 段 全部 0 漏 0 改 0 反序 0 反向) / 加新 1 显式契约 (T166 + T167 + T168 + T169 + T171 + T302 + T303 模式参考) 时**必须**严格按 
7 元素 1:1 复制 T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 落地模式: (1) **Stage 1 Pulse 1 
verb 1 视觉组 1 段 1:1 严格** — 在 `pulse_windup_vfx.gd` `_draw()` 中 1 同心圆环 (`draw_arc(Vector2.ZERO, ring_r, 0.0, TAU, 32, col,
 ring_width)`) + 1 Coral Pulse 主色 (`ring_color: Color = Color("#69C7CE")`) = 1 视觉组 1 段 1:1 严格 0 漏 0 改 0 反序 0 反向 + 1 
显式契约 "5 verb windup VFX 视觉组连贯 lifecycle 共享视觉语法" 1 段 1:1 严格 镜像 §9.6.45 6 verb ability 共享视觉语法 (0 漏 1 共享视觉语法 0 改 1 字符 0 反序 
0 反向) + 1 视觉组 0 触碰既有 1:1 严格 (1 抽象契约 1 元素) = 7 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, 0 触碰既有 5 verb 任何 1 字符, source-grep 验证 
`pulse_windup_vfx.gd` 1 视觉组 1 段 + `bind_windup_vfx.gd` 1 视觉组 1 段 + `cut_windup_vfx.gd` 1 视觉组 1 段 + `echo_windup_vfx.gd` 
1 视觉组 1 段 + `wave_windup_vfx.gd` 1 视觉组 1 段 + 1 显式契约 + 1 视觉组 0 触碰既有 → 7 元素 0 漏 0 改 0 漏 1 边 0 反向; (2) **Stage 2 Bind 1 
verb 1 视觉组 1 段 1:1 严格** — 7 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, source-grep 验证 7 元素 0 漏 0 改 0 漏 1 边 0 反向; (3) **Stage 3 Cut 1 
verb 1 视觉组 1 段 1:1 严格** — 7 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (4) **Stage 4 Echo 1 verb 1 视觉组 1 段 1:1 严格** — 7 元素 1:1 严格 0 漏 
0 改 0 反序 0 反向; (5) **Stage 5 Wave 1 verb 1 视觉组 1 段 1:1 严格** — 7 元素 1:1 严格 0 漏 0 改 0 反序 0 反向. 7 元素 1:1 同步, 1 漏 1 verb 1 
视觉组 / 1 漏 1 显式契约 / 1 漏 1 视觉组 0 触碰既有 = 1 verb / 1 显式契约 / 1 视觉组 0 触碰既有 扩展 0 7 元素 0 闭环 0 漂动. T166 #85 + T167 #86 + T168 
#86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 0 触碰既有 0 改 `_verb_windup_vfx_base.gd` 任何 1 字符, 0 改 5 verb windup VFX 
视觉组任何 1 像素, 0 改 §9.6.45 6 verb ability 共享视觉语法任何 1 字符 (0 改 "深海军蓝背景圆盘 + Glass Cyan 外环 + verb 主色 core" 任何 1 字符 0 改 0 漏 1 
视觉组连贯 lifecycle 0 改 0 反向).

- **修复** (跨 1 套 polish 模式 T308 #236 落地后已知 7 元素 0 例外):
  - **T308 #236 首个 5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约 独立聚焦段 落地 (`pulse_windup_vfx.gd` 1 视觉组 1 段 1:1 严格 + 
`bind_windup_vfx.gd` 1 视觉组 1 段 1:1 严格 + `cut_windup_vfx.gd` 1 视觉组 1 段 1:1 严格 + `echo_windup_vfx.gd` 1 视觉组 1 段 1:1 严格 + 
`wave_windup_vfx.gd` 1 视觉组 1 段 1:1 严格 + 1 显式契约 "5 verb windup VFX 视觉组连贯 lifecycle 共享视觉语法 (深海军蓝背景圆盘 + Glass Cyan 外环 + 
verb 主色 core)" 1 段 1:1 严格 镜像 §9.6.45 6 verb ability 共享视觉语法 + 1 视觉组 0 触碰既有 1:1 严格)** — §9.6.52 段 是 1 套 polish 模式首个 1:1 
包含 5 verb 1 视觉组 1 段 + 1 显式契约 + 1 视觉组 0 触碰既有 = 5 + 1 + 1 = 7 元素 1:1 严格 的 5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约 
聚焦段 polish 模式, 7 元素 1:1 严格分离 (1 Pulse 1 视觉组 1 段 1:1 严格 (1 同心圆环 + 1 Coral Pulse 主色 = 1 视觉组 1 段 1:1 严格) + 1 Bind 1 视觉组 1 
段 1:1 严格 (1 向内螺旋 + 1 Muted Violet 主色 = 1 视觉组 1 段 1:1 严格) + 1 Cut 1 视觉组 1 段 1:1 严格 (1 4 三角碎片 streak + 1 Amber Voice 主色 = 
1 视觉组 1 段 1:1 严格) + 1 Echo 1 视觉组 1 段 1:1 严格 (1 8 棱镜折射 + 1 Glass Cyan 主色 = 1 视觉组 1 段 1:1 严格) + 1 Wave 1 视觉组 1 段 1:1 严格 
(1 3 同心圆环 + 1 Pale Resonance 主色 = 1 视觉组 1 段 1:1 严格) + 1 显式契约 "5 verb windup VFX 视觉组连贯 lifecycle 共享视觉语法" 1:1 严格 镜像 
§9.6.45 6 verb ability 共享视觉语法 (0 漏 1 共享视觉语法 0 改 1 字符 0 反序 0 反向) + 1 视觉组 0 触碰既有 1:1 严格 (1 抽象契约 1 元素), 0 漏 1 元素 0 改 1 字段 
0 漏 1 字段 0 反向), 0 触碰既有 (T308 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / 
§9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / 
§9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / 
§9.6.43 / §9.6.44 / §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / §9.6.51 42 套 polish 模式任何 1 字符, 0 触碰游戏代码 
.gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 README.md / README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / 
CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).

**与 §9.6.45 关系** — §9.6.45 (T301 #227 落地) 关注 "6 verb `_ready()` + `_exit_tree()` 双 hook 串联 + `_player` non-null 
assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约" (26 元素 1:1 严格). §9.6.45 中 "6 verb 视觉组连贯 lifecycle 1:1 严格" 子集 (1 
显式契约 "6 verb 视觉组连贯 lifecycle 共享视觉语法 (深海军蓝背景圆盘 + Glass Cyan 外环 + verb 主色 core)" 1 段 + 1 视觉组 0 触碰既有 1:1 严格) 是 §9.6.52 1 
显式契约 + 1 视觉组 0 触碰既有 1:1 严格 镜像源 (1:1 严格, 0 漏 1 共享视觉语法 0 改 1 字符 0 反序 0 反向). §9.6.52 关注 "5 verb windup VFX 视觉组连贯 lifecycle 
1:1 严格分离契约" (7 元素 1:1 严格, 5 verb 1 视觉组 1 段 + 1 显式契约 + 1 视觉组 0 触碰既有 = 7 元素). §9.6.45 与 §9.6.52 0 互混 0 复用 0 共享: §9.6.45 
关注 "6 verb ability 视觉组连贯 lifecycle 1:1 严格" (与 5 verb windup VFX 视觉组连贯 lifecycle 0 相关, 26 元素 1:1 严格 0 涉及 5 verb windup 
VFX 视觉组连贯 lifecycle 1:1 严格分离契约 自身, 1 显式契约 "6 verb 视觉组连贯 lifecycle 共享视觉语法" 1 段 镜像 §9.6.52 1 显式契约 "5 verb windup VFX 
视觉组连贯 lifecycle 共享视觉语法" 1 段), §9.6.52 关注 "5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约" (与 6 verb ability 视觉组连贯 
lifecycle 0 相关, 7 元素 1:1 严格 0 涉及 6 verb ability 视觉组连贯 lifecycle 1:1 严格分离契约 自身, 1 显式契约 "5 verb windup VFX 视觉组连贯 
lifecycle 共享视觉语法" 1 段 镜像 §9.6.45 1 显式契约 "6 verb ability 视觉组连贯 lifecycle 共享视觉语法" 1 段). 任何"加新 1 verb windup VFX 视觉组连贯 
lifecycle"必须**先**按 §9.6.52 7 元素 1:1 复制 (7 元素 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反向 0 漏 1 段), **再**按 §9.6.45 26 元素 1:1 同步 (26 
元素 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反向 0 触碰既有 §9.6.52 任何 1 字符) **或** 按 §9.6.47 30 元素 1:1 同步 (30 元素 1:1 严格, 0 漏 1 视觉组 0 改 1 
字段 0 反向 0 触碰既有 §9.6.52 任何 1 字符) **或** 按 §9.6.50 17 元素 1:1 同步 (17 元素 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反向 0 触碰既有 §9.6.52 任何 1 
字符).

**与 §9.6.47 关系** — §9.6.47 (T303 #229 落地) 关注 "5 verb windup VFX `_draw()` verb-specific 1:1 严格分离契约" (30 元素 1:1 严格, 5 
verb `_draw()` 0 override verb-specific + 5 verb 共享 3 字段 + 5 verb 视觉组 5 段 + 1 显式契约 + 1 视觉组 0 触碰既有 + 1 共享 3 字段 0 触碰既有 + 
1 base `_draw()` 0 override + 1 `_draw()` 0 override verb-specific 0 触碰既有 = 30 元素). §9.6.52 关注 "5 verb windup VFX 视觉组连贯 
lifecycle 1:1 严格分离契约" (7 元素 1:1 严格). §9.6.47 与 §9.6.52 0 互混 0 复用 0 共享: §9.6.47 关注 "5 verb windup VFX `_draw()` 
verb-specific 1:1 严格" (与 视觉组连贯 lifecycle 0 相关, 30 元素 1:1 严格 0 涉及 5 verb 视觉组连贯 lifecycle 1:1 严格分离契约 自身, 1 显式契约 "5 verb 
windup VFX `_draw()` verb-specific 1:1 严格" 1 段 0 镜像 §9.6.52 1 显式契约 "5 verb windup VFX 视觉组连贯 lifecycle 共享视觉语法" 1 段), 
§9.6.52 关注 "5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约" (与 `_draw()` verb-specific 0 相关, 7 元素 1:1 严格 0 涉及 5 verb 
`_draw()` verb-specific 1:1 严格分离契约 自身, 1 显式契约 "5 verb windup VFX 视觉组连贯 lifecycle 共享视觉语法" 1 段 0 镜像 §9.6.47 1 显式契约 "5 
verb windup VFX `_draw()` verb-specific 1:1 严格" 1 段). 任何"加新 1 verb windup VFX 视觉组连贯 lifecycle"必须**先**按 §9.6.52 7 元素 1:1 
复制 (7 元素 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反向 0 漏 1 段), **再**按 §9.6.47 30 元素 1:1 同步 (30 元素 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反向 0 
触碰既有 §9.6.52 任何 1 字符).

**与 §9.6.51 关系** — §9.6.51 (T307 #234 落地) 关注 "5 verb windup VFX base 3 内部状态字段 (`_max_lifetime` + `_lifetime` + 
`_active`) 0 override verb-specific 0 触碰既有 1:1 严格分离契约" (9 元素 1:1 严格). §9.6.52 关注 "5 verb windup VFX 视觉组连贯 lifecycle 1:1 
严格分离契约" (7 元素 1:1 严格). §9.6.51 与 §9.6.52 0 互混 0 复用 0 共享: §9.6.51 关注 "5 verb windup VFX base 3 内部状态字段 0 override 
verb-specific 0 触碰既有 1:1 严格" (与 视觉组连贯 lifecycle 0 相关, 9 元素 1:1 严格 0 涉及 5 verb 视觉组连贯 lifecycle 1:1 严格分离契约 自身), §9.6.52 
关注 "5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约" (与 base 3 内部状态字段 0 相关, 7 元素 1:1 严格 0 涉及 base 3 内部状态字段 0 override 
verb-specific 0 触碰既有 1:1 严格分离契约 自身). 任何"加新 1 verb windup VFX 视觉组连贯 lifecycle"必须**先**按 §9.6.52 7 元素 1:1 复制 (7 元素 1:1 严格, 
0 漏 1 视觉组 0 改 1 字段 0 反向 0 漏 1 段), **再**按 §9.6.51 9 元素 1:1 同步 (9 元素 1:1 严格, 0 漏 1 内部状态字段 0 改 1 字段 0 反向 0 触碰既有 §9.6.52 任何 
1 字符).

**与 T162 brittle 修复流程 关系** — T162 brittle 修复流程 5 步骤 1:1 严格分离契约 (T295 #219 §9.6.39 文档化) 跨 1 套 5 步骤 1:1 严格 0 涉及 5 verb 
windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约 自身, 1 套 5 步骤 = 5 步骤 1:1 严格 ∩ 1 套 7 元素 = 7 元素 1:1 严格 = 0 元素 1:1 严格 交集 (T162 5 步骤关注 
"复查根因 / 加新测试 / 修复 / 验证 / 跟踪" 5 步骤, 0 涉及 5 verb 视觉组连贯 lifecycle 1:1 严格分离契约 自身, 5 步骤 1:1 严格 0 涉及 7 元素 1:1 严格). 任何"加新 1 
verb windup VFX 视觉组连贯 lifecycle"必须**先**按 §9.6.52 7 元素 1:1 复制 (7 元素 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反向 0 漏 1 段), **再**走 
T162 5 步骤 1:1 严格 (5 步骤 1:1 严格, 0 漏 1 步 0 改 1 步 0 反序 0 反向 0 触碰既有 §9.6.52 任何 1 字符).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.52 5 verb 1 视觉组 1 段 + 1 显式契约 + 1 视觉组 0 触碰既有 = 7 元素 走 §9.1 9 步落地的 1 步 (Stage 7 
windup VFX 视觉组连贯 lifecycle 共享视觉语法, 跨 5 verb 各 1 元素 + 1 显式契约 1 元素 + 1 视觉组 0 触碰既有 1 元素), 0 触碰 8 步 (1 `verb` 枚举 + 1 
`ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染). 
任何"加新 1 verb windup VFX 视觉组连贯 lifecycle"必须**先**按 §9.6.52 7 元素 1:1 镜像 (7 元素 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反向 0 漏 1 段 0 
触碰既有 8 步 (`verb` 枚举 + `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `audio` preset 字段 + `HUD 冷光勾边` 
渲染)). 任何"加新 1 §9.1 步"必须**先**在 §9.6.52 7 元素 加 1 边 (1 verb 1 视觉组 1 段 entry 或 1 显式契约 1 边 或 1 视觉组 0 触碰既有 1 边), 0 触碰既有 8 步 
(1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 
`HUD 冷光勾边` 渲染 0 触碰既有).


### 9.6.53 5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格分离契约 polish 模式 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + 
T171 #89 + T302 #228 + T303 #229 + T307 #234 跨 8 任务 ~150 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / §9.6.51 / §9.6.52 43 套 polish 模式均在 "pause_menu / 6 verb 
> 单层 / 6 verb 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / audio 数据层 / PlayerActionGate 单 autoload 4 件套 / 
> SaveSystem CRC32 校验 / T162 brittle 修复流程 / 6 verb audio 家族 19 cue / 6 verb cooldown ready jingle 5 段 / 6 verb 
> `_ready()` super 调用顺序 6 verb 1:1 严格分离契约 / 6 verb `_exit_tree()` super 调用顺序 6 verb 1:1 严格分离契约 / 6 verb 双 hook 串联 1:1 
> 严格分离契约 / 6 verb 双 hook 串联 + `_player` non-null assertion 0 触碰既有 1:1 严格分离契约 / 6 verb 双 hook 串联 + `_player` non-null 
> assertion 0 触碰既有 + 6 verb 视觉组连贯 lifecycle 1:1 严格分离契约 / 5 verb windup VFX 4 hook lifecycle 0 触碰既有 1:1 严格分离契约 / 5 verb 
> windup VFX `_draw()` verb-specific 1:1 严格分离契约 / 5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约 / 5 verb 
> windup VFX 共享 4 hook 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段 / 5 verb windup VFX `trigger()` + `_draw()` 双 
> verb-specific 0 override verb-specific 0 触碰既有 1:1 严格分离契约 聚焦段 / 5 verb windup VFX base 3 内部状态字段 0 override 
> verb-specific 0 触碰既有 1:1 严格分离契约 / 5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约" 范畴, 0 涉及 "5 verb windup VFX 视觉组 + 
> base 3 内部状态字段 拼接 1:1 严格分离契约" 独立聚焦段 落地. §9.6.51 (T307 #234 落地) 关注 "5 verb windup VFX base 3 内部状态字段 (`_max_lifetime` + 
> `_lifetime` + `_active`) 0 override verb-specific 0 触碰既有 1:1 严格分离契约" (9 元素 1:1 严格, 5 verb 0 override 3 内部状态字段 + 1 
> base 3 内部状态字段 0 触碰既有 + 1 Whisper 0 override 3 内部状态字段 rename 镜像 + 1 显式契约 + 1 3 内部状态字段 0 override verb-specific 0 触碰既有 
> = 9 元素). §9.6.52 (T308 #236 落地) 关注 "5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约" (7 元素 1:1 严格, 5 verb 视觉组 5 段 + 1 
> 显式契约 + 1 视觉组 0 触碰既有 = 7 元素). 上述 2 段均聚焦 5 verb windup VFX 单一维度 (visual group OR base 3 internal state field) 1:1 
> 严格分离契约, 0 涉及 "5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接" 1:1 严格分离契约 独立聚焦段 自身 (即 5 verb 视觉组 5 段 + 5 verb 0 override 3 
> 内部状态字段 1:1 跨维度同步, 1 verb 1 视觉组 1 段 必 0 override 3 内部状态字段, 2 维度 1 拼接 0 漏 1 边). 本节 (T309 #237 落地) 首个落在**5 verb windup 
> VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格分离契约** 聚焦段 (5 verb 1 视觉组 1 段 1:1 严格 (Pulse 同心圆环 + Coral Pulse 核 / Bind 向内螺旋 + Muted 
> Violet 核 / Cut 4 三角碎片 + Amber Voice 核 / Echo 8 棱镜折射 + Glass Cyan 核 / Wave 3 同心圆环 + Pale Resonance 核, 跨 5 verb 各 1 元素 
> = 5 元素 1:1 严格 0 漏 0 改 0 反序 0 反向) + 5 verb 0 override 3 内部状态字段 1:1 严格 (5 verb 各自 0 override `_max_lifetime: float = 
> 0.10` + 0 override `_lifetime: float = 0.0` + 0 override `_active: bool = false` 3 内部状态字段 1:1 严格 0 漏 0 改 0 反序 0 反向, 跨 
> 5 verb 各 1 元素 = 5 元素 1:1 严格) + 1 显式契约 "5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格 (1 verb 1 视觉组 1 段 必 0 override 
> 3 内部状态字段, 5 verb 0 漏 1 拼接 边)" 1 段 1:1 严格 (1 契约 1 元素) + 1 拼接 0 触碰既有 1:1 严格 (1 verb 1 视觉组 1 段 0 override 3 内部状态字段 拼接 0 
> 漏 1 边 0 反序 0 反向, 1 抽象契约 1 元素)) polish 模式 — 跨 5 verb × 1 视觉组 1 段 + 5 verb × 1 0 override 3 内部状态字段 + 1 显式契约 + 1 拼接 0 
> 触碰既有 = 5 + 5 + 1 + 1 = 12 元素 1:1 严格 (注意: 5 verb 1 视觉组 1 段 1:1 严格 = 5 verb 各自 1 元素 = 5 元素 + 5 verb 0 override 3 内部状态字段 
> 1:1 严格 = 5 verb 各自 1 元素 = 5 元素 + 1 显式契约 1 元素 + 1 拼接 0 触碰既有 1 元素 = 5 + 5 + 1 + 1 = 12 元素 1:1 严格, 0 漂动 0 后续 主要 
> pre-existing 风险) 0 触碰既有 5 verb 任何 1 字符 0 反向 0 例外. 任何"加新 1 verb windup VFX 视觉组 + 3 内部状态字段 拼接" (e.g. 未来加 7th verb 
> "Drain" 接入 5 verb 视觉组 + 3 内部状态字段 拼接 1:1 严格分离) 必须**先**按 Stage 1 Pulse 1 verb 1 视觉组 1 段 + 1 verb 0 override 3 内部状态字段 
> 1:1 拼接 加 1 verb entry (Stage 1 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反序 0 反向, **0 漏 1 拼接 边 0 漏 1 verb**), **再**按 Stage 2 Bind 
> / Stage 3 Cut / Stage 4 Echo / Stage 5 Wave 1 verb 1 视觉组 1 段 + 1 verb 0 override 3 内部状态字段 1:1 拼接 4 verb entries (4 
> verb 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反序 0 反向, 5 verb 0 漏 0 改 0 反序 0 例外), **再**按 1 显式契约 "5 verb windup VFX 视觉组 + base 3 
> 内部状态字段 拼接 1:1 严格" 1:1 严格 (0 漏 1 拼接 共享契约 0 改 1 字符 0 反序 0 反向). 跨 1 套 polish 模式 × 5 verb 1 视觉组 1 段 + 5 verb 0 override 3 
> 内部状态字段 + 1 显式契约 + 1 拼接 0 触碰既有 = 5 + 5 + 1 + 1 = 12 元素 1:1 严格 0 漏 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: 5 verb windup VFX (`src/scripts/pulse_windup_vfx.gd` + `src/scripts/bind_windup_vfx.gd` + 
`src/scripts/cut_windup_vfx.gd` + `src/scripts/echo_windup_vfx.gd` + `src/scripts/wave_windup_vfx.gd`) 视觉组 + base 3 
内部状态字段 拼接 1:1 严格分离契约 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 + T307 #234 跨 8 任务 
~150 轮落地) 现有的 12 元素 1:1 严格 (1 Pulse 1 视觉组 1 段 + 1 Pulse 0 override 3 内部状态字段 = 1 verb 1 拼接 1:1 严格 (1 视觉组 1 段 1:1 严格 + 1 
0 override 3 内部状态字段 1:1 严格 = 2 元素 1 拼接 0 漏 0 改 0 反序 0 反向) + 1 Bind 1 视觉组 1 段 + 1 Bind 0 override 3 内部状态字段 = 1 verb 1 拼接 
1:1 严格 + 1 Cut 1 视觉组 1 段 + 1 Cut 0 override 3 内部状态字段 = 1 verb 1 拼接 1:1 严格 + 1 Echo 1 视觉组 1 段 + 1 Echo 0 override 3 
内部状态字段 = 1 verb 1 拼接 1:1 严格 + 1 Wave 1 视觉组 1 段 + 1 Wave 0 override 3 内部状态字段 = 1 verb 1 拼接 1:1 严格 + 1 显式契约 "5 verb 
windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格 (1 verb 1 视觉组 1 段 必 0 override 3 内部状态字段, 5 verb 0 漏 1 拼接 边)" 1 段 1:1 严格 + 1 拼接 
0 触碰既有 1:1 严格 (1 抽象契约 1 元素)), 在反复 polish (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 
+ T307 #234 跨 8 任务 ~150 轮落地) 期间出现 3 类典型 fragile: (1) **"加新 1 verb windup VFX 漏 1 边 '5 verb 视觉组 + 3 内部状态字段 拼接 1:1 严格' 
共享契约"** — 加新 1 verb windup VFX (e.g. 未来加 7th verb "Drain" 接入 5 verb 视觉组 + 3 内部状态字段 拼接 1:1 严格分离) 落地时漏 1 边 "5 verb windup 
VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格" 共享契约 (e.g. 漏 7th verb 1 视觉组 1 段 + 0 override 3 内部状态字段 1 拼接 1:1 严格 0 镜像 1 verb, 1 
verb 0 1 拼接 0 闭环 0 漂动); (2) **"加新 1 verb windup VFX 漏 1 边 '1 显式契约 5 verb 视觉组 + 3 内部状态字段 拼接 1:1 严格' 契约"** — 加新 1 verb 
windup VFX (e.g. 7th verb "Drain" 接入 视觉组 + 3 内部状态字段 拼接 1:1 严格分离) 落地时漏 1 边 "1 显式契约 5 verb windup VFX 视觉组 + base 3 内部状态字段 
拼接 1:1 严格 (1 verb 1 视觉组 1 段 必 0 override 3 内部状态字段, 5 verb 0 漏 1 拼接 边)" 显式契约 (e.g. 漏 `_verb_windup_vfx_base.gd` 中 0 
include 1 显式契约 "5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格" 1 段 0 镜像 1 base, 1 base 0 1 显式契约 0 闭环 0 漂动); (3) **"修 
1 verb windup VFX 视觉组 0 override 3 内部状态字段 拼接 0 触碰既有 1 元素 0 触碰边界"** — 修 1 verb windup VFX 视觉组 (e.g. 改 Pulse 1 verb 视觉组 中 
"同心圆环" 顺序) 或 修 1 verb 0 override 3 内部状态字段 (e.g. 改 Wave 1 verb 视觉组 0 override `_max_lifetime` 顺序) 时 0 触碰既有 1 元素 (1 拼接 0 
触碰既有 1:1 严格), 跨 verb 1:1 漂动. 3 类症状根因: 5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 序列 0 文档化独立聚焦段, 任何新 1 verb windup VFX 
1 视觉组 / 0 override 3 内部状态字段 / 1 拼接 共享契约 / 1 拼接 0 触碰既有 接入只能"凭经验" 1:1 镜像 12 元素 (5 verb 1 视觉组 1 段 + 5 verb 0 override 3 
内部状态字段 + 1 显式契约 + 1 拼接 0 触碰既有 = 12 元素 1:1 严格), 1 漏 1 verb 1 拼接 / 1 漏 1 显式契约 / 1 漏 1 拼接 0 触碰既有 = 1 verb 扩展 0 12 元素 0 闭环 
0 漂动.

- **触发场景**: 加新 1 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格 (e.g. 未来加 7th verb "Drain" 接入 5 verb 视觉组 + 3 内部状态字段 拼接 
1:1 严格分离, 实际现状: 5 verb 视觉组 5 段 + 5 verb 0 override 3 内部状态字段 全部 0 漏 0 改 0 反序 0 反向) / 加新 1 显式契约 (T166 + T167 + T168 + 
T169 + T171 + T302 + T303 + T307 模式参考) 时**必须**严格按 12 元素 1:1 复制 T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + 
T302 #228 + T303 #229 + T307 #234 落地模式: (1) **Stage 1 Pulse 1 verb 1 视觉组 1 段 + 1 verb 0 override 3 内部状态字段 1:1 拼接 1:1 
严格** — 在 `pulse_windup_vfx.gd` 中 1 同心圆环 + 1 Coral Pulse 主色 + 0 override `_max_lifetime: float = 0.10` + 0 override 
`_lifetime: float = 0.0` + 0 override `_active: bool = false` = 1 verb 1 视觉组 1 段 1:1 严格 + 1 verb 0 override 3 内部状态字段 
1:1 严格 = 1 verb 1 拼接 1:1 严格 (2 元素 1 拼接 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格" 1 
段 1:1 严格 镜像 §9.6.51 + §9.6.52 (0 漏 1 拼接 共享契约 0 改 1 字符 0 反序 0 反向) + 1 拼接 0 触碰既有 1:1 严格 (1 抽象契约 1 元素) = 12 元素 1:1 严格 0 漏 
0 改 0 反序 0 反向, 0 触碰既有 5 verb 任何 1 字符, source-grep 验证 `pulse_windup_vfx.gd` 1 视觉组 1 段 + 0 override 3 内部状态字段 + 
`bind_windup_vfx.gd` 1 视觉组 1 段 + 0 override 3 内部状态字段 + `cut_windup_vfx.gd` 1 视觉组 1 段 + 0 override 3 内部状态字段 + 
`echo_windup_vfx.gd` 1 视觉组 1 段 + 0 override 3 内部状态字段 + `wave_windup_vfx.gd` 1 视觉组 1 段 + 0 override 3 内部状态字段 + 1 显式契约 + 
1 拼接 0 触碰既有 → 12 元素 0 漏 0 改 0 漏 1 边 0 反向; (2) **Stage 2 Bind 1 verb 1 视觉组 1 段 + 1 verb 0 override 3 内部状态字段 1:1 拼接 1:1 
严格** — 12 元素 1:1 严格 0 漏 0 改 0 反序 0 反向, source-grep 验证 12 元素 0 漏 0 改 0 漏 1 边 0 反向; (3) **Stage 3 Cut 1 verb 1 视觉组 1 段 + 
1 verb 0 override 3 内部状态字段 1:1 拼接 1:1 严格** — 12 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (4) **Stage 4 Echo 1 verb 1 视觉组 1 段 + 1 
verb 0 override 3 内部状态字段 1:1 拼接 1:1 严格** — 12 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (5) **Stage 5 Wave 1 verb 1 视觉组 1 段 + 1 verb 
0 override 3 内部状态字段 1:1 拼接 1:1 严格** — 12 元素 1:1 严格 0 漏 0 改 0 反序 0 反向. 12 元素 1:1 同步, 1 漏 1 verb 1 视觉组 1 段 / 1 漏 1 verb 0 
override 3 内部状态字段 / 1 漏 1 显式契约 / 1 漏 1 拼接 0 触碰既有 = 1 verb / 1 显式契约 / 1 拼接 0 触碰既有 扩展 0 12 元素 0 闭环 0 漂动. T166 #85 + T167 
#86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 + T307 #234 0 触碰既有 0 改 `_verb_windup_vfx_base.gd` 任何 1 字符, 
0 改 5 verb windup VFX 视觉组任何 1 像素, 0 改 5 verb windup VFX 0 override 3 内部状态字段任何 1 字段, 0 改 §9.6.51 9 元素 任何 1 字符, 0 改 
§9.6.52 7 元素 任何 1 字符 (0 改 "深海军蓝背景圆盘 + Glass Cyan 外环 + verb 主色 core" 任何 1 字符 0 改 0 漏 1 视觉组连贯 lifecycle 0 改 0 反向 0 改 0 漏 
1 内部状态字段 0 改 0 反向).

- **修复** (跨 1 套 polish 模式 T309 #237 落地后已知 12 元素 0 例外):
  - **T309 #237 首个 5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格分离契约 独立聚焦段 落地 (`pulse_windup_vfx.gd` 1 视觉组 1 段 1:1 严格 
+ 0 override 3 内部状态字段 1:1 严格 = 1 拼接 + `bind_windup_vfx.gd` 1 视觉组 1 段 1:1 严格 + 0 override 3 内部状态字段 1:1 严格 = 1 拼接 + 
`cut_windup_vfx.gd` 1 视觉组 1 段 1:1 严格 + 0 override 3 内部状态字段 1:1 严格 = 1 拼接 + `echo_windup_vfx.gd` 1 视觉组 1 段 1:1 严格 + 0 
override 3 内部状态字段 1:1 严格 = 1 拼接 + `wave_windup_vfx.gd` 1 视觉组 1 段 1:1 严格 + 0 override 3 内部状态字段 1:1 严格 = 1 拼接 + 1 显式契约 "5 
verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格 (1 verb 1 视觉组 1 段 必 0 override 3 内部状态字段, 5 verb 0 漏 1 拼接 边)" 1 段 1:1 严格 
镜像 §9.6.51 + §9.6.52 + 1 拼接 0 触碰既有 1:1 严格)** — §9.6.53 段 是 1 套 polish 模式首个 1:1 包含 5 verb 1 视觉组 1 段 + 5 verb 0 override 
3 内部状态字段 + 1 显式契约 + 1 拼接 0 触碰既有 = 5 + 5 + 1 + 1 = 12 元素 1:1 严格 的 5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格分离契约 
聚焦段 polish 模式, 12 元素 1:1 严格分离 (1 Pulse 1 视觉组 1 段 1:1 严格 + 1 Pulse 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 拼接 1:1 严格 (2 元素 
1 拼接 0 漏 0 改 0 反序 0 反向) + 1 Bind 1 视觉组 1 段 1:1 严格 + 1 Bind 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 拼接 1:1 严格 + 1 Cut 1 
视觉组 1 段 1:1 严格 + 1 Cut 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 拼接 1:1 严格 + 1 Echo 1 视觉组 1 段 1:1 严格 + 1 Echo 0 override 3 
内部状态字段 1:1 严格 = 1 verb 1 拼接 1:1 严格 + 1 Wave 1 视觉组 1 段 1:1 严格 + 1 Wave 0 override 3 内部状态字段 1:1 严格 = 1 verb 1 拼接 1:1 严格 + 
1 显式契约 "5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格" 1 段 1:1 严格 镜像 §9.6.51 + §9.6.52 (0 漏 1 拼接 共享契约 0 改 1 字符 0 反序 0 
反向) + 1 拼接 0 触碰既有 1:1 严格 (1 抽象契约 1 元素), 0 漏 1 元素 0 改 1 字段 0 漏 1 字段 0 反向), 0 触碰既有 (T309 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / 
§9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / 
§9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / 
§9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / 
§9.6.49 / §9.6.50 / §9.6.51 / §9.6.52 43 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 
0 触碰 README.md / README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / 
REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).

**与 §9.6.51 关系** — §9.6.51 (T307 #234 落地) 关注 "5 verb windup VFX base 3 内部状态字段 (`_max_lifetime` + `_lifetime` + 
`_active`) 0 override verb-specific 0 触碰既有 1:1 严格分离契约" (9 元素 1:1 严格, 5 verb 0 override 3 内部状态字段 + 1 base 3 内部状态字段 0 
触碰既有 + 1 Whisper 0 override 3 内部状态字段 rename 镜像 + 1 显式契约 + 1 3 内部状态字段 0 override verb-specific 0 触碰既有 = 9 元素). §9.6.53 
关注 "5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格分离契约" (12 元素 1:1 严格, 5 verb 1 视觉组 1 段 + 5 verb 0 override 3 内部状态字段 + 
1 显式契约 + 1 拼接 0 触碰既有 = 12 元素). §9.6.51 与 §9.6.53 是 "姊妹段 + 拼接段" (1 套 polish 模式 × 5 verb 0 override 3 内部状态字段 5 元素 1:1 严格 
⊂ 1 套 polish 模式 × 5 verb 1 拼接 10 元素 1:1 严格, 1 套 polish 模式 × 9 元素 = 9 元素 1:1 严格 ∩ 1 套 polish 模式 × 12 元素 = 12 元素 1:1 严格 = 
6 元素 1:1 严格 交集 (5 verb 0 override 3 内部状态字段 5 元素 + 1 显式契约 1 元素 0 漏 0 改 0 反序 0 反向, 9 元素 1:1 严格 是 12 元素 1:1 严格 子集 0 涉及 5 
verb 1 视觉组 1 段 0 涉及 1 拼接 0 触碰既有). 任何"加新 1 verb windup VFX 视觉组 + 3 内部状态字段 拼接"必须**先**按 §9.6.51 9 元素 1:1 复制 5 verb 0 
override 3 内部状态字段 5 元素 (5 元素 1:1 严格, 0 漏 1 内部状态字段 0 改 1 字段 0 反序 0 反向 0 漏 1 段), **再**按 §9.6.52 7 元素 1:1 复制 5 verb 1 视觉组 
1 段 5 元素 (5 元素 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反序 0 反向 0 漏 1 段), **再**按 §9.6.53 12 元素 1:1 拼接 (12 元素 1:1 严格, 0 漏 1 拼接 边 0 改 
1 字段 0 反向 0 触碰既有 §9.6.51 任何 1 字符 0 触碰既有 §9.6.52 任何 1 字符).

**与 §9.6.52 关系** — §9.6.52 (T308 #236 落地) 关注 "5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约" (7 元素 1:1 严格, 5 verb 1 视觉组 
1 段 + 1 显式契约 + 1 视觉组 0 触碰既有 = 7 元素). §9.6.53 关注 "5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格分离契约" (12 元素 1:1 严格, 5 
verb 1 视觉组 1 段 + 5 verb 0 override 3 内部状态字段 + 1 显式契约 + 1 拼接 0 触碰既有 = 12 元素). §9.6.52 与 §9.6.53 是 "姊妹段 + 拼接段" (1 套 
polish 模式 × 5 verb 1 视觉组 1 段 5 元素 1:1 严格 ⊂ 1 套 polish 模式 × 5 verb 1 拼接 10 元素 1:1 严格, 1 套 polish 模式 × 7 元素 = 7 元素 1:1 严格 
∩ 1 套 polish 模式 × 12 元素 = 12 元素 1:1 严格 = 6 元素 1:1 严格 交集 (5 verb 1 视觉组 1 段 5 元素 + 1 显式契约 1 元素 0 漏 0 改 0 反序 0 反向, 7 元素 
1:1 严格 是 12 元素 1:1 严格 子集 0 涉及 5 verb 0 override 3 内部状态字段 0 涉及 1 拼接 0 触碰既有). 任何"加新 1 verb windup VFX 视觉组 + 3 内部状态字段 
拼接"必须**先**按 §9.6.52 7 元素 1:1 复制 5 verb 1 视觉组 1 段 5 元素 (5 元素 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 反序 0 反向 0 漏 1 段), **再**按 
§9.6.51 9 元素 1:1 复制 5 verb 0 override 3 内部状态字段 5 元素 (5 元素 1:1 严格, 0 漏 1 内部状态字段 0 改 1 字段 0 反序 0 反向 0 漏 1 段), **再**按 
§9.6.53 12 元素 1:1 拼接 (12 元素 1:1 严格, 0 漏 1 拼接 边 0 改 1 字段 0 反向 0 触碰既有 §9.6.52 任何 1 字符 0 触碰既有 §9.6.51 任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 brittle 修复流程 5 步骤 1:1 严格分离契约 (T295 #219 §9.6.39 文档化) 跨 1 套 5 步骤 1:1 严格 0 涉及 5 verb 
windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格分离契约 自身, 1 套 5 步骤 = 5 步骤 1:1 严格 ∩ 1 套 12 元素 = 12 元素 1:1 严格 = 0 元素 1:1 严格 交集 
(T162 5 步骤关注 "复查根因 / 加新测试 / 修复 / 验证 / 跟踪" 5 步骤, 0 涉及 5 verb 视觉组 + 3 内部状态字段 拼接 1:1 严格分离契约 自身, 5 步骤 1:1 严格 0 涉及 12 元素 1:1 
严格). 任何"加新 1 verb windup VFX 视觉组 + 3 内部状态字段 拼接"必须**先**按 §9.6.53 12 元素 1:1 复制 (12 元素 1:1 严格, 0 漏 1 拼接 边 0 改 1 字段 0 反序 0 
反向 0 漏 1 段), **再**走 T162 5 步骤 1:1 严格 (5 步骤 1:1 严格, 0 漏 1 步 0 改 1 步 0 反序 0 反向 0 触碰既有 §9.6.53 任何 1 字符).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.53 5 verb 1 视觉组 1 段 + 5 verb 0 override 3 内部状态字段 + 1 显式契约 + 1 拼接 0 触碰既有 = 12 
元素 走 §9.1 9 步落地的 1 步 (Stage 7 windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格, 跨 5 verb 各 2 元素 (1 视觉组 1 段 + 0 override 3 
内部状态字段 1 拼接) + 1 显式契约 1 元素 + 1 拼接 0 触碰既有 1 元素), 0 触碰 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 
1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染). 任何"加新 1 verb windup VFX 视觉组 + 3 内部状态字段 拼接"必须**先**按 
§9.6.53 12 元素 1:1 镜像 (12 元素 1:1 严格, 0 漏 1 拼接 边 0 改 1 字段 0 反序 0 反向 0 漏 1 段 0 触碰既有 8 步 (`verb` 枚举 + `ability` 子类 + 
`const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `audio` preset 字段 + `HUD 冷光勾边` 渲染)). 任何"加新 1 §9.1 步"必须**先**在 
§9.6.53 12 元素 加 1 边 (1 verb 1 拼接 1 边 或 1 显式契约 1 边 或 1 拼接 0 触碰既有 1 边), 0 触碰既有 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 
`const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 触碰既有).


### 9.6.54 5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格分离契约 polish 模式 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + 
T171 #89 + T302 #228 + T303 #229 + T304 #231 跨 8 任务 ~150 轮落地) 文档化

> §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / 
> §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / §9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 
> / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / §9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / 
> §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / §9.6.51 / §9.6.52 / §9.6.53 44 套 polish 模式均在 "pause_menu 
> / 6 verb 单层 / 6 verb 跨组件 / Hub / 成就 / prewarm / 工具链 / 文档归档 / PlayerStats 持久化层 / audio 数据层 / PlayerActionGate 单 
> autoload 4 件套 / SaveSystem CRC32 校验 / T162 brittle 修复流程 / 6 verb audio 家族 19 cue / 6 verb cooldown ready jingle 5 段 / 
> 6 verb `_ready()` / `_exit_tree()` 双 hook 串联 / 5 verb windup VFX 4 hook lifecycle / 5 verb windup VFX `_draw()` 
> verb-specific / 5 verb windup VFX `trigger()` verb-specific / 5 verb windup VFX 共享 4 hook 0 override / 5 verb windup 
> VFX `trigger()` + `_draw()` 双 verb-specific / 5 verb windup VFX base 3 内部状态字段 / 5 verb windup VFX 视觉组连贯 lifecycle / 5 
> verb windup VFX 视觉组 + base 3 内部状态字段 拼接" 范畴, 0 涉及 "5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格分离契约" 独立聚焦段 落地. 
> §9.6.48 (T304 #231 落地) 关注 "5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约" (32 元素 1:1 严格, 5 verb `trigger()` 
> 0 override verb-specific + 5 verb 共享 4 元素 + 2 verb verb-specific 1 元素 + 1 显式契约 + 1 verb-specific 0 触碰既有 + 1 共享 4 元素 0 
> 触碰既有 + 1 base `trigger()` 0 override + 1 `trigger()` 0 override verb-specific 0 触碰既有 = 32 元素). §9.6.52 (T308 #236 落地) 
> 关注 "5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约" (7 元素 1:1 严格, 5 verb 1 视觉组 1 段 + 1 显式契约 + 1 视觉组 0 触碰既有 = 7 元素). 
> §9.6.53 (T309 #237 落地) 关注 "5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格分离契约" (12 元素 1:1 严格, 5 verb 1 视觉组 1 段 + 5 
> verb 0 override 3 内部状态字段 + 1 显式契约 + 1 拼接 0 触碰既有 = 12 元素). 上述 3 段均聚焦 5 verb windup VFX 单一维度 (`trigger()` OR 视觉组 OR 
> base 3 内部状态字段) 1:1 严格分离契约, 0 涉及 "5 verb windup VFX `trigger()` + 视觉组 拼接" 1:1 严格分离契约 独立聚焦段 自身 (即 5 verb `trigger()` 5 
> 段 1 verb-specific 0 override + 5 verb 视觉组 5 段 1:1 跨维度同步, 1 verb 1 `trigger()` 1 verb-specific 0 override 必 1 视觉组 1 段, 
> 2 维度 1 拼接 0 漏 1 边). 本节 (T310 #238 落地) 首个落在**5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格分离契约** 聚焦段 (5 verb 视觉组 5 段 
> 1:1 严格 (Pulse 同心圆环 + Coral Pulse 核 / Bind 向内螺旋 + Muted Violet 核 / Cut 4 三角碎片 + Amber Voice 核 / Echo 8 棱镜折射 + Glass 
> Cyan 核 / Wave 3 同心圆环 + Pale Resonance 核, 跨 5 verb 各 1 元素 = 5 元素 1:1 严格 0 漏 0 改 0 反序 0 反向) + 5 verb 0 override 
> `trigger()` verb-specific 1:1 严格 (5 verb 各自 0 override `trigger()` verb-specific 1:1 严格 0 漏 0 改 0 反序 0 反向 (Pulse 
> `trigger(origin, half_radius, duration)` 3 参 + Bind `trigger(origin, half_radius, duration)` 3 参 + Cut 
> `trigger(origin, half_radius, direction, duration)` 4 参 1 verb-specific `_direction` + Echo `trigger(origin, 
> half_radius, full_radius, duration)` 4 参 1 verb-specific `_max_radius` + Wave `trigger(origin, half_radius, 
> duration)` 3 参), 跨 5 verb 各 1 元素 = 5 元素 1:1 严格) + 1 显式契约 "5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格 (1 verb 1 
> `trigger()` 1 verb-specific 0 override 必 1 视觉组 1 段, 5 verb 0 漏 1 拼接 边)" 1 段 1:1 严格 (1 契约 1 元素) + 1 拼接 0 触碰既有 1:1 严格 
> (1 verb 1 `trigger()` 1 verb-specific 0 override 拼接 1 视觉组 1 段 0 漏 1 边 0 反序 0 反向, 1 抽象契约 1 元素)) polish 模式 — 跨 5 verb × 
> 1 视觉组 1 段 + 5 verb × 1 0 override `trigger()` verb-specific + 1 显式契约 + 1 拼接 0 触碰既有 = 5 + 5 + 1 + 1 = 12 元素 1:1 严格 
> (注意: 5 verb 视觉组 5 段 1:1 严格 = 5 verb 各自 1 元素 = 5 元素 + 5 verb 0 override `trigger()` verb-specific 1:1 严格 = 5 verb 各自 1 
> 元素 = 5 元素 + 1 显式契约 1 元素 + 1 拼接 0 触碰既有 1 元素 = 5 + 5 + 1 + 1 = 12 元素 1:1 严格, 0 漂动 0 后续 主要 pre-existing 风险) 0 触碰既有 5 
> verb 任何 1 字符 0 反向 0 例外. 任何"加新 1 verb windup VFX `trigger()` + 视觉组 拼接" (e.g. 未来加 7th verb "Drain" 接入 5 verb 
> `trigger()` + 视觉组 拼接 1:1 严格分离) 必须**先**按 Stage 1 Pulse 1 verb 1 `trigger()` 1 verb-specific 0 override + 1 verb 1 视觉组 
> 1 段 1:1 拼接 加 1 verb entry (Stage 1 1:1 严格, 0 漏 1 `trigger()` 0 改 1 字段 0 反序 0 反向, **0 漏 1 拼接 边 0 漏 1 verb**), **再**按 
> Stage 2 Bind / Stage 3 Cut / Stage 4 Echo / Stage 5 Wave 1 verb 1 `trigger()` 1 verb-specific 0 override + 1 verb 1 
> 视觉组 1 段 1:1 拼接 4 verb entries (4 verb 1:1 严格, 0 漏 1 `trigger()` 0 改 1 字段 0 反序 0 反向, 5 verb 0 漏 0 改 0 反序 0 例外), **再**按 
> 1 显式契约 "5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格" 1:1 严格 (0 漏 1 拼接 共享契约 0 改 1 字符 0 反序 0 反向). 跨 1 套 polish 模式 × 5 
> verb 视觉组 5 段 + 5 verb 0 override `trigger()` verb-specific + 1 显式契约 + 1 拼接 0 触碰既有 = 5 + 5 + 1 + 1 = 12 元素 1:1 严格 0 漏 
> 1 元素 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: 5 verb windup VFX (`src/scripts/pulse_windup_vfx.gd` + `src/scripts/bind_windup_vfx.gd` + 
`src/scripts/cut_windup_vfx.gd` + `src/scripts/echo_windup_vfx.gd` + `src/scripts/wave_windup_vfx.gd`) `trigger()` + 
视觉组 拼接 1:1 严格分离契约 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 #229 + T304 #231 跨 8 任务 ~150 
轮落地) 现有的 12 元素 1:1 严格 (1 Pulse 1 verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格 (`pulse_windup_vfx.gd` 
`trigger(origin, half_radius, duration)` 3 参 verb-specific = 1 verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格 0 漏 
0 改 0 反序 0 反向) + 1 Pulse 1 verb 1 视觉组 1 段 1:1 严格 = 1 verb 1 拼接 1:1 严格 (1 `trigger()` 1 verb-specific 0 override 1:1 严格 
+ 1 视觉组 1 段 1:1 严格 = 2 元素 1 拼接 0 漏 0 改 0 反序 0 反向) + 1 Bind 1 verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格 
(`bind_windup_vfx.gd` `trigger(origin, half_radius, duration)` 3 参 verb-specific = 1 verb 1 `trigger()` 1 verb-specific 
0 override 1:1 严格) + 1 Bind 1 verb 1 视觉组 1 段 1:1 严格 = 1 verb 1 拼接 1:1 严格 + 1 Cut 1 verb 1 `trigger()` 1 verb-specific 0 
override 1:1 严格 (`cut_windup_vfx.gd` `trigger(origin, half_radius, direction, duration)` 4 参 verb-specific = 1 verb 1 
`trigger()` 1 verb-specific 0 override 1:1 严格 含 1 verb-specific `_direction` 1 元素) + 1 Cut 1 verb 1 视觉组 1 段 1:1 严格 = 1 
verb 1 拼接 1:1 严格 + 1 Echo 1 verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格 (`echo_windup_vfx.gd` `trigger(origin, 
half_radius, full_radius, duration)` 4 参 verb-specific = 1 verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格 含 1 
verb-specific `_max_radius` 1 元素) + 1 Echo 1 verb 1 视觉组 1 段 1:1 严格 = 1 verb 1 拼接 1:1 严格 + 1 Wave 1 verb 1 `trigger()` 1 
verb-specific 0 override 1:1 严格 (`wave_windup_vfx.gd` `trigger(origin, half_radius, duration)` 3 参 verb-specific = 1 
verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格) + 1 Wave 1 verb 1 视觉组 1 段 1:1 严格 = 1 verb 1 拼接 1:1 严格 + 1 显式契约 "5 
verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格 (1 verb 1 `trigger()` 1 verb-specific 0 override 必 1 视觉组 1 段, 5 verb 0 漏 1 
拼接 边)" 1 段 1:1 严格 + 1 拼接 0 触碰既有 1:1 严格 (1 抽象契约 1 元素)), 在反复 polish (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 
+ T302 #228 + T303 #229 + T304 #231 跨 8 任务 ~150 轮落地) 期间出现 3 类典型 fragile: (1) **"加新 1 verb windup VFX 漏 1 边 '5 verb 
`trigger()` + 视觉组 拼接 1:1 严格' 共享契约"** — 加新 1 verb windup VFX (e.g. 未来加 7th verb "Drain" 接入 5 verb `trigger()` + 视觉组 拼接 
1:1 严格分离) 落地时漏 1 边 "5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格" 共享契约 (e.g. 漏 7th verb 1 `trigger()` 1 verb-specific 
0 override + 1 视觉组 1 段 1 拼接 1:1 严格 0 镜像 1 verb, 1 verb 0 1 拼接 0 闭环 0 漂动); (2) **"加新 1 verb windup VFX 漏 1 边 '1 显式契约 5 
verb `trigger()` + 视觉组 拼接 1:1 严格' 契约"** — 加新 1 verb windup VFX (e.g. 7th verb "Drain" 接入 `trigger()` + 视觉组 拼接 1:1 严格分离) 
落地时漏 1 边 "1 显式契约 5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格 (1 verb 1 `trigger()` 1 verb-specific 0 override 必 1 视觉组 
1 段, 5 verb 0 漏 1 拼接 边)" 显式契约 (e.g. 漏 `_verb_windup_vfx_base.gd` 中 0 include 1 显式契约 "5 verb windup VFX `trigger()` + 
视觉组 拼接 1:1 严格" 1 段 0 镜像 1 base, 1 base 0 1 显式契约 0 闭环 0 漂动); (3) **"修 1 verb windup VFX `trigger()` verb-specific 0 
override 拼接 0 触碰既有 1 元素 0 触碰边界"** — 修 1 verb windup VFX `trigger()` verb-specific (e.g. 改 Cut 1 verb `trigger()` 中 
`_direction` 顺序) 或 修 1 verb 视觉组 (e.g. 改 Pulse 1 verb 视觉组 中 "同心圆环" 顺序) 时 0 触碰既有 1 元素 (1 拼接 0 触碰既有 1:1 严格), 跨 verb 1:1 
漂动. 3 类症状根因: 5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 序列 0 文档化独立聚焦段, 任何新 1 verb windup VFX 1 `trigger()` 
verb-specific / 1 视觉组 / 1 拼接 共享契约 / 1 拼接 0 触碰既有 接入只能"凭经验" 1:1 镜像 12 元素 (5 verb 视觉组 5 段 + 5 verb 0 override `trigger()` 
verb-specific + 1 显式契约 + 1 拼接 0 触碰既有 = 12 元素 1:1 严格), 1 漏 1 verb 1 拼接 / 1 漏 1 显式契约 / 1 漏 1 拼接 0 触碰既有 = 1 verb 扩展 0 12 
元素 0 闭环 0 漂动.

- **触发场景**: 加新 1 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格 (e.g. 未来加 7th verb "Drain" 接入 5 verb `trigger()` + 视觉组 拼接 
1:1 严格分离, 实际现状: 5 verb `trigger()` 5 段 1 verb-specific 0 override + 5 verb 视觉组 5 段 全部 0 漏 0 改 0 反序 0 反向) / 加新 1 显式契约 
(T166 + T167 + T168 + T169 + T171 + T302 + T303 + T304 模式参考) 时**必须**严格按 12 元素 1:1 复制 T166 #85 + T167 #86 + T168 #86 + 
T169 #87 + T171 #89 + T302 #228 + T303 #229 + T304 #231 落地模式: (1) **Stage 1 Pulse 1 verb 1 `trigger()` 1 verb-specific 
0 override + 1 verb 1 视觉组 1 段 1:1 拼接 1:1 严格** — 在 `pulse_windup_vfx.gd` `trigger(origin, half_radius, duration)` 3 参 
verb-specific 0 override + 1 同心圆环 + 1 Coral Pulse 主色 = 1 verb 1 `trigger()` 1 verb-specific 0 override 1:1 严格 + 1 verb 
1 视觉组 1 段 1:1 严格 = 1 verb 1 拼接 1:1 严格 (2 元素 1 拼接 0 漏 0 改 0 反序 0 反向) + 1 显式契约 "5 verb windup VFX `trigger()` + 视觉组 拼接 
1:1 严格" 1 段 1:1 严格 镜像 §9.6.48 + §9.6.52 (0 漏 1 拼接 共享契约 0 改 1 字符 0 反序 0 反向) + 1 拼接 0 触碰既有 1:1 严格 (1 抽象契约 1 元素) = 12 元素 
1:1 严格 0 漏 0 改 0 反序 0 反向, 0 触碰既有 5 verb 任何 1 字符, source-grep 验证 `pulse_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 
override + 1 视觉组 1 段 + `bind_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 override + 1 视觉组 1 段 + `cut_windup_vfx.gd` 
1 `trigger()` 1 verb-specific 0 override (含 1 `_direction` 1 verb-specific 1 元素) + 1 视觉组 1 段 + `echo_windup_vfx.gd` 1 
`trigger()` 1 verb-specific 0 override (含 1 `_max_radius` 1 verb-specific 1 元素) + 1 视觉组 1 段 + `wave_windup_vfx.gd` 1 
`trigger()` 1 verb-specific 0 override + 1 视觉组 1 段 + 1 显式契约 + 1 拼接 0 触碰既有 → 12 元素 0 漏 0 改 0 漏 1 边 0 反向; (2) **Stage 2 
Bind 1 verb 1 `trigger()` 1 verb-specific 0 override + 1 verb 1 视觉组 1 段 1:1 拼接 1:1 严格** — 12 元素 1:1 严格 0 漏 0 改 0 反序 0 
反向, source-grep 验证 12 元素 0 漏 0 改 0 漏 1 边 0 反向; (3) **Stage 3 Cut 1 verb 1 `trigger()` 1 verb-specific 0 override + 1 
verb 1 视觉组 1 段 1:1 拼接 1:1 严格** — 12 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (4) **Stage 4 Echo 1 verb 1 `trigger()` 1 
verb-specific 0 override + 1 verb 1 视觉组 1 段 1:1 拼接 1:1 严格** — 12 元素 1:1 严格 0 漏 0 改 0 反序 0 反向; (5) **Stage 5 Wave 1 verb 
1 `trigger()` 1 verb-specific 0 override + 1 verb 1 视觉组 1 段 1:1 拼接 1:1 严格** — 12 元素 1:1 严格 0 漏 0 改 0 反序 0 反向. 12 元素 1:1 
同步, 1 漏 1 verb 1 `trigger()` 1 verb-specific 0 override / 1 漏 1 verb 1 视觉组 1 段 / 1 漏 1 显式契约 / 1 漏 1 拼接 0 触碰既有 = 1 verb 
/ 1 显式契约 / 1 拼接 0 触碰既有 扩展 0 12 元素 0 闭环 0 漂动. T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T302 #228 + T303 
#229 + T304 #231 0 触碰既有 0 改 `_verb_windup_vfx_base.gd` 任何 1 字符, 0 改 5 verb windup VFX `trigger()` verb-specific 任何 1 字段,
 0 改 5 verb windup VFX 视觉组任何 1 像素, 0 改 §9.6.48 32 元素 任何 1 字符, 0 改 §9.6.52 7 元素 任何 1 字符 (0 改 "深海军蓝背景圆盘 + Glass Cyan 外环 + 
verb 主色 core" 任何 1 字符 0 改 0 漏 1 `trigger()` 拼接 0 改 0 反向 0 改 0 漏 1 视觉组 0 改 0 反向).

- **修复** (跨 1 套 polish 模式 T310 #238 落地后已知 12 元素 0 例外):
  - **T310 #238 首个 5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格分离契约 独立聚焦段 落地 (`pulse_windup_vfx.gd` 1 `trigger()` 1 
verb-specific 0 override 1:1 严格 + 1 视觉组 1 段 1:1 严格 = 1 拼接 + `bind_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 
override 1:1 严格 + 1 视觉组 1 段 1:1 严格 = 1 拼接 + `cut_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 override 1:1 严格 (含 1 
`_direction` 1 verb-specific 1 元素) + 1 视觉组 1 段 1:1 严格 = 1 拼接 + `echo_windup_vfx.gd` 1 `trigger()` 1 verb-specific 0 
override 1:1 严格 (含 1 `_max_radius` 1 verb-specific 1 元素) + 1 视觉组 1 段 1:1 严格 = 1 拼接 + `wave_windup_vfx.gd` 1 `trigger()` 
1 verb-specific 0 override 1:1 严格 + 1 视觉组 1 段 1:1 严格 = 1 拼接 + 1 显式契约 "5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格 (1 
verb 1 `trigger()` 1 verb-specific 0 override 必 1 视觉组 1 段, 5 verb 0 漏 1 拼接 边)" 1 段 1:1 严格 镜像 §9.6.48 + §9.6.52 + 1 拼接 0 
触碰既有 1:1 严格)** — §9.6.54 段 是 1 套 polish 模式首个 1:1 包含 5 verb 1 视觉组 1 段 + 5 verb 0 override `trigger()` verb-specific + 1 
显式契约 + 1 拼接 0 触碰既有 = 5 + 5 + 1 + 1 = 12 元素 1:1 严格 的 5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格分离契约 聚焦段 polish 模式, 12 
元素 1:1 严格分离 (1 Pulse 1 `trigger()` 1 verb-specific 0 override 1:1 严格 + 1 Pulse 1 视觉组 1 段 1:1 严格 = 1 verb 1 拼接 1:1 严格 (2 
元素 1 拼接 0 漏 0 改 0 反序 0 反向) + 1 Bind 1 `trigger()` 1 verb-specific 0 override 1:1 严格 + 1 Bind 1 视觉组 1 段 1:1 严格 = 1 verb 
1 拼接 1:1 严格 + 1 Cut 1 `trigger()` 1 verb-specific 0 override 1:1 严格 + 1 Cut 1 视觉组 1 段 1:1 严格 = 1 verb 1 拼接 1:1 严格 + 1 
Echo 1 `trigger()` 1 verb-specific 0 override 1:1 严格 + 1 Echo 1 视觉组 1 段 1:1 严格 = 1 verb 1 拼接 1:1 严格 + 1 Wave 1 
`trigger()` 1 verb-specific 0 override 1:1 严格 + 1 Wave 1 视觉组 1 段 1:1 严格 = 1 verb 1 拼接 1:1 严格 + 1 显式契约 "5 verb windup 
VFX `trigger()` + 视觉组 拼接 1:1 严格" 1 段 1:1 严格 镜像 §9.6.48 + §9.6.52 (0 漏 1 拼接 共享契约 0 改 1 字符 0 反序 0 反向) + 1 拼接 0 触碰既有 1:1 
严格 (1 抽象契约 1 元素), 0 漏 1 元素 0 改 1 字段 0 漏 1 字段 0 反向), 0 触碰既有 (T310 0 触碰 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / 
§9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 / 
§9.6.27 / §9.6.28 / §9.6.29 / §9.6.30 / §9.6.31 / §9.6.32 / §9.6.33 / §9.6.34 / §9.6.35 / §9.6.36 / §9.6.37 / §9.6.38 / 
§9.6.39 / §9.6.40 / §9.6.41 / §9.6.42 / §9.6.43 / §9.6.44 / §9.6.45 / §9.6.46 / §9.6.47 / §9.6.48 / §9.6.49 / §9.6.50 / 
§9.6.51 / §9.6.52 / §9.6.53 44 套 polish 模式任何 1 字符, 0 触碰游戏代码 .gd / .tscn / 任何 gameplay code, 0 触碰 §10 决策记录流程, 0 触碰 
README.md / README.zh-CN.md 内容, 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md / CHANGELOG_ARCHIVE.md / 
REVIEW_LOG.md / REVIEW_LOG_ARCHIVE.md 内容).

**与 §9.6.48 关系** — §9.6.48 (T304 #231 落地) 关注 "5 verb windup VFX `trigger()` verb-specific 1:1 严格分离契约" (32 元素 1:1 严格, 5 
verb `trigger()` 0 override verb-specific + 5 verb 共享 4 元素 + 2 verb verb-specific 1 元素 + 1 显式契约 + 1 verb-specific 0 
触碰既有 + 1 共享 4 元素 0 触碰既有 + 1 base `trigger()` 0 override + 1 `trigger()` 0 override verb-specific 0 触碰既有 = 32 元素). 
§9.6.54 关注 "5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格分离契约" (12 元素 1:1 严格, 5 verb 1 视觉组 1 段 + 5 verb 0 override 
`trigger()` verb-specific + 1 显式契约 + 1 拼接 0 触碰既有 = 12 元素). §9.6.48 与 §9.6.54 是 "姊妹段 + 拼接段" (1 套 polish 模式 × 5 verb 0 
override `trigger()` verb-specific 5 元素 1:1 严格 ⊂ 1 套 polish 模式 × 5 verb 1 拼接 10 元素 1:1 严格, 1 套 polish 模式 × 32 元素 = 32 
元素 1:1 严格 ∩ 1 套 polish 模式 × 12 元素 = 12 元素 1:1 严格 = 6 元素 1:1 严格 交集 (5 verb 0 override `trigger()` verb-specific 5 元素 + 1 
显式契约 1 元素 0 漏 0 改 0 反序 0 反向, 32 元素 1:1 严格 是 12 元素 1:1 严格 父集 0 涉及 5 verb 1 视觉组 1 段 0 涉及 1 拼接 0 触碰既有). 任何"加新 1 verb 
windup VFX `trigger()` + 视觉组 拼接"必须**先**按 §9.6.48 32 元素 1:1 复制 5 verb 0 override `trigger()` verb-specific 5 元素 (5 元素 
1:1 严格, 0 漏 1 `trigger()` 0 改 1 字段 0 反序 0 反向 0 漏 1 段), **再**按 §9.6.52 7 元素 1:1 复制 5 verb 1 视觉组 1 段 5 元素 (5 元素 1:1 严格, 0 
漏 1 视觉组 0 改 1 字段 0 反序 0 反向 0 漏 1 段), **再**按 §9.6.54 12 元素 1:1 拼接 (12 元素 1:1 严格, 0 漏 1 拼接 边 0 改 1 字段 0 反向 0 触碰既有 §9.6.48 
任何 1 字符 0 触碰既有 §9.6.52 任何 1 字符).

**与 §9.6.52 关系** — §9.6.52 (T308 #236 落地) 关注 "5 verb windup VFX 视觉组连贯 lifecycle 1:1 严格分离契约" (7 元素 1:1 严格, 5 verb 1 视觉组 
1 段 + 1 显式契约 + 1 视觉组 0 触碰既有 = 7 元素). §9.6.54 关注 "5 verb windup VFX `trigger()` + 视觉组 拼接 1:1 严格分离契约" (12 元素 1:1 严格, 5 
verb 1 视觉组 1 段 + 5 verb 0 override `trigger()` verb-specific + 1 显式契约 + 1 拼接 0 触碰既有 = 12 元素). §9.6.52 与 §9.6.54 是 "姊妹段 
+ 拼接段" (1 套 polish 模式 × 5 verb 1 视觉组 1 段 5 元素 1:1 严格 ⊂ 1 套 polish 模式 × 5 verb 1 拼接 10 元素 1:1 严格, 1 套 polish 模式 × 7 元素 = 
7 元素 1:1 严格 ∩ 1 套 polish 模式 × 12 元素 = 12 元素 1:1 严格 = 6 元素 1:1 严格 交集 (5 verb 1 视觉组 1 段 5 元素 + 1 显式契约 1 元素 0 漏 0 改 0 反序 0 
反向, 7 元素 1:1 严格 是 12 元素 1:1 严格 子集 0 涉及 5 verb 0 override `trigger()` verb-specific 0 涉及 1 拼接 0 触碰既有). 任何"加新 1 verb 
windup VFX `trigger()` + 视觉组 拼接"必须**先**按 §9.6.52 7 元素 1:1 复制 5 verb 1 视觉组 1 段 5 元素 (5 元素 1:1 严格, 0 漏 1 视觉组 0 改 1 字段 0 
反序 0 反向 0 漏 1 段), **再**按 §9.6.48 32 元素 1:1 复制 5 verb 0 override `trigger()` verb-specific 5 元素 (5 元素 1:1 严格, 0 漏 1 
`trigger()` 0 改 1 字段 0 反序 0 反向 0 漏 1 段), **再**按 §9.6.54 12 元素 1:1 拼接 (12 元素 1:1 严格, 0 漏 1 拼接 边 0 改 1 字段 0 反向 0 触碰既有 
§9.6.52 任何 1 字符 0 触碰既有 §9.6.48 任何 1 字符).

**与 §9.6.53 关系** — §9.6.53 (T309 #237 落地) 关注 "5 verb windup VFX 视觉组 + base 3 内部状态字段 拼接 1:1 严格分离契约" (12 元素 1:1 严格, 5 
verb 1 视觉组 1 段 + 5 verb 0 override 3 内部状态字段 + 1 显式契约 + 1 拼接 0 触碰既有 = 12 元素). §9.6.54 关注 "5 verb windup VFX `trigger()` 
+ 视觉组 拼接 1:1 严格分离契约" (12 元素 1:1 严格, 5 verb 1 视觉组 1 段 + 5 verb 0 override `trigger()` verb-specific + 1 显式契约 + 1 拼接 0 
触碰既有 = 12 元素). §9.6.53 与 §9.6.54 是 "姊妹段 + 拼接段" (1 套 polish 模式 × 5 verb 1 视觉组 1 段 5 元素 1:1 严格 共享 1 套 polish 模式 × 5 verb 
1 视觉组 1 段 5 元素 1:1 严格, 1 套 polish 模式 × 12 元素 = 12 元素 1:1 严格 ∩ 1 套 polish 模式 × 12 元素 = 12 元素 1:1 严格 = 6 元素 1:1 严格 交集 (5 
verb 1 视觉组 1 段 5 元素 + 1 显式契约 1 元素 0 漏 0 改 0 反序 0 反向, 5 verb 0 override 3 内部状态字段 0 涉及 5 verb 0 override `trigger()` 
verb-specific 0 涉及 1 拼接 0 触碰既有 双方 各自 独立). 任何"加新 1 verb windup VFX `trigger()` + 视觉组 拼接"必须**先**按 §9.6.53 12 元素 1:1 复制 
(12 元素 1:1 严格, 0 漏 1 拼接 边 0 改 1 字段 0 反序 0 反向 0 漏 1 段), **再**按 §9.6.54 12 元素 1:1 复制 5 verb 0 override `trigger()` 
verb-specific 替换 5 verb 0 override 3 内部状态字段 (5 元素 1:1 严格, 0 漏 1 `trigger()` 0 改 1 字段 0 反序 0 反向 0 漏 1 段 0 触碰既有 §9.6.53 
任何 1 字符).

**与 T162 brittle 修复流程 关系** — T162 brittle 修复流程 5 步骤 1:1 严格分离契约 (T295 #219 §9.6.39 文档化) 跨 1 套 5 步骤 1:1 严格 0 涉及 5 verb 
windup VFX `trigger()` + 视觉组 拼接 1:1 严格分离契约 自身, 1 套 5 步骤 = 5 步骤 1:1 严格 ∩ 1 套 12 元素 = 12 元素 1:1 严格 = 0 元素 1:1 严格 交集 (T162 
5 步骤关注 "复查根因 / 加新测试 / 修复 / 验证 / 跟踪" 5 步骤, 0 涉及 5 verb `trigger()` + 视觉组 拼接 1:1 严格分离契约 自身, 5 步骤 1:1 严格 0 涉及 12 元素 1:1 
严格). 任何"加新 1 verb windup VFX `trigger()` + 视觉组 拼接"必须**先**按 §9.6.54 12 元素 1:1 复制 (12 元素 1:1 严格, 0 漏 1 拼接 边 0 改 1 字段 0 反序 
0 反向 0 漏 1 段), **再**走 T162 5 步骤 1:1 严格 (5 步骤 1:1 严格, 0 漏 1 步 0 改 1 步 0 反序 0 反向 0 触碰既有 §9.6.54 任何 1 字符).

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` 
(7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.54 5 verb 1 视觉组 1 段 + 5 verb 0 override `trigger()` verb-specific + 1 显式契约 + 1 
拼接 0 触碰既有 = 12 元素 走 §9.1 9 步落地的 1 步 (Stage 7 windup VFX `trigger()` + 视觉组 拼接 1:1 严格, 跨 5 verb 各 2 元素 (1 视觉组 1 段 + 0 
override `trigger()` verb-specific 1 拼接) + 1 显式契约 1 元素 + 1 拼接 0 触碰既有 1 元素), 0 触碰 8 步 (1 `verb` 枚举 + 1 `ability` 子类 + 1 
`const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染). 任何"加新 1 verb windup 
VFX `trigger()` + 视觉组 拼接"必须**先**按 §9.6.54 12 元素 1:1 镜像 (12 元素 1:1 严格, 0 漏 1 拼接 边 0 改 1 字段 0 反序 0 反向 0 漏 1 段 0 触碰既有 8 步 
(`verb` 枚举 + `ability` 子类 + `const` 字段 + `input` map + `shop` 系统 + `icon` PNG + `audio` preset 字段 + `HUD 冷光勾边` 渲染)). 
任何"加新 1 §9.1 步"必须**先**在 §9.6.54 12 元素 加 1 边 (1 verb 1 拼接 1 边 或 1 显式契约 1 边 或 1 拼接 0 触碰既有 1 边), 0 触碰既有 8 步 (1 `verb` 枚举 + 
1 `ability` 子类 + 1 `const` 字段 + 1 `input` map + 1 `shop` 系统 + 1 `icon` PNG + 1 `audio` preset 字段 + 1 `HUD 冷光勾边` 渲染 0 
触碰既有).
