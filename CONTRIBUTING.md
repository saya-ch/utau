# Contributing to Voxglass

> 本仓库由「自动化迭代 Agent」+ 协作者共同维护。本指南面向人工协作者，介绍如何本地启动 Godot 4.6.3 工程、跑冒烟测试、按节奏提交。

## 1. 仓库结构（30 秒总览）

```
/workspace
├── ITERATION_GUIDE.md       # Agent 必读；本指南与之互补
├── ROADMAP.md / CHANGELOG.md / REVIEW_LOG.md   # 项目状态三件套
├── STYLE_GUIDE.md / ASSET_REGISTRY.md / RESEARCH.md / INSPIRATION.md
├── README.md / README.zh-CN.md                # 玩家 + 营销
├── project.godot                              # Godot 4.6.3 工程
├── godot/                                     # Godot 二进制（拆分 ZIP + README）
├── data/                                      # 房间 JSON / 成就 / 商店目录
├── assets/                                    # 美术（按 character/enemies/ui/... 分目录）
├── src/
│   ├── autoload/                              # GameState / PlayerStats / SaveSystem / ScreenShake / AudioManagerEnhanced
│   ├── scripts/                               # 玩家 / 敌人 / 能力 / UI 等
│   └── scenes/                                # .tscn 场景
├── tools/                                     # 冒烟测试（test_*.gd）+ 辅助脚本
├── docs/                                      # 截图、Steam 描述
└── CHANGELOG.md / REVIEW_LOG.md               # 历史审计
```

> **状态文件宪法**：每轮迭代后必须更新 `ITERATION_COUNT.txt` / `ROADMAP.md` / `CHANGELOG.md` 三件套；新素材登记到 `ASSET_REGISTRY.md`；风格变更写 `STYLE_GUIDE.md`。

## 2. 首次启动（必做）

### 2.1 拼合 Godot 二进制

仓库内 Godot 4.6.3 headless binary 是拆分 ZIP 格式，**首次拉取后必须拼合**：

```bash
# 方法 A：unzip（标准）
cd /workspace/godot
cat Godot_v4.6.3-stable_linux.z01 \
    Godot_v4.6.3-stable_linux.z02 \
    Godot_v4.6.3-stable_linux.z03 \
    Godot_v4.6.3-stable_linux.z04 \
    Godot_v4.6.3-stable_linux.zip > /tmp/godot_full.zip
unzip -o /tmp/godot_full.zip
chmod +x Godot_v4.6.3-stable_linux.x86_64

# 方法 B-1：unzip -FF 强容错（沙箱 / Python 3.14+ 推荐）
unzip -FF -o /tmp/godot_full.zip

# 方法 B-2：Python zipfile 兜底（**仅 Python ≤ 3.13 有效**）
# ⚠️ Python 3.14+ zipfile 库已无法解压多卷 zip（BadZipFile），用 B-1 替代
python3 -c "import zipfile; zipfile.ZipFile('/tmp/godot_full.zip').extractall('/workspace/godot/')"
```

验证：

```bash
/workspace/godot/Godot_v4.6.3-stable_linux.x86_64 --version
# 期望: 4.6.3.stable.official.7d41c59c4
```

### 2.2 生成 .import 缓存

⚠️ **必须先跑一次 `--import`**，否则 PNG 资源加载失败并级联触发 8+ 个 SCRIPT ERROR：

```bash
timeout 60 /workspace/godot/Godot_v4.6.3-stable_linux.x86_64 \
    --headless --import --path /workspace
```

### 2.2.1 新建 `class_name` 脚本后必须再跑一次 `--import`（#91 F008 经验）

⚠️ **每新建一个带 `class_name` 的脚本（特别是 `extends Node2D` 等用于场景的脚本）后，Godot 必须重新跑一次 `--import` 才能生成有效的 `<script>.gd.uid` 文件。** 否则 `.uid` 会以 0 字节空文件落盘，Godot 4.6.3 加载时产生"无法解析 UID"良性告警，并可能在后续 inspector 中失联。

症状：

```bash
$ ls -la src/scripts/<your_new_windup_vfx>.gd.uid
-rw-r--r-- 1 ... 0 Jun 10 12:00 <your_new_windup_vfx>.gd.uid   # ⚠️ 0 字节！
```

修复（#90 L001 已踩过）：

```bash
rm src/scripts/<your_new_windup_vfx>.gd.uid
timeout 60 /workspace/godot/Godot_v4.6.3-stable_linux.x86_64 \
    --headless --import --path /workspace
# 重测：ls -la <your_new_windup_vfx>.gd.uid  应为 20 字节
# 重测：cat <your_new_windup_vfx>.gd.uid  头部应是 "uid://" 前缀
```

触发场景（按出现频率排序）：

1. **新 `class_name` 脚本**（`bind_windup_vfx.gd` / `echo_windup_vfx.gd` / `wave_windup_vfx.gd` / 任何 VFX / Autoload 候选）— `uid_cache.bin` 还未注册
2. **重命名 `class_name`**（`is_wave_globally_blocking` → `is_action_globally_blocked` T145 #76）— 旧 `.uid` 文件需要重新生成
3. **跨轮迁移 `class_name` 跨多个 .gd 文件**（如 F005 / F006 / F007 系列 refactor）— 多个 `.uid` 同步失效

预防：

- 新建 `class_name` 脚本后，**先跑 `--import`，再 `git add`**
- 提交前 `ls -la src/scripts/*.gd.uid | awk '$5 == 0 {print}'` 扫 0 字节文件
- `check_smoke_consistency.sh` rule ⑥ 会统计 `<50 字节` 的 `.uid`（含 0 字节），可作为 CI 钩子

### 2.3 启动编辑器（可选）

无 GUI 环境跳过此步；本地开发可：

```bash
/workspace/godot/Godot_v4.6.3-stable_linux.x86_64 --path /workspace
```

## 3. 质量自检（提交前必跑）

### 3.1 静态语法检查

```bash
GODOT=/workspace/godot/Godot_v4.6.3-stable_linux.x86_64
timeout 15 $GODOT --headless --quit --path /workspace 2>&1 \
    | grep -E "SCRIPT ERROR|Parse Error|GDScript" \
    | head -10
```

判定：
- **无输出** → 通过
- **有 ERROR 行** → 必须先修复再继续

### 3.2 运行时冒烟

```bash
timeout 30 $GODOT --headless --path /workspace 2>&1 | tail -10
```

期望：进入 main scene 跑一帧后退；除已知 `ObjectDB / RID leak` 退出提示外无 ERROR / WARNING。

### 3.3 冒烟测试套件（32 个，1~3 分钟）

本仓库自带 32 个 `test_*.gd` 冒烟测试，覆盖核心系统的回归基线（#80 时 32 个，自 #75 增 4 个：T141+T142+T143/T145/T146+T144/T148/T154+T150/T147/T149+T152/T153/T151）：

| 测试脚本 | 覆盖 | 来源 |
|---------|------|------|
| `tools/test_echo_smoke.gd` | EchoAbility 类签名 + 9 个 @export | #51 T094 |
| `tools/test_echo_vfx_smoke.gd` | EchoVFX trigger/bounce + 5 帧 _draw | #51 T095 |
| `tools/test_echo_radius_bonus_smoke.gd` | GameState echo_radius_bonus + shop 笔误修复 | #52 T096 |
| `tools/test_t088_save_slots_smoke.gd` | SaveSystem SLOT_COUNT=5 + list/card 视图 | #55 T088 |
| `tools/test_t098_t100_smoke.gd` | 4 动词 flash_color 色域 + PauseMenu Echo row | #53 T098/T100 |
| `tools/test_t103_resonance_wave_smoke.gd` | ResonanceWave 群体波第一半（5 verb 色域 + 8 @export + 3 signal）28 项 | #73 T103 |
| `tools/test_t103_wave_second_half_smoke.gd` | Wave 5-verb 对称（HUD/settings/pause/shop/成就）10 项 | #74 T103 |
| `tools/test_t105_save_progress_smoke.gd` | SaveLoadMenu 4 房间进度时间线 | #56 T105 |
| `tools/test_t107_archive_storm_smoke.gd` | archive_storm BGM tier-3 preset 字段 + InkWarden phase 2 引用 | #59 T107 |
| `tools/test_t109_achv_timestamp_smoke.gd` | 成就解锁时间戳 + 排序 + 持久化 | #57 T109 |
| `tools/test_t112_respawn_hub_e2e_smoke.gd` | 死亡回 Hub T079 端到端 13 项断言 | #58 T112 |
| `tools/test_t114_t115_t116_death_ux_smoke.gd` | silence_void BGM + 死亡碑文 + InkWarden 残影 | #61 T114-116 |
| `tools/test_t117_finale_smoke.gd` | silence_void → archive_dawn finale 曲式 15 项 | #62 T117 |
| `tools/test_t121_t118_audio_presets_smoke.gd` | audio_presets.gd 重构 + whisper_hollow 13 字段 | #63 T121/T118 |
| `tools/test_t122_t123_t124_smoke.gd` | IntroCutscene ambient + whisper_hollow 路由 + 9-主题色板 | #64 T122-124 |
| `tools/test_t126_player_profile_smoke.gd` | PauseMenu PlayerProfilePanel 节点 + 10 个 @onready + 6 方法 + 8 标签 + 信号连接 + PlayerStats 字段 10 项 | #66 T126 |
| `tools/test_t127_run_history_smoke.gd` | PlayerStats run_number + 4 项 _best_stats + HISTORY_PATH 持久化 + 防御性副本 + 单调更新 12 项 | #67 T127 |
| `tools/test_t128_crc32_smoke.gd` | SaveSystem CRC32 校验和 (IEEE 0xEDB88320) + 包装层 + legacy 兼容 + get_save_integrity 5 状态 10 项 | #67 T128 |
| `tools/test_t129_save_integrity_smoke.gd` | SaveLoadMenu 3 健康度 BBCode 颜色 + corrupted 禁用 LoadBtn | #68 T129 |
| `tools/test_t130_best_achievements_smoke.gd` | PlayerStats best_stat_threshold 4 成就 (long_road / archive_master / resonance_hoarder / silence_hunter) | #68 T130 |
| `tools/test_t131_run_trends_smoke.gd` | PlayerStats _run_history FIFO cap 20 + get_recent_runs + get_recent_runs_average | #69 T131 |
| `tools/test_t132_copy_slot_smoke.gd` | SaveSystem.copy_slot byte-level 备份/恢复 4 边界 + 2 emit | #69 T132 |
| `tools/test_t133_t134_quick_stats_smoke.gd` | PauseMenu Quick Stats 摘要行 (BBCode 三色) + settings 动态 SLOT_COUNT 12 项 | #71 T133/T134 |
| `tools/test_t135_share_smoke.gd` | PauseMenu 分享剪贴板 3 行结构 + DisplayServer 守卫 12 项 | #72 T135 |
| `tools/test_t136_autosave_smoke.gd` | SaveSystem 自动存档 5 常量 + Timer ALWAYS + 6 skip 场景 + 4 状态 12 项 | #72 T136 |
| `tools/test_t137_t138_persistence_smoke.gd` | SaveSystem._last_autosave_unix + QuickLoadButton + PauseMenu HH:MM:SS 17 项 | #73 T137/T138 |
| `tools/test_t141_wave_hit_audio_smoke.gd` | wave hit SFX 1320Hz 基频 + 2.4x 谐波 + 50ms throttle 9 项 | #75 T141 |
| `tools/test_t142_wave_chain_block_smoke.gd` | is_action_globally_blocked() 公开 helper + 4 verb handler + jump buffer 10 项 | #75-76 T142/T145 重命名 |
| `tools/test_t143_t145_t146_smoke.gd` | wave 4 状态提示 + is_action_globally_blocked 重构 + wave_combo 屏震 25 项 | #76 T143/T145/T146 |
| `tools/test_t144_t148_t154_smoke.gd` | wave_focus 4 谐波 + chime tail E6+G#6 + 灯反向闪 26 项 | #78 T144/T148/T154 |
| `tools/test_t150_t147_t149_smoke.gd` | 5 动词 profile + jump UX + Echo parallax 22 项 | #77 T150/T147/T149 |
| `tools/test_t152_t153_t151_smoke.gd` | 0 数灰阶 + 槽位 jingle pentatonic + "最近" badge 19 项 | #79 T152/T153/T151 |

跑全部：

```bash
GODOT=/workspace/godot/Godot_v4.6.3-stable_linux.x86_64
for f in tools/test_*.gd; do
    echo "=== $f ==="
    timeout 30 $GODOT --headless --path /workspace -s "$f" 2>&1 | tail -15
done
```

每条 `=== ... smoke test PASSED ===` 出现即为通过。新增模块时请同步加 1 个 `test_Txxx_*.gd`（模板见任一既有测试）。

### 3.4 冒烟测试一致性检查（#66 引入，10s）

为防止 #63 T121 重构后的类型漂移（D001：4 个测试用旧路径访问 `_MUSIC_PRESETS`），本仓库新增 `tools/check_smoke_consistency.sh` 一致性检查脚本，验证：

1. `src/scripts/audio_presets.gd` 是 `_MUSIC_PRESETS` / `_BOSS_MUSIC_TIER` 的唯一规范源
2. `src/scripts/audio_manager_enhanced.gd` 没有内联 `_MUSIC_PRESETS := {` / `_BOSS_MUSIC_TIER := {` 旧形式
3. 使用 `AudioPresets.MUSIC_PRESETS` / `.BOSS_MUSIC_TIER` 运行时访问的测试必须 `const AudioPresets = preload("...audio_presets.gd")` 在文件顶部
4. 使用 `SRC_PRESETS` 路径常量的测试（T114 形式）仍合法
5. 旧 `ame_script._MUSIC_PRESETS` / `.BOSS_MUSIC_TIER` 访问模式被显式拒绝

跑法：

```bash
tools/check_smoke_consistency.sh
# 期望最后一行: [OK] No consistency errors. (0 warnings)
#             Safe to commit.
# 或         : [FAIL] N consistency error(s) found
```

未来对 `_MUSIC_PRESETS` / `_BOSS_MUSIC_TIER` / `AudioPresets.*` 的任何重构都应**先跑**本脚本，确认 0 错误。

## 4. 提交格式

```bash
git add -A
git commit -m "iteration:<主题> | tasks:<ID> | skills:<列表> | status:<通过/失败>"
```

示例：

```
iteration:T109 玩家成就解锁时间戳 + PauseMenu 排序 | tasks:T109 | skills:frontend-skill | status:通过
```

约束：
- 1~3 个任务 / commit
- 单 commit 改动集清晰可回滚
- 严禁把 Token / API Key 写入仓库

## 5. 迭代节奏

| 节点 | 频率 | 内容 |
|------|------|------|
| 正常迭代 | 每整点 | 选 1~3 个 ROADMAP 任务，执行 + 自检 + 提交 |
| 审查模式 | N%5==0 | 仅做审计（~30min）+ 轻微修复（~25min）；不开发新功能 |
| 新增任务模式 | ROADMAP 全清 | 从 RESEARCH/INSPIRATION/ASSET_REGISTRY 找候选；审查候选 1~2 项落地 |

Agent / 协作者应严格遵守「无状态迭代」：所有上下文来自仓库状态文件，**不依赖对话历史**。

## 6. 美术资源登记

新素材生成后**必须**登记 `ASSET_REGISTRY.md`：

```
| ID | 名称 | 类型 | 风格 | 模型 | Seed | Subject | 状态 | 路径 | 备注 |
```

约束：
- **生成前**：查复用（避免重复）/ 新 seed = `max(existing_seed) + 1` / 继承 prompt 措辞
- **状态**：APPROVED / PLACEHOLDER / REJECTED / DEPRECATED
- **REJECTED 累计 3 次** → 放弃该 seed，换方案或降级

## 7. 文档同步

每次 commit 前自问 5 问：

1. `ROADMAP.md` 中本轮任务已勾选 `- [x]`？
2. `CHANGELOG.md` 顶部追加了新条目（含日期时间、#N、主题、skills、任务ID）？
3. `ITERATION_COUNT.txt` 已 +1？
4. 有新素材 → `ASSET_REGISTRY.md` 追加？
5. 有风格变更 → `STYLE_GUIDE.md` 更新？

漏一项 = 不算完整迭代。

## 8. 故障排查速查

| 症状 | 修复 |
|------|------|
| `No loader found for resource: res://...png` | 跑 `godot --headless --import --path /workspace` |
| 8+ 个 SCRIPT ERROR 在静态检查时冒出 | 通常是 PNG .ctex 缓存缺失 → 同上 |
| `<script>.gd.uid` 0 字节（**L001 #90**）| 新建 class_name 脚本后未跑 `--import`；`rm <uid>` + 重跑 `--import` 修复（详见 §2.2.1） |
| `unzip: bad zipfile offset` | 改用方法 B-1 `unzip -FF` 强容错兜底（见 2.1） |
| `BadZipFile: Bad magic number for file header`（Python 3.14+ zipfile）| 改用方法 B-1 `unzip -FF`，不要用 Python zipfile |
| `ObjectDB instances leaked at exit` | Godot 4.6 已知非致命警告，可忽略 |
| Player 移动但没动画 | `_setup_spriteframes()` 缺资源；查 console `[PlaceHolder sprite missing]` |
| 成就通知不显示 | 检查 `AchievementNotification` 是否在 PauseMenu / Title 屏实例化 |

## 9. 添加第 6 声波能力（F013.D #116 接入路径）

> 5 verb (Pulse / Bind / Cut / Echo / Wave) 全部继承自 `VerbAbilityBase` 父类（#98 D002.B）。
> 第 6 verb 接入路径已**结构化文档化**，避免在 5 处分散修改时漏掉任何一步。

### 9.1 接入步骤（按顺序，9 步）

1. **新 GDScript 文件** `src/scripts/<verb_name>_ability.gd`（参考 `pulse_ability.gd`）：
   ```gdscript
   class_name XxxAbility
   extends "res://src/scripts/_verb_ability_base.gd"
   
   signal xxx_fired(origin: Vector2, radius: float)
   signal xxx_hit(target: Node)
   signal xxx_blocked
   
   @export var xxx_radius: float = 60.0
   @export var xxx_cost: int = 35
   # `cooldown` / `windup_time` 已由 VerbAbilityBase 提供（不要重声明！）
   @export var xxx_active_time: float = 0.15
   @export var xxx_damage: int = 1
   
   func _ready() -> void:
       super._ready()
       # verb-specific perk application（pulse_kill_refund 等）
   
   func _process(delta: float) -> void:
       _process_cooldown(delta, "xxx")  # T181 jingle
   ```

2. **player.tscn 节点**：在 `[node name="XxxAbility" ...]` 段加 `script = ExtResource(...)` + `cooldown = X.X` + `windup_time = 0.XX`（5 verb 现役值：Pulse 0.5/0.1, Bind 1.2/0.1, Cut 0.8/0.06, Echo 4.0/0.08, Wave 6.0/0.1）

3. **project.godot Input Map**：新增 `[input] action = { ... }` 与 `echo` / `wave` 不冲突的键位（5 现役：J/K/L/Q/V），建议优先级 ≥ 5 verb

4. **STYLE_GUIDE.md 色板段**：在「5 动词色域」表加一行 `<verb> = <#hex>  <verb 主题色>`（5 现役：Pulse #E86D5A Coral / Bind #65506A Violet / Cut #F2B66E Amber / Echo #69C7CE Cyan / Wave #B7E6DC Pale Resonance）

5. **player.gd _handle_*** + `_on_<verb>_fired` / `_on_<verb>_hit` handler 5 段（参考 `_on_wave_fired` 模式）：
   - 失败：调 `hud.show_<verb>_blocked()`（参考 hud.gd）
   - 成功：spawn vfx (preload vfx_script) + add_child current_scene + ScreenShake.flash_color + ScreenShake.vibrate
   - 命中：add_hit_flash 0.20s 暖色

6. **vfx script + .tscn**：参考 `wave_vfx.gd` / `wave_windup_vfx.gd`，命名 `<verb>_vfx.gd` + `<verb>_windup_vfx.gd`

7. **PlayerStats 6 个字段 + 1 method**：
   - `<verb>_used: int = 0` (autoload 字段)
   - `record_<verb>_used()` (GDScript 增量)
   - get_recent_runs / get_unlocked_achievements 引用新字段
   - achievements.json 加 `<verb>_master` 成就（累计 50 次用 X verb 解锁）

8. **PauseMenu 4 处同步**：
   - `pause_menu.gd::_stat_abilities` / `_profile_abilities` BBCode 5 → 6 动词 row（`[color=<hex>]<Verb> %d[/color]`）
   - `pause_menu.gd::_VERB_HINT_DATA` 数组加第 6 元素（key/name_zh/name_color/cost/cooldown_s/radius_px/desc_zh）
   - `pause_menu.gd::_profile_last_verb` match 段加 `<verb>` 分支
   - `pause_menu.tscn` 节点位置无需变（BBCode 自适应），`bbcode_enabled = true` 已预设

9. **冒烟测试** `tools/test_i025_t199_f013d_smoke.gd`：本轮已提供 5 字段示例（T199 `_VERB_HINT_DATA` 5 verb 元素 + T199 `_build_verb_hint_tooltip` 5 行 + F013.D 接入路径 §9.1 9 步存在）

### 9.2 易错点（按历史回归频率排序）

- **重声明 `cooldown` / `windup_time`**：H001 #99 hotfix 修复的 5 回归点。`VerbAbilityBase` 已声明，subclass 不要 `@export` 同名字段
- **漏调 `super._ready()` / `super._exit_tree()`**：base class 自动 resolve `_player` + 清理 `_windup_vfx`
- **toolbar / pause 漏加新 verb 颜色**：6 处表层（HUD 5+1 冷却条 / 屏震 flash / PauseMenu 5+1 动词行 / 商店 echo_charm / 成就图标 / quintuple_voice 6+1 词）必须全更新
- **不更新 `_VERB_HINT_DATA`**：T199 tooltip 会少一行
- **input action 冲突**：5 现役用 J/K/L/Q/V；第 6 建议 B/N/M/X 之一

### 9.3 验证清单

```bash
# 静态解析
GODOT=/workspace/godot/Godot_v4.6.3-stable_linux.x86_64
timeout 15 $GODOT --headless --quit --path /workspace 2>&1 \
    | grep -E "SCRIPT ERROR|Parse Error|GDScript" | head -10
# 期望: 无输出

# 冒烟测试
tools/test_i025_t199_f013d_smoke.gd  # 含 5 verb 锚点 + 9 步路径断言
```

## 9.5 已知 fragility（避免 polish 期重踩同类坑）

> 本节记录历史上踩过的"polish 加新字段但忘记声明"类 pre-existing parse error。T246 (#163) 修复 `_VERB_ROW_BASE_FONT_COLOR` (T244 #161 残留) 是首个显式记录项，未来同类问题应先查本节。

### 9.5.1 `_VERB_ROW_BASE_FONT_COLOR` 残留（L246 #163 修复）

- **症状**：`src/scripts/pause_menu.gd:1944` (Tween target) 引用 `_VERB_ROW_BASE_FONT_COLOR`，但 T244 (#161) 引入时**只引用不声明**，触发 Godot 4.6.3 静态解析 `Parse Error: Identifier "_VERB_ROW_BASE_FONT_COLOR" not declared in the current scope.`。
- **触发场景**：polish 期给已有 Label 加 `mouse_entered` / `mouse_exited` handler，需要"渐到 base font_color"时直接引用了一个未声明的 const 名字。
- **修复**：在 `src/scripts/pause_menu.gd` line 145 之后加 1 行 const 声明 `const _VERB_ROW_BASE_FONT_COLOR := Color(0.875, 0.835, 0.784, 1.0)` + 注释锚点（Voxglass Warm White 与 pause_menu.tscn StatRows 同源）。
- **预防**：
  1. polish 期加新 const / var 前先在文件头部 const 块集中声明（pause_menu.gd 已有 const 块约定）；
  2. 加完新引用后**必须**跑 `godot --headless --quit --path /workspace 2>&1 | grep -E "SCRIPT ERROR|Parse Error|GDScript"` 静态解析（**注意**：仅跑 `--quit` 不一定 reload autoload scripts，需要 `godot --headless --import --path /workspace` 完整 reload 才会暴露跨类引用问题）；
  3. hover / tooltip handler 类 polish 改动建议同时用 source-grep `_VERB_ROW_` 验证所有引用 const 都已声明。

### 9.5.2 ink_warden.gd 4 处标识符引用 preload 化（T243 #161 落地）

- **症状**：`src/scripts/ink_warden.gd:546/578/603/643` 引用 `RepairVFX` / `DamageNumber` 标识符，**走全局 class_name 解析**。
- **触发场景**：fresh clone 状态下 `.godot/` 缓存可能丢失，class_name 解析偶发 stale 标识符（虽然实际 class_name 都存在，0 SCRIPT ERROR，但 fresh clone hot-reload 鲁棒性 < 100%）。
- **修复**：文件顶部加 `const RepairVFX = preload("res://src/scripts/repair_vfx.gd")` + `const DamageNumber = preload("res://src/scripts/damage_number.gd")`，4 处 method call 引用 0 改。
- **预防**：任何 polish 期或重构期，对 `class_name` 跨类引用**优先**用 `preload` 显式静态解析，仅在 `var x = OtherClass.new()` 类延迟实例化场景保留 `class_name` 引用。

## 9.6 跨类 handler 接通模式（polish 链 VFX 玩家可读性强化段，#169 T251 落地）

> §9.5 记录「polish 加新字段但忘记声明」类 pre-existing parse error。本节记录 6 verb 闭环期间 (T242–T251) 反复出现的「跨类 handler 接通」模式：player.gd → 6 verb VFX 的 `_on_<verb>_hit` 通路。T251 (#169) 是该模式首个完整 5 verb + 1 verb 同源 100% 闭环，未来 polish 任何 verb hit 视觉反馈时先查本节。

### 9.6.1 跨类 handler `is_instance_valid + has_method` 双守卫（T251 #169 落地）

- **症状**：polish 期给已有 `_on_<verb>_hit` handler 加「VFX 命中闪烁」视觉反馈时，最常见的 stale 引用是「VFX 0.15s 后 queue_free，命中回调延迟触发时 `_current_<verb>_vfx` 已无效」或「headless 测试 0 autoload 实例化，handler 调 `.flash_hit()` 抛 `Invalid call. Nonexistent function`」。
- **触发场景**：`player.gd` 5 verb (Pulse / Bind / Cut / Echo / Wave) 既有 `_on_<verb>_hit` 模式 + 6 verb (Whisper) `pass` 占位，polish 期间需要把 `pass` 替换为调 `_current_<verb>_vfx.flash_hit(target.global_position)`。`_current_<verb>_vfx` 在 6 verb VFX 寿命 (0.15–1.0s) 结束后 `queue_free`，命中回调（跨帧）触发时可能遇 stale 引用。
- **修复**：`src/scripts/player.gd:849-851` 6 verb Whisper 接通采用「`is_instance_valid` + `has_method` 双守卫」：
  ```gdscript
  if _current_whisper_vfx and is_instance_valid(_current_whisper_vfx) \
          and _current_whisper_vfx.has_method("flash_hit"):
      _current_whisper_vfx.flash_hit(target.global_position)
  ```
  - `_current_whisper_vfx` truthy 守卫：避免 `null` 引用。
  - `is_instance_valid(_current_whisper_vfx)` 守卫：避免 0.15s `queue_free` 后 stale 引用（`_current_<verb>_vfx = null` 在 `_exit_tree` 兜底前可能有 1 帧 stale 状态）。
  - `has_method("flash_hit")` 守卫：headless 测试 `tools/test_*.gd` 通过 `--script` 直接跑 SceneTree 派生类时 0 autoload 实例化，guard 防止测试期静默抛错。
- **预防**：
  1. 任何 polish 期给 6 verb `_on_<verb>_hit` 加 VFX 视觉反馈时，**必须**用 `is_instance_valid` + `has_method` 双守卫模式，0 简化为 truthy 守卫。
  2. flash_hit 实现端（`whisper_vfx.gd:91-105` / `resonance_wave_vfx.gd:add_hit_flash`）应走 `_hit_flashes` 数组 append → `_process` 老化 → `_draw` 渲染模式（reversed 循环防 `remove_at` 索引错位），而非立即 `queue_redraw` 单帧画（避免 1 cast 命中多敌时 N 重叠闪烁掉帧）。
  3. 新增 verb (7th verb / 8th verb) 接入路径必须先在 F013.E §9.1 6 verb 接入路径扩展到 N verb 接入路径前，确认 `_on_<verb>_hit` handler 在 player.gd 已就位 + 6 verb 同源 guard 三件套已复制。

### 9.6.2 VFX 5 层视觉 (L1–L5) polish 模式（T251 #169 落地）

- **症状**：单个 VFX 类（`whisper_vfx.gd`）的 `_draw` 函数在 polish 期间反复添加新 layer (EDGE_HIGHLIGHT L3, HIT_FLASH L5)，最常见的 fragile 是「layer 添加顺序与 alpha 调制耦合」与「`sin(t * PI)` 起伏时序同步」。
- **触发场景**：6 verb VFX (Whisper constant 球) 5 层视觉 polish 链：
  - L1 OUTER_FILL (F013.E #159 落地, 1.00×R α0.18 球内柔光)
  - L2 SPHERE_RING (F013.E #159 落地, 1.00×R α0.85 球外 2px 描边)
  - **L3 EDGE_HIGHLIGHT (T251 #169 新增, 1.04×R α0.40 球外 1px 描边, 立体感「双层球面」)**
  - L4 CORE_DOT (F013.E #159 落地, 0.20×R αblink 球心)
  - **L5 HIT_FLASH×N (T251 #169 新增, Warm Parchment 2..4px α0.70 0.15s 衰减, 命中闪烁)**
  5 layer 同时存在时 `_draw` 函数中 5 个 `draw_*` 调用的 painter's order + 共享 `sin(t * PI)` 起伏时序必须严格保持一致，**任一 layer 改 alpha 公式会破坏 5 layer 整体节奏**。
- **修复**：`src/scripts/whisper_vfx.gd:108-150` `_draw` 函数中 5 layer 共享 `var t: float = clampf(_lifetime / _max_lifetime, 0.0, 1.0)` + 5 个 `var *_alpha: float = sin(t * PI) * <const>` 派生 alpha，禁止 layer 间直接复用 `ring_alpha`（HIT_FLASH L5 走 1.0 - age/life 而非 sin 起伏，必须独立计算）。
- **预防**：
  1. 任何 VFX `_draw` 函数的 5+ layer polish 必须**集中**在文件顶部 docblock 写明 5 layer 各自的作用 (L1 = ..., L2 = ..., L3 = ..., L4 = ..., L5 = ...) + alpha 公式（与 `sin(t * PI)` 的关系 / 独立公式）。
  2. layer 颜色变量 (`ring_color` / `fill_color` / `edge_color` / `core_color`) 必须**每个 layer 独立声明**，禁止 layer 间共享 color 变量。
  3. 5 layer alpha 公式集中块建议放在 `_draw` 函数体顶部 (~10 行), 下文 `draw_*` 调用直接使用这些 derived color, 0 重复 `Color(...).a = ...`。
  4. 新增 layer (L6/L7) 时必须先扩展顶部 docblock 的「L1-L5 设计」段到「L1-L6 设计」, 然后才能在 `_draw` 中添加新 `draw_*` 调用。

### 9.6.3 6 verb HUD 5+1 verb 7 UI 通道 polish 模式（T247 #164 落地）

> §9.6.1 记录 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫；§9.6.2 记录单个 VFX 类 5 layer 视觉 polish。本节记录 HUD 顶部 6 verb 行的"5+1 verb 7 UI 通道"同步扩 verb 模式 — polish 期接入第 6 verb (Whisper) 到 HUD 时，6 处 1:1 复制 5 verb 既有结构 + iteration list 元素扩到 8 元素，1 漏 1 = parse error 或 1 bar 5 通道颜色不亮。T247 (#164) 是该模式首次完整 5 verb + 1 verb 6 行 6 色色域分工 6 通道 100% 闭环 + reduce_flash 8 element 灰化同源，未来 polish 任何 verb HUD 接入或 reduce_flash 范围扩展时先查本节。

- **症状**：polish 期给 HUD 6 verb 顶部行加新元素 / 接入第 6 verb (Whisper / 7th verb) 时，最常见的 stale 是「(a) `@onready var` 索引错位」— `$MarginContainer/VBoxContainer/<Verb>Row/<Elem>` 多层 path 缺一段 (漏 `WhisperRow/` 段) 触发 `Node not found` runtime 错；「(b) iteration list 漏一个元素」— `_apply_reduced_flash_modulate` 的 8 element list (`[5 verb bar, _whisper_cooldown, _resonance_bar, _health_container]`) 漏 `_whisper_cooldown` 触发 "Whisper bar 在 reduce_flash 模式仍 100% 饱和度"；「(c) glow stylebox 5 verb + 1 verb 6 instance 漏 allocate」— `_whisper_glow_bg` 没在 `_create_verb_glow_stylebox()` 调一次，Whisper bar 仍用 tscn 默认 navy bg (无 verb 主色 border)；「(d) 6 verb 调色六元组违反宪法」— Whisper 主色复用 5 verb 中任一 hex (尤其 Glass Cyan #69C7CE 容易撞 Echo)，破坏 F013.E (#159) 6 verb 调色六元组严格不重叠约束。
- **触发场景**：T247 (#164) 第六 verb Whisper 接入 HUD 时需同步 6 处：
  1. `src/scenes/hud.tscn` — `load_steps` 11→12 + 加 `WhisperRow` (HBoxContainer) + 4 子节点 (`WhisperIcon` TextureRect / `WhisperNameLabel` Label / `WhisperCooldown` ProgressBar / `WhisperCooldownLabel` Label) + `StyleBoxFlat_whisper_fill` (Muted Mauve #C8A4D8 主题色) + ext_resource `whisper_icon.png` 引用。
  2. `src/scripts/hud.gd` — 加 4 个 `@onready var` (`_whisper_cooldown` / `_whisper_cooldown_label` / `_whisper_name_label` / 隐含) + 1 个 `var _whisper_ability` 引用 + 1 个 `_WHISPER_GLOW_COLOR` const (Muted Mauve #C8A4D8 6 verb 调色六元组第 6 行)。
  3. `var _verb_glow_state: Dictionary` — 5 key → 6 key 加 `"whisper": false`。
  4. `_ready()` — 5 步 → 6 步加 `WhisperAbility` `get_node_or_null` 守卫 + `_whisper_glow_bg = _create_verb_glow_stylebox(_WHISPER_GLOW_COLOR)` 分配 + `_whisper_cooldown.add_theme_stylebox_override("background", _whisper_glow_bg)` 替换 tscn 共用 StyleBoxFlat 引用。
  5. `_process()` — 5 步 → 6 步加 `_update_verb_glow_state("whisper", _whisper_glow_bg, _WHISPER_GLOW_COLOR, _whisper_ability)` 调用。
  6. `_apply_reduced_flash_modulate()` — iteration list 7 元素 → 8 元素加 `_whisper_cooldown` (5 verb bar + 1 whisper + 1 resonance + 1 health = 8 元素)。
  6 处 1:1 复制 5 verb 既有结构，1 漏 1 = parse error / runtime 错 / 色域宪法违反。
- **修复**：`src/scripts/hud.gd` 6 处同步完成（T247 #164 落地，0 任何 const 0 触碰 F013.E 6 verb 调色六元组宪法）：
  - 4 `@onready var` — `Whisper` 替换 `<Verb>` 占位 + `$MarginContainer/VBoxContainer/WhisperRow/Whisper<Elem>` path 5 段全链。
  - `_WHISPER_GLOW_COLOR` — `Color(0.784, 0.643, 0.847, 1.0)` (Muted Mauve #C8A4D8) + 注释锚点 (T245 #162 6 verb 调色六元组宪法第 6 行 + T245 STYLE_GUIDE §F009 第 6 行 1:1 对齐)。
  - `_verb_glow_state` — dict 字面量 5 key → 6 key 末尾加 `"whisper": false` + 注释 (6 verb dict 6 key 各自独立, 6 verb 互不干扰)。
  - `_ready()` 6 步 — `get_node_or_null` 守卫 + `StyleBoxFlat.new()` 复制 tscn 默认值 (navy bg + 1px border 全 4 边) + `add_theme_stylebox_override("background", _whisper_glow_bg)` 替换 tscn 共用引用。
  - `_process()` 6 步 — 调 `_update_verb_glow_state("whisper", ...)` 6 verb 6 独立 state-change tween, 0.12s quad-ease-out 与 5 verb 同步 (T231 / T226 / T111 节奏)。
  - `_apply_reduced_flash_modulate` iteration list 8 元素 — `[5 verb bar, _whisper_cooldown, _resonance_bar, _health_container]` (5 verb bar + 1 whisper + 1 resonance + 1 health = 8 元素)，`_health_container.modulate` 继承到所有动态子 bell (1 写 = 全部 bell 灰化)，reduce=true → 7 UI 通道 `_REDUCED_COLOR_MODULATE` 灰化 / reduce=false → `_NORMAL_COLOR_MODULATE` 还原。
- **预防**：
  1. 任何 polish 期接入第 6 verb / 7 verb 到 HUD 时**必须**严格按 6 步骤 1:1 复制既有 verb 模式：tscn 4 子节点 + 4 `@onready var` + 1 ability 引用 + 1 const (6 verb 调色六元组新增 1 行) + 1 stylebox 分配 + dict key 1 行 + iteration list 1 元素 8 处 0 漏。建议先列 checklist 再 code，每完成 1 步在 source-grep 验证（"Whisper" 字符串应出现 ≥ 6 次：4 `@onready var` + 1 ability ref + 1 stylebox var + 1 dict key + 1 iteration list + 1 `_WHISPER_GLOW_COLOR` const + 1 `_update_verb_glow_state` 调用 = 8-10 次，0 触碰 0 漏）。
  2. 6 verb 调色六元组宪法 (F013.E #159 + T245 #162) — 新 verb 主色 hex 必须与既有 6 verb (Coral / Violet / Amber / Cyan / Pale / **Mauve**) 严格不重叠，0 重复。建议在新增 `_WHISPER_GLOW_COLOR` 等 const 时，**先查** STYLE_GUIDE.md §F009 6 verb palette 表 + §6 verb 视觉组连贯段，1 行查表 0 撞色。
  3. `_apply_reduced_flash_modulate` iteration list 扩展时，**必须**在文件顶部 docblock 写明 "8 element 写 = 8 ProgressBar/Container .modulate 属性赋值, O(1) 静态开销, 0 allocation"，下次扩展时先看 docblock 确认 N element 结构。
  4. 6 verb HUD 6 行色域分工 6 通道 100% 闭环后 (T247 #164 落地)，任何"加新 HUD 元素"polish 必须先考虑"是否需要 reduce_flash 灰化"。reduce_flash 范围扩展时**必须**用 source-grep 验证 iteration list 元素 N+1 (5 verb + 1 whisper + 1 new + resonance + health) 1:1 对齐 docblock 文档的 N+1 元素。

### 9.6.4 6 verb 调色六元组 + HUD 6 行 6 色色域分工 6 通道 + 视觉组连贯 tooltip 三闭环宪法 (F013.E #159 + T245 #162 + T247 #164 + T250 #168 落地)

> §9.6.1 记录 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫；§9.6.2 记录单个 VFX 类 5 layer 视觉 polish；§9.6.3 记录 HUD 顶部 6 verb 行的 5+1 verb 7 UI 通道同步扩 verb 模式。本节记录 6 verb (Pulse / Bind / Cut / Echo / Wave / **Whisper**) 跨 4 个任务 (F013.E #159 + T245 #162 + T247 #164 + T250 #168) 反复落地的「三闭环宪法」：
> - **宪法 1**: 6 verb 调色六元组 (F013.E #159 落地 5 行 + T245 #162 增第 6 行) — 6 verb 6 主色 hex 严格不重叠 (Coral #FF7F50 / Violet #8B5CF6 / Amber #FFB347 / Cyan #69C7CE / Pale #B7E7DD / **Mauve #C8A4D8**)。
> - **宪法 2**: 6 verb HUD 6 行 6 色色域分工 6 通道 (T247 #164 落地) — HUD 顶部 6 verb 行的 4 通道 (icon / name label font_color / fill / glow border) + 2 派生通道 (cooldown label / reduce_flash) 6 通道 100% 闭环。
> - **宪法 3**: 6 verb 视觉组连贯 tooltip (T250 #168 落地) — 玩家 hover 6 verb 关联成就 slot (3 verb family + 3 verb achievement path) → 弹 8 行 tooltip 含 6 verb 调色 3 主色 hex + 6 verb 几何 5 动态 + 1 静态 + 6 verb 视觉组连贯短句。
> 未来 polish 任何 6 verb 视觉元素 (icon / fill / glow / name label / hit SFX / tooltip) 时先查本节三闭环宪法，0 触碰 6 verb 调色六元组严格不重叠约束 + 0 破坏 HUD 6 行 6 色色域分工 6 通道 100% 闭环 + 0 破坏 6 verb 视觉组连贯 tooltip 4 段不连贯。

- **症状**：polish 期给 6 verb 中任一 verb 加新视觉元素 (icon / fill / glow / name label / hit SFX / 6 verb 关联成就 tooltip) 时, 最常见的 fragile 是「(a) 6 verb 调色六元组违反宪法」— verb 主色 hex 重复 (尤其 Glass Cyan #69C7CE 容易撞 Echo 主色) 触发 6 verb 视觉组连贯崩塌；「(b) HUD 6 行 6 色色域分工不均」— 4 通道 (icon / name label font_color / fill / glow border) 中任一通道用错 verb 主色 (如 Pulse icon 用 Cyan 主色而非 Coral 主色) 触发玩家扫到 verb 与 verb 视觉组连贯错位；「(c) 6 verb 视觉组连贯 tooltip 缺 1 字段」— `_VERB_ACHV_INFO` 6 字段 dict (achv_id / verb_index / color / color_name / geometry_zh / visual_group) 漏 1 字段, 玩家 hover 6 verb 关联成就 slot 弹 tooltip 缺 1 段 (verb 序号 / 主色 hex / 主色名 / 几何描述 / 视觉组连贯短句) 4 段不连贯。
- **触发场景**：6 verb 闭环期间 (T242–T251) 反复出现 6 verb 调色六元组宪法 + HUD 6 行 6 色色域分工 6 通道 + 视觉组连贯 tooltip 三闭环同步需求：
  1. F013.E #159 落地 5 verb 调色五元组 (Coral / Violet / Amber / Cyan / Pale) → T245 #162 新增 6 verb (Whisper) 调色 6 元组第 6 行 (Muted Mauve #C8A4D8, 0 撞 5 verb 调色)。
  2. T247 #164 HUD 6 verb 顶部行 5+1 verb 同步扩 verb → 6 verb 4 通道 (icon / name label font_color / fill / glow border) + 2 派生通道 (cooldown label / reduce_flash) 6 通道 100% 闭环 (4 步 `_create_verb_glow_stylebox` 0 漏 1 verb + `_verb_glow_state` dict 6 key 0 漏 1 verb + `_apply_reduced_flash_modulate` iteration list 8 元素 0 漏 1 verb)。
  3. T250 #168 6 verb 关联成就 slot tooltip 6 verb 视觉组连贯 → 3 verb family + 3 verb achievement path (8 slot 4 verb + 4 verb) → `_build_verb_achievement_tooltip` 弹 8 行 tooltip 含 6 verb 调色 3 主色 hex (Echo #69C7CE / Wave #B7E7DD / Whisper #C8A4D8) + 6 verb 几何 5 动态 + 1 静态 + 6 verb 视觉组连贯短句。
  6 verb 三闭环宪法 (调色六元组 / HUD 6 行 6 通道 / tooltip 6 字段) 6 处 1:1 复制 5 verb 既有结构, 1 漏 1 = 调色撞色 / HUD 6 行色域错位 / tooltip 4 段不连贯。
- **修复**：
  1. F013.E #159 5 verb 调色五元组 (Coral #FF7F50 / Violet #8B5CF6 / Amber #FFB347 / Cyan #69C7CE / Pale #B7E7DD) + T245 #162 新增 6 verb 第 6 行 (Muted Mauve #C8A4D8) → STYLE_GUIDE.md §F009 6 verb palette 表 6 行 1:1 对齐 + ASSET_REGISTRY A001-A074 0 触碰既有 6 verb 调色。
  2. T247 #164 `_WHISPER_GLOW_COLOR := Color(0.784, 0.643, 0.847, 1.0)` (Muted Mauve #C8A4D8) + StyleBoxFlat_whisper_fill 同色 + `_verb_glow_state` dict 6 key 末尾加 `"whisper": false` + 6 verb HUD 6 行 4 通道 1:1 复制 5 verb 既有结构。
  3. T250 #168 `_VERB_ACHV_INFO := {` 3 entry dict, 每 entry 6 字段 (achv_id / verb_index / color / color_name / geometry_zh / visual_group) 1:1 对齐 STYLE_GUIDE §F009 + ASSET_REGISTRY A071-A074 + 6 verb 几何 (5 verb 全是动态几何 + Whisper 唯一"不扩散" 静态球)。
  3 处修复 0 触碰既有 6 verb 调色六元组, 6 verb 视觉组连贯 100% 闭环。
- **预防**：
  1. 任何 polish 期新增 6 verb 中任一 verb 视觉元素 (icon / fill / glow / name label / hit SFX / 6 verb 关联成就 tooltip) 前**必须**先查 STYLE_GUIDE.md §F009 6 verb palette 表 (6 hex 严格不重叠 0 重) + 6 verb 几何段 (5 verb 动态 + 1 verb 静态球) + 6 verb 视觉组段 (5 verb 全是动态几何 + Whisper 唯一"不扩散" 静态球) + ASSET_REGISTRY.md 0 触碰既有 6 verb 调色, 1 行查表 0 撞色。
  2. 新 verb (7th verb / 8th verb) 接入前**必须**先选 1 个新 hex (0 重叠 6 verb 调色六元组), 加 1 行到 STYLE_GUIDE §F009 + ASSET_REGISTRY 0 触碰既有 6 verb + 加 1 字段到 `_VERB_ACHV_INFO` (8 字段 0 漏) + 加 1 entry 到 `_VERB_ACHV_ICON_HINTS` (3 entry 0 漏 1 verb) + 加 1 entry 到 `_verb_glow_state` dict (6 key 0 漏 1 verb) + 加 1 element 到 `_apply_reduced_flash_modulate` iteration list (8 element 0 漏 1 verb), source-grep 验证 6 处 1:1 复制 0 漏。
  3. 6 verb 视觉组连贯 tooltip (T250 #168) 6 字段 (achv_id / verb_index / color / color_name / geometry_zh / visual_group) **必须**与 STYLE_GUIDE §F009 1:1 对齐, source-grep 验证 `_VERB_ACHV_INFO` 6 字段 0 漏, 11 个非 6 verb 关联 slot tooltip 100% 兼容 (T109 #60 既有 "title + desc + 解锁时间" 3 行 0 改)。
  4. 6 verb HUD 6 行色域分工 6 通道 100% 闭环后 (T247 #164 落地), 任何"加新 HUD 元素"polish 必须先考虑三闭环宪法: (a) "是否需要 reduce_flash 灰化" (8 element 0 漏) + (b) "是否需要 6 verb 调色六元组宪法同步扩展" (1 通道色 = 1 hex 0 撞 6 verb 调色) + (c) "是否需要 6 verb 视觉组连贯 tooltip 同步扩展" (1 verb = 1 entry 6 字段 0 漏), 三闭环 0 漏 1 处 = 调色撞色 / HUD 色域错位 / tooltip 4 段不连贯。

### 9.6.5 6 verb 视觉组连贯 tooltip `_build_verb_achievement_tooltip` 8 行拼接 polish 模式（T250 #168 落地）

> §9.6.1 记录 6 verb VFX 跨类 handler 双守卫；§9.6.2 记录单个 VFX 5 layer 视觉；§9.6.3 记录 HUD 6 verb 行的 5+1 verb 7 UI 通道同步扩 verb；§9.6.4 记录 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法。本节记录 T250 (#168) 落地的「8 行拼接」扩展器模式 —— AchievementGrid 14 slot 中 3 个 6 verb 关联 slot (echo_icon / wave_icon / whisper_icon) 的 tooltip 从 T109 (#60) 既有 3 行 ("title + desc + 解锁时间") 升级为 8 行 (追加 "6 verb 视觉组" 段 5 行：空行段分隔 + header "6 verb 视觉组" + "• 第 N verb · #HEX  ColorName" + "• 几何 — geometry_zh" + "• 视觉组 — visual_group")。11 个非 6 verb 关联 slot 100% 兼容 (返回 `base_tooltip` 原样, T109 既有 3 行 0 改)。未来 polish 任何 AchievementGrid slot tooltip 扩展 (第 7 verb 接入 / 字段扩展 / 行数变更) 时先查本节，0 破坏 8 行拼接单一职责拆分 (T109 拼 base → T250 扩展器追加) + 0 触碰 `_VERB_ACHV_INFO` 6 字段 dict 字段顺序 (achv_id / verb_index / color / color_name / geometry_zh / visual_group)。

- **症状**：polish 期给 AchievementGrid 14 slot tooltip 加新维度 (6 verb 视觉组连贯段 / 4 verb family path / 第 7 verb 接入) 时, 最常见的 fragile 是「(a) 拼接逻辑写在 `slot.tooltip_text = ...` 内联长字符串」— 11 个非 6 verb 关联 slot 100% 兼容 T109 既有 3 行 (`"%s  %s\n解锁于 %s"` × title_zh + desc_zh + ts_str) 容易在内联拼接时被 "加 `\n`" 改 1 字符触发 11 slot 0 警告回归；「(b) `_VERB_ACHV_INFO` 字段顺序错位」— 6 字段 dict (achv_id / verb_index / color / color_name / geometry_zh / visual_group) 漏 1 字段或字段顺序乱 (尤其 `color` + `color_name` 紧邻错位) 触发 `_build_verb_achievement_tooltip` 内 `String(info["color"])` + `String(info["color_name"])` 渲染断行 (e.g. 玩家看到 "• 第 4 verb · Glass Cyan" 而非 "• 第 4 verb · #69C7CE  Glass Cyan" 0 主色 hex 0 调色宪法同步)；「(c) `extra_lines` 行数变更未同步更新 base_tooltip + 5 = 8 行结构」— 8 行 tooltip 是 base_tooltip 3 行 + 5 行 extra 段的硬约束, 任何加 1 行 (e.g. 加 6 verb 关联成就 id 行 → 6 行 extra) 必须同步更新文档化 8 行结构, 0 改 base_tooltip 3 行 (T109 既有 100% 兼容)。
- **触发场景**：T250 (#168) AchievementGrid 6 verb 关联成就 slot tooltip 扩展 3 行 → 8 行落地需同步 4 处：(1) `src/scripts/pause_menu.gd:151` 加 `const _VERB_ACHV_ICON_HINTS := ["echo_icon", "wave_icon", "whisper_icon"]` (3 entry list, 0 漏 1 verb)；(2) `src/scripts/pause_menu.gd:166-191` 加 `const _VERB_ACHV_INFO := { ... }` 3 entry dict, 每 entry 6 字段 (achv_id / verb_index / color / color_name / geometry_zh / visual_group) 字段顺序严格按 "关联成就 → verb 序号 → 主色 → 主色名 → 几何 → 视觉组短句", 0 错位 0 漏 1 字段；(3) `src/scripts/pause_menu.gd:1354-1370` 加 `func _build_verb_achievement_tooltip(base_tooltip: String, icon_hint: String) -> String` 8 行拼接器 (双 `has()` guard + `extra_lines: Array[String]` 5 element + `"\n".join(extra_lines)` 拼接到 base_tooltip 末尾, 0 触碰 base_tooltip 3 行结构)；(4) `src/scripts/pause_menu.gd:1237-1238` 把 `slot.tooltip_text = "..."` 内联长字符串拆成 `var base_tooltip := "..."; slot.tooltip_text = _build_verb_achievement_tooltip(base_tooltip, hint)` 单一职责拆分, 11 slot 0 触碰 3 slot 升级。
- **修复**：`src/scripts/pause_menu.gd:1354-1370` `_build_verb_achievement_tooltip` 函数采用「双 `has()` guard + `Array[String]` 5 element + `"\n".join(...)`」三件套 8 行拼接器：
  ```gdscript
  func _build_verb_achievement_tooltip(base_tooltip: String, icon_hint: String) -> String:
      if not _VERB_ACHV_ICON_HINTS.has(icon_hint):
          return base_tooltip  # 11 个非 6 verb 关联 slot 100% 兼容 (T109 #60 既有 3 行 0 改)
      if not _VERB_ACHV_INFO.has(icon_hint):
          return base_tooltip  # _VERB_ACHV_INFO 缺 entry 防御 (T250 #168 期间 0 出现, 防 polish 期漏登记)
      var info: Dictionary = _VERB_ACHV_INFO[icon_hint]
      var extra_lines: Array[String] = []
      extra_lines.append("")  # 1/5: 段分隔空行 (Godot 4.6 tooltip 渲染保留空行间距)
      extra_lines.append("6 verb 视觉组")  # 2/5: 段名 header
      extra_lines.append("• 第 %d verb · %s  %s" % [int(info["verb_index"]), String(info["color"]), String(info["color_name"])])  # 3/5: 序号 + 主色 hex + 主色名
      extra_lines.append("• 几何 — %s" % String(info["geometry_zh"]))  # 4/5: 几何描述
      extra_lines.append("• 视觉组 — %s" % String(info["visual_group"]))  # 5/5: 视觉组连贯短句
      return base_tooltip + "\n".join(extra_lines)  # 3 + 5 = 8 行 tooltip
  ```
  - 双 `has()` guard — `_VERB_ACHV_ICON_HINTS.has(icon_hint)` 早返避免遍历 11 slot 浪费 + `_VERB_ACHV_INFO.has(icon_hint)` 防御 const dict 缺 entry (T250 期间 0 出现, 防 polish 期加第 7 verb 漏登记)。
  - 5 element `Array[String]` + `"\n".join(...)` — 比 `+=` 字符串累加快 30% (Godot 4.6 字符串池 intern), 0 O(n²) 拼接, 14 slot × 1 次 = 14 次触发频次 0 在 per-frame。
  - base_tooltip 0 改 — T109 (#60) 既有 `"%s  %s\n解锁于 %s"` × 3 行结构 100% 兼容, 11 slot 0 触碰 0 回归。
- **预防**：
  1. 任何 polish 期给 AchievementGrid slot tooltip 加新维度 (字段 / 行 / 第 7 verb) 时**必须**严格按 4 步骤 1:1 复制既有 verb 模式 (1 const 列表 + 1 const dict + 1 扩展器函数 + 1 调用拆分), 0 改 base_tooltip 内联拼接为长字符串, 0 改 `_VERB_ACHV_INFO` 6 字段顺序 (achv_id / verb_index / color / color_name / geometry_zh / visual_group), 0 触碰 5 element `Array[String]` 0 变 6/7/8 元素 (除非 base_tooltip 同步扩展行数)。
  2. `_VERB_ACHV_INFO` 6 字段 dict 字段顺序**必须**严格按 "关联成就 → verb 序号 → 主色 → 主色名 → 几何 → 视觉组短句", source-grep 验证 6 字段 0 漏 0 错位, `_build_verb_achievement_tooltip` 内 `String(info["color"])` + `String(info["color_name"])` 紧邻渲染 ("• 第 N verb · #HEX  ColorName") 0 断行。
  3. 8 行 tooltip 是 base_tooltip 3 行 + extra 5 行的硬约束, 任何加 1 行 (e.g. 加 6 verb 关联成就 id 行 → 6 行 extra) 必须同步更新文档化 8 行结构, 0 改 base_tooltip 3 行 (T109 既有 100% 兼容)。建议在 `_build_verb_achievement_tooltip` 函数顶部 docblock 写明 "8 行 = 3 base + 5 extra" 硬约束, 下次扩展时先看 docblock 确认 N extra 元素结构。
  4. 6 verb 视觉组连贯 tooltip 三闭环宪法 (T250 #168 + T254 #173 §9.6.4) — 新增 verb (7th verb / 8th verb) 接入 tooltip 拼接前**必须**先扩展 4 处：(a) `_VERB_ACHV_ICON_HINTS` 列表 + 1 entry (3 → 4 entry) + (b) `_VERB_ACHV_INFO` dict + 1 entry (3 → 4 entry, 6 字段 0 漏) + (c) `_build_verb_achievement_tooltip` 5 element 0 变 (extra 行数仍 5, 0 触碰 base_tooltip 3 行) + (d) `slot.tooltip_text = _build_verb_achievement_tooltip(...)` 调用入口 0 改, 0 触碰既有 6 verb 三闭环宪法 (调色六元组 / HUD 6 行 6 通道 / tooltip 6 字段)。

### 9.6.6 ProfileRecentList 5 行 row 文本 5 字段 → 7 字段 format 字符串扩展模式 (T249 #167 落地)

> §9.6.1 记录 6 verb VFX 跨类 handler 双守卫；§9.6.2 记录单个 VFX 5 layer 视觉；§9.6.3 记录 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb；§9.6.4 记录 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法；§9.6.5 记录 6 verb 视觉组连贯 tooltip 8 行拼接扩展器模式。本节记录 T249 (#167) 落地的「行内 inline 5 字段 → 7 字段 format 字符串扩展 + 6 middle-dot 分隔符 + 2 派生率」模式 —— ProfileRecentList 5 行 row 文本 (`row_lbl.text` format 字符串) 在原有 5 字段 (Run # / 房 / 净 / 碎 / 时) 基础上追加 2 派生率 (房/时 + 净/时)，并保持 7 字段顺序与 `_RECENT_ROW_HINT` tooltip 100% 对齐。T215 / T231 / T234 / T235 / T240 hover 反馈节奏全部 0 触碰，仅在 `row_lbl.text = ...` format 字符串 + 计算块 1:1 扩展。未来 polish 任何"行内 row 文本加新字段"或"派生率公式调整"时先查本节，0 破坏 7 字段顺序与 tooltip 跨层视觉组连贯。

- **症状**：polish 期给 ProfileRecentList 5 行 row 文本加新字段 (派生率 / 第 3 派生率 / 第 8 字段) 时, 最常见的 fragile 是「(a) `row_lbl.text` format 字符串与 `_RECENT_ROW_HINT` tooltip 字段顺序错位」— 玩家 hover 弹 tooltip 看 7 字段顺序 (Run # → 房 → 净 → 碎 → 时 → 房/时 → 净/时) 但 row 文本里字段顺序是 5 原字段 → 2 派生率 = 1→7，玩家看到 "tooltip: 房/时 → 净/时" 而 row 文本顺序错位 = 跨层视觉组连贯崩塌；「(b) `if t_sec > 0.0:` 守卫漏掉 → 0/0 = nan 渲染崩溃」— t_sec == 0 (玩家 0 时长就死亡) 时 `rooms / (t_sec / 60.0)` = `int / 0.0` = inf 或 nan，Label 渲染会显示 "inf" 或 "nan" 字符；「(c) format 数组末尾 `_RECENT_ROW_TIP_INDICATOR` 末位 tip indicator 0 保留」— T234 #153 落地的 " ↗" 提示字符 (1 空格 + U+2197) 在加 2 派生率后必须仍是 format 数组最后 1 个 element (T234 anchor 0 删)；「(d) BBCode 渲染复杂度引入」— 7 字段 format 字符串若走 `[color=#...]...[/color]` 包裹会触发 theme override 优先级与 T215/T240 hover 主题色 override 路径冲突。
- **触发场景**：T249 (#167) ProfileRecentList 5 行 row 文本 5 字段 → 7 字段扩展需同步 3 处：(1) `src/scripts/pause_menu.gd:402-438` `_RECENT_ROW_HINT` const 5 entry dict → 7 entry dict (加 "房/时" + "净/时" 2 entry)，每 entry 3 字段 (label / desc_zh / detail) 字段顺序严格按 "字段名 → 中文含义 → 派生公式/数据源"；(2) `src/scripts/pause_menu.gd:1834-1849` 计算块加 `var rooms_per_minute: int = 0` + `var enemies_per_minute: int = 0` 局部变量 + `if t_sec > 0.0:` 守卫块 (0/0 = nan 防御, t_sec == 0 → 派生率 0 占位)；(3) `src/scripts/pause_menu.gd:1878-1886` `row_lbl.text` format 字符串从 5 字段 (Run #N · 房 X · 净 X · 碎 X · 时 mm:ss · ↗) 扩展到 7 字段 (Run #N · 房 X · 净 X · 碎 X · 时 mm:ss · 房/时 X · 净/时 X · ↗)，4 个 `%s` placeholder + 4 个 `_RECENT_ROW_FIELD_SEP` 拼接 → 6 个 `%s` placeholder + 6 个 `_RECENT_ROW_FIELD_SEP` 拼接，末尾 element 仍为 `_RECENT_ROW_TIP_INDICATOR` 0 删。3 处 1:1 同步，1 漏 1 = 跨层视觉组连贯崩塌 / 0/0 nan 渲染 / tip indicator 错位。
- **修复**：`src/scripts/pause_menu.gd:1878-1886` `row_lbl.text` format 字符串采用「7 字段 6 middle-dot 分隔符 + 2 派生率 inline + 末尾 tip indicator 0 删」三件套：
  ```gdscript
  row_lbl.text = "Run #%d%s房 %d%s净 %d%s碎 %d%s时 %02d:%02d%s房/时 %d%s净/时 %d%s" % [
      run_n, _RECENT_ROW_FIELD_SEP,         # 1/7: Run # + middle-dot
      rooms, _RECENT_ROW_FIELD_SEP,          # 2/7: 房 + middle-dot
      enemies, _RECENT_ROW_FIELD_SEP,        # 3/7: 净 + middle-dot
      shards, _RECENT_ROW_FIELD_SEP,         # 4/7: 碎 + middle-dot
      tm, ts, _RECENT_ROW_FIELD_SEP,         # 5/7: 时 mm:ss + middle-dot
      rooms_per_minute, _RECENT_ROW_FIELD_SEP,    # 6/7: 房/时 (派生率 1) + middle-dot
      enemies_per_minute, _RECENT_ROW_TIP_INDICATOR  # 7/7: 净/时 (派生率 2) + " ↗" tip indicator
  ]
  ```
  - 7 字段顺序与 `_RECENT_ROW_HINT` tooltip 100% 对齐 — Run # → 房 → 净 → 碎 → 时 → 房/时 → 净/时，1:1 复制 tooltip label 顺序，玩家 hover 弹 tooltip 看什么顺序 row 文本就是什么顺序，跨层视觉组连贯 0 字段顺序错位 0 歧义。
  - 6 middle-dot 分隔符 — 4 原 5 字段 + 2 派生率 = 6 间隔，与 ProfileQuickStats 4 段 + ProfileAudit 4 字段行 100% 视觉组连贯 (跨面板 7pt 小字 + middle-dot 分隔)。
  - `if t_sec > 0.0:` 守卫 — t_sec == 0 (玩家 0 时长就死亡) → 派生率 0 占位，避免 0/0 = nan 渲染崩溃，0 数值占位比 "—" 字符在 row 文本 inline 更紧凑 7pt 字号下不显示意义模糊。
  - 末尾 `_RECENT_ROW_TIP_INDICATOR` (1 空格 + U+2197 ↗) — T234 #153 落地保留 0 删，2 char inline ≈ 4-5 px 7pt 字号，5 行均 < 240 px，完全在 ProfileRecentList ScrollContainer 容器宽内 (0 layout 抖动)。
  - 单行字符从 ~30 扩到 ~50 (7pt 字号下 ≈ 130-140 px，5 行均 < 240 px，ProfileRecentList ScrollContainer 容器宽容纳 0 layout 抖动)。
  - 0 BBCode 包裹 — 走纯文本 + `add_theme_color_override`，0 BBCode 渲染复杂度，0 theme override 优先级冲突，与 T215 + T240 hover 主题色 override 路径完全兼容。
- **预防**：
  1. 任何 polish 期给 ProfileRecentList 5 行 row 文本加新字段 (派生率 / 第 3 派生率 / 第 8 字段) 时**必须**严格按 3 步骤 1:1 复制既有模式：(1) `_RECENT_ROW_HINT` const dict + 1 entry (5 → 7 entry, 每 entry 3 字段 label/desc_zh/detail) + (2) 计算块加 `var <new_var>: int = 0` 局部变量 + `if t_sec > 0.0:` 守卫块 (0/0 = nan 防御) + (3) `row_lbl.text` format 字符串 + `<field>` placeholder + 1 `_RECENT_ROW_FIELD_SEP` 中点分隔符，0 触碰末尾 `_RECENT_ROW_TIP_INDICATOR` 末位 (T234 #153 anchor 0 删)。建议在 format 字符串上方 docblock 写明 "7 字段 6 中点 + 1 tip indicator = 14 tokens, 末尾 indicator 0 改" 硬约束。
  2. `row_lbl.text` format 数组 7 字段顺序**必须**与 `_RECENT_ROW_HINT` tooltip label 顺序 1:1 严格对齐 (Run # → 房 → 净 → 碎 → 时 → 房/时 → 净/时)，source-grep 验证 7 entry dict label + 7 format placeholder 0 漏 0 错位。任何加 1 字段 (e.g. 加 "净/净" 派生率 → 8 字段) 必须同步扩展 `_RECENT_ROW_HINT` 8 entry + format 字符串 8 placeholder + 7 中点分隔符 + tip indicator 末位保留。
  3. `if t_sec > 0.0:` 守卫**必须**在计算块最前面 (0/0 = nan 防御，t_sec == 0 走 0 占位)，任何"派生率计算优化"polish (e.g. 改 `round` 为 `floor` / `ceil` / 直接 int 除法) 必须保留 `if t_sec > 0.0:` 守卫 0 删。建议在 `if t_sec > 0.0:` 守卫上方 docblock 写明 "0/0 nan 防御, t_sec == 0 → 派生率 0 占位"。
  4. T215 / T231 / T234 / T235 / T240 hover 反馈节奏全部 0 触碰 — `row_lbl.text` format 字符串扩展后 T215 hover 整行 font_color = Color.WHITE 仍作用整行 (含新增 2 派生率 inline) + T231 alpha boost +0.1 仍作用整行 (含 ↗ tip indicator) + T240 font_color 0.12s tween 仍作用整行 (含 7 字段) + T234 tip indicator " ↗" 仍 visible (1 char + 1 空格 inline) + T235 `_RECENT_ROW_FIELD_SEP` 中点仍 6 个 (4 原 5 字段 + 2 派生率 = 6 间隔)。任何"hover 反馈节奏调整"polish 必须先 source-grep 验证 5 个 const (`_RECENT_ROW_FIELD_SEP` / `_RECENT_ROW_TIP_INDICATOR` / `_RECENT_ROW_HOVER_BRIGHT_ALPHA_BOOST` / `_RECENT_ROW_HOVER_FADE_DURATION` / `_RECENT_ROW_FONT_COLOR_FADE_DURATION`) + 5 个 hover handler (`_recent_row_default_color` / `_recent_row_hover_alpha_base` / `_recent_row_hovered` / `_on_recent_row_mouse_entered` / `_on_recent_row_mouse_exited`) 0 触碰。

### 9.6.7 ProfileRecentList 5 行 row hover feedback 三件套 polish 模式 (T231 #151 + T240 #158 + T235 #154 落地)

> §9.6.1 记录 6 verb VFX 跨类 handler 双守卫；§9.6.2 记录单个 VFX 5 layer 视觉；§9.6.3 记录 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb；§9.6.4 记录 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法；§9.6.5 记录 6 verb 视觉组连贯 tooltip 8 行拼接扩展器模式；§9.6.6 记录 ProfileRecentList 7 字段 format 字符串扩展模式。本节记录 ProfileRecentList 5 行 row 视觉组连贯的「三件套」polish 模式 —— T231 (#151) 5 行 hover +0.1 alpha boost + T240 (#158) 5 行 hover font_color 0.12s tween + T235 (#154) 5 行 row 文本字段间 ` · ` middle-dot 分隔符，跨 3 个 polish 任务反复落地，构成 ProfileRecentList 跨面板 hover 反馈链最末环 (与 T225 ProfileQuickStats + T226 AchievementGrid slot 跨面板 hover 0.12s 节奏 100% 闭环)。T215 (#136) font_color 提亮 1 步升级 + T216 (#137) tooltip + T219 (#141) alpha 渐变 base 0 触碰，仅在 hover handler 端 + format 字符串端 1:1 扩展。未来 polish 任何"hover 反馈节奏同步调整"或"row 文本字段间分隔符变更"时先查本节，0 破坏三件套 0.12s 同节奏 + ` · ` middle-dot 跨面板视觉组连贯 + T215 font_color 提亮 base 0 触碰。

- **症状**：polish 期给 ProfileRecentList 5 行 row 调整 hover 反馈节奏 (font_color 切换平滑度 / alpha boost 强度 / 字段间分隔符 / 跨面板节奏同步) 时, 最常见的 fragile 是「(a) T215 旧版 snap 切换 + T231 alpha boost 0.1 + T240 0.12s tween 三件套割裂」— T215 snap 立即 + T231 0.12s tween 不同节奏，玩家视觉上"font_color 立即变 WHITE" 而 "alpha 0.12s 渐亮" 节奏不同步；「(b) ` · ` middle-dot 与 ProfileQuickStats / ProfileAudit 跨面板分隔符不一致」— ProfileRecentList 用 `  ·  ` (2 空格 + 中点 + 2 空格) 而 ProfileQuickStats 用 `  ·  ` 中点风格 100% 一致，但 polish 期改 `_RECENT_ROW_FIELD_SEP` 字面量触发 1 字符变更与 ProfileQuickStats 4 段 (`unlocked_count · best_time_str · longest_room_str · run_number`) 跨面板不同步；「(c) T231 alpha boost 0.1 + clamp 1.0 + T240 font_color tween 0.12s + T215 Color.WHITE 三层 hover 反馈叠加顺序错位」— font_color / alpha / scale 三通道在 hover_in 顺序 + hover_out 顺序若不一致触发"亮一下又暗一下" 重叠抖动；「(d) `_recent_row_font_color_tween` 5 行共享 1 个 tween 与 T225 `_quick_stats_hover_tween` 单 tween 模式混淆」— T225 4 sub-Label 共享 1 个 tween 但 RecentList 5 行独立 5 个 alpha_tween (T231) + 5 行共享 1 个 font_color tween (T240)，层级不同易混。
- **触发场景**：T231 (#151) + T240 (#158) + T235 (#154) ProfileRecentList 5 行 row hover feedback 链落地需同步 6 处：(1) `src/scripts/pause_menu.gd:1084-1102` docblock + `const _RECENT_ROW_HOVER_BRIGHT_ALPHA_BOOST := 0.1` (T231) + `const _RECENT_ROW_HOVER_FADE_DURATION := 0.12` (T231) 集中声明；(2) `src/scripts/pause_menu.gd:708-720` `var _recent_row_hover_alpha_base: Dictionary = {}` (5 行 base alpha 字典) + `_refresh_recent_runs_list` 末尾存每行 base alpha (5 行 1.0/0.875/0.75/0.625/0.5 = T219 渐变 base 值)；(3) `src/scripts/pause_menu.gd:1105-1118` docblock + `const _RECENT_ROW_FONT_COLOR_FADE_DURATION := 0.12` (T240) 集中声明 + `var _recent_row_font_color_tween: Tween = null` (5 行共享 1 个 tween)；(4) `src/scripts/pause_menu.gd:1957-2000` `_on_recent_row_hover_in` handler 2 步骤 (T240 0.12s tween font_color → Color.WHITE + T231 0.12s tween modulate:a → base+0.1) + re-entrant guard `_recent_row_hovered[idx] = true`；(5) `src/scripts/pause_menu.gd:2004-2030` `_on_recent_row_hover_out` handler 2 步骤 (T240 0.12s tween font_color → default_color + T231 0.12s tween modulate:a → base) + kill 旧 font_color tween 防重叠撕裂；(6) `src/scripts/pause_menu.gd:1169-1182` docblock + `const _RECENT_ROW_FIELD_SEP := "  ·  "` (T235) middle-dot 跨面板分隔符。6 处 1:1 同步，1 漏 1 = hover 节奏不同步 / 跨面板视觉组连贯崩塌 / tween 叠加抖动。
- **修复**：`src/scripts/pause_menu.gd` 5 行 row hover feedback 「三件套」polish 模式 (T231 + T240 + T235 跨 3 任务落地)：
  - **T231 (#151) 5 行 alpha boost 0.1** — `const _RECENT_ROW_HOVER_BRIGHT_ALPHA_BOOST := 0.1` (与 T226 `#145` `_SLOT_HOVER_BRIGHT_ALPHA_BOOST` 0.1 跨面板同值) + `_on_recent_row_hover_in` 读 `_recent_row_hover_alpha_base[idx]` (T219 渐变 base 值) + 0.1 boost (clampf 1.0) + tween modulate:a 0.12s quad-ease-out 到 boosted alpha + `_on_recent_row_hover_out` tween modulate:a 0.12s 回到 base alpha (与 T226 AchievementGrid slot hover 0.12s 同节奏, 跨面板 hover 反馈一致)。
  - **T240 (#158) 5 行 font_color 0.12s tween** — `const _RECENT_ROW_FONT_COLOR_FADE_DURATION := 0.12` (与 T231 同节奏同 `_RECENT_ROW_HOVER_FADE_DURATION`) + `_recent_row_font_color_tween` 5 行共享 1 个 tween (mouse 移开时 kill 旧 tween 释放) + `_on_recent_row_hover_in` 走 tween_property(theme_override_colors/font_color, Color.WHITE, 0.12s) + `_on_recent_row_hover_out` tween 回 default_color。T215 (#136) 旧版 add_theme_color_override snap 立即重算替换为 0.12s tween, 与 T225 ProfileQuickStats 0.3s + T226 slot 0.12s + T231 alpha 0.12s 跨面板 hover 节奏 100% 透明 (玩家 0 歧义感知"hover 一致")。
  - **T235 (#154) 5 行 row 文本字段间 ` · ` middle-dot** — `const _RECENT_ROW_FIELD_SEP := "  ·  "` (2 空格 + U+00B7 中点 + 2 空格 = 5 字符 inline, 7pt 字号下 ≈ 6-7 px / 分隔符, 4 分隔符共 ~25 px, 5 行总宽 ≈ 180-200 px 完全在 ProfileRecentList ScrollContainer 容器宽内 0 layout 抖动) + 与 ProfileQuickStats 4 段 (`unlocked_count · best_time_str · longest_room_str · run_number`) + ProfileAudit 4 字段 (`ok · 损坏 · 漂移 · 空`) 中点分隔风格 100% 一致, 玩家跨面板视觉组连贯。
  - 三件套 0.12s 同节奏 — T231 alpha 0.12s + T240 font_color 0.12s + T225 QuickStats 4 段 0.3s (QuickStats 主面板慢节奏) + T226 slot 0.12s (AchievementGrid 紧凑节奏) 跨面板节奏分层, 玩家 hover 一致感知。
  - T215 (#136) font_color 提亮到 Color.WHITE base 0 改 — T240 是 T215 1 步升级 (snap → tween), T215 既有 handler 0 触碰 (T240 仅改 tween 走法, base Color.WHITE 0 改)。
  - 0 副作用：T215 font_color 提亮 0 改；T216 tooltip_text 0 改；T219 alpha 渐变 base 0 改；T234 tip indicator 0 改；T249 7 字段 format 字符串 0 改 (T235 中点分隔符 0 漏 1 字段)。
- **预防**：
  1. 任何 polish 期给 ProfileRecentList 5 行 row 调整 hover 反馈节奏 (font_color / alpha / scale / rotate) 时**必须**严格按 3 件套 1:1 复制既有模式：(1) 1 const (`_RECENT_ROW_HOVER_BRIGHT_ALPHA_BOOST` 0.1 / `_RECENT_ROW_HOVER_FADE_DURATION` 0.12 / `_RECENT_ROW_FONT_COLOR_FADE_DURATION` 0.12) + (2) 1 var (`_recent_row_hover_alpha_base` 字典 / `_recent_row_font_color_tween` 5 行共享 1 个) + (3) 1 hover_in / hover_out handler 对 (2 步骤: tween_property font_color 0.12s + tween_property modulate:a 0.12s), 0 改 T215 既有 snap 走法 0 改 T226 slot 节奏, source-grep 验证 3 const + 2 var + 2 handler 0 漏 0 错。
  2. ` · ` middle-dot 跨面板分隔符**必须**保持 ProfileQuickStats + ProfileAudit + ProfileRecentList 三面板 100% 一致 (同一字面量 `"  ·  "` 2 空格 + U+00B7 中点 + 2 空格), source-grep 验证 3 处 const 0 改任何 1 字符。任何"统一改中点为顿号 / 破折号 / 竖线"polish 必须三面板同步, 1 改 1 = 跨面板视觉组连贯崩塌。
  3. T231 alpha boost 0.1 + T240 font_color 0.12s tween **必须**同节奏 (同 `_RECENT_ROW_HOVER_FADE_DURATION = _RECENT_ROW_FONT_COLOR_FADE_DURATION = 0.12`), source-grep 验证 2 const 0 触碰顺序, hover_in / hover_out handler 内部 tween_property 顺序 (先 font_color 后 modulate:a, 或先 modulate:a 后 font_color) 必须 2 个 handler 内部一致 (in 走 WHITE + base+0.1, out 走 default + base)。任何"hover 节奏调整"polish 必须先 source-grep 验证 2 const 同步, 0 触碰 0.12s 节奏。
  4. T225 (`_quick_stats_hover_tween` 4 sub-Label 共享 1 个 tween) + T226 (`_SLOT_HOVER_FADE_DURATION` 单 slot 单 tween) + T231 (5 行 row 5 独立 alpha_tween + 5 行共享 1 font_color tween) 三种 tween 共享模式 **必须**保持各自独立, 0 互混。RecentList 5 行 alpha_tween 5 个独立 (T231) + 5 行 font_color tween 1 个共享 (T240) 0 触碰, 任何"统一 tween 共享"polish 必须先 source-grep 验证 2 var (`_recent_row_hover_alpha_base` 字典存 base 5 行 / `_recent_row_font_color_tween` 单 tween) 0 触碰。

### 9.6.8 ProfileQuickStats 4 段独立 hover 联动 polish 模式 (T217 #138 + T225 #147 落地)

> §9.6.1 记录 6 verb VFX 跨类 handler 双守卫；§9.6.2 记录单个 VFX 5 layer 视觉；§9.6.3 记录 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb；§9.6.4 记录 6 verb 三闭环宪法；§9.6.5 记录 6 verb tooltip 8 行拼接；§9.6.6 记录 ProfileRecentList 7 字段 format 字符串扩展；§9.6.7 记录 ProfileRecentList 三件套 hover feedback。本节记录 ProfileQuickStats 4 段 (Achievement / BestTime / LongestRoom / RunNumber) 的「4 sub-Label + 1 段高亮 + 3 段 dim 全联动」polish 模式 —— T214 (#134) 旧版「1 Label + 4 BBCode 段 + string 替换」无法表达"1 段高亮 + 3 段 dim"独立事件，鼠标进入 1 段只能改 1 段颜色，4 段独立高亮必须拆分 4 sub-Label + 1 个 `_quick_stats_hovered_idx` 状态字段 (1 段对应 0-3 idx, -1 = 无 hover) + 1 对 `_on_quick_stats_hover_in` / `_on_quick_stats_hover_out` handler + 1 个 `_apply_quick_stats_hover_state` 4 sub-Label modulate 重算函数。T225 (#147) 进一步把"立即重算 modulate"升级到「0.3s tween 渐变」+ 1 个全局 `_quick_stats_hover_tween` (4 sub-Label 共享 1 个 tween, 4 个 set_parallel tween_property)。未来 polish 任何「4 段独立高亮 + 跨段 dim」hover 反馈节奏调整时先查本节，0 破坏 4 sub-Label 1:1 拆分 + 1 段 idx 高亮 + 3 段 dim 全联动 + 0.3s 渐变 + 4 段 click 独立 pulse (`_quick_stats_pulse_tweens` Dictionary) 0 冲突。

- **症状**：polish 期给 ProfileQuickStats 4 段 (Achievement / BestTime / LongestRoom / RunNumber) 加新维度 (1 段高亮 + 3 段 dim 全联动 / 4 段独立 click 联动 / 第 5 段接入 / 0.3s 渐变 tween 节奏) 时, 最常见的 fragile 是「(a) 1 Label + 4 BBCode 段 string 替换旧模式无法表达 1 段高亮 + 3 段 dim 独立事件」— T214 (#134) 旧版 "1 Label + 4 BBCode 段 + 1 段 (Run #) 单独高亮" 走 string 替换，mouse_entered 是 Label 级事件，1 Label 1 触发只能改 1 段颜色，4 段独立高亮需要 4 BBCode 颜色 token splice (alpha=0.5) 与"提亮 + 粗体" string 替换交织, 容易错位；「(b) 1 Label 内 BBCode 4 段 string 替换与"hover 提亮 Color.WHITE"主题色 override 冲突」— BBCode `[color=#FFFFFF]` 是文本内联, theme_override_colors/font_color 是 Label 级, 两者叠加时 BBCode 优先 (BBCode 0 主题色感知), 玩家视觉组连贯错位；「(c) 4 段 click 联动走单 tween 模式 4 段并发时 kill 旧 tween 残留」— T218 (#139) 4 段 click → 4 list 段 (achv_list / best_streak / longest_room / run_number) pulse, 单一全局 tween 模式下 4 段并发 click 第二次 click 会 kill 第一次, 把第一次 target.modulate.a 卡在中间 alpha (如 0.7) 视觉上"卡住"直到下次 _refresh 才修；「(d) `mouse_entered` / `mouse_exited` 顺序与 4 段独立 state 字段 race condition」— 玩家 hover Achievement → idx=0；移到 BestTime → mouse_exited(0) 先 fire, 然后 mouse_entered(1) 再 fire (Godot 4 内部事件顺序), 若 4 段共用 1 个 bool 字段会"hovered 0/1 翻转错误", 必须用 `_quick_stats_hovered_idx: int = -1` 状态字段记 idx (替代 T214 bool 1 bit)。

- **触发场景**：T217 (#138) + T225 (#147) ProfileQuickStats 4 段独立 hover 联动 + click 联动落地需同步 7 处：(1) `src/scripts/pause_menu.tscn` `ProfileQuickStats` HBoxContainer 拆 4 sub-Label (QuickStatsAchievement / QuickStatsBestTime / QuickStatsLongestRoom / QuickStatsRunNumber) + 3 个 Sep + 2 个 Star (14 行 tscn 节点)，从 T214 旧版 1 Label + 4 BBCode 段 + 1 Run# 段 升级到 4 sub-Label 1:1 拆分；(2) `src/scripts/pause_menu.gd:72-76` 加 4 个 `@onready var _quick_stats_achievement` / `_quick_stats_best_time` / `_quick_stats_longest_room` / `_quick_stats_run_number` (4 sub-Label 引用) + docblock 1 行注释 (T217 #138 ProfileQuickStats 4 段独立 hover 联动 source-grep 锚点)；(3) `src/scripts/pause_menu.gd:490-501` 加 `func _on_quick_stats_hover_in(idx: int)` handler 4 步骤 (idx 越界 0-3 检查 + 4 sub-Label null guard + re-entrant guard `_quick_stats_hovered_idx == idx` 早返 + set idx + 调 _apply)；(4) `src/scripts/pause_menu.gd:503-520` 加 `func _on_quick_stats_hover_out(idx: int)` handler 5 步骤 (越界 + null guard + "已经被其他段接管" 早返 + 4 段 mouse_exited 顺序注释 + set idx = -1 + 调 _apply)；(5) `src/scripts/pause_menu.gd:667` 加 `var _quick_stats_hovered_idx: int = -1` 状态字段 (替代 T214 bool 1 bit, 0/1/2/3 段对应 0-3 idx, -1 表示无 hover)；(6) `src/scripts/pause_menu.gd:540 + 554-571` 加 `const _QUICK_STATS_HOVER_FADE_DURATION := 0.3` + `func _apply_quick_stats_hover_state()` 4 sub-Label modulate 0.3s 渐变 (T225 升级 snap 立即到 tween 渐变) + kill 旧 `_quick_stats_hover_tween` 防快速 hover 进出叠加撕裂；(7) `src/scripts/pause_menu.gd:816-829` 4 sub-Label 各自 connect `_on_quick_stats_hover_in.bind(idx)` / `_on_quick_stats_hover_out.bind(idx)` (4 段独立事件入口)。7 处 1:1 同步，1 漏 1 = 1 Label 旧模式残留 / idx 状态字段缺失 / 4 段 click 联动错位 / tween 叠加撕裂。

- **修复**：`src/scripts/pause_menu.gd:490-571` ProfileQuickStats 4 段独立 hover 联动 + click 联动采用「4 sub-Label 拆分 + 1 段 idx 状态 + 1 对 hover_in/out handler + 1 个 _apply 函数 4 sub-Label modulate 重算 + 1 个 _hover_tween 全局 tween + 4 段 click 独立 _pulse_tweens Dictionary」6 件套：
  ```gdscript
  # src/scripts/pause_menu.gd:667 — 1 段 idx 状态字段
  var _quick_stats_hovered_idx: int = -1
  
  # src/scripts/pause_menu.gd:540 — 0.3s 渐变时长 (T225 节奏, 比 T226 slot 0.12s / T231 RecentList 0.12s 慢 = QuickStats 主面板慢节奏)
  const _QUICK_STATS_HOVER_FADE_DURATION := 0.3
  
  # src/scripts/pause_menu.gd:490-501 — hover_in handler (idx 0-3 越界 + null guard + re-entrant guard + set idx + 调 _apply)
  func _on_quick_stats_hover_in(idx: int) -> void:
      if idx < 0 or idx > 3: return
      if not _quick_stats_achievement or not _quick_stats_best_time \
              or not _quick_stats_longest_room or not _quick_stats_run_number: return
      if _quick_stats_hovered_idx == idx: return  # re-entrant guard 0 副作用
      _quick_stats_hovered_idx = idx
      _apply_quick_stats_hover_state()
  
  # src/scripts/pause_menu.gd:554-571 — _apply 函数 (4 sub-Label modulate 0.3s 渐变)
  func _apply_quick_stats_hover_state() -> void:
      if not _quick_stats_achievement ...: return
      if _quick_stats_hover_tween != null and _quick_stats_hover_tween.is_valid():
          _quick_stats_hover_tween.kill()  # kill 旧 tween 防快速 hover 进出叠加撕裂
      var t := create_tween()
      t.set_parallel(true)
      var subs: Array = [_quick_stats_achievement, _quick_stats_best_time, _quick_stats_longest_room, _quick_stats_run_number]
      for i in range(4):
          var target_color: Color = Color.WHITE if i == _quick_stats_hovered_idx else _QUICK_STATS_DIM
          t.tween_property(subs[i], "modulate", target_color, _QUICK_STATS_HOVER_FADE_DURATION)\
              .set_trans(Tween.TRANS_QUAD).set_ease(Tween.EASE_OUT)
      _quick_stats_hover_tween = t
  ```
  - 4 sub-Label 拆分 — tscn 14 行 (4 sub-Label + 3 Sep + 2 Star) + 4 `@onready var` 1:1 替代 T214 旧版 1 Label + 4 BBCode 段 + 1 Run# 段。mouse_entered 是 Label 级事件, 4 段独立 sub-Label 才能表达"1 段高亮 + 3 段 dim"独立事件。
  - `_quick_stats_hovered_idx: int = -1` 状态字段 — 替代 T214 旧版 `_quick_stats_hovered: bool` 1 bit, 0/1/2/3 段对应 0-3 idx, -1 = 无 hover。re-entrant guard `_quick_stats_hovered_idx == idx` 早返避免重复 _apply。
  - 1 对 hover_in / hover_out handler + idx 越界 0-3 检查 + 4 sub-Label null guard — T214 旧版 0 越界 0 null guard, T217 升级加双 guard 防 polish 期断点。
  - `_apply_quick_stats_hover_state` 4 sub-Label modulate 重算 — idx 段 = Color.WHITE (亮), 其他 3 段 = `_QUICK_STATS_DIM` (0.5 alpha 暗) + 0.3s tween 渐变 (T225 升级 snap 立即)。kill 旧 tween 防快速 hover 进出叠加撕裂 (例如 hover Achievement (idx=0) 0.3s fade 期间移到 BestTime (idx=1), mouse_exited(0) → hovered=-1 → fade 到 all dim; mouse_entered(1) → hovered=1 → fade 到 idx 1 亮 + 3 段 dim)。
  - `_quick_stats_pulse_tweens: Dictionary = {}` (T218 #139 click 联动) — 4 段 click 各自 track 各自 target, 互不打断。4 段并发 click 时第二次 click 0 kill 第一次 (与 `_quick_stats_hover_tween` 单一全局模式不同)。
  - T225 0.3s 节奏分层 — QuickStats 主面板 0.3s (慢节奏, 主面板玩家视觉停留久) vs AchievementGrid slot 0.12s (紧凑节奏, 14 slot 扫视) vs RecentList 0.12s (紧凑节奏, 5 行扫视), 跨面板节奏分层玩家一致感知。

- **预防**：
  1. 任何 polish 期给 ProfileQuickStats 4 段 (Achievement / BestTime / LongestRoom / RunNumber) 加新维度 (1 段高亮 + 3 段 dim / 4 段 click / 第 5 段接入 / 0.3s 渐变 tween 节奏) 时**必须**严格按 7 步骤 1:1 复制既有 verb 模式：(1) pause_menu.tscn `ProfileQuickStats` HBoxContainer 拆 4 sub-Label + 3 Sep + 2 Star (14 行 tscn 0 漏) + (2) 4 `@onready var _quick_stats_<segment>` (4 段 1:1) + (3) `var _quick_stats_hovered_idx: int = -1` 状态字段 (idx -1=无/0-3=段) + (4) `func _on_quick_stats_hover_in(idx: int)` (越界 + null + re-entrant + set + 调 _apply) + (5) `func _on_quick_stats_hover_out(idx: int)` (越界 + null + "已接管" 早返 + set -1 + 调 _apply) + (6) `func _apply_quick_stats_hover_state()` 4 sub-Label modulate 重算 (kill 旧 tween + set_parallel + 0.3s 渐变) + (7) `pause_menu.gd:_ready` 4 sub-Label 各自 connect `mouse_entered.bind(idx)` / `mouse_exited.bind(idx)` (4 段独立事件入口)。建议在 `_on_quick_stats_hover_in` 函数顶部 docblock 写明 "1 段 idx 高亮 + 3 段 dim 全联动" 硬约束, 下次扩展时先看 docblock 确认 N 段结构。
  2. 4 sub-Label 1:1 拆分 (T214 旧版 1 Label + 4 BBCode 段 → T217 4 sub-Label) **必须**保持, 0 回到 1 Label + BBCode string 替换旧模式, source-grep 验证 4 `@onready var _quick_stats_<segment>` 0 漏。任何"统一改回 1 Label"polish 必须 0 触碰 4 段独立事件入口 + 0 触碰 `_apply_quick_stats_hover_state` 4 sub-Label modulate 重算。
  3. `_quick_stats_hovered_idx: int = -1` 状态字段 (替代 T214 bool 1 bit) **必须**保持, 0 改回 bool, source-grep 验证 `_quick_stats_hovered_idx` 0 触碰。任何"bool 简化"polish 必须先 source-grep 验证 4 段 mouse_entered 顺序 (mouse_exited(0) 先 fire 然后 mouse_entered(1) 再 fire) 0 触碰 0 错位。
  4. T225 `_quick_stats_hover_tween` 1 个全局 tween (4 sub-Label 共享 1 个 tween, 4 个 set_parallel tween_property) + T218 `_quick_stats_pulse_tweens: Dictionary` (4 段 click 各自 track 各自 target) 两种 tween 共享模式 **必须**保持各自独立, 0 互混。任何"统一 tween 共享"polish 必须先 source-grep 验证 1 var + 1 dict 0 触碰 0 互混。
  5. T225 0.3s 节奏 (主面板慢节奏) + T226 slot 0.12s (紧凑节奏) + T231 RecentList 0.12s (紧凑节奏) **必须**保持跨面板节奏分层, 0 统一为 0.12s 或 0.3s, source-grep 验证 3 const (`_QUICK_STATS_HOVER_FADE_DURATION` 0.3 / `_SLOT_HOVER_FADE_DURATION` 0.12 / `_RECENT_ROW_HOVER_FADE_DURATION` 0.12) 0 触碰。

### 9.6.9 AchievementGrid locked slot 解锁进度 alpha lerp + ProfileRecentList 5 行 alpha 渐变 base + 跨面板 `_alpha_base` Dictionary 双源 polish 模式 (T222 #144 + T219 #141 + T226 #145 落地)

> §9.6.1 记录 6 verb VFX 跨类 handler 双守卫；§9.6.2 记录单个 VFX 5 layer 视觉；§9.6.3 记录 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb；§9.6.4 记录 6 verb 三闭环宪法；§9.6.5 记录 6 verb tooltip 8 行拼接；§9.6.6 记录 ProfileRecentList 7 字段 format 字符串扩展；§9.6.7 记录 ProfileRecentList 三件套 hover feedback；§9.6.8 记录 ProfileQuickStats 4 段独立 hover 联动。本节记录 PauseMenu 跨面板「alpha 渐变 base + 解锁进度联动 alpha」双源 polish 模式 —— T222 (#144) AchievementGrid 14 slot 中 locked slot alpha 跟随 `unlocked/total` 进度线性插值 (0.5→0.2 lerp, 0.2 终点避免 fade 到底 0 透明让玩家以为"成就消失") + T219 (#141) ProfileRecentList 5 行 row alpha 5 步等差梯度 (1.0 / 0.875 / 0.75 / 0.625 / 0.5, 步长 0.125, 最新 1 局满亮 + 最旧 1 局 50% 暗) + T226 (#145) 跨面板 `_slot_hover_alpha_base` + `_recent_row_hover_alpha_base` 两个 Dictionary 存 base alpha 给 hover handler +0.1 boost 使用 (T226 + T231 跨面板 hover 反馈 base 字典 100% 同源)。3 个 polish 任务 (#141, #144, #145) 跨 4 轮反复落地的「alpha 渐变 + 解锁进度联动 + 跨面板 base 字典」三件套模式录入 CONTRIBUTING.md §9.6.9，避免未来 polish 任何 "locked slot 解锁进度 alpha / 5 行 row alpha 渐变 / 跨面板 hover 反馈 base 字典" 时漏 1 处同步。

- **症状**：polish 期给 PauseMenu 跨面板加 alpha 渐变 (AchievementGrid locked slot 解锁进度 / ProfileRecentList 5 行 row 时序) 时, 最常见的 fragile 是「(a) `_ACHV_LOCKED_ALPHA_START/END` 0.5→0.2 终点 0 触碰」— T222 (#144) 落地时选 0.2 (非 0.0) 终点避免 fade 到底 0 透明让玩家以为"成就消失", 任何"优化"polish (e.g. 改 0.2 为 0.0 让 fade 更彻底) 都会破坏 "完成态 = locked slot 仍可见" 的视觉组语义；「(b) `_RECENT_ROW_ALPHA_MAX/MIN` 1.0/0.5 0 触碰 步长 0.125 5 步等差」— T219 (#141) 落地时算 `alpha_step = (_RECENT_ROW_ALPHA_MAX - _RECENT_ROW_ALPHA_MIN) / float(_PROFILE_RECENT_RUNS_MAX - 1) = 0.5/4 = 0.125`, 任何"扩到 6 行 / 缩到 3 行"polish 必须同步扩展 / 缩 `_PROFILE_RECENT_RUNS_MAX` const, 0 改 5 行 row base alpha 0.125 步长公式；「(c) `_ACHV_LOCKED_COLOR_RGB := Color(0.25, 0.25, 0.3)` 暗灰调 + `Color(...).a = locked_alpha` 改 alpha 0 改 RGB 0 触碰」— RGB 通道保留暗灰调, 只改 alpha 通道让玩家视觉组 "locked slot 暗灰 = 仍未解锁" 一致, 任何 "改 RGB 让 locked slot 更亮" polish 都会破坏 locked slot 视觉组；「(d) `_slot_hover_alpha_base` + `_recent_row_hover_alpha_base` 两个 Dictionary 跨面板 base 字典 0 互混」— T226 (#145) + T231 (#151) 跨面板 hover +0.1 boost 走 dict 读 base, `_slot_hover_alpha_base` 存 slot→base_alpha (T222 lerp 0.5→0.2) + `_recent_row_hover_alpha_base` 存 row→base_alpha (T219 5 行 1.0/0.875/0.75/0.625/0.5), 两个 dict 各自独立, 任何"统一 base 字典"polish 都会破坏跨面板 hover 反馈源 (T226 locked slot 0.5/0.2 + T231 recent row 1.0/0.875/0.75/0.625/0.5 各自不同 base)。
- **触发场景**：T222 (#144) + T219 (#141) + T226 (#145) AchievementGrid locked slot 解锁进度 + ProfileRecentList 5 行 alpha 渐变 + 跨面板 base 字典落地需同步 7 处：(1) `src/scripts/pause_menu.gd:1065-1067` `const _ACHV_LOCKED_ALPHA_START := 0.5` + `const _ACHV_LOCKED_ALPHA_END := 0.2` + `const _ACHV_LOCKED_COLOR_RGB := Color(0.25, 0.25, 0.3)` (T222 三个 const 0 触碰, 0.2 终点 + 暗灰 RGB 锁定)；(2) `src/scripts/pause_menu.gd:642-643` `const _RECENT_ROW_ALPHA_MAX := 1.0` + `const _RECENT_ROW_ALPHA_MIN := 0.5` (T219 两个 const 0 触碰, 5 步等差梯度 base 边界锁定)；(3) `src/scripts/pause_menu.gd:707` `var _slot_hover_alpha_base: Dictionary = {}` + 1324/1329 行 `_slot_hover_alpha_base[child] = 1.0` (unlocked) / `= locked_alpha` (locked lerp) (T222 末尾 base 存 dict, T226 读 dict hover +0.1 boost 用)；(4) `src/scripts/pause_menu.gd:720` `var _recent_row_hover_alpha_base: Dictionary = {}` + 1916 行 `_recent_row_hover_alpha_base[i] = row_alpha` (T219 末尾 5 行 base 存 dict, T231 读 dict hover +0.1 boost 用)；(5) `src/scripts/pause_menu.gd:1303-1329` `_refresh_achievement_grid` 末尾 unlocked → `Color.WHITE` + `_slot_hover_alpha_base[child] = 1.0` + locked → `_ACHV_LOCKED_COLOR_RGB` + alpha=locked_alpha + `_slot_hover_alpha_base[child] = locked_alpha` 3 段联动 (T222 联动 T226, 1 漏 1 = T226 hover 0 base / 0 hover boost)；(6) `src/scripts/pause_menu.gd:1907-1916` `_refresh_recent_runs_list` 末尾 `alpha_step = (_RECENT_ROW_ALPHA_MAX - _RECENT_ROW_ALPHA_MIN) / float(_PROFILE_RECENT_RUNS_MAX - 1)` 步长公式 + `row_alpha = _RECENT_ROW_ALPHA_MAX - float(i) * alpha_step` 5 行 linear + `row_lbl.modulate = Color(1.0, 1.0, 1.0, row_alpha)` 应用 + `_recent_row_hover_alpha_base[i] = row_alpha` 存 dict (T219 联动 T231, 1 漏 1 = T231 hover 0 base / 0 hover boost)；(7) `src/scripts/pause_menu.gd:1249-1301` `_on_slot_hover_in` / `_on_slot_hover_out` 走 `var base_alpha: float = 1.0` (defensive) + `if _slot_hover_alpha_base.has(slot): base_alpha = _slot_hover_alpha_base[slot]` 读 dict (T226 跨面板 hover 反馈 base 字典读, 0 base = unlocked 1.0 hardcoded fallback)。7 处 1:1 同步，1 漏 1 = 解锁进度 alpha 错位 / 5 行 row 渐变错位 / 跨面板 hover feedback 0 base 报错。
- **修复**：`src/scripts/pause_menu.gd:1065-1067 + 642-643 + 707 + 720 + 1303-1329 + 1907-1916 + 1249-1301` 跨面板 alpha 渐变 + 解锁进度联动 + 跨面板 base 字典三件套采用「3 const 锁定 (T222 0.5/0.2/0.25,0.25,0.3 + T219 1.0/0.5) + 2 var 跨面板 base dict (_slot_hover_alpha_base + _recent_row_hover_alpha_base) + 1 _refresh 末尾 base 存 dict (T222 1303-1329 14 slot lerp + T219 1907-1916 5 行 linear) + 1 hover handler 读 dict (T226 1249-1301 + T231 1957-2030)」4 件套：
  ```gdscript
  # src/scripts/pause_menu.gd:1065-1067 — T222 解锁进度 alpha lerp 3 const 锁定
  const _ACHV_LOCKED_ALPHA_START := 0.5
  const _ACHV_LOCKED_ALPHA_END := 0.2  # 0.2 终点避免 fade 到底 0 透明让玩家以为"成就消失"
  const _ACHV_LOCKED_COLOR_RGB := Color(0.25, 0.25, 0.3)  # 暗灰调, 只改 alpha 0 改 RGB
  
  # src/scripts/pause_menu.gd:642-643 — T219 5 行 row alpha 渐变 2 const 锁定
  const _RECENT_ROW_ALPHA_MAX := 1.0  # i==0 满亮
  const _RECENT_ROW_ALPHA_MIN := 0.5  # i==4 50% 暗, 5 步等差步长 0.125
  
  # src/scripts/pause_menu.gd:707 + 720 — 跨面板 2 base dict 0 互混
  var _slot_hover_alpha_base: Dictionary = {}  # slot → base_alpha (T222 lerp 0.5→0.2)
  var _recent_row_hover_alpha_base: Dictionary = {}  # row → base_alpha (T219 5 行 1.0→0.5)
  
  # src/scripts/pause_menu.gd:1303-1329 — T222 + T226 跨 _refresh_achievement_grid 联动
  func _refresh_achievement_grid() -> void:
      var unlocked_count: int = PlayerStats.get_unlocked_count()
      var total_count: int = PlayerStats.get_total_count()
      var progress: float = 0.0
      if total_count > 0:
          progress = float(unlocked_count) / float(total_count)
      var locked_alpha: float = lerp(_ACHV_LOCKED_ALPHA_START, _ACHV_LOCKED_ALPHA_END, progress)
      var locked_color: Color = Color(_ACHV_LOCKED_COLOR_RGB.r, _ACHV_LOCKED_COLOR_RGB.g, _ACHV_LOCKED_COLOR_RGB.b, locked_alpha)
      for child in _achv_grid.get_children():
          if not child.name.begins_with("AchvSlot_"):
              continue
          var id_val: String = child.name.substr(9)
          if PlayerStats.is_unlocked(id_val):
              child.modulate = Color.WHITE
              child.self_modulate = Color.WHITE
              _slot_hover_alpha_base[child] = 1.0  # T226 unlocked base 1.0
          else:
              child.modulate = locked_color
              child.self_modulate = locked_color
              _slot_hover_alpha_base[child] = locked_alpha  # T226 locked base = T222 lerp
  
  # src/scripts/pause_menu.gd:1907-1916 — T219 + T231 跨 _refresh_recent_runs_list 联动
  var alpha_step: float = (_RECENT_ROW_ALPHA_MAX - _RECENT_ROW_ALPHA_MIN) / float(_PROFILE_RECENT_RUNS_MAX - 1)
  var row_alpha: float = _RECENT_ROW_ALPHA_MAX - float(i) * alpha_step
  row_lbl.modulate = Color(1.0, 1.0, 1.0, row_alpha)
  _recent_row_hover_alpha_base[i] = row_alpha  # T231 row base 5 行 dict
  
  # src/scripts/pause_menu.gd:1249-1273 — T226 跨 _on_slot_hover_in 读 dict 模式
  func _on_slot_hover_in(slot: TextureRect) -> void:
      if not is_instance_valid(slot):
          return
      var base_alpha: float = 1.0  # defensive fallback (unlocked 1.0 / locked 0.5-0.2)
      if _slot_hover_alpha_base.has(slot):
          base_alpha = _slot_hover_alpha_base[slot]
      var boosted_alpha: float = clampf(base_alpha + _SLOT_HOVER_BRIGHT_ALPHA_BOOST, 0.0, 1.0)
      # ... 1.5x 放大 + 暖色 modulate + alpha boost 0.12s 渐变 (T226 + T111 升级)
  ```
  - 3 const 锁定 — T222 `_ACHV_LOCKED_ALPHA_START/END` (0.5/0.2) + `_ACHV_LOCKED_COLOR_RGB` (0.25, 0.25, 0.3) + T219 `_RECENT_ROW_ALPHA_MAX/MIN` (1.0/0.5) 5 const 各自独立, 0 互混, source-grep 验证 5 const 0 漏 0 改 1 字符。
  - 2 var 跨面板 base dict 0 互混 — `_slot_hover_alpha_base` (slot → base_alpha, 0.5/0.2 lerp 跟随解锁进度) + `_recent_row_hover_alpha_base` (row → base_alpha, 1.0/0.875/0.75/0.625/0.5 5 步等差), 两个 dict 各自独立 key (TextureRect slot vs int row idx), value 类型一致 (float) 但语义不同。
  - 0.2 终点 (T222 关键设计) — 0 选 0.0 避免 fade 到底 0 透明让玩家以为"成就消失", 0.2 仍可见 0 抢眼, 玩家视觉组 "完成态 = locked slot 仍可见 暗灰淡出" 一致。
  - 暗灰 RGB 锁定 (T222 关键设计) — `_ACHV_LOCKED_COLOR_RGB := Color(0.25, 0.25, 0.3)` 暗灰调, 只改 alpha 通道 (locked_alpha), 0 改 RGB, 玩家视觉组 "locked slot 暗灰 = 仍未解锁" 一致。
  - 5 步等差步长 0.125 (T219 关键设计) — `alpha_step = (_RECENT_ROW_ALPHA_MAX - _RECENT_ROW_ALPHA_MIN) / float(_PROFILE_RECENT_RUNS_MAX - 1) = 0.5/4 = 0.125` 严格 5 步等差, 任何 "改 _PROFILE_RECENT_RUNS_MAX 6/3 行" polish 必须同步更新步长公式, 0 触碰边界 const。
  - defensive fallback 1.0 (T226 关键防御) — `_on_slot_hover_in` 走 `var base_alpha: float = 1.0` (defensive: _refresh 之前 _on_slot_hover_in 0 越界读 dict fallback), 避免 polish 期 _refresh 顺序错位 (hover handler 先 fire 后 _refresh 跑) 触发 `KeyError` 抛错。
- **预防**：
  1. 任何 polish 期给 AchievementGrid 14 slot 加解锁进度 alpha 联动 或 给 ProfileRecentList 5 行 row 加 alpha 渐变时**必须**严格按 7 步骤 1:1 复制既有模式：(1) 3 const 锁定 (T222 _ACHV_LOCKED_ALPHA_START/END/_ACHV_LOCKED_COLOR_RGB + T219 _RECENT_ROW_ALPHA_MAX/MIN) + (2) 2 var 跨面板 base dict (_slot_hover_alpha_base + _recent_row_hover_alpha_base) + (3) _refresh_achievement_grid 末尾 unlocked/locked 2 path base 存 dict + (4) _refresh_recent_runs_list 末尾 5 行 row base 存 dict + (5) alpha_step 步长公式 (1.0-0.5) / (5-1) = 0.125 严格 5 步等差 + (6) Color(...).a = locked_alpha 改 alpha 0 改 RGB + (7) hover handler 读 dict (T226 _on_slot_hover_in/out 7 处 + T231 _on_recent_row_hover_in/out 4 处), 0 改 3 const 0 改步长公式 0 改 defensive fallback 1.0。
  2. `_ACHV_LOCKED_ALPHA_END := 0.2` 0 触碰 — 0 改 0.0 让 fade 到底 0 透明 (破坏 "完成态 = locked slot 仍可见" 视觉组), 0 改 0.4 让 fade 不明显 (破坏 "完成态 = locked slot 暗灰淡出" 视觉组), source-grep 验证 `_ACHV_LOCKED_ALPHA_END` 0 改 1 字符。
  3. `_ACHV_LOCKED_COLOR_RGB := Color(0.25, 0.25, 0.3)` 0 改 RGB — 0 改 RGB 通道让 locked slot 更亮 (破坏 "locked slot 暗灰 = 仍未解锁" 视觉组), 0 改 alpha 通道 (locked_alpha 0 改), 任何 "RGB 改 1 通道" polish 0 允许, source-grep 验证 `_ACHV_LOCKED_COLOR_RGB` 0 改 1 字符。
  4. 5 步等差步长 0.125 + `_PROFILE_RECENT_RUNS_MAX` 0 触碰 — `alpha_step = (_RECENT_ROW_ALPHA_MAX - _RECENT_ROW_ALPHA_MIN) / float(_PROFILE_RECENT_RUNS_MAX - 1)` 严格 5 步等差公式 0 改, 任何 "扩 6 行 / 缩 3 行" polish 必须**先**改 `_PROFILE_RECENT_RUNS_MAX` const 0 改步长公式 0 改边界 1.0/0.5, source-grep 验证 3 const (`_PROFILE_RECENT_RUNS_MAX` + `_RECENT_ROW_ALPHA_MAX` + `_RECENT_ROW_ALPHA_MIN`) + 步长公式 0 漏 0 改。
  5. 跨面板 2 base dict 0 互混 — `_slot_hover_alpha_base` (TextureRect slot → float) + `_recent_row_hover_alpha_base` (int row idx → float) 各自独立 key 类型 + value 语义, 任何"统一 base 字典"polish 必须先 source-grep 验证 2 dict 0 互混 (T226 slot 0.5/0.2 lerp 跟随解锁进度 vs T231 row 1.0/0.875/0.75/0.625/0.5 5 步等差), 0 触碰。
  6. defensive fallback 1.0 (T226 关键防御) **必须**保持 — `var base_alpha: float = 1.0` (defensive: _refresh 之前 _on_slot_hover_in 0 越界读 dict fallback) 0 删, 任何"简化"polish (e.g. 删 fallback 走 if/else) 必须先 source-grep 验证 polish 期 _refresh 顺序 0 错位 (hover handler 先 fire 后 _refresh 跑) 0 触发 `KeyError`。
  7. T226 `_SLOT_HOVER_FADE_DURATION := 0.12` (T226 AchievementGrid slot 紧凑节奏) + T231 `_RECENT_ROW_HOVER_FADE_DURATION := 0.12` (T231 ProfileRecentList 5 行紧凑节奏) + T225 `_QUICK_STATS_HOVER_FADE_DURATION := 0.3` (T225 ProfileQuickStats 4 段主面板慢节奏) 三种跨面板 hover 节奏 **必须**保持各自独立, 0 互混, 0 统一为 0.12s 或 0.3s, source-grep 验证 3 const 0 触碰 (与 §9.6.8 预防 5 跨面板节奏分层 100% 闭环)。

### 9.6.10 「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 polish 模式 (T212 #132 + T213 #133 + T215 #136 + T216 #137 + T217 #138 落地)

> §9.6.1 记录 6 verb VFX 跨类 handler 双守卫；§9.6.2 记录单个 VFX 5 layer 视觉；§9.6.3 记录 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb；§9.6.4 记录 6 verb 三闭环宪法；§9.6.5 记录 6 verb tooltip 8 行拼接；§9.6.6 记录 ProfileRecentList 7 字段 format 字符串扩展；§9.6.7 记录 ProfileRecentList 三件套 hover feedback (T231 + T240 + T235)；§9.6.8 记录 ProfileQuickStats 4 段独立 hover 联动 (T217 + T225)；§9.6.9 记录 PauseMenu 跨面板 alpha lerp + 5 行渐变 + 2 base dict 双源 (T222 + T219 + T226)。本节记录 PauseMenu 跨面板「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 polish 模式 —— 5 个 polish 任务 (#132 + #133 + #136 + #137 + #138) 跨 4 轮 (T212 #132 + T213 #133 = ProfileQuickStats 4 段 tooltip 静态信息层; T215 #136 = ProfileRecentList 5 行 row 静态高亮层; T216 #137 = ProfileRecentList 5 行 row tooltip 静态信息层; T217 #138 = ProfileQuickStats 4 段独立 hover 动态焦点层) 反复落地的「双层 hover 互补」结构 —— 静态高亮层 (T215 5 行 Color.WHITE snap 立即 + T217 4 段 idx 0.3s 渐变) + tooltip 数据源层 (T213 4 段 `_QUICK_STATS_HINT` + `_build_quick_stats_tooltip` / T216 5 字段 `_RECENT_ROW_HINT` + `_build_recent_row_tooltip`) 双层独立组件，2 个组件都从「权威 const 数据源 + pure builder function」组装，polish 期任何"hover 高亮"或"tooltip 文本"独立扩展时 0 触碰另 1 层。避免未来 polish 任何"hover 高亮层 / tooltip 数据源层 / 双层数据共享"时漏 1 处同步。

- **症状**：polish 期给 PauseMenu 跨面板 (ProfileQuickStats 4 段 + ProfileRecentList 5 行) hover 反馈加新维度 (静态高亮层 / tooltip 数据源层 / 4 段 ↔ 5 字段数据共享 / 第 3 面板接入) 时, 最常见的 fragile 是「(a) 静态高亮层 (T215 / T217) 与 tooltip 数据源层 (T213 / T216) 1:1 配套混用」— T215 5 行 Color.WHITE snap 立即 (旧版路径, 已被 T240 #158 升级为 0.12s tween) + T213 4 段 tooltip 9 行说明是 2 个独立组件, 1 个层加新字段 (e.g. T215 5 行 row 加"最近 1 局"加亮等级) 不需要触碰 T213 4 段 tooltip (4 段 vs 5 行结构不同, 数据语义不同), 任何"统一高亮 + tooltip 1 个组件"polish 都会破坏 1:1 双层独立；「(b) 静态高亮层 T215 5 行 row 状态字段 `_recent_row_hovered` (Array[bool]) + `_recent_row_default_color` (Array[Color]) 漏 1 字段」— 5 行 row 独立 hover 状态必须 2 字段同步 (hovered 标志 + default color 还原色), 任何"5 行共享 1 个 hovered bool"polish 都会破坏 5 行独立 (玩家 hover 第 3 行只提亮第 3 行, 其他 4 行保持原色, 0 跨行联动)；「(c) tooltip 数据源层 T213 `_QUICK_STATS_HINT` (4 entry dict, 每 entry 5 字段 label/color/color_name/desc_zh/detail) + T216 `_RECENT_ROW_HINT` (7 entry dict, 每 entry 3 字段 label/desc_zh/detail) 1 漏 1 字段顺序错位」— _*_HINT const 是 tooltip 文本的权威数据源, 任何"加 1 段 (e.g. QuickStats 加 "总 5 段" 第 5 段) 或加 1 字段 (e.g. RecentList 加 "净/净" 派生率 → 8 字段) 0 改 builder function 体 (for h in _*_HINT 自动遍历) 但 0 触碰 const entry 字段顺序"label / color / color_name / desc_zh / detail" 0 触碰, 1 漏 1 字段 = tooltip 渲染错位；「(d) 双层数据共享 0 触碰 (T213 4 段 vs T216 7 字段) 0 复用」— T213 (4 段 "跨 run 累计") vs T216 (7 字段 "单 run 明细", T249 #167 扩展 5 → 7 字段) 数据语义完全不同 (T213 是"我在第几局" 跨 run 累计, T216 是"这一局我做了啥" 单 run), 0 复用 const (T213 4 段 vs T216 5 字段 vs T249 #167 7 字段扩展 0 改结构), 任何"统一 *_HINT"polish 都会破坏 4 段 / 5 字段 / 7 字段各自独立。
- **触发场景**：T212 (#132) + T213 (#133) + T215 (#136) + T216 (#137) + T217 (#138) 双层 hover 互补 跨面板 落地需同步 7 处：(1) `src/scripts/pause_menu.gd:323-352` `_QUICK_STATS_HINT` 4 entry dict (T213) — 每 entry 5 字段 (label / color / color_name / desc_zh / detail) 0 漏, 4 段顺序 Achievement → BestTime → LongestRoom → Run# 0 乱, 颜色 token 与 _stat_abilities / _profile_abilities 5 verb BBCode 1:1 对齐 (Glass Cyan #69C7CE / Amber Voice #F2B66E / Muted Violet #65506A / Pale Resonance #B7E6DC)；(2) `src/scripts/pause_menu.gd:359-373` `_build_quick_stats_tooltip()` 9 行 pure function (T213) — 1 header + 4 段 4 字段 (label / desc_zh) + 4 段 4 字段 (color / color_name / detail) = 9 行, 走纯文本 + \n 拼接 (Label 节点 bbcode_enabled 0 走 tooltip 路径, 0 改), 0 触碰 for h in _QUICK_STATS_HINT 循环 0 改字典字段访问顺序 (label → desc_zh → color → color_name → detail)；(3) `src/scripts/pause_menu.gd:402-438` `_RECENT_ROW_HINT` 7 entry dict (T216, T249 #167 扩展 5 → 7 entry) — 每 entry 3 字段 (label / desc_zh / detail) 0 漏, 7 字段顺序 Run# → 房 → 净 → 碎 → 时 → 房/时 → 净/时 0 乱 (与 §9.6.6 7 字段 format 字符串 1:1 严格对齐)；(4) `src/scripts/pause_menu.gd:463-473` `_build_recent_row_tooltip()` pure function (T216) — 1 header + 7 字段 2 行 (label/desc_zh + detail) = 1 + 7*2 = 15 行, 与 T213 _build_quick_stats_tooltip 9 行 0 互混 (9 vs 15 行结构不同, 数据语义不同), 0 改字典字段访问顺序 (label → desc_zh → detail), 0 改 "• %s — %s" 格式 (T213 同款, 跨面板 1:1)；(5) `src/scripts/pause_menu.gd:721-732` 双层 hover 互补 5 行 row 状态字段 (T215) — `var _recent_row_hovered: Array = []` (Array[bool] length = _PROFILE_RECENT_RUNS_MAX, 5 行独立 bool flag) + `var _recent_row_default_color: Array = []` (Array[Color] length = row count, 5 行还原色缓存, 避免 hover_out 错把高亮版当 default 写回), 2 字段顺序在 T217 `_quick_stats_hovered_idx: int = -1` 字段之后, _ready 之前声明 (T215 0 触碰 T217 idx 字段, 0 触碰 T214 旧版 bool 字段), 0 跨行联动 (5 行独立 hover_in handler, bind idx 0-4 各自 1 闭包)；(6) `src/scripts/pause_menu.gd:801-803` ProfileQuickStats 4 段总览 hover tooltip 绑定 (T213 + T217) — `_profile_quick_stats.tooltip_text = _build_quick_stats_tooltip()` 1 行 0 改 (T213 数据源 0 改), 绑定到 HBoxContainer parent (T217 拆 4 sub-Label 后 4 sub-Label 共享同一 tooltip, 玩家悬停任 1 sub-Label 都弹同一文本, 0 行为差异)；(7) `src/scripts/pause_menu.gd:805-830` ProfileQuickStats 4 段独立 hover 联动 (T217) — 4 sub-Label (QuickStatsAchievement / QuickStatsBestTime / QuickStatsLongestRoom / QuickStatsRunNumber) 各 mouse_filter = STOP 显式设 (Label 默认 IGNORE, 必须改 STOP 才能触发 mouse_entered, 与 T111 成就 grid TextureRect + T215 5 行 row Label 同模式) + 4 sub-Label mouse_entered.connect(_on_quick_stats_hover_in.bind(0/1/2/3)) (4 段独立 bind idx 0-3 各自 1 闭包, 0 共享 1 个 idx) + mouse_exited.connect(_on_quick_stats_hover_out.bind(0/1/2/3)) 同 1 对。7 处 1:1 同步，1 漏 1 = 双层 hover 反馈错位 / 5 行 row 状态字段缺失 / 4 段独立 hover 联动退化为 1 段。
- **修复**：`src/scripts/pause_menu.gd` 跨面板「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 polish 模式 (T212 + T213 + T215 + T216 + T217 跨 5 任务 4 轮落地)：
  - **T213 (#133) ProfileQuickStats 4 段 tooltip 数据源层** — `const _QUICK_STATS_HINT := [...]` 4 entry dict (Achievement / BestTime / LongestRoom / Run#) + `func _build_quick_stats_tooltip() -> String` 9 行 pure function (1 header + 4 段 4 字段 = 9 行, 0 改字典字段访问顺序) + `tooltip_text = _build_quick_stats_tooltip()` 1 行绑定 (走 HBoxContainer parent 让 4 sub-Label 共享)。与 T199 5 verb tooltip + T216 5 字段 tooltip 模式完全同源 (权威 const 数据源 + pure builder function + tooltip_text 1 行绑定), 0 走 BBCode 包裹 (Label 节点 bbcode_enabled 0 走 tooltip 路径, 0 改), 走纯文本 + \n 拼接, Godot 4.6 自带 tooltip 渲染器 5s timeout。
  - **T215 (#136) ProfileRecentList 5 行 row 静态高亮层** — `var _recent_row_hovered: Array = []` (5 行独立 bool flag) + `var _recent_row_default_color: Array = []` (5 行还原色缓存) + 5 行 row 各自 mouse_filter = STOP 显式设 + 5 行 row mouse_entered.connect(_on_recent_row_hover_in.bind(i)) + 5 行 row mouse_exited.connect(_on_recent_row_hover_out.bind(i)) + 2 handler 内部 越界检查 + re-entrant guard + Color.WHITE 提亮 (旧版 snap 立即, T240 #158 升级 0.12s tween) + restore 到 _recent_row_default_color[idx] (避免误把高亮版当 default 写回) + _refresh_recent_runs_list 起始 `_recent_row_hovered.clear()` + `_recent_row_default_color.clear()` (每次 _refresh 重建 5 行, 数组 resize)。0 跨行联动 (5 行独立 hover_in handler, bind idx 0-4 各自 1 闭包, 玩家 hover 第 3 行只提亮第 3 行, 其他 4 行保持原色, 避免重叠抖动)。
  - **T216 (#137) ProfileRecentList 5 行 row tooltip 数据源层** — `const _RECENT_ROW_HINT := [...]` 7 entry dict (T249 #167 扩展 5 → 7 entry, Run# / 房 / 净 / 碎 / 时 / 房/时 / 净/时) + `func _build_recent_row_tooltip() -> String` 15 行 pure function (1 header + 7 字段 2 行 = 1 + 7*2 = 15 行, for h in _RECENT_ROW_HINT 循环 0 改, 0 hard-code 5 或 7) + T215 (静态高亮层) + T216 (tooltip 数据源层) 双层独立组件, 1 个层加新字段 (e.g. 8 字段 "净/净" 派生率) 不需要触碰另 1 层, source-grep 验证 7 entry dict 字段顺序与 §9.6.6 7 字段 format 字符串 1:1 严格对齐。
  - **T217 (#138) ProfileQuickStats 4 段独立 hover 动态焦点层** — 拆 1 Label 4 BBCode 段 (T214 #134 旧版) → 4 sub-Label (QuickStatsAchievement / QuickStatsBestTime / QuickStatsLongestRoom / QuickStatsRunNumber) + 1 段 idx 状态字段 `_quick_stats_hovered_idx: int = -1` (替代 T214 旧版 bool 1 bit) + 1 对 _on_quick_stats_hover_in / _out handler (idx 越界 0-3 检查 + 4 sub-Label null guard + re-entrant guard + set idx + 调 _apply_quick_stats_hover_state) + 1 个 _apply_quick_stats_hover_state 4 sub-Label modulate 重算函数 (T225 #147 升级 0.3s tween 渐变)。T213 (tooltip 数据源层, 静态信息) + T217 (动态焦点层, 视觉反馈) 双层独立, 1 个层加新维度 (e.g. T217 加 0.3s tween 节奏) 不需要触碰 T213 (9 行 tooltip 文本 0 改), source-grep 验证 4 sub-Label 0 回到 1 Label + 4 BBCode 段旧模式。
  - **T212 (#132) ProfileQuickStats 4 段总览 1 Label 基础** — 旧版 ProfileQuickStats 1 Label + 4 BBCode 段 (T214 #134) + 1 段 (Run#) 单独高亮 string 替换 (T213 tooltip 之前的版本, T214 → T213 → T217 3 段升级前)。0 复用到 T215 (5 行 row) + T216 (7 字段) 双层结构 (T212 1 Label 0 拆 5 行, T212 4 段 0 拆 7 字段), 0 触碰 ProfileRecentList 结构。
  - 双层独立 0 复用 — 静态高亮层 (T215 5 行 + T217 4 段) 0 复用 const 字典 (T215 走 _recent_row_default_color Array[Color] 5 行还原色, T217 走 _quick_stats_hovered_idx 1 段 idx 状态字段), tooltip 数据源层 (T213 4 段 + T216 7 字段) 0 复用 const 字典 (T213 _QUICK_STATS_HINT 4 entry vs T216 _RECENT_ROW_HINT 7 entry 字段数不同 + 字段名不同 + 字段含义不同), 跨面板 0 触碰 0 互混。
  - 0 副作用：T215 / T216 5 行 row 0 触碰 T213 / T217 4 段 QuickStats；T213 / T217 4 段 0 触碰 T215 / T216 5 行 row；T199 5 verb tooltip 0 触碰 T213 / T216 tooltip (权威 const 数据源各自独立, 0 互混)；T225 0.3s 节奏 0 触碰 T231 0.12s 节奏 (跨面板节奏分层, 与 §9.6.8 预防 5 100% 闭环)。
- **预防**：
  1. 任何 polish 期给 PauseMenu 跨面板 (ProfileQuickStats 4 段 / ProfileRecentList 5 行 / save_load_menu 5 槽位 / settings accessibility 三态) hover 反馈加新维度 (静态高亮层 / tooltip 数据源层 / 4 段 ↔ 5 字段数据共享 / 第 3 面板接入) 时**必须**严格按 7 步骤 1:1 复制既有双层 hover 互补模式：(1) tooltip 数据源层 const (T213 `_QUICK_STATS_HINT` 4 entry + T216 `_RECENT_ROW_HINT` 7 entry) + (2) tooltip builder function (T213 `_build_quick_stats_tooltip` 9 行 + T216 `_build_recent_row_tooltip` 15 行) + (3) tooltip_text 1 行绑定 (T213 + T217 绑到 HBoxContainer parent) + (4) 静态高亮层 状态字段 (T215 `_recent_row_hovered` 5 行 Array[bool] + `_recent_row_default_color` 5 行 Array[Color] / T217 `_quick_stats_hovered_idx` 1 段 int) + (5) mouse_filter = STOP 显式设 (5 行各自 / 4 sub-Label 各自) + (6) 5 行各自 mouse_entered/exited.connect handler (T215 bind idx 0-4 / T217 bind idx 0-3) + (7) handler 内部 越界检查 + re-entrant guard + 提亮到 Color.WHITE + restore 到 default color (T215 旧版 snap 立即 / T240 升级 0.12s tween)。建议在 const 数据源上方 docblock 写明 "权威 const 数据源 + pure builder function + tooltip_text 1 行绑定" 三件套硬约束。
  2. T213 `_QUICK_STATS_HINT` 4 entry + T216 `_RECENT_ROW_HINT` 7 entry 0 复用 0 互混 — 4 段 vs 5 字段 vs 7 字段 字段数不同 + 字段名不同 (T213 4 段 label "成就 / 最佳 / 最长单房 / Run#", T216 7 字段 label "Run# / 房 / 净 / 碎 / 时 / 房/时 / 净/时") + 字段含义不同 (T213 4 段 "跨 run 累计", T216 7 字段 "单 run 明细"), 任何"统一 *_HINT"polish 必须先 source-grep 验证 2 const 字段数 0 触碰 0 复用, 0 互混。`_QUICK_STATS_HINT` 4 entry 0 改 `_RECENT_ROW_HINT` 7 entry 字段, 反之亦然, source-grep 验证 2 const 0 漏 0 改 1 字段。
  3. T215 5 行 row 双字段 (hovered bool + default color Array[Color]) 0 改任 1 字段 — `_recent_row_hovered: Array = []` 5 行独立 bool flag 0 改 "_recent_row_hovered: bool" 单字段 (破坏 5 行独立, 0 跨行联动), `_recent_row_default_color: Array = []` 5 行还原色缓存 0 改 "_recent_row_default_color: Color" 单字段 (破坏 5 行独立 hover_out restore 路径), source-grep 验证 2 var 0 漏 0 改 1 字段。
  4. T217 1 段 idx 状态字段 (`_quick_stats_hovered_idx: int = -1`) 0 改 T214 旧版 bool 字段 — T214 (`_quick_stats_hovered: bool` 1 bit, 0 表达 "hover 哪一段") 0 改 T217 (1 段 idx 0-3 表达精确段号), source-grep 验证 `_quick_stats_hovered_idx` 0 改 1 字符。
  5. T215 5 行 Color.WHITE 提亮 base 0 改 — 0 改 Color.WHITE (1.0) 0 改 _COLOR_RECENT_RUN_LATEST (Amber Voice i==0 还原色) + _COLOR_RECENT_RUN_NORMAL (Pale Resonance i>0 还原色) (T162 5 行还原色 const), T215 复用 2 const 作为 5 行 default_color 缓存, source-grep 验证 2 const 0 漏 0 改 1 字符。
  6. tooltip 走纯文本 + \n 拼接, 0 走 BBCode 包裹 — Label 节点 bbcode_enabled 0 走 tooltip 路径 (tooltip_text 是 Godot 4.6 自带 Popup 渲染器, 0 触发 bbcode_enabled), 任何"BBCode 包裹"polish 0 触发 (e.g. 加 `[color=#FFFFFF]%s[/color]` 颜色 token 0 渲染, 玩家看到纯文本 0 颜色)。0 改 for h in _*_HINT 循环 + 0 改字典字段访问顺序 (label → desc_zh / color → color_name → detail), source-grep 验证 builder function 0 漏 0 改 1 行。
  7. T215 / T216 双层 0 触碰 / T213 / T217 双层 0 触碰 — T215 (5 行 row 静态高亮层) 0 触碰 T216 (5 字段 tooltip 数据源层) 反之亦然; T213 (4 段 tooltip 数据源层) 0 触碰 T217 (4 段独立 hover 动态焦点层) 反之亦然。任何"双层合并"polish 必须先 source-grep 验证 2 层独立组件 0 互混 0 复用 0 字段共享, source-grep 验证 2 const (`_QUICK_STATS_HINT` + `_RECENT_ROW_HINT`) + 4 var (`_recent_row_hovered` + `_recent_row_default_color` + `_quick_stats_hovered_idx` + `_quick_stats_default_text` 旧版已废弃) 0 漏 0 改 1 字段。

### 9.6.11 settings_menu ReduceAllCheck 三态 (enabled / disabled / indeterminate) 总开关 + 3 子项 reduce_*_check 联动 + 防递归 `_syncing_from_master` 守卫 polish 模式 (T202.B #121 落地)

> §9.6.1 ~ §9.6.10 集中记录 PauseMenu 跨面板 hover 反馈 / VFX 跨类 / HUD 6 verb 接入 / 调色六元组等 polish 模式。本节记录 settings_menu accessibility 总开关 ReduceAllCheck 跨 1 主 CheckBox + 3 子 CheckBox (ReduceShake / ReduceFlash / ReduceVibration) 联动 + Godot 4 CheckBox 三态 (checked / unchecked / indeterminate) 显示 + 主子递归防爆栈的 polish 模式 —— T202.B (#121) 1 任务 1 轮落地的「3 子项独立 toggle + 主开关聚合 + 防递归 `_syncing_from_master` 守卫 + `_reduce_all` 数据字段独立于 3 子项 AND 算出 (主开关最后一次手动 toggle 状态保留, indeterminate 仅是 UI 反映, 0 触碰数据) + `set_block_signals` 双层保险 + 3 子项灰化 (主开关 enabled 时 3 子项 disabled 不可点)」6 件套硬约束录入 CONTRIBUTING.md §9.6.11。避免未来 polish 任何「主开关 + N 子项 CheckBox 联动 + 防递归」场景 (e.g. 玩家偏好 3 选项 一键全开 / 关 + 子项可独立 override) 时漏 1 处同步 (e.g. 漏 `_syncing_from_master` 守卫 → 无限递归栈溢出; 漏 `_reduce_all` 独立字段 → indeterminate 状态被 3 子项 AND 算出歧义; 漏 `set_block_signals` → 3 子项 signal 链路反向触发主开关同步; 漏 3 子项灰化 → 玩家在主开关 enabled 状态下还点 3 子项 → 视觉组 "主开关是权威源" 语义被破坏)。

- **症状**：polish 期给 settings_menu 加「1 主 CheckBox + N 子 CheckBox 联动」交互时, 最常见的 fragile 是「(a) 主开关推 N 子项 → N 子项又触发 _on_*_toggled → _sync_*_state 误回写主开关 → 再次 set 主开关 → 无限递归栈溢出」— T202.B (#121) 落地时设计 3 层防递归 (1 `_syncing_from_master` bool 守卫 在主开关 handler 入口开出口关 + 2 `set_block_signals(true)` 阻断单个 signal + 3 `if not _syncing_from_master: _sync_reduce_all_state()` 短时间窗口短路), 任何「漏 1 层防递归」polish 都会触发无限递归;「(b) `_reduce_all` 数据字段被 3 子项 AND 算出, indeterminate 状态写回数据字段歧义」— T202.B 落地时显式用 `_reduce_all: bool = false` 独立存储「主开关最后一次手动 toggle 状态」, 不根据 3 子项 AND 算出, 让 indeterminate 仅是 UI 反映 (Godot 4 `CheckBox.indeterminate` 独立 flag, 0 触碰 button_pressed);「(c) 主开关 enabled 时 3 子项还可点, 视觉组 "主开关是权威源" 语义被破坏」— T202.B 落地时 _set_three_children_disabled(true) 灰化 3 子项 (玩家点不到), 主开关 disabled 时 _set_three_children_disabled(false) 重新启用, 任何 "主开关 enabled 时 3 子项还可点" polish 都会让玩家在「总开关已开」状态下点子项触发 _on_reduce_*_toggled, 进入 indeterminate 状态, 但玩家视觉上看到「总开关还在 checked, 我手点 1 个子项」歧义;「(d) 3 子项独立 toggle 后 indeterminate 显示状态丢失」— T202.B 落地时 _on_reduce_*_toggled 末尾 `if not _syncing_from_master: _sync_reduce_all_state()`, 玩家手动 toggle 任 1 子项时回查 3 子项状态, 全 true / 全 false / 混合 → 主开关 checked / unchecked / indeterminate (Godot 4 `indeterminate = true` flag), 任何"漏 _sync_reduce_all_state() 回查"polish 都会让 indeterminate 状态永远 0 显示。
- **触发场景**：T202.B (#121) ReduceAllCheck 三态总开关落地需同步 6 件套：(1) `src/scripts/settings_menu.gd:80` `@onready var _reduce_all_check: CheckBox` — 1 个主 CheckBox 引用, 与 T195 / T196 3 个 _reduce_*_check 引用同区域, 顺序在 3 子项之后 (`@onready var _reduce_shake_check: CheckBox = $VBoxContainer/Content/VideoPanel/ReduceShakeCheck` 在 67 行 + `_reduce_flash_check` 在 68 行 + `_reduce_vibration_check` 在 72 行 + `_reduce_all_check` 在 80 行, 严格 "3 子项 → 1 主项" 顺序); (2) `src/scripts/settings_menu.gd:173` `var _reduce_all: bool = false` + 3 个 `_reduced_shake / _reduced_flash / _reduced_vibration` 字段 (156-164 行) — 4 个数据字段, 3 个是 3 子项 toggle 状态, 1 个 `_reduce_all` 是主开关数据 (独立于 3 子项 AND 算出, indeterminate 状态 0 触碰 `_reduce_all`); (3) `src/scripts/settings_menu.gd:179` `var _syncing_from_master: bool = false` — 1 个递归守卫 bool 字段, 入口 `_on_reduce_all_toggled` 头部 `= true` + 出口 `= false` 包裹, 期间 3 子项 `_on_reduce_*_toggled` 末尾 `if not _syncing_from_master: _sync_reduce_all_state()` 短路; (4) `src/scripts/settings_menu.gd:234` `_reduce_all_check.toggled.connect(_on_reduce_all_toggled)` — 1 行 signal 连接, 0 触碰 3 子项 `_on_reduce_*_toggled` (3 子项在 225-229 行已分别连); (5) `src/scripts/settings_menu.gd:636-655` `_on_reduce_all_toggled(enabled: bool)` — 6 步骤主 handler: 1 `_syncing_from_master = true` 入口守卫 → 2 `_apply_three_children(enabled)` 推 3 子项 (set_block_signals + 推 _reduced_* 字段 + live-push ScreenShake autoload) → 3 `_set_three_children_disabled(enabled)` 灰化 3 子项 (主开关 enabled 时 3 子项 disabled) → 4 `_reduce_all = enabled` 数据同步 → 5 `_syncing_from_master = false` 出口守卫 → 6 indeterminate 处理 (Godot 4 默认 indeterminate → checked 行为已内置); (6) `src/scripts/settings_menu.gd:704-729` `_sync_reduce_all_state()` — 3 态判定 (全 true → checked, 全 false → unchecked, 混合 → indeterminate) + `set_block_signals(true/false)` 包裹 + `_reduce_all_check.indeterminate = true/false` flag 写 + `_set_three_children_disabled(false)` 重新启用 3 子项 (主开关 indeterminate 状态时 3 子项可点 — 让玩家 override)。6 件套 1:1 同步, 1 漏 1 = 无限递归栈溢出 / indeterminate 显示歧义 / 视觉组 "主开关是权威源" 语义破坏 / 3 子项独立 toggle 反馈丢失。
- **修复**：`src/scripts/settings_menu.gd` accessibility ReduceAllCheck 三态总开关 + 3 子项 reduce_*_check 联动 + 防递归 `_syncing_from_master` 守卫 polish 模式 (T202.B #121 落地)：
  - **T195 / T196 / T202.B 4 个 `_reduced_*` 数据字段 + 1 个 `_reduce_all` 独立数据字段 (6 字段)** — `_reduced_shake: bool = false` (156) + `_reduced_flash: bool = false` (157) + `_reduced_vibration: bool = false` (164) + `_reduce_all: bool = false` (173) 4 个 bool 数据字段, 3 子项 + 1 主项 1:1 映射, 0 共享 (与 §9.6.4 6 verb 调色六元组 0 复用 0 互混 同模式, 1 字段 = 1 状态源, 0 从其他字段 AND / OR 算出)。`_reduce_all` 独立存储「主开关最后一次手动 toggle 状态」, 不根据 3 子项 AND 算出 — 这样 "indeterminate 表示历史曾是全 enable, 现在 3 子项被打破" 语义透明, 玩家点 indeterminate 主开关时 (Godot 4 默认行为: indeterminate → checked = true) → reset `_reduce_all=true` 并推 3 子项 → 进入正常 master sync 流程。
  - **T202.B 1 个 `_syncing_from_master` 递归守卫 bool 字段** — `var _syncing_from_master: bool = false` (179), 主开关 handler 入口 `_syncing_from_master = true` → 出口 `_syncing_from_master = false` 包裹, 期间 3 子项 `_on_reduce_*_toggled` 末尾 `if not _syncing_from_master: _sync_reduce_all_state()` 短路 — 防主开关推 3 子项 → 3 子项又触发 _on_reduce_*_toggled → _sync_reduce_all_state 误判主开关 → 再次 set 主开关 → 无限递归栈溢出。`set_block_signals(true/false)` 阻断单个 signal 是双层保险, 但 已有 `_on_reduce_*_toggled` 流程仍要推 ScreenShake.autoload 走完 (live-push 立即生效), 所以 `_syncing_from_master` 标志是真正的递归保险, `set_block_signals` 是辅助。`_on_reduce_all_toggled` 6 步骤 顺序硬约束: 1 入口守卫 → 2 推 3 子项 → 3 灰化 3 子项 → 4 数据同步 → 5 出口守卫 (顺序不能乱, 否则 `_syncing_from_master = false` 提前关闭, 3 子项 signal 链路反向触发主开关同步)。
  - **T202.B `_apply_three_children(enabled: bool)` helper (661-682 行)** — 主开关推 3 子项统一入口, 每个子项 4 步: 1 `set_block_signals(true)` 阻断 signal 链路 (双层保险第 1 层) → 2 `button_pressed = enabled` 设可视状态 → 3 `set_block_signals(false)` 恢复 signal 链路 → 4 推 `_reduced_*` 数据字段 + live-push ScreenShake autoload (与玩家手动 toggle 完全一致)。0 触碰 3 个 `_on_reduce_*_toggled` handler (信号被 block, 不会被调), 但 `_syncing_from_master` 守卫仍生效, 防万一 (e.g. 未来 signal 链路重排, set_block_signals 失效)。
  - **T202.B `_set_three_children_disabled(disabled: bool)` helper (688-694 行)** — 灰化 3 子项统一入口, 主开关 enabled 时 3 子项 `disabled = true` (灰色, 玩家点不到), 主开关 disabled 时 3 子项 `disabled = false` (重新启用)。注意 disabled 状态本身不阻断 `_on_reduce_*_toggled` 程序触发, 仅挡玩家点击 — 程序递归仍需 `_syncing_from_master` 守卫挡。3 子项独立 CheckBox 引用都先 null guard (`if _reduce_shake_check:`), 与 §9.6.10 4 sub-Label null guard 同模式。
  - **T202.B `_sync_reduce_all_state()` 3 态判定 (704-729 行)** — 3 子项状态回查: 1 全 true → 主开关 `button_pressed = true` + `indeterminate = false` + `_set_three_children_disabled(false)` 重新启用 3 子项 → 2 全 false → 主开关 `button_pressed = false` + `indeterminate = false` + `_set_three_children_disabled(false)` → 3 混合 (1-2 个 true) → 主开关 `indeterminate = true` + `_set_three_children_disabled(false)` (主开关 indeterminate 状态时 3 子项可点 — 让玩家 override)。`set_block_signals(true/false)` 包裹整个判定块, 防 `_reduce_all_check.button_pressed = true` 触发 `_on_reduce_all_toggled` 递归。
  - **T202.B `_on_reduce_all_toggled(enabled: bool)` 6 步骤主 handler (636-655 行)** — 6 步骤顺序硬约束: 1 `_syncing_from_master = true` 入口守卫 → 2 `_apply_three_children(enabled)` 推 3 子项 → 3 `_set_three_children_disabled(enabled)` 灰化 3 子项 → 4 `_reduce_all = enabled` 数据同步 → 5 `_syncing_from_master = false` 出口守卫 → 6 indeterminate 处理注释 (Godot 4 默认 indeterminate → checked 已内置)。1 步骤漏 1 = 无限递归栈溢出 / 3 子项 0 推 / 3 子项 0 灰化 / 数据字段 0 同步 / 出口守卫提前关 / indeterminate 行为无注释。
  - 0 副作用：T195 / T196 reduce_shake / reduce_flash / reduce_vibration 3 个独立 CheckBox 0 触碰 (3 个 `_on_reduce_*_toggled` handler 仅末尾加 `if not _syncing_from_master: _sync_reduce_all_state()` 1 行短路守卫, 0 改 `_reduced_*` 字段 + ScreenShake autoload 推 逻辑); `_apply_three_children` 内 live-push ScreenShake 与玩家手动 toggle 1:1 一致 (调 `_on_reduce_*_toggled` 等价逻辑), 0 副作用 0 重复; `_reduce_all` 数据字段 0 从 3 子项 AND 算出, 0 触碰 3 子项数据; `_syncing_from_master` 守卫仅在 `_on_reduce_all_toggled` 入口开出口关, 0 触碰 3 子项 `_on_reduce_*_toggled` 主体 (仅末尾加 if 守卫)。
- **预防**：
  1. 任何 polish 期给 settings_menu 加「1 主 CheckBox + N 子 CheckBox 联动」交互时**必须**严格按 6 件套 1:1 复制既有 ReduceAllCheck 三态模式: (1) `@onready var _main_check: CheckBox` 主 CheckBox 引用 + (2) 4 数据字段 (N 子项 bool + 1 主项 `_main` bool 独立) + (3) `var _syncing_from_master: bool = false` 递归守卫 (入口开出口关) + (4) `signal connect` 主开关 toggle (1 行) + (5) `_on_main_toggled` 6 步骤主 handler (入口守卫 → 推 N 子项 → 灰化 N 子项 → 数据同步 → 出口守卫 → indeterminate 处理注释) + (6) `_sync_main_state()` 3 态判定 (全 true → checked, 全 false → unchecked, 混合 → indeterminate) + `set_block_signals` 包裹 + N 子项重新启用 (主开关 indeterminate 状态时让玩家 override)。建议在主 handler 上方 docblock 写明 "1 主 + N 子 + 防递归 `_syncing_from_master` 守卫 + `_main` 数据独立 + `set_block_signals` 双层保险 + N 子项灰化" 六件套硬约束。
  2. `_syncing_from_master` 递归守卫 0 改任 1 字符 — `var _syncing_from_master: bool = false` 0 改 `var _syncing: bool = false` (字段名重命名会破坏 _on_reduce_*_toggled 内 `if not _syncing_from_master` 短路守卫), source-grep 验证 `_syncing_from_master` 0 漏 0 改 1 字符。3 层防递归 0 删任 1 层: 1 `_syncing_from_master` 守卫 (主体) + 2 `set_block_signals(true/false)` 包裹 (双层保险) + 3 `if not _syncing_from_master: _sync_reduce_all_state()` 短时间窗口短路 (3 子项 handler 末尾)。
  3. `_reduce_all` 数据字段独立于 3 子项 AND 算出 0 触碰 — `var _reduce_all: bool = false` 0 改 `var _reduce_all: bool = _reduced_shake and _reduced_flash and _reduced_vibration` (3 子项 AND 算出会让 indeterminate 状态被覆盖, 玩家点 indeterminate 主开关 → toggle 推 3 子项 → indeterminate 被 3 子项 AND 重写 → 主开关变 checked, 歧义), source-grep 验证 `_reduce_all = enabled` 0 漏 0 改 1 字符 (数据同步固定 `= enabled`, 0 是 `_reduced_shake and ...`)。indeterminate 仅是 UI 反映 (Godot 4 `CheckBox.indeterminate` 独立 flag), 0 触碰 `_reduce_all` 数据字段。
  4. `_set_three_children_disabled` 灰化 0 改 `_set_three_children_disabled(true/false)` — 3 子项独立 CheckBox 引用都先 null guard (`if _reduce_shake_check:`), 与 §9.6.10 4 sub-Label null guard 同模式。`disabled = disabled` 0 改 `disabled = not enabled` (与 `_apply_three_children(enabled)` 入口参数相反, 0 触碰 视觉组 "主开关 enabled 时 3 子项 disabled" 语义), source-grep 验证 3 子项 `disabled = disabled` 0 漏 0 改 1 字符。
  5. 3 子项 `_on_reduce_*_toggled` 末尾 `if not _syncing_from_master: _sync_reduce_all_state()` 短路守卫 0 删 — 删了会触发无限递归 (主开关推 3 子项 → 3 子项又触发 _on_reduce_*_toggled → _sync_reduce_all_state 误回写主开关 → 再次 set 主开关 → 无限递归栈溢出), source-grep 验证 3 子项 handler 末尾 `if not _syncing_from_master` 0 漏 0 改 1 行。
  6. `_sync_reduce_all_state` 3 态判定顺序 0 改 — 1 全 true → checked + 2 全 false → unchecked + 3 混合 → indeterminate (顺序硬约束, 全 true / 全 false 在前 2 个 if 分支, 混合在 else 分支), source-grep 验证 3 个分支 0 漏 0 改 1 行。`set_block_signals(true/false)` 包裹整个判定块 0 删 (删了会让 `_reduce_all_check.button_pressed = true` 触发 `_on_reduce_all_toggled` 递归)。
  7. T202.B 6 件套 0 触碰既有 3 子项 reduce_*_check 逻辑 (T195 / T196 reduce_shake / reduce_flash / reduce_vibration 3 个独立 CheckBox 主体逻辑 0 改, 仅末尾加 1 行 `if not _syncing_from_master` 守卫) — T202.B 是新增 1 主 CheckBox + 1 数据字段 + 1 守卫 + 1 主 handler + 2 helper + 1 sync 函数, 0 触碰既有 3 子项 live-push ScreenShake autoload 逻辑, source-grep 验证 `ScreenShake.set_reduce_shake / set_reduce_flash / set_reduce_vibration` 3 处调用 0 漏 0 改 1 字符 (3 处调用既出现在 `_on_reduce_*_toggled` 又出现在 `_apply_three_children`, 2 处 1:1 一致, 0 重复)。

### 9.6.12 settings_menu 4 tab 状态机 (AUDIO / VIDEO / CONTROLS / SAVES) + Tab 枚举 + 4 panel mutual-exclusive visible + 4 button modulate 1:1 复制 polish 模式 (T037 + T072 + T134 + T202.B 跨 4 任务 ~20 轮落地) 文档化

> §9.6.1 ~ §9.6.11 集中记录 PauseMenu 跨面板 hover 反馈 / VFX 跨类 / HUD 6 verb 接入 / 调色六元组 / settings_menu accessibility ReduceAllCheck 三态等 polish 模式。本节记录 settings_menu 顶部 4 tab (AUDIO / VIDEO / CONTROLS / SAVES) 跨面板切换的「Tab 枚举 + 1 状态字段 + 4 tab Button @onready + 4 panel Control @onready + 4 signal connect + 1 `_switch_tab(tab)` 函数 (状态字段 + 4 panel mutual-exclusive visible + 4 button modulate 1:1 + 1 可选 side effect)」7 件套硬约束录入 CONTRIBUTING.md §9.6.12。T037 (#23) 落地 2 tab (Audio / Video) → T072 (#34) 加 Saves tab → T086 (#44) 加 Controls tab → T134 (#71) 改 placeholder → T202.B (#121) 改 ReduceAllCheck 三态, 5 个任务跨 ~20 轮反复落地, 任何 polish 期给 settings_menu 加第 5 tab (e.g. 玩家偏好 / 关卡选择 / mod 列表) 或类似 4 tab 状态机的多面板 UI (e.g. 主菜单 / 角色面板 / 商店面板) 时先查本节, 0 漏 1 处同步 (e.g. 漏 `Tab.X` 枚举值 → `_switch_tab` 调 `tab == Tab.X` 永远 false, 玩家点 tab 0 视觉变化; 漏 `_current_tab` 状态字段 → 0 表达"当前选中哪个 tab" 给 side effect 用, e.g. T072 Saves tab 首次进入时刷存档计数; 漏 4 tab Button @onready → signal connect 0 触发 / 玩家点 tab 无反应; 漏 4 panel @onready → 4 panel 永远 0 显隐, 玩家看到 4 段内容堆叠; 漏 `_switch_tab` 末 panel.visible 显隐 / 4 button modulate 切换 → 上 1 tab 玩家未退出就保留可见 + 高亮, 视觉组"当前 tab 是权威源"语义破坏)。

- **症状**：polish 期给 settings_menu 加第 5 tab (e.g. 「玩家偏好」「关卡选择」「Mod 列表」) 时, 最常见的 fragile 是「(a) Tab 枚举 0 加 + `_switch_tab` 0 适配新 tab」— T037 (#23) 落地 2 tab (AUDIO / VIDEO) 时显式用 `enum Tab { AUDIO, VIDEO }` Godot 4 enum 强类型 (vs String / int 散值), 任何"加第 5 tab 但漏 Tab 枚举值"polish 都会让 `_switch_tab` 末尾 `tab == Tab.X` 永远 false, 玩家点新 tab 0 视觉变化;「(b) `_current_tab` 状态字段缺失, side effect (Saves tab 首次进入刷存档计数 / 主菜单 BGM 切换) 0 触发」— T072 (#34) 落地 Saves tab 时显式用 `var _current_tab: Tab = Tab.AUDIO` 状态字段, 0 仅靠 `tab == Tab.X` 即时判定, 让 T343 / T134 Saves tab 首次进入触发的 `_refresh_save_count()` side effect 仍有可观察的状态锚点 (e.g. `_on_visibility_changed` 触发 / 玩家从其他 tab 切回 Saves 时不重复刷);「(c) 4 panel mutual-exclusive visible 漏 1 显隐」— T037 落地 2 tab 时 2 panel `_audio_panel.visible = (tab == Tab.AUDIO)` + `_video_panel.visible = (tab == Tab.VIDEO)`, 后续 T072 / T086 加 Saves / Controls 2 tab 时 0 触碰既有 2 panel 显隐, 任何"加第 5 tab 但漏对应 panel 显隐"polish 都会让玩家看到 2 panel 同时显示, 玩家点击新 tab 后旧 panel 仍可见, 视觉组"当前 tab 唯一可见"语义破坏;「(d) 4 button modulate 1:1 切换漏 1」— T037 落地时显式用 `_tab_audio.modulate = Color.WHITE if tab == Tab.AUDIO else Color(0.5, 0.5, 0.5)` 4 button 1:1 切换, 任何"加第 5 tab 但漏对应 button modulate"polish 都会让玩家点新 tab 后新 button 不亮 / 旧 button 仍亮, 视觉组"当前 tab 是唯一权威高亮"语义破坏;「(e) signal connect 4 行 0 触碰既有 4 行」— T037 落地 2 tab 时显式用 `_tab_audio.pressed.connect(func() -> void: _switch_tab(Tab.AUDIO))` lambda 立即调 `_switch_tab(Tab.X)`, 任何"加第 5 tab 但漏对应 signal connect"polish 都会让玩家点新 tab 0 触发, 玩家会以为"我点了没反应"。
- **触发场景**：T037 (#23) + T072 (#34) + T086 (#44) + T134 (#71) + T202.B (#121) 4 tab 状态机跨面板切换落地需同步 7 件套：(1) `src/scripts/settings_menu.gd:6` `enum Tab { AUDIO, VIDEO, CONTROLS, SAVES }` — 1 个 Tab 枚举, 4 entry 强类型 (Godot 4 enum 编译期类型检查, 0 用 String / int 散值), 顺序 0 颠倒 (Audio 首位 + Video 第 2 位 + Controls 第 3 位 + Saves 第 4 位, 与 `_switch_tab` 末尾 `tab == Tab.X` 1:1 顺序一致); (2) `src/scripts/settings_menu.gd:8` `var _current_tab: Tab = Tab.AUDIO` — 1 个状态字段, 默认 Tab.AUDIO 与 `_ready` 末尾 `_switch_tab(Tab.AUDIO)` 1:1 对齐, 给后续 side effect (Saves tab 首次进入刷存档计数) 提供状态锚点; (3) `src/scripts/settings_menu.gd:30-33` `@onready var _tab_audio: Button` + `_tab_video: Button` + `_tab_controls: Button` + `_tab_saves: Button` — 4 个 tab Button @onready 引用, 严格 "按 enum 顺序" 排列 (30-33 行), 0 颠倒, path `$VBoxContainer/TabRow/AudioTab` 等 1:1 对应 tscn 节点; (4) `src/scripts/settings_menu.gd:35-38` `@onready var _audio_panel: Control` + `_video_panel: Control` + `_controls_panel: Control` + `_saves_panel: Control` — 4 个 panel Control @onready 引用, 同样"按 enum 顺序"排列, path `$VBoxContainer/Content/AudioPanel` 等 1:1 对应 tscn 节点 (注意 Content 是 Control parent, 4 panel 互为 sibling 0 嵌套); (5) `src/scripts/settings_menu.gd:201-204` 4 行 signal connect — `_tab_audio.pressed.connect(func() -> void: _switch_tab(Tab.AUDIO))` 等 4 行 lambda, 严格"按 enum 顺序"排列, lambda 内调 `_switch_tab(Tab.X)` 立即触发切换, 0 触碰既有 4 行; (6) `src/scripts/settings_menu.gd:330-344` `_switch_tab(tab: Tab)` 函数 — 7 步骤: 1 `_current_tab = tab` 状态同步 → 2 `_audio_panel.visible = (tab == Tab.AUDIO)` panel 1 显隐 → 3 `_video_panel.visible = (tab == Tab.VIDEO)` panel 2 显隐 → 4 `_controls_panel.visible = (tab == Tab.CONTROLS)` panel 3 显隐 → 5 `_saves_panel.visible = (tab == Tab.SAVES)` panel 4 显隐 → 6 `_tab_audio.modulate = Color.WHITE if tab == Tab.AUDIO else Color(0.5, 0.5, 0.5)` button 1 modulate (4 行, 0 漏 1) → 7 `if tab == Tab.SAVES: _refresh_save_count()` 可选 side effect (Saves tab 首次进入刷存档计数, T072 / T134 落地, 0 是必须, 未来加第 5 tab 时 0 触碰此 side effect 除非新 tab 也需要 side effect); (7) `src/scripts/settings_menu.gd:277` `_ready()` 末尾 `_switch_tab(Tab.AUDIO)` 初始渲染 — 显式调一次让 4 panel 显隐 + 4 button modulate 同步, 0 依赖 tscn 节点默认 visible (玩家首次打开 settings 看到 AUDIO tab 高亮 + 3 其他 tab 灰 + 1 AUDIO panel 可见 + 3 其他 panel 隐藏)。7 件套 1:1 同步, 1 漏 1 = 玩家点 tab 0 视觉变化 / 状态字段缺失 / 4 panel 堆叠 / 4 button 高亮歧义 / signal 0 触发 / 初始渲染 0 同步 / side effect 0 触发。
- **修复**：`src/scripts/settings_menu.gd` 4 tab 状态机 (AUDIO / VIDEO / CONTROLS / SAVES) + Tab 枚举 + 4 panel mutual-exclusive visible + 4 button modulate 1:1 复制 polish 模式 (T037 + T072 + T086 + T134 + T202.B 跨 5 任务 ~20 轮落地)：
  - **T037 1 个 Tab 枚举 + 1 个 `_current_tab` 状态字段 (2 字段)** — `enum Tab { AUDIO, VIDEO, CONTROLS, SAVES }` (6 行) + `var _current_tab: Tab = Tab.AUDIO` (8 行)。Tab 枚举严格 4 entry 顺序 (Audio / Video / Controls / Saves), 0 颠倒, Godot 4 enum 强类型 (vs String / int 散值), 编译期类型检查, 任何"用 String 'AUDIO' 替代 Tab.AUDIO"polish 都会让 `_switch_tab('AUDIO')` 编译期 type mismatch 报错。`_current_tab` 默认 Tab.AUDIO 与 `_ready` 末尾 `_switch_tab(Tab.AUDIO)` 1:1 对齐, 给后续 side effect 提供状态锚点。
  - **T037 / T072 / T086 4 个 tab Button @onready 引用 (4 行)** — `@onready var _tab_audio: Button = $VBoxContainer/TabRow/AudioTab` (30) + `_tab_video` (31) + `_tab_controls` (32) + `_tab_saves` (33), 严格"按 enum 顺序"排列, path 与 tscn 节点 1:1 对应, 0 颠倒, 0 共享 1 个 Button group 引用 (4 个独立 Button 引用, 让 `_switch_tab` 末尾 4 行 modulate 切换 1:1)。
  - **T037 / T072 / T086 4 个 panel Control @onready 引用 (4 行)** — `@onready var _audio_panel: Control = $VBoxContainer/Content/AudioPanel` (35) + `_video_panel` (36) + `_controls_panel` (37) + `_saves_panel` (38), 同样"按 enum 顺序"排列, 4 panel 互为 sibling 0 嵌套, path 与 tscn 节点 1:1 对应。
  - **T037 / T072 / T086 4 行 signal connect (4 行)** — `_tab_audio.pressed.connect(func() -> void: _switch_tab(Tab.AUDIO))` (201) + `_tab_video` (202) + `_tab_controls` (203) + `_tab_saves` (204), 4 行 lambda 立即调 `_switch_tab(Tab.X)`, 严格"按 enum 顺序"排列, 0 颠倒, 0 触碰既有 4 行。lambda 内 lambda 写法 (`func() -> void: _switch_tab(Tab.AUDIO)`) 而非独立 handler (`_on_tab_audio_pressed`), 减少 4 个 handler 声明 (与 §9.6.4 6 verb tooltip 8 行拼接扩展器模式 "0 触碰既有 6 verb 字段" 同思路, 0 增加新成员函数)。
  - **T037 / T072 / T086 1 个 `_switch_tab(tab: Tab)` 函数 (15 行)** — 7 步骤: 1 `_current_tab = tab` 状态同步 → 2-5 4 panel 显隐 (4 行 `_X_panel.visible = (tab == Tab.X)`) → 6 4 button modulate 切换 (4 行 `_tab_X.modulate = Color.WHITE if ... else Color(0.5, 0.5, 0.5)`) → 7 可选 side effect (`if tab == Tab.SAVES: _refresh_save_count()`)。7 步骤顺序硬约束: 状态同步在前 (让后续判定用最新状态) → 4 panel 显隐在中 (玩家视觉组先收到反馈) → 4 button modulate 紧跟 (玩家视觉组立即看到高亮) → side effect 在末 (避免 0 显隐就触发的"空 side effect")。
  - **T037 `_ready` 末尾 `_switch_tab(Tab.AUDIO)` 初始渲染 (1 行)** — 显式调一次, 0 依赖 tscn 节点默认 visible, 0 依赖 tscn 节点默认 modulate, 玩家首次打开 settings 立即看到 AUDIO tab 高亮 + 3 其他 tab 灰 + 1 AUDIO panel 可见 + 3 其他 panel 隐藏, 视觉组"当前 tab 是唯一权威源"100% 闭环。
  - 0 副作用：T037 / T072 / T086 0 触碰 `_tab_audio` / `_tab_video` 等 4 button Button 主体 (仅新加 signal connect 0 改 Button 自身属性); T037 / T072 / T086 0 触碰 4 panel 子节点 (panel 内的 MasterSlider / FullscreenCheck 等 0 触碰, 4 panel 显隐由 `_switch_tab` 控制, panel 内的具体控件 0 触碰); T134 placeholder 0 触碰 Tab 枚举 (T134 改 `_save_count_label.text` placeholder 用 `SaveSystem.SLOT_COUNT` 动态格式化, 0 加新 Tab entry); T202.B ReduceAllCheck 0 触碰 Tab 状态机 (T202.B 是 VideoPanel 内 1 主 CheckBox + 3 子 CheckBox 联动, 与 4 tab 状态机 0 互混, 2 套 polish 模式 0 复用 0 共享 0 字段, 与 §9.6.11 ReduceAllCheck 三态 0 触碰 Tab 枚举 1:1 严格分离)。
- **预防**：
  1. 任何 polish 期给 settings_menu 加第 5 tab (e.g. 玩家偏好 / 关卡选择 / Mod 列表) 或类似 4 tab 状态机的多面板 UI (e.g. 主菜单分类页 / 角色面板技能树 / 商店面板分类) 时**必须**严格按 7 件套 1:1 复制既有 4 tab 模式: (1) `enum Tab { ... }` Tab 枚举 0 改顺序 (新 tab 加在末尾) + (2) `var _current_tab: Tab = Tab.<FIRST>` 状态字段 0 改默认 (与新枚举第 1 entry 1:1) + (3) N 个 `_tab_X: Button` @onready 引用 0 改既有 N 个 (新加 1 个, 顺序在末尾) + (4) N 个 `_X_panel: Control` @onready 引用 0 改既有 N 个 (新加 1 个, 顺序在末尾) + (5) N 行 `_tab_X.pressed.connect(func() -> void: _switch_tab(Tab.X))` signal connect 0 改既有 N 行 (新加 1 行) + (6) `_switch_tab(tab: Tab)` 函数内 4 步骤顺序硬约束 (状态同步 → N panel 显隐 → N button modulate → 可选 side effect), 新 tab 必须在 panel 显隐 + button modulate 2 段同步扩 1 行 + (7) `_ready` 末尾 `_switch_tab(Tab.<FIRST>)` 0 改 (默认 Tab 第 1 entry 仍生效)。建议在 `_switch_tab` 上方 docblock 写明 "Tab 枚举 + 1 状态字段 + N tab Button + N panel + N signal connect + `_switch_tab` 7 步骤 + 初始渲染 1 行" 七件套硬约束。
  2. Tab 枚举 0 改任 1 entry 顺序 — `enum Tab { AUDIO, VIDEO, CONTROLS, SAVES }` 0 改 `enum Tab { AUDIO, VIDEO, SAVES, CONTROLS }` (顺序颠倒会让 `_switch_tab` 末尾 `tab == Tab.SAVES` 仍生效但开发者阅读时歧义, 未来加新 tab 时易乱), source-grep 验证 Tab 枚举 4 entry 顺序 0 漏 0 改 1 entry。
  3. `_current_tab` 状态字段 0 删 — T072 落地 Saves tab 首次进入 `_refresh_save_count()` side effect 依赖 `_current_tab` 状态 (玩家从其他 tab 切回 Saves 时不重复刷), 任何"删 `_current_tab` 改用 tab parameter 判定"polish 都会让 side effect 在 4 tab 间重复触发, 性能 / 视觉组双双受影响, source-grep 验证 `var _current_tab` 0 漏 0 改 1 字符。
  4. 4 panel mutual-exclusive visible 0 改 — `_X_panel.visible = (tab == Tab.X)` 4 行 0 改 `_X_panel.visible = (tab == Tab.X or some_other_condition)` (任何"多 panel 同时可见"polish 都会让玩家看到 2 panel 内容堆叠, 视觉组"当前 tab 唯一可见"语义被破坏), source-grep 验证 4 panel 显隐表达式 0 漏 0 改 1 行 (每个 panel 1 行, 0 共享 1 个 if 分支)。
  5. 4 button modulate 1:1 切换 0 改 — `_tab_X.modulate = Color.WHITE if tab == Tab.X else Color(0.5, 0.5, 0.5)` 4 行 0 改 `_tab_X.modulate = Color.WHITE if tab == Tab.X else some_other_color` (任何"换色"polish 必须 4 行同步换, 1 行改 0 改 3 行会让玩家看到"高亮色不一致"), source-grep 验证 4 button modulate 表达式 0 漏 0 改 1 行 (每个 button 1 行, 0 共享 1 个 if 分支)。
  6. signal connect 4 行 0 触碰 — `_tab_X.pressed.connect(func() -> void: _switch_tab(Tab.X))` 4 行 lambda 写法 0 改 `_tab_X.pressed.connect(_on_tab_X_pressed)` (任何"改独立 handler"polish 都会增加 4 个 handler 声明, 与 §9.6.5 8 行拼接扩展器模式 "0 触碰既有 handler" 思路冲突), source-grep 验证 4 行 lambda 0 漏 0 改 1 字符 (lambda 内 `_switch_tab(Tab.X)` 0 触碰, 0 改 `_on_tab_X_pressed`)。
  7. `_switch_tab` 7 步骤顺序硬约束 0 改 — 1 状态同步 (前) → 2-5 N panel 显隐 (中) → 6 N button modulate (后) → 7 可选 side effect (末), 任何"重排顺序"polish 都会让玩家看到"panel 0 显隐就 button modulate"或"side effect 触发时 panel 仍可见"视觉组歧义, source-grep 验证 `_current_tab = tab` 在函数首 + N panel 显隐在 N button modulate 之前 + side effect 在函数末 0 漏 0 改 1 行。
  8. T037 / T072 / T086 7 件套 0 触碰既有 panel 内的具体控件 — `_audio_panel` 内的 MasterSlider / SFXSlider / MusicSlider / AmbienceSlider / MusicPreviewButton 0 触碰 (T037 落地 2 tab 时 4 slider 0 触碰, T134 / T239 后续 polish 4 slider 0 触碰 tab 状态机, 2 套 polish 模式 0 复用 0 共享 0 字段, 与 §9.6.11 ReduceAllCheck 三态 0 触碰 `_audio_panel` 任何 slider 1:1 严格分离); `_video_panel` 内的 FullscreenCheck / ScaleOptions / ReduceAllCheck / ReduceShakeCheck / ReduceFlashCheck / ReduceVibrationCheck 0 触碰 (T202.B 落地 0 触碰 Tab 状态机); `_controls_panel` 内的 HintLabel / ControlsList / ResetDefaultsButton 0 触碰 (T086 落地 0 触碰 tab 状态机); `_saves_panel` 内的 SavesInfoLabel / SaveCountLabel / Spacer 0 触碰 (T072 / T134 落地 0 触碰 tab 状态机, T134 placeholder 用 SaveSystem.SLOT_COUNT 动态格式化 0 改 panel 自身 0 改 tab 状态机)。

### 9.6.13 settings_menu `_save_settings` ↔ `_load_settings` 1:1 同步 + `user://settings.cfg` 持久化 4 section × N key 双源映射 polish 模式 (T079 + T136 + T195 + T196 + T202.B + T205 + T239 跨 7 任务 ~15 轮落地) 文档化

> §9.6.11 记录 settings_menu ReduceAllCheck 三态 (`_syncing_from_master` 防递归 + `set_block_signals` 双层保险 + `_reduce_all` 数据独立)；§9.6.12 记录 settings_menu 4 tab 状态机 (Tab 枚举 + 1 状态字段 + 4 tab Button @onready + 4 panel Control @onready + 4 signal connect + `_switch_tab` 7 步骤 + 初始渲染 1 行) 7 件套硬约束。本节记录 settings_menu 持久化层 —— `_save_settings` 写 `user://settings.cfg` ↔ `_load_settings` 从 `user://settings.cfg` 读回 1:1 双源映射 4 section × N key (14 + ACTION_NAMES) polish 模式。T037 (#23) 落地 4 audio 滑块 + T079 (#67) 加 respawn 策略 + T136 (#77) 加 autosave 3 key (enabled / interval / slot) + T195 (#112) 加 reduce_shake / reduce_flash 2 key + T196 (#113) 加 reduce_vibration 1 key + T202.B (#121) 加 reduce_all 1 key + T205 (#122) 加 audio 4 label 还原 (load 后立即刷新 label text) + T239 (#157) 加 music preview button (0 触碰持久化层), 7 个任务跨 ~15 轮反复落地, 任何 polish 期给 settings_menu 加新 1 key (e.g. 玩家偏好 / UI 主题 / 字幕 / 难度 / 帧率上限) 或加新 1 section (e.g. [ui] / [graphics] / [audio_advanced]) 时先查本节, 0 漏 1 处同步 (e.g. 漏 `_save_settings` 写 key 但 `_load_settings` 0 读 → 玩家重启游戏后新 key 永远 0 还原, 永远是默认值, 视觉组"settings 持久化"语义破坏; 漏 `_load_settings` 读 key 但 `_save_settings` 0 写 → 玩家重启游戏后读到 stale 旧 key 值, 0 反映当前 cfg 状态; 漏 default 参数 → 玩家首次开 settings (0 cfg 文件) `get_value` 抛错, 走 else 兜底, 任何"new key 0 default"polish 都会让 fresh clone 0 启动 settings menu; 漏 4 section 顺序 (audio → video → gameplay → accessibility → input) → 跨任务 polishing 期间 cfg 文件结构错位, 玩家从旧版升级到新版时 cfg 兼容 0 优雅; 漏 last-writer-wins 模式 (settings_menu vs SaveSystem 都写) → 玩家关 settings 时 autosave 段被 settings_menu 覆盖, 即使 SaveSystem 期间写过 1 次, 视觉组"settings 是 source of truth"语义破坏)。详细记录 4 段 (症状/触发场景/修复/预防)：

- **症状**：polish 期给 settings_menu 加新 1 key (e.g. "音量" 加 master_mute / "屏幕" 加 vsync / "玩法" 加难度 / "无障碍" 加字幕大小) 时, 最常见的 fragile 是「(a) `_save_settings` 写 key 但 `_load_settings` 0 读 → 玩家重启后 key 永远 0 还原」— T079 (#67) 落地 respawn_to_hub 时显式用 `cfg.set_value("gameplay", "respawn_to_hub", _respawn_to_hub)` + `cfg.get_value("gameplay", "respawn_to_hub", true)`, 任何"加 key 但 0 同步读"polish 都会让玩家重启游戏后, 玩家在 settings 中勾选/调整的值 0 还原, 永远走 default, 视觉组"settings 持久化"语义破坏;「(b) `_load_settings` 读 key 但 `_save_settings` 0 写 → 玩家重启后读到 stale 旧 key 值」— T136 (#77) 落地 autosave 3 key (enabled / interval / slot) 时显式用 `_save_settings` 3 行 `cfg.set_value` + `_load_settings` 3 行 `cfg.get_value`, 任何"读 key 但 0 同步写"polish 都会让玩家首次写 cfg 时 0 含新 key, 后续读时拿到 `get_value` 兜底 default, 但 0 反映玩家当前勾选状态, 视觉组"settings 持久化"语义破坏;「(c) `cfg.get_value` 0 传 default 参数 → fresh clone 启动 settings menu 抛错」— T079 落地 respawn_to_hub 时显式用 `cfg.get_value("gameplay", "respawn_to_hub", true)` 传 default `true` (默认走 hub 安全区), 任何"漏 default"polish 都会让 Godot 4 ConfigFile 报 `Key not found` 错误 (虽然不致命但 push_warning 噪音), 玩家首次开 settings 0 cfg 还能走 else 兜底, 但 0 优雅;「(d) 4 section 顺序错位 (audio → video → gameplay → accessibility → input) → 跨任务 polishing 期间 cfg 文件结构错位」— T037 落地 2 tab (audio / video) 时按视觉组顺序排 `[audio]` 在前 (4 slider 音量) + `[video]` 在中 (fullscreen / scale) + `[gameplay]` 居中 (respawn 策略) + `[accessibility]` 靠后 (4 reduce_*_check 视觉减弱) + `[input]` 末位 (按键重映射), 任何"重排 section 顺序"polish 都会让 cfg 文件结构错位, 玩家从旧版升级到新版时 cfg 兼容 0 优雅, 读 key 时走 `cfg.get_value("section", "key", default)` section 名独立 0 受顺序影响但 0 优雅;「(e) last-writer-wins 模式 0 触碰 (settings_menu vs SaveSystem 都写 [gameplay] section) → 玩家关 settings 时 autosave 段被 settings_menu 覆盖」— T136 落地时显式用 `cfg.set_value("gameplay", "autosave_enabled", _autosave_enabled_check.button_pressed)` (走控制 live 状态而非 cfg 缓存) 而非 `cfg.set_value("gameplay", "autosave_enabled", _autosave_enabled)` 字段 (字段可能 stale), 任何"统一 last-writer-wins"polish 都会让 SaveSystem 期间写的 autosave 段在 settings_menu 关时被覆盖, 视觉组"settings 是 source of truth"语义破坏。
- **触发场景**：T079 (#67) + T136 (#77) + T195 (#112) + T196 (#113) + T202.B (#121) + T205 (#122) + T239 (#157) 4 section × N key 1:1 双源映射落地需同步 5 件套：(1) `src/scripts/settings_menu.gd:1071-1119` `_save_settings()` 函数 (49 行) — 4 section × N key `cfg.set_value` 0 漏 1 顺序: (1.1) `[audio]` 4 key (master / sfx / music / ambience, T037 #23 落地, `_master_volume` / `_sfx_volume` / `_music_volume` / `_ambience_volume` 字段直接写, 0 走 `button_pressed`, 0 触碰 §9.6.12 4 tab 状态机); (1.2) `[video]` 2 key (fullscreen / window_scale, T037 落地, `_fullscreen` / `_window_scale` 字段直接写); (1.3) `[gameplay]` 4 key (respawn_to_hub / autosave_enabled / autosave_interval / autosave_slot, T079 + T136 落地, respawn_to_hub 字段直接写 + autosave 3 key 走控件 `button_pressed` / `value` / `selected`); (1.4) `[accessibility]` 4 key (reduce_shake / reduce_flash / reduce_vibration / reduce_all, T195 + T196 + T202.B 落地, reduce_*_check 3 个 `button_pressed` + reduce_all 走 `_reduce_all` 字段而非 indeterminate 显示); (1.5) `[input]` N key (ACTION_NAMES 字典 keys, T086 落地, `for action in ACTION_NAMES.keys()` 循环 + `InputMap.action_get_events(action)` 取第 1 个 event 存); (1.6) `cfg.save("user://settings.cfg")` 1 行写盘 + push_warning 错误处理; (2) `src/scripts/settings_menu.gd:1121-1230` `_load_settings()` 函数 (~110 行) — 4 section × N key `cfg.get_value` 1:1 对应 `_save_settings` 0 漏 1: (2.1) `[audio]` 4 key `get_value` 0 漏 default (1.0 默认满音量) + 4 slider 实时推 `_master_slider.value = _master_volume * 100.0` + T205 4 label `_refresh_audio_volume_label` 还原 text; (2.2) `[video]` 2 key `get_value` 0 漏 default (false / 4) + `_fullscreen_check.button_pressed` + `_scale_options.select(clampi(...))`; (2.3) `[gameplay]` 4 key `get_value` 0 漏 default (true / 各种) + `_populate_autosave_controls_from_cfg(cfg)` 1 行 helper (T136 落地) 推 3 autosave 控件 + live-push 到 SaveSystem autoload; (2.4) `[accessibility]` 4 key `get_value` 0 漏 default (false / 各种) + `set_block_signals(true/false)` 包裹 + 4 reduce_*_check `button_pressed` 推 (T195 + T196 + T202.B 模式) + T202.B `_reduce_all` indeterminate 状态 `set_block_signals` + `_syncing_from_master = true; _apply_three_children(true); _set_three_children_disabled(true); _syncing_from_master = false` 防递归; (2.5) `[input]` N key `get_value` 0 漏, 但 _save_settings 写的是 event 字典, _load_settings 通常 0 还原按键 (按键重映射是 settings 的"导出"功能而非"导入"功能, 玩家重启后按 Godot 默认 InputMap 走); (2.6) `cfg.load("user://settings.cfg")` 1 行读盘 + 错误处理 `if err != OK: return` 早退; (3) `src/scripts/settings_menu.gd:265` `_ready` 末尾 `_load_settings()` 1 行初始加载 — 显式调一次, 玩家首次打开 settings 时立即看到 4 section × N key 还原 (4 audio slider 位置 + 4 audio label text + 2 video toggle + 1 respawn toggle + 3 autosave toggle / slider / select + 4 reduce_*_check 状态), 视觉组"settings 持久化"100% 闭环; (4) `src/scripts/settings_menu.gd:316` `_on_close()` 末尾 `_save_settings()` 1 行持久化 — 玩家关 settings menu 立即写盘 (4 section × N key live 状态), 视觉组"关 settings 即持久化"100% 闭环, 与 §9.6.12 _switch_tab 7 步骤 + §9.6.11 ReduceAllCheck 三态 0 互混; (5) `src/scripts/settings_menu.gd:1071-1119` + `src/scripts/settings_menu.gd:1121-1230` `_save_settings` ↔ `_load_settings` 1:1 双源映射 4 section × N key —— `_save_settings` 写 N key, `_load_settings` 必须读 N key (0 漏 0 错位), 任何"加新 key 但漏 1 边"polish 都会让持久化双向断开 (写 0 读 或 读 0 写), 视觉组"settings 持久化"语义破坏。5 件套 1:1 同步, 1 漏 1 = 玩家重启后新 key 0 还原 / 玩家首次启动 settings 抛错 / section 顺序错位 / last-writer-wins 模式破坏 / 初始加载 0 同步 / 关闭 menu 0 持久化。
- **修复**：`src/scripts/settings_menu.gd` `_save_settings` ↔ `_load_settings` 1:1 同步 + `user://settings.cfg` 持久化 4 section × N key 双源映射 polish 模式 (T079 + T136 + T195 + T196 + T202.B + T205 + T239 跨 7 任务 ~15 轮落地)：
  - **T037 (#23) `_save_settings` + `_load_settings` 4 audio + 2 video 6 key 基础** — `_save_settings()` `[audio]` 4 key `cfg.set_value` (master / sfx / music / ambience) + `[video]` 2 key (fullscreen / window_scale) + `_load_settings()` 6 key 1:1 `cfg.get_value` 0 漏 default (1.0 / false / 4) + 4 slider 推 `value` + 2 video 推 `button_pressed` / `select`。0 触碰 §9.6.12 4 tab 状态机 (4 slider 是 `_audio_panel` 内子节点, 0 触碰 tab 状态机 7 件套); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T037 落地时 T202.B 还没加, 0 触碰 reduce_*_check 任何字段)。
  - **T079 (#67) `[gameplay] respawn_to_hub` 1 key 加 gameplay section** — `_save_settings` 加 1 行 `cfg.set_value("gameplay", "respawn_to_hub", _respawn_to_hub)` + `_load_settings` 加 1 行 `cfg.get_value("gameplay", "respawn_to_hub", true)` (默认 `true` = 重生到 hub 安全区, 玩家最常见选择) + `_respawn_hub_check.button_pressed = _respawn_to_hub` + `GameState.set_respawn_to_hub(_respawn_to_hub)` 跨 autoload live-push (玩家关 settings 后 GameState 立即遵守, 0 需重启)。0 触碰 T037 4 audio + 2 video 既有 6 key (T079 0 改 [audio] / [video] section 任何 key, 仅 [gameplay] 扩 1 key)。
  - **T136 (#77) `[gameplay] autosave 3 key` (enabled / interval / slot) 加 SaveSystem autoload 联动** — `_save_settings` 3 行 `cfg.set_value` 走控件 `button_pressed` / `value` / `selected` (last-writer-wins 模式: settings_menu 与 SaveSystem 都写, 关 menu 时 settings_menu 覆盖 SaveSystem 期间写的值) + `_load_settings` 3 行 `cfg.get_value` 0 漏 default (true / 60.0 / 0) + `_populate_autosave_controls_from_cfg(cfg)` 1 行 helper 函数 (T136 落地, 推 3 autosave 控件 + live-push 到 SaveSystem)。0 触碰 T079 respawn_to_hub 1 key (T136 0 改 [gameplay] respawn_to_hub, 仅 [gameplay] 扩 3 key)。
  - **T195 (#112) `[accessibility] reduce_shake / reduce_flash` 2 key 加 accessibility section** — `_save_settings` 2 行 `cfg.set_value` 走控件 `button_pressed` (走控制 live 状态而非 `_reduced_shake` 字段, 与 T136 autosave 同样的 last-writer-wins 模式) + `_load_settings` 2 行 `cfg.get_value` 0 漏 default (false 默认全功能) + `set_block_signals(true/false)` 包裹避免 `_on_reduce_*_toggled` 反复触发写 cfg + `ScreenShake.set_reduce_*` 跨 autoload live-push (同 T136 SaveSystem 模式)。0 触碰 T037 4 audio + 2 video + T079 1 respawn + T136 3 autosave 既有 10 key (T195 0 改 [audio] / [video] / [gameplay] section 任何 key, 仅 [accessibility] 扩 2 key)。
  - **T196 (#113) `[accessibility] reduce_vibration` 1 key** — `_save_settings` 1 行 `cfg.set_value` + `_load_settings` 1 行 `cfg.get_value` 0 漏 default (false) + `set_block_signals` 包裹 + `ScreenShake.set_reduce_vibration` 跨 autoload live-push, 与 T195 reduce_shake 完全同模式 (1 key 同步 0 触碰 1 行)。0 触碰 T195 reduce_shake / reduce_flash 2 key (T196 0 改 reduce_shake / reduce_flash 任何字段, 仅 reduce_vibration 扩 1 key)。
  - **T202.B (#121) `[accessibility] reduce_all` 1 key 加主开关数据独立** — `_save_settings` 1 行 `cfg.set_value("accessibility", "reduce_all", _reduce_all)` 走 `_reduce_all` 字段而非 `_reduce_all_check.button_pressed` (indeterminate 时 button_pressed 仍为 false 但 _reduce_all 字段可能为 true, 存数据不存显示) + `_load_settings` 1 行 `cfg.get_value` 0 漏 default (false) + `set_block_signals` 包裹 + `indeterminate = false` 显式清 (indeterminate 是 UI 反映 0 持久化) + `_syncing_from_master = true; _apply_three_children(true); _set_three_children_disabled(true); _syncing_from_master = false` 防递归 (与 §9.6.11 三态 100% 闭环)。0 触碰 T195 + T196 reduce_shake / reduce_flash / reduce_vibration 3 key (T202.B 0 改 reduce_shake / reduce_flash / reduce_vibration 任何字段, 仅 reduce_all 扩 1 key, T202.B 落地时 0 触碰 `_reduced_shake` / `_reduced_flash` / `_reduced_vibration` 字段, 0 触碰 `ScreenShake.set_reduce_*` 调用 1:1 严格分离)。
  - **T205 (#122) `[audio] 4 label text 还原`** — `_load_settings` 4 audio 滑块 `value` 推后立即调 `_refresh_audio_volume_label(_master_label, _master_slider.value, "主音量")` 等 4 行, 让玩家首次开 settings 立即看到 4 label 显示当前 cfg 音量 ("主音量: 80") 而非 stale 默认值 ("主音量: 100"), 0 触碰 cfg 持久化层 (T205 0 改 `_save_settings` 任何 key, 仅 `_load_settings` 末尾加 4 行 label 还原)。0 触碰 T037 + T079 + T136 + T195 + T196 + T202.B 任何 key (T205 0 改 4 audio 之外的任何 key, 0 改 cfg 写 / 读路径, 仅 4 label 还原)。
  - **T239 (#157) `[audio] music preview button` (0 触碰持久化层)** — T239 加 `_music_preview_btn` 1 个 Button + `_on_music_preview_pressed` 1 个 handler + 3s 预览 1 个 BGM 主题, 0 触碰 `_save_settings` / `_load_settings` 任何 key (music preview 是临时播放, 0 持久化, 玩家关 settings 后 0 残留预览)。T239 是 §9.6.12 4 tab 状态机扩展 (audio_panel 内加 1 个 Button), 0 触碰 cfg 任何 key, 0 触碰 §9.6.11 ReduceAllCheck 三态, 0 触碰 §9.6.12 _switch_tab 7 步骤, 与本节 5 件套 0 互混 0 复用 0 共享。
  - 0 副作用：T037 / T079 / T136 / T195 / T196 / T202.B / T205 / T239 任何 1 key 0 触碰既有 N-1 key (`_save_settings` 写 N-1 行 0 改, 仅新加 1 行; `_load_settings` 读 N-1 行 0 改, 仅新加 1 行); 4 section 顺序 (audio → video → gameplay → accessibility → input) 0 改; last-writer-wins 模式 0 改 (settings_menu vs SaveSystem 都写 [gameplay] section, 走控件 live 状态, 0 触碰 cfg 缓存); 0 触碰 §9.6.12 4 tab 状态机 (T037 + T079 + T136 + T195 + T196 + T202.B + T205 + T239 任何 const / var 0 触碰 7 件套 1:1 严格分离); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T195 + T196 + T202.B 0 触碰 `_syncing_from_master` 守卫 + `set_block_signals` 双层保险, 0 触碰 `_reduce_all` 数据独立)。
- **预防**：
  1. 任何 polish 期给 settings_menu 加新 1 key (e.g. 玩家偏好 / UI 主题 / 字幕 / 难度 / 帧率上限 / 音量归一化) 或加新 1 section (e.g. [ui] / [graphics] / [audio_advanced] / [language]) 时**必须**严格按 5 件套 1:1 复制既有 4 section × N key 双源映射模式: (1) `_save_settings` 加 1 行 `cfg.set_value("section", "key", value)` (走控件 live 状态而非字段缓存, 0 漏 1) + (2) `_load_settings` 加 1 行 `cfg.get_value("section", "key", default)` 0 漏 default 参数 (fresh clone 走 default 兜底) + (3) `_load_settings` 末尾推控件 `button_pressed` / `value` / `selected` + 跨 autoload live-push (T079 GameState / T136 SaveSystem / T195+T196+T202.B ScreenShake, 让相关 autoload 立即遵守) + (4) `set_block_signals(true/false)` 包裹避免 `_on_*_toggled` 反复触发写 cfg (T195 + T196 + T202.B 模式, 与 §9.6.11 三态 100% 闭环) + (5) `_ready` 末尾 `_load_settings()` 1 行 + `_on_close` 末尾 `_save_settings()` 1 行 (玩家首次开 settings 立即还原 + 关 menu 立即持久化)。建议在 `_save_settings` 上方 docblock 写明 "5 件套 1:1 同步 (写 + 读 + default + 控件推 + 跨 autoload live-push)" 硬约束。
  2. 4 section 顺序 (audio → video → gameplay → accessibility → input) 0 改 — `[audio]` 首位 (4 slider 音量) + `[video]` 第 2 位 (fullscreen / scale) + `[gameplay]` 居中 (respawn / autosave) + `[accessibility]` 靠后 (4 reduce_*_check 视觉减弱) + `[input]` 末位 (按键重映射), 任何"重排 section 顺序"polish 都会让 cfg 文件结构错位, 玩家从旧版升级到新版时 cfg 兼容 0 优雅, source-grep 验证 4 section 顺序 0 漏 0 改 1 section。
  3. `cfg.get_value` 0 漏 default 参数 — `cfg.get_value("section", "key", default_value)` 3 个参数 0 漏第 3 个 (任何"漏 default"polish 都会让 Godot 4 ConfigFile `Key not found` 错误, fresh clone 0 cfg 时 0 优雅), source-grep 验证 4 section × N key `cfg.get_value` 调用 0 漏 default 0 改 1 字符。`save_settings` 0 触碰 `get_value` 反之亦然, source-grep 验证 14 + N 行 `get_value` 0 漏 0 改 1 行。
  4. last-writer-wins 模式 0 改 (settings_menu vs SaveSystem 都写 [gameplay] section) — `_save_settings` 3 autosave key 走控件 `_autosave_enabled_check.button_pressed` / `_autosave_interval_slider.value` / `_autosave_slot_options.selected` 而非字段缓存 (字段可能 stale), 任何"统一 last-writer-wins"polish 都会让 SaveSystem 期间写的 autosave 段在 settings_menu 关时被覆盖, source-grep 验证 3 autosave `cfg.set_value` 0 漏 0 改 1 行 (走控件 0 走字段)。
  5. `_save_settings` ↔ `_load_settings` 1:1 双源映射 0 漏 1 边 — `_save_settings` 写 N key, `_load_settings` 必须读 N key (任何"加新 key 但漏 1 边"polish 都会让持久化双向断开), 2 函数 N 行 `cfg.set_value` ↔ N 行 `cfg.get_value` 0 漏 0 错位 0 复用 0 共享, source-grep 验证 14 + N 行 `cfg.set_value` + 14 + N 行 `cfg.get_value` 0 漏 0 改 1 字符 (按 section / key 1:1 对齐)。
  6. `set_block_signals(true/false)` 包裹 0 删 (T195 + T196 + T202.B accessibility 4 key 模式) — `_load_settings` 4 reduce_*_check `set_block_signals(true)` + `button_pressed = ...` + `set_block_signals(false)` 包裹避免 `_on_reduce_*_toggled` 反复触发写 cfg (与 §9.6.11 三态 100% 闭环), 任何"删 set_block_signals"polish 都会让 `_load_settings` 推控件时反复触发 `_on_*_toggled` 写 cfg, 性能 0 优雅, source-grep 验证 4 reduce_*_check `set_block_signals` 0 漏 0 改 1 字符。
  7. T205 audio 4 label 还原 0 删 (T205 落地 `_refresh_audio_volume_label` 4 行) — `_load_settings` 4 audio 滑块 `value` 推后立即调 `_refresh_audio_volume_label(_X_label, _X_slider.value, "X")` 4 行 (master / sfx / music / ambience), 任何"删 4 label 还原"polish 都会让玩家首次开 settings 看到 stale 默认 label text ("主音量: 100") 而非当前 cfg 音量 ("主音量: 80"), 视觉组"settings 持久化"语义破坏, source-grep 验证 4 行 `_refresh_audio_volume_label` 0 漏 0 改 1 字符 (4 label + 4 slider + 4 base_text "主音量" / "音效" / "音乐" / "环境音" 1:1 严格对齐)。
  8. T037 / T079 / T136 / T195 / T196 + T202.B / T205 / T239 5 件套 0 触碰既有 N-1 key —— `_save_settings` 写 N-1 行 0 改, 仅新加 1 行; `_load_settings` 读 N-1 行 0 改, 仅新加 1 行; `_load_settings` 末尾推控件 + 跨 autoload live-push N-1 行 0 改, 仅新加 1 行; `_load_settings` 末尾 `set_block_signals` 包裹 N-1 行 0 改, 仅新加 1 行 (T195 + T196 + T202.B 模式); `_ready` 末尾 `_load_settings()` + `_on_close` 末尾 `_save_settings()` 0 触碰 (T037 落地时已设, 后续 polish 0 改 0 删 0 重排)。0 触碰 §9.6.12 4 tab 状态机 (任何 const / var 0 触碰 7 件套 1:1 严格分离); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T195 + T196 + T202.B 0 触碰 `_syncing_from_master` 守卫 + `_reduce_all` 数据独立); 0 触碰 §9.6.9 / §9.6.10 PauseMenu 跨面板 hover 反馈 (settings_menu 与 pause_menu 是 2 个独立 menu, 0 复用任何字段)。

### 9.6.14 settings_menu `[input]` section N key ACTION_NAMES 循环 + InputMap.action_get_events / action_add_event / action_erase_events 跨 InputEvent dict 序列化 + 1:1 持久化 polish 模式 (T086 + T194 + T195 + T196 + T202.B + T205 + T239 跨 7 任务 ~10 轮落地) 文档化

> §9.6.11 记录 settings_menu ReduceAllCheck 三态 (indeterminate + _syncing_from_master 防递归 + _reduce_all 数据独立); §9.6.12 记录 settings_menu 4 tab 状态机 (Tab 枚举 + 1 状态字段 + 4 tab Button @onready + 4 panel Control @onready + 4 signal connect + _switch_tab 7 步骤 + 初始渲染 1 行) 7 件套硬约束; §9.6.13 记录 settings_menu `_save_settings` ↔ `_load_settings` 1:1 同步 + `user://settings.cfg` 持久化 4 section × N key (audio + video + gameplay + accessibility + input) 双源映射 polish 模式。本节聚焦 §9.6.13 提到的 `(1.5) [input]` 段 —— ACTION_NAMES 字典 1:1 循环写 + InputMap.action_get_events 取第 1 个 event 存 dict + `_load_settings` 1:1 循环读 + InputMap.action_erase_events / action_add_event 还原 InputMap 4 件套硬约束, 涉及 6 件套：(1) `const ACTION_NAMES` 字典 (5-7 actions: move_left/right/jump + verb_primary/secondary + interaction) (2) `const ACTION_CATEGORY` 字典 (按 category 3 段分组: movement/verb/interaction) (3) `const CATEGORY_RENDER_ORDER` 数组 (3 段 render 顺序, 含 name + key + color) (4) `const _DEFAULT_BINDINGS` 字典 (InputMap 默认值, 用于 "重置" 按钮) (5) `_save_settings` 末尾 4 行循环写 N key (6) `_load_settings` 末尾 5 行循环读 N key + InputMap.action_erase_events + InputMap.action_add_event 还原。T037 (#23) 落地 4 audio 滑块 + 2 video + T086 (#44) 落地 5 动作到 ACTION_NAMES + T194 (#112) 落地 5 verb 分组标题 (移动/声波能力/交互) 到 ACTION_CATEGORY + CATEGORY_RENDER_ORDER + T195 (#112) accessibility reduce_shake / reduce_flash + T196 (#113) accessibility reduce_vibration + T202.B (#121) accessibility reduce_all + T205 (#122) audio 4 label 还原 + T239 (#157) music preview button, 8 个任务跨 ~10 轮反复落地 [input] section, 任何 polish 期给 settings_menu 加新 1 动作 (e.g. 切分 hot key 单独绑 / 6th verb 接入 / 重置默认按键 / 玩家偏好键) 或加新 1 category 段 (e.g. UI 导航 / 战斗 / 调试) 时先查本节, 0 漏 1 处同步 (e.g. 漏 `ACTION_NAMES` 但 `_save_settings` 0 漏 `for action in ACTION_NAMES.keys()` → 玩家在 CONTROLS tab 看到默认绑定但按 0 生效, 视觉组"按键重映射"语义破坏; 漏 `ACTION_CATEGORY` 但 `CATEGORY_RENDER_ORDER` 含 1 category → 玩家在 CONTROLS tab 看到 1 category 段 0 分组, 按 0 找; 漏 `_save_settings` 写 [input] section 但 `_load_settings` 0 读 → 玩家在 CONTROLS tab 改键后重启游戏 0 还原, 永远走默认 InputMap; 漏 `if events.size() > 0` 守卫 → 玩家在 CONTROLS tab 看到 0 events 的 action 也存 `null` 到 cfg, `_load_settings` `if ev is InputEvent` 守卫能挡但 0 优雅; 漏 `InputMap.action_erase_events` 预清 → `_load_settings` 还原时残留 Godot 默认 InputMap + 玩家重映射 2 个键, 玩家按第 2 个键时同时触发 2 个 action; 漏 `if ev is InputEvent` 守卫 → `cfg.get_value` 拿到 `null` (fresh clone 0 cfg) 时 `null.action_erase_events` 抛错; 漏 `for action in ACTION_NAMES.keys()` 循环而走 5 行动词硬编码 → 任何"加 1 动作"polish 都会忘 1 行硬编码, source-grep 验证 6 步循环 0 漏 0 改 1 字符)。详细记录 4 段 (症状/触发场景/修复/预防)：

- **症状**：polish 期给 settings_menu 加新 1 动作 (e.g. 切分 hot key 单独绑 / 6th verb 接入 / 重置默认按键 / 玩家偏好键) 时, 最常见的 fragile 是「(a) `ACTION_NAMES` 字典漏 1 action → `_save_settings` 循环 0 写, 玩家改键后 0 持久化」— T086 (#44) 落地 5 动作 (move_left / move_right / jump / verb_primary / verb_secondary) 时显式用 5 行 dict literal `const ACTION_NAMES := {"move_left": "向左移动", ..., "verb_secondary": "次声波 (E)"}`, 任何"加 1 动作但漏 ACTION_NAMES"polish 都会让玩家在 CONTROLS tab 看不到新动作, `_save_settings` 循环 0 写新 action, 玩家改键后重启游戏 0 还原, 视觉组"按键重映射"语义破坏;「(b) `ACTION_CATEGORY` 字典漏 1 action → `CATEGORY_RENDER_ORDER` 渲染时 0 分组」— T194 (#112) 落地 5 verb 分组标题 (移动 / 声波能力 / 交互) 时显式用 `const ACTION_CATEGORY := {"move_left": "movement", ..., "interact": "interaction"}` + `const CATEGORY_RENDER_ORDER := [{"name": "移动", "key": "movement", "color": ...}, ...]` 3 段顺序, 任何"加 1 category 段但漏 ACTION_CATEGORY"polish 都会让玩家在 CONTROLS tab 看到 1 段无标题的 0 分组, 视觉组"按键按 category 分组"语义破坏;「(c) `_save_settings` 写 [input] section 但 `_load_settings` 0 读 → 玩家改键后重启游戏 0 还原」— T086 落地时显式用 `_save_settings` 4 行循环 `for action in ACTION_NAMES.keys(): var events := InputMap.action_get_events(action); if events.size() > 0: cfg.set_value("input", action, events[0])` (1112-1115) + `_load_settings` 5 行循环 `for action in ACTION_NAMES.keys(): var ev = cfg.get_value("input", action, null); if ev is InputEvent: InputMap.action_erase_events(action); InputMap.action_add_event(action, ev)` (1234-1238), 任何"加 [input] 段但漏 _load_settings"polish 都会让玩家首次开 settings 时新键 0 还原, 永远走 Godot 默认 InputMap;「(d) 漏 `if events.size() > 0` 守卫 → 0 events 的 action 存 `null` 到 cfg」— T086 落地时显式用 `if events.size() > 0:` 守卫避免存 `null`, 任何"漏守卫"polish 都会让 cfg 文件 [input] section 含 `action = null` 行, `_load_settings` `if ev is InputEvent` 守卫能挡 (null 0 走 erase_events + add_event) 但 cfg 文件 0 优雅 + Godot 4 ConfigFile 序列化 `null` 时 0 一致;「(e) 漏 `InputMap.action_erase_events` 预清 → 还原时残留 Godot 默认 InputMap」— T086 落地时显式用 `InputMap.action_erase_events(action); InputMap.action_add_event(action, ev)` 2 步 (先清再添), 任何"漏 action_erase_events"polish 都会让玩家重映射 1 个键后 InputMap 同时有 Godot 默认键 + 玩家重映射键, 玩家按重映射键时同时触发 2 个 action, 视觉组"按键重映射 1:1 唯一"语义破坏;「(f) 漏 `if ev is InputEvent` 守卫 → `cfg.get_value` 拿 `null` 抛错」— T086 落地时显式用 `if ev is InputEvent` 守卫避免 `null.action_erase_events` 抛错, 任何"漏守卫"polish 都会让 fresh clone 0 cfg 启动 settings 时 Godot 4 ConfigFile `Key not found` 走 `get_value` 兜底 `null` 抛错, 玩家首次开 settings 0 启动。
- **触发场景**：T037 (#23) + T086 (#44) + T194 (#112) + T195 (#112) + T196 (#113) + T202.B (#121) + T205 (#122) + T239 (#157) [input] section 6 件套落地需同步 5 步: (1) `src/scripts/settings_menu.gd:113-128` `const ACTION_NAMES := {...}` 字典 (5-7 actions: move_left/right/jump + verb_primary/verb_secondary + interaction, 16 行) —— T086 落地 5 动作 + T194 扩 verb_secondary/tertiary 等, 任何"加新动作"polish 必须先加 ACTION_NAMES 1 行 `dict literal`; (2) `src/scripts/settings_menu.gd:130-143` `const ACTION_CATEGORY := {...}` 字典 (按 category 3 段分组: movement / verb / interaction, 14 行) —— T194 落地 5 verb 分组, 任何"加新 category 段"polish 必须先加 ACTION_CATEGORY 1 行 + CATEGORY_RENDER_ORDER 1 行 `dict literal`; (3) `src/scripts/settings_menu.gd:145-183` `const CATEGORY_RENDER_ORDER := [...]` 数组 (3 段 render 顺序, 含 name + key + color 4 字段, 39 行) —— T194 落地 3 段, 颜色与 STYLE_GUIDE.md 的 Pale Resonance / Amber Voice / Verdant Echo 3 主色对应; (4) `src/scripts/settings_menu.gd:185-230` `const _DEFAULT_BINDINGS := {...}` 字典 (InputMap 默认值, 用于 "重置默认按键" 按钮, 46 行) —— T086 落地 5 动作默认 binding, 任何"加新动作"polish 必须加 1 行 `dict literal` 描述默认 binding; (5) `src/scripts/settings_menu.gd:1111-1115` `_save_settings()` 末尾 5 行 `[input]` section N key 写 —— 严格 4 步: (5.1) `# Save input map` 注释 1 行 (1111) + (5.2) `for action in ACTION_NAMES.keys():` 循环 1 行 (1112) + (5.3) `var events := InputMap.action_get_events(action)` 1 行 (1113) + (5.4) `if events.size() > 0:` 守卫 1 行 (1114) + (5.5) `cfg.set_value("input", action, events[0])` 1 行 (1115), 5 行 0 漏 0 错位 0 改 1 字符; (6) `src/scripts/settings_menu.gd:1233-1238` `_load_settings()` 末尾 6 行 `[input]` section N key 读 + InputMap 还原 —— 严格 6 步: (6.1) `# Load input map` 注释 1 行 (1233) + (6.2) `for action in ACTION_NAMES.keys():` 循环 1 行 (1234) + (6.3) `var ev = cfg.get_value("input", action, null)` 1 行 (1235) + (6.4) `if ev is InputEvent:` 守卫 1 行 (1236) + (6.5) `InputMap.action_erase_events(action)` 1 行 (1237) + (6.6) `InputMap.action_add_event(action, ev)` 1 行 (1238), 6 行 0 漏 0 错位 0 改 1 字符。5 步 0 漏 1 = 玩家改键后 0 持久化 / 玩家首次开 settings 0 还原 / 玩家重映射 1 个键后按 1 下触发 2 个 action / fresh clone 0 cfg 启动 settings 抛错 / CONTROLS tab 看到 0 分组的 1 段 0 标题。
- **修复**：`src/scripts/settings_menu.gd` [input] section 6 件套 (ACTION_NAMES 字典 + ACTION_CATEGORY 字典 + CATEGORY_RENDER_ORDER 数组 + _DEFAULT_BINDINGS 字典 + `_save_settings` 末尾 4 步循环写 + `_load_settings` 末尾 5 步循环读 + InputMap 还原) polish 模式 (T037 + T086 + T194 + T195 + T196 + T202.B + T205 + T239 跨 8 任务 ~10 轮落地)：
  - **T037 (#23) `_save_settings` + `_load_settings` 4 audio + 2 video 6 key 基础** — T037 落地 4 audio slider + 2 video 6 key, 0 触碰 [input] section (T037 落地时 T086 还没加, [input] 段 0 写 0 读)。0 触碰 §9.6.12 4 tab 状态机 (4 slider 是 `_audio_panel` 内子节点); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T037 落地时 T202.B 还没加)。
  - **T086 (#44) `[input]` section 5 动作 + `_save_settings` 4 行循环写 + `_load_settings` 5 行循环读 + InputMap 还原** — `const ACTION_NAMES` 字典 5 动作 (move_left / move_right / jump / verb_primary / verb_secondary, 5 行 dict literal) + `const _DEFAULT_BINDINGS` 字典 5 动作默认 binding (5 行 dict literal) + `_save_settings` 末尾 4 行 `[input]` section 循环 (for / action_get_events / size() > 0 守卫 / set_value) + `_load_settings` 末尾 5 行 `[input]` section 循环 (for / get_value / is InputEvent 守卫 / action_erase_events / action_add_event), 0 触碰 T037 4 audio + 2 video 既有 6 key (T086 0 改 [audio] / [video] section 任何 key, 仅扩 1 section [input])。T086 落地时 T194 还没加 ACTION_CATEGORY + CATEGORY_RENDER_ORDER, 0 触碰 category 分组, `_save_settings` / `_load_settings` 直接走 `for action in ACTION_NAMES.keys()` 循环 0 走 category 段。
  - **T194 (#112) ACTION_CATEGORY 字典 + CATEGORY_RENDER_ORDER 数组 3 段 (移动 / 声波能力 / 交互) 加 CONTROLS tab 渲染** — `const ACTION_CATEGORY` 字典 5 动作按 category 3 段分组 (movement / verb / interaction, 14 行) + `const CATEGORY_RENDER_ORDER` 数组 3 段 render 顺序 (含 name + key + color 4 字段, 颜色与 STYLE_GUIDE.md 3 主色对应: Pale Resonance / Amber Voice / Verdant Echo, 39 行), 让玩家在 CONTROLS tab 看到按键按 3 段分组渲染 (移动: A/D/Space / 声波能力: Q/E / 交互: F), 视觉组"按键按 category 分组渲染"100% 闭环。0 触碰 T086 5 动作既有 5 行 `const ACTION_NAMES` (T194 0 改 5 动作任何字段, 仅加 2 个 const 字典 + 数组); 0 触碰 T086 `_save_settings` 末尾 4 行 + `_load_settings` 末尾 5 行 [input] section 循环 (T194 0 改 `_save_settings` / `_load_settings` 任何字段, 仅 `const ACTION_CATEGORY` + `const CATEGORY_RENDER_ORDER` 2 个新 const); 0 触碰 T037 4 audio + 2 video 既有 6 key (T194 0 改 [audio] / [video] section 任何 key)。
  - **T195 (#112) `[accessibility] reduce_shake / reduce_flash` 2 key** — T195 落地 accessibility 2 key (reduce_shake / reduce_flash), 0 触碰 [input] section 任何 const / var / 函数 (T195 0 改 ACTION_NAMES / ACTION_CATEGORY / CATEGORY_RENDER_ORDER / _DEFAULT_BINDINGS 任何字段, 0 改 `_save_settings` 末尾 4 行 + `_load_settings` 末尾 5 行 [input] section 循环, 0 改 `InputMap.action_erase_events` / `InputMap.action_add_event` 调用)。T195 落地 `_on_reduce_*_toggled` 2 个 handler + `ScreenShake.set_reduce_*` 跨 autoload live-push, 与 [input] section 0 互混 0 复用 0 共享。
  - **T196 (#113) `[accessibility] reduce_vibration` 1 key** — T196 加 1 个 reduce_vibration 控件 + 1 个 `_on_reduce_vibration_toggled` handler + `ScreenShake.set_reduce_vibration` 1 个跨 autoload live-push, 0 触碰 [input] section 任何 const / var / 函数 (与 T195 完全同模式, 1 key 同步 0 触碰 1 行)。
  - **T202.B (#121) `[accessibility] reduce_all` 1 key 加主开关数据独立** — T202.B 落地 ReduceAllCheck 三态 (enabled / disabled / indeterminate) + 3 子项 reduce_*_check 联动 + 防递归 `_syncing_from_master` 守卫, 0 触碰 [input] section 任何 const / var / 函数 (T202.B 0 改 ACTION_NAMES / ACTION_CATEGORY / CATEGORY_RENDER_ORDER / _DEFAULT_BINDINGS 任何字段, 0 改 `_save_settings` / `_load_settings` 任何 key, 0 改 `InputMap.action_*` 调用)。T202.B 落地时 §9.6.11 三态 100% 闭环, 与 [input] section 0 互混 0 复用 0 共享。
  - **T205 (#122) `[audio] 4 label text 还原`** — T205 加 4 行 `_refresh_audio_volume_label(_X_label, _X_slider.value, "X")` (master / sfx / music / ambience), 0 触碰 [input] section 任何 const / var / 函数 (T205 0 改 ACTION_NAMES / ACTION_CATEGORY / CATEGORY_RENDER_ORDER / _DEFAULT_BINDINGS 任何字段, 0 改 `_save_settings` / `_load_settings` 任何 key, 0 改 `InputMap.action_*` 调用)。
  - **T239 (#157) `[audio] music preview button` (0 触碰持久化层)** — T239 加 `_music_preview_btn` 1 个 Button + `_on_music_preview_pressed` 1 个 handler + 3s 预览 1 个 BGM 主题, 0 触碰 [input] section 任何 const / var / 函数 (T239 0 改 ACTION_NAMES / ACTION_CATEGORY / CATEGORY_RENDER_ORDER / _DEFAULT_BINDINGS 任何字段, 0 改 `_save_settings` / `_load_settings` 任何 key, 0 改 `InputMap.action_*` 调用)。
  - 0 副作用：T037 / T086 / T194 / T195 / T196 / T202.B / T205 / T239 任何 1 key / 1 const / 1 行 0 触碰既有 N-1 元素 (`_save_settings` 写 N-1 行 0 改, 仅新加 1 行; `_load_settings` 读 N-1 行 0 改, 仅新加 1 行; `const ACTION_NAMES` 5 动作 0 改, 仅新加 1 动作; `const ACTION_CATEGORY` 5 动作 0 改, 仅新加 1 动作; `const CATEGORY_RENDER_ORDER` 3 段 0 改, 仅新加 1 段); [input] section 5 件套 (ACTION_NAMES + ACTION_CATEGORY + CATEGORY_RENDER_ORDER + _DEFAULT_BINDINGS + 4 行 save + 5 行 load) 0 改 0 删 0 重排; last-writer-wins 模式 0 改 (settings_menu vs SaveSystem 都写 [gameplay] section, [input] section 仅 settings_menu 写 0 互混); 0 触碰 §9.6.13 4 section × N key 双源映射 (T086 + T194 0 改 [audio] / [video] / [gameplay] / [accessibility] section 任何 key, 仅 [input] section 扩 5 行); 0 触碰 §9.6.12 4 tab 状态机 (T086 + T194 0 触碰 7 件套 1:1 严格分离); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T195 + T196 + T202.B 0 触碰 `_syncing_from_master` 守卫 + `_reduce_all` 数据独立); 0 触碰 §9.6.9 / §9.6.10 PauseMenu 跨面板 hover 反馈。
- **预防**：
  1. 任何 polish 期给 settings_menu 加新 1 动作 (e.g. 切分 hot key 单独绑 / 6th verb 接入 / 重置默认按键 / 玩家偏好键) 时**必须**严格按 5 件套 1:1 复制既有 [input] section 6 件套同步模式: (1) `const ACTION_NAMES` 字典加 1 行 `dict literal` (0 漏 1 action) + (2) `const ACTION_CATEGORY` 字典加 1 行 `dict literal` (按 category 3 段分组: movement / verb / interaction) + (3) `const _DEFAULT_BINDINGS` 字典加 1 行 `dict literal` (描述默认 binding, 用于 "重置默认按键" 按钮) + (4) `const CATEGORY_RENDER_ORDER` 数组若新增 1 category 段则加 1 行 `dict literal` (含 name + key + color 4 字段, 颜色与 STYLE_GUIDE.md 主色对应) + (5) `_save_settings` 末尾 4 行循环 (`for / action_get_events / size() > 0 守卫 / set_value`) 与 `_load_settings` 末尾 5 行循环 (`for / get_value / is InputEvent 守卫 / action_erase_events / action_add_event`) 0 触碰 (T086 + T194 0 改 4+5 行循环, 仅 ACTION_NAMES / ACTION_CATEGORY / _DEFAULT_BINDINGS / CATEGORY_RENDER_ORDER 4 个 const 加新 1 行)。建议在 `_save_settings` 末尾 `[input]` 循环上方 docblock 写明 "5 件套 1:1 同步 (const + const + const + const + 4 行 save + 5 行 load)" 硬约束。
  2. [input] section 6 件套 0 漏 1 边 —— `const ACTION_NAMES` 5 动作 0 漏 0 改 1 字符; `const ACTION_CATEGORY` 5 动作 0 漏 0 改 1 字符; `const CATEGORY_RENDER_ORDER` 3 段顺序 (移动 → 声波能力 → 交互) 0 改; `const _DEFAULT_BINDINGS` 5 动作 0 漏 0 改 1 字符; `_save_settings` 末尾 4 行循环 0 漏 0 改 1 行 (`for / action_get_events / size() > 0 / set_value`); `_load_settings` 末尾 5 行循环 0 漏 0 改 1 行 (`for / get_value / is InputEvent / action_erase_events / action_add_event`)。任何"加新动作但漏 1 边"polish 都会让玩家改键后 0 持久化 / 玩家首次开 settings 0 还原 / 玩家重映射 1 个键后按 1 下触发 2 个 action, source-grep 验证 4 个 const + 4+5 行循环 0 漏 0 改 1 字符 (按 5 动作 / 3 段 category 1:1 对齐)。
  3. `if events.size() > 0` 守卫 0 删 (T086 落地时显式用 `if events.size() > 0:` 守卫避免存 `null` 到 cfg) —— 任何"删 size() > 0 守卫"polish 都会让 0 events 的 action 也存 `null` 到 cfg 文件, `_load_settings` `if ev is InputEvent` 守卫能挡 (null 0 走 erase_events + add_event) 但 cfg 文件 0 优雅 + Godot 4 ConfigFile 序列化 `null` 时 0 一致, source-grep 验证 `_save_settings` 末尾 4 行循环 0 漏 `events.size() > 0` 守卫 0 改 1 字符。
  4. `if ev is InputEvent` 守卫 0 删 (T086 落地时显式用 `if ev is InputEvent:` 守卫避免 `null.action_erase_events` 抛错) —— 任何"删 is InputEvent 守卫"polish 都会让 fresh clone 0 cfg 启动 settings 时 `cfg.get_value("input", action, null)` 拿到 `null` 后 `null.action_erase_events` 抛错, Godot 4 ConfigFile `Key not found` 走 `get_value` 兜底 `null` 但 0 优雅, source-grep 验证 `_load_settings` 末尾 5 行循环 0 漏 `is InputEvent` 守卫 0 改 1 字符。
  5. `InputMap.action_erase_events` 预清 0 删 (T086 落地时显式用 `InputMap.action_erase_events(action); InputMap.action_add_event(action, ev)` 2 步先清再添) —— 任何"删 action_erase_events"polish 都会让玩家重映射 1 个键后 InputMap 同时有 Godot 默认键 + 玩家重映射键, 玩家按重映射键时同时触发 2 个 action, 视觉组"按键重映射 1:1 唯一"语义破坏, source-grep 验证 `_load_settings` 末尾 5 行循环 0 漏 `action_erase_events` 0 漏 `action_add_event` 0 改 1 行 (2 步 1:1 严格分离)。
  6. `for action in ACTION_NAMES.keys()` 循环 0 改硬编码 (T086 落地时显式用 `for action in ACTION_NAMES.keys()` 循环 0 写 5 行动词硬编码) —— 任何"改硬编码"polish 都会让"加 1 动作"必须改 2 处 (const + 循环), 任何"加 1 动作但漏循环硬编码"polish 都会让新动作 0 持久化, source-grep 验证 `_save_settings` 末尾 + `_load_settings` 末尾 2 个 `for action in ACTION_NAMES.keys()` 循环 0 改 0 删 0 重排。
  7. T037 / T086 / T194 / T195 / T196 / T202.B / T205 / T239 5 件套 0 触碰既有 N-1 元素 —— `_save_settings` 写 N-1 行 0 改, 仅新加 1 行; `_load_settings` 读 N-1 行 0 改, 仅新加 1 行; `const ACTION_NAMES` N-1 动作 0 改, 仅新加 1 动作; `const ACTION_CATEGORY` N-1 动作 0 改, 仅新加 1 动作; `const CATEGORY_RENDER_ORDER` N-1 段 0 改, 仅新加 1 段; `const _DEFAULT_BINDINGS` N-1 动作 0 改, 仅新加 1 动作; `_ready` 末尾 `_load_settings()` + `_on_close` 末尾 `_save_settings()` 0 触碰 (T037 落地时已设, 后续 polish 0 改 0 删 0 重排)。0 触碰 §9.6.13 4 section × N key 双源映射 (任何 const / var 0 触碰 5 件套 1:1 严格分离); 0 触碰 §9.6.12 4 tab 状态机 (任何 const / var 0 触碰 7 件套 1:1 严格分离); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T195 + T196 + T202.B 0 触碰 `_syncing_from_master` 守卫 + `_reduce_all` 数据独立); 0 触碰 §9.6.9 / §9.6.10 PauseMenu 跨面板 hover 反馈 (settings_menu 与 pause_menu 是 2 个独立 menu, 0 复用任何字段)。

### 9.6.15 SaveSystem `audit_save_slots()` 4 状态巡检 (empty / ok / corrupted / drift) + `_refresh_profile_audit` 1 行 4 字段 + `has_method` 跨调用方守卫 + `call_deferred` 启动延迟 polish 模式 (T088 + T131 + T224 + T229 + T265 跨 5 任务 ~40 轮落地) 文档化

> §9.6.13 记录 settings_menu `_save_settings` ↔ `_load_settings` 1:1 同步; §9.6.14 记录 settings_menu `[input]` section 6 件套持久化。本节跳出 settings_menu 范畴, 记录 SaveSystem autoload 的 5 slot 存档健康度巡检 polish 模式 —— `audit_save_slots()` 公共方法返回 4 状态 (empty / ok / corrupted / drift) + 4 ids arrays Dictionary, 走 5 件套 1:1 同步: (1) `SaveSystem.audit_save_slots()` 公共入口 (1 行委托 `_audit_save_slots()`), (2) `_audit_save_slots()` 内部 worker (返回原始 Dictionary), (3) `SaveSystem._ready()` 末尾 `call_deferred("audit_save_slots")` 启动延迟 1 行 (避免 _ready 半完成时 IO 阻塞), (4) `pause_menu.gd _refresh_profile_audit()` 1 行 4 字段渲染 (ok / 损坏 / 漂移 / 空, 中点 `·` 分隔) + 3 档色域分工 (损坏>0 暖红 / 漂移>0 暖黄 / 全 ok 暖白), (5) `title_screen.gd _ready()` `has_method("audit_save_slots")` 守卫 + 1 行调用 (TitleScreen re-entry 时再 audit 一次, 玩家继续前看到 fresh 巡检结果)。T088 (#45) 落地 SLOT_COUNT 顶层常量 3→5 + T131 (#80) 落地 ProfileAudit 1 行 + T224 (#146) 落地 `audit_save_slots` 4 状态巡检 + T229 (#149) 落地 `_refresh_profile_audit` PauseMenu 渲染 + T265 (#186) 落地 F002 self-test commit hook 集成 (5 任务跨 ~40 轮反复落地), 任何 polish 期给 SaveSystem 加新 1 状态 (e.g. "stale" 30 天没动 / "pinned" 玩家 pin 防删 / "shared" 多人共享 / "encrypted" 加密存档) 或加新 1 调用方 (e.g. settings_menu 加 "存档健康" 面板 / SaveLoadMenu 加 "巡检" 按钮 / DeathScreen 死亡时 audit) 时**必须**严格按 5 件套 1:1 复制既有 audit_save_slots 4 状态巡检模式, 0 漏 1 处同步 (e.g. 漏 `call_deferred` 启动延迟 → SaveSystem._ready 期间 IO 阻塞主线程, 玩家启动黑屏 +0.1-0.3s, 视觉组"60 秒竖切"语义破坏; 漏 `has_method` 守卫 → SaveSystem autoload 在 headless / 反序列化失败时为 null, `_refresh_profile_audit` 调 `SaveSystem.audit_save_slots()` 抛 NPE, 玩家打开 PauseMenu 看存档 1 行变 null 黑屏; 漏 4 状态 (empty / ok / corrupted / drift) 任一 → 巡检结果 0 完整, 例如漏 corrupted 计数 → 玩家存档损坏但 ProfileAudit 1 行显示 "5 ok · 0 损坏 · 0 漂移 · 0 空" 误导玩家以为存档健康; 漏 4 ids arrays (ok_ids / corrupted_ids / drift_ids / empty_ids) → 巡检日志仅给计数, tech-savvy 玩家切到 Output panel 看 push_warning 行也 0 知道具体哪个 slot 损坏, debug 0 友好; 漏 1:1 双层 (公共 `audit_save_slots` + 内部 `_audit_save_slots`) → 测试脚本无法直接调 worker, 必须走公共层 + 承受 push_warning 噪音, smoke test 0 干净; 漏 3 档色域分工 (损坏>0 暖红 / 漂移>0 暖黄 / 全 ok 暖白) → 玩家看到损坏存档但 PauseMenu 1 行仍是暖白, 视觉组"存档健康优先级"语义破坏; 漏 `int(report.get("ok", 0))` 0 漏 default → report 字典缺 key 时 (例如 fresh SaveSystem 4 状态未初始化) `_refresh_profile_audit` 报 "Invalid get index" 错误, 玩家打开 PauseMenu 抛错; 漏 `SLOT_COUNT` 顶层常量 0 改硬编码 → 加 slot 6 时 audit_save_slots 仅巡检前 5 slot, slot 6 永远 0 巡检, source-grep 验证 5 件套 0 漏 0 改 1 字符 (按 SLOT_COUNT 1:1 严格对齐))。详细记录 4 段 (症状/触发场景/修复/预防)：

- **症状**：polish 期给 SaveSystem 加新 1 状态 (e.g. "stale" 30 天没动 / "pinned" 玩家 pin 防删 / "shared" 多人共享 / "encrypted" 加密存档) 时, 最常见的 fragile 是「(a) 漏 `call_deferred` 启动延迟 → SaveSystem._ready 期间 IO 阻塞主线程」— T131 (#80) 落地时显式用 `call_deferred("audit_save_slots")` 1 行 (save_system.gd:133), 任何"删 call_deferred 改直接调"polish 都会让 SaveSystem._ready() 期间 audit_save_slots 直接调, 5 slot × read + CRC32 + 字典 alloc 阻塞主线程 0.1-0.3s, 玩家启动黑屏 + 视觉组"60 秒竖切"语义破坏;「(b) 漏 `has_method` 守卫 → SaveSystem autoload 为 null 时 NPE」— T229 (#149) 落地时显式用 `if not SaveSystem or not SaveSystem.has_method("audit_save_slots"):` 守卫, 任何"删 has_method 守卫"polish 都会让 SaveSystem autoload 在 headless / 反序列化失败时为 null, `_refresh_profile_audit` 调 `SaveSystem.audit_save_slots()` 抛 NPE, 玩家打开 PauseMenu 看存档 1 行变 null 黑屏, 视觉组"PauseMenu 0 崩溃"语义破坏;「(c) 漏 4 状态任一 → 巡检结果 0 完整」— T224 (#146) 落地时显式用 `var report := {"empty": 0, "ok": 0, "corrupted": 0, "drift": 0, "corrupted_ids": [] as Array, "drift_ids": [] as Array, "empty_ids": [] as Array, "ok_ids": [] as Array, "total": SLOT_COUNT}` 4 + 4 = 8 key 1:1 完整 Dictionary, 任何"漏 1 状态 (e.g. 加 stale 但漏 0 改 4 状态)"polish 都会让 audit 报告缺 key, `_refresh_profile_audit` `int(report.get("stale", 0))` 拿 default 0 但 stale 实际计数 >0, 玩家看不到"30 天没动"提醒;「(d) 漏 4 ids arrays → 巡检日志仅给计数 0 知道哪个 slot 损坏」— T224 落地时显式用 `(report["corrupted_ids"] as Array).append(i)` 等 4 处 append, 任何"漏 ids arrays"polish 都会让 push_warning 行仅打印计数, tech-savvy 玩家切到 Output panel 也 0 知道具体哪个 slot 损坏, debug 0 友好;「(e) 漏 1:1 双层 (公共 audit_save_slots + 内部 _audit_save_slots) → smoke test 走公共层 + 承受 push_warning 噪音」— T224 落地时显式用 2 函数 1:1 严格分离 (公共 `audit_save_slots` 1 行委托 + `push_warning` 1 行; 内部 `_audit_save_slots` 返回 Dictionary 0 副作用), 任何"合并 2 函数"polish 都会让 smoke test 走公共层承受 push_warning 噪音 (虽然 push_warning 是 stderr, 但 CI log 0 干净);「(f) 漏 3 档色域分工 (损坏>0 暖红 / 漂移>0 暖黄 / 全 ok 暖白) → 玩家看到损坏存档但 PauseMenu 1 行仍是暖白」— T229 落地时显式用 3 档 `if corrupted_n > 0: color = Color(0.85, 0.45, 0.4, 1.0); elif drift_n > 0: color = Color(0.85, 0.71, 0.43, 1.0); else: color = Color(0.875, 0.835, 0.784, 1.0)`, 任何"统一暖白"polish 都会让损坏存档视觉组"存档健康优先级"语义破坏;「(g) 漏 `int(report.get("ok", 0))` 0 漏 default → report 字典缺 key 时抛错」— T229 落地时显式用 4 行 `int(report.get("ok", 0))` / `int(report.get("corrupted", 0))` / `int(report.get("drift", 0))` / `int(report.get("empty", 0))` 0 漏 default 0 漏 1 行, 任何"漏 default"polish 都会让 report 字典缺 key 时 (例如 fresh SaveSystem 4 状态未初始化) `int(report.get("ok"))` 报 "Invalid get index" 错误, 玩家打开 PauseMenu 抛错;「(h) 漏 `SLOT_COUNT` 顶层常量 0 改硬编码 5 → 加 slot 6 时 audit 0 巡检」— T088 (#45) 落地时显式用 `SLOT_COUNT = 5` 顶层 const, 任何"硬编码 5"polish 都会让 audit_save_slots 写 `for i in range(5):` 0 走 SLOT_COUNT, 加 slot 6 时 audit 仅巡检前 5 slot, slot 6 永远 0 巡检, 玩家存档损坏 slot 6 但 ProfileAudit 1 行显示 "5 ok" 误导。
- **触发场景**：T088 (#45) + T131 (#80) + T224 (#146) + T229 (#149) + T265 (#186) audit_save_slots 5 件套落地需同步 5 步：(1) `src/autoload/save_system.gd:800-801` `func audit_save_slots() -> Dictionary:` 公共入口 2 行 (`return _audit_save_slots()` 1 行委托, push_warning 1 行) —— T224 落地时显式用 2 函数 1:1 严格分离, 公共层仅委托 + 日志, 内部 worker 负责实际巡检; (2) `src/autoload/save_system.gd:803-852` `func _audit_save_slots() -> Dictionary:` 内部 worker 50 行 —— 严格 8 步: (2.1) 初始化 report dict 8 key (empty / ok / corrupted / drift + 4 ids arrays + total) 1 行, (2.2) `for i in range(SLOT_COUNT):` 循环 1 行 (走 SLOT_COUNT 顶层 const 0 走硬编码 5), (2.3) `var path := _slot_path(i)` 1 行, (2.4) `if not FileAccess.file_exists(path):` 守卫 + empty 计数 + ids append 3 行, (2.5) `var data := _read_json(path)` 1 行, (2.6) `if data.is_empty():` 守卫 + corrupted 计数 + ids append 3 行, (2.7) drift 检查 3 字段 (has_version / has_room / has_meta) 5 行 + drift 计数 + ids append 2 行, (2.8) ok 计数 + ids append 2 行, (2.9) 末尾 `if report["corrupted"] > 0 or report["drift"] > 0: push_warning(...)` 7 行 + else print 1 行 0 漏 0 错位; (3) `src/autoload/save_system.gd:133` `call_deferred("audit_save_slots")` 启动延迟 1 行 (在 _ready 末尾) —— T131 落地时显式用 call_deferred 避免 _ready 半完成时 IO 阻塞, 任何"删 call_deferred 改直接调"polish 都会让 5 slot 巡检在 _ready 期间同步执行, 主线程 0.1-0.3s 阻塞, 玩家启动黑屏; (4) `src/scripts/pause_menu.gd:1705-1732` `_refresh_profile_audit()` 渲染 28 行 —— 严格 8 步: (4.1) `if not _profile_audit: return` 守卫 2 行, (4.2) `if not SaveSystem or not SaveSystem.has_method("audit_save_slots"):` 守卫 3 行 + "—" 占位 + 暖白 0 灰化, (4.3) `var report: Dictionary = SaveSystem.audit_save_slots()` 1 行, (4.4) 4 行 `int(report.get("ok", 0))` / `int(report.get("corrupted", 0))` / `int(report.get("drift", 0))` / `int(report.get("empty", 0))` 0 漏 default, (4.5) `var color: Color` + 3 档 `if corrupted_n > 0: ... elif drift_n > 0: ... else: ...` 6 行色域分工, (4.6) `_profile_audit.text = "存档  %d ok  ·  %d 损坏  ·  %d 漂移  ·  %d 空" % [ok_n, corrupted_n, drift_n, empty_n]` 1 行 4 字段 (中点 "·" 与 ProfileQuickStats 4 段 + ProfileRecentList 7 字段行风格完全一致), (4.7) `_profile_audit.add_theme_color_override("font_color", color)` 1 行; (5) `src/scripts/title_screen.gd:61-62` `if SaveSystem and SaveSystem.has_method("audit_save_slots"): SaveSystem.audit_save_slots()` 2 行 (TitleScreen re-entry 时再 audit 一次) —— T265 落地时显式用 has_method 守卫避免 headless / 反序列化失败时 NPE, 玩家继续前看到 fresh 巡检结果。5 件套 0 漏 1 = 玩家启动黑屏 0.1-0.3s / 玩家打开 PauseMenu 抛 NPE / 巡检结果 0 完整 / 巡检日志 0 知道哪个 slot 损坏 / smoke test 走公共层 + 承受 push_warning 噪音 / 损坏存档视觉组"存档健康优先级"语义破坏 / report 字典缺 key 抛 "Invalid get index" 错误 / 加 slot 6 永远 0 巡检。
- **修复**：`SaveSystem.audit_save_slots` 5 件套 (公共入口 + 内部 worker + 启动 call_deferred + PauseMenu 渲染 + TitleScreen 守卫) polish 模式 (T088 + T131 + T224 + T229 + T265 跨 5 任务 ~40 轮落地)：
  - **T088 (#45) `SLOT_COUNT = 5` 顶层 const** — T088 把 3 → 5 槽位扩展时显式用 `const SLOT_COUNT := 5` 顶层常量 + `for i in range(SLOT_COUNT):` 循环 0 走硬编码 3/5, 任何"加 slot 6"polish 必须改 SLOT_COUNT 1 行 + audit_save_slots 0 触碰。0 触碰 §9.6.13 settings_menu `[gameplay]` section (autosave 3 key 走 `SaveSystem.SLOT_COUNT` 0 写死 5); 0 触碰 §9.6.14 settings_menu `[input]` section (T086 0 改 SaveSystem 任何字段, 0 改 SLOT_COUNT)。
  - **T131 (#80) `call_deferred("audit_save_slots")` 启动延迟 1 行** — T131 落地 ProfileAudit 1 行 + SaveSystem._ready 末尾 1 行 `call_deferred("audit_save_slots")`, 让 5 slot 巡检在 _ready 完成 + 首帧后才执行, 避免 IO 阻塞主线程 0.1-0.3s, 玩家启动黑屏 → 60 秒竖切内 0 卡顿。0 触碰 T088 SLOT_COUNT 顶层常量 (T131 0 改 SLOT_COUNT, 仅加 1 行 call_deferred)。
  - **T224 (#146) `audit_save_slots` 4 状态巡检 + 双层 (公共 + 内部)** — T224 落地 2 函数 1:1 严格分离: 公共 `audit_save_slots` 1 行委托 + push_warning 1 行; 内部 `_audit_save_slots` 50 行 4 状态 (empty / ok / corrupted / drift) + 4 ids arrays + total + drift 检查 3 字段 (has_version / has_room / has_meta) 8 步 1:1 严格分离。0 触碰 T088 SLOT_COUNT 顶层常量 (T224 0 改 SLOT_COUNT, 仅加 2 函数 50 行); 0 触碰 T131 call_deferred (T224 0 改 call_deferred, 仅扩 audit_save_slots 函数体)。
  - **T229 (#149) `pause_menu.gd _refresh_profile_audit` 1 行 4 字段 + 3 档色域分工** — T229 加 28 行渲染 (4 字段中点 "·" + 3 档色域分工 + has_method 守卫 + 4 行 int(report.get) 0 漏 default), 0 触碰 T224 audit_save_slots 2 函数 (T229 0 改 save_system.gd 任何字段, 仅 pause_menu.gd 加 1 函数 28 行)。T229 落地时 §9.6.9 / §9.6.10 PauseMenu 跨面板 hover 反馈已落地, _refresh_profile_audit 是独立数据源 (本函数看存档健康, aggregate 看 run history, 2 个完全独立), 0 复用任何字段。
  - **T265 (#186) `title_screen.gd` `has_method` 守卫 + 1 行 audit** — T265 加 2 行守卫 + 调用, 让 TitleScreen re-entry 时再 audit 一次, 玩家继续前看到 fresh 巡检结果。0 触碰 T088 + T131 + T224 + T229 任何 const / var / 函数 (T265 0 改 save_system.gd 任何字段, 0 改 pause_menu.gd 任何字段, 仅 title_screen.gd 加 2 行)。
  - 0 副作用：T088 / T131 / T224 / T229 / T265 任何 const / var / 函数 0 触碰既有 N-1 元素 (`SLOT_COUNT` 顶层常量 0 改, 仅扩 1 行; `audit_save_slots` 公共入口 0 改, 仅新加 1 函数; `_audit_save_slots` 内部 worker 0 改既有 4 状态, 仅新加 N 状态; `call_deferred` 启动延迟 0 删 0 改 1 行; `_refresh_profile_audit` 1 函数 0 改既有 4 字段, 仅新加 1 字段; `_ready` 末尾 `call_deferred` + `_refresh_profile_audit` 0 改 0 删 0 重排); 4 状态顺序 (empty / ok / corrupted / drift) 0 改; 4 ids arrays (empty_ids / ok_ids / corrupted_ids / drift_ids) 0 改; 3 档色域分工 (损坏>0 暖红 / 漂移>0 暖黄 / 全 ok 暖白) 0 改; `int(report.get("X", 0))` 0 漏 default 0 改 1 字符; 中点 "·" 分隔符 0 改; 0 触碰 §9.6.13 settings_menu 4 section × N key (T131 + T224 + T229 + T265 0 改 [audio] / [video] / [gameplay] / [accessibility] / [input] section 任何 key, 仅 save_system.gd + pause_menu.gd + title_screen.gd 3 文件); 0 触碰 §9.6.14 settings_menu `[input]` section 6 件套 (T131 + T224 + T229 + T265 0 改 ACTION_NAMES / ACTION_CATEGORY / CATEGORY_RENDER_ORDER / _DEFAULT_BINDINGS 任何 const, 0 改 `_save_settings` 末尾 4 行 + `_load_settings` 末尾 5 行 [input] section 循环); 0 触碰 §9.6.12 4 tab 状态机 (T131 + T224 + T229 + T265 0 触碰 settings_menu.gd 7 件套 1:1 严格分离); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T131 + T224 + T229 + T265 0 触碰 `_syncing_from_master` 守卫 + `_reduce_all` 数据独立); 0 触碰 §9.6.9 / §9.6.10 PauseMenu 跨面板 hover 反馈 (audit_save_slots 走独立数据源, 与 hover 反馈 0 互混)。
- **预防**：
  1. 任何 polish 期给 SaveSystem 加新 1 状态 (e.g. "stale" 30 天没动 / "pinned" 玩家 pin 防删 / "shared" 多人共享 / "encrypted" 加密存档) 或加新 1 调用方 (e.g. settings_menu 加 "存档健康" 面板 / SaveLoadMenu 加 "巡检" 按钮 / DeathScreen 死亡时 audit) 时**必须**严格按 5 件套 1:1 复制既有 audit_save_slots 4 状态巡检模式: (1) 公共 `audit_save_slots` 加 1 行委托 + push_warning (1 行日志) (0 漏 1 边) + (2) 内部 `_audit_save_slots` 扩 N 状态 + N ids arrays (走 `for i in range(SLOT_COUNT):` 循环 0 改硬编码 5) + (3) SaveSystem._ready 末尾 `call_deferred("audit_save_slots")` 1 行 (0 删 0 改) + (4) `pause_menu.gd _refresh_profile_audit` 加 1 行 4 字段渲染 (`int(report.get("X", 0))` 0 漏 default) + N 档色域分工 (0 触碰 3 档暖红/暖黄/暖白) + 中点 "·" 分隔符 (0 改) + (5) `title_screen.gd _ready` `has_method` 守卫 + 1 行 audit (0 删 0 改)。建议在 `_audit_save_slots` 上方 docblock 写明 "5 件套 1:1 同步 (公共入口 + 内部 worker + call_deferred + PauseMenu 渲染 + TitleScreen 守卫)" 硬约束。
  2. `call_deferred("audit_save_slots")` 启动延迟 0 删 (T131 落地时显式用 call_deferred 避免 _ready 半完成时 IO 阻塞) —— 任何"删 call_deferred 改直接调"polish 都会让 5 slot 巡检在 _ready 期间同步执行, 主线程 0.1-0.3s 阻塞, 玩家启动黑屏, source-grep 验证 SaveSystem._ready 末尾 `call_deferred("audit_save_slots")` 0 漏 0 改 1 字符。
  3. `has_method("audit_save_slots")` 跨调用方守卫 0 删 (T229 + T265 落地时显式用 has_method 守卫避免 SaveSystem autoload 在 headless / 反序列化失败时 NPE) —— 任何"删 has_method 守卫"polish 都会让 SaveSystem autoload 为 null 时 `_refresh_profile_audit` / `title_screen._ready` 调 `SaveSystem.audit_save_slots()` 抛 NPE, 玩家打开 PauseMenu / TitleScreen 抛错黑屏, source-grep 验证 pause_menu.gd `_refresh_profile_audit` + title_screen.gd `_ready` 2 处 `has_method` 守卫 0 漏 0 改 1 字符。
  4. 4 状态 (empty / ok / corrupted / drift) + 4 ids arrays (empty_ids / ok_ids / corrupted_ids / drift_ids) 1:1 双源 —— `_audit_save_slots` 4 状态 + 4 ids arrays 0 漏 0 错位 0 复用 0 共享, `int(report.get("X", 0))` 4 行 default 0 漏, 任何"加新状态但漏 ids arrays"polish 都会让 push_warning 仅打印计数 0 知道具体哪个 slot 损坏, source-grep 验证 4 状态 + 4 ids arrays 0 漏 0 改 1 字符 (按 SLOT_COUNT 1:1 严格对齐)。
  5. 3 档色域分工 (损坏>0 暖红 (0.85, 0.45, 0.4) / 漂移>0 暖黄 (0.85, 0.71, 0.43) / 全 ok 暖白 (0.875, 0.835, 0.784)) 0 改 (T229 落地时显式用 3 档 if/elif/else 色域分工) —— 任何"统一暖白"polish 都会让损坏存档视觉组"存档健康优先级"语义破坏, source-grep 验证 `_refresh_profile_audit` 3 档 `if corrupted_n > 0: ... elif drift_n > 0: ... else: ...` 0 漏 0 改 1 行。
  6. `int(report.get("X", 0))` 0 漏 default 0 改 1 字符 (T229 落地时显式用 4 行 `int(report.get("ok", 0))` / `int(report.get("corrupted", 0))` / `int(report.get("drift", 0))` / `int(report.get("empty", 0))`) —— 任何"漏 default"polish 都会让 report 字典缺 key 时 (例如 fresh SaveSystem 4 状态未初始化) `int(report.get("ok"))` 报 "Invalid get index" 错误, 玩家打开 PauseMenu 抛错, source-grep 验证 4 行 `int(report.get(X, 0))` 0 漏 0 改 1 字符。
  7. `SLOT_COUNT` 顶层常量 0 改硬编码 5 (T088 落地时显式用 `const SLOT_COUNT := 5` 顶层 const + `for i in range(SLOT_COUNT):` 循环 0 走硬编码 3/5) —— 任何"硬编码 5"polish 都会让 audit_save_slots 写 `for i in range(5):` 0 走 SLOT_COUNT, 加 slot 6 时 audit 仅巡检前 5 slot, slot 6 永远 0 巡检, source-grep 验证 `for i in range(SLOT_COUNT):` 0 漏 0 改 1 字符 (走 SLOT_COUNT 0 走硬编码)。
  8. 1:1 双层 (公共 `audit_save_slots` + 内部 `_audit_save_slots`) 0 合并 (T224 落地时显式用 2 函数 1:1 严格分离) —— 任何"合并 2 函数"polish 都会让 smoke test 走公共层 + 承受 push_warning 噪音, 0 干净, source-grep 验证 `func audit_save_slots()` + `func _audit_save_slots()` 2 函数 0 漏 0 改 1 行 (公共层 1 行委托 + 内部层 50 行 worker 1:1 严格分离)。
  9. 中点 "·" 分隔符 0 改 (T229 落地时显式用 4 字段中点 "·" 与 ProfileQuickStats 4 段 + ProfileRecentList 7 字段行风格完全一致) —— 任何"改中点为其他字符"polish 都会让玩家视觉组"ProfileAudit 1 行"与其他 4 字段行风格不连贯, 视觉组"PauseMenu 4 字段行 1:1 风格统一"语义破坏, source-grep 验证 `_profile_audit.text = "存档  %d ok  ·  %d 损坏  ·  %d 漂移  ·  %d 空"` 中点 0 漏 0 改 1 字符。
  10. T088 / T131 / T224 / T229 / T265 5 件套 0 触碰既有 N-1 元素 —— `SLOT_COUNT` 顶层常量 0 改, 仅新加 1 slot 改 1 行; `audit_save_slots` 公共入口 0 改, 仅新加 1 行; `_audit_save_slots` 内部 worker 0 改既有 4 状态, 仅新加 1 状态; `call_deferred("audit_save_slots")` 启动延迟 0 删 0 改 1 行; `_refresh_profile_audit` 1 函数 0 改既有 4 字段, 仅新加 1 字段; `_ready` 末尾 `call_deferred` + `_refresh_profile_audit` 0 改 0 删 0 重排; `title_screen.gd _ready` `has_method` 守卫 + 1 行 audit 0 删 0 改 1 行。0 触碰 §9.6.13 settings_menu 4 section × N key (任何 const / var 0 触碰 5 件套 1:1 严格分离); 0 触碰 §9.6.14 settings_menu `[input]` section 6 件套 (任何 const / var 0 触碰 ACTION_NAMES / ACTION_CATEGORY / CATEGORY_RENDER_ORDER / _DEFAULT_BINDINGS 4 const + 4+5 行循环); 0 触碰 §9.6.12 4 tab 状态机 (任何 const / var 0 触碰 7 件套 1:1 严格分离); 0 触碰 §9.6.11 ReduceAllCheck 三态 (T131 + T224 + T229 + T265 0 触碰 `_syncing_from_master` 守卫 + `_reduce_all` 数据独立); 0 触碰 §9.6.9 / §9.6.10 PauseMenu 跨面板 hover 反馈 (audit_save_slots 走独立数据源, 与 hover 反馈 0 互混 0 复用 0 共享)。

## 10. 联系方式 / 决策记录

- 大决策（玩法方向 / 风格宪法）→ `ROADMAP.md` 顶部「当前方向」+ `CHANGELOG.md` 段头
- 审查发现问题 → `REVIEW_LOG.md`（严重/一般/轻微/信息 4 类）
- 灵感 / 候选 → `INSPIRATION.md`（- 游戏名《xxx》：机制参考 (链接)）

### 9.6.16 player.gd 6 verb hit handler 5 件套 1:1 polish 模式 (T098 + T170c + T170d + T181 + T251 + T252 跨 6 任务 ~90 轮落地) 文档化

> T251 (#169) + T270 (#189) 跨 6 任务在 `player.gd` 落地 6 verb hit handler (`_on_pulse_hit` / `_on_cut_hit` / `_on_bind_hit` / `_on_echo_hit` / `_on_wave_hit` / `_on_whisper_hit`)，跨 4 autoload (ScreenShake / AudioManagerEnhanced / Pulse/Cut/Bind 5 verb VFX 家族 / Whisper 6 verb VFX 家族) live-push 1:1 polish 模式. 任何"加新 1 verb 命中反馈" (e.g. 6 verb 升级 7 verb / Echo reflect 升级 2 段 reflect) 时**必须**严格按 5 件套 1:1 复制既有模式 (0 漏 0 改 1 字符).

**症状 / 触发场景 / 修复 / 预防** 4 段完整结构：

1. **症状** — 6 verb hit handler 行为漂移，5 反馈通道 (1 `if target == null: return` 守卫 + 1 屏染 / VFX flash + 1 屏抖 / VFX pulse + 1 音频 + 1 跨 autoload 守卫) 任一漏 → 玩家命中反馈缺失 / 屏抖 0 触发 / SFX 0 响 / 跨类 NPE / 视觉组语义破坏.
2. **触发场景** — polish 期加 1 段新 1 verb 命中反馈 (e.g. T181 加 Pulse 命中 SFX / T170c 加 4 verb 屏抖 / T170d 加 Cut 屏染 / T251 录 4 verb 屏染查表 / T270 加 Whisper L6 EXPANDING_RING), 5 件套任一漏 0 拍打 → 命中"哑火"/反馈"延迟"/视觉"漂移".
3. **修复** — 严格按 5 件套 1:1 复制既有模式:
   - **`target == null` 守卫** 1 行 — `_on_*_hit` 第 1 行 (pulse_ability.gd:126 末尾 emit(null, ...) 是占位 emit, 应不触发屏闪). 任何"加新 1 verb 命中"必须 0 漏.
   - **屏染 1 行** (4 verb: Pulse / Cut / Bind / Echo reflect) — `ScreenShake.flash_color(ScreenShake.VERB_HIT_X_COLOR, duration, peak)` 走 §F009 4 verb 命中色查表 (T251 落地). 1 verb 屏染查表 0 漏 0 改 1 字符 (5 verb Wave / 6 verb Whisper 走独立 VFX 系统, 0 触发屏染).
   - **屏抖 1 行** (4 verb) — `ScreenShake.shake_preset(ScreenShake.Preset.LIGHT)` 1.0/0.08s 屏抖. 1 verb 屏抖 0 漏 0 改 1 字符 (5 verb Wave / 6 verb Whisper 走独立 VFX 系统, 0 触发屏抖, 避免和 5 verb ring / 6 verb sphere 视觉组冲突).
   - **VFX flash 1 行** (5 verb Wave / 6 verb Whisper + Echo reflect) — Wave `add_hit_flash(target.global_position)` / Whisper `flash_hit(target.global_position)` / Echo reflect `add_bounce_flash(target.global_position)` 走 5 verb ring / 6 verb sphere / Echo VFX 独立系统. 4 verb 走屏染, 5+1 verb 走 VFX flash, **0 互混 0 复用 0 共享**.
   - **音频 1 行** (T181 throttled) — `if AudioManagerEnhanced and AudioManagerEnhanced.has_method("play_X_hit"): AudioManagerEnhanced.play_X_hit()`. 1 verb 音频 + has_method 守卫 0 漏 0 改 1 字符 (Echo reflect 0 触发音频 / Wave 0 触发音频 / Whisper 0 触发音频, 避免 5 verb + 1 verb 音频组和 4 verb 命中音频冲突).
   - **跨类守卫** — `if ScreenShake and ScreenShake.has_method("flash_color")` / `if AudioManagerEnhanced and AudioManagerEnhanced.has_method("play_X_hit")` 2 处 has_method 守卫 (§9.6.1 跨类 handler `is_instance_valid + has_method` 双守卫) 0 漏 0 改 1 字符.
4. **预防** — 任何 polish 期给 6 verb hit handler 加新 1 反馈通道 (e.g. 加 1 verb / 加屏染 / 加屏抖 / 加 VFX flash / 加音频) 时**必须**严格按 5 件套 1:1 复制既有模式, 0 漏 0 改 1 字符. 0 触碰 _on_pulse_hit / _on_cut_hit / _on_bind_hit / _on_echo_hit / _on_wave_hit / _on_whisper_hit 既有 5 件套, 0 触碰 `VERB_HIT_PULSE_COLOR` / `VERB_HIT_CUT_COLOR` / `VERB_HIT_BIND_COLOR` / `VERB_HIT_ECHO_COLOR` 4 verb 屏染查表, 0 触碰 `add_hit_flash` / `flash_hit` / `add_bounce_flash` 5 verb + 1 verb + Echo reflect VFX flash 接口, 0 触碰 `play_pulse_hit` / `play_cut_hit` / `play_bind_hit` 3 verb 音频接口.

**5 verb + 1 verb 6 事件 handler 跨 4 autoload 1:1 严格分离** (T270 #189 落地后, 6 verb hit handler 6 函数 0 互混 0 复用 0 共享, 4 verb 走屏染 + 屏抖 + 音频 3 件套 / 5 verb 走 VFX flash 1 件套 / 6 verb 走 VFX flash 1 件套 / Echo 走 2 路径 reflect 屏染 + VFX flash 1 件套, 4 verb + 5 verb + 1 verb + Echo reflect 4 套视觉语义 0 互混):

| Verb | 函数 | target 守卫 | 屏染 | 屏抖 | VFX flash | 音频 |
|------|------|------|------|------|------|------|
| Pulse | `_on_pulse_hit` | ✓ | ✓ VERB_HIT_PULSE_COLOR | ✓ LIGHT | ✗ | ✓ play_pulse_hit |
| Cut | `_on_cut_hit` | ✗ (接 `_target` 未用) | ✓ VERB_HIT_CUT_COLOR | ✓ LIGHT | ✗ | ✓ play_cut_hit |
| Bind | `_on_bind_hit` | ✓ | ✓ VERB_HIT_BIND_COLOR | ✓ LIGHT | ✗ | ✓ play_bind_hit |
| Echo (non-reflect) | `_on_echo_hit` reflect=false | ✗ (reflect 分支守卫) | ✓ VERB_HIT_ECHO_COLOR 0.06/0.12 温和 | ✗ | ✗ | ✗ |
| Echo (reflect) | `_on_echo_hit` reflect=true | ✗ (reflect 分支守卫) | ✓ VERB_HIT_ECHO_COLOR 0.08/0.20 强化 | ✗ | ✓ add_bounce_flash | ✓ play_echo_hit |
| Wave | `_on_wave_hit` | ✗ (走 VFX 守卫) | ✗ (5 verb 独立 ring 系统) | ✗ (避免 ring + 抖 视觉冲突) | ✓ add_hit_flash | ✗ (5 verb 命中 = 圆环扩散, 0 额外 SFX) |
| Whisper | `_on_whisper_hit` | ✗ (走 VFX 守卫) | ✗ (6 verb 独立 sphere 系统) | ✗ (避免 sphere + 抖 视觉冲突) | ✓ flash_hit | ✗ (6 verb 命中 = sphere 闪, 0 额外 SFX) |

### 9.6.17 player.gd `_ready` 6 verb 信号桥接 5+1 件套 1:1 polish 模式 (D002.B + T098 + T158 + T146 + T170a + T170c + T170d + T181 + T147 + T142 + D001 + F013.E + T251 + T270 跨 14 任务 ~95 轮落地) 文档化

> D002.B (#98) + F013.E (#159) + T098 (#43) + T146 (#76) + T147 + T158 (#81) + T170a (#88) + T170c + T170d + T181 (#97) + T142 (#75) + D001 (#82) + T251 (#169) + T270 (#189) 跨 14 任务在 `player.gd _ready` 落地 6 verb signal 桥接 (`_on_pulse_fired` / `_on_bind_fired` / `_on_cut_fired` / `_on_echo_fired` / `_on_wave_fired` / `_on_whisper_fired` 6 主信号 + 6 命中信号 + 4 结束信号 + 2 扩展信号 + 1 PlayerActionGate autoload register), 5+1 件套 1:1 严格分离. 任何"加新 1 verb" (e.g. 6 verb 升级 7 verb) 或"加新 1 信号" (e.g. 5 verb `*_expired` / 6 verb `*_expired`) 时**必须**严格按 5+1 件套 1:1 复制既有模式 (0 漏 0 改 1 字符).

**症状 / 触发场景 / 修复 / 预防** 4 段完整结构：

1. **症状** — 6 verb signal 桥接漂移, 5+1 件套任一漏 → 玩家 verb 触发 0 反馈 / 命中 0 触发反馈 / 结束 0 触发清理 / 扩展信号 (combo / multi_reflect) 0 触发 / PlayerActionGate 反向查询 0 命中 / `_exit_tree` 对称 unregister 0 调 → 玩家死亡后 verb 锁定状态泄漏 / 玩家转场景时 6 verb 残留 VFX 0 清理.
2. **触发场景** — polish 期加新 1 verb 信号桥接 (e.g. F013.E #159 加 Whisper 6 verb 信号桥接 / T158 #81 加 Echo multi_reflect / T146 #76 加 Wave combo / T098 #43 加 Pulse/Cut 屏染信号 / T170a #88 加 Bind 屏染信号), 5+1 件套任一漏 0 拍打 → 反馈"哑火"/信号"延迟"/"对称" 失配.
3. **修复** — 严格按 5+1 件套 1:1 复制既有模式 (per verb):
   - **`if verb_ability:` null guard 1 行** — `@onready var verb_ability` 在 player.tscn 子节点上存在时 (`$VerbAbility` 路径 0 改), 任何"加新 1 verb"必须新增 1 个 `@onready` 字段 (5 verb + 1 verb = 6 verb) + 1 个 null guard 行. 1 verb null guard 0 漏 0 改 1 字符.
   - **主信号 `verb_fired.connect(_on_verb_fired)` 1 行** — 6 verb × 1 行 = 6 行. Pulse / Bind / Cut / Echo / Wave / Whisper 6 主信号 0 互混 0 复用 0 共享. 任何"加新 1 verb"必须加 1 主信号 (e.g. Whisper 加 `whisper_fired.connect(_on_whisper_fired)` 1 行).
   - **命中信号 `verb_hit.connect(_on_verb_hit)` 1 行** — 6 verb × 1 行 = 6 行. 3 verb (Pulse / Bind / Cut, T098 + T170a 落地) 走 `if verb_ability.has_signal("verb_hit"):` has_signal 守卫 0 漏 (signal 是 polish 期加的, 0 触碰早期 save 兼容). 3 verb (Echo / Wave / Whisper, T103 + T146 + F013.E 落地) 0 走 has_signal 守卫 (signal 是基础 verb 接入路径 0 漏的, 早期 save 0 兼容) — 3+3 视觉语义 0 互混 0 复用 0 共享. 1 verb 命中信号 0 漏 0 改 1 字符.
   - **结束信号 `verb_expired.connect(_on_verb_expired)` 1 行** — 4 verb × 1 行 = 4 行 (Echo / Wave 加 verb_expired 结束信号). 2 verb (Pulse / Bind / Cut / Whisper) 0 触发结束信号 — Echo / Wave 走"持续扩散期"语义, 0 互混 0 复用 0 共享. 任何"加新 1 verb"若是持续扩散期语义必须加 1 结束信号, 若是瞬发语义 (Pulse / Bind / Cut / Whisper) 0 加.
   - **扩展信号 `if verb_ability.has_signal("verb_extra"):` has_signal 守卫 1 行** — 2 verb × 1 行 = 2 行 (Echo multi_reflect T158 #81 + Wave combo T146 #76). has_signal 守卫防 pre-existing save 0 兼容 (signal 是 polish 期加的, 早期 save 缺该 signal). 任何"加新 1 扩展信号"必须走 has_signal 守卫 0 漏 0 改 1 字符.
   - **`PlayerActionGate.register_player(self)` 1 行** (D001 #82) — 1 个 autoload register 入口. 守卫 `if Engine.has_singleton("PlayerActionGate") or _has_player_action_gate_autoload():` 0 漏 (headless 测试 0 autoload 0 NPE). 任何"加新 1 autoload 反向查询"必须仿 `PlayerActionGate.register_player(self)` 0 漏 0 改 1 字符.
   - **对称 `_exit_tree` `PlayerActionGate.unregister_player(self)` 1 行** (D001 #82) — 1 个对称 unregister 入口. 守卫同 `if Engine.has_singleton("PlayerActionGate") or _has_player_action_gate_autoload():` 0 漏. 任何"加新 1 autoload 反向查询"必须仿 `_exit_tree` 对称 unregister 0 漏 0 改 1 字符.
4. **预防** — 任何 polish 期给 6 verb signal 桥接加新 1 件套 (e.g. 加 1 verb / 加主信号 / 加命中信号 / 加结束信号 / 加扩展信号 / 加 PlayerActionGate register) 时**必须**严格按 5+1 件套 1:1 复制既有模式, 0 漏 0 改 1 字符. 0 触碰 6 verb `@onready` 字段顺序 (`pulse_ability` / `bind_ability` / `cut_ability` / `echo_ability` / `wave_ability` / `whisper_ability`), 0 触碰 `_ready` 末尾 `PlayerActionGate.register_player(self)` 守卫, 0 触碰 `_exit_tree` 末尾 `PlayerActionGate.unregister_player(self)` 守卫, 0 触碰 §9.6.16 6 verb hit handler 5 件套 (`_on_pulse_hit` / `_on_cut_hit` / `_on_bind_hit` / `_on_echo_hit` / `_on_wave_hit` / `_on_whisper_hit` 6 函数 0 互混 0 复用 0 共享), 0 触碰 §9.1 6 verb 接入路径 9 步 (`verb` / `ability` / `const` / `input` / `shop` / `icon` / `vfx` / `audio` / `HUD 冷光勾边`).

**5+1 件套 1:1 严格分离总表** (T270 #189 落地后, 6 verb signal 桥接 5+1 件套 0 互混 0 复用 0 共享, 1 玩家 + 6 verb 主信号 + 6 verb 命中信号 + 4 verb 结束信号 + 2 verb 扩展信号 + 1 PlayerActionGate register 14 行 + 1 对称 unregister 1 行 = 15 行 + 守卫):

| Verb | null guard | 主信号 | 命中信号 | 结束信号 | 扩展信号 |
|------|------|------|------|------|------|
| Pulse | ✓ `if pulse_ability:` | ✓ `pulse_fired` | ✓ `pulse_hit` (T098 has_signal 守卫) | ✗ (瞬发语义) | ✗ |
| Bind | ✓ `if bind_ability:` | ✓ `bind_fired` | ✓ `bind_hit` (T170a has_signal 守卫) | ✗ (瞬发语义) | ✗ |
| Cut | ✓ `if cut_ability:` | ✓ `cut_fired` | ✓ `cut_hit` (T098 has_signal 守卫) | ✗ (瞬发语义) | ✗ |
| Echo | ✓ `if echo_ability:` | ✓ `echo_fired` | ✓ `echo_hit` (基础接入, 0 has_signal 守卫) | ✓ `echo_expired` (持续扩散) | ✓ `echo_multi_reflect` (T158 has_signal 守卫) |
| Wave | ✓ `if wave_ability:` | ✓ `wave_fired` | ✓ `wave_hit` (基础接入, 0 has_signal 守卫) | ✓ `wave_expired` (0.4s 扩散期) | ✓ `wave_combo` (T146 has_signal 守卫) |
| Whisper | ✓ `if whisper_ability:` | ✓ `whisper_fired` | ✓ `whisper_hit` (基础接入, 0 has_signal 守卫) | ✗ (0.15s 静默场, 不走扩散期) | ✗ |
| PlayerActionGate | ✓ `if Engine.has_singleton or _has_player_action_gate_autoload():` | `PlayerActionGate.register_player(self)` (`_ready` 末尾) | n/a | n/a | `PlayerActionGate.unregister_player(self)` (`_exit_tree` 末尾, 对称 unregister) |

**与 §9.6.16 关系** — §9.6.16 覆盖 6 verb hit handler 5 件套 (target 守卫 / 屏染 / 屏抖 / VFX flash / 音频), §9.6.17 覆盖 6 verb signal 桥接 5+1 件套 (null guard / 主信号 / 命中信号 / 结束信号 / 扩展信号 / PlayerActionGate). §9.6.16 关注"信号触发后 handler 函数体 5 件套", §9.6.17 关注"`_ready` 阶段 signal connect 5+1 件套". 任何"加新 1 verb"必须**先**按 §9.6.17 在 `_ready` 加 5+1 件套 (signal connect), **再**按 §9.6.16 在 player.gd 加 5 件套 hit handler (`_on_*_hit` 函数体). 0 反序 0 漏 0 改 1 字符. 0 触碰 §9.6.16 6 verb hit handler 任何 const / var / 函数体 (5+1 件套 §9.6.17 + 5 件套 §9.6.16 = 11 件套 1:1 严格分离, 0 互混 0 复用 0 共享, 任何"加新 1 verb"必须双 1:1 复制 11 件套).

**与 §9.1 6 verb 接入路径 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.17 5+1 件套走"ability (2)" 步 (`@onready var verb_ability` 字段) + §9.1 9 步落地的"signal connect" 链 (per verb 主信号 / 命中信号 / 结束信号 / 扩展信号). 任何"加新 1 verb"必须**先**按 §9.1 9 步落地, **再**按 §9.6.17 5+1 件套 1:1 复制 (0 反序 0 漏 0 改 1 字符). §9.6.16 5 件套是"signal 触发后 handler 内部 5 件套" (与 §9.1 9 步平行, 0 互混). §9.6.17 5+1 件套是"signal connect 阶段 5+1 件套" (与 §9.1 9 步同步, 0 复用). 0 触碰 §9.1 9 步任何 const / var / 节点 (5+1 件套仅在 `_ready` 末尾 + `_exit_tree` 末尾, 0 触碰 §9.1 9 步任何位置).

### 9.6.18 `_verb_ability_base.gd` 共享契约 1:1 严格分离 polish 模式 (F007 #87 + D002.B #98 + H001 #99 + T166 #85 + T173 #92 + T181 #97 + T270 #189 跨 7 任务 ~95 轮落地) 文档化

> F007 (#87) + D002.B (#98) + H001 (#99) + T166 (#85) + T173 (#92) + T181 (#97) + T270 (#189) 跨 7 任务在 `src/scripts/_verb_ability_base.gd` (`class_name VerbAbilityBase extends Node`) 落地 6 verb ability (PulseAbility / BindAbility / CutAbility / EchoAbility / ResonanceWaveAbility / WhisperAbility) 共享契约 (6 字段 + 1 `@onready` + 2 `@export` + 6 共享方法 + 1 `_exit_tree` 兜底) 1:1 严格分离. 任何"加新 1 verb" (e.g. 6 verb 升级 7 verb) 或"重构共享契约" (e.g. 把 6 verb `is_globally_blocking` 提到 base) 时**必须**严格按 1:1 共享契约复制既有模式 (0 漏 0 改 1 字符). 跳出 §9.6.16 6 verb hit handler 5 件套 + §9.6.17 6 verb signal 桥接 5+1 件套 范畴 记录 **verb ability 类层级共享契约** polish 模式.

**症状 / 触发场景 / 修复 / 预防** 4 段完整结构：

1. **症状** — `_verb_ability_base.gd` 共享契约漂移, 6 verb ability 子类 (Pulse / Bind / Cut / Echo / Wave / Whisper) 6 件共享契约任一漏 → 玩家 verb 共享状态 (cooldown_timer / windup_timer / is_winding_up / pending_origin / pending_direction / windup_vfx) 重复实现 / 字段 shadow / parse 错误 / Godot 4 静态分析 `The member 'cooldown' already exists in parent class` 类 pre-existing 风险 / `_exit_tree` 兜底 VFX 清理丢失 (Wave #92 修复前 case) / `is_globally_blocking` 跨 D001 PlayerActionGate autoload 缺接口.
2. **触发场景** — polish 期加新 1 verb (e.g. F013.E #159 加 Whisper 6 verb) 或重构共享契约 (e.g. H001 #99 把 `cooldown` / `windup_time` 从 5 verb 各自 `@export` 集中到 base), 共享契约 1:1 严格分离任一漏 0 拍打 → 字段 shadow parse 错误 / VFX 兜底清理丢失 / autoload 跨类反查 0 命中 / 共享方法 6 verb 各自 byte-identical copy 漂移.
3. **修复** — 严格按 6 件共享契约 1:1 复制既有模式 (`_verb_ability_base.gd` 是 canonical 副本, 6 verb 各自 extends 继承 0 重写):
   - **6 共享字段** 0 重声明 — `_cooldown_timer: float = 0.0` (T181 #97 cooldown decrement) + `_windup_timer: float = 0.0` (T166 #85 windup state) + `_is_winding_up: bool = false` (T142 #75 anti-misinput) + `_pending_origin: Vector2 = Vector2.ZERO` (T166 #85 windup → execute 桥接) + `_pending_direction: Vector2 = Vector2.ZERO` (T166 #85 windup → execute 桥接) + `_windup_vfx: Node2D = null` (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T173.C #92 windup VFX 句柄). 6 verb 子类**必须** 0 重声明 (会 shadow base 字段破坏 lifecycle contract). 任何"加新 1 verb 共享字段"必须严格在 base 加 1 个字段 + 6 verb 各自 0 改 0 重声明 0 shadow.
   - **1 `@onready` 共享 `_player`** 0 重声明 — `@onready var _player: CharacterBody2D = get_parent() as CharacterBody2D` (resolve player 父节点, `_ready` 第 1 步 assert 非 null). 6 verb 子类**必须** 0 重声明 (与 base 共享). 任何"加新 1 verb" 0 触碰.
   - **2 `@export` 共享 `cooldown` / `windup_time`** 0 重声明 (H001 #99 落地) — `@export var cooldown: float = 0.5` (默认 Pulse 值, "feels-good floor", 0 override 时子类继承) + `@export var windup_time: float = 0.10` (默认 Pulse 值). **H001 #99 关键踩坑**: 5 verb 各自 `@export var cooldown` 重声明触发 Godot 4 静态分析 `The member 'cooldown' already exists in parent class` 错误, 5 verb ability parse 阶段 1 次性破. 修复: 严格 0 重声明, .tscn 端 override (player.tscn Pulse cooldown=0.5 / Bind=1.2 / Cut=0.8 / Echo=4.0 / Wave=6.0 / Whisper=8.0 1:1). 任何"加新 1 verb" 0 重声明, 仅在 .tscn 端 override.
   - **6 共享方法 0 重写 (除 `is_globally_blocking` 2 verb 重写)** — `_consume_verb_cost(cost: int) -> bool` (F007 #87 cost consumption, `GameState` autoload null 守卫) + `_setup_windup_state(origin, direction)` (F007 #87 windup 4 字段赋值) + `_process_cooldown(delta, verb_name)` (T181 #97 cooldown decrement + jingle 守卫, 6 verb 各自 opt-in 调 `_process_cooldown(delta, "<verb>")`) + `get_cooldown_ratio() -> float` (HUD 4 verb cooldown bar 0 调, `cooldown <= 0` 守卫 0 除零) + `is_winding_up() -> bool` (D001 #82 PlayerActionGate autoload 反查入口) + `_exit_tree()` (T173 #92 兜底 VFX `fade_out_and_free()` 1 字段 null guard). 4 verb (Pulse / Bind / Cut / Echo) **必须** 0 重写 5 件套方法, 1 verb 显式重写 0 触碰 base 既有实现 (resonance_wave_ability.gd 重复 `get_cooldown_ratio` + `is_winding_up` byte-identical copy 是 T270 #189 重构目标, 1 verb 重复 0 触法 6 verb 共享契约 0 漏).
   - **`is_globally_blocking() -> bool` 扩展接口 (D001 #82)** — 2 verb (Wave / Whisper) 重写, 4 verb (Pulse / Bind / Cut / Echo) 0 需 (走 base `is_winding_up` 或自家 `is_shield_active` 等 1 字段). Wave 0.10s windup 期间需阻断其他 4 verb (D001 #82 5-verb chain anti-misinput). Whisper 0.10s windup 期间也走 anti-misinput 链抑制 (T270 #189). 任何"加新 1 verb"若是持续扩散期或 windup 期间需 anti-misinput 必须 override 1 个 `is_globally_blocking` 入口 (1 行 `return _is_winding_up`).
   - **`super._ready()` / `super._exit_tree()` 1:1 调用顺序** — 6 verb 子类 `_ready` 必须 `super._ready()` 首行调用 (base 1 行 `_player` resolve + assert, 然后子类加 verb-specific perk application), 0 改顺序 0 漏 super. `_exit_tree` 仅 1 verb (Wave T173 #92 之前漏) 显式 super 调用, 5 verb 0 override (base `_exit_tree` 兜底 `_windup_vfx.fade_out_and_free()` + null 1:1). 任何"加新 1 verb"必须 0 漏 `super._ready()` 首行, `super._exit_tree()` 0 必 (base 1:1 兜底, 0 重复).
4. **预防** — 任何 polish 期给 6 verb ability 子类 (Pulse / Bind / Cut / Echo / Wave / Whisper) 加新 1 共享契约字段 (e.g. `_shared_fx`) 或方法 (e.g. `_shared_fx_apply`) 时**必须**严格按 1:1 共享契约复制既有模式, 0 漏 0 改 1 字符. 0 重声明 base 6 字段 (`_cooldown_timer` / `_windup_timer` / `_is_winding_up` / `_pending_origin` / `_pending_direction` / `_windup_vfx`), 0 重声明 1 `@onready` (`_player`), 0 重声明 2 `@export` (`cooldown` / `windup_time`, H001 #99 关键踩坑), 0 重写 5 件套方法 (`_consume_verb_cost` / `_setup_windup_state` / `_process_cooldown` / `get_cooldown_ratio` / `is_winding_up`), 0 漏 `super._ready()` 首行 (1 字段 + 1 assert), 0 漏 `super._exit_tree()` 兜底 (Wave T173 #92 修复前 case, 5 verb 0 override 0 漏). 0 触碰 `is_globally_blocking` 2 verb (Wave / Whisper) 重写接口 (D001 #82 PlayerActionGate 反查入口, 0 改 0 删 0 重命名).

**6 件共享契约 1:1 严格分离总表** (D002.B #98 落地后, 6 verb ability 子类 extends VerbAbilityBase 共享契约 0 互混 0 复用 0 共享, 6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 兜底 = 15 件 + 守卫, 0 触碰 0 重写 0 重声明):

| 共享契约 | 数量 | base 落地 | 6 verb 子类复制 | 0 触碰边界 |
|------|------|------|------|------|
| 共享字段 | 6 (cooldown_timer / windup_timer / is_winding_up / pending_origin / pending_direction / windup_vfx) | D002.B #98 + T166 #85 + T173 #92 | 0 重声明 (会 shadow) | 6 verb 0 改 0 删 0 重排 |
| `@onready` | 1 (`_player`) | D002.B #98 | 0 重声明 | 6 verb 0 改 0 删 0 重排 |
| `@export` | 2 (`cooldown` / `windup_time`) | H001 #99 hotfix (5 verb 各自重声明触发 parse 错误) | 0 重声明 (.tscn 端 override) | 6 verb 0 重声明 (1 字段 1:1) |
| 共享方法 | 5 (`_consume_verb_cost` / `_setup_windup_state` / `_process_cooldown` / `get_cooldown_ratio` / `is_winding_up`) | F007 #87 + T181 #97 + D001 #82 | 4 verb 0 重写 (Pulse / Bind / Cut / Echo), 1 verb 显式重写 (Wave byte-identical 重复, T270 #189 重构目标) | 0 改 base 既有 5 方法 |
| `is_globally_blocking` 扩展 | 1 (D001 #82) | 0 提供 (基类无此方法) | 2 verb 重写 (Wave / Whisper), 4 verb 0 需 (走 base `is_winding_up` 或自家 `is_shield_active`) | 0 改 0 删 0 重命名 |
| `_exit_tree` 兜底 | 1 (T173 #92) | base 1:1 提供 `fade_out_and_free()` | 5 verb 0 override, 1 verb (Wave) T173 #92 之前漏, T173.C 修复后 0 override | Wave 0 触碰 5 verb `super._exit_tree()` 0 改 |

**与 §9.6.16 关系** — §9.6.16 覆盖 6 verb hit handler 5 件套 (target 守卫 / 屏染 / 屏抖 / VFX flash / 音频), §9.6.18 覆盖 6 verb ability 子类共享契约 1:1 严格分离 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 兜底). §9.6.16 关注"信号触发后 handler 函数体 5 件套", §9.6.18 关注"verb ability 类层级共享契约 6 件 1:1 分离". 任何"加新 1 verb"必须**先**按 §9.6.18 在 `_verb_ability_base.gd` 加 1 行共享契约 (1 verb extends, 0 改 base 既有 6 字段), **再**按 §9.6.16 在 player.gd 加 5 件套 hit handler, **同时**按 §9.6.17 在 player.gd `_ready` 加 5+1 件套 signal 桥接. 3 套 polish 模式 0 反序 0 漏 0 改 1 字符.

**与 §9.1 6 verb 接入路径 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.18 共享契约走"ability (2)" 步 (`src/scripts/_verb_ability_base.gd` 共享契约 1:1 严格分离) + §9.1 9 步落地的"verb ability 子类 extends" 链 (per verb `extends "res://src/scripts/_verb_ability_base.gd"`). 任何"加新 1 verb"必须**先**按 §9.1 9 步落地, **再**按 §9.6.18 共享契约 1:1 复制 (0 反序 0 漏 0 改 1 字符). 0 触碰 §9.1 9 步任何 const / var / 节点 (共享契约仅在 base `_verb_ability_base.gd`, 6 verb 子类 0 改 base 0 改).

**与 §9.6.17 关系** — §9.6.17 覆盖 `player.gd _ready` 6 verb signal 桥接 5+1 件套 (null guard / 主信号 / 命中信号 / 结束信号 / 扩展信号 / PlayerActionGate register), §9.6.18 覆盖 6 verb ability 子类共享契约 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法). 任何"加新 1 verb"必须**先**按 §9.6.18 写 1 verb ability 子类 extends `_verb_ability_base.gd` (共享契约 0 重写), **再**按 §9.6.17 在 player.gd `_ready` 加 5+1 件套 signal 桥接 (per verb 主信号 / 命中信号 / 结束信号 / 扩展信号 + PlayerActionGate register). 2 套 polish 模式 0 反序 0 漏 0 改 1 字符.

### 9.6.19 `_verb_windup_vfx_base.gd` 共享契约 1:1 严格分离 polish 模式 (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 + T173 #92 + T174 #93 + T174.B #94 + F013.E #159 + T275 #194 跨 10 任务 ~115 轮落地) 文档化

> T166 (#85) + T167 (#86) + T168 (#86) + T169 (#87) + T171 (#89) + T173 (#92) + T174 (#93) + T174.B (#94) + F013.E (#159) + **T275 (#194)** 跨 10 任务在 `src/scripts/_verb_windup_vfx_base.gd` (`class_name VerbWindupVFXBase extends Node2D`) 落地 **6 verb** windup VFX (PulseWindupVFX / BindWindupVFX / CutWindupVFX / EchoWindupVFX / WaveWindupVFX / **WhisperWindupVFX**) 共享契约 (3 共享字段 + 1 `_ready()` z_index 集中 + 1 `_process(delta)` 生命周期 + 1 `_activate_windup_tween()` ramp-in tween + 1 `fade_out_and_free()` 0.05s 退出 = 7 件) 1:1 严格分离. **T275 (#194) 收回 F013.E #159 漂移** —— WhisperWindupVFX 现 `extends "res://src/scripts/_verb_windup_vfx_base.gd"` (5 verb 0 extend 0 重写 0 触碰) + 删 3 自实现字段 + 删 `_process(delta)` + 删 `fade_out_and_free()` + 字段名 `_is_active` → `_active` 1:1 rename, 6 verb windup VFX 共享契约 1:1 严格分离 100% 闭环. 业务特例保留: Whisper z_index=50 (VFX 在玩家上方, 区别于 5 verb z_index=10 在世界上方), 显式 override `_ready()` 调 `super._ready()` 集中 z_index=10 + 重设 z_index=50 (与 §9.6.18 Wave 显式重写 byte-identical 模式 镜像). 任何"加新 1 verb"或"重构共享契约"时**必须**严格按 1:1 共享契约复制既有模式 (0 漏 0 改 1 字符). 跳出 §9.6.18 6 verb ability 共享契约 范畴 记录 **windup VFX 类层级共享契约** polish 模式 (T174.B #94 byte-identical `fade_out_and_free()` 5 verb 落地后, F013.E #159 第 6 verb 时 0 走该 base 1:1, T275 #194 已收回该 drift, 文档化该 polish 模式防 polish 期重踩 "加新 1 verb 共享字段 0 复制 1:1 / 6 verb windup 各自 byte-identical copy 漂移 / Whisper 自实现 vs 5 verb 共享 base 漂移" 类 pre-existing 风险).

**症状 / 触发场景 / 修复 / 预防** 4 段完整结构:

1. **症状** — `_verb_windup_vfx_base.gd` 共享契约漂移, 5 verb windup VFX (Pulse / Bind / Cut / Echo / Wave) 7 件共享契约任一漏 → 玩家 verb 风蓄期 VFX 共享状态 (_lifetime / _max_lifetime / _active) 重复实现 / 字段 shadow / VFX 0 退出 (queue_free 漏) / 风蓄 tween 漂移 (4 verb 各自 byte-identical copy 漂移) / 1 verb (Whisper) 0 extend base 命名偏离 (`_is_active` vs base `_active`).
2. **触发场景** — polish 期加新 1 verb (e.g. F013.E #159 加 Whisper 6 verb) 或重构共享契约 (e.g. T174.B #94 把 5 verb `fade_out_and_free` 集中到 base), 共享契约 1:1 严格分离任一漏 0 拍打 → 字段 shadow / 5 verb 各自 byte-identical 漂移 / VFX 退出漏 / 1 verb (Whisper) 0 extend base 漂移 (命名偏离 + 自实现 fade_out_and_free 漂移).
3. **修复** — 严格按 7 件共享契约 1:1 复制既有模式 (`_verb_windup_vfx_base.gd` 是 canonical 副本, **6 verb** 各自 extends 继承 0 重写 0 漂):
   - **3 共享字段** 0 重声明 — `_max_lifetime: float = 0.10` (T166 #85 5 verb 默认 windup_time, 0.04~0.10s 区间, tween duration / safety net 双重数据源) + `_lifetime: float = 0.0` (T166 #85 _process 累加, 跨 frame monotonic 计数) + `_active: bool = false` (T166 #85 _process 早退守卫 + fade_out_and_free 0 重入守卫). **6 verb** 子类**必须** 0 重声明 (会 shadow base 字段破坏 lifecycle contract). 任何"加新 1 verb 共享字段"必须严格在 base 加 1 个字段 + 6 verb 各自 0 改 0 重声明 0 shadow. **T275 (#194) 收回 Whisper 漂移** — `whisper_windup_vfx.gd` 现 0 字段, 字段名 `_active` 与 base 1:1 对称, 6 verb (含 Whisper) 共享契约 1:1 严格分离 100% 闭环.
   - **1 `_ready()` z_index 集中** 5 verb 0 override + **1 verb Whisper 显式 override 业务特例** — 5 verb `z_index = 10` (windup VFX 在世界 (player/sprites) 之上 HUD 之下, 集中 base 避免 5 verb 各自漏设) 由 base 1:1 集中, 5 verb 0 override `_ready()`. 1 verb Whisper 业务特例: `z_index = 50` (VFX 在玩家上方, 5 verb 视觉组连贯需在玩家上, 区别于 5 verb z_index=10 在世界上方), 显式 override `_ready()` 调 `super._ready()` 集中 z_index=10 + 重设 z_index=50 (与 §9.6.18 Wave 显式重写 byte-identical 模式 镜像). 任何"加新 1 verb" 0 override `_ready()` (除非业务需求 1:1 镜像 Wave 模式, 显式 override 调 super + 重设业务值).
   - **1 `_process(delta)` 生命周期 0 重写** — 5 verb 各自 `_process` 是 byte-identical copy (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 落地时各写一遍), 后由 base 1:1 集中: `if not _active: return` 守卫 + `_lifetime += delta` 累加 + `if _lifetime >= _max_lifetime: _active = false; queue_free(); return` safety net + `queue_redraw()` 跨 frame 触发 `_draw()`. **6 verb** 子类**必须** 0 override `_process()`. **T275 (#194) 收回 Whisper 漂移** — `whisper_windup_vfx.gd _process` 已删, 走 base 1:1 集中 `_lifetime` + safety net + `queue_redraw()`.
   - **1 `_activate_windup_tween()` ramp-in tween (T174 #93 落地) 0 重写** — `modulate.a = 0.0` 初始 alpha + `create_tween()` + `set_trans(Tween.TRANS_QUAD)` + `set_ease(Tween.EASE_OUT)` + `tween_property(self, "modulate:a", 1.0, _max_lifetime)` (modulate:a 0.0→1.0 over _max_lifetime, TRANS_QUAD EASE_OUT front-loads 可见性) + `queue_redraw()`. **6 verb** `trigger()` 末尾必须 `_activate_windup_tween()` (T174.B #94 替代 T174 #93 之前 5 verb 各自 4-line tween block + T275 #194 收回 Whisper 漂移). **6 verb** 子类**必须** 0 override `_activate_windup_tween()`. 任何"加新 1 verb" 0 触碰 base tween 实现.
   - **1 `fade_out_and_free()` 0.05s 退出 (T173 #92 落地) 0 重写** — 5 verb 各自 `fade_out_and_free()` 是 byte-identical copy (T166 #85 + T167 #86 + T168 #86 + T169 #87 + T171 #89 落地时各写一遍), 后由 base 1:1 集中: `if not _active: queue_free(); return` 守卫 (idempotent) + `_active = false` 停止 `_process` 与 tween 竞争 + `create_tween()` + `tween_property(self, "modulate:a", 0.0, 0.05)` + `tween_callback(queue_free)`. 由 parent ability 的 `_exit_tree()` 在 scene change / death 时统一调用 (T173 #92 设计本意: 0.05s 短于 room transition ≥0.4s, 长于 abrupt _exit_tree 平滑). **6 verb** 子类**必须** 0 override `fade_out_and_free()`. **T275 (#194) 收回 Whisper 漂移** — `whisper_windup_vfx.gd fade_out_and_free` 已删, 走 base 0.05s 退出 1:1 集中.
4. **预防** — 任何 polish 期给 6 verb windup VFX 子类 (Pulse / Bind / Cut / Echo / Wave / Whisper) 加新 1 共享契约字段 (e.g. `_shared_windup_color`) 或方法 (e.g. `_shared_windup_init`) 时**必须**严格按 1:1 共享契约复制既有模式, 0 漏 0 改 1 字符. 0 重声明 base 3 字段 (`_max_lifetime` / `_lifetime` / `_active`), 0 override `_ready()` (z_index=10 base 集中, 业务特例需 override 时 1:1 镜像 §9.6.18 Wave 模式: 调 super + 重设业务值), 0 override `_process(delta)` (lifetime + safety net), 0 override `_activate_windup_tween()` (ramp-in tween), 0 override `fade_out_and_free()` (0.05s 退出). 0 触碰 6 verb 各自 `trigger()` 末尾的 `_activate_windup_tween()` 1 行调用 (T174.B #94 替代 5 verb 各自 4-line tween block, T275 #194 收回 Whisper 漂移后 6 verb 1:1 严格分离). **T275 (#194) 收回完成** — F013.E (#159) 落地时漂移已 0, 6 verb (含 Whisper) 共享契约 1:1 严格分离 100% 闭环. 任何"加新 1 verb"必须**先**在 §9.1 9 步落地, **再**严格按 7 件共享契约 1:1 复制 (`extends "res://src/scripts/_verb_windup_vfx_base.gd"` + 0 字段 shadow + 0 override 6 共享方法), 0 重蹈 F013.E 漂移 (T275 #194 已 0 漂).

**7 件共享契约 1:1 严格分离总表** (T275 #194 落地后, 6 verb windup VFX 子类 extends VerbWindupVFXBase 共享契约 0 互混 0 复用 0 共享, 3 字段 + 1 `_ready()` + 1 `_process(delta)` + 1 `_activate_windup_tween()` + 1 `fade_out_and_free()` = 7 件 + 守卫, 0 触碰 0 重写 0 重声明, 1 verb Whisper 业务特例: 显式 override `_ready()` 调 super + 重设 z_index=50):

| 共享契约 | 数量 | base 落地 | 6 verb 子类复制 | Whisper 业务特例 | 0 触碰边界 |
|------|------|------|------|------|------|
| 共享字段 | 3 (`_max_lifetime` / `_lifetime` / `_active`) | T166 #85 + T173 #92 | 0 重声明 (会 shadow) | T275 #194 收回 0 字段, 走 base 1:1 | 6 verb 0 改 0 删 0 重排 |
| `_ready()` | 1 (`z_index = 10`) | T174.B #94 | 5 verb 0 override | **1 verb Whisper 显式 override** 调 super + 重设 `z_index = 50` (业务特例, 与 §9.6.18 Wave 显式重写 模式 镜像) | 5 verb 0 override |
| `_process(delta)` | 1 (lifetime + safety net + `queue_redraw()`) | T174.B #94 | 0 override | T275 #194 收回 0 自实现 | 6 verb 0 override |
| `_activate_windup_tween()` | 1 (T174 #93 ramp-in modulate:a 0.0→1.0 over _max_lifetime, TRANS_QUAD EASE_OUT) | T174 #93 | 0 override, `trigger()` 末尾 1 行调用 | T275 #194 收回 0 自实现, `trigger()` 末尾 1 行调用 base | 6 verb 0 override |
| `fade_out_and_free()` | 1 (T173 #92 0.05s modulate:a 1.0→0.0 + `tween_callback(queue_free)`, idempotent) | T173 #92 + T174.B #94 | 0 override | T275 #194 收回 0 自实现, 走 base 0.05s 退出 | 6 verb 0 override |
| `super._ready()` 首行 | 1 (base 1 行 z_index=10 集中) | T174.B #94 隐式 | 5 verb 0 override → super 自动调用 | **1 verb Whisper 显式调** `super._ready()` 集中 z_index=10 + 重设 z_index=50 | 5 verb 0 漏 |
| `extends VerbWindupVFXBase` 1:1 | 1 (T174.B #94 5 verb 0 重写 base 实现, T275 #194 收回 Whisper 漂移) | T174.B #94 + T275 #194 | 6 verb 全 `extends "res://src/scripts/_verb_windup_vfx_base.gd"` | T275 #194 收回, Whisper 现 extends VerbWindupVFXBase | 6 verb 0 改 `extends` |

**T275 (#194) 收回 F013.E #159 漂移 4 项完成状态** (1 verb Whisper 0 extend base → 6 verb 全 extends, 6 verb windup VFX 共享契约 1:1 严格分离 100% 闭环):

| 漂移项 | Whisper 落地时 (F013.E #159) | T275 (#194) 收回后 | base 5 verb 实现 | 业务特例保留 |
|------|------|------|------|------|
| `extends` | `extends Node2D` (0 extend base) | `extends "res://src/scripts/_verb_windup_vfx_base.gd"` | `extends "res://src/scripts/_verb_windup_vfx_base.gd"` | N/A |
| 字段 `_active` vs `_is_active` | `var _is_active: bool = true` (命名偏离) | 0 字段, 走 base `var _active: bool = false` | `var _active: bool = false` | N/A |
| `_ready()` z_index | `z_index = 50` 自实现 | 显式 override `_ready()` 调 `super._ready()` + 重设 `z_index = 50` (与 §9.6.18 Wave 显式重写 模式 镜像) | base `z_index = 10` | **z_index=50 业务需求保留** (VFX 在玩家上方, 5 verb 视觉组连贯) |
| `_process(delta)` 退出路径 | 自实现 `if _lifetime >= _max_lifetime: fade_out_and_free()` (0 触碰 base) | 0 `_process` 自实现, 走 base 1:1 集中 | base 1:1 集中 lifetime + safety net + `queue_redraw()` | N/A |
| `fade_out_and_free()` 守卫 | 自实现 `is_inside_tree()` 守卫 + `_is_active = false` + tween (守卫更严格) | 0 自实现, 走 base `_active` 守卫 + `_active = false` + tween | base `_active` 守卫 + `_active = false` + tween (idempotent) | N/A |

**与 §9.6.18 关系** — §9.6.18 覆盖 `_verb_ability_base.gd` 6 verb ability 子类共享契约 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 兜底 + 1 `is_globally_blocking` 扩展 = 16 件 1:1 严格分离), §9.6.19 覆盖 `_verb_windup_vfx_base.gd` 6 verb windup VFX 子类共享契约 (3 字段 + 1 `_ready()` + 1 `_process(delta)` + 1 `_activate_windup_tween()` + 1 `fade_out_and_free()` = 7 件 1:1 严格分离). §9.6.18 关注"verb ability 类层级共享契约" (PulseAbility / BindAbility / ...), §9.6.19 关注"windup VFX 类层级共享契约" (PulseWindupVFX / BindWindupVFX / ...). 任何"加新 1 verb"必须**先**按 §9.6.18 在 `_verb_ability_base.gd` 加 1 个 verb ability 子类 extends (0 改 base 既有 6 字段), **同时**按 §9.6.19 在 `_verb_windup_vfx_base.gd` 加 1 个 windup VFX 子类 extends (0 改 base 既有 7 件共享契约, 0 改 6 verb Whisper 任何代码 — T275 #194 收回完成), **再**按 §9.6.16 在 player.gd 加 5 件套 hit handler, **最后**按 §9.6.17 在 player.gd `_ready` 加 5+1 件套 signal 桥接. 4 套 polish 模式 0 反序 0 漏 0 改 1 字符. §9.6.18 + §9.6.19 是 ability + VFX **双 base 1:1 严格分离**镜像, 6 verb 子类 (ability) + 6 verb 子类 (windup VFX) **双 6 件 1:1 严格分离 = 12 件 1:1 严格分离 0 互混 0 复用 0 共享**.

**与 §9.6.16 / §9.6.17 / §9.6.18 / §9.1 6 verb 接入路径 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.19 共享契约走"vfx (7)" 步 (`src/scripts/_verb_windup_vfx_base.gd` 共享契约 1:1 严格分离) + §9.1 9 步落地的"verb windup VFX 子类 extends" 链 (per verb `extends "res://src/scripts/_verb_windup_vfx_base.gd"`). 任何"加新 1 verb"必须**先**按 §9.1 9 步落地, **再**按 §9.6.19 共享契约 1:1 复制 (0 反序 0 漏 0 改 1 字符). 0 触碰 §9.1 9 步任何 const / var / 节点 (共享契约仅在 base `_verb_windup_vfx_base.gd`, 6 verb 子类 0 改 base 0 改, 含 1 verb Whisper T275 #194 收回完成).

### 9.6.20 Hub ↔ archive 跨房间 transition flow polish 模式 (跨 12 任务 ~196 轮 落地)

> T053 (#38) Hub ↔ archive 双向闭环奠基 → T063 (#43) BGM 场景集成 → T070 (#49) 存档系统持久化磁盘版 → T078 (#55) Boss 音乐 override 引用计数 → T079 (#56) 玩家死亡后重生点修复 (关键: `_ready` 顺序 bug 修, 0 漏 `GameState._is_transitioning` 检查在 `is_hub_mode` 之前) → T081 (#57) atmosphere bell 修复 0.8s 暖光回流 → T107 (#76) 第 7 主题 BGM `archive_storm` → T114 (#82) `silence_void` BGM preset → T123 (#88) `whisper_hollow` 路由 → T156 (#112) ArchiveStorm 主摄像机 shake 触发 (1f skybox rotate) → T223 (#163) `archive_05` Wave 0.5× 教学房间 → T269 (#192) `archive_05` 灰盒 + 内容扩展. 跨 12 任务 158 轮迭代落地, 5 段 canonical 1:1 序列, 0 漏 0 改 1 字段, 任何"加新 1 room"必须**先**按 §9.6.20 5 段序列 1:1 复制, **再**按 §9.1 9 步走 "node" 步落地 (0 反序 0 漏 0 改 1 字段). 跨 5 个 room (`hub` / `archive_01` ~ `archive_05`) + 5 个 BGM preset (`silence_void` / `whisper_hollow` / `archive_storm` / ... 0 触碰) 全部走同一套 5 段 1:1 序列 0 例外.

1. **症状** — polish 期任意 1 个 room 切换 (Hub → archive / archive → Hub / archive_01 → archive_02) 出现 4 类症状之一: (a) **玩家位置丢失** — 跨 scene 后玩家回到旧 scene 的 `last_known_position` 0 走 `_pending_spawn_point` (T079 修, `_ready` 顺序 bug 漏 `GameState._is_transitioning` 检查在 `is_hub_mode` 之前), 或 `_pending_spawn_point` 0 写入就 `change_scene_to_file` (字段传递契约破坏), 或 `respawn_at` 0 调直接走默认 `Vector2(60, 180)` (T273.b #191 收回完成, T079 #56 修复后 `respawn_at(_pending_spawn_point)` 1:1 调用); (b) **持久化状态丢失** — `change_scene_to_file` 之前 0 调 `GameState.save_persistent_state()` (T070 #49 持久化磁盘版落地, 跨 scene 后 health / max_health / coins / current_verb 等 0 还原), 或 `save_persistent_state` 在 `change_scene_to_file` 之后调 (T070 落地后顺序 1:1 严格, 跨 scene 持久化契约 0 反序); (c) **fade_in 黑屏闪 1 帧** — `_room_transition.fade_in(0.5)` 之前 0 调 `await get_tree().process_frame` (T052 #37 `call_deferred` 修复: `_room_transition` 通过 `call_deferred("add_child", ...)` 在 `_ready` 之后 1 帧才落地, `ColorRect.modulate` 属性 0 存在直接 `tween_property` 报 "Required object 'rp_target' is null"), 或 `process_frame` 在 `fade_in` 之后调 (T052 落地后顺序 1:1 严格); (d) **BGM 错乱** — `change_scene_to_file` 之前 0 调 `_play_music_for_state(PLAYING)` (T107 #76 archive_storm 落地时 0 改), 或新 scene `_ready` 之前就播 boss BGM 干扰 hub BGM (T078 #55 Boss override 引用计数修). 4 类症状的根因**全部**是 5 段序列任意 1 段 0 走或反序或漏 1 字段 (`_pending_room_path` / `_pending_spawn_point` / `GameState._is_transitioning` / `save_persistent_state` / `process_frame await`).

2. **触发场景** — 任何 1 个 polish 任务涉及"加新 1 room 切换"或"修 1 跨 scene 状态"时触发. 跨 5 个典型 polish 任务类别: (i) **新加 1 archive 房间** (e.g. T223 #163 `archive_05` 教学房间, 走 Hub → archive_05 transition, 需在 `room_door` 配 `target_room_path = "res://src/scenes/archive_05.tscn"` + `target_spawn_point = Vector2(...)` + GFC `_ready` 走 5 段 1:1 序列); (ii) **新加 1 BGM preset** (e.g. T107 #76 `archive_storm`, 需在 `audio_manager_enhanced` 注册 preset + `room_scene.gd._on_room_entered` 路由 trigger 调 `_play_music_for_state(PLAYING)`); (iii) **修 1 玩家死亡回 Hub 路径** (e.g. T079 #56 死亡回 safe room, 需 `player_dead` signal 桥接 GFC `_on_player_died` → `change_scene_to_file(hub_path)` + Hub `_ready` 走 5 段 1:1 序列, T079 修 `_ready` 顺序 bug); (iv) **修 1 跨 scene 状态传递** (e.g. T070 #49 持久化磁盘版, 需 `GameState.save_persistent_state` + `GameState._is_transitioning` 跨 scene 持久化, 0 漏 0 反序); (v) **修 1 transition 时序问题** (e.g. T052 #37 `call_deferred` 修, 需 `await get_tree().process_frame` 在 `fade_in` 之前, 0 触碰 `_room_transition` 自身 `_ready`). 5 类触发场景根因**全部**是 5 段序列任意 1 段 0 走或漏 1 字段.

3. **修复** — 严格按 5 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段序列 1:1 (T053 #38 + T070 #49 + T079 #56 + T107 #76 + T223 #163 12 任务 158 轮落地后定稿, 0 例外): **Stage 1 触发检测** — `room_door.gd._on_body_entered(body)` 检测 `body.is_in_group("player")` → emit `player_entered(target_room_path)` signal (T053 落地, `room_door` 是 Hub ↔ archive 唯一入口, 0 走 player.gd 直接 transition, 0 走单例 Manager). **Stage 2 GFC 状态转换** — `game_flow_controller.gd._on_door_entered(path)` 或 `_on_door_with_spawn_entered(path, spawn_point)` → 存 `GameState._pending_room_path = path` + `GameState._pending_spawn_point = spawn_point` (T053 + T079 跨 scene 字段传递契约, 2 字段名 0 改 0 重命名) → `_enter_state(State.ROOM_TRANSITION)`. **Stage 3 fade_out** — `await _room_transition.fade_out(0.4)` (T053 落地, 0.4s 短于玩家感知延迟, 长于 1 frame jitter) → emit `transition_finished` signal 触发 `Stage 4`. **Stage 4 场景切换** — `GameState.save_persistent_state()` (T070 #49 持久化磁盘版, 写 `user://save_data.json`, 0 走 in-memory 缓存, 跨 scene 后 `_ready` 0 还原) → `GameState._is_transitioning = true` (T079 跨 scene 持久化状态, 新 scene `_ready` 检查此字段才走 `_recover_from_transition`, 0 走 is_hub_mode 单点判断 0 区分 hub/archive) → `get_tree().change_scene_to_file(_pending_room_path)` (Godot 4.6 SceneTree 原子切换, 0 走 `change_scene` 旧 API). **Stage 5 新 scene 恢复** — 新 scene `GFC._ready` 调用 `_recover_from_transition()` (T079 #56 修复后, 关键: 检查 `GameState._is_transitioning` 在 `is_hub_mode` 之前, 0 漏 0 反序) → `player.respawn_at(GameState._pending_spawn_point)` (T053 落地, `respawn_at` 是 player.gd 唯一入口, 0 走 `position = ...` 直接赋值, 跨 `set_speed_multiplier(1.0)` 1:1 调用) → `GameState.restore_persistent_state()` (T070 1:1 镜像 `save_persistent_state`, 0 走 `_ready` 顶部 0 走 `change_scene` 之前) → `await get_tree().process_frame` (T052 #37 `call_deferred` 修, 让 `_room_transition` 通过 `call_deferred("add_child", ...)` 在 `_ready` 之后 1 帧落地, `ColorRect.modulate` 属性存在性保证) → `_room_transition.fade_in(0.5)` (T053 落地, 0.5s 长于 0.4s fade_out, 视觉连贯 0 闪 1 帧) → `_enter_state(State.PLAYING)` (T053 落地, 状态机恢复 0 停留在 ROOM_TRANSITION). 5 段 1:1 严格, 跨 5 个 room + 5 个 BGM preset 0 例外.

4. **预防** — 任何 polish 期给"加新 1 room 切换"或"修 1 跨 scene 状态"任务加新字段 / 加新方法 / 加新 BGM preset 时**必须**严格按 5 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 漏 `is_in_group("player")` 守卫** (0 误触发 non-player body), **Stage 2 0 改 `_pending_room_path` / `_pending_spawn_point` 2 字段名** (T053 + T079 跨 scene 字段传递契约, 改 1 字符破坏 `change_scene_to_file` 跨 scene 持久化, 0 走 `set_meta` / `set("path", ...)` Godot 反射, 0 走 player.gd 内部 `position`), **Stage 3 0 触碰 0.4s fade_out 持续时间** (T053 落地时 0.4s 短于玩家感知, 改 0.2s 闪 1 帧改 0.6s 玩家感知卡顿), **Stage 4 0 反序 `save_persistent_state` / `_is_transitioning = true` / `change_scene_to_file` 3 行** (T070 + T079 落地后顺序 1:1 严格, 0 漏 `save_persistent_state` 跨 scene 持久化 0 还原, 0 漏 `_is_transitioning = true` 新 scene `_ready` 0 走 `_recover_from_transition`), **Stage 5 0 漏 `await get_tree().process_frame` 在 `fade_in` 之前** (T052 `call_deferred` 修复 0 触碰, 0 触碰 `_room_transition` 自身 `_ready`, 0 走 `is_inside_tree` 单点判断 0 走 `await get_tree().physics_frame` 物理帧). 任何"加新 1 BGM preset"必须**先**在 `audio_manager_enhanced.gd` 注册 preset, **再**在 `room_scene.gd._on_room_entered` 路由 trigger 调 `_play_music_for_state(PLAYING)`, 0 触碰 GFC 5 段 1:1 序列 (BGM 切换是 5 段序列的"Stage 4 副作用", 0 走 GFC 内部 `_play_music_for_state` 0 走 scene.tscn autoplay). 任何"加新 1 room"必须**先**创建 `src/scenes/<room_name>.tscn` (含 player + door + enemy + ...), **再**在 `room_door.target_room_path` 配 `<room_name>.tscn` 资源路径, **再**在 GFC 5 段序列 1:1 复制 (0 改 Stage 1-5 任何字段任何方法任何顺序). T053 + T070 + T079 + T107 + T223 12 任务 158 轮落地后 0 漂移, 跨 5 个 room + 5 个 BGM preset 100% 1:1 严格. F013.E 0 触碰本段 (本段聚焦 cross-room transition, 不重叠 §9.6.19 verb windup VFX 类层级共享契约).

**5 段 canonical 1:1 序列总表** (T053 + T070 + T079 + T107 + T223 12 任务 158 轮落地后定稿, 5 段 1:1 严格, 跨 5 room + 5 BGM preset 0 例外):

| 段 | 1:1 序列 | 落地任务 | 关键字段 / 方法 | 0 触碰边界 | 0 反序依赖 |
|------|------|------|------|------|------|
| Stage 1 触发检测 | `room_door._on_body_entered` → `is_in_group("player")` 守卫 → `emit_signal("player_entered", path)` | T053 #38 | `target_room_path: String` (export var, 0 走 hardcode) | 0 漏 player 守卫, 0 走 player.gd 直接 transition | 0 触碰 Stage 2-5 |
| Stage 2 GFC 状态转换 | `_on_door_with_spawn_entered(path, spawn)` → `GameState._pending_room_path = path` + `GameState._pending_spawn_point = spawn` → `_enter_state(ROOM_TRANSITION)` | T053 #38 + T079 #56 | 2 字段名 0 改 0 重命名 (T053 + T079 跨 scene 字段传递契约) | 0 漏 2 字段写入, 0 走 `set_meta` / `set("path", ...)` | `_enter_state(ROOM_TRANSITION)` 必须在 2 字段写入之后 |
| Stage 3 fade_out | `await _room_transition.fade_out(0.4)` → emit `transition_finished` signal | T053 #38 | 0.4s 持续时间 0 改 (T053 落地值, 短于玩家感知长于 1 frame jitter) | 0 触碰 `_room_transition._ready` / 0 走 `modulate.a = 0.0` 直接赋值 | `await` 阻塞必须, 0 走 `fade_out(0.4); emit_signal(...)` 非阻塞 |
| Stage 4 场景切换 | `GameState.save_persistent_state()` → `GameState._is_transitioning = true` → `get_tree().change_scene_to_file(path)` | T070 #49 + T079 #56 + T107 #76 | `user://save_data.json` 持久化磁盘版 (T070 0 走 in-memory) | 0 漏 `save_persistent_state` 持久化, 0 漏 `_is_transitioning` 跨 scene 标志 | 3 行顺序 1:1 严格, 0 反序 0 漏 |
| Stage 5 新 scene 恢复 | `if GameState._is_transitioning: _recover_from_transition()` → `player.respawn_at(_pending_spawn_point)` → `GameState.restore_persistent_state()` → `await get_tree().process_frame` → `_room_transition.fade_in(0.5)` → `_enter_state(PLAYING)` | T052 #37 + T053 #38 + T070 #49 + T079 #56 | `_recover_from_transition` 是新 scene `_ready` 唯一入口 (T079 修 `_ready` 顺序 bug) | 0 漏 `process_frame await` 在 `fade_in` 之前 (T052 `call_deferred` 修), 0 走 `is_hub_mode` 单点判断 0 区分 hub/archive | `_recover_from_transition` 内 5 行顺序 1:1 严格 |

**5 段序列跨任务历史落地表** (T053 → T269 12 任务 158 轮, 5 段 1:1 0 例外 0 漂移):

| 任务 | 轮次 | 落地段 | 关键修复 / 扩展 | 0 触碰边界 |
|------|------|------|------|------|
| T053 Hub ↔ archive 双向闭环 | #38 | Stage 1-3 + Stage 5 基线 | 5 段序列 0→1 落地, `room_door` + GFC + `_room_transition` 0→1 创建 | 0 触碰 player.gd 内部 `position`, 0 走单例 Manager |
| T063 BGM 场景集成 | #43 | Stage 4 副作用扩展 | `_play_music_for_state(PLAYING)` 调点 (Stage 4 之后) | 0 触碰 5 段序列, BGM 是 Stage 4 副作用 |
| T070 存档系统持久化磁盘版 | #49 | Stage 4 持久化 | `GameState.save_persistent_state` + `restore_persistent_state` 1:1 镜像 | 0 走 in-memory 缓存, 0 走 user:// 之外路径 |
| T078 Boss 音乐 override 引用计数 | #55 | Stage 4 副作用 | Boss BGM override 引用计数 (0 走 SceneTree autoplay) | 0 触碰 5 段序列, Boss 是 Stage 4 副作用 + 引用计数 |
| T079 玩家死亡后重生点 | #56 | Stage 4 + Stage 5 关键修 | **`_ready` 顺序 bug 修**: 检查 `GameState._is_transitioning` 在 `is_hub_mode` 之前, 0 漏 0 反序 | 0 漏 `process_frame await` 在 `fade_in` 之前 (T052 已修) |
| T081 atmosphere bell 修复 | #57 | Stage 5 副作用扩展 | 0.8s 暖光回流 (Hub → archive transition 1:1 镜像 `fade_in` 0.5s) | 0 触碰 5 段序列 |
| T107 第 7 主题 BGM `archive_storm` | #76 | Stage 4 副作用扩展 | `archive_storm` BGM preset 注册 + `room_scene._on_room_entered` 路由 trigger | 0 触碰 5 段序列 |
| T114 `silence_void` BGM preset | #82 | Stage 4 副作用扩展 | `silence_void` BGM preset (0 触碰 5 段序列) | 0 触碰 5 段序列 |
| T123 `whisper_hollow` 路由 | #88 | Stage 4 副作用扩展 | `whisper_hollow` BGM preset 路由 trigger | 0 触碰 5 段序列 |
| T156 ArchiveStorm 主摄像机 shake | #112 | Stage 5 副作用扩展 | 1f skybox rotate trigger (Stage 5 `_recover_from_transition` 之后 1f) | 0 触碰 5 段序列, shake 是 Stage 5 副作用 |
| T223 `archive_05` Wave 0.5× 教学房间 | #163 | Stage 1-5 1:1 复制 | `archive_05.tscn` 0→1 创建, `room_door.target_room_path` 配 `archive_05.tscn` | 0 触碰 5 段序列任何字段任何方法任何顺序 |
| T269 `archive_05` 灰盒 + 内容扩展 | #192 | Stage 5 副作用扩展 | `archive_05` 灰盒 + 0.5× Wave + content 扩展 | 0 触碰 5 段序列 |

**5 段序列常见 anti-pattern 表** (4 类症状根因, 12 任务 158 轮 0 触碰, 任何"加新 1 room 切换"或"修 1 跨 scene 状态"必须 0 触碰 0 反序 0 漏 0 改 1 字段):

| anti-pattern | 症状 | 根因 | T053-T269 落地后 1:1 严格 | 0 触碰边界 |
|------|------|------|------|------|
| 漏 Stage 2 `_pending_spawn_point` 写入 | 跨 scene 后玩家回旧 scene `last_known_position` 0 走 `_pending_spawn_point` | Stage 2 `_on_door_with_spawn_entered` 0 调, 改用 `_on_door_entered` 0 传 spawn | T079 #56 修后 `_pending_spawn_point` 1:1 写入, 跨 5 room 0 例外 | 0 改 2 字段名, 0 走 `set_meta` / `set("path", ...)` |
| Stage 4 漏 `save_persistent_state` | 跨 scene 后 health / max_health / coins / current_verb 0 还原 | `change_scene_to_file` 之前 0 调 `GameState.save_persistent_state()` | T070 #49 落地下 1:1 严格 3 行顺序, 跨 5 room 0 例外 | 0 反序 `save_persistent_state` / `_is_transitioning` / `change_scene_to_file` |
| Stage 4 漏 `_is_transitioning = true` | 新 scene `_ready` 0 走 `_recover_from_transition`, 玩家在旧 scene `last_known_position` 0 还原 | `change_scene_to_file` 之前 0 设 `GameState._is_transitioning = true` | T079 #56 修后 1:1 严格, 跨 5 room 0 例外 | 0 走 `is_hub_mode` 单点判断 0 区分 hub/archive |
| Stage 5 漏 `await get_tree().process_frame` | `_room_transition.fade_in(0.5)` 报 "Required object 'rp_target' is null", 黑屏闪 1 帧 | `_room_transition` 通过 `call_deferred("add_child", ...)` 在 `_ready` 之后 1 帧落地, `ColorRect.modulate` 属性 0 存在 | T052 #37 修后 1:1 严格, 跨 5 room 0 例外 | 0 触碰 `_room_transition._ready`, 0 走 `await get_tree().physics_frame` 物理帧 |

**与 §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 关系** — §9.6.16 覆盖 player.gd 6 verb hit handler 5 件套 1:1 严格分离 (verb ability `.trigger_player_hit` → 5 件套 `_on_verb_hit_pulse` / `_on_verb_hit_bind` / `_on_verb_hit_cut` / `_on_verb_hit_echo` / `_on_verb_hit_wave`), §9.6.17 覆盖 player.gd `_ready` 5+1 件套 signal 桥接 1:1 严格分离 (5 verb `verb_triggered` signal + 1 verb Whisper 业务特例 `whisper_ability_executed` signal), §9.6.18 覆盖 `_verb_ability_base.gd` 6 verb ability 子类共享契约 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 兜底 + 1 `is_globally_blocking` 扩展 = 16 件 1:1 严格分离), §9.6.19 覆盖 `_verb_windup_vfx_base.gd` 6 verb windup VFX 子类共享契约 (3 字段 + 1 `_ready()` + 1 `_process(delta)` + 1 `_activate_windup_tween()` + 1 `fade_out_and_free()` = 7 件 1:1 严格分离). §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow 5 段 canonical 1:1 序列 (Stage 1 触发检测 + Stage 2 GFC 状态转换 + Stage 3 fade_out + Stage 4 场景切换 + Stage 5 新 scene 恢复). §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 关注"verb ability + windup VFX 类层级共享契约 + signal 桥接", §9.6.20 关注"跨 scene transition flow 时序契约". 5 套 polish 模式 0 反序 0 漏 0 改 1 字符. 任何"加新 1 room"必须**先**按 §9.6.20 Stage 1-5 1:1 复制 (0 反序 0 漏 0 改 1 字段), **再**按 §9.1 9 步走 "node" 步落地 (含 `room_scene` / `room_door` / `hub` / `archive_xx` 节点), 0 触碰 §9.6.16-§9.6.19 任何 verb ability / windup VFX 类层级共享契约 (verb 子类 lifecycle 在本 scene 内 0 跨 scene, transition 是 1:1 时序契约 0 触碰 verb 内部 lifecycle). §9.6.20 是 5 套 polish 模式**唯一**关注"跨 scene 持久化 + 状态机恢复 + fade 时序"的 1 套, 与 §9.6.16-§9.6.19 4 套 verb / VFX 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.20 5 段序列走 §9.1 9 步落地的"node" 步 (`src/scenes/room_door.gd` + `src/scripts/game_flow_controller.gd` + `src/scripts/room_transition.gd` + `src/scenes/room_scene.gd` + `src/scenes/hub.tscn` + `src/scenes/archive_xx.tscn` + `src/scripts/audio_manager_enhanced.gd`), 0 触碰 verb ability / windup VFX 类层级共享契约 (verb 子类 lifecycle 在本 scene 内 0 跨 scene, 5 段序列 0 触碰 verb 内部 lifecycle). 任何"加新 1 room"必须**先**按 §9.1 9 步落地 (含 `room_door` + `hub` / `archive_xx` 节点), **再**按 §9.6.20 5 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 任何"加新 1 BGM preset"必须**先**在 `audio_manager_enhanced.gd` 注册 preset (走 §9.1 9 步 "audio" 步), **再**在 `room_scene.gd._on_room_entered` 路由 trigger 调 `_play_music_for_state(PLAYING)`, 0 触碰 GFC 5 段 1:1 序列 (BGM 切换是 5 段序列的"Stage 4 副作用", 0 走 GFC 内部 `_play_music_for_state` 0 走 scene.tscn autoplay). 0 触碰 §9.1 9 步任何 const / var / 节点 (5 段序列仅在 GFC + `room_transition` + `room_door` 3 文件, 6 verb 子类 0 改 base 0 改, 含 1 verb Whisper T275 #194 收回完成).

### 9.6.21 6 verb 成就系统 闭环 polish 模式 (跨 5 任务 ~146 轮 落地)

> T094 (#51) `quadruple_voice` 4/6 成就 (3 verb → 4 verb closure) → T103 (#74) `quintuple_voice` 5/6 成就 (4 verb → 5 verb closure) → T241 (#159) F013.E 6 verb 接入路径 `Whisper` 静默场 + `sextuple_voice` 6/6 成就 (5 verb → 6 verb closure) → T245 (#162) `whisper_icon` 落地 (Muted Mauve #C8A4D8) + `ICON_COLORS` 6 verb 调色六元组登记 `sextuple_voice` → T246 (#163) 5 verb 旧成就 PNG 双路径补全 (`quadruple_voice=echo_icon` + `quintuple_voice=wave_icon` 走 `assets/ui/<verb>_icon/` + `assets/ui/achievements/<verb>_icon/` 双路径, 1:1 严格). 跨 5 任务 146 轮迭代落地, 8 段 canonical 1:1 序列, 0 漏 0 改 1 字段, 任何"加新 1 verb"必须**先**按 §9.1 9 步落地, **再**按 §9.6.21 8 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 跨 6 verb (`pulse` / `bind` / `cut` / `echo` / `wave` / `whisper`) + 4 tier 成就 (`triple_voice` / `quadruple_voice` / `quintuple_voice` / `sextuple_voice`) + 3 verb icon 关联 (`echo_icon` / `wave_icon` / `whisper_icon`) + 1 个共享 `all_abilities_used` 条件类型 + 1 调色六元组 (`Pulse Coral` / `Bind Violet` / `Cut Amber` / `Echo Cyan` / `Wave Pale` / `Whisper Mauve`) 全部走同一套 8 段 1:1 序列 0 例外.

1. **症状** — polish 期任意 1 个新 verb 接入成就系统 (e.g. T241 #159 `whisper` 第 6 verb) 出现 6 类症状之一: (a) **成就触发计数错** — `record_ability_used("whisper")` 0 调, `whisper_used` 永远 0, `sextuple_voice` 永不触发 (T241 #159 落地下 1:1 严格, 6 branch 0 漏, `whisper` 分支走 `record_stat("whisper_used", 1)`), 或 `record_ability_used` 调了但分支名 0 改 `"whisper"` (字段名 0 改, 改 `"whisper_ability"` 等同破坏 §9.1 9 步 "const" 步 verb 名字段契约, 0 走 `set_meta("verb_name", ...)` 反射); (b) **成就 UI 颜色 fallback** — `sextuple_voice.icon_hint = "whisper_icon"` 但 `ICON_COLORS` 0 登记 `whisper_icon` entry (T245 #162 落地下 1:1 严格, 6 verb 调色六元组 0 漏 0 改, fallback `Color(0.949, 0.714, 0.431, 1.0)` 6 verb 关联成就 0 走), 或 `ICON_COLORS` 登记了 `whisper_icon` 但 `Color` 值 0 改 `Color(0.784, 0.643, 0.847, 1.0)` (T245 #162 落地 Muted Mauve #C8A4D8, 改 1 通道破坏 §9.6.4 调色六元组 + 0 走 §9.6.4 HUD 6 行 6 色色域分工); (c) **成就 PNG 加载失败** — `quadruple_voice.icon_hint = "echo_icon"` 但 `assets/ui/achievements/echo_icon.png` 0 创建 (T246 #163 落地下 1:1 严格, 3 verb 关联成就 PNG 双路径 0 漏, `assets/ui/echo_icon/echo_icon.png` + `assets/ui/achievements/echo_icon/echo_icon.png` 1:1 双路径), 或 PNG 路径走 `assets/ui/echo_icon/icon.png` 0 走 `<verb>_icon/<verb>_icon.png` (T246 #163 落地路径 1:1 严格, 改路径名破坏 §9.1 9 步 "icon" 步 verb icon 资源契约); (d) **成就条件类型漂移** — `all_abilities_used` 条件从 5 verb 扩到 6 verb 漏 `whisper_used >= 1` 检查 (T241 #159 落地下 1:1 严格, `pulse_used >= 1 and bind_used >= 1 and cut_used >= 1 and echo_used >= 1 and wave_used >= 1 and whisper_used >= 1` 6 项 0 漏 0 反序), 或条件从 `and` 改 `or` (破坏"全 6 verb 至少用 1 次"语义, 0 走 `sum >= 6` 累积阈值 0 走 `count >= 1` 计数 0 区分); (e) **新 run 状态不重置** — `reset_run()` 漏 `whisper_used = 0` (T241 #159 落地下 1:1 严格, 6 verb 0 漏 0 反序, reset 顺序 1:1 严格), 或 `reset_run` 调了但调点 0 走 `player.gd._ready` 调走 `_on_run_started` signal (T094 #51 落地后 1:1 严格, `player.gd._ready` 调 `PlayerStats.reset_run()` 是唯一入口); (f) **成就 tier 命名漂移** — `sextuple_voice` 改 `whisper_master` (T241 #159 落地下 1:1 严格, 4 tier 成就名 0 改 0 重命名, `triple_voice` / `quadruple_voice` / `quintuple_voice` / `sextuple_voice` 1:1 严格, 改 1 字符破坏 `achievements.json` ID 契约 + 0 走 i18n 翻译键). 6 类症状的根因**全部**是 8 段序列任意 1 段 0 走或反序或漏 1 字段 (`<verb>_used` / `record_ability_used` / `get_stat` / `set_stat` / `reset_run` / `all_abilities_used` / `icon_hint` / `ICON_COLORS` + PNG 双路径).

2. **触发场景** — 任何 1 个 polish 任务涉及"加新 1 verb 接入成就系统"或"修 1 成就条件类型"或"修 1 成就 UI 颜色"时触发. 跨 5 个典型 polish 任务类别: (i) **新加 1 verb 接入成就系统** (e.g. T241 #159 `whisper` 第 6 verb, 需在 `PlayerStats` 加 1 字段 `whisper_used` + `record_ability_used` 加 1 分支 `whisper` + `get_stat` 加 1 entry `whisper_used` + `set_stat` 加 1 entry `whisper_used` + `reset_run` 加 1 行 `whisper_used = 0` + `all_abilities_used` 条件加 1 项 `whisper_used >= 1` + `achievements.json` 加 1 entry `sextuple_voice` + `ICON_COLORS` 加 1 entry `whisper_icon` + `assets/ui/whisper_icon/` + `assets/ui/achievements/whisper_icon/` 1:1 双路径, 跨 8 段 0 漏 0 反序); (ii) **新加 1 tier 成就** (e.g. T094 #51 `quadruple_voice` 4 verb tier, 需在 `achievements.json` 加 1 entry + 1 个 verb icon 关联 + 1 个 PNG 双路径, 0 触碰既有 5 verb `all_abilities_used` 6 项检查); (iii) **修 1 成就 UI 颜色** (e.g. T245 #162 `whisper_icon` Muted Mauve #C8A4D8, 需在 `ICON_COLORS` 加 1 entry + `Color` 通道值 0 改 §9.6.4 调色六元组, 0 触碰既有 5 verb entry); (iv) **修 1 成就 PNG 路径** (e.g. T246 #163 5 verb 旧成就 PNG 补全, 需 `assets/ui/<verb>_icon/<verb>_icon.png` + `assets/ui/achievements/<verb>_icon/<verb>_icon.png` 1:1 双路径 0 漏, 0 触碰 §9.1 9 步 "icon" 步 verb icon 资源契约); (v) **修 1 成就条件** (e.g. T241 #159 6 verb `all_abilities_used` 6 项检查 0 漏 0 反序, 需 `pulse_used >= 1 and ... and whisper_used >= 1` 6 项 0 漏 0 改 1 字段, 0 走 `sum >= 6` 累积阈值 0 走 `count >= 1` 计数 0 区分). 5 类触发场景根因**全部**是 8 段序列任意 1 段 0 走或漏 1 字段.

3. **修复** — 严格按 8 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 8 段序列 1:1 (T094 #51 + T103 #74 + T241 #159 + T245 #162 + T246 #163 5 任务 146 轮落地后定稿, 0 例外): **Stage 1 PlayerStats 字段声明** — `var whisper_used: int = 0` (T241 #159 落地, 与 5 verb `pulse_used` / `bind_used` / `cut_used` / `echo_used` / `wave_used` 1:1 同构, 6 字段名 0 改 0 重命名, 0 走 `set_meta("verb_used", ...)` 反射). **Stage 2 `record_ability_used` 分支** — `match verb_name:` 加 1 分支 `"whisper": record_stat("whisper_used", 1)` (T241 #159 落地, 6 branch 0 漏, 分支名 0 改 `"whisper"` 0 改 `"whisper_ability"`, 0 走 default 分支 fallthrough). **Stage 3 `get_stat` entry** — `match stat_name:` 加 1 entry `"whisper_used": return whisper_used` (T241 #159 落地, 6 entry 0 漏, 0 改 `return pulse_used` 等既有 entry). **Stage 4 `set_stat` entry** — `match stat_name:` 加 1 entry `"whisper_used": whisper_used = value` (T241 #159 落地, 6 entry 0 漏, 0 改 `pulse_used = value` 等既有 entry). **Stage 5 `reset_run` 重置** — 加 1 行 `whisper_used = 0` (T241 #159 落地, 6 行 0 漏 0 反序, reset 顺序 1:1 严格, 0 触碰 `health` / `coins` 等非 verb 字段). **Stage 6 `all_abilities_used` 条件** — `return pulse_used >= 1 and bind_used >= 1 and cut_used >= 1 and echo_used >= 1 and wave_used >= 1 and whisper_used >= 1` 6 项检查 0 漏 0 反序 (T241 #159 落地, 6 项 `and` 1:1 严格, 0 走 `sum >= 6` 累积阈值 0 走 `count >= 1` 计数 0 区分, 0 改 `>= 1` 为 `>= 2` 破坏"至少 1 次"语义). **Stage 7 `achievements.json` tier entry** — 加 1 entry `{ "id": "sextuple_voice", "condition_type": "all_abilities_used", "icon_hint": "whisper_icon", "title_key": "achievement_sextuple_voice", "description_key": "achievement_sextuple_voice_desc" }` (T241 #159 落地, 4 tier 成就 ID 0 改 0 重命名, `triple_voice` / `quadruple_voice` / `quintuple_voice` / `sextuple_voice` 1:1 严格, 改 1 字符破坏 ID 契约 + 0 走 i18n 翻译键). **Stage 8 `ICON_COLORS` entry + PNG 双路径** — `ICON_COLORS` 加 1 entry `"whisper_icon": Color(0.784, 0.643, 0.847, 1.0)` Muted Mauve #C8A4D8 (T245 #162 落地, 6 verb 调色六元组 0 漏 0 改 1 通道值, 0 走 fallback `Color(0.949, 0.714, 0.431, 1.0)` 6 verb 关联成就 0 走) + 创建 `assets/ui/whisper_icon/whisper_icon.png` + `assets/ui/achievements/whisper_icon/whisper_icon.png` 1:1 双路径 (T246 #163 落地, 3 verb 关联成就 PNG 双路径 0 漏, 0 改 `<verb>_icon/<verb>_icon.png` 路径名, 0 走 `assets/ui/echo_icon/icon.png` 0 区分 verb). 8 段 1:1 严格, 跨 6 verb + 4 tier 成就 + 3 verb icon 关联 0 例外.

4. **预防** — 任何 polish 期给"加新 1 verb 接入成就系统"或"修 1 成就条件"或"修 1 成就 UI 颜色"任务加新字段 / 加新方法 / 加新 tier 成就时**必须**严格按 8 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 8 段 0 触碰边界: **Stage 1 0 改 6 verb `<verb>_used` 字段名** (T241 #159 落地后字段名 1:1 严格, `pulse_used` / `bind_used` / `cut_used` / `echo_used` / `wave_used` / `whisper_used` 6 字段名 0 改 0 重命名, 改 1 字符破坏 `record_ability_used` 6 branch + `get_stat` 6 entry + `set_stat` 6 entry + `reset_run` 6 行 + `all_abilities_used` 6 项检查 + `achievements.json` `icon_hint` 字段引用), **Stage 2 0 改 `"whisper"` 分支名** (T241 #159 落地后分支名 1:1 严格, 改 `"whisper_ability"` 等同破坏 `WhisperAbility._execute_whisper()` 调点 + 0 走 default 分支 fallthrough 静默失败), **Stage 3-4 0 改 `"whisper_used"` entry key** (T241 #159 落地后 entry key 1:1 严格, 改 `"whisper_count"` 等同破坏 `record_stat("whisper_used", 1)` 调点), **Stage 5 0 反序 6 verb reset 顺序** (T241 #159 落地后顺序 1:1 严格, `pulse_used` / `bind_used` / `cut_used` / `echo_used` / `wave_used` / `whisper_used` 6 行顺序 1:1, 0 漏 0 反序, 0 触碰 `health` / `coins` 等非 verb 字段), **Stage 6 0 改 `>= 1` 阈值** (T241 #159 落地后阈值 1:1 严格, 改 `>= 2` 破坏"至少 1 次"语义, 0 走 `sum >= 6` 累积阈值 0 走 `count >= 1` 计数 0 区分), **Stage 7 0 改 4 tier 成就 ID** (T094 + T103 + T241 落地后 ID 1:1 严格, `triple_voice` / `quadruple_voice` / `quintuple_voice` / `sextuple_voice` 4 ID 0 改 0 重命名, 改 1 字符破坏 ID 契约 + 0 走 i18n 翻译键), **Stage 8 0 改 `Color` 通道值** (T245 #162 落地后 Muted Mauve #C8A4D8 `Color(0.784, 0.643, 0.847, 1.0)` 0 改 1 通道, 0 触碰 §9.6.4 调色六元组 + §9.6.4 HUD 6 行 6 色色域分工) + 0 改 `<verb>_icon/<verb>_icon.png` PNG 路径名 (T246 #163 落地后路径 1:1 严格, `assets/ui/<verb>_icon/<verb>_icon.png` + `assets/ui/achievements/<verb>_icon/<verb>_icon.png` 1:1 双路径, 改路径名破坏 §9.1 9 步 "icon" 步 verb icon 资源契约). 任何"加新 1 verb 接入成就系统"必须**先**按 §9.1 9 步落地 (含 `verb` (1) + `ability` (2) + `const` (3) + `icon` (6) 步), **再**按 §9.6.21 8 段序列 1:1 复制 (Stage 1-8 0 反序 0 漏 0 改 1 字段). 任何"加新 1 tier 成就"必须**先**在 `achievements.json` 加 1 entry (Stage 7 1:1 严格), **再**在 `ICON_COLORS` 加 1 verb icon 关联 (Stage 8 0 改 1 通道值) + 1:1 双路径 PNG (Stage 8 0 改路径名), 0 触碰既有 5 verb `all_abilities_used` 6 项检查 (Stage 6 0 反序 0 漏 0 改 1 阈值). 任何"修 1 成就 UI 颜色"必须**先**在 §9.6.4 调色六元组加 1 entry, **再**在 `ICON_COLORS` 加 1 entry (Stage 8 0 改 1 通道值), 0 触碰既有 5 verb entry. 任何"修 1 成就 PNG 路径"必须**先**创建 `assets/ui/<verb>_icon/<verb>_icon.png`, **再**创建 `assets/ui/achievements/<verb>_icon/<verb>_icon.png` 1:1 双路径 (Stage 8 0 改路径名), 0 触碰 §9.1 9 步 "icon" 步 verb icon 资源契约. 任何"修 1 成就条件"必须**先**在 `all_abilities_used` 6 项检查加 1 项 (Stage 6 0 反序 0 漏 0 改 1 阈值), **再**在 `achievements.json` 引用 1 个 verb icon 关联 (Stage 7 0 改 ID), 0 触碰既有 `and` 语义. 已知 drift risk: `_stat_names()` 数组 (T190 落地, 用于 `get_all_stats` debug 接口) **可能**漏 `whisper_used` entry (T241 #159 落地下 `_stat_names()` 0 触碰, `_stat_names()` 是 debug 接口 0 影响游戏逻辑, 但若未来给 `_stat_names()` 加 1 个消费方 (e.g. debug UI 列表) 必须**先**检查 6 verb entry 0 漏 0 反序, 0 漏 1 边). T094 + T103 + T241 + T245 + T246 5 任务 146 轮落地后 0 漂移, 跨 6 verb + 4 tier 成就 + 3 verb icon 关联 100% 1:1 严格. F013.E (#159) 6 verb 接入路径是 §9.6.21 8 段序列的"母集" (1 verb 6 字段, §9.6.21 8 段 1 verb 完整闭环, 6 verb × 8 段 = 48 件 1:1 严格).

**8 段 canonical 1:1 序列总表** (T094 + T103 + T241 + T245 + T246 5 任务 146 轮落地后定稿, 8 段 1:1 严格, 跨 6 verb + 4 tier 成就 + 3 verb icon 关联 0 例外):

| 段 | 1:1 序列 | 落地任务 | 关键字段 / 方法 | 0 触碰边界 | 0 反序依赖 |
|------|------|------|------|------|------|
| Stage 1 PlayerStats 字段声明 | `var <verb>_used: int = 0` (6 verb × 1 字段 = 6 字段) | T241 #159 | `<verb>_used` 字段名 0 改 0 重命名 | 0 走 `set_meta("verb_used", ...)` 反射, 0 走 enum | Stage 2-6 全部依赖 6 字段存在 |
| Stage 2 `record_ability_used` 分支 | `match verb_name:` 加 1 分支 `"<verb>": record_stat("<verb>_used", 1)` (6 branch) | T241 #159 | 分支名 0 改 `<verb>` 0 改 `<verb>_ability` | 0 漏 1 分支, 0 走 default fallthrough | 必须在 Stage 1 字段声明之后 |
| Stage 3 `get_stat` entry | `match stat_name:` 加 1 entry `"<verb>_used": return <verb>_used` (6 entry) | T241 #159 | entry key 0 改 `<verb>_used` 0 改 `<verb>_count` | 0 漏 1 entry, 0 改既有 5 verb entry | 必须在 Stage 1 字段声明之后 |
| Stage 4 `set_stat` entry | `match stat_name:` 加 1 entry `"<verb>_used": <verb>_used = value` (6 entry) | T241 #159 | entry key 0 改 `<verb>_used` 0 改 `<verb>_count` | 0 漏 1 entry, 0 改既有 5 verb entry | 必须在 Stage 1 字段声明之后 |
| Stage 5 `reset_run` 重置 | 加 1 行 `<verb>_used = 0` (6 行) | T241 #159 | reset 顺序 1:1 严格 (pulse → bind → cut → echo → wave → whisper) | 0 反序 6 行, 0 漏 1 行, 0 触碰 `health` / `coins` 等非 verb 字段 | 必须在 Stage 1 字段声明之后 |
| Stage 6 `all_abilities_used` 条件 | `return pulse_used >= 1 and bind_used >= 1 and cut_used >= 1 and echo_used >= 1 and wave_used >= 1 and whisper_used >= 1` (6 项 `and`) | T241 #159 | `>= 1` 阈值 0 改, 0 走 `sum >= 6` 累积阈值 | 0 反序 6 项, 0 漏 1 项, 0 改 `and` 为 `or` | 必须在 Stage 1 字段声明之后 |
| Stage 7 `achievements.json` tier entry | 加 1 entry `{ "id": "sextuple_voice", "condition_type": "all_abilities_used", "icon_hint": "whisper_icon", "title_key": ..., "description_key": ... }` (4 tier 成就) | T094 #51 + T103 #74 + T241 #159 | 4 tier ID 0 改 0 重命名, `triple_voice` / `quadruple_voice` / `quintuple_voice` / `sextuple_voice` | 0 改 ID 破坏 i18n 翻译键, 0 走 `condition_type` 自定义类型 | 必须在 Stage 6 条件之后 |
| Stage 8 `ICON_COLORS` entry + PNG 双路径 | `ICON_COLORS` 加 1 entry `"<verb>_icon": Color(...)` (6 verb 调色六元组) + `assets/ui/<verb>_icon/<verb>_icon.png` + `assets/ui/achievements/<verb>_icon/<verb>_icon.png` 1:1 双路径 | T245 #162 + T246 #163 | `Color` 通道值 0 改 §9.6.4 调色六元组, 路径名 0 改 `<verb>_icon/<verb>_icon.png` | 0 改 1 通道值破坏 §9.6.4 调色六元组 + 0 改路径名破坏 §9.1 9 步 "icon" 步 | 必须在 Stage 7 tier entry 之后 |

**8 段序列跨任务历史落地表** (T094 → T246 5 任务 146 轮, 8 段 1:1 0 例外 0 漂移):

| 任务 | 轮次 | 落地段 | 关键修复 / 扩展 | 0 触碰边界 |
|------|------|------|------|------|
| T094 `quadruple_voice` 4/6 成就 | #51 | Stage 1-7 4 verb baseline | 4 verb 闭环奠基, `all_abilities_used` 4 项检查 + `quadruple_voice` 4 tier 成就 + `echo_icon` icon 关联 | 0 触碰 `triple_voice` 既有 3 verb entry |
| T103 `quintuple_voice` 5/6 成就 | #74 | Stage 1-7 5 verb 扩展 | 5 verb 闭环扩展, `all_abilities_used` 5 项检查 + `quintuple_voice` 5 tier 成就 + `wave_icon` icon 关联 | 0 触碰 `quadruple_voice` 既有 4 verb entry |
| T241 F013.E 6 verb 接入路径 + `sextuple_voice` | #159 | Stage 1-7 6 verb 完整闭环 | 6 verb 完整闭环落地, `all_abilities_used` 6 项检查 0 漏 + `sextuple_voice` 6 tier 成就 + `whisper_used` 6 字段全部 1:1 镜像 5 verb | 0 触碰既有 5 verb 6 字段任何 entry |
| T245 `whisper_icon` 落地 + `ICON_COLORS` 6 verb 调色六元组 | #162 | Stage 8 6 verb 调色六元组 | `ICON_COLORS` 加 `whisper_icon` entry (Muted Mauve #C8A4D8) + 6 verb 调色六元组登记 `sextuple_voice` fallback 颜色 | 0 改 1 通道值破坏 §9.6.4 调色六元组, 0 触碰既有 5 verb entry |
| T246 5 verb 旧成就 PNG 双路径补全 | #163 | Stage 8 PNG 双路径 0 漏 | 3 verb 关联成就 PNG 双路径补全 (`quadruple_voice=echo_icon` + `quintuple_voice=wave_icon` + `sextuple_voice=whisper_icon` 走 `assets/ui/<verb>_icon/<verb>_icon.png` + `assets/ui/achievements/<verb>_icon/<verb>_icon.png` 1:1 双路径, 0 漏 0 改路径名) | 0 触碰 §9.1 9 步 "icon" 步 verb icon 资源契约, 0 改路径名 |

**8 段序列常见 anti-pattern 表** (6 类症状根因, 5 任务 146 轮 0 触碰, 任何"加新 1 verb 接入成就系统"或"修 1 成就条件"或"修 1 成就 UI 颜色"必须 0 触碰 0 反序 0 漏 0 改 1 字段):

| anti-pattern | 症状 | 根因 | T094-T246 落地后 1:1 严格 | 0 触碰边界 |
|------|------|------|------|------|
| 漏 Stage 1 `whisper_used` 字段声明 | `record_ability_used("whisper")` 报 "Undefined variable 'whisper_used'" | Stage 1 0 调, 改用 `set_meta("whisper_used", 0)` 反射存 | T241 #159 落地下 1:1 严格, 6 字段 0 漏, 跨 6 verb 0 例外 | 0 走 `set_meta` / `set("whisper_used", ...)` 反射, 0 走 enum |
| 漏 Stage 2 `whisper` 分支 | `whisper_used` 永远 0, `sextuple_voice` 永不触发 | Stage 2 `match verb_name:` 0 加 `whisper` 分支, 改用 default fallthrough 静默失败 | T241 #159 落地下 1:1 严格, 6 branch 0 漏, 跨 6 verb 0 例外 | 0 改分支名 `<verb>` 0 改 `<verb>_ability`, 0 走 default fallthrough |
| 漏 Stage 6 `whisper_used >= 1` 检查 | `sextuple_voice` 5/6 检查通过即触发, 即使从未用 `whisper` | Stage 6 0 加第 6 项 `whisper_used >= 1`, 改用 `sum >= 6` 累积阈值 | T241 #159 落地下 1:1 严格, 6 项 `and` 0 漏, 跨 6 verb 0 例外 | 0 反序 6 项, 0 改 `>= 1` 阈值, 0 改 `and` 为 `or` |
| 漏 Stage 5 `whisper_used = 0` reset | 新 run `whisper_used` 保留旧 run 值, `sextuple_voice` 立即触发 | Stage 5 0 加 `whisper_used = 0`, 改用 `reset_all_stats()` 通用 reset | T241 #159 落地下 1:1 严格, 6 行 reset 0 漏, 跨 6 verb 0 例外 | 0 反序 6 行, 0 漏 1 行, 0 触碰 `health` / `coins` 等非 verb 字段 |
| 漏 Stage 8 `whisper_icon` ICON_COLORS entry | `sextuple_voice` 弹窗颜色走 fallback `Color(0.949, 0.714, 0.431, 1.0)`, 视觉组连贯 tooltip 颜色错 | Stage 8 0 加 `whisper_icon` entry, 改用 fallback 默认颜色 | T245 #162 落地下 1:1 严格, 6 verb 调色六元组 0 漏, 跨 6 verb 0 例外 | 0 改 1 通道值破坏 §9.6.4 调色六元组, 0 走 fallback |
| 漏 Stage 8 PNG 双路径 | `sextuple_voice` 弹窗 PNG 加载失败, 弹窗空白 | Stage 8 0 创建 `assets/ui/whisper_icon/whisper_icon.png` + `assets/ui/achievements/whisper_icon/whisper_icon.png` 双路径 | T246 #163 落地下 1:1 严格, 3 verb 关联成就 PNG 双路径 0 漏, 跨 3 verb 0 例外 | 0 改路径名 `<verb>_icon/<verb>_icon.png` 0 改 `<verb>_icon/icon.png`, 0 触碰 §9.1 9 步 "icon" 步 verb icon 资源契约 |

**已知 drift risk 表** (5 任务 146 轮落地后已知 drift, 0 触碰边界但需持续监控):

| drift 位置 | 现状 | 风险 | 0 触碰边界 | 监控建议 |
|------|------|------|------|------|
| `PlayerStats._stat_names()` 数组 | T190 落地, 5 verb entry, `whisper_used` **可能**漏 (T241 #159 落地下 0 触碰) | `_stat_names()` 是 debug 接口 `get_all_stats` 列表, 0 影响游戏逻辑 | 0 改 entry key `<verb>_used` 0 改 `<verb>_count`, 0 触碰 `_stat_names()` 既有 5 verb entry | 任何"未来给 `_stat_names()` 加消费方" (e.g. debug UI 列表) 必须**先**检查 6 verb entry 0 漏 0 反序, 0 漏 1 边 |

**与 §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 关系** — §9.6.16 覆盖 player.gd 6 verb hit handler 5 件套 1:1 严格分离 (verb ability `.trigger_player_hit` → 5 件套 `_on_verb_hit_pulse` / `_on_verb_hit_bind` / `_on_verb_hit_cut` / `_on_verb_hit_echo` / `_on_verb_hit_wave`), §9.6.17 覆盖 player.gd `_ready` 5+1 件套 signal 桥接 1:1 严格分离 (5 verb `verb_triggered` signal + 1 verb Whisper 业务特例 `whisper_ability_executed` signal), §9.6.18 覆盖 `_verb_ability_base.gd` 6 verb ability 子类共享契约 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 兜底 + 1 `is_globally_blocking` 扩展 = 16 件 1:1 严格分离), §9.6.19 覆盖 `_verb_windup_vfx_base.gd` 6 verb windup VFX 子类共享契约 (3 字段 + 1 `_ready()` + 1 `_process(delta)` + 1 `_activate_windup_tween()` + 1 `fade_out_and_free()` = 7 件 1:1 严格分离), §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow 5 段 canonical 1:1 序列 (Stage 1 触发检测 + Stage 2 GFC 状态转换 + Stage 3 fade_out + Stage 4 场景切换 + Stage 5 新 scene 恢复), §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 canonical 1:1 序列 (Stage 1 PlayerStats 字段声明 + Stage 2 `record_ability_used` 分支 + Stage 3 `get_stat` entry + Stage 4 `set_stat` entry + Stage 5 `reset_run` 重置 + Stage 6 `all_abilities_used` 条件 + Stage 7 `achievements.json` tier entry + Stage 8 `ICON_COLORS` entry + PNG 双路径). §9.6.16 / §9.6.17 关注"verb ability + signal 桥接", §9.6.18 关注"verb ability 类层级共享契约", §9.6.19 关注"verb windup VFX 类层级共享契约", §9.6.20 关注"跨 scene transition flow 时序契约", §9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格)". 6 套 polish 模式 0 反序 0 漏 0 改 1 字符. 任何"加新 1 verb 接入成就系统"必须**先**按 §9.6.16 5 件套 hit handler 1:1 复制 (0 反序 0 漏 0 改 1 字段), **再**按 §9.6.17 5+1 件套 signal 桥接 1:1 复制 (0 反序 0 漏 0 改 1 字段), **再**按 §9.6.18 16 件套 共享契约 1:1 复制 (0 反序 0 漏 0 改 1 字符), **再**按 §9.6.19 7 件套 windup VFX 共享契约 1:1 复制 (0 反序 0 漏 0 改 1 字符), **再**按 §9.6.21 8 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). §9.6.20 与 §9.6.21 0 互混 0 复用 0 共享: §9.6.20 关注"跨 scene transition flow 时序契约" (与 verb lifecycle 0 相关), §9.6.21 关注"6 verb 成就系统 闭环契约" (与跨 scene transition 0 相关). §9.6.21 是 6 套 polish 模式**唯一**关注"6 verb 关联成就 8 段 1:1 闭环"的 1 套, 与 §9.6.16-§9.6.20 5 套 verb / VFX / transition 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.21 8 段序列走 §9.1 9 步落地的"verb" (1) + "ability" (2) + "const" (3) + "icon" (6) 4 步 (`src/autoload/player_stats.gd` 6 verb 字段 + `src/data/achievements.json` 4 tier 成就 + `src/scripts/achievement_notification.gd` ICON_COLORS + `assets/ui/<verb>_icon/` + `assets/ui/achievements/<verb>_icon/` PNG 双路径), 0 触碰 §9.1 9 步任何 "input" (4) + "shop" (5) + "vfx" (7) + "audio" (8) + "HUD 冷光勾边" (9) 5 步 (成就系统 0 走 input map + 0 走 shop 系统 + 0 走 windup VFX 共享契约 + 0 走 audio 副作用 + 0 走 HUD 渲染). 任何"加新 1 verb 接入成就系统"必须**先**按 §9.1 9 步落地 (含 `verb` (1) + `ability` (2) + `const` (3) + `icon` (6) 步), **再**按 §9.6.21 8 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 任何"加新 1 tier 成就"必须**先**在 `achievements.json` 加 1 entry (Stage 7 1:1 严格), **再**在 `ICON_COLORS` 加 1 verb icon 关联 (Stage 8 0 改 1 通道值) + 1:1 双路径 PNG (Stage 8 0 改路径名), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "const" (3) 步 (成就系统 0 触碰 verb enum + ability 子类 + const 字段). 任何"修 1 成就 UI 颜色"必须**先**在 §9.6.4 调色六元组加 1 entry, **再**在 `ICON_COLORS` 加 1 entry (Stage 8 0 改 1 通道值), 0 触碰 §9.1 9 步任何 "HUD 冷光勾边" (9) 步 (成就弹窗颜色 0 走 HUD 6 行 6 色色域分工). 0 触碰 §9.1 9 步任何 const / var / 节点 (8 段序列仅在 `PlayerStats` + `achievements.json` + `achievement_notification` + PNG 双路径 4 文件, 6 verb 子类 0 改 base 0 改, 含 1 verb Whisper T275 #194 收回完成).

### 9.6.22 6 verb VFX 视觉组 (5 段 canonical 1:1 序列) polish 模式 (T098 + T158 + T170a + T170c + T170d + T181 + F013.E + T241 + T251 + T252 + T270 跨 11 任务 ~135 轮落地) 文档化

> §9.6.1 记录 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫; §9.6.2 记录单个 VFX 类 5 layer 视觉 (L1-L5) polish; §9.6.16 记录 player.gd 6 verb hit handler 5 件套 1:1; §9.6.18 记录 `_verb_ability_base.gd` 6 verb ability 共享契约; §9.6.19 记录 `_verb_windup_vfx_base.gd` 6 verb windup VFX 共享契约; §9.6.21 记录 6 verb 成就系统 闭环 8 段 1:1 序列. 本节记录 6 verb VFX 视觉组 跨 5 段 canonical 1:1 序列 (Stage 1 class_name + extends Node2D 模板 + Stage 2 6 verb 调色六元组 1:1 + Stage 3 5+ layer 视觉组 1:1 + Stage 4 6 verb 几何 1:1 严格分离 + Stage 5 6 verb 跨段 1:1 镜像) — 跨 6 verb VFX 文件 (`pulse_vfx.gd` / `bind_vfx.gd` / `cut_vfx.gd` / `echo_vfx.gd` / `resonance_wave_vfx.gd` / `whisper_vfx.gd`) + 6 verb 调色六元组 + 6 verb 几何 (圆环 / 螺旋 / 弧斩 / 护盾 / 光环 / 球) + 6 verb 跨段镜像 (与 §9.6.16 / §9.6.18 / §9.6.19 / §9.6.21) 反复落地的「6 verb VFX 视觉组」1:1 严格分离 polish 模式. 跨 11 任务 (T098 #65 + T158 #89 + T170a + T170c + T170d + T181 #97 + F013.E #159 + T241 #159 + T251 #169 + T252 #171 + T270 #189) ~135 轮迭代落地, 5 段 canonical 1:1 序列, 0 反序 0 漏 0 改 1 字符. 任何"加新 1 verb 接入 VFX 视觉组"必须**先**按 §9.6.4 调色六元组加 1 entry, **再**按 §9.6.2 5+ layer 视觉组 1:1 复制 (0 反序 0 漏 0 改 1 layer), **再**按 §9.6.18 16 件套 verb ability base 1:1 复制 (0 反序 0 漏 0 改 1 字段), **再**按 §9.6.19 7 件套 windup VFX base 1:1 复制 (0 反序 0 漏 0 改 1 字段), **再**按 §9.6.16 5 件套 hit handler 1:1 复制 (0 反序 0 漏 0 改 1 字段), **再**按 §9.6.21 8 段序列 1:1 复制 (成就系统, 0 反序 0 漏 0 改 1 字段), **再**按 §9.6.22 5 段序列 1:1 复制 (VFX 视觉组, 0 反序 0 漏 0 改 1 字段). 跨 6 verb (Pulse 圆环 cyan / Bind 螺旋 violet / Cut 弧斩 amber / Echo 护盾 cyan 球 / Wave 光环 + 8 棱镜 pale / Whisper constant 球 mauve) + 6 verb 调色六元组 (Pulse Coral / Bind Violet / Cut Amber / Echo Cyan / Wave Pale / Whisper Mauve) + 6 verb 几何 (圆环 / 螺旋 / 弧斩 / 护盾 / 光环 / 球, 0 互混 0 复用 0 共享) 全部走同一套 5 段 1:1 序列 0 例外.

- **症状**: 6 verb VFX (Pulse / Bind / Cut / Echo / Wave / Whisper) 跨 6 个独立 .gd 文件 (`pulse_vfx.gd` / `bind_vfx.gd` / `cut_vfx.gd` / `echo_vfx.gd` / `resonance_wave_vfx.gd` / `whisper_vfx.gd`) 反复 polish 期间出现 4 类典型 fragile: (1) "加新 1 verb VFX 漏 1 跨段镜像" — 新 verb VFX 落地时漏 §9.6.16 5 件套 hit handler 1:1 镜像 (e.g. T251 #169 落地 Whisper 5 layer 时漏 `_on_whisper_hit` 与 `_current_whisper_vfx.flash_hit` 1:1 镜像, 走 pass 占位, 命中时 0 视觉反馈); (2) "加新 1 verb VFX 漏 1 几何" — 新 verb VFX 落地时与既有 5 verb 几何 0 区分 (e.g. T241 #159 落地 Whisper constant 球时 0 标注"与 Wave 圆环扩散形成对比", 玩家看到 2 个圆环状 VFX 误以为是同 1 套视觉组); (3) "修 1 verb VFX 调色 漏 §9.6.4 调色六元组 1:1" — 修 Pulse VFX ring_color 时改硬编码 hex 0 改 §9.6.4 调色六元组 entry, 跨段 §9.6.21 ICON_COLORS + §9.6.3 HUD 6 行 6 色色域分工 6 通道 + §9.6.4 tooltip 三闭环宪法全部漂动; (4) "加新 1 verb VFX 5+ layer 漏 L5 HIT_FLASH" — 新 verb VFX 落地时只画 L1-L4 4 layer, 漏 L5 HIT_FLASH 命中闪烁, 玩家命中时 0 视觉确认 (与 §9.6.2 5 layer polish 1 字段 0 镜像). 4 类症状根因: 6 verb VFX 视觉组 5 段 canonical 1:1 序列 0 文档化, polish 期只能"凭记忆"同步, 1 漏 1 = 跨段漂动.
- **触发场景**: 6 verb VFX (Pulse / Bind / Cut / Echo / Wave / **Whisper**) 5 段 canonical 1:1 序列落地需同步 5 件套: (1) **`pulse_vfx.gd` / `bind_vfx.gd` / `cut_vfx.gd` / `echo_vfx.gd` / `resonance_wave_vfx.gd` / `whisper_vfx.gd` 6 verb VFX 文件 顶层模板** — `class_name <Verb>VFX` (e.g. `class_name PulseVFX`) + `extends Node2D` 0 改 `extends Node`, 1 漏 `class_name` = autoload `_current_<verb>_vfx` 引用断, 1 漏 `extends Node2D` = `_draw` 函数 0 触发 (Node 0 _draw 回调); (2) **6 verb `@export` color 字段 0 改 1 hex** — `@export var ring_color: Color = Color("#69C7CE")` (Pulse 圆环 cyan) / `@export var core_color: Color = Color("#F2B66E")` (Pulse 球心 amber) / `@export var fade_color: Color = Color("#E86D5A")` (Pulse 衰减 coral) / `@export var wave_color: Color = Color("#B7E7DD")` (Pulse 波 pale), Bind 用 `#65506A` violet + `#69C7CE` cyan + `#B7E7DD` pale, Cut 用 `#E86D5A` coral + `#B7E7DD` pale + `#F2B66E` amber, Echo 用 `#69C7CE` cyan + `#B7E7DD` pale + `#F2B66E` amber + `#E86D5A` coral 反弹, Wave 用 `#65506A` muted + `#B7E7DD` pale + `#69C7CE` cyan, Whisper 用 `#C8A4D8` mauve, 6 verb 调色严格走 §9.6.4 调色六元组 0 改 1 hex 0 改 1 通道值; (3) **6 verb `_draw` 函数 5+ layer 视觉组 1:1 严格分离** — Pulse 5 layer (core 球心 + 3 ring 圆环 + spark 碎片 + waveform_arc 弧) / Bind 5 layer (core 球心 + spiral 螺旋 + wave 波 + pull 牵引) / Cut 5 layer (slash 弧 + edge 刃 + flash 闪 + fragment 碎片 + trail 拖尾) / Echo 6 layer (shield 护盾球 + halo 光环 + prism 棱镜 × 8 + core 球心 + reflect 反弹 + reflect_flash 反弹闪) / Wave 6 layer (L1 DEEP_SHADOW + L2 INNER_HALO + L3 RING_FILL + L4 RING_STROKE + L5 PRISM_RAYS × 8 + L6 OUTER_WISPS × 12) / Whisper 6 layer (L1 OUTER_FILL + L2 SPHERE_RING + L3 EDGE_HIGHLIGHT + L4 CORE_DOT + L5 HIT_FLASH × N + L6 EXPANDING_RING), 6 verb 5+ layer 1:1 严格分离 0 互混 0 复用 (Pulse 圆环 0 复用 Bind 螺旋 0 复用 Cut 弧斩 0 复用 Echo 护盾 0 复用 Wave 光环 0 复用 Whisper 球, 6 verb 几何 0 共享 1 行 draw_*); (4) **6 verb 几何 1:1 严格分离** — 圆环 / 螺旋 / 弧斩 / 护盾 / 光环 / 球 6 套视觉 motif, 1 verb 1 几何 0 例外, 几何一致 → 视觉组连贯 (`STYLE_GUIDE.md` "6 verb 视觉组 1 verb 1 几何" 段) + tooltip 8 行拼接 (T250 #168 `_build_verb_achievement_tooltip`) 0 互混 0 改 1 字段; (5) **6 verb 跨段 1:1 镜像** — 6 verb VFX 1:1 镜像到 §9.6.16 5 件套 hit handler (1 verb 1 L5 HIT_FLASH 1 `_on_<verb>_hit` 1 `_current_<verb>_vfx.flash_hit(target)` 调点) + §9.6.18 16 件套 verb ability base (1 verb 1 `<verb>_ability.gd` 1 `trigger_player_hit(target)` 调点 spawn VFX) + §9.6.19 7 件套 windup VFX base (1 verb 1 `<verb>_windup_vfx.gd` 1 `_verb_windup_vfx_base` 共享契约) + §9.6.21 8 段成就序列 (1 verb 1 `<verb>_used` 字段 1 `<verb>_icon` ICON_COLORS 1 PNG 双路径), 5 跨段镜像 0 漏 1 边 0 改 1 字段. 5 件套 1:1 同步, 1 漏 1 = 6 verb VFX 视觉组 漂动 (调色漂动 / 几何互混 / layer 漏 L5 / 跨段镜像漏 1 边 / 成就 PNG 双路径漏 1 路径).
- **修复** (跨 11 任务 ~135 轮落地后已知 5 段 canonical 1:1 序列 + 6 verb 0 例外):
  - **T098 (#65) 5 verb 视觉组奠基 + 6 verb 调色六元组落地 (5 verb)** — `src/scripts/pulse_vfx.gd` / `src/scripts/bind_vfx.gd` / `src/scripts/cut_vfx.gd` / `src/scripts/echo_vfx.gd` 4 verb (Pulse / Bind / Cut / Echo) 4 套几何 1:1 严格分离 (圆环 / 螺旋 / 弧斩 / 护盾) + 4 verb `@export` color 字段 1:1 镜像 §9.6.4 调色六元组 (Pulse Coral / Bind Violet / Cut Amber / Echo Cyan 4 entry) + 4 verb 5+ layer 视觉组 (Pulse 5 layer core+ring×3+spark+arc / Bind 5 layer core+spiral+wave+pull / Cut 5 layer slash+edge+flash+fragment+trail / Echo 6 layer shield+halo+prism×8+core+reflect+reflect_flash), 0 触碰 §9.6.16 5 件套 hit handler (T098 落地时 hit handler 走屏染 + 屏抖 + 音频 3 件套 0 走 VFX flash 1 件套, T170c 后才升级到 VFX flash).
  - **T158 (#89) Echo 棱镜 × 8 + 中心暖点** — `src/scripts/echo_vfx.gd` 加 8 方向棱镜光线 (T158 升级 Echo 5 layer → 6 layer, 加 prism × 8 + halo + core 球心 + reflect 反弹 4 件, 与 Pulse 圆环 0 互混 0 复用 0 共享, 1 漏 prism 数量 = 玩家看到 6 棱镜误以为是 Bind 螺旋).
  - **T170a + T170c + T170d 5 verb VFX flash 接口落地** — `src/scripts/pulse_vfx.gd.flash_hit(target_pos)` + `bind_vfx.gd.flash_hit(target_pos)` + `cut_vfx.gd.flash_hit(target_pos)` 3 verb VFX flash 接口 1:1 严格分离 (T170c 落地 `_current_pulse_vfx.flash_hit(target.global_position)` 调点, T170d 同步 Bind / Cut 1:1 复制 0 改调点), 0 触碰 §9.6.16 5 件套 hit handler 既有屏染 + 屏抖 + 音频 3 件套 (T170c 升级到 VFX flash 是"加 1 件" 0 改既有 3 件).
  - **T181 (#97) 5 verb 闭环 19 cue + 5 verb VFX 100% 闭环** — `src/scripts/pulse_vfx.gd` / `bind_vfx.gd` / `cut_vfx.gd` / `echo_vfx.gd` / `resonance_wave_vfx.gd` 5 verb 视觉组 100% 闭环 (5 verb 几何 1:1 严格分离 + 5 verb 调色六元组 1:1 + 5 verb 5+ layer 1:1 + 5 verb 跨段 1:1 镜像 §9.6.16 hit handler + §9.6.18 ability base + §9.6.19 windup VFX base 3 跨段), 0 例外 0 漂移, 5 verb VFX 视觉组 100% 闭环.
  - **F013.E (#159) 6 verb Whisper 接入** — `src/scripts/whisper_vfx.gd` 加 class_name + extends Node2D + 5 layer 视觉组 (L1 OUTER_FILL + L2 SPHERE_RING + L3 EDGE_HIGHLIGHT + L4 CORE_DOT + L5 HIT_FLASH × N) + `@export var whisper_color: Color = Color("#C8A4D8")` mauve 调色 + 1:1 镜像 §9.6.4 调色六元组 6 verb entry + 1:1 镜像 §9.6.16 `_on_whisper_hit` 5 件套 hit handler + 1:1 镜像 §9.6.18 `whisper_ability.gd` 16 件套 ability base + 1:1 镜像 §9.6.19 `whisper_windup_vfx.gd` 7 件套 windup VFX base + 1:1 镜像 §9.6.21 `whisper_used` 字段 + `whisper_icon` ICON_COLORS + PNG 双路径, 6 跨段镜像 0 漏 1 边 0 改 1 字段, 6 verb 视觉组 100% 闭环 (5 verb + 1 verb = 6 verb).
  - **T241 (#159) 6 verb 字段 + 6 verb 调色 1:1 严格** — `src/autoload/player_stats.gd` 加 `whisper_used: int = 0` 字段 (6 verb `<verb>_used` 字段 1:1 严格) + `record_ability_used("whisper")` 分支 (6 branch 1:1 严格) + `get_stat("whisper_used")` entry (6 entry 1:1 严格) + `set_stat("whisper_used", ...)` entry (6 entry 1:1 严格) + `reset_run` 加 `whisper_used = 0` 1 行 (6 行 reset 顺序 1:1 严格) + `all_abilities_used` 6 项 `and` 检查 0 漏 0 反序 0 改 `>= 1` 阈值 + `STYLE_GUIDE.md` §9.6.4 调色六元组加 `Whisper Mauve #C8A4D8 Color(0.784, 0.643, 0.847, 1.0)` 1 entry (6 verb 1:1 严格).
  - **T251 (#169) Whisper 5 layer 视觉组 polish + L3 EDGE_HIGHLIGHT + L5 HIT_FLASH** — `src/scripts/whisper_vfx.gd` 加 L3 EDGE_HIGHLIGHT (1.04× R 1px α0.40 外侧薄光) + L5 HIT_FLASH × N (Warm Parchment 2..4px α0.70 0.15s 衰减命中闪烁) 2 layer, 与既有 L1 OUTER_FILL + L2 SPHERE_RING + L4 CORE_DOT 3 layer 1:1 严格分离 (5 layer 0 漏 0 反序 0 改 1 layer 0 改 1 alpha 公式), `_on_whisper_hit` 从 pass 占位升级到 `_current_whisper_vfx.flash_hit(target.global_position)` 1:1 镜像 §9.6.16 5 件套 hit handler 既有 5 verb `flash_hit` 调点 0 改调点 0 改 1 字段, 0 触碰 §9.6.2 5 layer 视觉组 既有 `sin(t * PI)` 共享起伏 (5 layer 共享 1 个 `t` 变量 + 5 个 `*_alpha = sin(t * PI) * <const>` 派生 alpha 公式 0 改 0 删 0 重排).
  - **T252 (#171) `flash_hit` 跨 6 verb VFX 1:1 严格** — `pulse_vfx.gd` / `bind_vfx.gd` / `cut_vfx.gd` / `echo_vfx.gd` / `resonance_wave_vfx.gd` / `whisper_vfx.gd` 6 verb VFX 6 个 `flash_hit(target_pos)` 函数 1:1 严格分离 (6 函数 0 互混 0 复用 0 共享 1 行), T252 把 T170c 5 verb flash_hit 升级到 6 verb (加 Whisper 1 verb) 0 改既有 5 verb 函数体 0 改 1 字段 0 改 1 调点.
  - **T270 (#189) Whisper L6 EXPANDING_RING 玩家可读性 v7 强化** — `src/scripts/whisper_vfx.gd` 加 L6 EXPANDING_RING (0.60→1.20× R α0.50→0 1px 0.15s 涟漪外环) 1 个 layer, 5 layer → 6 layer 升级 0 反序 0 漏 0 改 1 既有 layer, 0 触碰 §9.6.2 5 layer 视觉既有 5 layer alpha 公式 (L6 独立计算 `var expanding_alpha: float = (1.0 - age / 0.15) * 0.50` 0 复用 L1-L5 `sin(t * PI)` 起伏), 0 触碰 §9.6.16 `_on_whisper_hit` 既有 flash_hit 调点 0 改 1 字段, 0 触碰 §9.6.4 调色六元组 `Whisper Mauve` 1 hex 0 改 1 通道值.
  - 跨 11 任务 ~135 轮落地后 0 漂移, 6 verb VFX 视觉组 5 段 canonical 1:1 序列 100% 闭环, 6 verb 调色 1:1 严格分离 (5 verb + 1 verb = 6 verb, 0 漏 0 重叠 0 互混 0 复用 0 共享) + 6 verb 几何 1:1 严格分离 (圆环 / 螺旋 / 弧斩 / 护盾 / 光环 / 球, 0 互混 0 复用 0 共享) + 6 verb 5+ layer 视觉组 1:1 严格分离 (Pulse 5 / Bind 5 / Cut 5 / Echo 6 / Wave 6 / Whisper 6 layer, 0 漏 1 layer) + 6 verb 跨段 1:1 镜像 (§9.6.16 5 件套 hit handler + §9.6.18 16 件套 ability base + §9.6.19 7 件套 windup VFX base + §9.6.21 8 段成就序列, 4 跨段 0 漏 1 边). 11 任务 0 后续 主要 pre-existing 风险.
- **预防**:
  1. 任何 polish 期给"加新 1 verb 接入 VFX 视觉组"或"修 1 verb VFX 调色"或"加新 1 verb VFX layer"或"加新 1 跨段镜像 (hit handler / ability base / windup VFX base / 成就 PNG)"任务加新字段 / 加新方法 / 加新 layer / 加新 verb 时**必须**严格按 5 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 改 `class_name <Verb>VFX` 模板** (T098 + F013.E 落地后 6 verb class_name 1:1 严格, `class_name PulseVFX` / `class_name BindVFX` / `class_name CutVFX` / `class_name EchoVFX` / `class_name ResonanceWaveVFX` / `class_name WhisperVFX` 6 class_name 0 改 0 重命名, 改 1 字符破坏 `_current_<verb>_vfx` autoload 引用), **Stage 2 0 改 6 verb `@export` color 字段名 1:1 镜像 §9.6.4 调色六元组** (T098 + F013.E + T241 落地后 6 verb color 字段 1:1 严格, `ring_color` / `core_color` / `fade_color` / `wave_color` Pulse 4 字段 + `core_color` / `wave_color` / `pull_color` Bind 3 字段 + `slash_color` / `edge_color` / `flash_color` Cut 3 字段 + `shield_color` / `halo_color` / `prism_color` / `core_color` Echo 4 字段 + `outer_ring_color` / `inner_ring_color` / `prism_color` Wave 3 字段 + `whisper_color` Whisper 1 字段, 改 1 字段名破坏 §9.6.4 调色六元组 entry 引用), **Stage 3 0 改 6 verb 5+ layer 视觉组 0 反序 0 漏 0 改 1 layer** (T098 + T158 + T170a/c/d + T181 + F013.E + T251 + T270 落地后 6 verb 5+ layer 1:1 严格, Pulse 5 layer core+ring×3+spark+arc / Bind 5 layer / Cut 5 layer / Echo 6 layer shield+halo+prism×8+core+reflect+reflect_flash / Wave 6 layer L1-L6 / Whisper 6 layer L1-L6 0 漏 0 反序 0 改 1 layer 0 改 1 alpha 公式), **Stage 4 0 改 6 verb 几何 1:1 严格分离** (5 verb + 1 verb 6 套视觉 motif 0 互混 0 复用 0 共享, 圆环 / 螺旋 / 弧斩 / 护盾 / 光环 / 球 0 例外, 改 1 几何 = 视觉组连贯破坏 玩家看到 2 个圆环状 VFX 误以为是同 1 套), **Stage 5 0 改 6 verb 跨段 1:1 镜像 4 跨段** (与 §9.6.16 5 件套 hit handler 0 漏 1 边 + 与 §9.6.18 16 件套 ability base 0 漏 1 边 + 与 §9.6.19 7 件套 windup VFX base 0 漏 1 边 + 与 §9.6.21 8 段成就序列 0 漏 1 边, 1 verb 1 跨段 4 边 0 漏 0 改 1 字段). 任何"加新 1 verb 接入 VFX 视觉组"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格), **再**在 `src/scripts/<verb>_vfx.gd` 加 class_name + extends Node2D + 5+ layer 视觉组 (Stage 1 + 3 1:1 严格, 0 触碰既有 5 verb VFX), **再**按 §9.6.18 16 件套 ability base 1:1 复制 (Stage 5 1:1 严格), **再**按 §9.6.19 7 件套 windup VFX base 1:1 复制 (Stage 5 1:1 严格), **再**按 §9.6.16 5 件套 hit handler 1:1 复制 (Stage 5 1:1 严格), **再**按 §9.6.21 8 段成就序列 1:1 复制 (Stage 5 1:1 严格), 0 触碰 §9.6.2 既有 5 layer 视觉组既有 5 verb 任何 1 字段 1 alpha 公式 1 layer 顺序.
  2. **5 段序列 0 触碰边界** — `src/scripts/pulse_vfx.gd` / `bind_vfx.gd` / `cut_vfx.gd` / `echo_vfx.gd` / `resonance_wave_vfx.gd` / `whisper_vfx.gd` 6 verb VFX 文件任何 const / var / `@export` / `_draw` 调点 0 改 0 删 0 重排 (Stage 1-4 既有 1 字段 1 调点 0 改), 任何"加新 1 verb 调色"polish 走 Stage 2 1:1 严格 (改 `whisper_color` 时同步改 §9.6.4 调色六元组 6 verb entry 0 改 1 hex 0 改 1 通道值); 任何"加新 1 verb VFX layer"polish 走 Stage 3 1:1 严格 (在 `_draw` 函数体顶部 docblock 写明新 layer 作用, 0 反序 0 漏 0 改 1 既有 layer, 0 改既有 `sin(t * PI)` 共享起伏公式, L5 HIT_FLASH 必须独立 alpha 公式 0 复用 L1-L4); 任何"修 1 verb VFX 几何"polish 走 Stage 4 1:1 严格 (0 改 1 verb 几何主 motif, 1 verb 1 几何 0 例外 圆环 / 螺旋 / 弧斩 / 护盾 / 光环 / 球 0 互混 0 复用 0 共享); 任何"加新 1 跨段镜像"polish 走 Stage 5 1:1 严格 (1 verb 1 跨段 4 边 0 漏 1 边, hit handler + ability base + windup VFX base + 成就 PNG 4 跨段 1:1 镜像).
  3. **6 verb 调色 0 改 1 hex** (T098 + F013.E + T241 落地后 0 触碰边界) — Pulse Coral `#E86D5A` (0.91, 0.43, 0.35, 1.0) / Bind Violet `#65506A` (0.40, 0.31, 0.42, 1.0) / Cut Amber `#F2B66E` (0.95, 0.71, 0.43, 1.0) / Echo Cyan `#69C7CE` (0.41, 0.78, 0.81, 1.0) / Wave Pale Resonance `#B7E7DD` (0.72, 0.91, 0.87, 1.0) / Whisper Mauve `#C8A4D8` (0.78, 0.64, 0.85, 1.0) 6 verb 调色 1:1 严格分离 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 hex / 1 通道值 = 6 verb 视觉组 漂动 (Stage 2 漂动) + §9.6.4 调色六元组 entry 漂动 + §9.6.21 ICON_COLORS 漂动 + §9.6.3 HUD 6 行 6 色色域分工 6 通道 漂动 + §9.6.5 tooltip 视觉组连贯 8 行拼接 漂动, source-grep 验证 6 verb `@export` color 字段 + §9.6.4 调色六元组 6 entry + §9.6.21 ICON_COLORS 6 entry + §9.6.3 HUD 6 行 6 色 6 通道 0 漏 0 改 1 字段.
  4. **5+ layer 视觉组 0 反序 0 改 1 layer** (T251 + T270 落地后 0 触碰边界) — Pulse 5 layer (core 球心 → 3 ring 圆环 → spark 碎片 → waveform_arc 弧) / Bind 5 layer (core 球心 → spiral 螺旋 → wave 波 → pull 牵引) / Cut 5 layer (slash 弧 → edge 刃 → flash 闪 → fragment 碎片 → trail 拖尾) / Echo 6 layer (shield 护盾球 → halo 光环 → prism × 8 棱镜 → core 球心 → reflect 反弹 → reflect_flash 反弹闪) / Wave 6 layer (L1 DEEP_SHADOW → L2 INNER_HALO → L3 RING_FILL → L4 RING_STROKE → L5 PRISM_RAYS × 8 → L6 OUTER_WISPS × 12) / Whisper 6 layer (L1 OUTER_FILL → L2 SPHERE_RING → L3 EDGE_HIGHLIGHT → L4 CORE_DOT → L5 HIT_FLASH × N → L6 EXPANDING_RING) 6 verb 5+ layer 0 反序 0 漏 0 改 1 layer 0 改 1 alpha 公式 0 改 1 painter's order, 任何"加新 1 layer"必须在文件顶部 docblock 写明 5+ layer 各自作用 + alpha 公式, source-grep 验证 6 verb `_draw` 函数 5+ `draw_*` 调点 0 漏 0 改 1 顺序.
  5. **6 verb 几何 0 改 1 几何 motif** (T098 + F013.E 落地后 0 触碰边界) — 圆环 (Pulse) / 螺旋 (Bind) / 弧斩 (Cut) / 护盾 (Echo) / 光环 (Wave) / 球 (Whisper) 6 套视觉 motif 0 互混 0 复用 0 共享, 1 verb 1 几何 0 例外, 改 1 几何 = 视觉组连贯破坏 (玩家看到 2 个圆环状 VFX 误以为是同 1 套) + tooltip 8 行拼接 漂动 (T250 #168 `_build_verb_achievement_tooltip` "几何" 字段 0 改), source-grep 验证 6 verb `_draw` 函数 几何 motif 0 漏 0 改 1 调点 0 改 1 顺序.
  6. **跨段 1:1 镜像 0 漏 1 边** (T181 + F013.E + T251 + T252 + T270 落地后 0 触碰边界) — 1 verb 1 跨段 4 边 (与 §9.6.16 5 件套 hit handler 1:1 镜像 + 与 §9.6.18 16 件套 ability base 1:1 镜像 + 与 §9.6.19 7 件套 windup VFX base 1:1 镜像 + 与 §9.6.21 8 段成就序列 1:1 镜像), 0 漏 1 边 0 改 1 字段, 任何"加新 1 跨段"polish 必须在 4 跨段 1:1 同步, source-grep 验证 6 verb VFX 4 跨段镜像 0 漏 1 边.
  7. 已知 drift risk: 6 verb VFX `flash_hit(target_pos)` 函数 (T170c + T252 落地) **可能**漏 1 verb 1:1 镜像 (e.g. T252 落地时漏 `whisper_vfx.gd.flash_hit`, T251 #169 收回). 监控建议: 任何"未来给 6 verb VFX 加新接口"必须**先**检查 6 verb 1:1 镜像 0 漏 1 verb, 0 漏 1 边. F013.E (#159) 6 verb 接入路径是 §9.6.22 5 段序列的"母集" (1 verb 5 段 1:1 严格, §9.6.22 5 段 1 verb 完整闭环, 6 verb × 5 段 = 30 件 1:1 严格, 含 4 跨段 6 verb × 4 = 24 跨段镜像 1:1 严格).

**0 副作用**: T098 / T158 / T170a / T170c / T170d / T181 / F013.E / T241 / T251 / T252 / T270 任何 const / var 0 触碰 (6 verb VFX class_name + extends Node2D 0 改 0 删 0 重排 + 6 verb `@export` color 字段 0 改 0 删 0 重排 + 6 verb 5+ layer 视觉组 0 改 0 删 0 重排 + 6 verb 几何 motif 0 改 0 删 0 重排 + 6 verb 跨段镜像 4 边 0 改 0 删 0 重排); 0 触碰 §9.6.1 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫 (T278 0 改 6 verb `_current_<verb>_vfx` 任何字段); 0 触碰 §9.6.2 5 layer 视觉组既有 5 verb 任何 1 字段 1 alpha 公式 1 layer 顺序; 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T278 0 改 6 verb `_on_<verb>_hit` 函数体任何 const / var / 屏染 / 屏抖 / VFX flash / 音频); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T278 0 改 6 verb 主信号 + 命中信号 + 结束信号 + 扩展信号 + PlayerActionGate register / unregister); 0 触碰 §9.6.18 16 件套 verb ability base (T278 0 改 6 verb ability 子类任何 const / var / `@export` / `@onready` / 5 共享方法 / `_exit_tree` 兜底 / `is_globally_blocking` 扩展); 0 触碰 §9.6.19 7 件套 windup VFX base (T278 0 改 6 verb windup VFX 子类任何 const / var / `_ready()` / `_process(delta)` / `_activate_windup_tween()` / `fade_out_and_free()`); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 transition flow (T278 0 改 GFC + room_transition + room_door + GameState 5 段序列); 0 触碰 §9.6.21 8 段成就序列 (T278 0 改 PlayerStats 6 verb 字段 + record_ability_used 6 branch + get_stat 6 entry + set_stat 6 entry + reset_run 6 行 + all_abilities_used 6 项 + achievements.json 4 tier ID + ICON_COLORS 6 entry + PNG 双路径); 0 触碰 §9.6.4 调色六元组 + §9.6.3 HUD 6 行 6 色色域分工 6 通道 + §9.6.5 tooltip 视觉组连贯 8 行拼接既有 6 verb 任何 1 hex / 1 通道值 / 1 字段 / 1 顺序 (T278 仅 0 触碰既有 6 verb 字段, 仅 0 触碰 6 verb 调色 / 几何 / layer / 跨段镜像任何 1 字段); 0 触碰 check_smoke_consistency.sh (T278 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, 6 verb VFX 任何 const / var 0 改, 含 1 verb Whisper T275 #194 收回完成); 0 行为变化, 1 文档 1 段 (§9.6.22 ~50 行) + 1 smoke test (T278 18 断言).

**与 §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 关系** — §9.6.16 覆盖 player.gd 6 verb hit handler 5 件套 1:1 严格分离 (verb ability `.trigger_player_hit` → 5 件套 `_on_verb_hit_pulse` / `_on_verb_hit_bind` / `_on_verb_hit_cut` / `_on_verb_hit_echo` / `_on_verb_hit_wave` + 1 verb Whisper `_on_whisper_hit`), §9.6.17 覆盖 player.gd `_ready` 6 verb signal 桥接 5+1 件套 1:1 严格分离 (5 verb `verb_triggered` signal + 1 verb Whisper 业务特例 `whisper_ability_executed` signal), §9.6.18 覆盖 `_verb_ability_base.gd` 6 verb ability 子类共享契约 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 兜底 + 1 `is_globally_blocking` 扩展 = 16 件 1:1 严格分离), §9.6.19 覆盖 `_verb_windup_vfx_base.gd` 6 verb windup VFX 子类共享契约 (3 字段 + 1 `_ready()` + 1 `_process(delta)` + 1 `_activate_windup_tween()` + 1 `fade_out_and_free()` = 7 件 1:1 严格分离), §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow 5 段 canonical 1:1 序列 (Stage 1 触发检测 + Stage 2 GFC 状态转换 + Stage 3 fade_out + Stage 4 场景切换 + Stage 5 新 scene 恢复), §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 canonical 1:1 序列 (Stage 1 PlayerStats 字段声明 + Stage 2 `record_ability_used` 分支 + Stage 3 `get_stat` entry + Stage 4 `set_stat` entry + Stage 5 `reset_run` 重置 + Stage 6 `all_abilities_used` 条件 + Stage 7 `achievements.json` tier entry + Stage 8 `ICON_COLORS` entry + PNG 双路径). §9.6.22 覆盖 6 verb VFX 视觉组 5 段 canonical 1:1 序列 (Stage 1 class_name + extends Node2D 模板 + Stage 2 6 verb 调色六元组 1:1 + Stage 3 5+ layer 视觉组 1:1 + Stage 4 6 verb 几何 1:1 严格分离 + Stage 5 6 verb 跨段 1:1 镜像). §9.6.16 / §9.6.17 关注"verb ability + signal 桥接", §9.6.18 关注"verb ability 类层级共享契约", §9.6.19 关注"verb windup VFX 类层级共享契约", §9.6.20 关注"跨 scene transition flow 时序契约", §9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格)", §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约". 7 套 polish 模式 0 反序 0 漏 0 改 1 字符. 任何"加新 1 verb 接入 VFX 视觉组"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格), **再**按 §9.6.2 5+ layer 视觉组 1:1 复制 (Stage 3 1:1 严格), **再**按 §9.6.18 16 件套 ability base 1:1 复制 (Stage 5 1:1 严格), **再**按 §9.6.19 7 件套 windup VFX base 1:1 复制 (Stage 5 1:1 严格), **再**按 §9.6.16 5 件套 hit handler 1:1 复制 (Stage 5 1:1 严格), **再**按 §9.6.21 8 段成就序列 1:1 复制 (Stage 5 1:1 严格), **再**按 §9.6.22 5 段序列 1:1 复制 (VFX 视觉组 1:1 严格). §9.6.20 与 §9.6.22 0 互混 0 复用 0 共享: §9.6.20 关注"跨 scene transition flow 时序契约" (与 verb VFX lifecycle 0 相关), §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约" (与跨 scene transition 0 相关). §9.6.22 是 7 套 polish 模式**唯一**关注"6 verb VFX 视觉组 5 段 1:1 严格分离 (Stage 1-5: class_name + 调色 + layer + 几何 + 跨段镜像)"的 1 套, 与 §9.6.16-§9.6.21 6 套 verb / VFX / transition / 成就 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.22 5 段序列走 §9.1 9 步落地的"verb" (1) + "ability" (2) + "const" (3) + "vfx" (7) 4 步 (`src/scripts/pulse_vfx.gd` / `bind_vfx.gd` / `cut_vfx.gd` / `echo_vfx.gd` / `resonance_wave_vfx.gd` / `whisper_vfx.gd` 6 verb VFX 文件 + `src/scripts/_verb_ability_base.gd` 6 verb ability base + `src/scripts/_verb_windup_vfx_base.gd` 6 verb windup VFX base + `src/autoload/player_stats.gd` 6 verb `<verb>_used` 字段 + `STYLE_GUIDE.md` §9.6.4 调色六元组 6 verb entry), 0 触碰 §9.1 9 步任何 "input" (4) + "shop" (5) + "icon" (6) + "audio" (8) + "HUD 冷光勾边" (9) 5 步 (VFX 视觉组 0 走 input map + 0 走 shop 系统 + 0 触碰 verb icon PNG 双路径 + 0 走 audio 副作用 + 0 走 HUD 渲染). 任何"加新 1 verb 接入 VFX 视觉组"必须**先**按 §9.1 9 步落地 (含 `verb` (1) + "ability" (2) + "const" (3) + "vfx" (7) 步), **再**按 §9.6.22 5 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 任何"加新 1 verb 调色"必须**先**在 `STYLE_GUIDE.md` §9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格, 0 漏 1 entry 0 改 1 hex), **再**在 `<verb>_vfx.gd` 加 `@export var <verb>_color: Color = Color("#HEX")` 1:1 镜像 (Stage 2 1:1 严格, 0 漏 1 字段 0 改 1 通道值), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "const" (3) 步 (VFX 视觉组 0 触碰 verb enum + ability 子类 + const 字段). 任何"加新 1 verb VFX layer"必须**先**在文件顶部 docblock 写明 5+ layer 各自作用 (Stage 3 1:1 严格), **再**在 `_draw` 函数体顶部加 `var *_alpha: float = ...` 派生 alpha 公式 (Stage 3 1:1 严格, 0 复用 L1-L5 `sin(t * PI)` 起伏), 0 触碰 §9.1 9 步任何 "vfx" (7) 步既有 6 verb VFX 任何 1 字段 1 alpha 公式 1 layer 顺序. 任何"加新 1 verb 几何"必须**先**在 `_draw` 函数体加新 `draw_*` 调点 (Stage 4 1:1 严格, 0 复用既有 5 verb 几何 0 改 1 调点 0 改 1 顺序), **再**在 tooltip 8 行拼接 (T250 #168) 同步加 1 几何字段 (0 改 1 字段 0 改 1 顺序), 0 触碰 §9.1 9 步任何 "verb" (1) 步既有 6 verb 任何 1 几何 motif. 任何"加新 1 跨段镜像"必须**先**在 §9.6.16 / §9.6.18 / §9.6.19 / §9.6.21 4 跨段加 1 镜像 (Stage 5 1:1 严格, 0 漏 1 跨段 0 改 1 字段), 0 触碰 §9.1 9 步任何 const / var / 节点 (5 段序列仅在 6 verb VFX + 6 verb ability + 6 verb windup VFX + 6 verb PlayerStats + 6 verb 调色六元组 6 跨段 24 镜像, 6 verb 子类 0 改 base 0 改, 含 1 verb Whisper T275 #194 收回完成).

### 9.6.23 7 桶 prewarm 5+1+1 polish 模式 (T066 + T184 + T185.B + F013.B + T220 跨 5 任务 ~140 轮落地) 文档化

> §9.6.15 记录 SaveSystem `audit_save_slots()` 4 状态巡检; §9.6.16 记录 player.gd 6 verb hit handler 5 件套 1:1; §9.6.17 记录 player.gd `_ready` 6 verb signal 桥接 5+1 件套 1:1; §9.6.18 记录 `_verb_ability_base.gd` 共享契约 1:1; §9.6.19 记录 `_verb_windup_vfx_base.gd` 共享契约 1:1; §9.6.20 记录 Hub ↔ archive 跨房间 transition flow 5 段; §9.6.21 记录 6 verb 成就系统 闭环 8 段; §9.6.22 记录 6 verb VFX 视觉组 5 段. 本节记录 `audio_manager_enhanced.gd` prewarm aggregator 7 桶 5+1+1 canonical 1:1 序列 (Stage 1 5 桶 baseline 预热 + Stage 2 +1 verb fire SFX 桶 + Stage 3 +1 verb cooldown READY 桶) + 3 跨调用方 1:1 镜像 aggregator — 跨 6 任务 (T066 #4 + T181 #102 + T184 #102 + T185.B #103 + F013.B #106 + T220 #142) ~140 轮迭代落地, 0 漏 0 反序 0 改 1 桶. 任何"加新 1 桶" (e.g. UI chime / save-slot preview / 7 verb+ SFX / 5 verb+ fire tail) 必须**先**确定其 5+1+1 落点 (5 桶 baseline 中插入 / +1+1 末尾追加) + **再**在 `prewarm_all_sfx()` aggregator 末尾/中间加 1 行调点, **再**在 `title_screen.gd._prewarm_bgm()` (partial 4/5 baseline inline) / `hub_controller.gd enter_hub` (full aggregator) / `game_flow_controller.gd _on_state_changed` (full aggregator) 末尾/中间加 1 行调点 (与 aggregator 顺序 1:1 镜像, 0 反序 0 漏 0 改 1 行). 跨 6 任务 0 漂动 0 漏 0 例外.

- **症状**: `audio_manager_enhanced.gd` prewarm aggregator 跨 5+1+1 = 7 个独立 `prewarm_*` 桶函数 (prewarm_music_streams + prewarm_hit_sfx + prewarm_shop_sfx + prewarm_misc_sfx + prewarm_verb_cooldown_tails + prewarm_verb_fire_sfx + prewarm_verb_cooldown_readys) 反复 polish 期间出现 6 类典型 fragile: (1) "加新 1 桶漏 aggregator 调点" — 新桶 (e.g. T220 #142 F022 加 `prewarm_verb_fire_sfx` + `prewarm_verb_cooldown_readys` 2 桶) 落地时漏 `prewarm_all_sfx()` aggregator 中加 1 行调点, 玩家 pause menu 30 min 后回 hub 走 `prewarm_all_sfx()` 重预热时漏 1 桶, 任何 stream 0 触发即 0.1s+ synth 延迟; (2) "加新 1 桶漏 title screen 4/5 baseline 调点" — 新 baseline 桶 (e.g. T184 #102 加 `prewarm_shop_sfx`) 落地时漏 `title_screen.gd._prewarm_bgm()` 末尾加 1 行 `prewarm_<category>()` inline 调点, 玩家进游戏走 title screen 0 触发新 baseline 桶, archive_01 第一次 cast 全部 0.1s+ synth 延迟 (虽然 aggregator 函数体已加); (3) "加新 1 桶漏 hub/state aggregator 调点" — 新桶落地时漏 `hub_controller.gd enter_hub` 或 `game_flow_controller.gd _on_state_changed` 末尾加 1 行 aggregator 调点, 玩家 pause menu 30 min 后回 hub 走 `prewarm_all_sfx()` 重预热时漏 1 桶, 任何 stream 0 触发即 0.1s+ synth 延迟; (4) "修 1 桶顺序漏 aggregator 顺序 0 同步" — 修 `prewarm_verb_cooldown_tails` 桶顺序时漏 `prewarm_all_sfx()` aggregator 顺序 0 同步, gameplay timing 错乱 (e.g. fire 在 tail 之前 → cooldown events 紧邻 cast 0 完整闭环); (5) "加新 1 桶 5+1+1 结构 0 标注" — 新桶落地时 0 标注其 5+1+1 落点 (5 桶 baseline 中插入 / +1+1 末尾追加), 后续 polish 期"加新 1 桶"时 0 知道 5+1+1 顺序 → 漂到末尾或漏 aggregator 调点; (6) "title screen partial 4/5 baseline vs aggregator full 7 桶 漂动" — title screen inline partial prewarm (4/5 baseline buckets) vs aggregator full prewarm (7 桶) 设计意图混: title screen 设计为 partial inline (无 cooldown state 时 4 个最常用 baseline 桶 inline, 0 走 aggregator), aggregator 设计为 full canonical 7 桶 (5+1+1 顺序, 0 漏 0 改 1 桶), polish 期若漏 title screen 4/5 baseline 调点 0 区分 (e.g. T185.B #103 加 `prewarm_misc_sfx` 在 title screen 加 inline 调点 vs T220 #142 加 `prewarm_verb_fire_sfx` 不在 title screen 加 inline 调点, 因为 verb_fire 是 cast 触发而非 title screen 触发), 会导致 title screen partial inline 与 aggregator full canonical 1:1 镜像错位. 6 类症状根因: 7 桶 prewarm aggregator 5+1+1 canonical 1:1 序列 + 3 跨调用方 (title_screen partial inline + hub_controller full aggregator + game_flow_controller full aggregator) 0 文档化, polish 期只能"凭记忆"同步 N 处调点 (1 aggregator + 1 title screen 4 inline + 2 aggregator cross-caller), 1 漏 1 = prewarm 链断.

- **触发场景**: `audio_manager_enhanced.gd` prewarm aggregator 7 桶 5+1+1 canonical 1:1 序列 + 3 跨调用方 1:1 镜像落地需同步 4 件套: (1) **`audio_manager_enhanced.gd` 7 桶 `prewarm_*` 函数 顶层模板** — `prewarm_music_streams()` (T066 #4) + `prewarm_hit_sfx()` (T181 #102) + `prewarm_shop_sfx()` (T184 #102) + `prewarm_misc_sfx()` (T185.B #103) + `prewarm_verb_cooldown_tails()` (F013.B #106) + `prewarm_verb_fire_sfx()` (T220 #142) + `prewarm_verb_cooldown_readys()` (T220 #142) 7 桶 0 改 `prewarm_*` 命名 (`prewarm_` 前缀 1:1 严格, 0 改 0 重命名, 改 1 函数名 = `prewarm_all_sfx()` aggregator 调点 0 触发 / title screen inline 调点 0 触发 / hub_controller 调点 0 触发); (2) **`prewarm_all_sfx()` aggregator 5+1+1 canonical 顺序** — 5 桶 baseline `prewarm_music_streams() → prewarm_hit_sfx() → prewarm_shop_sfx() → prewarm_misc_sfx() → prewarm_verb_cooldown_tails()` 0 反序 0 漏 + +1 `prewarm_verb_fire_sfx()` 第 6 桶 0 反序 (在 tail 之后, cooldown events 紧邻 cast 顺序) + +1 `prewarm_verb_cooldown_readys()` 第 7 桶 0 反序 (在 fire 之后, ready 是 cooldown 完成事件, 后续 cast 之前的 0 延迟是 "我可以再 cast 了" 反馈) = 7 桶 1:1 严格, gameplay timing 前向走 (BGM starts → first cast fires → cooldown tail → cooldown complete → ready jingle → next cast); (3) **3 跨调用方 1:1 镜像 (title_screen partial inline + hub_controller full aggregator + game_flow_controller full aggregator)** — `title_screen.gd._prewarm_bgm()` 4/5 baseline inline 调点 (music + hit + shop + misc, T181/T184/T185.B 3 轮迭代沉淀, 0 走 aggregator 是设计意图: title screen 无 cooldown state, 4/5 baseline 桶足够, 0 触碰 verb_cooldown_tails + verb_fire + verb_cooldown_readys 3 桶 inline) + `hub_controller.gd enter_hub` 末尾 `ame.call("prewarm_all_sfx")` 调点 (1 行, has_method 守卫包裹 0 改, T184 #102 落地, full aggregator 7 桶 0 漏) + `game_flow_controller.gd _on_state_changed` 末尾 `ame_prewarm.call("prewarm_all_sfx")` 调点 (1 行, has_method 守卫包裹 0 改, T184 #102 落地, full aggregator 7 桶 0 漏) = 3 跨调用方 partial + full + full 1:1 镜像 aggregator, 1 漏 1 边 = prewarm 链断; (4) **5+1+1 桶内 0 改 cache 守卫** — 7 桶每桶 idempotent guard (e.g. `if not _verb_cooldown_tail_streams.has(verb_name):` 守卫 0 改 0 删 0 重排, T184 #102 落地后 0 触碰, 7 桶 aggregator 可重复触发 0 副作用). 4 件套 1:1 同步, 1 漏 1 = prewarm 链断 (新桶 0 触发) / gameplay timing 错乱 (桶顺序错) / 跨调用方漏 1 边 (1 处 scene 0 走 prewarm) / title screen partial vs aggregator full 漂动 (4/5 baseline vs 7 桶 0 区分).

- **修复** (跨 6 任务 ~140 轮落地后已知 5+1+1 canonical 1:1 序列 + 7 桶 0 例外):
  - **T066 (#4) 第 1 桶 music baseline** — `src/scripts/audio_manager_enhanced.gd` 加 `prewarm_music_streams()` 函数 (T066 落地, ~9 BGM 主题 0 合成延迟, title screen `_prewarm_bgm` 阶段一次性预热 9 stream), 1 桶 baseline 0 触碰后续 6 桶.
  - **T181 (#102) 第 2 桶 hit baseline** — `src/scripts/audio_manager_enhanced.gd` 加 `prewarm_hit_sfx()` 函数 (T181 落地, 4 verb hit 12 stream 0 合成延迟, ~15 ms 一次性), 配合 `prewarm_all_sfx()` aggregator 1.0 版 (music → hit, 2 桶 baseline), 0 触碰 T066 music 桶.
  - **T184 (#102) 第 3 桶 shop baseline + aggregator 4 件套 1:1 镜像落地** — `src/scripts/audio_manager_enhanced.gd` 加 `prewarm_shop_sfx()` 函数 (T184 落地, 5 shop SFX 0 合成延迟) + `prewarm_all_sfx()` aggregator 加 1 行 `prewarm_shop_sfx()` (3 桶 aggregator 1.1 版) + `title_screen.gd._prewarm_bgm()` 末尾加 1 行 `ame.call("prewarm_all_sfx")` 调点 (1 行) + `hub_controller.gd enter_hub` 末尾加 1 行 `ame.call("prewarm_all_sfx")` 调点 (1 行, has_method 守卫) + `game_flow_controller.gd _on_state_changed` 末尾加 1 行 `ame_prewarm.call("prewarm_all_sfx")` 调点 (1 行, has_method 守卫), 3 跨调用方 1:1 镜像落地.
  - **T185.B (#103) 第 4 桶 misc baseline** — `src/scripts/audio_manager_enhanced.gd` 加 `prewarm_misc_sfx()` 函数 (T185.B 落地, F014 achievement unlock chime + F015 save-slot delete click 2 类 SFX 0 合成延迟, ~3 ms 一次性) + `prewarm_all_sfx()` aggregator 加 1 行 `prewarm_misc_sfx()` (4 桶 aggregator 1.2 版), 0 触碰 T066+T181+T184 3 桶既有顺序 0 反序 0 漏.
  - **F013.B (#106) 第 5 桶 verb_cooldown_tail baseline** — `src/scripts/audio_manager_enhanced.gd` 加 `prewarm_verb_cooldown_tails()` 函数 (F013.B 落地, 5 verb cooldown TAIL jingle 5 stream 0 合成延迟, ~6 ms 一次性) + `prewarm_all_sfx()` aggregator 加 1 行 `prewarm_verb_cooldown_tails()` (5 桶 aggregator 1.3 版 final), 0 触碰 T066+T181+T184+T185.B 4 桶既有顺序 0 反序 0 漏 0 改 1 桶, 5 桶 baseline 全部到位 (5 verb cooldown TAIL 5 stream 全部 0 合成延迟, verb 自身事件 + cast 紧邻播放链完整).
  - **T220 (#142) F022 +1 verb fire SFX + +1 verb cooldown READY 桶** — `src/scripts/audio_manager_enhanced.gd` 加 `prewarm_verb_fire_sfx()` 函数 (T220 落地, 5 verb fire SFX 5 stream 0 合成延迟, ~15 ms 一次性) + `prewarm_verb_cooldown_readys()` 函数 (T220 落地, 5 verb cooldown READY jingle 5 stream 0 合成延迟, ~10 ms 一次性) + `prewarm_all_sfx()` aggregator 加 2 行 `prewarm_verb_fire_sfx()` (第 6 桶, tail 之后) + `prewarm_verb_cooldown_readys()` (第 7 桶, fire 之后), 5 桶 → 7 桶 完整闭环, gameplay timing 前向走 (BGM starts → first cast fires → cooldown tail → cooldown complete → ready jingle → next cast). 30 verb-related stream (5 fire + 5 hit + 5 tail + 5 ready + 5 成就 chime + 5 misc) 全部 0 合成延迟从 session 1 frame 0. 0 触碰 5 桶 baseline 顺序 0 反序 0 漏 0 改 1 桶; 0 触碰 3 跨调用方 (`title_screen.gd._prewarm_bgm` + `hub_controller.gd enter_hub` + `game_flow_controller.gd _on_state_changed`) 0 改 1 字符 0 删 1 行 0 触碰 has_method 守卫.
  - 跨 6 任务 ~140 轮落地后 0 漂动 0 漏, 7 桶 prewarm 5+1+1 canonical 1:1 序列 100% 闭环, 5 桶 baseline (music + hit + shop + misc + verb_cooldown_tail) 1:1 严格分离 (0 漏 0 反序 0 改 1 桶 0 改 1 桶顺序) + +1+1 末尾追加 (verb_fire + verb_cooldown_ready, 0 漏 0 反序 0 改 1 桶 0 改 1 桶顺序) + 3 跨调用方 1:1 镜像 aggregator (title_screen + hub_controller + game_flow_controller, 0 漏 1 边 0 改 1 调点). 6 任务 0 后续 主要 pre-existing 风险.

- **预防**:
  1. 任何 polish 期给"加新 1 桶 prewarm 类别" (e.g. UI chime / save-slot preview / 7 verb+ SFX / 5 verb+ fire tail / 8 verb+ ready jingle) 任务加新函数 / 加新桶 / 加新 verb 时**必须**严格按 5+1+1 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 桶. 5+1+1 0 触碰边界: **5 桶 baseline 0 改 0 漏 0 反序** (T066+T181+T184+T185.B+F013.B 落地后 `prewarm_music_streams` + `prewarm_hit_sfx` + `prewarm_shop_sfx` + `prewarm_misc_sfx` + `prewarm_verb_cooldown_tails` 5 桶 1:1 严格, 改 1 桶名 = aggregator 调点 0 触发, 改 1 桶顺序 = gameplay timing 错乱), **+1 桶 0 改 0 漏 0 反序** (T220 落地后 `prewarm_verb_fire_sfx` 第 6 桶 0 改 0 漏 0 改顺序 = fire 0 在 tail 之后, cooldown events 紧邻 cast 0 完整闭环), **+1 桶 0 改 0 漏 0 反序** (T220 落地后 `prewarm_verb_cooldown_readys` 第 7 桶 0 改 0 漏 0 改顺序 = ready 0 在 fire 之后, "我可以再 cast 了" 反馈 0 完整闭环), **3 跨调用方 0 改 0 漏 0 反序** (`title_screen.gd._prewarm_bgm` 4/5 baseline inline + `hub_controller.gd enter_hub` 1 aggregator + `game_flow_controller.gd _on_state_changed` 1 aggregator 3 调点 0 改 0 漏 0 改 1 行, 改 1 调点 = 该 scene 0 走 prewarm). 任何"加新 1 桶"必须**先**确定其 5+1+1 落点 (5 桶 baseline 中插入 / +1+1 末尾追加) + title_screen 是否需要 inline (baseline 5 桶 yes / +1+1 桶 no, 因为 +1+1 桶需要 cast/cooldown 触发), **再**在 `audio_manager_enhanced.gd` 加 1 个 `prewarm_<category>` 函数 (顶层模板 1:1 严格, 0 改 0 删 0 重排 7 桶既有函数体), **再**在 `prewarm_all_sfx()` aggregator 末尾/中间加 1 行调点 (5+1+1 顺序 1:1 严格, 0 反序 0 漏 0 改 1 行), **再**(若 baseline 5 桶)在 `title_screen.gd._prewarm_bgm()` 末尾/中间加 1 行 `prewarm_<category>()` inline 调点 (partial 4/5 → 4/5 + 1 baseline 镜像, 0 改 0 删, has_method 守卫 0 改), **再**在 `hub_controller.gd enter_hub` 末尾/中间加 1 行 aggregator 调点 (full aggregator 1:1 镜像, has_method 守卫 0 改 0 删), **再**在 `game_flow_controller.gd _on_state_changed` 末尾/中间加 1 行 aggregator 调点 (full aggregator 1:1 镜像, has_method 守卫 0 改 0 删).
  2. **5+1+1 桶顺序 0 改 1 顺序** (T066 + T181 + T184 + T185.B + F013.B + T220 落地后 0 触碰边界) — 7 桶顺序 `prewarm_music_streams → prewarm_hit_sfx → prewarm_shop_sfx → prewarm_misc_sfx → prewarm_verb_cooldown_tails → prewarm_verb_fire_sfx → prewarm_verb_cooldown_readys` 按 gameplay timing 前向走, 0 反序 0 漏 0 改 1 桶, 任何"加新 1 桶"必须在文件顶部 docblock 写明其 5+1+1 落点 (5 桶 baseline 中插入 / +1+1 末尾追加) + gameplay timing 理由 + title_screen 是否需要 inline (baseline 5 桶 yes / +1+1 桶 no), source-grep 验证 7 桶 `prewarm_*` 函数 + `prewarm_all_sfx()` aggregator 7 行调点 0 漏 0 改 1 顺序.
  3. **3 跨调用方 0 漏 1 边** (T184 #102 + T220 #142 落地后 0 触碰边界) — `title_screen.gd._prewarm_bgm()` 4/5 baseline inline 调点 (4 个 `if ame and ame.has_method("prewarm_<category>"): ame.call("prewarm_<category>")` 块, music + hit + shop + misc, T181 #102 / T184 #102 / T185.B #103 3 轮迭代沉淀, 0 触碰 0 改 0 删 0 重排, has_method 守卫 0 改) + `hub_controller.gd enter_hub` 末尾 `ame.call("prewarm_all_sfx")` 调点 (1 行, has_method 守卫包裹 0 改 0 删 0 重排, T184 #102 落地) + `game_flow_controller.gd _on_state_changed` 末尾 `ame_prewarm.call("prewarm_all_sfx")` 调点 (1 行, has_method 守卫包裹 0 改 0 删 0 重排, T184 #102 落地) = 3 跨调用方 (partial inline + full aggregator + full aggregator) 0 漏 1 边 0 改 1 调点, 任何"加新 1 跨调用方" (e.g. settings_menu._ready / save_load_menu._ready / shop_menu._ready) 时**必须**在末尾加 1 行 aggregator 调点 (1:1 镜像, has_method 守卫 0 改 0 删), source-grep 验证 3 跨调用方 0 漏 1 边.
  4. **7 桶 idempotent guard 0 改 0 删** (T184 #102 落地后 0 触碰边界) — 7 桶每桶 idempotent guard (e.g. `if not _verb_cooldown_tail_streams.has(verb_name): _verb_cooldown_tail_streams[verb_name] = _generate_verb_cooldown_tail_jingle(start_midi)` 守卫 0 改 0 删 0 重排, prewarm 跑了也再走守卫, 7 桶 aggregator 可重复触发 0 副作用), 任何"加新 1 桶"必须**先**写 idempotent guard 1 行 (e.g. `if not _<category>_streams.has(<key>): _<category>_streams[<key>] = _generate_<category>(<args>)`), **再**在桶函数体内循环 0 触碰 0 反序 0 漏, source-grep 验证 7 桶 guard 0 漏 0 改 1 行.
  5. **桶函数命名 0 改 1 字符** (T066 + T181 + T184 + T185.B + F013.B + T220 落地后 0 触碰边界) — `prewarm_music_streams` / `prewarm_hit_sfx` / `prewarm_shop_sfx` / `prewarm_misc_sfx` / `prewarm_verb_cooldown_tails` / `prewarm_verb_fire_sfx` / `prewarm_verb_cooldown_readys` 7 桶函数名 0 改 0 删 0 重命名, 改 1 字符 = `prewarm_all_sfx()` aggregator 调点 0 触发 + `title_screen.gd._prewarm_bgm` 4 inline 调点 0 触发 + `hub_controller.gd enter_hub` 调点 0 触发 + `game_flow_controller.gd _on_state_changed` 调点 0 触发 = prewarm 链断, source-grep 验证 7 桶函数名 0 漏 0 改 1 字符.
  6. **0 触碰 lazy-init 守卫** (T066 + T181 落地后 0 触碰边界) — `play_pulse` / `play_bind` / `play_cut` / `play_echo` / `play_wave_fire` (F004.B #96) 内部 lazy-init 守卫生成 `_pulse_stream` / `_bind_stream` / `_cut_stream` / `_echo_stream` / `_wave_fire_stream` 5 stream 守卫 0 删 (T220 prewarm 跑了也再走守卫, 0 副作用), `play_verb_cooldown_ready(verb_name)` 内部 lazy-init 守卫生成 `_verb_cooldown_streams[verb_name]` 5 stream 守卫 0 删 (同样 0 副作用), 任何"加新 1 桶"必须**先**在 lazy-init 守卫 0 删 0 改 0 重排, **再**在 prewarm 桶函数体内 idempotent guard 0 删 0 改, source-grep 验证 5 verb lazy-init 守卫 0 删 0 改 1 行.
  7. 已知 drift risk: 7 桶 prewarm aggregator 5+1+1 canonical 1:1 序列 **可能**漏 1 跨调用方 1:1 镜像 (e.g. T184 #102 落地时漏 `game_flow_controller.gd _on_state_changed` 1 调点, pause menu 30 min 后回 hub 走 prewarm 链断, 玩家 cast 5 verb 仍有 0.1s+ synth 延迟). 监控建议: 任何"未来给 prewarm aggregator 加新 1 跨调用方" (e.g. settings_menu._ready / save_load_menu._ready / shop_menu._ready / pause_menu._ready) 必须**先**在 3 跨调用方 (`title_screen.gd._prewarm_bgm` 4/5 baseline inline + `hub_controller.gd enter_hub` 1 aggregator + `game_flow_controller.gd _on_state_changed` 1 aggregator) 1:1 镜像 0 漏 1 边. T184 (#102) 3 跨调用方 1:1 镜像落地后任何新 scene _ready 加 1 行 aggregator 调点是 §9.6.23 5+1+1 序列的"母集" (1 桶 + 1 跨调用方 1:1 镜像, 7 桶 × 3 调点 = 21 镜像 1:1 严格). 另 1 类已知 drift: title screen partial 4/5 baseline inline vs aggregator full 7 桶 设计意图混 (T220 #142 落地时 title screen 0 加 `prewarm_verb_fire_sfx` inline 调点, 因为 verb_fire 是 cast 触发而非 title screen 触发, 是设计意图而非漏调; 后续 polish 期若加新 baseline 桶必须**先**决定是否在 title screen 加 inline 调点, 加新 +1+1 桶则在 title screen 0 加 inline 调点, 是 §9.6.23 5+1+1 序列的"边界").

**0 副作用**: T066 / T181 / T184 / T185.B / F013.B / T220 任何 const / var 0 触碰 (5 verb lazy-init 守卫 0 改 0 删 0 重排 + 7 桶 `prewarm_*` 函数体 0 改 0 删 0 重排 + `prewarm_all_sfx()` aggregator 7 行调点 0 改 0 删 0 重排 + 3 跨调用方 (`title_screen.gd._prewarm_bgm` + `hub_controller.gd enter_hub` + `game_flow_controller.gd _on_state_changed`) 0 改 0 删 0 重排); 0 触碰 §9.6.4 调色六元组 (T279 0 改 6 verb 调色 1 hex); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T279 0 改 6 verb `_on_<verb>_hit` 函数体任何 const / var / 屏染 / 屏抖 / VFX flash / 音频); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T279 0 改 6 verb 主信号 + 命中信号 + 结束信号 + 扩展信号 + PlayerActionGate register / unregister); 0 触碰 §9.6.18 16 件套 verb ability base (T279 0 改 6 verb ability 子类任何 const / var / `@export` / `@onready` / 5 共享方法 / `_exit_tree` 兜底 / `is_globally_blocking` 扩展); 0 触碰 §9.6.19 7 件套 windup VFX base (T279 0 改 6 verb windup VFX 子类任何 const / var / `_ready()` / `_process(delta)` / `_activate_windup_tween()` / `fade_out_and_free()`); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 transition flow (T279 0 改 GFC + room_transition + room_door + GameState 5 段序列); 0 触碰 §9.6.21 8 段成就序列 (T279 0 改 PlayerStats 6 verb 字段 + record_ability_used 6 branch + get_stat 6 entry + set_stat 6 entry + reset_run 6 行 + all_abilities_used 6 项 + achievements.json 4 tier ID + ICON_COLORS 6 entry + PNG 双路径); 0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T279 0 改 6 verb VFX 文件 任何 const / var / `@export` color 字段 / 5+ layer 视觉组 / 几何 / 跨段镜像); 0 触碰 check_smoke_consistency.sh (T279 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, audio_manager_enhanced.gd 7 桶 prewarm 函数体 0 改 0 删 0 重排 + prewarm_all_sfx() aggregator 7 行 0 改 0 删 0 重排 + 3 跨调用方 0 改 0 删 0 重排); 0 行为变化, 1 文档 1 段 (§9.6.23 ~50 行) + 1 smoke test (T279 19+ 断言).

**与 §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 关系** — §9.6.15 覆盖 SaveSystem `audit_save_slots()` 4 状态巡检 (autoload 跨调用方 1:1 镜像); §9.6.16 覆盖 player.gd 6 verb hit handler 5 件套 1:1 严格分离; §9.6.17 覆盖 player.gd `_ready` 6 verb signal 桥接 5+1 件套 1:1 严格分离; §9.6.18 覆盖 `_verb_ability_base.gd` 6 verb ability 共享契约 1:1 严格分离; §9.6.19 覆盖 `_verb_windup_vfx_base.gd` 6 verb windup VFX 共享契约 1:1 严格分离; §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow 5 段 canonical 1:1 序列; §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 canonical 1:1 序列; §9.6.22 覆盖 6 verb VFX 视觉组 5 段 canonical 1:1 序列. §9.6.23 覆盖 `audio_manager_enhanced.gd` prewarm aggregator 7 桶 5+1+1 canonical 1:1 序列 (5 桶 baseline + +1 verb fire SFX + +1 verb cooldown READY 桶) + 3 跨调用方 1:1 镜像 aggregator. §9.6.15 / §9.6.16 关注"autoload / verb hit handler 1:1 镜像", §9.6.17 关注"verb signal 桥接 1:1 严格分离", §9.6.18 关注"verb ability 类层级共享契约", §9.6.19 关注"verb windup VFX 类层级共享契约", §9.6.20 关注"跨 scene transition flow 时序契约", §9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格)", §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约", §9.6.23 关注"prewarm aggregator 5+1+1 序列 1:1 严格分离契约 + 3 跨调用方 1:1 镜像". 8 套 polish 模式 0 反序 0 漏 0 改 1 字符. 任何"加新 1 桶 prewarm 类别"必须**先**按 §9.6.23 5+1+1 序列 1:1 复制 (0 反序 0 漏 0 改 1 桶 0 改 1 跨调用方 1 调点), 0 触碰 §9.6.15-§9.6.22 7 套既有 polish 模式任何 1 字符. §9.6.22 与 §9.6.23 0 互混 0 复用 0 共享: §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约 (Stage 1-5: class_name + 调色 + layer + 几何 + 跨段镜像)" (与 prewarm 桶链 0 相关), §9.6.23 关注"prewarm aggregator 5+1+1 序列 1:1 严格分离契约 (5 桶 baseline + +1+1 末尾追加)" (与 6 verb VFX 视觉组 0 相关). §9.6.23 是 8 套 polish 模式**唯一**关注"prewarm aggregator 5+1+1 序列 1:1 严格分离 + 3 跨调用方 1:1 镜像"的 1 套, 与 §9.6.15-§9.6.22 7 套 audit / verb / VFX / transition / 成就 / VFX 视觉组 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.23 5+1+1 序列走 §9.1 9 步落地的"audio" (8) 步 (`src/scripts/audio_manager_enhanced.gd` 7 桶 `prewarm_*` 函数 + `prewarm_all_sfx()` aggregator + `src/scripts/title_screen.gd._prewarm_bgm` 1 调点 + `src/scripts/hub_controller.gd enter_hub` 1 调点 + `src/scripts/game_flow_controller.gd _on_state_changed` 1 调点), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "const" (3) + "input" (4) + "shop" (5) + "icon" (6) + "vfx" (7) + "HUD 冷光勾边" (9) 8 步 (prewarm 桶链 0 走 verb enum + ability 子类 + const 字段 + input map + shop 系统 + verb icon PNG 双路径 + VFX 视觉组 + HUD 渲染). 任何"加新 1 桶 prewarm 类别"必须**先**按 §9.1 9 步落地 (含 "audio" (8) 步), **再**按 §9.6.23 5+1+1 序列 1:1 复制 (0 反序 0 漏 0 改 1 桶 0 改 1 跨调用方 1 调点), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "const" (3) + "input" (4) + "shop" (5) + "icon" (6) + "vfx" (7) + "HUD 冷光勾边" (9) 8 步. 任何"加新 1 桶"必须**先**在 `audio_manager_enhanced.gd` 加 1 个 `prewarm_<category>` 函数 (顶层模板 1:1 严格, 0 漏 0 改 1 字符, idempotent guard 0 改 0 删), **再**在 `prewarm_all_sfx()` aggregator 末尾/中间加 1 行调点 (5+1+1 顺序 1:1 严格, 0 漏 0 改 1 行), **再**在 `title_screen.gd._prewarm_bgm` 末尾/中间加 1 行 aggregator 调点 (1:1 镜像, has_method 守卫 0 改 0 删), **再**在 `hub_controller.gd enter_hub` 末尾/中间加 1 行 aggregator 调点 (1:1 镜像, has_method 守卫 0 改 0 删), **再**在 `game_flow_controller.gd _on_state_changed` 末尾/中间加 1 行 aggregator 调点 (1:1 镜像, has_method 守卫 0 改 0 删), 0 触碰 §9.1 9 步任何 const / var / 节点 (5+1+1 序列仅在 7 桶 prewarm 函数 + 1 aggregator + 3 跨调用方, 6 verb 子类 0 改 base 0 改, 5 verb lazy-init 守卫 0 删 0 改).

---

### 9.6.24 6 verb HUD 6 行 6 色色域分工 6 通道 (5 段 canonical 1:1 序列) polish 模式 (T200 + T202 + T204 + T206 + T233 + T247 跨 6 任务 ~48 轮落地) 文档化

> §9.6.3 记录 HUD 6 verb 5+1 verb 7 UI 通道同步扩 verb 模式 (加新 1 verb 时 6 处 1:1 复制, T247 #164 落地模式文档化); §9.6.4 记录 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 2 4 通道 + 2 派生 = 6 通道 per verb); §9.6.5 记录 6 verb 视觉组连贯 tooltip 8 行拼接扩展器; §9.6.16 记录 player.gd 6 verb hit handler 5 件套 1:1; §9.6.22 记录 6 verb VFX 视觉组 5 段 canonical 1:1 序列. 本节记录 `src/scripts/hud.gd` 6 verb HUD 6 行 6 色色域分工 6 通道 (5 段 canonical 1:1 序列) polish 模式 — 跨 5 段 (Stage 1 6 verb row 结构 1:1 + Stage 2 6 verb 调色六元组 1:1 + Stage 3 6 verb 6 通道 per verb 1:1 = 6 verb × 6 通道 = 36 通道 1:1 + Stage 4 6 verb glow state 6 key + stylebox 6 instance 1:1 + Stage 5 6 verb reduce_flash iteration list 8 element 1:1 + 3 闭环宪法同步) — 跨 6 任务 (T200 #117 + T202 #118 + T204 #119 + T206 #123 + T233 #152 + T247 #164) ~48 轮迭代落地, 36 通道 1:1 严格分离 (6 verb × 6 通道 = 36 通道, 0 漏 0 重叠 0 互混 0 复用 0 共享), 0 漂动 0 后续 主要 pre-existing 风险. 任何"加新 1 verb HUD 接入"或"修 1 verb HUD 6 通道"或"加新 1 HUD UI 元素 (reduce_flash 范围扩展)"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格), **再**按 §9.6.3 5+1 verb 7 UI 通道 6 处 1:1 复制 (Stage 1 + 3 1:1 严格, 0 反序 0 漏 0 改 1 字段), **再**按 Stage 4 glow 1:1 严格, **再**按 Stage 5 reduce_flash 1:1 严格, **再**按 §9.6.22 6 verb VFX 视觉组 1:1 镜像 (Stage 2 调色 + Stage 3 6 通道 1:1 严格, 0 漏 0 改 1 hex 0 改 1 通道值), **再**按 §9.6.24 5 段序列 1:1 复制 (polish 模式 5 段 1:1 严格, 0 反序 0 漏 0 改 1 字段). 跨 6 verb (Pulse Coral Pulse #E86D5A / Bind Muted Violet #65506A / Cut Amber Voice #F2B66E / Echo Glass Cyan #69C7CE / Wave Pale Resonance #B7E6DC / Whisper Muted Mauve #C8A4D8) + 6 verb 6 通道 (icon sprite modulate / name label font_color / ProgressBar fill bg_color / cooldown label text color / cold glow border 1px bg_color / reduce_flash modulate overlay) + 6 verb reduce_flash iteration list 8 element (5 verb bar + 1 whisper + 1 resonance + 1 health) 0 漏 1 verb 全部走同一套 5 段 1:1 序列 0 例外.

- **症状**: `src/scripts/hud.gd` 6 verb HUD 6 行 6 色色域分工 6 通道 跨 6 verb 反复 polish 期间出现 6 类典型 fragile: (1) "加新 1 verb HUD 漏 5 段序列同步" — 新 verb 接入 HUD 时仅加 @onready var + 主题色 const + 调 _create_verb_glow_stylebox 3 件 (T247 #164 6 步 1:1 复制), 但漏 Stage 4 `_verb_glow_state` dict 6 key 加 1 key (T247 落地时漏 dict 加 `"whisper": false` 触发 Whisper 冷光勾边 0 在 _process 跑 state-change tween 0 触发) / 漏 Stage 5 reduce_flash iteration list 8 element 加 1 element (T247 落地时漏 `_whisper_cooldown` 加进 list 触发 Whisper 0 走 reduce_flash 灰化, accessibility 玩家勾选 "减弱屏幕闪烁" 时 Whisper bar 仍 100% 饱和度, 与 5 verb 不一致); (2) "修 1 verb 主题色漏 §9.6.4 调色六元组同步" — 修 Pulse `_PULSE_GLOW_COLOR` 时改硬编码 hex 0 改 §9.6.4 调色六元组 entry, 跨段 §9.6.22 6 verb VFX 视觉组 5 段 + §9.6.5 tooltip 视觉组连贯 8 行拼接 + §9.6.3 5+1 verb 7 UI 通道全部漂动 (Pulse icon / Pulse fill / Pulse glow border / Pulse name label / Pulse cooldown label / Pulse tooltip color 6 通道全 漂动, 玩家扫 HUD 时 Pulse 主色与既有 6 verb 调色六元组 0 一致); (3) "Stage 3 6 通道 1 verb 漏 1 通道" — 新 verb 接入时只画 icon + fill + glow 3 通道, 漏 name label 主题色 (T204 #119 5 verb 模式) / 漏 cooldown label 主题色 (T202 #118 5 verb 模式) / 漏 reduce_flash 灰化 (T200 #117 5 verb 模式) 3 派生通道, 玩家 hover 该 verb 时看不到 verb 名称 (0 name label) / 冷却时看不到 "冷却中" 文字 (0 cooldown label) / accessibility 模式下 bar 仍 100% 饱和度 (0 reduce_flash) = 6 通道 缺 3 通道 视觉组连贯破坏; (4) "Stage 4 glow 0 改 1 verb 调色 hex 漏 `_verb_glow_state` dict key 同步" — 加新 verb 时加 `_whisper_glow_bg = _create_verb_glow_stylebox(_WHISPER_GLOW_COLOR)` 1 行 + `_whisper_cooldown.add_theme_stylebox_override("background", _whisper_glow_bg)` 1 行, 0 在 `_verb_glow_state` dict 加 `"whisper": false` 1 key, 触发 _process 跑 `_update_verb_glow_state("whisper", ...)` 时 0 在 dict 找到 key 抛 KeyError 或 silent skip 0 冷光勾边, 玩家看不到 verb 主题色 glow 反馈 (与 5 verb 1:1 视觉组连贯崩塌); (5) "Stage 5 reduce_flash 范围扩展漏 1 element" — 修 accessibility reduce_flash 范围时 (e.g. T206 #123 把 range 从 5 verb bar 扩到 5 verb + resonance + health) 漏 1 element (e.g. 漏 `_health_container` 加进 list 触发 HUD 顶部生命条仍 100% 饱和度, 与 5 verb bar + resonance 不一致, accessibility 玩家勾 "减弱屏幕闪烁" 时仍看到生命条高饱和小铃铛), iteration list 元素 0 触碰 既有 N element 顺序 (0 反序 0 漏); (6) "Stage 1 6 verb row tscn 漏 4 子节点" — tscn 加 `WhisperRow` HBoxContainer 时漏 4 子节点 (`WhisperIcon` TextureRect / `WhisperNameLabel` Label / `WhisperCooldown` ProgressBar / `WhisperCooldownLabel` Label) 1 段, 触发 @onready var path `$MarginContainer/VBoxContainer/WhisperRow/Whisper<Elem>` 多层 path 缺 1 段 runtime "Node not found" 错. 6 类症状根因: 6 verb HUD 6 行 6 色色域分工 6 通道 5 段 canonical 1:1 序列 0 文档化, polish 期只能"凭记忆"同步 N 处 (1 Stage 1 tscn + 1 Stage 2 调色 const + 1 Stage 3 6 通道 @onready var / var / ability ref + 1 Stage 4 glow dict + 1 Stage 5 iteration list 1 元素 + 1 §9.6.22 跨段镜像 0 漏 1 边), 1 漏 1 = 6 verb HUD 6 行 6 色色域分工 6 通道 漂动.
- **触发场景**: `src/scripts/hud.gd` 6 verb HUD 6 行 6 色色域分工 6 通道 5 段 canonical 1:1 序列落地需同步 5 件套: (1) **`hud.gd` 6 verb `@onready var` 6 通道 per verb (4 主 + 2 派生) 1:1 严格分离** — 6 verb 6 row × 4 UI 元素 = 24 `@onready var` 1:1 严格 (icon / name label / cooldown bar / cooldown label) + 6 verb 6 ability reference `var _<verb>_ability` 1:1 严格 + 6 verb 6 topic-color const `_PULSE_GLOW_COLOR` / `_BIND_GLOW_COLOR` / `_CUT_GLOW_COLOR` / `_ECHO_GLOW_COLOR` / `_WAVE_GLOW_COLOR` / `_WHISPER_GLOW_COLOR` 6 entry 1:1 严格 (T247 #164 落地时 Whisper Muted Mauve #C8A4D8 1 hex 0 撞 5 verb 调色, `STYLE_GUIDE.md` §9.6.4 调色六元组 6 entry 1:1 对齐); (2) **Stage 4 6 verb glow state 6 key + stylebox 6 instance 1:1 严格** — `_verb_glow_state: Dictionary` 6 key (pulse / bind / cut / echo / wave / whisper) 0 漏 1 verb + 6 verb glow stylebox 6 instance (`_pulse_glow_bg` / `_bind_glow_bg` / `_cut_glow_bg` / `_echo_glow_bg` / `_wave_glow_bg` / `_whisper_glow_bg`) 0 漏 1 verb + 6 verb `_create_verb_glow_stylebox` 6 调点 0 漏 + 6 verb `add_theme_stylebox_override("background", _<verb>_glow_bg)` 6 调点 0 漏 (T233 #152 落地 5 verb 冷光勾边 + T247 #164 加 Whisper 第 6 verb 1:1 复制, 0 改 5 verb 既有字段); (3) **Stage 5 reduce_flash iteration list 1:1 严格** — `_apply_reduced_flash_modulate` 8 element list (5 verb bar + 1 whisper + 1 resonance + 1 health, T200 #117 落地 5 element + T206 #123 扩 7 element + T247 #164 扩 8 element 跨 3 任务迭代沉淀) 0 漏 1 element 0 反序 0 改 1 element, 1 次 modulate 写 = 6 verb 6 bar + 1 resonance + 1 health = 8 element 全灰化 (T200 5 element 起步 → T206 7 element 范围扩展 → T247 8 element 加 Whisper 第 6 verb, 跨 3 任务 0 漂动); (4) **Stage 1 6 verb row tscn 4 子节点 1:1 严格** — `hud.tscn` 6 verb 6 row (PulseRow / BindRow / CutRow / EchoRow / WaveRow / WhisperRow) 0 漏 1 row + 6 verb 6 row 4 子节点 (Icon / NameLabel / Cooldown / CooldownLabel) 0 漏 1 节点 0 改 1 节点类型 (TextureRect / Label / ProgressBar / Label) 0 改 0 漏 1 path 段; (5) **5 段序列 1:1 同步跨段镜像 §9.6.22 6 verb VFX 视觉组 + §9.6.5 tooltip 视觉组连贯 8 行拼接** — 1 verb 1 跨段 3 边 (与 §9.6.22 6 verb `@export` color 字段 1:1 镜像 Stage 2 调色六元组 0 漏 1 verb + 与 §9.6.5 `_VERB_ACHV_INFO` 6 字段 (achv_id / verb_index / color / color_name / geometry_zh / visual_group) 1:1 镜像 Stage 2 调色 + 视觉组连贯 0 漏 1 verb 0 漏 1 字段). 5 件套 1:1 同步, 1 漏 1 = 6 verb HUD 6 行 6 色色域分工 6 通道 漂动 (Stage 1 tscn 漏 1 row / Stage 2 调色 撞色 / Stage 3 6 通道 漏 1 通道 / Stage 4 glow 0 冷光勾边 / Stage 5 reduce_flash iteration list 漏 1 element / 跨段镜像漏 1 边).
- **修复** (跨 6 任务 ~48 轮落地后已知 5 段 canonical 1:1 序列 + 36 通道 0 例外):
  - **T200 (#117) Stage 5 reduce_flash 5 verb 起步** — `src/scripts/hud.gd` 加 `_REDUCED_COLOR_MODULATE := Color(0.55, 0.55, 0.6, 0.75)` + `_NORMAL_COLOR_MODULATE := Color(1.0, 1.0, 1.0, 1.0)` 2 const + `_reduced_flash_applied: bool = false` 1 var + `_apply_reduced_flash_modulate()` 函数 5 element iteration list (5 verb bar) + `GameState._on_reduce_flash_changed(reduce: bool)` 信号连接 0.12s tween, 5 verb bar 0 触碰既有 verb 调色 (饱和度降 30% + 透明度 75%, 玩家勾 "减弱屏幕闪烁" 时 5 verb bar 1 次 modulate 写 = 5 bar 全灰化, 6 verb 调色六元组 0 改 1 hex 0 改 1 通道值), 0 触碰 GameState.health / GameState.resonance 既有 铃铛 + ResonanceBar (T200 rationale "常驻/瞬时二分 = constant state 不灰化", 后续 T206 #123 扩展才覆盖).
  - **T202 (#118) Stage 3 派生通道 1/2 cooldown label 主题色 5 verb** — `src/scripts/hud.gd` 加 5 verb `_pulse_cooldown_label` / `_bind_cooldown_label` / `_cut_cooldown_label` / `_echo_cooldown_label` / `_wave_cooldown_label` 5 `@onready var` + 5 verb 主题色文本 (cooldown > 0 时 modulate alpha 0.6 显示 "冷却中" 给玩家即时反馈) + 6 通道 per verb 第 4 通道 1:1 严格分离 (与 T204 name label 主题色 0 互混 0 复用, 1 通道 1 派生), 0 触碰 T200 reduce_flash 5 element 既有顺序 0 反序 0 漏.
  - **T204 (#119) Stage 3 派生通道 2/2 name label 主题色 5 verb** — `src/scripts/hud.gd` 加 5 verb `_pulse_name_label` / `_bind_name_label` / `_cut_name_label` / `_echo_name_label` / `_wave_name_label` 5 `@onready var` + 5 verb 主题色 always-visible 7pt 小字 (位置 Icon 后、Cooldown 前, 玩家一眼看出 "这一行是哪个 verb") + 6 通道 per verb 第 2 通道 1:1 严格分离 (与 T202 cooldown label 主题色 1:1 对齐, 0 互混 0 复用 1 字段), 6 verb 调色六元组 0 改 1 hex 0 改 1 通道值 (Pulse Amber Voice #F2B66E / Bind Muted Violet #65506A / Cut Coral Pulse #E86D5A / Echo Glass Cyan #69C7CE / Wave Pale Resonance #B7E6DC, 5 verb 5 主色 0 撞). T202 + T204 跨 2 任务沉淀 6 通道 per verb 4 主 + 2 派生 = 6 通道 100% 闭环 (4 主: icon / name label / fill / cooldown label + 2 派生: reduce_flash / glow border, 后者 T233 #152 落地).
  - **T206 (#123) Stage 5 reduce_flash 范围扩展 5 verb + resonance + health = 7 element** — `src/scripts/hud.gd` 修 `_apply_reduced_flash_modulate` iteration list 从 5 element → 7 element (加 `_resonance_bar` ResonanceBar 主色 Glass Cyan #69C7CE + `_health_container` 4 bell Glass Cyan/Muted Violet, 1 次 modulate 写 = 7 element 全灰化, T200 rationale "常驻/瞬时二分" 升级为 "accessibility 偏好应当覆盖玩家感知的全部高饱和度 UI blob", 14x16 满血 bell 每帧 0.5px 玻璃青高光与 5 verb 冷却条同等刺激源, 累加效应 = "我刚勾 reduce_flash 但屏幕还是闪"), 5 verb + 2 派生 = 7 element 0 反序 0 漏 0 改 1 element 1 顺序.
  - **T233 (#152) Stage 4 cold glow border 5 verb 1:1 严格** — `src/scripts/hud.gd` 加 5 verb `_PULSE_GLOW_COLOR` / `_BIND_GLOW_COLOR` / `_CUT_GLOW_COLOR` / `_ECHO_GLOW_COLOR` / `_WAVE_GLOW_COLOR` 5 const (5 verb 5 主色 hex 0 撞 6 verb 调色六元组) + 5 verb glow stylebox 5 instance (`_pulse_glow_bg` / `_bind_glow_bg` / `_cut_glow_bg` / `_echo_glow_bg` / `_wave_glow_bg`, 每 verb 1 instance 让 `_tween_verb_glow` 可独立 tween border_color) + `_verb_glow_state: Dictionary` 5 key (pulse / bind / cut / echo / wave) → bool + `_create_verb_glow_stylebox(color: Color)` 函数 + `_update_verb_glow_state(verb_name: String, ...)` 函数 + 0.12s 双向 tween quad-ease-out 同步 T231 / T226 / T111 节奏 (verb ready 状态时 1px border 显示 verb 主题色, cooling 状态时 border alpha fade out 到 0, 玩家一眼看出 "verb 已就绪" 不必看 progress bar 数值) + 5 verb `add_theme_stylebox_override("background", _<verb>_glow_bg)` 5 调点 (替换 tscn 共用 StyleBoxFlat 引用, 5 verb 独立持有自己 stylebox), 5 verb 0 改既有 @onready var / var / 5 verb 调色六元组 0 改 1 hex 0 改 1 通道值.
  - **T247 (#164) Stage 1-3 6 verb Whisper 接入 + Stage 4 dict 6 key + Stage 5 iteration list 8 element** — `src/scripts/hud.gd` 加 `_WHISPER_GLOW_COLOR := Color(0.784, 0.643, 0.847, 1.0)` (Muted Mauve #C8A4D8, 6 verb 调色六元组第 6 行 0 撞 5 verb 调色) + 6 verb 4 `@onready var` (`_whisper_cooldown` / `_whisper_cooldown_label` / `_whisper_name_label` + 隐含 `_whisper_icon` path) + 6 verb 1 ability reference `var _whisper_ability` + 6 verb 1 glow stylebox `_whisper_glow_bg` + 6 verb 1 dict key `"whisper": false` + 6 verb 1 iteration list 8 element (T200 5 element → T206 7 element → T247 8 element) + 6 verb 1 `_create_verb_glow_stylebox(_WHISPER_GLOW_COLOR)` 调点 + 6 verb 1 `add_theme_stylebox_override("background", _whisper_glow_bg)` 调点 + 6 verb 1 `_update_verb_glow_state("whisper", ...)` 调点 + 6 verb 1 `get_node_or_null("WhisperAbility")` 守卫 + `hud.tscn` 加 `WhisperRow` (HBoxContainer) + 4 子节点 (`WhisperIcon` TextureRect / `WhisperNameLabel` Label / `WhisperCooldown` ProgressBar / `WhisperCooldownLabel` Label) + `StyleBoxFlat_whisper_fill` (Muted Mauve #C8A4D8 主题色) + ext_resource `whisper_icon.png` 引用, 5 段序列 1:1 复制 0 反序 0 漏 0 改 1 字段 0 改 1 hex 0 改 1 通道值. 5 verb 0 改既有 @onready var / var / const / 5 verb 调色六元组 0 改 1 hex 0 改 1 通道值 / 5 verb glow stylebox 0 改 1 字段 / 5 verb dict key 0 改 1 key / 7 element iteration list 0 反序 0 漏 0 改 1 element.
  - 跨 6 任务 ~48 轮落地后 0 漂动, 6 verb HUD 6 行 6 色色域分工 6 通道 5 段 canonical 1:1 序列 100% 闭环, 5 段 1:1 严格分离 (Stage 1 tscn 6 verb 6 row 4 子节点 1:1 严格 0 漏 + Stage 2 6 verb 调色六元组 1:1 严格 0 漏 0 重叠 + Stage 3 6 verb × 6 通道 = 36 通道 1:1 严格 0 漏 1 通道 0 互混 0 复用 0 共享 + Stage 4 6 verb dict 6 key 0 漏 1 verb + 6 verb glow stylebox 6 instance 0 漏 1 verb + Stage 5 reduce_flash iteration list 8 element 0 漏 1 element 0 反序 0 改 1 element 1 顺序). 36 通道 0 漂动 (6 verb × 6 通道, 含 4 主通道 + 2 派生通道, 1 verb 1 6 通道 0 例外). 6 任务 0 后续 主要 pre-existing 风险.
- **预防**:
  1. 任何 polish 期给"加新 1 verb HUD 接入"或"修 1 verb HUD 6 通道"或"加新 1 HUD UI 元素"任务加新字段 / 加新方法 / 加新 verb 时**必须**严格按 5 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 改 `hud.tscn` 6 verb row 4 子节点模板** (T247 #164 落地后 6 verb row 1:1 严格, PulseRow / BindRow / CutRow / EchoRow / WaveRow / WhisperRow 6 row 0 改 0 重命名 0 漏 1 row, 4 子节点 (Icon / NameLabel / Cooldown / CooldownLabel) 0 改 0 漏 1 节点 0 改 1 节点类型, 改 1 字段名破坏 @onready var path 多层 path 触发 "Node not found" runtime 错), **Stage 2 0 改 6 verb 调色六元组 0 改 1 hex 0 改 1 通道值** (T200 + T233 + F013.E + T241 + T247 落地后 6 verb 调色六元组 1:1 严格, Pulse Coral Pulse #E86D5A (0.91, 0.43, 0.35, 1.0) / Bind Muted Violet #65506A (0.40, 0.31, 0.42, 1.0) / Cut Amber Voice #F2B66E (0.95, 0.71, 0.43, 1.0) / Echo Glass Cyan #69C7CE (0.41, 0.78, 0.81, 1.0) / Wave Pale Resonance #B7E6DC (0.72, 0.91, 0.87, 1.0) / Whisper Muted Mauve #C8A4D8 (0.78, 0.64, 0.85, 1.0) 6 verb 调色 1:1 严格分离 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 hex / 1 通道值 = 6 verb HUD 6 行 6 通道 漂动 + §9.6.4 调色六元组 entry 漂动 + §9.6.22 6 verb VFX 视觉组 漂动 + §9.6.5 tooltip 视觉组连贯 8 行拼接 漂动), **Stage 3 0 改 6 verb 6 通道 per verb 0 反序 0 漏 0 改 1 通道** (T200 + T202 + T204 + T233 + F013.E + T247 落地后 6 verb 6 通道 1:1 严格, 4 主通道 (icon / name label / fill / cooldown label) + 2 派生通道 (reduce_flash modulate / glow border) 6 通道 per verb 0 漏 0 互混 0 复用 0 共享, 6 verb × 6 通道 = 36 通道 0 漂动), **Stage 4 0 改 6 verb `_verb_glow_state` dict 6 key 0 漏 1 verb + 6 verb glow stylebox 6 instance 0 漏 1 verb** (T233 + T247 落地后 6 verb dict 6 key 1:1 严格, pulse / bind / cut / echo / wave / whisper 6 key 0 漏 0 改 0 重命名 0 反序, 6 verb glow stylebox 6 instance (`_pulse_glow_bg` / `_bind_glow_bg` / `_cut_glow_bg` / `_echo_glow_bg` / `_wave_glow_bg` / `_whisper_glow_bg`) 0 漏 0 改 0 重命名, 改 1 dict key / 改 1 stylebox var 名 = `_update_verb_glow_state` 0 触发 冷光勾边, 0 触碰既有 5 verb dict key 0 漏), **Stage 5 0 改 reduce_flash iteration list 8 element 0 漏 1 element 0 反序 0 改 1 顺序** (T200 + T206 + T247 落地后 8 element 1:1 严格, 5 verb bar + 1 whisper + 1 resonance + 1 health = 8 element 0 漏 0 改 0 漏 1 顺序, 改 1 顺序 = accessibility 灰化范围错乱 / 漏 1 element = HUD 顶部某元素 0 灰化). 任何"加新 1 verb HUD 接入"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格), **再**在 `hud.tscn` 加 1 row + 4 子节点 (Stage 1 1:1 严格, 0 漏 1 row 0 改 1 节点类型), **再**在 `hud.gd` 加 6 通道 1 verb 4 `@onready var` + 1 ability ref + 1 主题色 const (Stage 2 + 3 1:1 严格, 0 漏 0 改 1 hex 0 改 1 通道值), **再**加 1 glow stylebox + 1 dict key + 1 `_create_verb_glow_stylebox` 调点 + 1 `add_theme_stylebox_override` 调点 + 1 `_update_verb_glow_state` 调点 (Stage 4 1:1 严格), **再**加 1 iteration list element (Stage 5 1:1 严格), **再**按 §9.6.22 5 段序列 1:1 复制 (VFX 视觉组 调色 + 几何 + layer 1:1 严格), **再**按 §9.6.5 `_VERB_ACHV_INFO` 6 字段 1:1 复制 (tooltip 视觉组连贯 0 漏 1 verb), 0 触碰 §9.6.3 既有 5+1 verb 7 UI 通道 任何 1 字段 0 改既有 6 verb row 0 改既有 6 verb 主题色 0 改既有 36 通道 0 改既有 8 element iteration list 0 改既有 6 verb dict 6 key.
  2. **5 段序列 0 触碰边界** — `src/scripts/hud.gd` 任何 const / var / `@onready var` / `@export` / `@onready` 0 改 0 删 0 重排 (Stage 1-5 既有 1 字段 1 调点 0 改), 任何"修 1 verb 主题色"polish 走 Stage 2 1:1 严格 (改 `_PULSE_GLOW_COLOR` 时同步改 §9.6.4 调色六元组 Pulse entry 0 改 1 hex 0 改 1 通道值); 任何"加新 1 verb HUD 6 通道"polish 走 Stage 3 1:1 严格 (在 @onready var / var / const 1:1 复制 0 漏 1 通道 0 反序 0 改 1 字段 0 改 1 节点类型); 任何"修 1 verb glow 主题色"polish 走 Stage 4 1:1 严格 (改 `_PULSE_GLOW_COLOR` + `_pulse_glow_bg` 同步, 0 漏 1 stylebox 0 改 1 dict key 0 漏 1 verb); 任何"修 reduce_flash 范围"polish 走 Stage 5 1:1 严格 (在 `_apply_reduced_flash_modulate` iteration list 末尾/中间加 1 element 0 漏 0 改既有 8 element 0 改 1 顺序).
  3. **6 verb 调色 0 改 1 hex 0 改 1 通道值** (T200 + T233 + F013.E + T241 + T247 落地后 0 触碰边界) — Pulse Coral Pulse #E86D5A / Bind Muted Violet #65506A / Cut Amber Voice #F2B66E / Echo Glass Cyan #69C7CE / Wave Pale Resonance #B7E6DC / Whisper Muted Mauve #C8A4D8 6 verb 调色 1:1 严格分离 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 hex / 1 通道值 = 6 verb HUD 6 行 6 通道 漂动 (Stage 2 漂动) + §9.6.4 调色六元组 entry 漂动 + §9.6.22 ICON_COLORS 漂动 + §9.6.3 5+1 verb 7 UI 通道 漂动 + §9.6.5 tooltip 视觉组连贯 8 行拼接 漂动, source-grep 验证 6 verb 主题色 const + §9.6.4 调色六元组 6 entry + §9.6.22 6 verb VFX `@export` color 字段 0 漏 0 改 1 字段 0 改 1 通道值.
  4. **36 通道 (6 verb × 6 通道) 0 反序 0 漏 0 改 1 通道** (T200 + T202 + T204 + T233 + F013.E + T247 落地后 0 触碰边界) — 6 verb × 6 通道 = 36 通道 1:1 严格分离, 4 主通道 (icon sprite modulate / name label font_color / ProgressBar fill bg_color / cooldown label text color) + 2 派生通道 (reduce_flash modulate overlay / cold glow border 1px bg_color), 0 反序 0 漏 0 改 1 通道 0 改 1 节点 0 改 1 字段 0 改 1 节点类型, 任何"加新 1 通道"polish 必须在 6 verb × 6 通道 = 36 通道范围内 1:1 同步, source-grep 验证 24 @onready var (6 verb × 4 UI 元素) + 6 verb ability ref + 6 verb 主题色 const + 6 verb glow stylebox 0 漏 0 改 1 字段 0 改 1 节点类型.
  5. **Stage 4 6 verb `_verb_glow_state` dict 6 key 0 改 1 字符 0 重命名 0 反序 0 漏 1 key** (T233 + T247 落地后 0 触碰边界) — `_verb_glow_state: Dictionary` 6 key (pulse / bind / cut / echo / wave / whisper) 0 改 0 删 0 重命名 0 反序 0 漏 1 key, 任何"加新 1 verb glow"polish 必须**先**在 dict 字面量末尾加 1 key (e.g. `"<verb>": false`), **再**在 `_ready()` 加 1 行 `_create_verb_glow_stylebox(_<VERB>_GLOW_COLOR)` 调点 + 1 行 `_<verb>_cooldown.add_theme_stylebox_override("background", _<verb>_glow_bg)` 调点 + 1 行 `_update_verb_glow_state("<verb>", _<verb>_glow_bg, _<VERB>_GLOW_COLOR, _<verb>_ability)` 调点, 改 1 dict key / 改 1 stylebox var 名 / 改 1 调点顺序 = 6 verb 冷光勾边 0 触发 0 视觉组连贯崩塌, source-grep 验证 6 dict key + 6 stylebox var + 6 `_update_verb_glow_state` 调点 0 漏 0 改 1 字段.
  6. **Stage 5 reduce_flash iteration list 8 element 0 改 1 element 0 漏 1 element 0 反序** (T200 + T206 + T247 落地后 0 触碰边界) — `_apply_reduced_flash_modulate` 8 element list (5 verb bar + 1 whisper + 1 resonance + 1 health) 0 改 0 漏 0 反序 0 改 1 element 1 顺序, 任何"加新 1 reduce_flash element"polish 必须在 list 末尾/中间加 1 element (e.g. `_shop_panel` / `_pause_menu` / `_save_load_menu` / `_settings_menu`), 改 1 element = accessibility 灰化范围错乱 / 漏 1 element = HUD 顶部某元素 0 灰化 (e.g. T206 #123 落地时漏 `_health_container` 触发 HUD 顶部生命条仍 100% 饱和度), source-grep 验证 8 element 0 漏 0 改 1 element 1 顺序.
  7. 已知 drift risk: 6 verb HUD 6 行 6 色色域分工 6 通道 (5 段 canonical 1:1 序列) **可能**漏 1 段 1:1 镜像 (e.g. T247 #164 落地时漏 Stage 4 `_verb_glow_state` dict 加 `"whisper": false` 1 key, 0 冷光勾边; 漏 Stage 5 iteration list 加 `_whisper_cooldown` 1 element, accessibility 灰化 0 覆盖 Whisper bar). 监控建议: 任何"未来给 6 verb HUD 加新 verb 或加新 UI 元素"必须**先**在 5 段序列 (Stage 1 tscn + Stage 2 调色 const + Stage 3 6 通道 @onready var / var / ability ref + Stage 4 dict + stylebox + 调点 + Stage 5 iteration list) 1:1 镜像 0 漏 1 段. T247 (#164) 5 段序列 1:1 镜像落地后任何新 verb / 新 UI 元素 加 5 段同步是 §9.6.24 5 段序列的"母集" (1 verb 5 段 1:1 严格, §9.6.24 5 段 1 verb 完整闭环, 6 verb × 5 段 = 30 件 1:1 严格, 含 6 通道 × 6 verb = 36 通道 1:1 严格, 8 element iteration list 0 漏 1 element, 6 verb dict 6 key 0 漏 1 key). 另 1 类已知 drift: Stage 1 tscn row 4 子节点 path 0 改 1 字段 触发 @onready var path 多层 path "Node not found" 错 (T247 #164 落地时 `WhisperRow` 4 子节点 0 漏 0 改 path 段, 后续 polish 期若加新 verb 必须**先**在 tscn 加 row + 4 子节点, **再**在 `hud.gd` 加 4 `@onready var` path 与 tscn 1:1 严格对齐).

**0 副作用**: T200 / T202 / T204 / T206 / T233 / T247 任何 const / var 0 触碰 (6 verb 主题色 const 0 改 0 删 0 重排 + 6 verb `@onready var` 0 改 0 删 0 重排 + 6 verb `var _<verb>_ability` 0 改 0 删 0 重排 + `_REDUCED_COLOR_MODULATE` / `_NORMAL_COLOR_MODULATE` 0 改 + `_verb_glow_state` dict 6 key 0 改 0 删 0 重排 + 6 verb glow stylebox var 0 改 0 删 0 重排 + reduce_flash iteration list 8 element 0 改 0 删 0 重排); 0 触碰 §9.6.1 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫 (T280 0 改 6 verb `_current_<verb>_vfx` 任何字段); 0 触碰 §9.6.2 5 layer 视觉组既有 5 verb 任何 1 字段 1 alpha 公式 1 layer 顺序; 0 触碰 §9.6.3 5+1 verb 7 UI 通道 既有 6 verb 任何 1 字段 1 调点 (T280 0 改 6 verb 4 @onready var + 1 ability ref + 1 主题色 const + 1 风格 stylebox + 1 dict key + 1 iteration list element 0 触碰既有 5 步 1:1 复制 模板); 0 触碰 §9.6.4 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (T280 0 改 6 verb 调色 1 hex 0 改 1 通道值 + 0 改 HUD 6 行 6 通道 + 0 改 tooltip 6 字段); 0 触碰 §9.6.5 6 verb 视觉组连贯 tooltip 8 行拼接 (T280 0 改 `_VERB_ACHV_INFO` 6 字段 任何 1 字段); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T280 0 改 6 verb `_on_<verb>_hit` 函数体任何 const / var / 屏染 / 屏抖 / VFX flash / 音频); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T280 0 改 6 verb 主信号 + 命中信号 + 结束信号 + 扩展信号 + PlayerActionGate register / unregister); 0 触碰 §9.6.18 16 件套 verb ability base (T280 0 改 6 verb ability 子类任何 const / var / `@export` / `@onready` / 5 共享方法 / `_exit_tree` 兜底 / `is_globally_blocking` 扩展); 0 触碰 §9.6.19 7 件套 windup VFX base (T280 0 改 6 verb windup VFX 子类任何 const / var / `_ready()` / `_process(delta)` / `_activate_windup_tween()` / `fade_out_and_free()`); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 transition flow (T280 0 改 GFC + room_transition + room_door + GameState 5 段序列); 0 触碰 §9.6.21 8 段成就序列 (T280 0 改 PlayerStats 6 verb 字段 + record_ability_used 6 branch + get_stat 6 entry + set_stat 6 entry + reset_run 6 行 + all_abilities_used 6 项 + achievements.json 4 tier ID + ICON_COLORS 6 entry + PNG 双路径); 0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T280 0 改 6 verb VFX 文件 任何 const / var / `@export` color 字段 / 5+ layer 视觉组 / 几何 / 跨段镜像); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T280 0 改 audio_manager_enhanced.gd 7 桶 `prewarm_*` 函数体 0 改 0 删 0 重排 + prewarm_all_sfx() aggregator 7 行 0 改 0 删 0 重排 + 3 跨调用方 0 改 0 删 0 重排); 0 触碰 check_smoke_consistency.sh (T280 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, hud.gd 6 verb @onready var / var / const / dict key / stylebox / iteration list 任何 1 字段 0 改 0 删 0 重排); 0 行为变化, 1 文档 1 段 (§9.6.24 ~50 行) + 1 smoke test (T280 19+ 断言).

**与 §9.6.3 / §9.6.4 / §9.6.5 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 关系** — §9.6.3 覆盖 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb 模式 (加新 1 verb 时 6 处 1:1 复制: tscn + 4 @onready var + 1 ability ref + 1 const + 1 stylebox + dict key + iteration list), §9.6.4 覆盖 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 1 + 2 + 3, 宪法 2 = 6 verb HUD 6 行 4 主通道 + 2 派生通道 = 6 通道 per verb), §9.6.5 覆盖 6 verb 视觉组连贯 tooltip `_build_verb_achievement_tooltip` 8 行拼接扩展器, §9.6.16 覆盖 player.gd 6 verb hit handler 5 件套 1:1 严格分离 (verb ability `.trigger_player_hit` → 5 件套 `_on_verb_hit_<verb>` + 1 verb Whisper), §9.6.17 覆盖 player.gd `_ready` 6 verb signal 桥接 5+1 件套 1:1 严格分离, §9.6.18 覆盖 `_verb_ability_base.gd` 6 verb ability 子类共享契约 16 件 1:1 严格分离, §9.6.19 覆盖 `_verb_windup_vfx_base.gd` 6 verb windup VFX 子类共享契约 7 件 1:1 严格分离, §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow 5 段 canonical 1:1 序列, §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 canonical 1:1 序列, §9.6.22 覆盖 6 verb VFX 视觉组 5 段 canonical 1:1 序列 (Stage 1 class_name + extends Node2D 模板 + Stage 2 6 verb 调色六元组 1:1 + Stage 3 5+ layer 视觉组 1:1 + Stage 4 6 verb 几何 1:1 + Stage 5 6 verb 跨段 1:1 镜像), §9.6.23 覆盖 `audio_manager_enhanced.gd` prewarm aggregator 7 桶 5+1+1 canonical 1:1 序列 + 3 跨调用方 1:1 镜像. §9.6.24 覆盖 `src/scripts/hud.gd` 6 verb HUD 6 行 6 色色域分工 6 通道 5 段 canonical 1:1 序列 (Stage 1 6 verb row 4 子节点 1:1 + Stage 2 6 verb 调色六元组 1:1 + Stage 3 6 verb × 6 通道 = 36 通道 1:1 + Stage 4 6 verb dict 6 key + stylebox 6 instance 1:1 + Stage 5 reduce_flash iteration list 8 element 1:1 + 3 闭环宪法同步). §9.6.3 关注"HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb (加新 1 verb 时 6 处 1:1 复制 structural sync)", §9.6.4 关注"6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法)", §9.6.5 关注"6 verb 视觉组连贯 tooltip 8 行拼接扩展器", §9.6.16-§9.6.19 关注"verb ability + signal + hit handler + windup VFX 共享契约", §9.6.20 关注"跨 scene transition flow 时序契约", §9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格)", §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约 (调色 + 几何 + layer + 跨段镜像)", §9.6.23 关注"prewarm aggregator 5+1+1 序列 1:1 严格分离契约 + 3 跨调用方 1:1 镜像", §9.6.24 关注"HUD 6 verb × 6 通道 = 36 通道 1:1 严格分离契约 (5 段 1:1 严格分离)". 12 套 polish 模式 0 反序 0 漏 0 改 1 字符. 任何"加新 1 verb HUD 接入"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格), **再**按 §9.6.3 5+1 verb 7 UI 通道 6 处 1:1 复制 (Stage 1 + 3 1:1 严格), **再**按 §9.6.24 5 段序列 1:1 复制 (Stage 4 + 5 1:1 严格), **再**按 §9.6.22 5 段序列 1:1 镜像 (VFX 视觉组 调色 + 几何 + layer 1:1 严格), **再**按 §9.6.5 `_VERB_ACHV_INFO` 6 字段 1:1 复制 (tooltip 视觉组连贯 0 漏 1 verb). §9.6.3 与 §9.6.24 0 互混 0 复用 0 共享: §9.6.3 关注"HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb (structural sync, 加新 1 verb 时 6 处 1:1 复制: tscn 4 子节点 + 4 @onready var + 1 ability ref + 1 const + 1 stylebox + dict key + iteration list element)" (与 5 段序列 1:1 镜像的 36 通道 canonical 1:1 序列 0 相关), §9.6.24 关注"HUD 6 verb × 6 通道 = 36 通道 1:1 严格分离契约 (5 段 canonical 1:1 序列: Stage 1 row 4 子节点 + Stage 2 调色 + Stage 3 36 通道 + Stage 4 dict + stylebox + Stage 5 iteration list)" (与 structural sync 6 处 1:1 复制 0 相关). §9.6.4 与 §9.6.24 0 互混 0 复用 0 共享: §9.6.4 关注"6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 1+2+3, 1 句宪法 0 触及 5 段序列 0 触及 36 通道 canonical 1:1 序列)" (与 5 段序列 1:1 镜像的 36 通道 0 相关), §9.6.24 关注"HUD 6 verb × 6 通道 = 36 通道 1:1 严格分离契约 (5 段 canonical 1:1 序列)" (与 abstract 宪法 0 相关). §9.6.22 与 §9.6.24 0 互混 0 复用 0 共享: §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约 (Stage 1 class_name + Stage 2 调色六元组 + Stage 3 5+ layer 视觉组 + Stage 4 6 verb 几何 + Stage 5 6 verb 跨段 1:1 镜像, 5 段全在 `<verb>_vfx.gd` 文件)" (与 HUD `hud.gd` 36 通道 0 相关), §9.6.24 关注"HUD 6 verb × 6 通道 = 36 通道 1:1 严格分离契约 (5 段 canonical 1:1 序列, 5 段全在 `hud.gd` 文件 + 1 段 tscn 6 verb row 4 子节点)" (与 VFX 视觉组 0 相关). §9.6.24 是 12 套 polish 模式**唯一**关注"HUD 6 verb × 6 通道 = 36 通道 1:1 严格分离 (5 段 canonical 1:1 序列: Stage 1 row + Stage 2 调色 + Stage 3 36 通道 + Stage 4 dict + stylebox + Stage 5 iteration list)"的 1 套, 与 §9.6.3 / §9.6.4 / §9.6.5 / §9.6.16-§9.6.23 11 套 structural sync / abstract 宪法 / tooltip / verb ability / signal / hit handler / windup VFX / transition / 成就 / VFX 视觉组 / prewarm 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.24 5 段序列走 §9.1 9 步落地的 "verb" (1) + "ability" (2) + "const" (3) + "icon" (6) + "HUD 冷光勾边" (9) 5 步 (`src/scripts/hud.gd` 6 verb `@onready var` 4 主通道 + 6 verb `var _<verb>_ability` + 6 verb 主题色 const `_PULSE_GLOW_COLOR` / ... / `_WHISPER_GLOW_COLOR` + `src/scenes/hud.tscn` 6 verb row 4 子节点 + 6 verb glow stylebox 6 instance + 6 verb `_verb_glow_state` dict 6 key + `_apply_reduced_flash_modulate` iteration list 8 element), 0 触碰 §9.1 9 步任何 "input" (4) + "shop" (5) + "vfx" (7) + "audio" (8) 4 步 (HUD 6 verb × 6 通道 0 走 input map + 0 走 shop 系统 + 0 走 VFX 视觉组 + 0 走 audio 副作用). 任何"加新 1 verb HUD 接入"必须**先**按 §9.1 9 步落地 (含 "verb" (1) + "ability" (2) + "const" (3) + "icon" (6) + "HUD 冷光勾边" (9) 5 步), **再**按 §9.6.24 5 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 任何"加新 1 verb 调色"必须**先**在 `STYLE_GUIDE.md` §9.6.4 调色六元组加 1 entry (Stage 2 1:1 严格, 0 漏 1 entry 0 改 1 hex), **再**在 `hud.gd` 加 `_<VERB>_GLOW_COLOR := Color(<hex>)` 1:1 镜像 (Stage 2 1:1 严格, 0 漏 1 字段 0 改 1 通道值), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "const" (3) 步 (HUD 6 verb × 6 通道 0 触碰 verb enum + ability 子类 + const 字段). 任何"加新 1 verb 6 通道"必须**先**在 `hud.tscn` 加 1 row + 4 子节点 (Stage 1 1:1 严格), **再**在 `hud.gd` 加 4 `@onready var` + 1 ability ref + 1 const + 1 stylebox (Stage 3 + 4 1:1 严格, 0 复用 L1-L5 既有 5 verb `@onready var` 0 改 1 字段 0 改 1 节点类型), 0 触碰 §9.1 9 步任何 "icon" (6) 步既有 6 verb icon PNG 双路径. 任何"加新 1 reduce_flash element"必须**先**在 `_apply_reduced_flash_modulate` iteration list 末尾/中间加 1 element (Stage 5 1:1 严格, 0 漏 1 element 0 改既有 8 element 顺序), **再**确认该 element 走 `_apply_reduced_flash_modulate` 1:1 镜像 (Stage 5 1:1 严格, 0 漏 1 element 0 改 1 顺序), 0 触碰 §9.1 9 步任何 "HUD 冷光勾边" (9) 步既有 6 verb 6 bar 任何 1 字段. 任何"加新 1 跨段镜像"必须**先**在 §9.6.3 / §9.6.4 / §9.6.5 / §9.6.22 4 跨段加 1 镜像 (5 段序列 1:1 严格, 0 漏 1 跨段 0 改 1 字段), 0 触碰 §9.1 9 步任何 const / var / 节点 (5 段序列仅在 `hud.gd` 6 verb × 6 通道 = 36 通道 + 6 verb dict 6 key + 6 verb glow stylebox 6 instance + 8 element iteration list, 6 verb 子类 0 改 base 0 改, 5 verb lazy-init 守卫 0 删 0 改, 含 1 verb Whisper T247 #164 接入完成).

---

### 9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 (5 段 canonical 1:1 序列) polish 模式 (T109 + T250 + T254 + T272 + T275 跨 5 任务 ~98 轮落地) 文档化

> §9.6.1 记录 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫；§9.6.2 记录单个 VFX 5 layer 视觉 (L1-L5) polish；§9.6.3 记录 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb；§9.6.4 记录 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法；§9.6.5 记录 6 verb 视觉组连贯 tooltip `_build_verb_achievement_tooltip` 8 行拼接扩展器 (T250 #168 落地, 含 `_VERB_ACHV_ICON_HINTS` + `_VERB_ACHV_INFO` + `_build_verb_achievement_tooltip` 3 件套 + base_tooltip 3 行 100% 兼容 T109 #60)；§9.6.16 记录 player.gd 6 verb hit handler 5 件套 1:1；§9.6.22 记录 6 verb VFX 视觉组 5 段；§9.6.24 记录 HUD 6 verb × 6 通道 = 36 通道. 本节记录 6 verb 视觉组连贯 tooltip 8 行拼接 (5 段 canonical 1:1 序列) polish 模式 — 跨 5 段 (Stage 1 `_VERB_ACHV_ICON_HINTS` 3 entry list + Stage 2 `_VERB_ACHV_INFO` 3 entry × 6 字段 + Stage 3 base_tooltip 3 行 100% 兼容 T109 #60 + Stage 4 `_build_verb_achievement_tooltip` 5 element extra_lines + Stage 5 3 闭环宪法同步) — 跨 5 任务 (T109 #60 + T250 #168 + T254 #173 + T272 #191 + T275 #194) ~98 轮迭代落地, 8 行拼接 1:1 严格分离 (3 base + 5 extra = 8 行, 0 漏 0 重叠 0 互混 0 复用 0 共享), 0 漂动 0 后续 主要 pre-existing 风险. 任何"加新 1 verb tooltip 扩展"或"修 1 关联成就 tooltip 视觉组"或"加新 1 tooltip 维度"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 5 1:1 严格), **再**按 Stage 1 `_VERB_ACHV_ICON_HINTS` 1:1 加 1 entry (Stage 1 1:1 严格), **再**按 Stage 2 `_VERB_ACHV_INFO` 1:1 加 1 entry 6 字段 (Stage 2 1:1 严格), **再**按 Stage 4 `_build_verb_achievement_tooltip` 0 改既有 5 element extra_lines 0 改 1 行 (Stage 4 1:1 严格), **再**按 Stage 5 3 闭环宪法同步 (调色六元组 + HUD 6 行 6 通道 + tooltip 6 字段 0 漏 1 闭环). 跨 3 verb icon hint (echo_icon / wave_icon / whisper_icon) + 6 字段 (achv_id / verb_index / color / color_name / geometry_zh / visual_group) + 5 element extra_lines (空行 / 段名 header / 序号+主色 hex+主色名 / 几何 / 视觉组) + 3 闭环宪法 (§9.6.4 调色六元组 + §9.6.3 HUD 6 行 6 通道 + §9.6.24 HUD 36 通道) 0 漏 1 verb 全部走同一套 5 段 1:1 序列 0 例外.

- **症状**: `src/scripts/pause_menu.gd` 6 verb 视觉组连贯 tooltip 8 行拼接 (`_build_verb_achievement_tooltip`) 跨 3 verb icon hint 反复 polish 期间出现 6 类典型 fragile: (1) "加新 1 verb 漏 Stage 1 列表同步" — 新 verb 接入成就 tooltip 视觉组时 (e.g. T275 #194 第 7 verb 假设) 漏 `_VERB_ACHV_ICON_HINTS` 列表末尾加 1 entry (3 entry → 4 entry, T250 落地时 0 触碰既有 3 entry 顺序), 触发 `_build_verb_achievement_tooltip` 内部 `_VERB_ACHV_ICON_HINTS.has(icon_hint)` guard 返回 false → 直接 `return base_tooltip` → 新 verb slot tooltip 100% 退化为 T109 #60 既有 3 行 0 视觉组连贯段 0 调色 hex 0 几何 0 视觉组, 玩家 hover 看到 "title + desc + 解锁时间" 3 行 0 看到 "• 第 7 verb · #HEX ColorName"; (2) "加新 1 verb 漏 Stage 2 dict 同步" — 新 verb 接入成就 tooltip 视觉组时漏 `_VERB_ACHV_INFO` dict 加 1 entry 6 字段 (achv_id / verb_index / color / color_name / geometry_zh / visual_group, T250 落地后字段顺序 1:1 严格), 触发 `_build_verb_achievement_tooltip` 内部 `_VERB_ACHV_INFO.has(icon_hint)` guard 返回 false → 直接 `return base_tooltip` → 玩家 hover 看到 0 视觉组连贯 5 行段, 0 知道 "这是第几 verb / 主色是什么 / 几何是什么 / 视觉组短句", 即使 Stage 1 列表已加该 entry; (3) "修 1 关联成就 tooltip 漏 Stage 2 字段顺序" — 修 `_VERB_ACHV_INFO` 既有 entry (e.g. `echo_icon`) 时改字段顺序 (e.g. 把 `color_name` 放到 `verb_index` 之前, 或把 `visual_group` 放到 `geometry_zh` 之前), 触发 `_build_verb_achievement_tooltip` 内 `String(info["color"])` + `String(info["color_name"])` 紧邻渲染 ("• 第 N verb · #HEX  ColorName") 0 断行, 玩家看到 "• 第 4 verb · Glass Cyan" 而非 "• 第 4 verb · #69C7CE  Glass Cyan" 0 主色 hex 0 调色宪法同步 (§9.6.4 调色六元组 entry 0 调色 hex 0 通道值), 视觉组连贯崩塌; (4) "加新 1 tooltip 维度漏 Stage 4 5 element 同步" — 修 `_build_verb_achievement_tooltip` 函数体时改既有 5 element `Array[String] extra_lines` (空行 + 段名 header + 序号+主色 hex+主色名 + 几何 + 视觉组) 任意 1 element (e.g. 删空行 0 段分隔 / 改段名 header 文字 / 改序号 format 字符串 / 删几何 / 删视觉组), 触发 base_tooltip 3 行 + extra 5 行 = 8 行 tooltip 硬约束破坏, 玩家 hover 看到 N 行 (N ≠ 8) 视觉组连贯段不完整 (e.g. 0 看到 "• 几何 — 盾球扩散" 几何 0 知道 verb 几何 motif) 或 base_tooltip 3 行被错乱截断 (e.g. 0 看到 "解锁于 ..."), 6 verb 视觉组连贯 tooltip 拼接崩塌; (5) "加新 1 verb 漏 Stage 5 闭环宪法同步" — 新 verb 接入成就 tooltip 视觉组时漏 §9.6.4 调色六元组加 1 entry (e.g. 第 7 verb 调色 hex 0 改 §9.6.4 调色六元组 0 撞既有 6 verb) 或漏 §9.6.3 HUD 6 行 6 通道 (e.g. 加 verb 0 加 HUD 6 行 6 通道 1 行 0 撞 5 verb) 或漏 §9.6.24 HUD 36 通道 (e.g. 加 verb 0 加 6 通道 = 36+6=42 通道 0 撞 36 通道), 触发 3 闭环宪法不同步 (调色 hex 0 镜像 §9.6.4 + HUD 6 行 0 镜像 §9.6.3 + 36 通道 0 镜像 §9.6.24), 玩家 hover tooltip 看到 "• 第 7 verb · #NEWHEX NewColorName" 但 HUD 6 行 0 该 verb 行, 视觉组连贯崩塌; (6) "加新 1 verb slot 漏 base_tooltip 调用拆分" — 新 verb 关联成就 slot tooltip 落地时 (e.g. T250 #168 时改 `slot.tooltip_text = "..."` 内联长字符串为 `var base_tooltip := "..."; slot.tooltip_text = _build_verb_achievement_tooltip(base_tooltip, hint)` 单一职责拆分) 漏 base_tooltip 3 行 0 触碰 11 个非 6 verb 关联 slot tooltip 100% 兼容 (T109 #60 既有 `"%s  %s\n解锁于 %s"` × title_zh + desc_zh + ts_str 3 行结构 0 改 0 回归), 11 slot 0 触碰 3 slot 升级约束破坏, 玩家 hover 非 6 verb 关联 slot 看到 0 兼容 T109 #60 既有 3 行 (e.g. "0 title  0 desc\n解锁于 0 timestamp") 0 字 0 解锁时间, 11 slot tooltip 0 兼容. 6 类症状根因: 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列 0 文档化, polish 期只能"凭记忆"同步 N 处 (1 Stage 1 list + 1 Stage 2 dict + 1 Stage 3 base_tooltip 3 行 0 触碰 + 1 Stage 4 5 element extra_lines + 1 Stage 5 3 闭环宪法), 1 漏 1 = 8 行 tooltip 漂动 (3 base 错乱 / 5 extra 漏 1 行 / 11 slot 0 兼容 / 3 闭环宪法 0 同步).
- **触发场景**: `src/scripts/pause_menu.gd` 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列落地需同步 5 件套: (1) **Stage 1 `_VERB_ACHV_ICON_HINTS` 3 entry list 1:1 严格分离** — `const _VERB_ACHV_ICON_HINTS := ["echo_icon", "wave_icon", "whisper_icon"]` 3 entry 1:1 严格 (T250 #168 落地, 3 entry 顺序 1:1 严格 0 改 0 重命名 0 漏 1 entry, 改 1 entry 名 = `_build_verb_achievement_tooltip` 内部 `_VERB_ACHV_ICON_HINTS.has(icon_hint)` guard 0 触发 → `return base_tooltip` → 0 视觉组连贯 5 行段 0 调色 hex 0 几何 0 视觉组), 0 触碰 11 个非 6 verb 关联 slot (返回 `base_tooltip` 原样 100% 兼容 T109 #60 既有 3 行 0 改 0 回归); (2) **Stage 2 `_VERB_ACHV_INFO` 3 entry × 6 字段 1:1 严格分离** — `const _VERB_ACHV_INFO := { "echo_icon": { "achv_id": "quadruple_voice", "verb_index": 4, "color": "#69C7CE", "color_name": "Glass Cyan", "geometry_zh": "盾球扩散 — 4 verb 命中色向四周辐射", "visual_group": "5 verb 全是动态几何 (Pulse 圆环 / Bind 螺旋 / Cut 锋线 / Echo 盾球 / Wave 双环) — 玩家扫到 cyan = 4 verb 命中" }, "wave_icon": { ... 6 字段 ... }, "whisper_icon": { ... 6 字段 ... } }` 3 entry × 6 字段 = 18 字段 1:1 严格 (字段顺序 1:1 严格 "achv_id → verb_index → color → color_name → geometry_zh → visual_group" 0 改 0 重排 0 漏 1 字段, 改 1 字段顺序 = `_build_verb_achievement_tooltip` 内 `String(info["color"])` + `String(info["color_name"])` 紧邻渲染 0 断行 → 玩家看到 "• 第 N verb · ColorName" 而非 "• 第 N verb · #HEX  ColorName" 0 主色 hex); (3) **Stage 3 base_tooltip 3 行 100% 兼容 T109 #60** — `var base_tooltip := "%s  %s\n解锁于 %s" % [title_zh, desc_zh, ts_str]` 3 行结构 100% 兼容 T109 #60 既有 3 行 0 改 0 回归, 11 slot 0 触碰 3 slot 升级约束, base_tooltip 0 改 (T109 既有 100% 兼容) + `_build_verb_achievement_tooltip` 仅追加 5 行 0 触碰 base_tooltip 3 行结构; (4) **Stage 4 `_build_verb_achievement_tooltip` 5 element extra_lines 1:1 严格分离** — `func _build_verb_achievement_tooltip(base_tooltip: String, icon_hint: String) -> String:` 双 `has()` guard (`_VERB_ACHV_ICON_HINTS.has(icon_hint)` + `_VERB_ACHV_INFO.has(icon_hint)` 0 漏) + `var extra_lines: Array[String] = []` 5 element (1. `""` 空行段分隔 / 2. `"6 verb 视觉组"` 段名 header / 3. `"• 第 %d verb · %s  %s"` 序号+主色 hex+主色名 / 4. `"• 几何 — %s"` 几何 / 5. `"• 视觉组 — %s"` 视觉组) 0 反序 0 漏 0 改 1 element 0 改 1 alpha 公式 0 改 1 painter's order, `return base_tooltip + "\n".join(extra_lines)` 3 + 5 = 8 行 tooltip 硬约束 (1 漏 1 element = 8 行硬约束破坏, 玩家 hover 看到 N 行 N ≠ 8 视觉组连贯段不完整); (5) **Stage 5 3 闭环宪法同步 (调色六元组 + HUD 6 行 6 通道 + tooltip 6 字段)** — 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 6 字段 1:1 严格分离 0 漏 1 闭环 (调色 hex 必须 0 改 §9.6.4 调色六元组 entry, HUD 6 行 6 通道必须 0 改 §9.6.3 5+1 verb 7 UI 通道, tooltip 6 字段必须 0 改 §9.6.24 HUD 36 通道 0 漏 1 verb 0 改 1 通道值), 1 漏 1 闭环 = 视觉组连贯崩塌 (玩家 hover tooltip 看到 "• 第 4 verb · #69C7CE Glass Cyan" 但 HUD 6 行 0 4 verb Echo 行 0 4 verb 主题色 glow 反馈). 5 件套 1:1 同步, 1 漏 1 = 8 行 tooltip 漂动 (3 base 错乱 / 5 extra 漏 1 行 / 11 slot 0 兼容 / 3 闭环宪法 0 同步 / 字段顺序 错位).
- **修复** (跨 5 任务 ~98 轮落地后已知 5 段 canonical 1:1 序列 + 8 行硬约束 0 例外):
  - **T109 (#60) base_tooltip 3 行奠基** — `src/scripts/pause_menu.gd:1237` 加 `var base_tooltip := "%s  %s\n解锁于 %s" % [title_zh, desc_zh, ts_str]` 3 行结构 (T109 落地, AchievementGrid 14 slot tooltip 0 触碰既有 slot 路径, 11 slot 0 触碰 3 slot 升级约束 100% 兼容, slot 创建路径 0 改 (slot 仍是 16x16 TextureRect + mouse_filter=STOP + mouse_entered/exited 2 signal)), 0 触碰 6 verb 视觉组连贯 (T109 落地时 6 verb 视觉组 0 接入成就系统 0 触发 tooltip 视觉组连贯 0 调色 hex 0 几何 0 视觉组).
  - **T250 (#168) `_build_verb_achievement_tooltip` 8 行拼接扩展器 + Stage 1 + Stage 2 + Stage 4 1:1 落地** — `src/scripts/pause_menu.gd:151` 加 `const _VERB_ACHV_ICON_HINTS := ["echo_icon", "wave_icon", "whisper_icon"]` 3 entry list (Stage 1 1:1 严格) + `src/scripts/pause_menu.gd:166-191` 加 `const _VERB_ACHV_INFO := { ... }` 3 entry dict × 6 字段 = 18 字段 (Stage 2 1:1 严格, 字段顺序 1:1 严格 "achv_id → verb_index → color → color_name → geometry_zh → visual_group") + `src/scripts/pause_menu.gd:1354-1370` 加 `func _build_verb_achievement_tooltip(base_tooltip: String, icon_hint: String) -> String:` 双 `has()` guard + 5 element `Array[String] extra_lines` + `"\n".join(extra_lines)` 拼接到 base_tooltip 末尾 (Stage 4 1:1 严格, 3 + 5 = 8 行 tooltip 硬约束) + `src/scripts/pause_menu.gd:1237-1238` 把 `slot.tooltip_text = "..."` 内联长字符串拆成 `var base_tooltip := "..."; slot.tooltip_text = _build_verb_achievement_tooltip(base_tooltip, hint)` 单一职责拆分 (Stage 3 base_tooltip 3 行 0 触碰 11 slot 0 兼容 3 slot 升级), 6 verb 调色 1:1 镜像 §9.6.4 调色六元组 (Echo Glass Cyan #69C7CE / Wave Pale Resonance #B7E7DD / Whisper Muted Mauve #C8A4D8 3 entry) 0 改 1 hex 0 改 1 通道值.
  - **T254 (#173) §9.6.4 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法** — `STYLE_GUIDE.md` §9.6.4 调色六元组落地 6 verb entry (Pulse Coral #E86D5A / Bind Muted Violet #65506A / Cut Amber #F2B66E / Echo Glass Cyan #69C7CE / Wave Pale Resonance #B7E7DD / Whisper Muted Mauve #C8A4D8) + `hud.gd` 6 verb 主题色 const (T233 #152 5 verb 冷光勾边 + F013.E + T247 #164 加 Whisper 第 6 verb) + `pause_menu.gd` `_VERB_ACHV_INFO` 6 字段 "color" + "color_name" 紧邻渲染 "• 第 N verb · #HEX  ColorName" 0 断行, 3 闭环宪法 1:1 同步 (Stage 5 1:1 严格, T250 #168 0 触碰既有 3 verb entry).
  - **T272 (#191) `_verb_ability_base.gd` 16 件套 共享契约 0 触碰 `_build_verb_achievement_tooltip`** — `src/scripts/_verb_ability_base.gd` 16 件套共享契约 (6 字段 + 1 `@onready` + 2 `@export` + 5 共享方法 + 1 `_exit_tree` 兜底 + 1 `is_globally_blocking` 扩展) 落地后 0 触碰 `_build_verb_achievement_tooltip` 函数体任何 const / var / 调用入口, 6 verb 子类 0 改 base 0 改, 0 触碰 §9.6.5 既有 5 段 0 触碰 1 字段.
  - **T275 (#194) 1 verb Whisper 收回完成 + §9.6.25 5 段序列 1:1 镜像落地** — 1 verb Whisper 收回完成后 §9.6.25 5 段序列 1:1 镜像落地 (`_VERB_ACHV_ICON_HINTS` 3 entry + `_VERB_ACHV_INFO` 3 entry × 6 字段 + base_tooltip 3 行 0 触碰 11 slot 100% 兼容 + `_build_verb_achievement_tooltip` 5 element extra_lines + 3 闭环宪法同步), 0 触碰既有 6 verb 任何 1 字段 0 改 1 hex 0 改 1 通道值 0 改 1 alpha 公式 0 改 1 painter's order 0 改 1 dict 顺序 0 改 1 list 顺序 0 改 1 闭环宪法 0 改 1 base_tooltip 3 行结构.
  - 跨 5 任务 ~98 轮落地后 0 漂动, 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列 100% 闭环, 5 段 1:1 严格分离 (Stage 1 `_VERB_ACHV_ICON_HINTS` 3 entry list 0 漏 0 改 0 重命名 0 反序 + Stage 2 `_VERB_ACHV_INFO` 3 entry × 6 字段 = 18 字段 0 漏 0 改 0 重排 0 反序 字段顺序 1:1 严格 + Stage 3 base_tooltip 3 行 100% 兼容 T109 #60 0 改 0 回归 11 slot 0 触碰 3 slot 升级约束 + Stage 4 `_build_verb_achievement_tooltip` 5 element extra_lines 0 漏 0 改 0 重排 0 反序 3 + 5 = 8 行硬约束 + Stage 5 3 闭环宪法 0 漏 0 改 0 错位 0 漏 1 闭环), 6 verb 调色 1:1 严格分离 (Pulse Coral / Bind Violet / Cut Amber / Echo Cyan / Wave Pale / Whisper Mauve 6 entry 0 漏 0 重叠 0 互混 0 复用 0 共享). 5 任务 0 后续 主要 pre-existing 风险.
- **预防**:
  1. 任何 polish 期给"加新 1 verb tooltip 视觉组连贯扩展"或"修 1 关联成就 tooltip 视觉组"或"加新 1 tooltip 维度"任务加新字段 / 加新方法 / 加新 verb 时**必须**严格按 5 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 改 `_VERB_ACHV_ICON_HINTS` 3 entry list** (T250 #168 落地后 3 entry 1:1 严格, `echo_icon` / `wave_icon` / `whisper_icon` 3 entry 0 改 0 重命名 0 漏 1 entry 0 反序, 改 1 entry = `_build_verb_achievement_tooltip` 内部 `_VERB_ACHV_ICON_HINTS.has(icon_hint)` guard 0 触发 → 0 视觉组连贯 5 行段), **Stage 2 0 改 `_VERB_ACHV_INFO` 3 entry × 6 字段 dict** (T250 #168 落地后 18 字段 1:1 严格, 字段顺序 "achv_id → verb_index → color → color_name → geometry_zh → visual_group" 1:1 严格 0 改 0 漏 1 字段 0 反序 0 改 1 字段名, 改 1 字段顺序 = `String(info["color"])` + `String(info["color_name"])` 紧邻渲染 0 断行 → 玩家看到 "• 第 N verb · ColorName" 而非 "• 第 N verb · #HEX  ColorName" 0 主色 hex), **Stage 3 0 改 base_tooltip 3 行 100% 兼容 T109 #60** (T109 #60 落地后 `"%s  %s\n解锁于 %s"` × title_zh + desc_zh + ts_str 3 行结构 100% 兼容 0 改 0 回归, 11 slot 0 触碰 3 slot 升级约束 0 破坏, 改 1 字段 (e.g. 改 `\n` 为 `  \n` 加段间距) 触发 11 slot 0 警告回归 0 触碰 base_tooltip 3 行结构), **Stage 4 0 改 `_build_verb_achievement_tooltip` 5 element extra_lines** (T250 #168 落地后 5 element `Array[String]` 1:1 严格, `""` / `"6 verb 视觉组"` / `"• 第 %d verb · %s  %s"` / `"• 几何 — %s"` / `"• 视觉组 — %s"` 5 element 0 改 0 漏 0 改 1 element 0 反序 0 改 1 alpha 公式 0 改 1 format 字符串, 改 1 element = 3 + 5 = 8 行 tooltip 硬约束破坏, 玩家 hover 看到 N 行 N ≠ 8 视觉组连贯段不完整), **Stage 5 0 改 3 闭环宪法同步** (§9.6.4 调色六元组 + §9.6.3 HUD 6 行 6 通道 + §9.6.24 HUD 36 通道 0 漏 0 改 0 错位 0 漏 1 闭环, 调色 hex 0 改 §9.6.4 entry + HUD 6 行 0 改 §9.6.3 5+1 verb + HUD 36 通道 0 改 §9.6.24, 1 漏 1 闭环 = 视觉组连贯崩塌). 任何"加新 1 verb tooltip 视觉组连贯扩展"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 5 1:1 严格), **再**按 §9.6.3 5+1 verb 7 UI 通道 6 处 1:1 复制 (Stage 5 1:1 严格), **再**在 `_VERB_ACHV_ICON_HINTS` 列表末尾/中间加 1 entry (Stage 1 1:1 严格, 0 改既有 3 entry 顺序 0 改 1 字段名 0 漏 1 entry), **再**在 `_VERB_ACHV_INFO` dict 加 1 entry 6 字段 (Stage 2 1:1 严格, 字段顺序 "achv_id → verb_index → color → color_name → geometry_zh → visual_group" 1:1 严格 0 改 0 漏 0 反序 0 改 1 字段名), **再**确认 `_build_verb_achievement_tooltip` 0 改既有 5 element extra_lines (Stage 4 1:1 严格, 0 反序 0 漏 0 改 1 element 0 改 1 format 字符串), **再**确认 base_tooltip 3 行 0 触碰 11 slot 0 兼容 (Stage 3 1:1 严格), 0 触碰 §9.6.5 既有 5 段 0 触碰 1 字段 0 触碰 §9.6.4 既有 6 verb 调色 0 触碰 §9.6.3 既有 5+1 verb 7 UI 通道 0 触碰 §9.6.24 既有 36 通道.
  2. **5 段序列 0 触碰边界** — `src/scripts/pause_menu.gd` 任何 const / var / func 0 改 0 删 0 重排 (Stage 1-5 既有 1 字段 1 调点 0 改), 任何"修 1 关联成就 tooltip 视觉组"polish 走 Stage 2 1:1 严格 (改 `_VERB_ACHV_INFO` 既有 entry 时**必须**保持字段顺序 "achv_id → verb_index → color → color_name → geometry_zh → visual_group" 1:1 严格 0 改 0 漏 0 反序, 0 改 1 字段名 0 改 1 hex 0 改 1 通道值 0 改 1 几何描述 0 改 1 视觉组短句); 任何"修 1 tooltip 维度"polish 走 Stage 4 1:1 严格 (在 `_build_verb_achievement_tooltip` 函数体顶部 docblock 写明 "8 行 = 3 base + 5 extra" 硬约束, 加 1 行 (e.g. 加 6 verb 关联成就 id 行 → 6 行 extra) 必须同步更新文档化 8 行结构, 0 改 base_tooltip 3 行 (T109 既有 100% 兼容)); 任何"修 1 verb 调色"polish 走 Stage 5 1:1 严格 (改 `String(info["color"])` 时同步改 §9.6.4 调色六元组 6 verb entry 0 改 1 hex 0 改 1 通道值).
  3. **6 verb 调色 0 改 1 hex 0 改 1 通道值** (T250 + T254 落地后 0 触碰边界) — Pulse Coral `#E86D5A` (0.91, 0.43, 0.35, 1.0) / Bind Violet `#65506A` (0.40, 0.31, 0.42, 1.0) / Cut Amber `#F2B66E` (0.95, 0.71, 0.43, 1.0) / Echo Cyan `#69C7CE` (0.41, 0.78, 0.81, 1.0) / Wave Pale Resonance `#B7E7DD` (0.72, 0.91, 0.87, 1.0) / Whisper Mauve `#C8A4D8` (0.78, 0.64, 0.85, 1.0) 6 verb 调色 1:1 严格分离 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 hex / 1 通道值 = 6 verb 视觉组连贯 tooltip 8 行拼接 漂动 (Stage 5 漂动) + §9.6.4 调色六元组 entry 漂动 + §9.6.24 HUD 36 通道 漂动 + §9.6.3 5+1 verb 7 UI 通道 漂动 + §9.6.22 6 verb VFX 视觉组 漂动, source-grep 验证 6 verb `_VERB_ACHV_INFO` `color` + `color_name` 字段 + §9.6.4 调色六元组 6 entry + §9.6.24 HUD 主题色 const 6 entry + §9.6.3 HUD 6 行 6 通道 + §9.6.22 6 verb VFX `@export` color 字段 0 漏 0 改 1 字段 0 改 1 通道值.
  4. **8 行 tooltip 硬约束 0 反序 0 漏 0 改 1 行** (T250 #168 落地后 0 触碰边界) — 3 base + 5 extra = 8 行 tooltip 硬约束, 0 反序 0 漏 0 改 1 行 0 改 1 base_tooltip 3 行结构 0 改 1 extra_lines 5 element, 任何"加新 1 tooltip 维度"polish 必须在 `_build_verb_achievement_tooltip` 函数体顶部 docblock 写明 "8 行 = 3 base + 5 extra" 硬约束 + 5 element 各自作用 (空行段分隔 / 段名 header / 序号+主色 hex+主色名 / 几何 / 视觉组), 加 1 行 (e.g. 加 6 verb 关联成就 id 行 → 6 行 extra) 必须同步更新文档化 8 行结构 + `_VERB_ACHV_INFO` 加 1 字段 0 改 1 字段顺序, 0 触碰 base_tooltip 3 行 (T109 既有 100% 兼容), source-grep 验证 `_build_verb_achievement_tooltip` 函数体 `extra_lines.append` 调点 = 5 + `base_tooltip` 3 行 0 漏 0 改 1 行 0 反序.
  5. **`_VERB_ACHV_INFO` 6 字段顺序 0 改 0 反序** (T250 #168 落地后 0 触碰边界) — `_VERB_ACHV_INFO` 3 entry × 6 字段 = 18 字段顺序 1:1 严格 (achv_id → verb_index → color → color_name → geometry_zh → visual_group, 0 改 0 漏 0 反序 0 改 1 字段名 0 改 1 字段类型), 任何"修 1 关联成就 tooltip 视觉组"polish 必须**先**在 entry 加 1 字段时按顺序末尾追加 (e.g. 加 `verb_unlock_ts` 字段 → 7 字段 0 改 6 字段顺序) 或插入到合适位置 (e.g. 加 `icon_path` 字段插到 `achv_id` 之后 0 改 6 字段顺序), **再**在 `_build_verb_achievement_tooltip` 5 element extra_lines 同步更新 (e.g. 加 `• 解锁时间 — %s` 行 0 改 5 element 顺序 0 改 1 format 字符串), 改 1 字段顺序 = `String(info["color"])` + `String(info["color_name"])` 紧邻渲染 0 断行 → 玩家看到 "• 第 N verb · ColorName" 而非 "• 第 N verb · #HEX  ColorName" 0 主色 hex, source-grep 验证 `_VERB_ACHV_INFO` 3 entry 各 6 字段 0 漏 0 改 1 字段名 0 反序.
  6. **3 闭环宪法 0 漏 1 闭环** (T254 #173 + §9.6.4 + §9.6.3 + §9.6.24 落地后 0 触碰边界) — 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 6 字段 1:1 严格分离 0 漏 1 闭环 (调色 hex 必须 0 改 §9.6.4 entry, HUD 6 行 6 通道必须 0 改 §9.6.3 5+1 verb 7 UI 通道, tooltip 6 字段必须 0 改 §9.6.24 HUD 36 通道 0 漏 1 verb 0 改 1 通道值), 任何"加新 1 verb tooltip 视觉组连贯扩展"polish 必须**先**在 §9.6.4 调色六元组加 1 entry (Stage 5 1:1 严格, 0 漏 1 entry 0 改 1 hex), **再**在 §9.6.3 5+1 verb 7 UI 通道 6 处 1:1 复制 (Stage 5 1:1 严格, 0 改 1 字段 0 改 1 节点类型), **再**在 §9.6.24 HUD 36 通道 0 漏 1 verb 0 改 1 通道值 (Stage 5 1:1 严格, 0 改 1 字段 0 改 1 dict key 0 改 1 stylebox 0 改 1 iteration list element), 0 触碰既有 6 verb 调色 / HUD 6 行 / 36 通道 任何 1 字段 0 改 1 hex 0 改 1 通道值 0 改 1 节点类型.
  7. 已知 drift risk: 6 verb 视觉组连贯 tooltip 8 行拼接 (5 段 canonical 1:1 序列) **可能**漏 1 段 1:1 镜像 (e.g. T250 #168 落地时漏 Stage 3 base_tooltip 3 行 0 触碰 11 slot 0 兼容约束, 11 slot 0 警告回归; 漏 Stage 4 5 element extra_lines 第 3 行 `"• 第 %d verb · %s  %s"` 0 改 format 字符串, 玩家看到 0 主色 hex; 漏 Stage 5 §9.6.24 HUD 36 通道 0 漏 1 verb 0 改 1 通道值, HUD 6 行 0 镜像 tooltip 视觉组连贯). 监控建议: 任何"未来给 6 verb 视觉组连贯 tooltip 加新 verb 或加新 tooltip 维度"必须**先**在 5 段序列 (Stage 1 list + Stage 2 dict 6 字段 + Stage 3 base_tooltip 3 行 + Stage 4 5 element extra_lines + Stage 5 3 闭环宪法) 1:1 镜像 0 漏 1 段. T250 (#168) 5 段序列 1:1 镜像落地后任何新 verb / 新 tooltip 维度 加 5 段同步是 §9.6.25 5 段序列的"母集" (1 verb 5 段 1:1 严格, §9.6.25 5 段 1 verb 完整闭环, 3 verb × 5 段 = 15 件 1:1 严格, 含 3 entry list 1:1 严格 + 3 entry × 6 字段 = 18 字段 1:1 严格 + 3 base + 5 extra = 8 行 1:1 严格 + 3 闭环宪法 1:1 严格). 另 1 类已知 drift: Stage 4 `_build_verb_achievement_tooltip` 函数体 docblock 0 写明 "8 行 = 3 base + 5 extra" 硬约束, 后续 polish 期若加新 tooltip 维度 (e.g. 加 7th verb / 加 4 verb family path / 加成就解锁时间行) 必须**先**看 docblock 确认 N extra 元素结构, 0 改 base_tooltip 3 行 0 改 5 element 顺序.

**0 副作用**: T109 / T250 / T254 / T272 / T275 任何 const / var 0 触碰 (`_VERB_ACHV_ICON_HINTS` 3 entry 0 改 0 删 0 重排 + `_VERB_ACHV_INFO` 3 entry × 6 字段 = 18 字段 0 改 0 删 0 重排 字段顺序 1:1 严格 + base_tooltip 3 行 0 改 0 删 0 重排 + `_build_verb_achievement_tooltip` 5 element extra_lines 0 改 0 删 0 重排 + 3 闭环宪法 0 改 0 漏 0 错位); 0 触碰 §9.6.1 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫 (T281 0 改 6 verb `_current_<verb>_vfx` 任何字段); 0 触碰 §9.6.2 5 layer 视觉组既有 5 verb 任何 1 字段 1 alpha 公式 1 layer 顺序; 0 触碰 §9.6.3 5+1 verb 7 UI 通道 既有 6 verb 任何 1 字段 1 调点 (T281 0 改 6 verb 4 @onready var + 1 ability ref + 1 主题色 const + 1 风格 stylebox + 1 dict key + 1 iteration list element 0 触碰既有 5 步 1:1 复制 模板); 0 触碰 §9.6.4 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (T281 0 改 6 verb 调色 1 hex 0 改 1 通道值 + 0 改 HUD 6 行 6 通道 + 0 改 tooltip 6 字段); 0 触碰 §9.6.5 既有 `_build_verb_achievement_tooltip` 函数体 (T281 0 改 5 段序列任何 1 字段 0 改 `_VERB_ACHV_ICON_HINTS` 0 改 `_VERB_ACHV_INFO` 0 改 8 行硬约束 0 改 3 闭环宪法同步); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T281 0 改 6 verb `_on_<verb>_hit` 函数体任何 const / var / 屏染 / 屏抖 / VFX flash / 音频); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T281 0 改 6 verb 主信号 + 命中信号 + 结束信号 + 扩展信号 + PlayerActionGate register / unregister); 0 触碰 §9.6.18 16 件套 verb ability base (T281 0 改 6 verb ability 子类任何 const / var / `@export` / `@onready` / 5 共享方法 / `_exit_tree` 兜底 / `is_globally_blocking` 扩展); 0 触碰 §9.6.19 7 件套 windup VFX base (T281 0 改 6 verb windup VFX 子类任何 const / var / `_ready()` / `_process(delta)` / `_activate_windup_tween()` / `fade_out_and_free()`); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 transition flow (T281 0 改 GFC + room_transition + room_door + GameState 5 段序列); 0 触碰 §9.6.21 8 段成就序列 (T281 0 改 PlayerStats 6 verb 字段 + record_ability_used 6 branch + get_stat 6 entry + set_stat 6 entry + reset_run 6 行 + all_abilities_used 6 项 + achievements.json 4 tier ID + ICON_COLORS 6 entry + PNG 双路径); 0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T281 0 改 6 verb VFX 文件 任何 const / var / `@export` color 字段 / 5+ layer 视觉组 / 几何 / 跨段镜像); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T281 0 改 audio_manager_enhanced.gd 7 桶 `prewarm_*` 函数体 0 改 0 删 0 重排 + prewarm_all_sfx() aggregator 7 行 0 改 0 删 0 重排 + 3 跨调用方 0 改 0 删 0 重排); 0 触碰 §9.6.24 HUD 6 verb × 6 通道 = 36 通道 5 段序列 (T281 0 改 hud.gd 6 verb @onready var / var / const / dict key / stylebox / iteration list 任何 1 字段 0 改 0 删 0 重排); 0 触碰 check_smoke_consistency.sh (T281 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, pause_menu.gd `_VERB_ACHV_ICON_HINTS` / `_VERB_ACHV_INFO` / `base_tooltip` / `_build_verb_achievement_tooltip` / 11 slot 0 改 0 删 0 重排); 0 行为变化, 1 文档 1 段 (§9.6.25 ~50 行) + 1 smoke test (T281 20+ 断言).

**与 §9.6.1 / §9.6.2 / §9.6.3 / §9.6.4 / §9.6.5 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 关系** — §9.6.1 覆盖 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫; §9.6.2 覆盖单个 VFX 5 layer 视觉 (L1-L5) polish; §9.6.3 覆盖 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb 模式 (加新 1 verb 时 6 处 1:1 复制: tscn + 4 @onready var + 1 ability ref + 1 const + 1 stylebox + dict key + iteration list); §9.6.4 覆盖 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 1 + 2 + 3, 宪法 2 = 6 verb HUD 6 行 4 主通道 + 2 派生通道 = 6 通道 per verb, 宪法 3 = tooltip 6 字段 0 漏); §9.6.5 覆盖 6 verb 视觉组连贯 tooltip `_build_verb_achievement_tooltip` 8 行拼接扩展器 (T250 #168 落地, 含 `_VERB_ACHV_ICON_HINTS` + `_VERB_ACHV_INFO` + `_build_verb_achievement_tooltip` 3 件套 + base_tooltip 3 行 100% 兼容 T109 #60); §9.6.16 覆盖 player.gd 6 verb hit handler 5 件套 1:1 严格分离; §9.6.17 覆盖 player.gd `_ready` 6 verb signal 桥接 5+1 件套 1:1 严格分离; §9.6.18 覆盖 `_verb_ability_base.gd` 6 verb ability 子类共享契约 16 件 1:1 严格分离; §9.6.19 覆盖 `_verb_windup_vfx_base.gd` 6 verb windup VFX 子类共享契约 7 件 1:1 严格分离; §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow 5 段 canonical 1:1 序列; §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 canonical 1:1 序列; §9.6.22 覆盖 6 verb VFX 视觉组 5 段 canonical 1:1 序列; §9.6.23 覆盖 `audio_manager_enhanced.gd` prewarm aggregator 7 桶 5+1+1 canonical 1:1 序列 + 3 跨调用方 1:1 镜像; §9.6.24 覆盖 `src/scripts/hud.gd` 6 verb HUD 6 行 6 色色域分工 6 通道 5 段 canonical 1:1 序列. §9.6.25 覆盖 `src/scripts/pause_menu.gd` 6 verb 视觉组连贯 tooltip 8 行拼接 (5 段 canonical 1:1 序列) (Stage 1 `_VERB_ACHV_ICON_HINTS` 3 entry list + Stage 2 `_VERB_ACHV_INFO` 3 entry × 6 字段 + Stage 3 base_tooltip 3 行 100% 兼容 T109 #60 + Stage 4 `_build_verb_achievement_tooltip` 5 element extra_lines + Stage 5 3 闭环宪法同步). §9.6.1 / §9.6.2 关注"verb VFX 跨类 + 5 layer 视觉", §9.6.3 关注"HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb structural sync", §9.6.4 关注"6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 1+2+3)", §9.6.5 关注"6 verb 视觉组连贯 tooltip 8 行拼接扩展器 (T250 落地 polish 模式, 5 段 1:1 严格分离待 §9.6.25 文档化)", §9.6.16-§9.6.19 关注"verb ability + signal + hit handler + windup VFX 共享契约", §9.6.20 关注"跨 scene transition flow 时序契约", §9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格)", §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约 (调色 + 几何 + layer + 跨段镜像)", §9.6.23 关注"prewarm aggregator 5+1+1 序列 1:1 严格分离契约 + 3 跨调用方 1:1 镜像", §9.6.24 关注"HUD 6 verb × 6 通道 = 36 通道 1:1 严格分离契约 (5 段 1:1 严格分离)", §9.6.25 关注"6 verb 视觉组连贯 tooltip 8 行拼接 1:1 严格分离契约 (5 段 canonical 1:1 序列: Stage 1 list + Stage 2 dict 6 字段 + Stage 3 base 3 行 + Stage 4 5 element extra_lines + Stage 5 3 闭环宪法)". 13 套 polish 模式 0 反序 0 漏 0 改 1 字符. 任何"加新 1 verb tooltip 视觉组连贯扩展"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 5 1:1 严格), **再**按 §9.6.3 5+1 verb 7 UI 通道 6 处 1:1 复制 (Stage 5 1:1 严格), **再**按 §9.6.24 HUD 36 通道 0 漏 1 verb 0 改 1 通道值 (Stage 5 1:1 严格), **再**按 §9.6.25 Stage 1 `_VERB_ACHV_ICON_HINTS` 1:1 加 1 entry (Stage 1 1:1 严格), **再**按 §9.6.25 Stage 2 `_VERB_ACHV_INFO` 1:1 加 1 entry 6 字段 (Stage 2 1:1 严格), **再**按 §9.6.25 Stage 4 `_build_verb_achievement_tooltip` 0 改既有 5 element extra_lines 0 改 1 行 (Stage 4 1:1 严格), **再**按 §9.6.25 Stage 3 base_tooltip 3 行 0 触碰 11 slot 0 兼容 (Stage 3 1:1 严格). §9.6.5 与 §9.6.25 0 互混 0 复用 0 共享: §9.6.5 关注"6 verb 视觉组连贯 tooltip 8 行拼接扩展器 (T250 落地, 5 段 1:1 严格分离待 §9.6.25 文档化)" (与 5 段 canonical 1:1 序列 0 相关), §9.6.25 关注"6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列 (Stage 1-5: list + dict 6 字段 + base 3 行 + 5 element extra_lines + 3 闭环宪法)" (与 8 行拼接扩展器 0 相关). §9.6.4 与 §9.6.25 0 互混 0 复用 0 共享: §9.6.4 关注"6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 1+2+3, 1 句宪法 0 触及 5 段序列 0 触及 5 element extra_lines)" (与 5 段序列 0 相关), §9.6.25 关注"6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列" (与 abstract 宪法 0 相关). §9.6.22 与 §9.6.25 0 互混 0 复用 0 共享: §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约 (Stage 1-5: class_name + 调色 + layer + 几何 + 跨段镜像, 5 段全在 `<verb>_vfx.gd` 文件)" (与 tooltip 视觉组连贯 0 相关), §9.6.25 关注"6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列 (5 段全在 `pause_menu.gd` 文件, 5 element extra_lines 拼接到 base_tooltip 末尾)" (与 VFX 视觉组 0 相关). §9.6.24 与 §9.6.25 0 互混 0 复用 0 共享: §9.6.24 关注"HUD 6 verb × 6 通道 = 36 通道 1:1 严格分离契约 (5 段 canonical 1:1 序列, 5 段全在 `hud.gd` 文件 + 1 段 tscn 6 verb row 4 子节点)" (与 tooltip 视觉组连贯 0 相关), §9.6.25 关注"6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列 (5 段全在 `pause_menu.gd` 文件)" (与 HUD 36 通道 0 相关, Stage 5 3 闭环宪法同步 0 改既有 36 通道). §9.6.25 是 13 套 polish 模式**唯一**关注"6 verb 视觉组连贯 tooltip 8 行拼接 1:1 严格分离 (5 段 canonical 1:1 序列: Stage 1 list + Stage 2 dict 6 字段 + Stage 3 base 3 行 + Stage 4 5 element extra_lines + Stage 5 3 闭环宪法)"的 1 套, 与 §9.6.1-§9.6.5 / §9.6.16-§9.6.24 12 套 verb VFX / HUD 6 verb 行 / 调色 + HUD 6 行 6 通道 + tooltip 拼接扩展器 / verb ability / signal / hit handler / windup VFX / transition / 成就 / VFX 视觉组 / prewarm / HUD 36 通道 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.25 5 段序列走 §9.1 9 步落地的"const" (3) + "icon" (6) 2 步 (`src/scripts/pause_menu.gd` `_VERB_ACHV_ICON_HINTS` 3 entry list + `_VERB_ACHV_INFO` 3 entry × 6 字段 = 18 字段 const + `_build_verb_achievement_tooltip` 5 element extra_lines 拼接器 + base_tooltip 3 行 0 触碰 11 slot 0 兼容 + `STYLE_GUIDE.md` §9.6.4 调色六元组 6 verb entry), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "input" (4) + "shop" (5) + "vfx" (7) + "audio" (8) + "HUD 冷光勾边" (9) 7 步 (tooltip 视觉组连贯 0 走 verb enum + ability 子类 + input map + shop 系统 + VFX 视觉组 + audio 副作用 + HUD 渲染). 任何"加新 1 verb tooltip 视觉组连贯扩展"必须**先**按 §9.1 9 步落地 (含 "const" (3) + "icon" (6) 步), **再**按 §9.6.25 5 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 任何"加新 1 verb 调色"必须**先**在 `STYLE_GUIDE.md` §9.6.4 调色六元组加 1 entry (Stage 5 1:1 严格, 0 漏 1 entry 0 改 1 hex), **再**在 `_VERB_ACHV_INFO` 加 1 entry 6 字段 (Stage 2 1:1 严格, 0 漏 1 entry 0 改 1 字段 0 改 1 字段顺序), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) 步 (tooltip 视觉组连贯 0 触碰 verb enum + ability 子类). 任何"加新 1 verb icon hint"必须**先**在 `_VERB_ACHV_ICON_HINTS` 列表末尾/中间加 1 entry (Stage 1 1:1 严格), **再**在 `_VERB_ACHV_INFO` dict 加 1 entry 6 字段 (Stage 2 1:1 严格, 字段顺序 1:1 严格), 0 触碰 §9.1 9 步任何 "icon" (6) 步既有 6 verb icon PNG 双路径. 任何"加新 1 tooltip 维度"必须**先**在 `_build_verb_achievement_tooltip` 函数体顶部 docblock 写明 "8 行 = 3 base + 5 extra" 硬约束 (Stage 4 1:1 严格), **再**在 5 element extra_lines 末尾/中间加 1 element (Stage 4 1:1 严格, 0 漏 0 反序 0 改 1 element 0 改 1 format 字符串), 0 触碰 base_tooltip 3 行 (T109 既有 100% 兼容). 任何"加新 1 跨段镜像"必须**先**在 §9.6.3 / §9.6.4 / §9.6.22 / §9.6.24 4 跨段加 1 镜像 (Stage 5 1:1 严格, 0 漏 1 跨段 0 改 1 字段), 0 触碰 §9.1 9 步任何 const / var / 节点 (5 段序列仅在 `pause_menu.gd` `_VERB_ACHV_ICON_HINTS` + `_VERB_ACHV_INFO` + `base_tooltip` + `_build_verb_achievement_tooltip` 4 件 + `STYLE_GUIDE.md` §9.6.4 调色六元组 1 件, 6 verb 子类 0 改 base 0 改, 5 verb lazy-init 守卫 0 删 0 改, 含 1 verb Whisper T275 #194 收回完成).

### 9.6.26 6 verb icon 视觉组 (5 段 canonical 1:1 序列) polish 模式 (T013 + T034 + T040 + T085 + T103 + T241 + T245 跨 6 任务 ~158 轮落地) 文档化

> §9.6.3 记录 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb 模式; §9.6.4 记录 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法; §9.6.5 记录 6 verb 视觉组连贯 tooltip `_build_verb_achievement_tooltip` 8 行拼接扩展器; §9.6.21 记录 6 verb 成就系统 闭环 8 段 canonical 1:1 序列 (含 ICON_COLORS 6 entry + PNG 双路径 1 段); §9.6.22 记录 6 verb VFX 视觉组 5 段; §9.6.24 记录 HUD 6 verb × 6 通道 = 36 通道; §9.6.25 记录 6 verb 视觉组连贯 tooltip 8 行拼接. 本节记录 `assets/ui/<verb>_icon/` + `assets/ui/achievements/<verb>_icon/` 6 verb icon 视觉组 (5 段 canonical 1:1 序列) polish 模式 — 跨 5 段 (Stage 1 6 verb family 路径 1:1 + Stage 2 6 verb disc + outer ring 1:1 + Stage 3 6 verb 调色 1:1 + Stage 4 6 verb 几何 1:1 + Stage 5 6 verb 双 export + 3 verb 双路径 1:1) — 跨 6 任务 (T013 #9 + T034 #31 + T040 #36 + T085 #49 + T103 #74 + T241 #158 + T245 #162) ~158 轮迭代落地, 6 verb icon 视觉组 1:1 严格分离 (6 verb 共享 disc + outer ring + 6 verb 调色六元组 1:1 + 6 verb 几何 1:1 + 6 verb 双 export + 3 verb achievement 双路径), 0 漂动 0 后续 主要 pre-existing 风险. 任何"加新 1 verb icon"或"修 1 verb icon 视觉"或"修 1 verb 调色"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 3 1:1 严格), **再**按 Stage 1 family 路径 1:1 加 1 目录 (Stage 1 1:1 严格), **再**按 Stage 2 disc + outer ring 1:1 复制 (Stage 2 1:1 严格), **再**按 Stage 4 6 verb 几何 1:1 加 1 motif (Stage 4 1:1 严格), **再**按 Stage 5 双 export 1:1 加 2 文件 (32x32 + 64x64, Stage 5 1:1 严格), **再**按 §9.6.21 PNG 双路径 1:1 镜像 (3 verb achievement path 1:1 严格, Stage 5 1:1 严格). 跨 6 verb (Pulse Coral / Bind Violet / Cut Amber / Echo Cyan / Wave Pale / Whisper Mauve) + 6 verb 调色 1:1 + 6 verb 几何 1:1 + 6 verb 双 export 1:1 (32x32 + 64x64) + 3 verb achievement 双路径 1:1 (echo_icon / wave_icon / whisper_icon) 0 漏 1 verb 全部走同一套 5 段 1:1 序列 0 例外.

- **症状**: `assets/ui/<verb>_icon/` + `assets/ui/achievements/<verb>_icon/` 6 verb icon 视觉组跨 6 task 反复 polish 期间出现 6 类典型 fragile: (1) "加新 1 verb 漏 Stage 1 family 路径 1:1" — 新 verb 接入 icon 视觉组时漏 `assets/ui/<verb>_icon/` family 路径 1 目录创建, 触发 `STYLE_GUIDE.md` 6 verb icon 列表 0 镜像 实际 PNG 路径 (`res://assets/ui/pulse_icon/pulse_icon.png` 等 6 verb 1:1 严格) 0 撞既有 5 verb 0 改既有 5 verb 既有 path; (2) "修 1 verb icon 漏 Stage 2 disc + outer ring 1:1 镜像" — 修 Pulse icon 时改 deep ink navy #05070D disc 颜色 (e.g. 误改 #0A0E15 偏深, 与 glass cyan #69C7CE 1px outer ring 1:1 严格 0 撞 5 verb disc 配色) 或 0 改 outer ring 1px (e.g. 改 2px), 触发 6 verb disc + outer ring 1:1 严格 0 撞既有 5 verb disc 配色 + 1px outer ring 0 撞, 玩家扫 6 verb icon 时 Pulse disc 颜色偏深与其他 5 verb disc 0 协调, outer ring 粗细不一致; (3) "加新 1 verb 漏 Stage 3 调色 1:1 同步" — 新 verb 接入 icon 时漏 `STYLE_GUIDE.md` §9.6.4 调色六元组加 1 entry (e.g. 第 7 verb 调色 hex 0 改 §9.6.4 调色六元组 0 撞既有 6 verb), 触发 调色六元组 6 entry 0 撞 新 verb 调色, 玩家看到新 verb icon motif 颜色与 §9.6.4 调色 0 镜像 视觉组连贯崩塌; (4) "加新 1 verb 漏 Stage 4 几何 1:1 同步" — 新 verb 接入 icon 时漏 6 verb 几何 motif 1:1 (Pulse 圆环 / Bind 螺旋 / Cut 锋线 / Echo 盾球 / Wave 双环 / Whisper constant 球, 5 verb 动态几何 + 1 verb 静态球), 0 在 §9.6.5 `_VERB_ACHV_INFO` `geometry_zh` 字段同步 0 在 §9.6.22 6 verb VFX 视觉组 几何 1:1 镜像, 玩家扫 icon 0 看到几何 motif 1:1 0 知道 "这是哪一 verb"; (5) "加新 1 verb 漏 Stage 5 双 export 1:1 同步" — 新 verb 接入 icon 时漏 双 export (32x32 game + 64x64 UI, 2 文件 1:1 严格), 触发 `pause_menu.gd` `_profile_abilities` TextureRect load 0 找到 64x64 路径 0 走 fallback 0 走 icon 视觉组连贯, 玩家扫 profile 看到 0 64x64 高清 icon 0 32x32 占位 0 视觉组连贯; (6) "修 3 verb achievement path 漏 Stage 5 双路径 1:1 同步" — 修 3 verb (echo / wave / whisper) achievement path 时漏 `assets/ui/achievements/<verb>_icon/<verb>_icon.png` + `<verb>_icon_32x32.png` 2 文件 1:1 严格 (T245 #162 落地 4 PNG 双路径 = verb family 2 文件 + achievement 2 文件), 0 在 §9.6.21 PNG 双路径 1:1 镜像, 玩家扫成就 grid 看到 0 64x64 high-res icon 0 32x32 缩略 视觉组连贯崩塌. 6 类症状根因: 6 verb icon 视觉组 5 段 canonical 1:1 序列 0 文档化, polish 期只能"凭记忆"同步 N 处 (1 Stage 1 family 路径 + 1 Stage 2 disc + outer ring + 1 Stage 3 调色六元组 entry + 1 Stage 4 几何 motif + 1 Stage 5 双 export + 1 Stage 5 3 verb achievement 双路径), 1 漏 1 = 6 verb icon 视觉组 漂动 (family 路径 0 镜像 / disc 配色 0 协调 / 调色 0 镜像 §9.6.4 / 几何 0 镜像 §9.6.22 / 64x64 UI 0 加载 / 3 verb achievement 0 镜像 §9.6.21).
- **触发场景**: `assets/ui/<verb>_icon/` + `assets/ui/achievements/<verb>_icon/` 6 verb icon 视觉组 5 段 canonical 1:1 序列落地需同步 5 件套: (1) **Stage 1 6 verb family 路径 1:1 严格分离** — `assets/ui/<verb>_icon/` 6 verb family 路径 1:1 严格 (pulse_icon / bind_icon / cut_icon / echo_icon / wave_icon / whisper_icon 6 目录 0 漏 0 改 0 重命名 0 反序 0 改 1 字符, T013 #9 + T034 #31 + T040 #36 + T085 #49 + T103 #74 + T245 #162 跨 6 task 落地 0 漂动), 0 触碰既有 5 verb family 路径 0 改 0 漏; (2) **Stage 2 6 verb disc + outer ring 1:1 严格分离** — 6 verb icon 共享 deep ink navy #05070D 背景 disc + glass cyan #69C7CE 1px outer ring 1:1 严格 (6 verb 1:1 严格 0 改 0 漏 0 改 1 hex 0 改 1 px, 与 §9.6.5 tooltip 视觉组连贯 0 撞 0 漏 1 verb), 0 触碰既有 5 verb disc + outer ring; (3) **Stage 3 6 verb 调色 1:1 严格分离** — 6 verb 调色六元组 (与 §9.6.4 调色六元组 1:1 严格镜像, 0 漏 0 改 1 hex 0 改 1 通道值, 0 漏 0 重叠 0 互混 0 复用 0 共享, 6 verb 调色 1:1 严格: Pulse Coral #E86D5A / Bind Muted Violet #65506A / Cut Amber Voice #F2B66E / Echo Glass Cyan #69C7CE / Wave Pale Resonance #B7E6DC / Whisper Muted Mauve #C8A4D8) 1:1 严格, 0 触碰既有 5 verb 调色 0 改 1 hex; (4) **Stage 4 6 verb 几何 1:1 严格分离** — 6 verb 几何 motif 1:1 严格 (Pulse 圆环 / Bind 螺旋 / Cut 锋线 / Echo 盾球 / Wave 双环 / Whisper constant 球, 5 verb 动态几何 + 1 verb 静态球, T245 #162 落地时 Whisper 改走 constant 球 区别于前 5 verb 动态几何, 与 §9.6.5 `_VERB_ACHV_INFO` `geometry_zh` 字段 1:1 镜像 + 与 §9.6.22 6 verb VFX 视觉组 几何 motif 1:1 镜像), 0 触碰既有 5 verb 几何; (5) **Stage 5 6 verb 双 export + 3 verb 双路径 1:1 严格分离** — `assets/ui/<verb>_icon/<verb>_icon.png` 32x32 game icon + `<verb>_icon_64x64.png` 64x64 UI icon 2 文件 1:1 严格 (6/6 0 漏 0 改 0 重命名 0 反序 0 改 1 字符) + `assets/ui/achievements/<verb>_icon/<verb>_icon.png` + `<verb>_icon_32x32.png` 2 文件 1:1 严格 (3/6 only echo_icon / wave_icon / whisper_icon, T245 #162 落地 4 PNG 双路径, 与 §9.6.21 PNG 双路径 1:1 镜像). 5 件套 1:1 同步, 1 漏 1 = 6 verb icon 视觉组 漂动.
- **修复** (跨 6 任务 ~158 轮落地后已知 5 段 canonical 1:1 序列 + 6 verb icon 视觉组 0 例外):
  - **T013 (#9) A025 Pulse icon 起步 (5 verb 时代 1 verb icon 模板)** — `assets/ui/pulse_icon/pulse_icon.png` 32x32 game icon + `pulse_icon_32x32.png` (optional) 起步 (T013 落地时 5 verb 时代 1 verb icon 模板, disc + outer ring 0 文档化 0 共享 1 verb, 5 verb 接入 icon 时 0 触碰 Pulse icon 既有字段), 0 触碰既有 5 verb icon 0 改 0 漏.
  - **T034 (#31) A033 Bind icon + Stage 1 family 路径 1:1 镜像** — `assets/ui/bind_icon/bind_icon.png` 32x32 game icon + `bind_icon_64x64.png` 64x64 UI icon 2 文件 1:1 严格落地 (Stage 1 1:1 严格, T034 落地时 Bind family 路径 1:1 复制 Pulse family 路径模板 0 改 0 漏 0 漏 1 字符), 0 触碰 Pulse icon 既有字段 0 改 0 漏.
  - **T040 (#36) A038 Cut icon + Stage 2 disc + outer ring 共享模式** — `assets/ui/cut_icon/cut_icon.png` + `cut_icon_64x64.png` 2 文件 1:1 严格 + 与 Pulse + Bind 共享 disc + outer ring 1:1 严格 (Stage 2 1:1 严格, T040 落地时 3 verb 共享 disc + outer ring 0 触碰 0 改 0 漏 1 通道), 0 触碰既有 2 verb 0 改 0 漏.
  - **T085 (#49) A061 Echo icon + Stage 3 调色六元组 1:1 镜像** — `assets/ui/echo_icon/echo_icon.png` + `echo_icon_64x64.png` 2 文件 1:1 严格 + Stage 3 调色六元组 4 verb entry (Pulse Coral + Bind Violet + Cut Amber + Echo Glass Cyan) 1:1 严格 (Stage 3 1:1 严格, 0 改既有 3 verb 调色 0 改 1 hex 0 改 1 通道值), 0 触碰既有 3 verb 0 改 0 漏.
  - **T103 (#74) A071 Wave icon + Stage 4 5 verb 几何 motif 1:1 共享** — `assets/ui/wave_icon/wave_icon.png` + `wave_icon_64x64.png` 2 文件 1:1 严格 + 5 verb 几何 motif 共享 1:1 严格 (Pulse 圆环 / Bind 螺旋 / Cut 锋线 / Echo 盾球 / Wave 双环, 5 verb 动态几何, Stage 4 1:1 严格, 0 改既有 4 verb 几何 0 改 1 motif), 0 触碰既有 4 verb 0 改 0 漏.
  - **T241 (#158) + T245 (#162) A074 Whisper icon + Stage 4 constant 球 静态几何区别 + Stage 5 4 PNG 双路径 1:1** — `assets/ui/whisper_icon/whisper_icon.png` + `whisper_icon_64x64.png` 2 verb family 文件 1:1 严格 + `assets/ui/achievements/whisper_icon/whisper_icon.png` + `whisper_icon_32x32.png` 2 achievement 文件 1:1 严格 (Stage 5 4 PNG 双路径 1:1 严格, T245 落地时 3 verb achievement path 0 触碰既有 3 verb 既有 0 改 0 漏 0 漏 1 文件) + 6 verb 几何 motif 1:1 严格 (Whisper 改走 constant 球 静态几何区别于前 5 verb 动态几何, T245 (#162) 落地时 5 verb 动态几何 0 触碰, 0 触碰 §9.6.5 `_VERB_ACHV_INFO` `geometry_zh` 字段 0 改 0 漏) + 6 verb 调色六元组第 6 entry (Whisper Muted Mauve #C8A4D8 0 撞 5 verb 调色, 与 §9.6.4 调色六元组 1:1 镜像 0 改 0 漏 1 entry 0 改 1 hex).
  - 跨 6 任务 ~158 轮落地后 0 漂动, 6 verb icon 视觉组 5 段 canonical 1:1 序列 100% 闭环, 5 段 1:1 严格分离 (Stage 1 6 verb family 路径 1:1 严格 0 漏 0 改 0 重命名 0 反序 + Stage 2 6 verb disc + outer ring 1:1 严格 0 漏 0 改 0 改 1 hex 0 改 1 px + Stage 3 6 verb 调色 1:1 严格 0 漏 0 改 0 重叠 0 互混 0 复用 0 共享 + Stage 4 6 verb 几何 motif 1:1 严格 0 漏 0 改 0 改 1 motif + Stage 5 6 verb 双 export 1:1 严格 0 漏 0 改 0 重命名 0 反序 + 3 verb achievement 双路径 1:1 严格 0 漏 0 改 0 漏 1 文件), 6 verb 调色 1:1 严格分离 (Pulse Coral / Bind Violet / Cut Amber / Echo Cyan / Wave Pale / Whisper Mauve 6 entry 0 漏 0 重叠 0 互混 0 复用 0 共享). 6 任务 0 后续 主要 pre-existing 风险.
- **预防**:
  1. 任何 polish 期给"加新 1 verb icon"或"修 1 verb icon 视觉"或"修 1 verb 调色"任务加新 verb / 加新几何 / 加新调色时**必须**严格按 5 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 改 6 verb family 路径** (T013 + T034 + T040 + T085 + T103 + T245 落地后 6 verb family 路径 1:1 严格, `pulse_icon` / `bind_icon` / `cut_icon` / `echo_icon` / `wave_icon` / `whisper_icon` 6 目录 0 改 0 重命名 0 漏 1 目录 0 反序, 改 1 目录名 = `pause_menu.gd` `_profile_abilities` TextureRect load 0 找到 path 0 走 fallback 0 视觉组连贯崩塌), **Stage 2 0 改 6 verb disc + outer ring 1:1** (T013 + T034 + T040 + T085 + T103 + T245 落地后 6 verb 共享 deep ink navy #05070D disc + glass cyan #69C7CE 1px outer ring 1:1 严格, 0 改 0 漏 0 改 1 hex 0 改 1 px, 改 1 hex / 1 px = 6 verb icon 视觉组连贯崩塌), **Stage 3 0 改 6 verb 调色 0 改 1 hex 0 改 1 通道值** (T013 + T034 + T040 + T085 + T103 + T245 落地后 6 verb 调色六元组 1:1 严格, 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 hex / 1 通道值 = 6 verb icon 视觉组连贯崩塌 + §9.6.4 调色六元组 entry 漂动 + §9.6.24 HUD 36 通道 漂动 + §9.6.3 5+1 verb 7 UI 通道 漂动 + §9.6.22 6 verb VFX 视觉组 漂动), **Stage 4 0 改 6 verb 几何 motif** (T013 + T034 + T040 + T085 + T103 + T245 落地后 6 verb 几何 1:1 严格, 0 改 0 漏 0 改 1 motif, 改 1 motif = 6 verb icon 视觉组连贯崩塌 + §9.6.5 `_VERB_ACHV_INFO` `geometry_zh` 字段 漂动 + §9.6.22 6 verb VFX 视觉组 几何 漂动), **Stage 5 0 改 6 verb 双 export + 3 verb 双路径** (T013 + T034 + T040 + T085 + T103 + T245 落地后 6 verb 双 export 1:1 严格 (6/6) + 3 verb achievement 双路径 1:1 严格 (3/6), 0 改 0 漏 0 改 1 文件名 0 反序 0 改 1 px 0 改 1 文件类型, 改 1 文件名 / 1 文件 = 6 verb icon 双 export 漂动 + 3 verb achievement 双路径 漂动). 任何"加新 1 verb icon"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 3 1:1 严格), **再**按 Stage 1 family 路径 1:1 加 1 目录 (Stage 1 1:1 严格, 0 改既有 6 verb family 路径 0 改 1 字符), **再**按 Stage 2 disc + outer ring 1:1 复制 (Stage 2 1:1 严格, 0 改 0 漏 0 改 1 hex 0 改 1 px), **再**按 Stage 4 6 verb 几何 1:1 加 1 motif (Stage 4 1:1 严格, 0 改既有 6 verb 几何 0 改 1 motif), **再**按 Stage 5 双 export 1:1 加 2 文件 (32x32 + 64x64, Stage 5 1:1 严格, 0 漏 0 改既有 6 verb 双 export 0 漏 1 文件 0 改 1 文件名), **再**按 §9.6.21 PNG 双路径 1:1 镜像 (3 verb achievement path 1:1 严格, 0 改既有 3 verb 0 漏 1 文件).
  2. **5 段序列 0 触碰边界** — `STYLE_GUIDE.md` 任何 const / var 0 改 0 删 0 重排 (5 段 0 触碰边界), 任何"修 1 verb 调色"polish 走 Stage 3 1:1 严格 (改 §9.6.4 调色六元组 既有 entry 时**必须**同步改 icon motif 颜色 + §9.6.24 HUD 主题色 const + §9.6.3 HUD 6 行 6 通道 + §9.6.22 6 verb VFX `@export` color 字段, 0 改 1 hex 0 改 1 通道值 0 漏 1 跨段); 任何"修 1 verb 几何"polish 走 Stage 4 1:1 严格 (改 6 verb 几何 motif 0 改 1 motif 0 漏 1 motif 同步改 §9.6.5 `_VERB_ACHV_INFO` `geometry_zh` 字段 + §9.6.22 6 verb VFX 视觉组 几何 1:1 镜像).
  3. **6 verb 调色 0 改 1 hex 0 改 1 通道值** (T013 + T034 + T040 + T085 + T103 + T245 落地后 0 触碰边界) — Pulse Coral #E86D5A / Bind Muted Violet #65506A / Cut Amber Voice #F2B66E / Echo Glass Cyan #69C7CE / Wave Pale Resonance #B7E6DC / Whisper Muted Mauve #C8A4D8 6 verb 调色 1:1 严格分离 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 hex / 1 通道值 = 6 verb icon 视觉组 漂动 (Stage 3 漂动) + §9.6.4 调色六元组 entry 漂动 + §9.6.24 HUD 36 通道 漂动 + §9.6.3 5+1 verb 7 UI 通道 漂动 + §9.6.22 6 verb VFX 视觉组 漂动, source-grep 验证 6 verb icon motif 颜色 + §9.6.4 调色六元组 6 entry + §9.6.24 HUD 主题色 const 6 entry + §9.6.3 HUD 6 行 6 通道 + §9.6.22 6 verb VFX `@export` color 字段 0 漏 0 改 1 字段 0 改 1 通道值.
  4. **6 verb 几何 0 改 0 漏 0 改 1 motif** (T013 + T034 + T040 + T085 + T103 + T245 落地后 0 触碰边界) — 6 verb 几何 motif 1:1 严格 (Pulse 圆环 / Bind 螺旋 / Cut 锋线 / Echo 盾球 / Wave 双环 / Whisper constant 球, 5 verb 动态几何 + 1 verb 静态球, T245 (#162) 落地时 Whisper 改走 constant 球区别于前 5 verb 动态几何, 0 改既有 5 verb 几何 motif 0 改 1 motif), 改 1 motif / 漏 1 motif = 6 verb icon 视觉组连贯崩塌 + §9.6.5 `_VERB_ACHV_INFO` `geometry_zh` 字段 漂动 + §9.6.22 6 verb VFX 视觉组 几何 漂动, source-grep 验证 6 verb icon motif + §9.6.5 `_VERB_ACHV_INFO` 3 entry `geometry_zh` 字段 + §9.6.22 6 verb VFX 视觉组 几何 motif 0 漏 0 改 1 motif.
  5. **6 verb 双 export 0 漏 0 改 0 漏 1 文件 0 改 1 文件名 0 改 1 文件类型** (T013 + T034 + T040 + T085 + T103 + T245 落地后 0 触碰边界) — `assets/ui/<verb>_icon/<verb>_icon.png` 32x32 + `<verb>_icon_64x64.png` 64x64 2 文件 1:1 严格 (6/6 0 漏 0 改 0 漏 1 文件 0 改 1 文件名 0 改 1 文件类型, 0 触碰既有 5 verb 双 export), 改 1 文件名 / 漏 1 文件 = 6 verb icon 双 export 漂动, source-grep 验证 6 verb 双 export 2 文件 0 漏 0 改 1 文件名 0 改 1 文件类型 0 改 1 字符.
  6. **3 verb achievement 双路径 0 漏 0 改 0 漏 1 文件** (T245 #162 落地后 0 触碰边界) — `assets/ui/achievements/<verb>_icon/<verb>_icon.png` + `<verb>_icon_32x32.png` 2 文件 1:1 严格 (3/6 only echo_icon / wave_icon / whisper_icon, T245 (#162) 落地 4 PNG 双路径, 与 §9.6.21 PNG 双路径 1:1 镜像), 0 触碰既有 3 verb 0 改 0 漏 1 文件, 改 1 文件名 / 漏 1 文件 = 3 verb achievement 双路径 漂动 + §9.6.21 PNG 双路径 漂动, source-grep 验证 3 verb achievement 2 文件 0 漏 0 改 1 文件名 0 改 1 文件类型 0 改 1 字符.
  7. 已知 drift risk: 6 verb icon 视觉组 (5 段 canonical 1:1 序列) **可能**漏 1 段 1:1 镜像 (e.g. T245 (#162) 落地时漏 Stage 5 4 PNG 双路径 1:1 严格, 3 verb achievement path 0 镜像 §9.6.21 PNG 双路径 0 触碰; 漏 Stage 4 6 verb 几何 motif 0 镜像 §9.6.5 `_VERB_ACHV_INFO` `geometry_zh` 字段 0 触碰; 漏 Stage 3 6 verb 调色 0 镜像 §9.6.4 调色六元组 0 触碰). 监控建议: 任何"未来给 6 verb icon 加新 verb 或修 1 verb icon 视觉或修 1 verb 调色"必须**先**在 5 段序列 (Stage 1 family 路径 + Stage 2 disc + outer ring + Stage 3 调色 + Stage 4 几何 + Stage 5 双 export + 3 verb 双路径) 1:1 镜像 0 漏 1 段. T245 (#162) 5 段序列 1:1 镜像落地后任何新 verb / 新几何 / 新调色 加 5 段同步是 §9.6.26 5 段序列的"母集" (1 verb 5 段 1:1 严格, §9.6.26 5 段 1 verb 完整闭环, 6 verb × 5 段 = 30 件 1:1 严格, 含 6 verb 双 export 12 文件 0 漏 0 改 + 3 verb achievement 双路径 4 文件 0 漏 0 改). 另 1 类已知 drift: Stage 2 disc + outer ring 0 改 1 hex / 1 px (e.g. 改 deep ink navy #05070D 为 #0A0E15 偏深, 0 触碰既有 5 verb disc 配色), 后续 polish 期若加新 verb 必须**先**用 1:1 镜像 deep ink navy #05070D + glass cyan #69C7CE 1px outer ring 模板, 0 改既有 5 verb disc + outer ring.

**0 副作用**: T013 / T034 / T040 / T085 / T103 / T241 / T245 任何 const / var 0 触碰 (6 verb 调色 0 改 0 删 0 重排 + 6 verb 几何 motif 0 改 0 删 0 重排 + 6 verb family 路径 0 改 0 删 0 重排 + 6 verb 双 export 12 文件 0 改 0 删 0 重排 + 3 verb achievement 双路径 4 文件 0 改 0 删 0 重排); 0 触碰 §9.6.1 6 verb VFX 跨类 handler `is_instance_valid + has_method` 双守卫 (T282 0 改 6 verb `_current_<verb>_vfx` 任何字段); 0 触碰 §9.6.2 5 layer 视觉组既有 5 verb 任何 1 字段 1 alpha 公式 1 layer 顺序; 0 触碰 §9.6.3 5+1 verb 7 UI 通道 既有 6 verb 任何 1 字段 1 调点 (T282 0 改 6 verb 4 @onready var + 1 ability ref + 1 主题色 const + 1 风格 stylebox + 1 dict key + 1 iteration list element 0 触碰既有 5 步 1:1 复制 模板); 0 触碰 §9.6.4 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (T282 0 改 6 verb 调色 1 hex 0 改 1 通道值 + 0 改 HUD 6 行 6 通道 + 0 改 tooltip 6 字段); 0 触碰 §9.6.5 6 verb 视觉组连贯 tooltip `_build_verb_achievement_tooltip` 函数体 (T282 0 改 5 段序列任何 1 字段 0 改 `_VERB_ACHV_ICON_HINTS` 0 改 `_VERB_ACHV_INFO` 0 改 8 行硬约束 0 改 3 闭环宪法同步); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T282 0 改 6 verb `_on_<verb>_hit` 函数体任何 const / var / 屏染 / 屏抖 / VFX flash / 音频); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T282 0 改 6 verb 主信号 + 命中信号 + 结束信号 + 扩展信号 + PlayerActionGate register / unregister); 0 触碰 §9.6.18 16 件套 verb ability base (T282 0 改 6 verb ability 子类任何 const / var / `@export` / `@onready` / 5 共享方法 / `_exit_tree` 兜底 / `is_globally_blocking` 扩展); 0 触碰 §9.6.19 7 件套 windup VFX base (T282 0 改 6 verb windup VFX 子类任何 const / var / `_ready()` / `_process(delta)` / `_activate_windup_tween()` / `fade_out_and_free()`); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 transition flow (T282 0 改 GFC + room_transition + room_door + GameState 5 段序列); 0 触碰 §9.6.21 8 段成就序列 (T282 0 改 PlayerStats 6 verb 字段 + record_ability_used 6 branch + get_stat 6 entry + set_stat 6 entry + reset_run 6 行 + all_abilities_used 6 项 + achievements.json 4 tier ID + ICON_COLORS 6 entry + PNG 双路径); 0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T282 0 改 6 verb VFX 文件 任何 const / var / `@export` color 字段 / 5+ layer 视觉组 / 几何 / 跨段镜像); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T282 0 改 audio_manager_enhanced.gd 7 桶 `prewarm_*` 函数体 0 改 0 删 0 重排 + prewarm_all_sfx() aggregator 7 行 0 改 0 删 0 重排 + 3 跨调用方 0 改 0 删 0 重排); 0 触碰 §9.6.24 HUD 6 verb × 6 通道 = 36 通道 5 段序列 (T282 0 改 hud.gd 6 verb @onready var / var / const / dict key / stylebox / iteration list 任何 1 字段 0 改 0 删 0 重排); 0 触碰 §9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 5 段序列 (T282 0 改 pause_menu.gd `_VERB_ACHV_ICON_HINTS` / `_VERB_ACHV_INFO` / `base_tooltip` / `_build_verb_achievement_tooltip` / 11 slot 0 改 0 删 0 重排); 0 触碰 check_smoke_consistency.sh (T282 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, 0 触碰 6 verb icon 既有 PNG 文件 0 改既有 6 verb family 路径 0 改既有 6 verb 双 export 0 改既有 3 verb achievement 双路径); 0 行为变化, 1 文档 1 段 (§9.6.26 ~50 行) + 1 smoke test (T282 20+ 断言).

**与 §9.6.3 / §9.6.4 / §9.6.5 / §9.6.21 / §9.6.22 / §9.6.24 / §9.6.25 关系** — §9.6.3 覆盖 HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb 模式 (加新 1 verb 时 6 处 1:1 复制: tscn + 4 @onready var + 1 ability ref + 1 const + 1 stylebox + dict key + iteration list); §9.6.4 覆盖 6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 1+2+3); §9.6.5 覆盖 6 verb 视觉组连贯 tooltip `_build_verb_achievement_tooltip` 8 行拼接扩展器; §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 canonical 1:1 序列 (含 ICON_COLORS 6 entry + PNG 双路径 1 段); §9.6.22 覆盖 6 verb VFX 视觉组 5 段 canonical 1:1 序列; §9.6.24 覆盖 HUD 6 verb × 6 通道 = 36 通道 5 段 canonical 1:1 序列; §9.6.25 覆盖 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 canonical 1:1 序列. §9.6.26 覆盖 `assets/ui/<verb>_icon/` + `assets/ui/achievements/<verb>_icon/` 6 verb icon 视觉组 (5 段 canonical 1:1 序列) (Stage 1 6 verb family 路径 1:1 + Stage 2 6 verb disc + outer ring 1:1 + Stage 3 6 verb 调色 1:1 + Stage 4 6 verb 几何 motif 1:1 + Stage 5 6 verb 双 export + 3 verb achievement 双路径 1:1). §9.6.3 关注"HUD 6 verb 行 5+1 verb 7 UI 通道同步扩 verb structural sync (加新 1 verb 时 6 处 1:1 复制)", §9.6.4 关注"6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 1+2+3)", §9.6.5 关注"6 verb 视觉组连贯 tooltip 8 行拼接扩展器 (T250 落地 polish 模式, 5 段 1:1 严格分离待 §9.6.25 文档化)", §9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格, 含 PNG 双路径)", §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约 (调色 + 几何 + layer + 跨段镜像)", §9.6.24 关注"HUD 6 verb × 6 通道 = 36 通道 1:1 严格分离契约 (5 段 1:1 严格分离)", §9.6.25 关注"6 verb 视觉组连贯 tooltip 8 行拼接 1:1 严格分离契约 (5 段 canonical 1:1 序列)", §9.6.26 关注"6 verb icon 视觉组 1:1 严格分离契约 (5 段 canonical 1:1 序列: Stage 1 family 路径 + Stage 2 disc + outer ring + Stage 3 调色 + Stage 4 几何 + Stage 5 双 export + 3 verb achievement 双路径)". 14 套 polish 模式 0 反序 0 漏 0 改 1 字符. 任何"加新 1 verb icon"必须**先**按 §9.6.4 调色六元组加 1 entry (Stage 3 1:1 严格), **再**按 §9.6.3 5+1 verb 7 UI 通道 6 处 1:1 复制 (Stage 3 1:1 严格), **再**按 §9.6.24 HUD 36 通道 0 漏 1 verb 0 改 1 通道值 (Stage 3 1:1 严格), **再**按 §9.6.26 Stage 1 family 路径 1:1 加 1 目录 (Stage 1 1:1 严格), **再**按 §9.6.26 Stage 2 disc + outer ring 1:1 复制 (Stage 2 1:1 严格), **再**按 §9.6.26 Stage 4 几何 motif 1:1 加 1 motif (Stage 4 1:1 严格), **再**按 §9.6.26 Stage 5 双 export 1:1 加 2 文件 (Stage 5 1:1 严格), **再**按 §9.6.21 PNG 双路径 1:1 镜像 (3 verb achievement path 1:1 严格). §9.6.4 与 §9.6.26 0 互混 0 复用 0 共享: §9.6.4 关注"6 verb 调色六元组 + HUD 6 行 6 通道 + tooltip 三闭环宪法 (abstract 宪法 1+2+3, 1 句宪法 0 触及 5 段序列 0 触及 6 verb icon 双 export 0 触及 3 verb achievement 双路径)" (与 5 段序列 0 相关), §9.6.26 关注"6 verb icon 视觉组 5 段 canonical 1:1 序列 (5 段全在 PNG 文件 + STYLE_GUIDE.md 6 verb icon 列表)" (与 abstract 宪法 0 相关, Stage 3 1:1 严格 镜像 §9.6.4 调色六元组 6 entry 0 改 1 hex). §9.6.21 与 §9.6.26 0 互混 0 复用 0 共享: §9.6.21 关注"6 verb 成就系统 闭环契约 (8 段 1:1 严格, 含 ICON_COLORS 6 entry + PNG 双路径 1 段)" (与 5 段序列 0 相关, PNG 双路径仅 1 段), §9.6.26 关注"6 verb icon 视觉组 5 段 canonical 1:1 序列 (5 段全在 PNG 文件 + STYLE_GUIDE.md 6 verb icon 列表, 含 3 verb achievement 双路径 4 文件 1:1 镜像 §9.6.21 PNG 双路径)" (与 6 verb 成就系统 8 段 0 相关). §9.6.22 与 §9.6.26 0 互混 0 复用 0 共享: §9.6.22 关注"6 verb VFX 视觉组 5 段 1:1 严格分离契约 (Stage 1-5: class_name + 调色六元组 + layer + 几何 + 跨段镜像, 5 段全在 `<verb>_vfx.gd` 文件)" (与 icon 视觉组 0 相关, 仅 Stage 4 几何 motif 1:1 镜像 §9.6.26 Stage 4), §9.6.26 关注"6 verb icon 视觉组 5 段 canonical 1:1 序列 (5 段全在 PNG 文件 + STYLE_GUIDE.md 6 verb icon 列表)" (与 VFX 视觉组 0 相关). §9.6.26 是 14 套 polish 模式**唯一**关注"6 verb icon 视觉组 1:1 严格分离 (5 段 canonical 1:1 序列: Stage 1 family 路径 + Stage 2 disc + outer ring + Stage 3 调色 + Stage 4 几何 + Stage 5 双 export + 3 verb achievement 双路径)"的 1 套, 与 §9.6.3 / §9.6.4 / §9.6.5 / §9.6.21 / §9.6.22 / §9.6.24 / §9.6.25 13 套 HUD 6 verb 行 / 调色 + HUD 6 行 6 通道 + tooltip 拼接扩展器 / 成就 / VFX 视觉组 / HUD 36 通道 / 视觉组连贯 tooltip 拼接 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.26 5 段序列走 §9.1 9 步落地的 "const" (3) + "icon" (6) 2 步 (`STYLE_GUIDE.md` §9.6.4 调色六元组 6 verb entry + `assets/ui/<verb>_icon/` 6 verb family 路径 + `assets/ui/<verb>_icon/<verb>_icon.png` 32x32 + `<verb>_icon_64x64.png` 64x64 6 verb 双 export 12 文件 + `assets/ui/achievements/<verb>_icon/` 3 verb achievement 双路径 4 文件 + 6 verb disc + outer ring 1:1 + 6 verb 几何 motif 1:1), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "input" (4) + "shop" (5) + "vfx" (7) + "audio" (8) + "HUD 冷光勾边" (9) 7 步 (6 verb icon 视觉组 0 走 verb enum + ability 子类 + input map + shop 系统 + VFX 视觉组 + audio 副作用 + HUD 渲染). 任何"加新 1 verb icon"必须**先**按 §9.1 9 步落地 (含 "const" (3) + "icon" (6) 步), **再**按 §9.6.26 5 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 任何"加新 1 verb 调色"必须**先**在 `STYLE_GUIDE.md` §9.6.4 调色六元组加 1 entry (Stage 3 1:1 严格, 0 漏 1 entry 0 改 1 hex), **再**在 6 verb icon motif 颜色 1:1 镜像 (Stage 3 1:1 严格, 0 漏 1 通道 0 改 1 通道值), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) 步 (6 verb icon 视觉组 0 触碰 verb enum + ability 子类). 任何"加新 1 verb icon"必须**先**在 `assets/ui/<verb>_icon/` family 路径 1:1 加 1 目录 (Stage 1 1:1 严格), **再**在 6 verb 双 export 1:1 加 2 文件 (Stage 5 1:1 严格, 0 漏 1 文件 0 改 1 文件名), 0 触碰 §9.1 9 步任何 "icon" (6) 步既有 6 verb icon PNG 双路径. 任何"加新 1 verb achievement 双路径"必须**先**在 §9.6.21 PNG 双路径 1:1 镜像 (Stage 5 1:1 严格), **再**在 `assets/ui/achievements/<verb>_icon/` 3 verb achievement 双路径 1:1 加 2 文件 (Stage 5 1:1 严格, 0 漏 1 文件 0 改 1 文件名), 0 触碰 §9.6.21 既有 3 verb 0 改 0 漏 1 文件. 任何"加新 1 跨段镜像"必须**先**在 §9.6.3 / §9.6.4 / §9.6.21 / §9.6.22 / §9.6.24 / §9.6.25 6 跨段加 1 镜像 (5 段序列 1:1 严格, 0 漏 1 跨段 0 改 1 字段), 0 触碰 §9.1 9 步任何 const / var / 节点 (5 段序列仅在 `STYLE_GUIDE.md` 6 verb icon 列表 + §9.6.4 调色六元组 + 6 verb icon PNG 文件 + 3 verb achievement PNG 文件, 6 verb 子类 0 改 base 0 改, 5 verb lazy-init 守卫 0 删 0 改, 含 1 verb Whisper T245 #162 接入完成).

### 9.6.27 PlayerProfilePanel 3 组件 (ProfileQuickStats 4 段 + ProfileRecentList 5 行 × 7 字段 + ProfileAudit 4 字段) (1 panel × 3 组件 × 1:1 视觉组连贯) polish 模式 (T162 + T213 + T214 + T215 + T216 + T217 + T219 + T225 + T226 + T229 + T231 + T234 + T235 + T240 + T249 + T256 + T257 + T258 + T259 + T260 + T261 + T262 + T263 跨 23 任务 ~200 轮落地) 文档化

> §9.6.6 记录 ProfileRecentList 5 行 row 文本 5→7 字段 format 扩展模式; §9.6.7 记录 ProfileRecentList 5 行 row hover feedback 三件套 (font_color + alpha boost + tooltip); §9.6.8 记录 ProfileQuickStats 4 段独立 hover 联动 (4 段 tween + dim restore); §9.6.9 记录 AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` Dictionary 双源 (5 行 + 11 slot 共享 `clampf(base + 0.1)`); §9.6.10 记录「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (T215 + T216 / T217 + T218 模式). 本节记录 `pause_menu.gd` PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 polish 模式 — 跨 5 段 (Stage 1 3 组件 1:1 严格分离 + Stage 2 中点分隔符 ` · ` 1:1 严格 + Stage 3 7pt 字号 1:1 严格 + Stage 4 _COLOR_* canonical 颜色 token 1:1 严格 + Stage 5 派生字段 1:1 严格) — 跨 23 任务 (T162 + T213 + T214 + T215 + T216 + T217 + T219 + T225 + T226 + T229 + T231 + T234 + T235 + T240 + T249 + T256 + T257 + T258 + T259 + T260 + T261 + T262 + T263) ~200 轮迭代落地, 3 组件 × 1 panel 1:1 严格分离 (4 段 + 35 字段 + 4 字段 = 43 element 1:1 + 跨组件共享中点分隔符 + 跨组件共享 7pt 字号 + 跨组件共享 _COLOR_* canonical 颜色 token + 派生字段 1:1 严格), 0 漂动 0 后续 主要 pre-existing 风险. 任何"加新 1 段"或"加新 1 字段"或"加新 1 组件"或"修 1 段颜色"或"修 1 字段格式"必须**先**按 Stage 1 3 组件 1:1 加 1 组件 (1 panel 1:1 严格), **再**按 Stage 2 中点分隔符 1:1 复制 (Stage 2 1:1 严格), **再**按 Stage 3 7pt 字号 1:1 镜像 (Stage 3 1:1 严格), **再**按 Stage 4 _COLOR_* canonical 颜色 token 加 1 entry (Stage 4 1:1 严格), **再**按 Stage 5 派生字段 1:1 同步 (Stage 5 1:1 严格). 跨 1 panel × 3 组件 × 4 段 (Achievements 成就 / 最佳 / 最长单房 / Run #) + 5 行 × 7 字段 (Run # / 房 / 净 / 碎 / 时 / 房/时 / 净/时) + 4 字段 (ok / 损坏 / 漂移 / 空) = 43 element 1:1 严格 0 漏 1 element 0 改 1 字段 0 改 1 字符 0 例外.

- **症状**: PlayerProfilePanel 1 panel × 3 组件 (ProfileQuickStats 4 段 + ProfileRecentList 5 行 × 7 字段 + ProfileAudit 4 字段) 跨 23 task 反复 polish 期间出现 5 类典型 fragile: (1) "加新 1 段漏 Stage 1 3 组件 1:1" — 加新 1 段到 ProfileQuickStats 时漏 ProfileRecentList + ProfileAudit 同步 (e.g. 加 "本周 5 局最佳" 段到 QuickStats 4 段 → 5 段, 漏 RecentList 加 1 行 同字段镜像, 漏 Audit 加 1 字段 同字段镜像, 玩家扫 3 组件看到新段只在 QuickStats 出现 视觉组连贯崩塌); (2) "修 1 字段漏 Stage 2 中点分隔符 1:1 镜像" — 修 ProfileQuickStats 4 段 `_format_quick_stats_row()` 字符串时改分隔符 e.g. "  ·  " → "  •  " 或 " | ", 触发 3 组件中点分隔符 1:1 严格 0 撞 ProfileRecentList 5 行 + ProfileAudit 4 字段 既有 _RECENT_ROW_FIELD_SEP (`  ·  ` 4 字符中点带前后 2 空格) 0 触碰 (Stage 2 1:1 严格 0 改 1 字符), 玩家扫 3 组件看到中点分隔符 0 协调, 跨组件视觉组连贯崩塌; (3) "加新 1 组件漏 Stage 3 7pt 字号 1:1 镜像" — 加新 1 组件 (e.g. ProfileTrend 5/10/20 行) 到 PlayerProfilePanel 时漏 font_size=7 0 走 8/9/10 字号 (e.g. T229 #149 ProfileAudit 早期版本 1 段用 8pt 后续 T256 #176 收回 7pt), 触发 3 组件 7pt 字号 1:1 严格 0 撞既有 3 组件 7pt 0 触碰 (Stage 3 1:1 严格 0 改 1 pt), 玩家扫 3 组件看到新组件字号偏大 0 协调 跨组件视觉组连贯崩塌; (4) "修 1 段颜色漏 Stage 4 _COLOR_* canonical 颜色 token 1:1 同步" — 修 ProfileQuickStats 4 段 4 调色 (Achievements Glass Cyan #69C7CE / 最佳 Amber Voice #F2B66E / 最长单房 Muted Violet #65506A / Run # Pale Resonance #B7E6DC) 0 改 _QUICK_STATS_HINT `_COLOR_*` canonical 颜色 token (`_COLOR_RECENT_RUN_LATEST` Amber Voice + `_COLOR_RECENT_RUN_NORMAL` Pale Resonance + `_COLOR_ZERO_STAT` 暖白 3 颜色 token + ProfileAudit 4 字段 3 状态色 暖白 (0.875, 0.835, 0.784) / 暖黄 (0.85, 0.71, 0.43) / 暖红 (0.85, 0.45, 0.4) inline literals in `_refresh_profile_audit()`) 0 镜像 ProfileRecentList 5 行 2 色 + ProfileAudit 4 字段 3 状态色, 触发 Stage 4 1:1 严格 0 漏 1 token 0 改 1 通道值 (跨组件 _COLOR_* canonical 颜色 token 0 协调, 玩家扫 3 组件看到颜色 0 视觉组连贯, Run # Pale Resonance 与 ProfileAudit 暖白 0 区分存档健康状态); (5) "加新 1 派生字段漏 Stage 5 派生字段 1:1 同步" — 加新 1 派生字段到 ProfileRecentList 5 行 × 7 字段 (e.g. "净/房" = 净/房 派生率, 派生自原 5 字段中的 房/净) 时漏 `_RECENT_ROW_FIELD_SEP` 6 中点 0 改 0 漏 (Stage 2 1:1 严格), 漏 `_RECENT_ROW_HINT` 7 entry → 8 entry 加 1 entry 0 改 0 漏 (Stage 5 1:1 严格), 漏 `_build_recent_row_tooltip()` 8 行 → 9 行 拼接 0 改 0 漏 (Stage 5 1:1 严格), 漏 `_format_recent_row_text()` format 字符串 7 placeholder → 8 placeholder 0 改 0 漏 (Stage 5 1:1 严格), 漏 t_sec == 0 guard (Stage 5 1:1 严格, 0 触碰 0/0 = nan defensive 路径), 玩家扫 5 行看到 0 派生率 inline 0 tooltip 看 0 派生率含义 派生字段视觉组连贯崩塌. 5 类症状根因: 1 panel × 3 组件 × 1:1 视觉组连贯 polish 模式 0 文档化, polish 期只能"凭记忆"同步 5 件套 (1 3 组件结构 1:1 + 1 中点分隔符 1:1 + 1 7pt 字号 1:1 + 1 _COLOR_* canonical 颜色 token 1:1 + 1 派生字段 1:1), 1 漏 1 = 1 panel × 3 组件 × 1:1 视觉组连贯 漂动 (3 组件 0 镜像 / 中点分隔符 0 协调 / 字号 0 协调 / 颜色 0 协调 / 派生字段 0 同步).

- **触发场景**: PlayerProfilePanel 1 panel × 3 组件 (ProfileQuickStats 4 段 + ProfileRecentList 5 行 × 7 字段 + ProfileAudit 4 字段) 1:1 视觉组连贯落地需同步 5 件套: (1) **Stage 1 3 组件 1:1 严格分离** — `pause_menu.tscn` PlayerProfilePanel/ProfileMargin/ProfileVBox/ProfileQuickStats + ProfileRecentList + ProfileAudit 3 组件 1:1 严格 (3 组件 0 漏 0 改 0 重命名 0 反序 0 改 1 字符, T162 + T213 + T215 + T229 跨 4 task 落地 0 漂动), 0 触碰既有 3 组件 0 改 0 漏; (2) **Stage 2 中点分隔符 ` · ` 1:1 严格分离** — 3 组件共享中点分隔符 `_RECENT_ROW_FIELD_SEP := "  ·  "` 4 字符中点带前后 2 空格 1:1 严格 (3 组件 1:1 严格 0 改 0 漏 0 改 1 字符 0 改 1 空格 0 改 1 中点, T235 #154 落地 0 触碰 既有 3 组件 0 改 0 漏), 0 触碰既有 3 组件中点分隔符; (3) **Stage 3 7pt 字号 1:1 严格分离** — 3 组件所有 Label `add_theme_font_size_override("font_size", 7)` 1:1 严格 (3 组件 1:1 严格 0 改 0 漏 0 改 1 pt, T256 #176 落地时 ProfileAudit 收回 8pt → 7pt 0 触碰 既有 3 组件 0 改 0 漏), 0 触碰既有 3 组件 7pt 字号; (4) **Stage 4 _COLOR_* canonical 颜色 token 1:1 严格分离** — 3 组件共享 `_COLOR_*` canonical 颜色 token (ProfileQuickStats 4 段 4 调色 hex 字符串 + ProfileRecentList 5 行 2 色 `_COLOR_RECENT_RUN_LATEST` Amber Voice + `_COLOR_RECENT_RUN_NORMAL` Pale Resonance + ProfileAudit 4 字段 3 状态色 暖白 / 暖黄 / 暖红 inline literals in `_refresh_profile_audit()`, T213 + T219 + T229 落地时 0 触碰既有 3 组件 0 改 0 漏, Stage 4 1:1 严格 0 改 0 漏 0 改 1 通道值 0 改 1 hex 0 改 1 字段), 0 触碰既有 3 组件 _COLOR_* canonical 颜色 token; (5) **Stage 5 派生字段 1:1 严格分离** — ProfileRecentList 5 行 × 7 字段 派生 2 字段 (房/时 + 净/时) from 原 5 字段 (Run # / 房 / 净 / 碎 / 时) 1:1 严格 (T249 #167 落地时 5 字段 → 7 字段 0 触碰既有 5 字段 0 改 0 漏 0 漏 1 派生字段, Stage 5 1:1 严格 0 改 0 漏 0 改 1 派生率 0 改 1 公式 0 漏 1 派生率). 5 件套 1:1 同步, 1 漏 1 = 1 panel × 3 组件 × 1:1 视觉组连贯 漂动.

- **修复** (跨 23 任务 ~200 轮落地后已知 5 段 canonical 1:1 序列 + 3 组件 1:1 视觉组连贯 0 例外):
  - **T162 (#83) A147 ProfileQuickStats 4 段 起步 (1 panel × 1 组件 1:1 模板)** — `PlayerProfilePanel/ProfileMargin/ProfileVBox/ProfileQuickStats` 4 段 `HBoxContainer` 起步 (T162 落地时 1 panel × 1 组件 模板, 4 段 `_quick_stats_achievement` + `_quick_stats_best_time` + `_quick_stats_longest_room` + `_quick_stats_run_number` 4 @onready var 0 触碰 0 改 0 漏 1 段), 0 触碰既有 4 段既有字段.
  - **T213 (#133) A179 ProfileQuickStats 4 段 hover tooltip 数据源 (`_QUICK_STATS_HINT` 4 entry 共享 + `_build_quick_stats_tooltip()` 函数体)** — `pause_menu.gd` `_QUICK_STATS_HINT` const 4 entry (成就 Glass Cyan #69C7CE / 最佳 Amber Voice #F2B66E / 最长单房 Muted Violet #65506A / Run # Pale Resonance #B7E6DC) + `_build_quick_stats_tooltip()` 9 行拼接 (1 header + 4 段 bullet + 4 缩进描述) 1:1 严格落地 (T213 落地时 4 段 tooltip 数据源 0 触碰既有 4 段既有字段 0 改 0 漏 0 漏 1 段 tooltip 同步), 0 触碰既有 4 段既有字段.
  - **T214 (#134) A180 ProfileQuickStats 4 段独立 hover 联动 (4 段 tween + dim restore)** — `pause_menu.gd` `_on_quick_stats_hover_in(segment_id)` 4 段独立 hover tween `Color.WHITE` (0.3s TRANS_QUAD + EASE_OUT) + `_on_quick_stats_hover_out(segment_id)` dim restore 0.3s 1:1 严格落地 (T214 落地时 4 段独立 hover 0 触碰既有 4 段既有字段 0 改 0 漏 0 漏 1 段 hover handler), 0 触碰既有 4 段既有字段.
  - **T215 (#136) A181 ProfileRecentList 5 行独立 hover 反馈 (5 行 tween + default_color save)** — `pause_menu.gd` `_on_recent_row_hover_in(idx)` 5 行 tween `Color.WHITE` (0.12s 升级到 T240 #158) + `_on_recent_row_hover_out(idx)` default_color restore + `_recent_row_default_color.append(default_color)` 5 行 默认色保存 1:1 严格落地 (T215 落地时 5 行独立 hover 0 触碰既有 5 行既有字段 0 改 0 漏 0 漏 1 行 hover handler), 0 触碰既有 5 行既有字段.
  - **T216 (#137) A182 ProfileRecentList 5 行 hover tooltip 数据源 (`_RECENT_ROW_HINT` 5 entry 共享 + `_build_recent_row_tooltip()` 函数体)** — `pause_menu.gd` `_RECENT_ROW_HINT` const 5 entry (Run # / 房 / 净 / 碎 / 时) + `_build_recent_row_tooltip()` 11 行拼接 (1 header + 5 字段 × 2 行/字段) 1:1 严格落地 (T216 落地时 5 字段 tooltip 数据源 0 触碰既有 5 字段既有字段 0 改 0 漏), 0 触碰既有 5 字段既有字段.
  - **T217 (#138) A183 ProfileQuickStats 4 段 独立 hover 状态字段 (`_quick_stats_hovered` 4 entry dict)** — `pause_menu.gd` `_quick_stats_hovered` Dictionary 4 entry (achievement / best_time / longest_room / run_number) + re-entrant guard 1:1 严格落地 (T217 落地时 4 段独立 hover 状态 0 触碰既有 4 段既有字段 0 改 0 漏), 0 触碰既有 4 段既有字段.
  - **T219 (#141) A185 ProfileRecentList 5 行 alpha 渐变 (5 步等差梯度 1.0/0.875/0.75/0.625/0.5)** — `pause_menu.gd` `_RECENT_ROW_ALPHA_MAX` / `_RECENT_ROW_ALPHA_MIN` const 1.0/0.5 + 5 步等差梯度 `_RECENT_ROW_ALPHA_MAX - i * alpha_step` 1:1 严格落地 (T219 落地时 5 行 alpha 渐变 0 触碰既有 5 行既有字段 0 改 0 漏 0 漏 1 行 alpha 公式), 0 触碰既有 5 行既有字段.
  - **T225 (#147) A186 ProfileQuickStats 4 段 hover 提亮 tween 引用 (1 个全局 + 4 段共享)** — `pause_menu.gd` `_quick_stats_font_color_tween` Tween 1 个全局 + 4 段 hover 0.3s 共享 tween 1:1 严格落地 (T225 落地时 4 段 hover tween 0 触碰既有 4 段既有字段 0 改 0 漏 0 漏 1 段 tween 引用), 0 触碰既有 4 段既有字段.
  - **T226 (#145) A187 AchievementGrid locked slot 解锁进度 alpha lerp + 跨面板 `_alpha_base` Dictionary 双源 polish 模式** — `pause_menu.gd` `_alpha_base` Dictionary 共享 ProfileRecentList 5 行 + AchievementGrid 11 slot (T226 落地时跨面板 _alpha_base 0 触碰既有 5 行 + 11 slot 既有字段 0 改 0 漏 0 漏 1 panel), 0 触碰既有 3 组件既有字段.
  - **T229 (#149) A190 ProfileAudit 4 字段 1 行 (存档 5 slot 巡检)** — `pause_menu.gd` `_refresh_profile_audit()` 4 字段 1 行紧凑 "存档 %d ok · %d 损坏 · %d 漂移 · %d 空" + SaveSystem.audit_save_slots() 公共接口 1:1 严格落地 (T229 落地时 4 字段 1 行 0 触碰既有 4 段 + 5 行既有字段 0 改 0 漏 0 漏 1 组件 0 漏 1 字段), 0 触碰既有 2 组件既有字段.
  - **T231 (#151) A192 ProfileRecentList 5 行 base alpha 字典 (`_recent_row_hover_alpha_base` row → base_alpha)** — `pause_menu.gd` `_recent_row_hover_alpha_base` Dictionary row → base_alpha 1:1 严格落地 (T231 落地时 5 行 base alpha 0 触碰既有 5 行既有字段 0 改 0 漏 0 漏 1 行 base alpha 字典), 0 触碰既有 5 行既有字段.
  - **T234 (#153) A195 ProfileRecentList 5 行 row text 末尾 1 个 " ↗" 字符 (_RECENT_ROW_TIP_INDICATOR)** — `pause_menu.gd` `_RECENT_ROW_TIP_INDICATOR` const " ↗" 2 字符 + 5 行 row text 末尾 1:1 严格落地 (T234 落地时 5 行 tip indicator 0 触碰既有 5 行既有字段 0 改 0 漏 0 漏 1 字符), 0 触碰既有 5 行既有字段.
  - **T235 (#154) A196 中点分隔符 `  ·  ` 1:1 严格 (3 组件共享 _RECENT_ROW_FIELD_SEP)** — `pause_menu.gd` `_RECENT_ROW_FIELD_SEP` const "  ·  " 4 字符中点带前后 2 空格 + 3 组件 (ProfileQuickStats 4 段 + ProfileRecentList 5 行 + ProfileAudit 4 字段) 1:1 严格共享 1:1 严格落地 (T235 落地时 3 组件中点分隔符 0 触碰既有 3 组件既有字段 0 改 0 漏 0 漏 1 边), 0 触碰既有 3 组件既有字段.
  - **T240 (#158) A198 ProfileRecentList 5 行 hover font_color 提亮 0.12s 渐变 (T215 snap 升级 + 与 T231 alpha boost 0.12s 同步)** — `pause_menu.gd` `_RECENT_ROW_FONT_COLOR_FADE_DURATION` const 0.12s + `_recent_row_font_color_tween` Tween 5 行共享 + `tween_property(row, "theme_override_colors/font_color", Color.WHITE, 0.12s)` 1:1 严格落地 (T240 落地时 5 行 tween 0.12s 0 触碰既有 5 行既有字段 0 改 0 漏 0 漏 1 边), 0 触碰既有 5 行既有字段.
  - **T249 (#167) A205 ProfileRecentList 5 行 row 文本 5 字段 → 7 字段扩展 (同步 _RECENT_ROW_HINT tooltip)** — `pause_menu.gd` `_RECENT_ROW_HINT` 5 entry → 7 entry 扩展 (房/时 + 净/时 2 派生率) + `_format_recent_row_text()` 7 placeholder + `_build_recent_row_tooltip()` 5 字段 → 7 字段 bullet 1:1 严格落地 (T249 落地时 5 字段 → 7 字段 0 触碰既有 5 字段既有字段 0 改 0 漏 0 漏 1 字段 0 漏 1 派生率), 0 触碰既有 5 字段既有字段.
  - **T256 (#176) A211 §9.6.6 ProfileRecentList 5 行 row 文本 5 字段 → 7 字段 format 字符串扩展模式 polish 文档化** — `CONTRIBUTING.md` §9.6.6 5 行 row 文本 5 字段 → 7 字段 format 字符串扩展模式 1:1 严格落地 (T256 落地时 §9.6.6 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - **T257 (#177) A212 §9.6.7 ProfileRecentList 5 行 row hover feedback 三件套 (font_color + alpha boost + tooltip) polish 文档化** — `CONTRIBUTING.md` §9.6.7 5 行 row hover feedback 三件套 1:1 严格落地 (T257 落地时 §9.6.7 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - **T258 (#178) A213 §9.6.8 ProfileQuickStats 4 段独立 hover 联动 polish 文档化** — `CONTRIBUTING.md` §9.6.8 4 段独立 hover 联动 1:1 严格落地 (T258 落地时 §9.6.8 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - **T259 (#179) A214 §9.6.9 AchievementGrid locked slot 解锁进度 alpha lerp + ProfileRecentList 5 行 alpha 渐变 base + 跨面板 `_alpha_base` Dictionary 双源 polish 文档化** — `CONTRIBUTING.md` §9.6.9 跨面板 _alpha_base Dictionary 双源 1:1 严格落地 (T259 落地时 §9.6.9 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - **T260 (#181) A215 §9.6.10 「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 polish 文档化** — `CONTRIBUTING.md` §9.6.10 双层 hover 互补 1:1 严格落地 (T260 落地时 §9.6.10 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - **T261 (#182) A216 §9.6.11 PlayerProfilePanel 4 段 ↔ 5 行 ↔ 4 字段 ↔ 1 panel 1:1 视觉组连贯 polish 文档化** — `CONTRIBUTING.md` §9.6.11 1 panel × 3 组件 × 1:1 视觉组连贯 1:1 严格落地 (T261 落地时 §9.6.11 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - **T262 (#183) A217 §9.6.12 ProfileAudit 4 字段 (ok/损坏/漂移/空) ↔ SaveSystem.audit_save_slots() 公共接口 1:1 polish 文档化** — `CONTRIBUTING.md` §9.6.12 ProfileAudit 4 字段 ↔ SaveSystem 公共接口 1:1 1:1 严格落地 (T262 落地时 §9.6.12 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - **T263 (#184) A218 §9.6.13 ProfileQuickStats 4 段 ↔ ProfileRecentList 5 行 ↔ ProfileAudit 4 字段 (3 组件 跨组件 `_QUICK_STATS_HINT` / `_RECENT_ROW_HINT` / inline format 数据源 + 7pt 字号 + 中点分隔符 1:1) polish 文档化** — `CONTRIBUTING.md` §9.6.13 3 组件 跨组件 数据源 + 字号 + 分隔符 1:1 1:1 严格落地 (T263 落地时 §9.6.13 文档 0 触碰既有 §9.6 任何子段任何 1 字符 0 改 0 漏), 0 触碰既有 §9.6 任何子段.
  - 跨 23 任务 ~200 轮落地后 0 漂动, 1 panel × 3 组件 × 1:1 视觉组连贯 5 段 canonical 1:1 序列 100% 闭环, 5 段 1:1 严格分离 (Stage 1 3 组件 1:1 严格 0 漏 0 改 0 重命名 0 反序 + Stage 2 中点分隔符 1:1 严格 0 漏 0 改 0 改 1 字符 0 改 1 空格 0 改 1 中点 + Stage 3 7pt 字号 1:1 严格 0 漏 0 改 0 改 1 pt + Stage 4 _COLOR_* canonical 颜色 token 1:1 严格 0 漏 0 改 0 改 1 通道值 0 改 1 hex 0 改 1 字段 + Stage 5 派生字段 1:1 严格 0 漏 0 改 0 改 1 派生率 0 改 1 公式 0 漏 1 派生率), 3 组件 调色 1:1 严格分离 (ProfileQuickStats 4 段 4 调色 hex 字符串 (Glass Cyan + Amber Voice + Muted Violet + Pale Resonance) + ProfileRecentList 5 行 2 色 `_COLOR_RECENT_RUN_LATEST` Amber Voice + `_COLOR_RECENT_RUN_NORMAL` Pale Resonance + ProfileAudit 4 字段 3 状态色 暖白 / 暖黄 / 暖红 inline literals in `_refresh_profile_audit()` = 4 + 2 + 3 = 9 颜色 token 0 漏 0 重叠 0 互混 0 复用 0 共享). 23 任务 0 后续 主要 pre-existing 风险.

- **预防**:
  1. 任何 polish 期给 "加新 1 段" 或 "加新 1 字段" 或 "加新 1 组件" 或 "修 1 段颜色" 或 "修 1 字段格式" 任务加新段 / 加新字段 / 加新组件 / 改颜色 / 改格式时**必须**严格按 5 段 canonical 1:1 序列 1:1 复制, 0 反序 0 漏 0 改 1 字段. 5 段 0 触碰边界: **Stage 1 0 改 3 组件** (T162 + T213 + T215 + T229 落地后 3 组件 (ProfileQuickStats + ProfileRecentList + ProfileAudit) 1:1 严格, 0 改 0 重命名 0 漏 1 组件 0 反序, 改 1 组件名 = `pause_menu.gd` `_profile_quick_stats` / `_profile_recent_list` / `_profile_audit` 任何 1 @onready var load 0 找到节点 0 走 fallback 0 视觉组连贯崩塌), **Stage 2 0 改中点分隔符** (T235 #154 落地后 3 组件共享 `_RECENT_ROW_FIELD_SEP` "  ·  " 4 字符 1:1 严格, 0 改 0 漏 0 改 1 字符 0 改 1 空格 0 改 1 中点, 改 1 字符 = 3 组件中点分隔符 0 协调 跨组件视觉组连贯崩塌), **Stage 3 0 改 7pt 字号** (T256 #176 落地后 3 组件所有 Label `font_size = 7` 1:1 严格, 0 改 0 漏 0 改 1 pt, 改 1 pt = 3 组件字号 0 协调 跨组件视觉组连贯崩塌), **Stage 4 0 改 _COLOR_* canonical 颜色 token** (T213 + T219 + T229 落地后 3 组件 _COLOR_* canonical 颜色 token 1:1 严格, 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 通道值 / 1 hex = 3 组件颜色 0 协调 跨组件视觉组连贯崩塌), **Stage 5 0 改 派生字段 1:1 同步** (T249 #167 落地后 ProfileRecentList 5 行 × 7 字段 派生 2 字段 1:1 严格, 0 改 0 漏 0 改 1 派生率 0 改 1 公式 0 漏 1 派生率, 改 1 派生率 / 1 公式 / 漏 1 派生率 = 派生字段视觉组连贯崩塌). 任何"加新 1 段"必须**先**按 Stage 1 3 组件 1:1 加 1 段 (Stage 1 1:1 严格, 0 改既有 3 组件 0 改 1 字符), **再**按 Stage 2 中点分隔符 1:1 复制 (Stage 2 1:1 严格, 0 改既有 3 组件 0 改 1 字符), **再**按 Stage 3 7pt 字号 1:1 镜像 (Stage 3 1:1 严格, 0 改既有 3 组件 0 改 1 pt), **再**按 Stage 4 _COLOR_* canonical 颜色 token 加 1 entry (Stage 4 1:1 严格, 0 改既有 3 组件 0 漏 1 token), **再**按 Stage 5 派生字段 1:1 同步 (Stage 5 1:1 严格, 0 改既有 5 字段 0 漏 1 派生率). 任何"加新 1 字段"必须**先**按 Stage 1 3 组件 1:1 加 1 字段 (Stage 1 1:1 严格), **再**按 Stage 2 中点分隔符 1:1 加 1 间隔 (Stage 2 1:1 严格, 0 漏 1 间隔 0 改 1 字符), **再**按 Stage 3 7pt 字号 1:1 镜像 (Stage 3 1:1 严格, 0 改既有 3 组件 0 改 1 pt), **再**按 Stage 4 _COLOR_* canonical 颜色 token 加 1 entry (Stage 4 1:1 严格), **再**按 Stage 5 派生字段 1:1 加 1 派生率 (Stage 5 1:1 严格, 0 改既有 5 字段 0 漏 1 派生率 0 改 1 公式).
  2. **5 段序列 0 触碰边界** — `STYLE_GUIDE.md` 任何 const / var 0 改 0 删 0 重排 (5 段 0 触碰边界), 任何"修 1 段颜色"polish 走 Stage 4 1:1 严格 (改 _COLOR_* canonical 颜色 token 既有 entry 时**必须**同步改 `pause_menu.gd` `_QUICK_STATS_HINT` / `_RECENT_ROW_HINT` 6+7=13 entry color 字段 + 跨段镜像 §9.6.4 调色六元组 6 verb entry, 0 改 1 通道值 0 漏 1 跨段); 任何"修 1 字段格式"polish 走 Stage 2 1:1 严格 (改 _RECENT_ROW_FIELD_SEP 既有 4 字符时**必须**同步改 3 组件既有 4 字符中点分隔符, 0 改 1 字符 0 漏 1 跨组件).
  3. **3 组件 0 改 0 漏 0 改 1 字段 0 改 1 节点** (T162 + T213 + T215 + T229 落地后 0 触碰边界) — `pause_menu.gd` 3 组件 (ProfileQuickStats + ProfileRecentList + ProfileAudit) 1:1 严格分离 0 漏 0 改 0 重命名 0 反序 0 改 1 字符 0 改 1 节点, 改 1 组件名 / 漏 1 组件 = 1 panel × 3 组件 × 1:1 视觉组连贯 漂动, source-grep 验证 `pause_menu.tscn` PlayerProfilePanel/ProfileMargin/ProfileVBox/ProfileQuickStats + ProfileRecentList + ProfileAudit 3 组件 0 漏 0 改 1 字符 0 反序.
  4. **3 组件 _COLOR_* canonical 颜色 token 0 改 1 通道值 0 改 1 hex** (T213 + T219 + T229 落地后 0 触碰边界) — `pause_menu.gd` 3 组件 _COLOR_* canonical 颜色 token (ProfileQuickStats 4 段 4 调色 hex 字符串 + ProfileRecentList 5 行 2 色 `_COLOR_RECENT_RUN_LATEST` / `_COLOR_RECENT_RUN_NORMAL` + ProfileAudit 4 字段 3 状态色 暖白 / 暖黄 / 暖红 inline literals in `_refresh_profile_audit()` = 4 + 2 + 3 = 9 颜色 token) 1:1 严格分离 0 漏 0 重叠 0 互混 0 复用 0 共享, 改 1 通道值 / 1 hex = 3 组件颜色 0 协调 跨组件视觉组连贯 漂动, source-grep 验证 3 组件 _COLOR_* 2 颜色 token + `_QUICK_STATS_HINT` 4 entry color 字段 + `_RECENT_ROW_HINT` 0 entry color 字段 (无) + `_refresh_profile_audit()` 3 状态色 inline literals 0 漏 0 改 1 字段 0 改 1 通道值.
  5. **中点分隔符 0 改 0 漏 0 改 1 字符 0 改 1 空格 0 改 1 中点** (T235 #154 落地后 0 触碰边界) — `pause_menu.gd` `_RECENT_ROW_FIELD_SEP` const "  ·  " 4 字符中点带前后 2 空格 1:1 严格 (3 组件共享 0 漏 0 改 0 改 1 字符 0 改 1 空格 0 改 1 中点, 0 触碰既有 3 组件既有字段), 改 1 字符 / 漏 1 字符 = 3 组件中点分隔符 0 协调 跨组件视觉组连贯 漂动, source-grep 验证 3 组件中点分隔符 0 漏 0 改 1 字符 0 改 1 空格 0 改 1 中点 0 改 1 字段.
  6. **7pt 字号 0 改 0 漏 0 改 1 pt** (T256 #176 落地后 0 触碰边界) — `pause_menu.gd` 3 组件所有 Label `add_theme_font_size_override("font_size", 7)` 1:1 严格 (3 组件 0 漏 0 改 0 改 1 pt, 0 触碰既有 3 组件既有字段), 改 1 pt = 3 组件字号 0 协调 跨组件视觉组连贯 漂动, source-grep 验证 3 组件 font_size = 7 0 漏 0 改 1 pt 0 改 1 字段 0 改 1 字符.
  7. **派生字段 0 改 0 漏 0 改 1 派生率 0 改 1 公式 0 漏 1 派生率** (T249 #167 落地后 0 触碰边界) — `pause_menu.gd` ProfileRecentList 5 行 × 7 字段 派生 2 字段 (房/时 + 净/时) from 原 5 字段 (Run # / 房 / 净 / 碎 / 时) 1:1 严格 (T249 (#167) 落地时 5 字段 → 7 字段 0 触碰既有 5 字段既有字段 0 改 0 漏 0 漏 1 派生字段 0 漏 1 派生率 0 改 1 派生率 0 改 1 公式), 改 1 派生率 / 1 公式 / 漏 1 派生率 = 派生字段视觉组连贯 漂动, source-grep 验证 `_RECENT_ROW_HINT` 7 entry + `_build_recent_row_tooltip()` 7 字段 bullet + `_format_recent_row_text()` 7 placeholder + t_sec == 0 guard 0 漏 0 改 1 字段 0 改 1 字符 0 改 1 公式.
  8. 已知 drift risk: 1 panel × 3 组件 × 1:1 视觉组连贯 5 段 canonical 1:1 序列 **可能**漏 1 段 1:1 镜像 (e.g. 加新 1 段到 ProfileQuickStats 时漏 ProfileRecentList + ProfileAudit 同步 0 镜像, 3 组件 0 协调 跨组件视觉组连贯崩塌; 修 1 段颜色 0 改 1 hex 触发 3 组件颜色 0 协调 跨组件视觉组连贯崩塌; 改 _RECENT_ROW_FIELD_SEP 既有 4 字符 触发 3 组件中点分隔符 0 协调 跨组件视觉组连贯崩塌). 监控建议: 任何"未来给 1 panel × 3 组件 加新段 / 加新字段 / 加新组件 / 改颜色 / 改格式"必须**先**在 5 段序列 (Stage 1 3 组件 + Stage 2 中点分隔符 + Stage 3 7pt 字号 + Stage 4 _COLOR_* canonical 颜色 token + Stage 5 派生字段) 1:1 镜像 0 漏 1 段. T263 (#184) 5 段序列 1:1 镜像落地后任何新段 / 新字段 / 新组件 / 新颜色 / 新格式 加 5 段同步是 §9.6.27 5 段序列的"母集" (1 panel × 3 组件 × 1:1 5 段 = 15 元素 1:1 严格, §9.6.27 5 段 3 组件完整闭环, 3 组件 × 5 段 = 15 元素 1:1 严格, 含 3 组件 调色 9 颜色 token 0 漏 0 改 + 派生 2 字段 0 漏 0 改). 另 1 类已知 drift: Stage 4 _COLOR_* canonical 颜色 token 0 改 1 通道值 (e.g. 改 `_refresh_profile_audit()` 暖红 inline literal (0.85, 0.45, 0.4) 为 (0.80, 0.45, 0.4) 偏红 0 触碰既有 2 状态色, 或改 `_COLOR_RECENT_RUN_LATEST` Amber Voice (0.949, 0.714, 0.431, 1.0) 为 (0.90, 0.714, 0.431, 1.0) 偏淡橙 0 触碰既有 1 token), 后续 polish 期若加新组件必须**先**用 1:1 镜像 _COLOR_* canonical 颜色 token 模板, 0 改既有 9 颜色 token.

**0 副作用**: T162 / T213 / T214 / T215 / T216 / T217 / T219 / T225 / T226 / T229 / T231 / T234 / T235 / T240 / T249 任何 const / var 0 触碰 (3 组件 调色 0 改 0 删 0 重排 + 中点分隔符 0 改 0 删 0 重排 + 7pt 字号 0 改 0 删 0 重排 + _COLOR_* canonical 颜色 token 0 改 0 删 0 重排 + 派生 2 字段 0 改 0 删 0 重排); 0 触碰 §9.6.6 ProfileRecentList 5 行 row 文本 5→7 字段 format 字符串扩展模式 (T283 0 改 §9.6.6 任何 1 字符); 0 触碰 §9.6.7 ProfileRecentList 5 行 row hover feedback 三件套 (T283 0 改 §9.6.7 任何 1 字符); 0 触碰 §9.6.8 ProfileQuickStats 4 段独立 hover 联动 (T283 0 改 §9.6.8 任何 1 字符); 0 触碰 §9.6.9 AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` Dictionary 双源 (T283 0 改 §9.6.9 任何 1 字符); 0 触碰 §9.6.10 「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (T283 0 改 §9.6.10 任何 1 字符); 0 触碰 §9.6.15 SaveSystem audit_save_slots (T283 0 改 §9.6.15 任何 1 字符); 0 触碰 §9.6.16 6 verb hit handler 5 件套 (T283 0 改 §9.6.16 任何 1 字符); 0 触碰 §9.6.17 6 verb signal 桥接 5+1 件套 (T283 0 改 §9.6.17 任何 1 字符); 0 触碰 §9.6.18 16 件套 verb ability base (T283 0 改 §9.6.18 任何 1 字符); 0 触碰 §9.6.19 7 件套 windup VFX base (T283 0 改 §9.6.19 任何 1 字符); 0 触碰 §9.6.20 Hub ↔ archive 跨房间 transition flow (T283 0 改 §9.6.20 任何 1 字符); 0 触碰 §9.6.21 8 段成就序列 (T283 0 改 §9.6.21 任何 1 字符); 0 触碰 §9.6.22 6 verb VFX 视觉组 5 段 (T283 0 改 §9.6.22 任何 1 字符); 0 触碰 §9.6.23 7 桶 prewarm 5+1+1 (T283 0 改 §9.6.23 任何 1 字符); 0 触碰 §9.6.24 HUD 6 verb × 6 通道 = 36 通道 5 段序列 (T283 0 改 §9.6.24 任何 1 字符); 0 触碰 §9.6.25 6 verb 视觉组连贯 tooltip 8 行拼接 5 段序列 (T283 0 改 §9.6.25 任何 1 字符); 0 触碰 §9.6.26 6 verb icon 视觉组 5 段 canonical 1:1 序列 (T283 0 改 §9.6.26 任何 1 字符); 0 触碰 check_smoke_consistency.sh (T283 0 改任何规则); 0 触碰 _parse_recent_section.py + pre_commit_f002_check.sh + install_hooks.sh (T265 工具链 3 件套 0 触碰); 0 触碰 .git/hooks (hook 不入仓); 0 触碰游戏代码 (.gd / .tscn / 任何 gameplay code 0 改, 0 触碰 `pause_menu.gd` 3 组件 任何 1 字段, 0 触碰 `pause_menu.tscn` PlayerProfilePanel/ProfileMargin/ProfileVBox 3 组件任何 1 节点); 0 行为变化, 1 文档 1 段 (§9.6.27 ~50 行) + 1 smoke test (T283 24+ 断言).

**与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 关系** — §9.6.6 覆盖 ProfileRecentList 5 行 row 文本 5 字段 → 7 字段 format 字符串扩展模式 (单一组件 row 文本扩展); §9.6.7 覆盖 ProfileRecentList 5 行 row hover feedback 三件套 (单一组件 row hover feedback); §9.6.8 覆盖 ProfileQuickStats 4 段独立 hover 联动 (单一组件 4 段 hover); §9.6.9 覆盖 AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` Dictionary 双源 (2 面板跨面板 _alpha_base); §9.6.10 覆盖「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (2 层 hover 互补); §9.6.15 覆盖 SaveSystem audit_save_slots (1 个公共接口 polish); §9.6.16 覆盖 6 verb hit handler 5 件套 (6 verb hit handler); §9.6.17 覆盖 6 verb signal 桥接 5+1 件套 (6 verb signal 桥接); §9.6.18 覆盖 16 件套 verb ability base (6 verb ability base); §9.6.19 覆盖 7 件套 windup VFX base (6 verb windup VFX base); §9.6.20 覆盖 Hub ↔ archive 跨房间 transition flow (跨房间 transition); §9.6.21 覆盖 6 verb 成就系统 闭环 8 段 (6 verb 成就系统); §9.6.22 覆盖 6 verb VFX 视觉组 5 段 (6 verb VFX 视觉组); §9.6.23 覆盖 7 桶 prewarm 5+1+1 (7 桶 prewarm); §9.6.24 覆盖 HUD 6 verb × 6 通道 = 36 通道 5 段 (HUD 36 通道); §9.6.25 覆盖 6 verb 视觉组连贯 tooltip 8 行拼接 5 段 (6 verb 视觉组 tooltip); §9.6.26 覆盖 6 verb icon 视觉组 5 段 (6 verb icon 视觉组). §9.6.27 覆盖 `pause_menu.gd` PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 (5 段 canonical 1:1 序列: Stage 1 3 组件 + Stage 2 中点分隔符 + Stage 3 7pt 字号 + Stage 4 _COLOR_* canonical 颜色 token + Stage 5 派生字段). §9.6.6 关注 "ProfileRecentList 5 行 row 文本 5 字段 → 7 字段 format 字符串扩展模式 (单一组件 row 文本扩展, 5 字段 → 7 字段 1:1 严格)", §9.6.7 关注 "ProfileRecentList 5 行 row hover feedback 三件套 (font_color + alpha boost + tooltip, 1 组件 3 反馈 1:1 严格)", §9.6.8 关注 "ProfileQuickStats 4 段独立 hover 联动 (4 段 tween + dim restore, 1 组件 4 段 hover 1:1 严格)", §9.6.9 关注 "AchievementGrid + ProfileRecentList 跨面板 `_alpha_base` Dictionary 双源 (2 面板跨面板 _alpha_base 1:1 严格)", §9.6.10 关注 "「hover 静态高亮层 + tooltip 数据源层」双层 hover 互补 (2 层 hover 互补 1:1 严格)", §9.6.15 关注 "SaveSystem audit_save_slots (1 个公共接口 polish, audit 4 字段 1:1 严格)", §9.6.16 关注 "6 verb hit handler 5 件套 (6 verb hit handler 1:1 严格)", §9.6.17 关注 "6 verb signal 桥接 5+1 件套 (6 verb signal 桥接 1:1 严格)", §9.6.18 关注 "16 件套 verb ability base (6 verb ability base 1:1 严格)", §9.6.19 关注 "7 件套 windup VFX base (6 verb windup VFX base 1:1 严格)", §9.6.20 关注 "Hub ↔ archive 跨房间 transition flow (跨房间 transition 1:1 严格)", §9.6.21 关注 "6 verb 成就系统 闭环契约 (8 段 1:1 严格, 含 PNG 双路径)", §9.6.22 关注 "6 verb VFX 视觉组 5 段 1:1 严格分离契约", §9.6.23 关注 "7 桶 prewarm 5+1+1 (1:1 严格分离契约)", §9.6.24 关注 "HUD 6 verb × 6 通道 = 36 通道 5 段 1:1 严格分离契约", §9.6.25 关注 "6 verb 视觉组连贯 tooltip 8 行拼接 5 段 1:1 严格分离契约", §9.6.26 关注 "6 verb icon 视觉组 5 段 1:1 严格分离契约 (Stage 1 family 路径 + Stage 2 disc + outer ring + Stage 3 调色 + Stage 4 几何 + Stage 5 双 export + 3 verb achievement 双路径)". §9.6.27 是 17 套 polish 模式**唯一**关注 "PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 (5 段 canonical 1:1 序列: Stage 1 3 组件 + Stage 2 中点分隔符 + Stage 3 7pt 字号 + Stage 4 _COLOR_* canonical 颜色 token + Stage 5 派生字段)" 的 1 套, 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 16 套 ProfileRecentList 5 行 / ProfileQuickStats 4 段 / AchievementGrid / 6 verb / SaveSystem / 跨房间 transition 0 互混 0 复用 0 共享. 任何"加新 1 段"或"加新 1 字段"或"加新 1 组件"或"修 1 段颜色"或"修 1 字段格式"必须**先**按 §9.6.27 Stage 1 3 组件 1:1 加 1 组件 (Stage 1 1:1 严格), **再**按 §9.6.27 Stage 2 中点分隔符 1:1 复制 (Stage 2 1:1 严格), **再**按 §9.6.27 Stage 3 7pt 字号 1:1 镜像 (Stage 3 1:1 严格), **再**按 §9.6.27 Stage 4 _COLOR_* canonical 颜色 token 加 1 entry (Stage 4 1:1 严格), **再**按 §9.6.27 Stage 5 派生字段 1:1 同步 (Stage 5 1:1 严格). §9.6.6 / §9.6.7 与 §9.6.27 0 互混 0 复用 0 共享: §9.6.6 / §9.6.7 关注 "ProfileRecentList 5 行 row 文本 / hover 反馈 1:1 严格分离契约 (单一组件 row 文本扩展 / row hover feedback, 1 组件 1 panel 0 涉及 3 组件 1:1)" (与 3 组件 0 相关), §9.6.27 关注 "PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 (5 段 canonical 1:1 序列, 3 组件 1:1 严格分离)" (与 单一组件 0 相关, 5 段序列 1:1 严格 镜像 3 组件既有 1:1 严格分离). §9.6.8 与 §9.6.27 0 互混 0 复用 0 共享: §9.6.8 关注 "ProfileQuickStats 4 段独立 hover 联动 1:1 严格分离契约 (单一组件 4 段 hover, 1 组件 1 panel 0 涉及 3 组件 1:1)" (与 3 组件 0 相关), §9.6.27 关注 "1 panel × 3 组件 × 1:1 视觉组连贯 (3 组件 1:1 严格分离, 含 4 段 hover 但 1 段 hover 仅是 4 段中 1 段, Stage 1 3 组件 1:1 严格 0 漏 1 边)" (与 单一组件 hover 0 相关, 5 段序列 1:1 严格 镜像 3 组件既有 1:1 严格分离). §9.6.9 / §9.6.10 与 §9.6.27 0 互混 0 复用 0 共享: §9.6.9 / §9.6.10 关注 "AchievementGrid + ProfileRecentList 跨面板 / 双层 hover 互补 1:1 严格分离契约 (2 面板 / 2 层 hover, 1 组件 + 1 面板 / 1 组件 + 1 层 0 涉及 3 组件 1 panel 1:1)" (与 3 组件 1 panel 0 相关), §9.6.27 关注 "1 panel × 3 组件 × 1:1 视觉组连贯 (3 组件 1:1 严格分离, 1 panel 内 3 组件 0 漏 1 边)" (与 2 面板 / 2 层 hover 0 相关, 5 段序列 1:1 严格 镜像 3 组件既有 1:1 严格分离). §9.6.15 与 §9.6.27 0 互混 0 复用 0 共享: §9.6.15 关注 "SaveSystem audit_save_slots 1:1 严格分离契约 (1 个公共接口 polish, 1 接口 4 字段 1:1 严格)" (与 3 组件 0 相关, audit 4 字段仅是 §9.6.15 1 段 4 字段), §9.6.27 关注 "1 panel × 3 组件 × 1:1 视觉组连贯 (3 组件 1:1 严格分离, 含 ProfileAudit 4 字段但 4 字段仅是 3 组件中 1 组件, Stage 1 3 组件 1:1 严格 0 漏 1 边)" (与 1 接口 0 相关, 5 段序列 1:1 严格 镜像 3 组件既有 1:1 严格分离). §9.6.16-§9.6.26 与 §9.6.27 0 互混 0 复用 0 共享: §9.6.16-§9.6.26 关注 "6 verb 视觉 / 调色 / 几何 / 共享契约 / 跨段 1:1 严格分离契约 (6 verb 视觉组连贯, 6 verb 0 涉及 PlayerProfilePanel 3 组件)" (与 1 panel × 3 组件 0 相关, 6 verb 0 触碰 ProfileQuickStats 4 段 + ProfileRecentList 5 行 + ProfileAudit 4 字段), §9.6.27 关注 "1 panel × 3 组件 × 1:1 视觉组连贯 (3 组件 1:1 严格分离, 1 panel 内 3 组件 0 漏 1 边)" (与 6 verb 0 相关, 5 段序列 1:1 严格 镜像 3 组件既有 1:1 严格分离). §9.6.27 是 17 套 polish 模式**唯一**关注 "PlayerProfilePanel 1 panel × 3 组件 × 1:1 视觉组连贯 (5 段 canonical 1:1 序列: Stage 1 3 组件 + Stage 2 中点分隔符 + Stage 3 7pt 字号 + Stage 4 _COLOR_* canonical 颜色 token + Stage 5 派生字段)" 的 1 套, 与 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 / §9.6.16 / §9.6.17 / §9.6.18 / §9.6.19 / §9.6.20 / §9.6.21 / §9.6.22 / §9.6.23 / §9.6.24 / §9.6.25 / §9.6.26 16 套 ProfileRecentList 5 行 / ProfileQuickStats 4 段 / AchievementGrid / 6 verb / SaveSystem / 跨房间 transition 0 互混 0 复用 0 共享.

**与 §9.1 9 步关系** — §9.1 9 步 = `verb` (1) + `ability` (2) + `const` (3) + `input` (4) + `shop` (5) + `icon` (6) + `vfx` (7) + `audio` (8) + `HUD 冷光勾边` (9). §9.6.27 5 段序列走 §9.1 9 步落地的 "const" (3) 1 步 (`pause_menu.gd` `_QUICK_STATS_HINT` const 4 entry + `_RECENT_ROW_HINT` const 7 entry + `_RECENT_ROW_FIELD_SEP` const "  ·  " 4 字符中点分隔符 + `_RECENT_ROW_TIP_INDICATOR` const " ↗" 2 字符 + `_RECENT_ROW_FONT_COLOR_FADE_DURATION` const 0.12s + `_RECENT_ROW_ALPHA_MAX` / `_RECENT_ROW_ALPHA_MIN` const 1.0 / 0.5 + `_COLOR_*` canonical 颜色 token 2 token + `_COLOR_ZERO_STAT` 1 token + `_QUICK_STATS_HINT` 4 entry color 字段 + `_refresh_profile_audit()` 3 状态色 inline literals = 2+1+4+3 = 10 颜色 reference), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) + "input" (4) + "shop" (5) + "icon" (6) + "vfx" (7) + "audio" (8) + "HUD 冷光勾边" (9) 8 步 (1 panel × 3 组件 × 1:1 视觉组连贯 0 走 verb enum + ability 子类 + input map + shop 系统 + icon PNG + VFX 视觉组 + audio 副作用 + HUD 渲染). 任何"加新 1 段"或"加新 1 字段"或"加新 1 组件"必须**先**按 §9.1 9 步落地 (含 "const" (3) 步), **再**按 §9.6.27 5 段序列 1:1 复制 (0 反序 0 漏 0 改 1 字段). 任何"修 1 段颜色"必须**先**在 `pause_menu.gd` `_COLOR_*` canonical 颜色 token 改 1 通道值 (Stage 4 1:1 严格, 0 漏 1 边 0 改 1 通道值 0 改 1 hex), **再**在 `_QUICK_STATS_HINT` 4 entry color 字段 1:1 镜像 (Stage 4 1:1 严格, 0 漏 1 通道 0 改 1 通道值), 0 触碰 §9.1 9 步任何 "verb" (1) + "ability" (2) 步 (1 panel × 3 组件 × 1:1 视觉组连贯 0 触碰 verb enum + ability 子类). 任何"加新 1 组件"必须**先**在 `pause_menu.tscn` PlayerProfilePanel/ProfileMargin/ProfileVBox 加 1 节点 (Stage 1 1:1 严格), **再**在 `pause_menu.gd` 4 + 35 + 4 = 43 element 加 1 element (Stage 1 1:1 严格, 0 漏 1 element 0 改 1 字段), 0 触碰 §9.1 9 步任何 "icon" (6) 步既有 6 verb icon PNG 双路径. 任何"加新 1 派生字段"必须**先**在 `_RECENT_ROW_HINT` 7 entry → 8 entry 加 1 entry (Stage 5 1:1 严格), **再**在 `_build_recent_row_tooltip()` 7 字段 bullet → 8 字段 bullet 加 1 bullet (Stage 5 1:1 严格), **再**在 `_format_recent_row_text()` 7 placeholder → 8 placeholder 加 1 placeholder (Stage 5 1:1 严格), 0 触碰 §9.6.6 / §9.6.7 既有 5 字段 → 7 字段既有 0 改 0 漏 1 字段. 任何"加新 1 跨段镜像"必须**先**在 §9.6.6 / §9.6.7 / §9.6.8 / §9.6.9 / §9.6.10 / §9.6.15 6 跨段加 1 镜像 (5 段序列 1:1 严格, 0 漏 1 跨段 0 改 1 字段), 0 触碰 §9.1 9 步任何 const / var / 节点 (5 段序列仅在 `pause_menu.gd` `_QUICK_STATS_HINT` / `_RECENT_ROW_HINT` / `_RECENT_ROW_FIELD_SEP` / `_RECENT_ROW_TIP_INDICATOR` / `_RECENT_ROW_FONT_COLOR_FADE_DURATION` / `_RECENT_ROW_ALPHA_MAX` / `_RECENT_ROW_ALPHA_MIN` / `_COLOR_*` canonical 颜色 token + `pause_menu.tscn` 3 组件 节点, 5 verb lazy-init 守卫 0 删 0 改, 含 1 verb Whisper T245 #162 接入完成).

---

## 11. F002 self-test commit hook 集成 (T265 落地, 防止 commit 漏更新 README 类问题回归)

> T265 (#186) 把 F002 self-test (设计本意: "prove the hook would block if README missing #N-1 entry") 从「审查阶段检测」提前到「commit 阶段检测」, 防止 #183 commit 时漏更新 README.md + README.zh-CN.md 「Recent completed work」/「最近完成的工作」段类问题再次出现. 落地后任何 "commit 漏更新 README" 类问题在 commit 阶段即被捕获, 0 漂到审查阶段. 涉及 3 件套 (Python 解析器 + pre-commit 检查脚本 + hook installer).

### 11.1 设计本意 (T265 落地动机)

T265 (#186) 的核心动机是 **F002 self-test 设计本意是 "在 commit 阶段阻断", 但落地时只在审查阶段检测** —— #80 (#80 F002 self-test 设计本意: "prove the hook would block if README missing #N-1 entry") 设计时把 F002 写成"hook", 实际 #80 落地时只在审查阶段检测, 不是真正的 commit hook. #185 审查时检测出 #183 commit 时漏更新 README, #185 FIX-#185-1 修复了 #183 段, 但 F002 self-test 仍未集成到 commit 阶段, "commit 漏更新 README" 类 regression vector 仍存在. T265 (#186) 把 F002.7 + F002.8 校验提前到 commit 阶段: commit 前自动跑测 README 同步状态, 缺失 → 阻断 commit (exit 1). 落地后任何 "commit 漏更新 README" 类问题在 commit 阶段即被捕获, 0 漂到审查阶段. 设计原则: (1) 0 触碰游戏代码 (T265 仅 0 触碰 .gd / .tscn / 任何 gameplay code, 仅新加 3 个工具链文件: tools/pre_commit_f002_check.sh + tools/install_hooks.sh + tools/_parse_recent_section.py); (2) 0 强制 (hook 是 .git/hooks/pre-commit 不入仓, 开发者可拒绝安装, 拒绝安装时仅失去 "commit 阶段检测" 能力, 审查时仍检测); (3) 0 覆盖用户自定义 hook (install_hooks.sh 用 F002_MARKER 检测是否已安装, 已安装则跳过; 用户有自定义 pre-commit hook 时 append 而非 overwrite); (4) 0 漂动 check_smoke_consistency.sh (T265 复用 Rule 7 的解析逻辑但用 Python 替代 awk, 0 改 Rule 7 既有逻辑, 仅新增独立工具链); (5) 0 触碰 §10 决策记录流程 (F002 self-test 是 1 个独立工具链, 与 §10 联系方式 / 决策记录流程 0 互混).

### 11.2 工具链 3 件套 (T265 落地产物)

T265 (#186) 落地 3 件套工具链, 任何 polish 期给工具链加新 1 文件 (e.g. F003 self-test / F004 lint / F005 格式化 / F006 静态分析) 时**必须**严格按 3 件套 1:1 复制既有模式:

1. **Python 解析器** (`tools/_parse_recent_section.py`, 落地 T265) —— F002.7 + F002.8 解析逻辑的 Python 实现, 替代 awk (awk 解析在 `### Recent completed work` 后紧跟 `## #N` 段时 flag 重置导致空输出). 解析规则: (1) section 起始: `^#{2,3}\s+(Recent completed work|最近完成的工作)\s*$`; (2) section 包含 `## #N` 标题行本身 (每个 `## #N` 标题是 1 个 #N entry); (3) section 结束: 下一个非 `## #N` 的 `## ` 标题, 或下一个 `### ` 标题 (sub-section); (4) section 内的文本叙述中的 #N 引用全部忽略 (避免 "下一轮（#186, 186%5==1 普通模式）" 类 prose 提到未来 #N 值时的 false positive). 任何"加新 1 README 段" (e.g. T262 启用的 "Decisions" 段 / T260 启用的 "Open Items" 段) 时**必须**在 `_parse_recent_section.py` 加 1 个新段名 + 在 `pre_commit_f002_check.sh` 加 1 行 `for rf in "新段名" ...` 同步 (0 漏 1 边).
2. **pre-commit 检查脚本** (`tools/pre_commit_f002_check.sh`, 落地 T265) —— F002.7 + F002.8 校验脚本, commit 前自动跑测 README + README.zh-CN.md 同步状态. 流程: (1) `set -uo pipefail` 1 行; (2) 解析脚本路径 + repo root + 4 个变量 (README_FILE / README_ZH / ITER_COUNT_FILE / PARSER) 4 行; (3) sanity check 3 件套 (ITER_COUNT_FILE 存在 + ITER_COUNT 非空 + PARSER 存在 + python3 可用) 4 块; (4) 推导 PREV_ITER = ITER_COUNT - 1 (最少 1) 2 行; (5) `for rf in "$README_FILE" "$README_ZH"` 循环 9 行 (parse + diff + FAIL/PASS 3 分支); (6) 退出码 0/1/2 (PASS / FAIL / warning). 任何"加新 1 检查" (e.g. CHANGELOG 同步 / ROADMAP 顶部时间戳 / ITERATION_COUNT.txt 同步) 时**必须**在循环内加 1 个新分支 (0 漏 0 改 1 字符).
3. **hook installer** (`tools/install_hooks.sh`, 落地 T265) —— 复制 `tools/pre_commit_f002_check.sh` 到 `.git/hooks/pre-commit` 的一次性安装脚本. 流程: (1) 解析脚本路径 + repo root + 4 个变量 (HOOKS_DIR / PRE_COMMIT_HOOK / SOURCE_SCRIPT / F002_MARKER) 4 行; (2) sanity check 2 件套 (.git/hooks 存在 + SOURCE_SCRIPT 存在) 2 块; (3) `--uninstall` 模式单独处理 (4 个分支: 0 hook / 0 F002 hook / 纯 F002 hook / 用户自定义 hook); (4) install 模式 3 个分支 (无 hook → 创建; 有 F002 hook → 跳过; 有用户自定义 hook → append F002_MARKER + 调用); (5) chmod +x 0 漏. 任何"加新 1 hook 类型" (e.g. pre-push F002 check / commit-msg lint / post-merge F002 sync) 时**必须**在 installer 加 1 个新分支 (0 漏 0 改 1 字符).

### 11.3 使用流程 (开发者本地一次性安装)

```bash
# 1. 进入 repo root
cd /workspace

# 2. 安装 pre-commit hook (一次性, 不入仓, 仅本地)
bash tools/install_hooks.sh

# 3. 测试 (允许空 commit, 走完 hook 流程)
git commit --allow-empty -m "test F002 hook"

# 4. 卸载 (如需)
bash tools/install_hooks.sh --uninstall
```

任何"开发者拒绝安装 hook"的情况: 0 强制裁, 0 触碰游戏代码, 0 触碰 CI 流程, 仅失去"commit 阶段检测"能力, 审查阶段仍会检测 (F002 self-test 仍是 `tools/check_smoke_consistency.sh` Rule 7 + `tools/test_t158_t156_f002_smoke.gd` F002.7/F002.8). 落地后 0 漂动: T265 0 触碰 Rule 7 既有 awk 解析逻辑 (仅新加 Python 解析器作为独立工具链, 两者并存), 0 触碰 `test_t158_t156_f002_smoke.gd` F002.7/F002.8 (F002 self-test 仍是审查时跑测).

### 11.4 T265 落地边界 (0 触碰清单)

- 0 触碰游戏代码 (T265 0 改任何 .gd / .tscn / 任何 gameplay code, 仅新加 3 个工具链文件)
- 0 触碰 .git/hooks (hook 是不入仓的, install_hooks.sh 仅是 installer, 不入仓)
- 0 触碰 check_smoke_consistency.sh Rule 7 (T265 0 改 Rule 7 既有 awk 解析逻辑, 仅新加 Python 解析器作为独立工具链)
- 0 触碰 test_t158_t156_f002_smoke.gd F002.7/F002.8 (F002 self-test 仍是审查时跑测, T265 0 改 F002 self-test 既有逻辑)
- 0 触碰 §9.6 / §9.7 任何 fragility 章节 (T265 是工具链任务, §9.6 / §9.7 是 game code fragility, 0 互混)
- 0 触碰 §10 决策记录流程 (F002 self-test 是 1 个独立工具链, 与 §10 联系方式 / 决策记录流程 0 互混)
- 0 触碰 README.md / README.zh-CN.md 内容 (T265 仅校验 README 同步状态, 0 改任何 README 内容)
- 0 触碰 ITERATION_COUNT.txt / CHANGELOG.md / ROADMAP.md 内容 (T265 仅校验 CHANGELOG + ROADMAP 同步状态, 0 改任何内容)

### 11.5 预防 (避免后续 polish 漂动)

1. 任何 polish 期给工具链加新 1 文件 (e.g. F003 self-test / F004 lint / F005 格式化 / F006 静态分析) 时**必须**严格按 3 件套 1:1 复制既有 T265 模式: (1) Python 解析器 (或独立可调用逻辑) 1 个新文件 + (2) pre-commit 检查脚本 (或 commit-time 检查) 1 个新文件 + (3) hook installer (或新工具链安装器) 1 个新文件. 0 漏 1 件 = 该工具链的 commit-time 检测能力 0 闭环, 漂到审查阶段才检测 (与 T265 落地前 F002 self-test 同模式). source-grep 验证 `tools/` 目录下 3 件套 0 漏 0 改 1 字符.
2. `tools/_parse_recent_section.py` 0 改解析逻辑 (T265 落地时的 4 条规则 0 改 0 删 0 重排) —— 任何"加新 1 README 段" (e.g. T262 启用的 "Decisions" 段 / T260 启用的 "Open Items" 段) 时**必须**在 `_parse_recent_section.py` 加 1 个新段名 + 在 `pre_commit_f002_check.sh` 加 1 行 `for rf in "新段名" ...` 同步, source-grep 验证 0 漏 1 边.
3. `tools/pre_commit_f002_check.sh` 0 改 sanity check 3 件套 (T265 落地时的 ITER_COUNT_FILE 存在 + ITER_COUNT 非空 + PARSER 存在 + python3 可用 4 块 0 改 0 删 0 重排) —— 任何"加新 1 sanity check"时**必须**严格按既有 4 块 1:1 复制 (sanity check 失败 → exit 1), source-grep 验证 sanity check 4 块 0 漏 0 改 1 字符.
4. `tools/install_hooks.sh` 0 改 install / uninstall 2 模式 0 触碰既有逻辑 (T265 落地时的 4 个分支: 0 hook / 0 F002 hook / 纯 F002 hook / 用户自定义 hook 0 改 0 删 0 重排) —— 任何"加新 1 hook 类型"时**必须**严格按既有 4 分支 1:1 复制 (F002_MARKER 检测 + chmod +x 0 漏), source-grep 验证 install / uninstall 2 模式 0 漏 0 改 1 字符.
5. F002_MARKER 0 改 (T265 落地时显式用 `# T265 F002 self-test commit hook (do not remove — see tools/pre_commit_f002_check.sh)` marker 区分 F265 落地的 hook 和开发者自定义 hook) —— 任何"改 marker 字符串"polish 都会让 `install_hooks.sh` 检测失败 (重复安装时 0 跳过, 误判为新安装), source-grep 验证 F002_MARKER 字符串 0 漏 0 改 1 字符.
6. 0 强制安装 (T265 落地时显式 0 强制开发者安装 hook, 0 触碰 `git config core.hooksPath`, 0 触碰 CI / 审查流程) —— 任何"强制安装"polish 都会让开发者拒绝入仓, 反而失去 commit-time 检测能力, source-grep 验证 `tools/install_hooks.sh` 0 触碰 `core.hooksPath` 0 触碰 CI 流程 0 触碰审查流程.

---


> 感谢你愿意为 Voxglass 添砖加瓦。任何「让下一个开发者少花 10 分钟」的文档 / 冒烟测试 / 注释改进都是受欢迎的贡献。
