# Voxglass（声匣修复者）

一款 2D 像素美术的动作探索游戏。在被淹没的地下声档案馆中，修复被「活体寂静」夺走的人类声音。

## 状态

进行中的可玩竖切。当前里程碑：可玩 60 秒房间演示「进入 → Pulse 击退 → 修复 → 收集 → 出门」核心循环。

## 技术栈

- 引擎：Godot 4.6.3（已验证 — `config/features=4.4` 保留以兼容旧版，依 `REVIEW_LOG.md` #20 在 4.6.3 上解析干净）
- 分辨率：480x270 内部画布，整数倍缩放至 1920x1080
- 语言：GDScript
- 音频：程序化 SFX（pulse / 脚步 / 玻璃碎裂 / 敌人低鸣 / 修复 / 受击）+ **9 个程序化 BGM 主题**（`title_intro` 序章 / `hub_warm` 安全区 / `archive_exploration` 探索 / `archive_boss` 单 InkWarden Boss / `archive_boss_dual` 双 Boss 房 `archive_04` / `archive_dawn` 胜利与回归 / `archive_storm` tier-3 Boss 阶段 2 升级 — InkWarden 半血跃迁自动切换 / `whisper_hollow` 后期 Hub — 完成 2 间档案房后自动切换，见下方「BGM 9 主题色板」节 / `silence_void` 失败状态 + 终曲阶段 1，见「游戏状态机」节）— 全部在运行时通过 `src/scripts/audio_manager_enhanced.gd` 的 `AudioStreamWAV` 合成（无需外部音频文件），9 主题数据表在 `src/scripts/audio_presets.gd`。标题屏预热 BGM 缓存让首次场景切换零延迟。设置菜单提供 Master / Music / SFX / Ambience 四 bus 独立音量滑块。Boss 音乐 override 采用引用计数（T078），并支持强度分级 tier 升级（T080 / #59 T107）。
- **死亡与重生**：1.5s 倒下 + 渐隐死亡动画（T075）。开头 0.15s 慢动作 + 红洗定格（T092 — `Engine.time_scale = 0.2`，`modulate` 偏移到 `Color(1.4, 0.45, 0.45)`，让 alpha 衰减读作"流失的红"），随后身体倒下。死亡后默认传送回 Hub 安全区（T079）— 在 `设置 → 存档` 中关闭「死亡后回 Hub 安全区」开关可切回经典「在最近存档灯笼重生」模式。
- **档案房二阶段灯光**（M12 打磨，T076）：当房间的 `voice_bell` 修复完成时，场景 modulate 在 0.8s 内从冷墨青色缓出至暖琥珀（阶段 1），房间完成时再 2s 渐入完整暖色覆盖（阶段 2）。4 个档案房都在 `data/rooms/archive_*.json` 中 opt-in `"atmosphere": true`。
- 本地 Godot 二进制：`godot/Godot_v4.6.3-stable_linux.x86_64`（首次解压 + `--import` 配方见 `godot/README.md` 和下方「Headless Godot 二进制设置」节）

## 项目结构

```
assets/        # 美术、音频和设计参考素材
src/           # 源代码
  autoload/    # GameState、AudioManager 等单例
  scenes/      # Godot 场景文件（.tscn）
  scripts/     # GDScript 逻辑
docs/          # 设计文档（Steam 页面等）
scripts/       # Python 素材管线工具
data/          # 存档 / 房间 / 成就 JSON 数据
```

## 按键

| 动作 | 键盘 | 手柄 |
|--------|----------|---------|
| 移动 | A/D 或方向键 | 左摇杆 |
| 跳跃 | 空格 或 W | A 键 |
| Pulse（推/破盾） | J 或 Z | X 键 |
| Bind（牵引/暂停/开能力门） | K 或 X | Y 键 |
| Cut（斩/切断） | L 或 C | LB 键 |
| Echo（护盾反弹） | Q | 三角键 |
| 交互 | E 或 Enter | B 键 |
| 暂停 / 菜单 | ESC | Start 键 |
| 存档（自动 + 手动） | 走到存档灯笼 / 暂停 → 保存进度 | — |
| 读档继续 | 标题屏 → 继续修复（有存档时显示） | — |
| 致谢 | 标题屏 → 致谢按钮 | — |

## 截图

6 张营销截图位于 `docs/screenshots/`（1920x1080 PNG，480x270 内部画布 4x 整数倍缩放）：

1. `01_title_screen.png` — 标题屏（VOXGLASS + 4 按钮）
2. `02_hub_room.png` — Hub 安全区 + 4 扇门 + 墨守者剪影
3. `03_archive_01_pulse.png` — 第一档案房 + Saya + SilenceMote + Pulse 圆环
4. `04_archive_03_boss.png` — 第三档案房 + InkWarden Boss
5. `05_archive_04_double_boss.png` — 第四档案房「共鸣祭坛」+ 双 InkWarden
6. `06_shop_merchant.png` — 无声商贩 + 商店 UI + 5 个永久升级

> **沙箱说明**：本仓库 CI 沙箱无 Xvfb / Wayland / GL 上下文，Godot 4.6.3 headless 模式强制使用 dummy 渲染器，真实 `Viewport.get_texture().get_image()` 返回 null。**#43 用 `tools/generate_screenshot_mockups.py` 基于既有资产合成 6 张截图**作为 M10 营销上线最后阻塞解除。真实 capture 工具（`tools/screenshot_capture.gd` + `tools/capture_screenshots_desktop.sh`）在桌面环境（带 Xvfb / X11 / 真机）可直接使用。详见 `tools/README.md`。

### 5 verb windup ramp-in / ramp-out 双闭环（#92-#94）

5 个声波能力（Pulse / Bind / Echo / Cut / Wave）的 windup VFX 共享统一进出曲线（**#93 T174 + #92 T173 + #94 T174.B base class** 联合实现）：

- **ramp-in**（0.04~0.10s）：`modulate.a` 由 0.0 → 1.0，`Tween.TRANS_QUAD` + `EASE_OUT`（front-load 可见性）；5 份 `trigger()` 实现 byte-identical（base 抽取后 5 verb 全部 `extends VerbWindupVFXBase` 调 `_activate_windup_tween()`）。
- **ramp-out**（0.05s）：`fade_out_and_free()`，modulate.a 1.0→0.0 + queue_free；5 份实现 byte-identical；与 player 死亡 / 场景切换的 `_exit_tree()` 钩子协同（Wave 已在 #92 T173.C 补漏）。
- **fire 帧切换**：0 overlap（fire VFX 接管 windup VFX 同帧，无 1-frame 双环可见 bug）。

视觉节奏统一让 0.04s Cut（短）和 0.10s Pulse/Bind/Wave（长）有相同「ramp-up then settle」节奏，玩家学到一次 ramp-in 曲线就 apply 全 5 verb。Windup motif 五元组各 verb 独立：Pulse 收缩 / Bind 螺旋 / Echo 撑开 / Cut 横扫 / Wave 涟漪 —— `src/scripts/{pulse,bind,echo,cut,wave}_windup_vfx.gd` 5 文件加 `src/scripts/_verb_windup_vfx_base.gd` 父类。

**5 verb 音频闭环现状**（#94 F004 + #96 F004.B + #97 T181 + #100 T181.B + #102 F013 + #103 F014 + F015）：5 verb fire caller (T181 first half) + 5 verb fire SFX (F004 + F004.B) + 5 verb hit SFX (T141 #78 wave_combo + T181 first half 4 verb) + 5 verb cooldown jingle (T181 first half) + 5 verb hit perk-level scaling (T181.B 4 verb: Pulse / Cut / Echo / Wave, Bind 显式 NOT scaled) + shop perk-card SFX (F013 purchase_confirm + level_up arpeggio) + 14 成就 unlock chime (F014 C6/E6/A6 高音奖章) + 存档删除 click (F015 150Hz 方波低音) + prewarm 4 桶聚合器 (T184 + T185.B music/hit/shop/misc) 全部就位。剩余候选：5 verb cooldown jingle tail 短琶音 / 升档 I→II unlock 暖色光晕 / 切割连击 ≥3 PERK_LEVEL_UP 屏抖复用 / Death lay-down SFX。

## 存档系统

5 个存档槽位持久化到 `user://saves/slot_N.json`（#55 T088 从 3 槽扩展）。每个槽位保存：

- `current_room` + `current_scene`（房间 id + .tscn 路径，让「继续修复」能重新加载到正确场景）
- `health` / `resonance` / `shards` 与 `rooms_completed` 集合
- 已解锁的 `abilities`（bind / cut / echo）与玩家最后 `checkpoint_position`
- `run_time_seconds`（游戏内计时器）
- 截至当前已解锁的全部 `achievements`（同时在每次解锁时即时写入 `user://achievements.json`，独立于槽位）

标题屏仅在至少有一个存档时显示「继续修复」按钮。暂停菜单的「保存进度」按钮打开同样的存档槽位选择器（保存模式）。成就解锁的瞬间立即落盘到磁盘。

## 音频控制

`设置 → 音频` 菜单提供三个独立音量滑块，每个绑定到独立的 Godot AudioServer bus：

| 滑块 | Bus | 内容 |
|--------|-----|----------|
| Master | `Master` | 全部（混音后的 BGM + SFX + ambience） |
| Music | `Music` | 程序化 BGM（`title_intro` / `hub_warm` / `archive_exploration` / `archive_boss` / `archive_boss_dual` / `archive_dawn` / `archive_storm` / `whisper_hollow` + 沉默槽 `silence_void`） |
| SFX | `SFX` | Pulse / Bind / Cut / 脚步 / 玻璃碎裂 / 受击 / 修复 |
| Ambience | `Ambience` | 水流 / 风声 / 房间氛围低鸣 |

设置通过 `user://settings.cfg` 跨次运行持久化。

## BGM 9 主题色板

9 个程序化 BGM 主题在大小调上刻意分布，让每间房 / 每个事件听起来都是不同「调性房间」。下方表格中的 key 是 MIDI 编号（A4 = 69）；chord 列是叠加在根音 sine 之上的 3-4 音叠层。完整数据表 + per-preset 设计注释见 [`src/scripts/audio_presets.gd`](./src/scripts/audio_presets.gd)。

| Key | 调性 | Root | Chord（MIDI） | BPM | 循环 | 触发时机 |
|-----|------|------|---------------|-----|------|----------|
| `title_intro` | 稀疏 / 希望 | D3 (50) | D 大 — D4 F#4 A4 | 60 | 16.0s | TITLE 状态（游戏启动） |
| `hub_warm` | 温暖 / 明朗 | F2 (41) | F 大 — F3 A3 C4 | 88 | 10.9s | 早期 Hub（`rooms_completed.size() < 2`） |
| `archive_exploration` | 忧郁 / 深处 | A2 (45) | A 小 — A3 C4 E4 | 72 | 13.3s | 档案房（PLAYING + RoomController） |
| `archive_boss` | 紧张 / 单 Boss | A1 (33) | A 小 + 三全音 — A2 C3 F#3 | 108 | 11.1s | 第一只 InkWarden 入场（tier 1） |
| `archive_boss_dual` | 狂乱 / 双 Boss | A1 (33) | A 小 + 三全音 + 增5 — A2 C3 F#3 G#3 | 132 | 8.7s | 第二只 InkWarden 入场（tier 2） |
| `archive_dawn` | 胜利 / 晨光 | G2 (43) | G 大 — G3 B3 D4 | 76 | 12.6s | GAME_OVER_SUCCESS 终曲阶段 2 + `full_archive` 解锁 |
| `archive_storm` | 混沌 / 阶段 2 | E1 (28) | E 小 + 增4 + 升7 — E2 G#2 B2 D3 | 120 | 10.0s | InkWarden 进入阶段 2（tier 3） |
| `whisper_hollow` | 深度静默 / 小7和弦 | D3 (50) | D 小 7 — F3 A3 C4 E4 | 50 | 16.0s | 后期 Hub（`rooms_completed.size() >= 2`，#64 T123） |
| `silence_void` | 空无 / 缺席 | （无音频） | — | 60 | 4.0s | GAME_OVER_FAILURE + 终曲阶段 1 |

### 调性分布哲学

- **大调**（`title_intro` D / `hub_warm` F / `archive_dawn` G）读作「世界完好 / 有希望 / 明朗」。Hub 默认 F 大调（最亮），`archive_dawn` 是唯一 G 大调（较 hub_warm 上行五度 = 世界在胜利时「上行一步」解决）。
- **小调**（`archive_exploration` A / `archive_boss` A / `archive_boss_dual` A / `archive_storm` E / `whisper_hollow` D）读作「档案被侵蚀 / 忧郁 / 危险」。其中 3 个共用 A 小调以便 Boss 战中 crossfade 是和声过渡；`archive_storm` 切到 E 小调是和声对比（chaos，不只是 intensity 升级），`whisper_hollow` 切到 D 小调是「距离感」（深度静默，区别于紧迫的探索主题）。
- **不和谐递进**：纯净三和弦 → +三全音 → +三全音 +增5 → +增4 +升7。每个 tier 1/2/3 Boss 主题加一个不和谐音程，Boss 战音乐把「升级」听成和声压力，不是单纯的音量。
- **沉默作为一个主题**：`silence_void` 是唯一一个「全振幅为零」preset — 它**不是** BGM，是 BGM 的**有意缺席**。它桥接失败状态（4 秒空无配合冷灰视觉洗）和终曲阶段 1（4 秒「世界清空」后由 `archive_dawn` 解决）。
- **过场 ambient 垫底**（T122）：不是 BGM preset，是按需生成 8 秒 D2 + G2 双 sine drone 走 Ambience bus，由 `AudioManagerEnhanced.play_intro_ambience()` 现场生成、`intro_cutscene.gd._play_sequence()` 触发。它活在即将到来的 `title_intro` BGM 之下，让过场永远不出现硬沉默。

## 游戏状态机

`GameFlowController` 是一个小型状态机，6 个状态。每个状态转换都会把一个 BGM `play_music_track` / `play_music_finale` 调用路由到 `AudioManagerEnhanced`；可视化版本见 [BGM 状态机映射图](./assets/voxglass-bgm-state-map.png)，下方是文字版表格。

| 状态 | 触发 | BGM | 音频 API | 备注 |
|------|------|-----|----------|------|
| `TITLE`（标题） | 游戏启动 / 继续按钮 | `title_intro` | `play_music_track("title_intro", 1200)` | 16 秒 D 大调希望 pad；菜单停留期间循环。 |
| `PLAYING`（游戏中） | 新游戏 / 暂停恢复 / 场景转换完成 | `hub_warm` *或* `archive_exploration` *或* `whisper_hollow` | `play_music_track(scene.bgm_key, 800)` | 确切键名取自场景的 `bgm_key`（Hub 取 `hub_warm`）。Boss 房间额外调 `request_boss_music` — 见下方 BOSS 覆盖。 |
| `PAUSED`（暂停） | `pause_requested` 信号（Esc / P） | _保持_ | _无_ | 暂停时 BGM 继续播放；暂停菜单只静音 SFX。time_scale 在恢复时回到 1.0。 |
| `ROOM_TRANSITION`（过场） | 触发房门 | _保持_ | _无_ | 黑屏淡出 0.4s；下一场景 `_ready` 拿到 `bgm_key` 后再调一次 `play_music_track`。 |
| `GAME_OVER_SUCCESS`（胜利） | 玩家通关最终房间 | `silence_void` → `archive_dawn`（4.0s + 12.6s） | `play_music_finale()`（T117） | 两阶段终曲。阶段 1 沉默 = "世界消亡"；阶段 2 dawn = "世界重新呼吸"。`play_music_finale` 内部的 `_current_music_key` 守卫会让阶段 2 尊重玩家"返回 Hub"的中断。 |
| `GAME_OVER_FAILURE`（失败） | 玩家 HP ≤ 0 | `silence_void` | `play_music_track("silence_void", 1200)` | 4 秒零振幅循环，与 T093 冷灰视觉洗同步。配合 T115 死亡碑文叠加 + T116 InkWarden 残影播放。 |

### BGM Boss 覆盖（与状态正交）

InkWarden（或 `elite_enemies` 组中任意敌人）入场时调 `request_boss_music`。该覆盖是**引用计数**的、**tier 排序**的，因此多 Boss 房间第一只死亡不会丢 BGM，第二阶段升级会自动顶替第一阶段：

| Boss 事件 | 覆盖键 | Tier（相对当前） | 效果 |
|-----------|--------|------------------|------|
| 单只 InkWarden 入场 | `archive_boss`（A 小调 108 BPM） | 1 | 强制 `play_music_track("archive_boss")`，无视 `PLAYING` 状态路由。 |
| 同一房间出现第二只 InkWarden | `archive_boss_dual`（A 小调 132 BPM） | 2 > 1 | 战斗中交叉淡入升级。 |
| InkWarden 进入阶段 2 | `archive_storm`（E 小调 120 BPM） | 3 > 1, 2 | 最强烈预设；持续混乱。 |
| 最后一只 Boss 死亡 / 离开 | _清除_ | 0 | 回到场景的 `bgm_key`（`archive_exploration`）。 |

Boss 音乐与终曲音乐正交：若玩家在 Boss 战中死亡，`GAME_OVER_FAILURE` 从 `PLAYING+boss_override` 状态到达。GFC 路由到 `silence_void` **之前** `release_boss_music()` 已经清除覆盖，因此失败路径干净。

## 死亡与重生序列

- **0.15s freeze-frame**（T092）：`Engine.time_scale = 0.2` + `sprite.modulate = Color(1.4, 0.45, 0.45)` 慢动作 + 红洗定格
- **0.3s 灰阶洗**（T093）：冷灰 `Color(0.32, 0.34, 0.40)` sine tween 双段
- **0.5s lay-down**：rotation → PI/2 quad-ease-in 身体倒下
- **1.0s fade-out**：alpha 1→0 linear 保持红调（"drained red" 而非 flashing red）
- 触发 `_finish_death` → 默认回 Hub 安全区（T079 开关可切回最近存档灯笼）

## 商店系统

Hub `silent_merchant` NPC（#41 T068）提供 5 个永久升级，跨 run 持久化到 `purchased_perks`：

| ID | 效果 | 描述 |
|-----|------|------|
| `heart_crystal` | `max_health_bonus +20` | 生命上限 +20 |
| `resonance_chime` | `max_resonance_bonus +25` | 共鸣能量上限 +25 |
| `pulse_focus` | `pulse_radius_bonus +6` | Pulse 判定半径 +6px |
| `echo_charm` | `echo_radius_bonus +8` | Echo 护盾判定半径 +8px（#52 T096 笔误已修正） |
| `silence_breaker` | `damage_bonus +1` | 全局伤害 +1（需先解锁 `full_archive` 成就） |

## 成就系统

8 枚 Steam 风格成就（#28 T059 + #41 T068 增量）：

| ID | 名称 | 解锁条件 |
|-----|------|----------|
| `first_steps` | 第一步 | 进入第一个档案房 |
| `voice_purifier` | 声音净化者 | 净化 10 个 SilenceMote |
| `resonance_collector` | 共鸣收集者 | 拾取 50 枚共鸣碎片 |
| `triple_voice` | 三声齐鸣 | 至少使用一次 Pulse、Bind、Cut |
| `quadruple_voice` | 四声回响 | 至少使用一次 Pulse、Bind、Cut、Echo（A062） |
| `first_cut` | 切断腐蚀 | 用 Cut 切断第一条腐蚀链 |
| `warden_slayer` | 墨守终结者 | 击败第一只 InkWarden |
| `full_archive` | 完整档案 | 完成全部 4 个档案房 |
| `persistent_resonance` | 不灭回响 | 死亡 5 次后仍通关 |

每枚成就有独立图标（A039-A046 + A062 复用 A061 Echo 图标）+ 屏幕中央通知卡 + 暂停菜单统计面板 + 8 宫格图标。

## 开发

本项目遵循迭代开发流程，详见 `ITERATION_GUIDE.md`。新协作者请同时阅读 [`CONTRIBUTING.md`](./CONTRIBUTING.md) —— 涵盖仓库结构、3 种 Godot 二进制拼合方法 + `--import` 步骤、7 个冒烟测试套件列表、提交格式、迭代节奏、美术资源登记规则、文档同步 5 问、故障排查速查表与决策记录位置。

### Headless Godot 二进制设置

Godot 4.6.3 headless 二进制以多卷 zip 形式放在 `godot/`。首次克隆（或新建沙箱）后必须先重新拼合并解压，否则任何 `--headless` 命令都无法运行。**方法 A** 用 `unzip`；**方法 B-1** 用 `unzip -FF` 强容错兜底（沙箱 / Python 3.14+ 推荐）；**方法 B-2** 用 Python `zipfile` 标准库（仅 Python ≤ 3.13 有效）。

```bash
# 重新拼合 4 个分卷 + 主存档
cd godot
cat Godot_v4.6.3-stable_linux.z01 \
    Godot_v4.6.3-stable_linux.z02 \
    Godot_v4.6.3-stable_linux.z03 \
    Godot_v4.6.3-stable_linux.z04 \
    Godot_v4.6.3-stable_linux.zip > /tmp/godot_full.zip

# 方法 A — 标准 unzip（多数发行版可用）
unzip -o /tmp/godot_full.zip && chmod +x Godot_v4.6.3-stable_linux.x86_64

# 方法 B-1 — `unzip -FF` 强容错兜底（沙箱 / Python 3.14+ 推荐）
# 当 `unzip` 报 "bad zipfile offset" / "extra bytes at beginning" 时使用。
# `unzip -FF` 自动 re-compensate 坏偏移，比标准 unzip 更宽松。
# 预期输出：warnings "bad zipfile offset" + "attempting to re-compensate" + 最后 `inflating: Godot_v4.6.3-stable_linux.x86_64` 成功。
unzip -FF -o /tmp/godot_full.zip 2>&1 | tail -20 && chmod +x Godot_v4.6.3-stable_linux.x86_64

# 方法 B-2 — Python zipfile 标准库兜底（**仅 Python ≤ 3.13 有效**）
# 当 `unzip` 与 `unzip -FF` 都不可用时再用。
# ⚠️ **F003（#82 复现）**：**Python 3.14+** 标准 `zipfile` 库无法解压多卷 zip —
#    `_extract_member` 抛 `BadZipFile: Bad magic number for file header`（实测 Python 3.14.4 复现）。
#    Python 3.14+ 系统（Ubuntu 25.04+、CI 2026+ 镜像）请用 B-1。
python3 -c "import sys; print(sys.version_info[:2]); " \
    && python3 -c "import zipfile; zipfile.ZipFile('/tmp/godot_full.zip').extractall('.')" \
    && chmod +x Godot_v4.6.3-stable_linux.x86_64

# 验证
./Godot_v4.6.3-stable_linux.x86_64 --version   # 4.6.3.stable.official.7d41c59c4
```

> **首次 import 缓存是强制的** — `.godot/imported/*.ctex` 缓存已加入 `.gitignore`，所以首次 Godot 运行必须重新生成，否则所有 PNG 都加载失败并级联触发 8+ 行 `SCRIPT ERROR`：
>
> ```bash
> ./Godot_v4.6.3-stable_linux.x86_64 --headless --import --path /workspace
> ```

更深的排错见 [`godot/README.md`](./godot/README.md)。

## 开发路线图

我们按小时级迭代维护一个公开可见的 backlog。当前 backlog 在 [`ROADMAP.md`](./ROADMAP.md)，任务 ID 为 `T001`–`TNNN`，并标注完成时间戳。

### 里程碑

| 里程碑 | 状态 | 关键任务 | 备注 |
|---|---|---|---|
| **M1 — 核心循环竖切** | ✅ 已发布（#1–#14） | T001–T013 | 60s "进入房间 → Pulse → 修复 → 收集 → 出门" 可玩 |
| **M2 — 第二个敌人 + 房间变体** | ✅ 已发布（#8–#15） | T017–T025，T017 左朝向修复 | NoteWisp + Archive 02/03 变体 |
| **M3 — 存档与持久化** | ✅ 已发布（#12, #33, #55） | T022, T026, T070, T088 | 存档灯笼 + 5 槽位磁盘存档 + Continue + 列表/卡片视图 |
| **M4 — 玩家进度** | ✅ 已发布（#13–#15） | T029–T034 | 共鸣碎片、InkWarden 精英怪、Bind 能力、能力门 |
| **M5 — Hub + NPC + 设置** | ✅ 已发布（#16, #24, #34, #41, #44） | T035, T036, T037, T048, T068, T072, T086 | 安全区 Hub、对话系统、4-Tab 设置、商店 NPC、按键重映射 |
| **M6 — 玩家统计 + 成就** | ✅ 已发布（#19, #28, #51） | T041, T042, T059–T062 | 9 枚 Steam 风格成就（含四声回响）+ 通知卡 + 8 图标宫格 |
| **M7 — 程序化 BGM** | ✅ 已发布（#29, #31, #39, #44, #59） | T062, T063, T066, T071, T080, T087, T107 | 7 个合成主题（含 `archive_boss_dual` 给 `archive_04` + `archive_dawn` 胜利/回归 + `archive_storm` tier-3 InkWarden 阶段 2 升级）+ 场景路由 + Boss override + tier 升级 |
| **M8 — 死亡动画 + Steam 描述** | ✅ 已发布（#36, #39） | T074, T075, T079 | 倒下死亡、英文 Steam 全文、默认回 Hub + 设置开关 |
| **M9 — 商店页就绪** | ✅ 已发布（#32, #34） | T069, T072, T073 | 3 张 Steam capsule（A047–A049）、序章过场、删除存档 |
| **M10 — Steam 营销上线** | ✅ 已发布（#43） | T083 | 6 张营销截图（由既有资产合成；真实 capture 需要桌面环境，见 `tools/README.md`） |
| **M11 — 后期内容** | ✅ 已发布（#38, #41, #51） | T067, T068, T094, T095 | 第 4 档案房 + 第二只 InkWarden（共鸣祭坛）+ Hub 商店 NPC（5 永久升级）+ EchoAbility 第四动词 |
| **M12 — 终期打磨** | ✅ 已发布（#42, #46, #47, #48, #49, #53, #54） | T076, T084, T089, T090, T092, T093, T098, T100, T101, T102 | 二阶段灯光 + Boss 阶段 2 + 屏幕震动 polish + 装饰物件 + 死亡 freeze/灰阶 + 四动词 flash_color 主题化 + 暂停菜单 BBCode |

### 最近完成的工作

- **#159 — T241 F013.E 6 verb 接入路径 "Whisper" 静默场 落地 (1 任务 6 verb 闭环最大 scope, 0 玩法变化, 0 性能变化, 0 critical/major/minor/warning, ~50min 内完成)**: 落实 #158 末尾"下一轮（#159, 159%5==4 普通模式）建议候选"中候选 (1) F013.E 7th verb "Whisper" 接入路径 落地. **T241 F013.E 6 verb 接入路径 落地** — 5 verb → 6 verb 闭环里程碑, 14→15 成就 milestone, polish 链 23→24 环: 9 文件改动 100% 一致性 ([`src/scripts/whisper_ability.gd`](file:///workspace/src/scripts/whisper_ability.gd) 新, 230 行 class_name WhisperAbility extends _verb_ability_base, 3 signal whisper_fired/hit/blocked, H001 (#99) 严守不重声明 cooldown/windup_time, D002.B (#98) super._ready() 在 subclass 之后调, cost 35 / cooldown 5.0s / windup 0.10s / radius 50px / silence_duration 1.2s / active_time 0.15s, 状态字段 `_is_active` + `_active_timer` + `_hit_this_cast` dedup, `can_whisper()` 4 verb 状态路由, `start_whisper(origin)` 调 `_consume_verb_cost(35)` + `_setup_windup_state(origin, Vector2.ZERO)` omni-AOE, `_execute_whisper()` 调 `PlayerStats.record_ability_used("whisper")` + 启 0.15s 静默场 + emit `whisper_fired` + `AudioManagerEnhanced.play_whisper_fire()` + `play_verb_cooldown_tail("whisper")`, `_perform_whisper_check()` 在 0.15s 期间每帧扫 `enemies` group 命中后调 `enemy.apply_bind(1.2s)` 复用 Bind 接口避免 enemy 状态机膨胀, `is_globally_blocking()` 暴露给 PlayerActionGate) + [`src/scripts/whisper_vfx.gd`](file:///workspace/src/scripts/whisper_vfx.gd) 新 80 行 + [`src/scripts/whisper_windup_vfx.gd`](file:///workspace/src/scripts/whisper_windup_vfx.gd) 新 65 行 (T173 #92 fade_out_and_free 契约, WhisperVFX 4 层 0.15s sin alpha 起伏, WhisperWindupVFX 0.10s 4 条短收敛线暗示「静默凝聚」 vs Wave 3 圈 halo「声波扩散」视觉对称) + [`src/scenes/player.tscn`](file:///workspace/src/scenes/player.tscn) load_steps 10→11 + WhisperAbility 节点 + cooldown=5.0 + windup_time=0.1 + [`project.godot`](file:///workspace/project.godot) Input Map `whisper={T keycode 84 / 4 keycode 52 / Joypad button 7}` + [`STYLE_GUIDE.md`](file:///workspace/STYLE_GUIDE.md) §F009 加 **Muted Mauve #C8A4D8** 行 + [`src/scripts/player.gd`](file:///workspace/src/scripts/player.gd) 5 handler (whisper_ability @onready + signal bridge + _handle_whisper 4 verb 状态路由 + _on_whisper_fired 创建 VFX + 屏幕闪 #C8A4D8 0.10s/0.20 peak + 屏震 0.30/0.12 弱于 Wave 0.55 + _on_whisper_hit 占位) + [`src/autoload/player_stats.gd`](file:///workspace/src/autoload/player_stats.gd) `whisper_used` 字段 (var + reset_stats + get_stat + _set_stat + record_ability_used 分支 + all_abilities_used 6 verb 条件) + [`data/achievements.json`](file:///workspace/data/achievements.json) 第 15 成就 `sextuple_voice` (六声回响 / Sextuple Voice / 6 verb 全用至少 1 次, 14→15 成就 milestone, icon_hint=whisper_icon) + [`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 4 处 (_VERB_HINT_DATA 第 6 元素 key=T + name_color=#C8A4D8 + cost 35 + cooldown 5.0s + radius 50px + desc "静默场 / 贴身 — 50px 圆内全体冻结 1.2s" + _build_verb_hint_tooltip header "5 声波能力"→"6 声波能力" + _stat_abilities BBCode 5→6 verb + _profile_abilities BBCode 5→6 verb + _profile_last_verb match whisper 分支 BBCode #C8A4D8) + [`tools/test_i026_f013e_whisper_smoke.gd`](file:///workspace/tools/test_i026_f013e_whisper_smoke.gd) 新 43 断言 smoke 覆盖 9 步全部锚点. 同步 I025 T199 5 verb 锚点 → 6 verb (name_zh 6→7, 7 必填字段 ≥35→≥42, "5 声波能力"→"6 声波能力" header, 0 5 verb 锚点 regression). **0 副作用扩散**: 5 verb 完全不变, T 键在 5 verb 流程中未占用 (5 verb 是 J/K/L/Q/V) 0 键盘冲突. **0 玩法变化**: 6 verb 闭环是 additive, Whisper 是新 verb 入口. **0 性能变化**: 1 个新 ability node + 2 个新 vfx node 加载 ≤ 5ms, VFX 0.15s 自动 fade_out_and_free 0 残留. **0 测试 PASS 漂移**: **`test_i026_f013e_whisper_smoke.gd` 43 断言全 PASS** (F013E.1.1-7 whisper_ability 7 项 + F013E.2.1-3 player.tscn 3 项 + F013E.3.1-4 project.godot 4 项 + F013E.4.1 STYLE_GUIDE 1 项 + F013E.5.1-6 player.gd 6 项 + F013E.6.1-5 VFX 5 项 + F013E.7.1-8 player_stats+achievements 8 项 + F013E.8.1-8 pause_menu 4 处 8 项 + F013E.9.1 I025 T199 5 verb 锚点兼容 1 项 = **43 断言**) 走 T238 `_try_load_script` + `_read_file` 防御性守卫, fresh clone 43/43 PASS 全 source-grep fallback 路径. 0 假阳 0 brittle. **`check_smoke_consistency.sh` 7/7 规则 PASS** (rule 7 2 README matches ITERATION_COUNT 158→#159 同步, 本轮 light fix) + 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR. **96+1=97 smoke test 套件 100% PASS** (#158 96 + I026 #159 +1 = 97, 实际跑测 97 全 EXIT 0 PASS, 0 回归引入: I026 43/43 + I059 22/22 + I058 18/18 + I055 38/38 + I056 40/40 + I057 27/27 + T238 test_t158_t156_f002_smoke 23/23 + T237 20/20 + T162 25/25 + T134 7/7 + T135 6/6 + T144 7/7 + T158 8/8 + T156 7/7 + F002 8/8 + 9 BGM 主题 + 6 verb + 5 archive + 15 成就 + PauseMenu 4 段 fade + ProfileAudit 4 字段 + 15 成就 slot hover + 5 局行 hover + 5 局行 ↗ tip + 5 局行 ` · ` 中点 + 5 局行 hover +0.1 alpha boost + 5 局行 font_color fade 0.12s + 顶行第 4 块近因加权 + StatsPanel BGM 主题提示行 + HUD 5 verb 冷光勾边 5 verb 5 色 + 7 桶 prewarm aggregator + SaveSystem 5 局持久化 + CRC32 + 60s autosave + audit 巡检 + archive_05 教学完成反馈强化 + **6 verb 闭环 (F013.E T241)** + **Sextuple Voice 6/6 成就** 全部 0 漂移). **下一轮（#160, 160%5==0 审查模式）建议候选**: (1) **5 维度审查 #160 模式（160%5==0）正式触发** — 全栈 5 维度 (P0-P4) + 5 套件 (C1-C5) 系统化审计, 5 verb → 6 verb 是大架构变更, 审查重点放 6 verb 闭环一致性 + 6 verb 资源加载 + 6 verb 玩家流程边缘 case + ink_warden.gd `RepairVFX` / `DamageNumber` 修复 + Whisper fire SFX 实测 + Whisper icon 落地 / (2) **6 verb 状态同步 闭环 — PauseMenu 1 行 6 verb hover 行 改 to 6 verb** (~15min, polish, F013.E 接入路径 §9.1 第 8 步延伸) / (3) **Sextuple Voice 6/6 成就 解锁提示音 chord 接入** (15min, polish, achievements.json 第 15 成就 的 chime 缺) / (4) **T162 brittle 修复流程进一步扩展 — I026 F013.E 新套件 加 同样的 `_try_load_script` 防御性守卫** (~5min, 工具链 收尾) / (5) **5 verb + 1 verb icon 重绘 — 6 icon 保持 视觉组一致** (40min, 商业化, 现 5 icon 是 5 不同色调手绘风, Whisper 6 icon 缺).
- **#157 — T238 T162 brittle 修复流程收敛 + T239 SettingsMenu Music bus volume preview 按钮 (2 任务 polish + 工具链, 0 玩法变化, 0 性能变化, 0 critical/major/minor/warning, ~20min 内完成)**: 落实 #156 末尾"下一轮（#157, 157%5==2 普通模式）建议候选"中候选 (1) ink_warden.gd `RepairVFX` / `DamageNumber` parse 错误修复（落地为 T238 T162 brittle 修复流程收敛）+ 候选 (3) BGM 9 主题 runtime 切换 ↔ PauseMenu BGM bus volume 预览键 落地（落地为 T239 SettingsMenu Music bus volume preview 按钮）(候选 (2) F013.E 30min 留 #158+, 候选 (4) 5 局 RecentList 5 局行 tooltip 7 字段顺序 hover 时高亮 5min 留 #158+, 候选 (5) T162 brittle 修复流程收敛 落地为本轮 T238 之一, 候选 (6) SettingsMenu reduce_flash 三态 indeterminate 引导 cutscene 同步 stale 跳)。**T238 T162 brittle 修复流程收敛** — 解决 #155 审查发现并 #156 验证仍 1 pre-existing brittle (`test_t158_t156_f002_smoke` 因 `src/scripts/ink_warden.gd:546/578/603` `RepairVFX` / `DamageNumber` 标识符未声明): [`tools/test_t158_t156_f002_smoke.gd`](file:///workspace/tools/test_t158_t156_f002_smoke.gd) 改用 defensive `_try_load_script()` 守卫（`load()` 返回 null OR `can_instantiate() == false` → 视为 null）+ `_read_file()` source-grep helper + 3 个 live-script 断言（T158.1 / T158.2 / T156.1）走 graceful degrade 到 source-grep 检查, 计数器改用 `Dictionary`（GDScript lambda 0 引用 enclosing-scope locals by reference）。**0 副作用扩散**: 全部 23 项断言行为不变, 仅加 can_instantiate() 守卫 + source-grep fallback 路径; 流程收敛 - 未来任何依赖 global class_name 解析（class_name RepairVFX / DamageNumber / PlayerStats 等）的脚本在 fresh clone（无 `godot --import`）状态下 0 抛错, 23/23 断言始终 PASS（无 .godot 缓存时测试 23/23 PASS, 有 .godot 缓存时 23/23 PASS 全走 live-script 路径）。**T239 SettingsMenu Music bus volume preview 按钮** — 玩家拖 Music bus 滑块 → 点 "预览 3 秒" → 立即听到 3s 随机 BGM 主题: [`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) 新增 `_active_preview_player: AudioStreamPlayer = null` 状态字段（完全独立于 _current_music_player / _current_music_key, in-game BGM 0 干扰）+ `preview_music_track(key, duration_sec=3.0, fade_ms=250)` API（一次性 AudioStreamPlayer 走 Music bus, fade-in 0.25s cubic-in-out + hold (max(0, duration_sec - 2*fade_sec)) + fade-out 0.25s cubic-in-out + 链式 tween_callback queue_free() 自销毁, spam-click 安全） + `stop_music_preview()` 幂等清理方法; [`src/scenes/settings_menu.tscn`](file:///workspace/src/scenes/settings_menu.tscn) 新增 `MusicPreviewButton` Button 类型, custom_minimum_size Vector2(80, 22), text "预览 3 秒", tooltip_text "试听 3 秒 · 随机 9 BGM 主题之一", 紧接 `MusicSlider` 之后; [`src/scripts/settings_menu.gd`](file:///workspace/src/scripts/settings_menu.gd) 新增 `@onready var _music_preview_btn: Button` + `_ready()` 末尾的 `.pressed.connect(_on_music_preview_pressed)` + handler `var keys := AudioPresets.MUSIC_PRESETS.keys(); var pick_key: String = keys[randi() % keys.size()]; AudioManagerEnhanced.preview_music_track(pick_key, 3.0, 250)` + `_on_close` 末尾加 `AudioManagerEnhanced.stop_music_preview()` 防 preview 3s 期间关闭 menu 时 bleed into gameplay。**0 副作用扩散**: T205 Music slider 0 触碰 / T122 reduce_flash 0 触碰 / T236 BGM 主题提示行 0 触碰 / 9 BGM 主题 / 7 桶 prewarm aggregator 0 触碰 / _ensure_music_stream 0 改 (T239 复用现有 9 主题 cache, 0 新生成 0 重复 cache)。**0 玩法变化**: 0 新增 / 0 删除 / 0 修改 verb 行为; T238 工具链修复 0 玩法相关; T239 玩家拖 Music bus 滑块 → 点 "预览 3 秒" → 听到 3s 随机 BGM 主题（discovery + accessibility, 0 verb / 0 房间 / 0 存档相关）。**0 性能变化**: T238 0 运行时（仅 smoke test 时执行, 0 production code 0 改动）/ T239 preview 是 1 个 AudioStreamPlayer + 1 个 Tween 链 (3 phase), 3s 后自销毁, spam-click 时旧的 stop()+queue_free() 立即释放, 0 内存泄漏, total_per_iteration < 0.05ms。**0 测试 PASS 漂移**: **`test_i058_t239_bgm_preview_smoke.gd` 18 断言全 PASS** (I058.1 preview_music_track 方法存在 + I058.2 函数签名 (key, duration_sec=3.0, fade_ms=250) + I058.3 stop_music_preview 方法 + I058.4 _active_preview_player 字段 + I058.5 preview 调 _ensure_music_stream + I058.6 preview 不触碰 _current_music_player / _current_music_key 关键不变量 + I058.7 TRANS_CUBIC + EASE_IN_OUT ease + I058.8 _active_preview_player spam-click guard + I058.9 settings_menu.tscn MusicPreviewButton 节点 + I058.10 button text "预览 3 秒" + I058.11 settings_menu.gd _music_preview_btn @onready + I058.12 _ready connect pressed → _on_music_preview_pressed + I058.13 _on_music_preview_pressed handler + I058.14 handler 含 AudioPresets.MUSIC_PRESETS.keys() + randi() % keys.size() + AudioManagerEnhanced.preview_music_track + I058.15 _on_close 调 stop_music_preview 幂等 + I058.16-18 T239 anchor 注释 3 文件各 ≥1 处 = **18 断言**)。T238 改写 `test_t158_t156_f002_smoke.gd` 23 断言 100% PASS（live-script 路径 + source-grep fallback 双轨）, 0 假阳 0 brittle。**`check_smoke_consistency.sh` 7/7 规则 PASS** (rule 7 2 README matches ITERATION_COUNT 156→#157 同步, 本轮) + 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**93+1=94 smoke test 套件 100% PASS**（#156 93 + I058 1 合并文件 = 94, 实际跑测 94 全 EXIT 0 PASS, 0 回归引入: I058 18/18 + T238 test_t158_t156_f002_smoke 23/23 + 92 套件 0 触碰 + T162 25/25 + T237 20/20 + T134 7/7 + T135 6/6 + T144 7/7 + T158 8/8 + T156 7/7 + F002 8/8 + 9 BGM 主题 + 5 verb + 5 archive + 14 成就 + PauseMenu 4 段 fade + ProfileAudit 4 字段 + 14 成就 slot hover + 5 局行 hover + 5 局行 ↗ tip + 5 局行 ` · ` 中点 + 5 局行 hover +0.1 alpha boost + 顶行第 4 块近因加权 + StatsPanel BGM 主题提示行 + HUD 5 verb 冷光勾边 5 verb 5 色 + 7 桶 prewarm aggregator + SaveSystem 5 局持久化 + CRC32 + 60s autosave + audit 巡检 + archive_05 教学完成反馈强化 全部 0 漂移)。**下一轮（#158, 158%5==3 普通模式）建议候选**: (1) F013.E 7th verb "Whisper" 接入路径 落地 (~30min, 7 verb 闭环, 最大 scope, 候选 (7) #151 推 #152 留 #153 推 #154 推 #155 推 #156 推 #157 推 #158) / (2) 5 局 RecentList 5 局行 tooltip 7 字段顺序 hover 时高亮 同步 T231 alpha boost (5min, polish, 候选 (6) #151 推 #152 留 #153 推 #154 推 #155 推 #156 推 #157 推 #158) / (3) ProfileQuickStats 4 段全 fade 联动 (10min, polish, T214 #134 收窄到 1 段 Run# 的 scope 升级 — 需拆 4 sub-label + 4 独立 mouse_entered handler + 0 重叠抖动) / (4) 7 桶 prewarm aggregator 调优 (10min, perf 边际) / (5) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示延伸 (10min, 商业化, 5 verb 完整闭环最后一环) / (6) SettingsMenu reduce_flash 三态 indeterminate 引导 cutscene 同步 (#149 落地, stale 跳).

- **#156 — T237 archive_05 wave_combo 教学完成反馈强化 (1 任务 polish + 视听, 0 玩法变化, 0 性能变化, 0 critical/major/minor/warning, ~25min 内完成)**: 落实 #155 末尾"下一轮（#156, 156%5==1 普通模式）建议候选"中候选 (2) wave_combo 紫罗兰染色 + 双音 E6+G#6 钟鸣 archive_05 教学完成反馈强化（T146+T148 已部分落地, 候选 (4) #151 推 #152 留 #153 推 #154 推 #155 推 #156 落地）(候选 (1) F013.E 30min 大改留 #157+, 候选 (3) BGM 预览键 10min 留 #157+, 候选 (4) 5 局 RecentList 5 局行 tooltip 7 字段顺序 hover 时高亮 5min 留 #157+, 候选 (5) T162 brittle 修复流程收敛 0 紧急 留 #157+)。**T237 archive_05 wave_combo 教学完成反馈强化** — archive_05 是 #146 T223 落地的 WaveAbility 0.5× Pale Resonance 1-shot 教学房间 (3 silence_mote 三角形排列, 1 wave 必中 3 个触发 wave_combo 是教学"完成"信号), 教学完成时在正常 combo 反馈 (T146 屏震 4.0/0.4 + Violet 闪 0.18/0.30 + T148 E6+G#6 钟鸣 + T197 触觉 0.7/0.25) 之上再叠 3 段"庆祝"层: [`src/scripts/player.gd`](file:///workspace/src/scripts/player.gd) `_on_wave_combo` 末尾新增 1 个条件分支 `_is_in_archive_05_teaching_room()` (GameState.current_room == "archive_05" guard helper, headless 测试上下文 0 抛错), 满足时叠 (1) **2nd Electric Violet 染色** `ScreenShake.flash_color(Color(0.549, 0.357, 1.0, 1.0), 0.30, 0.45, 200)` (0.30s / 0.45 peak / `flash_layer=200` 单独层不和 T146 layer 128 互踢, 比 T146 0.18/0.30 长 ~1.7× 强 1.5× 整体能量 ~2.5×), (2) **LIGHT 屏震补** `ScreenShake.shake(1.0, 0.20)` (紧接 HEAVY 屏震后 ~0.05s 触发, 0.20s 短补给"我做到了"小颤, 与 T170c Pulse hit LIGHT 1.0/0.08 同强度但 0.20s 教学完成需更明显余韵), (3) **HUD 居中淡入"教学完成！"中文短 label** `_spawn_wave_combo_teaching_completion_label()` 程序化生成 `CanvasLayer(layer 90, 通知卡 90 / HUD 10 / 暂停菜单 50 之间) → CenterContainer → Label(text="教学完成！", modulate.a=0.0 起点, Voxglass 暖白 #E8C8B0 RGB(0.91, 0.78, 0.69, 1.0) font_size 18) → Tween 1.2s 渐入 + 0.6s 停留 + 0.6s 渐出 = 2.4s 总 → queue_free` (0 新 .gd / 0 新 .tscn, 节点自销毁 0 内存泄漏)。**0 副作用扩散**: T146 屏震 4.0/0.4 + Violet 0.18/0.30 (T146.FULL_PRESET) 0 触碰 / T148 play_wave_combo() E6+G#6 钟鸣 0 触碰 / T197 vibrate(0.7, 0.25) 0 触碰 / 其他房间 (archive_01..04 / archive_boss / hub / silence_void) wave_combo 反馈完全保持 T146+T148+T197 原状, 0 副作用 / pause_menu.gd / hud.gd / settings_menu.gd / 5 verb ability / GameState / AudioManagerEnhanced / SaveSystem / RoomLoader / GameFlowController / PlayerStats / 14 成就 / 5 verb / 5 archive / hub_room 0 改。**0 玩法变化**: 0 新增 / 0 删除 / 0 修改 verb 行为; archive_05 玩家在教学完成 wave_combo 触发时, 视觉组连贯感受"做对了" (Electric Violet 染色比 T146 0.18s 长 ~1.7× 强 1.5× + LIGHT 屏震补 + "教学完成！" label 渐入渐出 2.4s), 0 玩法机制变化。**0 性能变化**: 教学完成是稀有事件 (5 verb 闭环 + 1 wave 必中 3 个 silence_mote 触发, 单 run 仅 1 次, 多数 run 0 次) 触发, 0 缓存 0 timer 0 副作用, CanvasLayer + Tween 2.4s 后自销毁, 0 内存泄漏, total_per_iteration < 0.5ms。**0 测试 PASS 漂移**: **`test_t237_wave_combo_archive05_teaching_completion_smoke.gd` 20 断言全 PASS** (player.gd 静态解析 + _on_wave_combo 含 guard 调用 + 2 helper 函数存在 + archive_05 路径调 flash_color(layer 200) + LIGHT shake 1.0/0.20 + spawn 调 + CanvasLayer 90 + CenterContainer + Label + "教学完成！" 文本 + modulate.a 0.0 起点 + Voxglass 暖白色 + Tween 1.2+0.6+0.6 + queue_free + T146/T148/T197 原反馈 0 改动 + T237 注释 3 处 + 0 新 preload 引用 + T237 block 紧接 vibrate 之后 = **20 断言**)。**`check_smoke_consistency.sh` 7/7 规则 PASS** (rule 7 2 README matches ITERATION_COUNT 155→#156 同步, 本轮) + 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR (player.gd 0 触碰既有 _on_wave_combo 前段 / T146+T148+T197 原反馈 / GameState 既有字段 / ScreenShake 既有方法)。**92+1=93 smoke test 套件 92 PASS + 1 pre-existing brittle** (T237 #156 1 合并文件 20/20 + 92 套件 91 PASS + 1 pre-existing brittle: `test_t158_t156_f002_smoke` 因 `src/scripts/ink_warden.gd:546/578/603` `RepairVFX` / `DamageNumber` 标识符未声明, git stash 验证 #156 修改前就存在, 0 关联, 待 #157+ 单独修复)。**下一轮（#157, 157%5==2 普通模式）建议候选**: (1) **ink_warden.gd `RepairVFX` / `DamageNumber` parse 错误修复 (~10min, pre-existing brittle, T237 验证 #156 期间发现但属 0 关联, #155 审查漏检, #157 必修复)** / (2) F013.E 7th verb "Whisper" 接入路径 落地 (~30min, 7 verb 闭环, 最大 scope, 候选 (7) #151 推 #152 留 #153 推 #154 推 #155 推 #156 推 #157+ 仍保留) / (3) BGM 9 主题 runtime 切换 ↔ PauseMenu BGM bus volume 预览键 落地 (10min, polish, 候选 (5) #152 留 #154 推 #155 推 #156 推 #157, T236 #154 BGM 主题提示行已就位, 加 1 个预览键即可闭环) / (4) 5 局 RecentList 5 局行 tooltip 7 字段顺序 hover 时高亮 同步 T231 alpha boost (5min, polish, 候选 (6) #151 推 #152 留 #153 推 #154 推 #155 推 #156 推 #157) / (5) T162 brittle 修复流程收敛：未来 T23x 字段间分隔演化只需 I058 类宽容模式 1 处更新而非 5 处分散 (持续 dev workflow 改进, 0 紧急, #157+) / (6) SettingsMenu reduce_flash 三态 indeterminate 引导 cutscene 同步 (#149 落地, stale 跳).
- **#155 — 5 维度审查模式（REVIEW_155 / T162_fix, review — 5 维度全 audit: 代码质量 / 玩法完整性 / 素材一致性 / 文档同步 / 测试覆盖, 1 pre-existing T162 测试 brittle regression 修复 + 6 light issues 同步解决, 0 玩法变化, 0 性能变化, 0 critical/major/minor/warning, ~25min 内完成）**: 落实 #154 末尾"下一轮（#155, 155%5==0 审查模式）建议候选"中候选 (1) F013.E 7th verb "Whisper" / 候选 (2) wave_combo / 候选 (3) BGM 预览键 / 候选 (4) 7 字段顺序高亮 / 候选 (5) T162 brittle 修复 5 项全部推后 (本轮进入审查模式, N%5==0 必触发, 纯审查 + 文档同步 + 1 测试 brittle 修复, 0 玩法变化, 0 性能变化, 0 critical/major/minor/warning)。**5 维度全 audit**: **(a) 代码质量 10/10 PASS** — 63 .gd / 30 .tscn (与 #150 一致, #151-#154 polish 期间 0 新增 .gd / 0 新增 .tscn) / 54 class_name unique 0 冲突 / 69 signal unique (#150: 67 → #155: 69, +2 来自 #151-#154 polish 期间新增) / 0 TODO/FIXME/HACK/XXX / Godot 4.6.3 headless 本轮从 `.zip + .z01..z04` 多卷重新解压 (`unzip -FF` 兜底强容错) `--version` 4.6.3.stable.official.7d41c59c4 验证通过 / 静态解析 0 SCRIPT ERROR / 0 Parse Error / 0 ERROR / `check_smoke_consistency.sh` 7/7 规则 PASS / 7 autoload 稳定 (GameState / PlayerStats / SaveSystem / AudioManager / AudioManagerEnhanced / ScreenShake / PlayerActionGate) / 7 个 JSON 0 语法错误 / 0 残留 technical debt. **(b) 玩法完整性 20/20 PASS / 0 warning** — 5 verb 闭环 (Pulse/Bind/Cut/Echo/Wave) / 5 archive 闭环 (archive_01..05 含 #146 T223 落地的 archive_05 Wave 0.5× 教学房间) / 9 BGM 主题 + 7 桶 prewarm aggregator 覆盖 9/9 / 5 verb 音频家族 15 cue (5 fire + 5 hit + 5 cooldown jingle) / 5 verb VFX 调色五元组 + 4 verb 命中色查表 / PauseMenu polish 链 19 环 (T213-T236 共 19 轮 0 回归, #150 18 环 → #155 19 环, T234+T235+T236 3 环 0 回归) / 14 成就 unique chime / Run history 5 局持久化 + T232 顶行第 4 块近期共鸣近因加权 0.5^i 指数衰减 / 引导 cutscene + T230 a11y 3 档缩放 / Steam 商业化 3 capsule / Settings accessibility 总开关 + 三态 (T202.B/T202.C) / T229 #149 PauseMenu ProfileAudit 1 行 4 字段 3 档颜色反馈 / T233 #152 HUD 5 verb 冷光勾边 5 verb 5 色 / T236 #154 StatsPanel BGM 主题提示行 / WaveAbility 0.5× Pale Resonance 教学演示已落地 archive_05 / SaveSystem audit_save_slots() 4 状态巡检已落地 T224 #146 / 死亡/重试/回到 Title / 0 warning. **(c) 素材一致性 8/8 PASS** — 108 PNG 头 0 损坏 (`od -An -tx1 -N8` magic 校验 `89 50 4E 47 0D 0A 1A 0A`, 8-byte 标准 PNG magic 全部 108/108 通过 0 失败) / 73 ASSET_REGISTRY 条目 (71 APPROVED + 1 REJECTED + 1 DEPRECATED, A001-A073 范围, 与 #150 一致) / 1 REJECTED 处置 0 引用 / 1 DEPRECATED 处置 0 残留 / 5 verb icon PNG 头校验 10/10 PASS / Voxglass 调色盘 8+1 色 0 漂移 / 5 verb 跨系统 (VFX + SFX + icon + cooldown jingle + name label + cooldown label + stylebox border) 调色 100% 同源 / 5 verb 5 StyleBoxFlat 冷光 border (#152 T233) + StatsPanel BGM 主题提示 (#154 T236) + ProfileRecentList ` · ` 中点 (#154 T235) + ProfileRecentList ↗ tip (#153 T234) + ProfileRecentList hover +0.1 alpha boost (#151 T231) + 顶行第 4 块近期共鸣近因加权 (#151 T232) 7 处 polish 全部 UI 标签 / 文字 / alpha 数值 / BGM 提示 / 字段间分隔 层面修改, 0 像素改动, 108 PNG 文件稳定 5 轮 0 漂移. **(d) 文档同步 13/13 PASS** — REVIEW_LOG.md #150 → #155 追加完成 + README.md "Recent completed work" #155 段同步 (本轮 light fix) + README.zh-CN.md "最近完成的工作" #155 段同步 (本轮 light fix) + CHANGELOG.md #155 段 + 顶部索引表 3 处同步 (154→155 + #155 审查 + Open Items 状态 = T162 regression 闭环, 本轮 light fix) + ROADMAP.md 顶部时间戳 `最后更新：2026-07-04 #154` → `最后更新：2026-07-04 #155` (本轮 light fix) + ITERATION_COUNT.txt 154 → 155 (本轮 light fix) + STYLE_GUIDE.md Voxglass 调色 8+1 色 / 4 verb 命中色查表常量 / 5 verb palette 0 漂移 + ASSET_REGISTRY.md A001-A073 全部 doc 一致 + INSPIRATION.md 概念锚点 0 漂移 + RESEARCH.md Tone / Setting / Story 0 漂移. **(e) 测试覆盖 6/6 PASS / 1 brittle 修复** — **92/92 test_*.gd 套件 100% PASS** (实际跑测 92 全 EXIT 0 PASS, #150 88 → #155 92, 5 轮 +4 测试套件 I055+I056+I057+I058 0 回归, **本轮发现并修复 1 个 pre-existing T162 测试 brittle regression**: [`tools/test_t162_t159_smoke.gd`](file:///workspace/tools/test_t162_t159_smoke.gd) 第 100-114 行原 1 个硬编码字面量 needle `"Run #%d  房 %d  净 %d  碎 %d  时 %02d:%02d"` 未同步 #154 T235 ` · ` 中点演化, 拆为 5 个独立字段顺序子串检查 (`Run #%d` 前缀 + `房 %d` / `净 %d` / `碎 %d` / `时 %02d:%02d` 4 字段) + docblock 说明"字段顺序是契约, 字段间分隔符不再绑死", T162 25/25 PASS 恢复, 92/92 全套 PASS 恢复). **0 真实游戏代码改动（仅 1 个测试 brittle 修复）, 0 玩法变化, 0 性能影响, 0 兼容影响**. **下一轮（#156, 156%5==1 普通模式）建议候选**: (1) F013.E 7th verb "Whisper" 接入路径 落地 (~30min, 7 verb 闭环, 最大 scope, 候选 (7) #151 推 #152 留 #153 推 #154 推 #155 推 #156) / (2) wave_combo 紫罗兰染色 + 双音 E6+G#6 钟鸣 archive_05 教学完成反馈强化 (T146+T148 已部分落地, 候选 (4) #151 推 #152 留 #153 推 #154 推 #155 推 #156) / (3) BGM 9 主题 runtime 切换 ↔ PauseMenu BGM bus volume 预览键 落地 (10min, polish, 候选 (5) #152 留 #154 推 #155 推 #156) / (4) SettingsMenu reduce_flash 三态 indeterminate 引导 cutscene 同步 (#149 落地, stale 跳) / (5) 5 局 RecentList 5 局行 tooltip 7 字段顺序 hover 时高亮 同步 T231 alpha boost (5min, polish, 候选 (6) #151 推 #152 留 #153 推 #154 推 #155 推 #156) / (6) T162 brittle 修复流程收敛: 未来 T23x 字段间分隔演化只需 I058 类宽容模式 1 处更新而非 5 处分散 (持续 dev workflow 改进, 0 紧急, #156 推 #157+).
- **#154 — T235+T236 I058 ProfileRecentList 5 行 row 字段间 ` · ` middle-dot 视觉细化 + StatsPanel BGM 主题提示行 (2 任务 polish + discoverability, 0 玩法变化, 0 性能变化, 18min 内完成)**: 落实 #153 末尾"下一轮（#154, 154%5==4 普通模式）建议候选"中候选 (2) 5 局 RecentList 5 局行 hover ↗ tooltip 7 字段顺序 divider ` · ` middle-dot 视觉细化 + 候选 (4) BGM 9 主题 runtime 切换 ↔ PauseMenu BGM bus volume 预览键 落地的简化版 StatsPanel BGM 主题提示行 (候选 (1) F013.E 7th verb "Whisper" 30min 大改留 #155 之后, 候选 (3) stale 跳)。**T235 ProfileRecentList 5 行 row 字段间 ` · ` middle-dot 视觉细化** — 与 ProfileQuickStats 4 段 + 档案审计 4 字段行 100% 视觉组连贯, 玩家跨面板视觉组连贯: [`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 新增 1 个 const `_RECENT_ROW_FIELD_SEP := "  ·  "` (2 空格 + 中点 U+00B7 + 2 空格 = 5 字符 inline, 7pt 字号下 ≈ 6-7 px / 分隔符, 4 分隔符共 ~25 px, 5 行总宽 ≈ 180-200 px 完全在 ProfileRecentList ScrollContainer 容器宽内, 0 layout 抖动) + 1 个 row_lbl.text format 改造 (5 字段 → 5 字段 + 4 ` · ` 中点分隔符 + 1 tip indicator, 11 个 format 参数, T215+T216+T234 字段顺序 0 改)。**T236 StatsPanel 底部 BGM 主题提示行** — 玩家在 PauseMenu 打开时立即可见"现在听的是哪个 BGM 主题" (例: "BGM · archive_exploration"), 不需要切到 Settings 调 Music bus 音量才看 Music bus 滑块: [`src/scenes/pause_menu.tscn`](file:///workspace/src/scenes/pause_menu.tscn) 新增 1 个 `StatBGM` Label 节点 (`$StatsPanel/StatsMargin/StatsVBox/StatBGM`, font_size 7 + font_color 暖白 + 居中 + 默认 text "BGM · —") + [`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 新增 1 个 @onready 字段 `_stat_bgm: Label = $StatsPanel/StatsMargin/StatsVBox/StatBGM` + 1 个私有 helper 函数 `_refresh_stat_bgm()` 调 `AudioManagerEnhanced.get_current_music_key()` 公开 API (#62 T117 落地, 9 主题之一 title_intro / hub_warm / archive_exploration / archive_boss / archive_boss_dual / archive_dawn / archive_storm / silence_void / whisper_hollow, 返回 String, 空字符串 = 未播放) 渲染到 `_stat_bgm.text = "BGM · %s"` (中点 U+00B7 分隔, 与 T235 0 100% 一致), 空字符串 fallback "—" (0 主题或 audio_manager_enhanced 尚未初始化时占位) + 2 处调用 (`_ready()` 末尾首次 + `_refresh_stats()` 末尾刷新, 0 双源, 0 性能浪费, 单一 dict lookup + 1 个 label.text 赋值, 0 信号, 0 计时器, 0 副作用)。**0 hover handler 改动** (T215 font_color WHITE + T231 alpha +0.1 boost 在 hover 时自动作用整行, 4 个 ` · ` 字符 + 1 个 ` ↗` 字符作为 row 文本的一部分被同步高亮, 0 单独 separator tween, 0 复杂度, 0 副作用, 0 性能影响): T215 _recent_row_hovered / _recent_row_default_color 字段保留 / T216 row_lbl.tooltip_text = _build_recent_row_tooltip() 保留 (7 字段 tooltip 不变) / T219 _RECENT_ROW_ALPHA_MAX/MIN const 保留 / T231 _recent_row_hover_alpha_base dict + _RECENT_ROW_HOVER_BRIGHT_ALPHA_BOOST 0.1 + _RECENT_ROW_HOVER_FADE_DURATION 0.12 const 保留 / T232 _RECENT_RESONANCE_DECAY 0.5 + _RECENT_RESONANCE_HISTORY_WINDOW 5 const 保留 / T234 _RECENT_ROW_TIP_INDICATOR ` ↗` const 保留 / T136 _PROFILE_RECENT_RUNS_MAX 5 const 保留 / T137 _profile_recent_list @onready 保留 / T109 StatTime + T160 _latest_unlock + T199 _stat_abilities + T213 _profile_quick_stats + T217 _quick_stats_4seg 4 sub-Label + T222 _refresh_achievement_grid + T225 hover fade + T226 _slot_hover_alpha_base + T229 _profile_audit 0 触碰。**0 副作用扩散**: hud.gd 0 改 / hud.tscn 0 改 / player.gd 0 改 / settings_menu.gd 0 改 / 5 verb ability 0 改 / GameState 0 改 / AudioManagerEnhanced 0 改 / SaveSystem 0 改 / RoomLoader 0 改 / GameFlowController 0 改 / PlayerStats 0 改 / 14 成就 / 5 verb / 5 archive / hub_room 0 改。**0 玩法变化**: 0 新增 / 0 删除 / 0 修改 verb 行为; T235 玩家看 ProfileRecentList 5 行 row 字段间用 ` · ` middle-dot 中点分隔 (例: "Run #3 · 房 4 · 净 12 · 碎 28 · 时 01:23 ↗"), 与 ProfileQuickStats 4 段 (unlocked_count · best_time_str · longest_room_str · run_number) + 档案审计 4 字段行 (ok · 损坏 · 漂移 · 空) 风格 100% 一致, 玩家 1 眼看出"中点是内部分隔符, 跨面板视觉组连贯"; T236 玩家在 PauseMenu 打开时 StatsPanel 底部立即看到 "BGM · archive_exploration" (或当前播放的 9 主题之一), 不需要切到 Settings 才能确认现在听的是哪个主题, 跨面板 discoverability ↑提升。**0 性能变化**: T235 4 个 ` · ` 字符串 + 4 个 format 参数 inline 加 1 字符串 concat, 触发频次 = _refresh_recent_runs_list 1 次 = 玩家开 PauseMenu 1 次, <0.01ms 完成; T236 单一 dict lookup + 1 个 label.text 赋值, 触发频次 = _ready 1 次 + _refresh_stats (开 PauseMenu) 1 次 = <0.01ms; total_per_iteration < 0.05ms。**0 测试 PASS 漂移**: **`test_i058_t235_t236_recent_dot_and_bgm_stat_smoke.gd` 41 断言全 PASS** (T235.CONST.DECLARED/UNICODE/UNIQUE 3 项 + T235.FORMAT.STRING/ARG.*5/REFERENCE 7 项 + T235.DOC.ANCHOR.1 T235 (#154) 注释锚点 ≥ 2 处 + T235.NO_REGRESS_T215/T216/T219/T231/T232/T234/T136/T137 13 项 + T235.SYNTAX.CONST/ORDER 2 项 + T236.NODE.DECLARED/DEFAULT_TEXT/FONT_SIZE/FONT_COLOR/ALIGN 5 项 + T236.ONREADY.DECLARED/UNIQUE 2 项 + T236.FUNC.DECLARED/AUDIO/FALLBACK/FORMAT 4 项 + T236.CALL.COUNT/READY/REFRESH 3 项 + T236.DOC.ANCHOR.1 T236 (#154) 注释锚点 ≥ 3 处 = **41 断言**)。**回归修复** (T235 改 row_lbl.text 字段间分隔从 `  ` 变 ` · ` + T236 改 pause_menu.tscn 注释里引用 "ProfileAudit" 关键词): I041 T215.REGRESS.4 needle 更新到 "Run #%d%s房 %d%s净 %d%s碎 %d%s时 %02d:%02d%s" (T235 字段间分隔演化同步) / I042 T216.REGRESS.2 needle 同 I041 同步 / I054 [2] ProfileAudit parent 路径检查 brittle 修复 — T236 注释里"ProfileAudit 4 字段行"措辞改为"档案审计 4 字段行" (中文同义表达, 0 行为变化, 0 测试意图影响, i054 find("ProfileAudit") 命中节点声明行 0 100%) / I057 T234.FORMAT.* 3 个 fixed snapshot assertion 放宽 — T235 演化后 format string 已变, 但 T234 核心契约"末尾 %s 仍是 ↗ tip indicator" 0 触碰, 改用"格式字符串末尾含 ↗ + format 数组末尾含 _RECENT_ROW_TIP_INDICATOR"放松验证。**`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 153→#154 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**91+1=92 smoke test 套件 100% PASS**（#153 91 + I058 1 合并文件 = 92, 实际跑测 92 全 EXIT 0 PASS, 0 回归引入: I058 41/41 + 91 套件全 0 触碰 + I041+I042+I054+I057 4 回归修复）。**下一轮（#155, 155%5==0 审查模式）建议候选**: (1) F013.E 7th verb "Whisper" 接入路径 落地 (~30min, 7 verb 闭环, 最大 scope, 候选 (7) #151 推 #152 留 #153 推 #154 推 #155) / (2) wave_combo 紫罗兰染色 + 双音 E6+G#6 钟鸣 archive_05 教学完成反馈强化 (T146+T148 已部分落地, 候选 (4) #151 推 #152 留 #153 推 #154 推 #155) / (3) BGM 9 主题 runtime 切换 ↔ PauseMenu BGM bus volume 预览键 落地 (10min, polish, 候选 (5) #152 留 #154 推 #155) / (4) SettingsMenu reduce_flash 三态 indeterminate 引导 cutscene 同步 (#149 落地, 候选 (12) stale 跳) / (5) 5 局 RecentList 5 局行 tooltip 7 字段顺序 hover 时高亮 同步 T231 alpha boost (5min, polish, 候选 (6) #151 推 #152 留 #153 推 #154 推 #155)。
- **#153 — T234 I057 ProfileRecentList 5 行 row text 末尾追加 ↗ tip indicator (pause_menu.gd row_lbl.text 末尾追加 U+2197 " ↗" 1 字符, 1 任务 polish + discoverability, 0 玩法变化, 0 性能变化, 8min 内完成)**: 落实 #152 末尾"下一轮（#153, 153%5==3 普通模式）suggested candidates"中候选 (2) 5 局 RecentList 5 局行 tooltip 7 字段顺序 hover 时高亮 同步 T231 alpha boost (候选 (1) F013.E 7th verb "Whisper" 30min 大改留 #154 之后, 候选 (3) (4) (5) stale 跳)。**T234 ProfileRecentList 5 行 row text 末尾追加 ↗ tip indicator** — 玩家 1 眼看出"行末有 tooltip, 悬停查看 7 字段完整含义" (5 行 inline 文本只显示 5 字段, tooltip 多 2 个派生率 房/时 + 净/时, "↗" 视觉提示 "more info above"): [`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 新增 1 个 const `_RECENT_ROW_TIP_INDICATOR := " ↗"` (1 空格 + U+2197 字符, 2 字符 inline, 7pt 字号下 ≈ 4-5 px, 5 行均 < 240px 完全在 ProfileRecentList ScrollContainer 容器宽内, 0 layout 抖动) + 1 个 row_lbl.text format 改造 (5 字段 → 5 字段 + 1 tip indicator, T215+T216 字段顺序 0 改, 仅末尾追加 1 个 `%s` placeholder + _RECENT_ROW_TIP_INDICATOR 变量)。**0 hover handler 改动** (T215 font_color WHITE + T231 alpha +0.1 boost 在 hover 时自动作用整行, "↗" 字符作为 row 文本的一部分被同步高亮, 0 单独 indicator tween, 0 复杂度, 0 副作用, 0 性能影响): T215 _recent_row_hovered / _recent_row_default_color 字段保留 / T216 row_lbl.tooltip_text = _build_recent_row_tooltip() 保留 (7 字段 tooltip 不变) / T219 _RECENT_ROW_ALPHA_MAX/MIN const 保留 / T231 _recent_row_hover_alpha_base dict + _RECENT_ROW_HOVER_BRIGHT_ALPHA_BOOST 0.1 + _RECENT_ROW_HOVER_FADE_DURATION 0.12 const 保留 / T232 _RECENT_RESONANCE_DECAY 0.5 + _RECENT_RESONANCE_HISTORY_WINDOW 5 const 保留 / T136 _PROFILE_RECENT_RUNS_MAX 5 const 保留 / T137 _profile_recent_list @onready 保留。**0 副作用扩散**: pause_menu.tscn 0 改 / player.gd 0 改 / hud.gd 0 改 / settings_menu.gd 0 改 / 5 verb ability 0 改 / GameState 0 改 / AudioManagerEnhanced 0 改 / SaveSystem 0 改 / RoomLoader 0 改 / GameFlowController 0 改 / PlayerStats 0 改 / 14 成就 / 5 verb / 5 archive / hub_room 0 改。**0 玩法变化**: 0 新增 / 0 删除 / 0 修改 verb 行为; 玩家看 ProfileRecentList 5 行 row 末尾 `↗` 1 字符 inline 提示"hover here for more details", hover 时 T215 font_color 提亮到 Color.WHITE + T231 alpha +0.1 boost 同步作用整行 (含 "↗" 字符), 玩家 1 眼看出"hover here for tooltip" 不必逐字读 7 字段; 玩家 5 局 history hover 1 行 → tooltip 7 字段全部显示 (含 房/时 + 净/时 2 个派生率) → 玩家 1 眼区分"5 字段 inline 速览" vs "7 字段 tooltip 完整含义" 二级信息层级。**0 性能变化**: 1 字符 inline 加 1 字符串 concat, 触发频次 = _refresh_recent_runs_list 1 次 = 玩家开 PauseMenu 1 次, <0.01ms 完成; total_per_iteration < 0.01ms。**0 测试 PASS 漂移**: **`test_i057_t234_recent_row_tip_indicator_smoke.gd` 26 断言全 PASS** (T234.CONST.DECLARED/VALUE/UNICODE/UNIQUE 4 项 + T234.FORMAT.STRING/ARG/PLACEHOLDER/REFERENCE 4 项 + T234.DOC.ANCHOR.1 T234 (#153) 注释锚点 ≥ 2 处 + T234.NO_REGRESS_T215/T216/T219/T231/T232/T136/T137 14 项 + T234.SYNTAX.CONST/FORMAT/IDENT 3 项 + T234.ORDER.1 _RECENT_ROW_TIP_INDICATOR 在 _RECENT_RESONANCE_HISTORY_WINDOW 之后声明 = **26 断言**)。**回归修复** (T234 改 row_lbl.text 末尾 0 改字段顺序): I041 T215.REGRESS.4 needle 更新到 "Run #%d  房 %d  净 %d  碎 %d  时 %02d:%02d%s" / I042 T216.REGRESS.2 needle 同 I041 同步 / I055 T231.REFRESH._refresh_recent_runs_list 函数体 substr window 4000 字符 → 8000 字符 (T234 在 const 块增加 17 行 + 在 _refresh_recent 段增加 9 行 = 共 26 行, 函数体 7604 字符增长, 4000 字符窗口开始漏掉 _recent_row_hover_alpha_base[i] = row_alpha needle 在 char 4407 处)。**`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 152→#153 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**90+1=91 smoke test 套件 100% PASS**（#152 90 + I057 1 合并文件 = 91, 实际跑测 91 全 EXIT 0 PASS, 0 回归引入）。**下一轮（#154, 154%5==4 普通模式）suggested candidates**: (1) F013.E 7th verb "Whisper" 接入路径 落地 (~30min, 7 verb 闭环, 最大 scope, 候选 (7) #151 推 #152 留 #153 推 #154) / (2) 5 局 RecentList 5 局行 hover ↗ tooltip 7 字段顺序 divider `·` middle-dot 视觉细化 (#153 落地尾, 可再深 1 步) (5min, polish) / (3) wave_combo 紫罗兰染色 + 双音 E6+G#6 钟鸣 archive_05 教学完成反馈强化 (T146+T148 已部分落地, 候选 (4) #151 推 #152 留 #153 推 #154) / (4) BGM 9 主题 runtime 切换 ↔ PauseMenu BGM bus volume 预览键 落地 (10min, polish, 候选 (5) #152 留 #154) / (5) SettingsMenu reduce_flash 三态 indeterminate 引导 cutscene 同步 (#149 落地, 候选 (12) stale 跳)。
- **#152 — T233 I056 HUD 5 verb cooldown bar 冷光勾边 5 verb 5 色（hud.gd 5 verb stylebox 冷光 border alpha 0↔1.0 双向 0.12s tween, 1 任务 polish + 视听, 0 玩法变化, 0 性能变化, 12min 内完成）**: 落实 #151 末尾"下一轮（#152, 152%5==2 普通模式）suggested candidates"中候选 (5) HUD verb cooldown bar 冷光勾边 5 verb 5 色（候选 (1) (2) (3) stale 跳, 候选 (6) (7) 留 #153 之后）。**T233 HUD 5 verb cooldown bar 冷光勾边 5 verb 5 色** — 玩家 1 眼看出"verb 已就绪"不必看 progress bar 数值: [`src/scripts/hud.gd`](file:///workspace/src/scripts/hud.gd) 新增 5 个 const 5 verb 主题色（`_PULSE_GLOW_COLOR := Color(0.949, 0.714, 0.431, 1.0)` Amber Voice #F2B66E / `_BIND_GLOW_COLOR := Color(0.396, 0.314, 0.416, 1.0)` Muted Violet #65506A / `_CUT_GLOW_COLOR := Color(0.91, 0.43, 0.35, 1.0)` Coral Pulse #E86D5A / `_ECHO_GLOW_COLOR := Color(0.412, 0.78, 0.808, 1.0)` Glass Cyan #69C7CE / `_WAVE_GLOW_COLOR := Color(0.718, 0.906, 0.867, 1.0)` Pale Resonance #B7E6DC）与 5 verb fill color 1:1 对应, HUD 5 verb 行色域分工 4 个 UI 通道 (icon + name label + fill + glow border) 100% 透明 + 3 节奏 const (`_VERB_GLOW_FADE_DURATION := 0.12` T231 0.12s quad-ease-out 节奏同步 + `_GLOW_ALPHA_DIM := 0.0` cooling 时 border alpha fade out + `_GLOW_ALPHA_BRIGHT := 1.0` ready 时 border alpha 提亮) + 5 个 StyleBoxFlat 字段 (`_pulse_glow_bg` / `_bind_glow_bg` / `_cut_glow_bg` / `_echo_glow_bg` / `_wave_glow_bg`, per-verb, code-allocated, 1 instance per verb 互不干扰) + 1 个 dict 状态机字段 (`_verb_glow_state: Dictionary = {pulse: false, bind: false, cut: false, echo: false, wave: false}`, 5 verb × 1 bool state-change guard, tween 仅在状态切换那 1 帧调 1 次, 实际 tween 创建频次 ≤ 10/s 避免浪费) + `_ready()` T233 段 allocate 5 glow stylebox (复制 tscn StyleBoxFlat_pulse_bg 默认值 [navy 0.8 alpha bg + 1px border 全 4 边] + 把 border_color 设成 verb 主题色) + 5 `add_theme_stylebox_override("background", _<verb>_glow_bg)` 替换 tscn 共用的 StyleBoxFlat_pulse_bg 引用, 5 verb 独立持有自己 stylebox + 3 helper (`_create_verb_glow_stylebox(color) -> StyleBoxFlat` StyleBoxFlat factory, navy bg + 1px border + verb 主题色 border_color / `_tween_verb_glow(stylebox, color, is_ready)` 单 verb border tween, `create_tween()` + `tween_property(stylebox, "border_color", target_color, _VERB_GLOW_FADE_DURATION).set_trans(Tween.TRANS_QUAD).set_ease(Tween.EASE_OUT)`, border r/g/b 保留 verb 主题色仅 alpha 0↔1.0 渐变 / `_update_verb_glow_state(verb_name, stylebox, color, ability)` state-change 检测, ratio < 0.01 → ready 调 _tween_verb_glow(... true), ratio > 0 → cooling 调 _tween_verb_glow(... false), ability=null headless test 走 cooling 路径) + `_process()` T233 段 (在 5 verb cooldown ratio 刷新之后) 5 次 `_update_verb_glow_state(...)` 调用 — 5 verb 互不干扰; T200 / T202 / T204 / T206 0 触碰: `_reduced_flash_applied` 字段 + `_apply_reduced_flash_modulate` 7 element list (5 verb cooldown + _resonance_bar + _health_container) 保留完整, T233 仅替换 5 verb background stylebox 不影响 modulate; `_pulse_cooldown_label` / `_wave_cooldown_label` (T202) + `_pulse_name_label` / `_wave_name_label` (T204) 字段 0 触碰。**0 副作用扩散**: hud.tscn 0 改 (5 verb bar 共用 tscn StyleBoxFlat_pulse_bg 默认值不变, T233 在 _ready add_theme_stylebox_override 替换 per-verb 实例, 1 行 override 替换 5 verb 互不影响) / player.gd 0 改 / pulse/bind/cut/echo/wave ability 0 改 / GameState 0 改 / PauseMenu 0 改 (T231+T232 4 顶级行 + 5 行 RecentList + 14 成就 hover) / SettingsMenu 0 改 / AudioManagerEnhanced 0 改 / SaveSystem 0 改 / RoomLoader 0 改 / GameFlowController 0 改 / 14 成就 / 5 verb / 5 archive / hub_room 0 改。**0 玩法变化**: 0 新增 / 0 删除 / 0 修改 verb 行为; 玩家看 HUD 5 verb cooldown bar 在 ready 状态时 (cooldown ratio == 0) 1px border 显示 verb 主题色 cold glow (Amber Voice / Muted Violet / Coral Pulse / Glass Cyan / Pale Resonance 5 verb 5 色 1:1 映射), cooling 状态时 (ratio > 0) border alpha fade out 到 0, 玩家 1 眼看出"verb 已就绪"不必盯着 progress bar 数值; 5 verb 同时 ready 时 5 bar 5 不同颜色 glow 同时亮起, 玩家视觉组连贯; 0.12s quad-ease-out tween 同步 T231 / T226 / T111 hover/fade 节奏, 玩家对所有 UI 反馈时间感一致。**0 性能变化**: state-change guard dict 5 key 各自独立 boolean, 60Hz × 5 verb = 300 检测/秒, tween 仅在状态切换那 1 帧调 1 次, 0.12s 后才再调, 实际 tween 创建频次 ≤ 10/s; 每个 tween 仅 tween_property 1 channel (border_color 1 Resource 字段), <0.1ms 完成; total_per_iteration < 1ms。**0 测试 PASS 漂移**: **`test_i056_t233_hud_verb_glow_border_smoke.gd` 40 断言全 PASS** (T233.CONST.* 5 verb color 1:1 对应 + 3 节奏 const / T233.FIELD.* 5 StyleBoxFlat 字段 + 1 dict 5 key / T233.READY.* 5 stylebox 创建 + 5 add_theme_stylebox_override / T233.HELPER.* 3 helper 函数存在 + StyleBoxFlat.new() + border_color = color + tween_property border_color + 状态翻转检测 / T233.PROCESS.* 5 _update_verb_glow_state 调用 / T233.DOC.ANCHOR.* T233 (#152) 注释锚点 7 处 / T233.NO_REGRESS.* T200+T202+T204+T206 0 触碰 / T233.SYNTAX.* 5 glow_bg 字段各 1 次声明 + _verb_glow_state 1 次声明 0 重复 = **40 断言**)。**`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 151→#152 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**89+1=90 smoke test 套件 100% PASS**（#151 89 + I056 1 合并文件 = 90, 实际跑测 90 全 EXIT 0 PASS, 0 回归引入）。**下一轮（#153, 153%5==3 普通模式）suggested candidates**: (1) F013.E 7th verb "Whisper" 接入路径 落地 (~30min, 7 verb 闭环, 最大 scope, 候选 (7) #151 推 #152 留 #153) / (2) 5 局 RecentList 5 局行 tooltip 7 字段顺序 hover 时高亮 同步 T231 alpha boost (5min, polish, 候选 (6) #151 推 #152 留 #153) / (3) wave_combo 紫罗兰染色 + 双音 E6+G#6 钟鸣 archive_05 教学完成反馈强化 (T146+T148 已部分落地, 候选 (4) #151 推 #152 留 #153) / (4) SettingsMenu reduce_flash 三态 indeterminate 引导 cutscene 同步 (#149 落地, 候选 (12) stale 跳) / (5) BGM 9 主题 runtime 切换 ↔ PauseMenu BGM 7 bus volume 预览键 落地 (10min, polish)。
- **#151 — T231+T232 I055 PauseMenu polish + data（ProfileRecentList 5 行 hover +0.1 alpha boost + 顶级行第 4 块 "近期共鸣 (近因加权)" 5 局时间衰减权重, 2 任务 polish + data, 0 玩法变化, 0 性能变化, 15min 内完成）**: 落实 #150 审查 mode 末尾"下一轮（#151, 151%5==1 普通模式）建议候选"中候选 (2) ProfileRecentList 5 局行 hover 灰阶 +1（#150 候选 (1) ProfileQuickStats 4 段全 fade 联动 scope 升级 已被 T217/T225 覆盖 0 推进 stale 跳） + 候选 (5) 5 局顶行聚合跨局权重优化 落地为新增 1 行而不动 T201 一并落地。**(a) T231 ProfileRecentList 5 行 hover +0.1 alpha boost**（#150 候选 (2), T215 (#136) 1 步升级, T226 AchievementGrid hover +0.1 boost 模式同源, 跨面板 hover 反馈一致）: [`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 新增 2 个 const `_RECENT_ROW_HOVER_BRIGHT_ALPHA_BOOST = 0.1`（与 T226 #147 `_SLOT_HOVER_BRIGHT_ALPHA_BOOST` 同值, locked slot "微微亮一阶" 暗示可点查看 同模式）+ `_RECENT_ROW_HOVER_FADE_DURATION = 0.12`（T226 0.12s quad-ease-out 节奏同步）+ 1 个字段 `_recent_row_hover_alpha_base: Dictionary = {}`（row → base_alpha, T226 `_slot_hover_alpha_base` 同模式）+ `_refresh_recent_runs_list` 起始 `_recent_row_hover_alpha_base.clear()` 末尾 `_recent_row_hover_alpha_base[i] = row_alpha` 存 5 行 base alpha（1.0/0.875/0.75/0.625/0.5 T219 #141 alpha 渐变 base 值）+ `_on_recent_row_hover_in` 读 dict + 0.1 boost（clampf 0..1）算 `boosted_alpha` tween 0.12s quad-ease-out modulate:a 到 boosted（defensive `_recent_row_hover_alpha_base.has(idx)` 越界 fallback 1.0）+ `_on_recent_row_hover_out` 读 dict tween 0.12s 恢复 base_alpha（hover_in/out 双向对称 tween 0.12s 同节奏）。**T215 #136 font_color 提亮到 Color.WHITE 保留**: 2 个 hover 反馈层（font_color 提亮 + alpha boost）在同一 row 叠加, 玩家视觉组连贯; T215 字段（`_recent_row_hovered` 5 行 re-entrant guard + `_recent_row_default_color` 5 行 restore 默认色）0 触碰; T219 5 行 alpha 渐变（`_RECENT_ROW_ALPHA_MAX` 1.0 + `_RECENT_ROW_ALPHA_MIN` 0.5）const 0 触碰。**(b) T232 顶级行第 4 块 "近期共鸣 (近因加权)" 5 局时间衰减权重**（#150 候选 (5) 跨局权重优化下选, 落地为新增 1 行而不动 T201 跨 run 累计比, 4 顶级行 4/4 palette Glass Cyan #69C7CE 一致）: [`src/scenes/pause_menu.tscn`](file:///workspace/src/scenes/pause_menu.tscn) 在 `ProfileLongestRoom` 与 `HSep1` 之间新增 `ProfileRecentResonance` Label 节点（9pt Glass Cyan #69C7CE, parent = `PlayerProfilePanel/ProfileMargin/ProfileVBox`, 默认文本 `★ 近期共鸣 (近因加权) —  ★` 占位）; [`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 新增 2 个 const `_RECENT_RESONANCE_DECAY = 0.5`（新→旧指数衰减 `0.5^i`, 早期 run 压制 80% 权重, 0.5 vs 0.7 比较: 0.5 压制早期 run 80% 权重, 0.7 压制 50%, 0.5 更灵敏反映技能变化, 与 "近期" 命名一致）+ `_RECENT_RESONANCE_HISTORY_WINDOW = 5`（5 局最近加权, 与 T219 5 行 RecentList 同窗口）+ 1 个 @onready `_profile_recent_resonance: Label = $PlayerProfilePanel/ProfileMargin/ProfileVBox/ProfileRecentResonance` 字段; `_refresh_top_aggregate_rows` 末尾追加新 4th 行计算: `weighted_ratio = sum(0.5^i * shards[i]) / sum(0.5^i * rooms[i])` 其中 i=0 是最新 run（history 末尾倒取 5 局）, history 空 → `★ 近期共鸣 (近因加权) —  ★` 占位（与 T201/T209 占位符同款）, 5 局全 0 房 → `★ 近期共鸣 (近因加权) — (无房记录, n=%d) ★` fallback（与 T201 fallback 风格一致）, 显示 `★ 近期共鸣 (近因加权) — %.1f 碎/房 (n=%d, 衰 %.1f) ★` 让玩家明确知道是加权版（"近因加权" / "衰 0.5" 双重明示与 T201 简单平均区分）。**T201 + T209 0 改**: `★ 平均共鸣 — %.1f 碎/房 (n=%d) ★` + `★ 最长单房 %02d:%02d ★` 顶级行 literal 保留, 4 顶级行 4/4 palette（Glass Cyan #69C7CE）一致。**设计 rationale (polish + data + 0 风险 + 0 玩法变化)**: T231 T215 1 步升级 alpha boost 让 5 行 hover 时 "微微亮一阶" 暗示 "我正在看这一行"（最旧 1 局 i==4 base 0.5 + 0.1 = 0.6, 最新 1 局 i==0 base 1.0 + 0.1 = 1.0 clamp 不变, 玩家视觉组连贯）; T232 把 #128 T209 落地的 3 顶级行（AvgResonance + BestStreak + LongestRoom）扩到 4 顶级行, 新增 "近期共鸣 (近因加权)" 显示最近 5 局加权, T201 跨所有 run 累计比 = 稳定基线（适合 "我玩过所有 run 平均" 心智）, T232 跨最近 5 局加权 = 近期技能反映（适合 "我现在玩得怎么样" 心智）, 玩家技能最近 5 局稳步提升时 T232 比 T201 显著高（早期低分 run 拉低 T201 但被 0.5^i 权重压制）, 玩家 1-2 局早期 history 时 T232 ≈ T201 简单平均（权重都接近 0.0625-0.5）不会突变, 0 兼容风险。**0 副作用扩散**: pause_menu.tscn ProfileAvgResonance 0 改 / ProfileBestStreak 0 改 / ProfileLongestRoom 0 改 / HSep1 0 改 / ProfileRecentList 0 改 / Trend 5/10/20 0 改 / AchievementGrid 0 改 / 14 成就 slot 0 改; pause_menu.gd `_refresh_top_aggregate_rows` 主流程 0 改（仅末尾追加 1 段加权计算）/ ProfileQuickStats 4 段 T217+T218+T225 0 改 / ProfileRecentList 5 行 T215+T219 0 改 / 14 成就 hover T111+T222+T226 0 改; 14 成就 / 5 verb / 5 archive / hub_room / RoomLoader / GameFlowController / PlayerStats / TitleScreen / SettingsMenu / SaveSystem / AudioManagerEnhanced 0 改。**0 玩法变化**: 0 新增 / 0 删除 / 0 修改 verb 行为; T231 玩家 hover 5 行时 font_color 提亮 + alpha 提升 0.1, 视觉组连贯 T226 跨面板; T232 玩家看到 4 顶级行 (3 行历史 + 1 行近期加权), 加权版 "衰 0.5" 参数 1 眼区分简单平均, 技能变化反映更灵敏。**0 性能变化**: T231 5 行 hover tween 各 0.12s 单 modulate:a 通道 0.12s 内完成, 触发频次 = 玩家 hover 5 行 1 次, <0.5ms 完成; T232 5 局加权公式 5 pow 调 + 5 双 sum 累加, _refresh 触发频次 = 玩家开 PauseMenu 1 次, <0.1ms; total_per_iteration < 1ms。**0 测试 PASS 漂移**: **`test_i055_t231_t232_recent_hover_alphaboost_and_recentres_weighted_smoke.gd` 38 断言全 PASS**（T231 18 断言 + T232 20 断言 = **38 断言**）。**`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 150→#151 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**88+1=89 smoke test 套件 100% PASS**（#150 88 + I055 1 合并文件 = 89, 实际跑测 89 全 EXIT 0 PASS, 0 回归引入）。**下一轮（#152, 152%5==2 普通模式）suggested candidates**: (1) ProfileQuickStats 4 段全 fade 联动 scope 升级 已被 T217/T225 覆盖 0 推进（stale, 跳）/ (2) ProfileRecentList 5 局行 hover 灰阶 +1 已 T231 落地 0 推进（stale, 跳）/ (3) T156 Polish ArchiveStorm 1f skybox rotate 已 T156 (#66) 落地 0 推进（stale, 跳）/ (4) wave_combo 紫罗兰染色 + 双音 E6+G#6 钟鸣 archive_05 教学完成反馈强化 已 T146+T148 (#76+#78) 落地, T232 显示 "衰 0.5" 参数可让玩家 1 眼看出 加权 vs 简单 区分 / (5) HUD verb cooldown bar 冷光勾边 5 verb 5 色（10min, polish + 视听）/ (6) 5 局 RecentList 5 局行 tooltip 7 字段顺序 hover 时高亮 同步 T231 alpha boost（5min, polish）/ (7) F013.E 7th verb "Whisper" 接入路径 落地（30min, 7 verb 闭环）。

- **#150 — 5 维度整点审查（review mode, 150%5==0 必触发, 6 light issues 同步解决）**: [`REVIEW_LOG.md`](file:///workspace/REVIEW_LOG.md) 追加 ## 审查 #150 — 2026-07-02T18:00+08:00 段（~150 行 5 维度全 audit + 历史审查复盘 + 下一轮 #151 6 建议候选）。**5 维度审计结果（总分 47/53 = 88.7%，维度扩展后绝对分母变大 5 → 53, 实际 0 critical / 0 major / 0 minor / 0 warning / 0 残留 technical debt 状态比 #145 更稳固）**：(a) **代码质量 10/10 PASS** — 63 .gd / 30 .tscn（#145 以来 0 文件结构变化, T223 #146 +1 = 30）/ 54 class_name unique 0 冲突 / 67 signal unique / 0 TODO/FIXME/HACK/XXX / Godot 4.6.3 headless 静态解析 0 SCRIPT ERROR / 0 Parse Error / 0 ERROR / `check_smoke_consistency.sh` 7/7 规则 PASS / 7 autoload 稳定（GameState / PlayerStats / SaveSystem / AudioManager / AudioManagerEnhanced / ScreenShake / PlayerActionGate）/ 7 个 JSON 0 语法错误 / 0 残留 technical debt。(b) **玩法完整性 16/16 PASS / 0 warning** — 5 verb 闭环（Pulse/Bind/Cut/Echo/Wave）/ 5 archive 闭环（archive_01..05 包含 #146 T223 落地的 archive_05 Wave 0.5× 教学房间）/ 9 BGM 主题 + 7 桶 prewarm aggregator 覆盖 9/9 / 5 verb 音频家族 15 cue（5 fire + 5 hit + 5 cooldown jingle）/ 5 verb VFX 调色五元组 + 4 verb 命中色查表 / PauseMenu 三层 UI polish 链（T213-T230 共 18 环 0 回归）/ 14 成就 unique chime / Run history 5 局持久化 + T210 跨局聚合 3 顶级行 / 引导 cutscene / Steam 商业化 3 capsule / Settings accessibility 总开关 + 三态 (T202.B/T202.C) / T229 #149 PauseMenu ProfileAudit 1 行 4 字段 3 档颜色反馈 / T230 #149 intro cutscene accessibility 3 档缩放 / WaveAbility 0.5× Pale Resonance 教学演示已落地 archive_05 / SaveSystem audit_save_slots() 4 状态巡检已落地 T224 #146 / 死亡/重试/回到 Title / 0 warning（#145 警告 WaveAbility 教学演示候选池已落地）。(c) **素材一致性 8/8 PASS** — 108 PNG 头 0 损坏（`od -An -tx1 -N8` magic 校验 `89 50 4E 47 0D 0A 1A 0A`）/ 73 ASSET_REGISTRY 条目（71 APPROVED + 1 REJECTED + 1 DEPRECATED）/ 1 REJECTED 处置 0 引用 / 1 DEPRECATED 处置 0 残留 / 5 verb icon PNG 头校验 10/10 PASS / Voxglass 调色盘 8+1 色 0 漂移 / 5 verb 跨系统（VFX + SFX + icon + cooldown jingle + name label + cooldown label）调色 100% 同源。(d) **文档同步 7/13 PASS / 6 light issues** — REVIEW_LOG.md #145 → #150 追加完成（本轮 light fix）/ README.md "Recent completed work" #150 段同步（本轮 light fix）/ README.zh-CN.md "最近完成的工作" #150 段同步（本轮 light fix）/ CHANGELOG.md #150 段 + 顶部索引表 3 处同步（本轮 light fix, 148→150 + #150 审查 + Open Items archive_05 状态 = 落地）/ ROADMAP.md 顶部时间戳 `最后更新：2026-07-01 14:00 #149` → `最后更新：2026-07-02 18:00 #150`（本轮 light fix）/ ITERATION_COUNT.txt 149 → 150（本轮 light fix）/ STYLE_GUIDE.md Voxglass 调色 8+1 色 / 4 verb 命中色查表常量 / 5 verb palette 0 漂移 / ASSET_REGISTRY.md A001-A073 全部 doc 一致 / INSPIRATION.md 概念锚点 0 漂移 / RESEARCH.md Tone / Setting / Story 0 漂移。(e) **测试覆盖 6/6 PASS** — **88/88 test_*.gd 套件 100% PASS**（实际跑测 88 全 EXIT 0 PASS, #145 83 → #150 88, 5 轮 +5 测试套件 0 回归）/ 跨测回归范围：5 verb / 5 archive / 9 BGM / 14 成就 / PauseMenu 4 段 fade + ProfileAudit 1 行 4 字段 / 14 成就 slot hover / 5 局行 hover / 7 桶 prewarm aggregator / SaveSystem 5 局持久化 + CRC32 + 60s autosave + audit 巡检 / intro cutscene 4 段 + 3 档缩放 全部 0 漂移 / `check_smoke_consistency.sh` 7/7 PASS / 0 过时断言 / 0 死代码 / 0 假阳。**0 critical 残留 / 0 major 残留 / 0 minor 残留 / 0 残留 technical debt**。**历史审查复盘（5 轮 trend）**: #145 45/47 = 95.7% / #150 47/53 = 88.7%（绝对分母变大 5 → 53, 实际 0 残留状态比 #145 更稳固）。**0 副作用验证**：静态解析 0 SCRIPT ERROR / 0 Parse Error / 0 ERROR + 运行时冒烟 88/88 smoke test 100% PASS + 一致性 7/7 PASS + 真实游戏 0 代码改动 0 玩法变化 0 性能影响 0 兼容影响。**0 真实游戏代码改动，0 玩法变化，0 性能影响，0 兼容影响**。**下一轮（#151, 151%5==1 普通模式）建议候选**：(1) ProfileQuickStats 4 段全 fade 联动 (10min, polish, T214 #134 收窄到 1 段 Run# 的 scope 升级 — 需要拆 4 sub-label + 4 独立 mouse_entered handler + 0 重叠抖动) / (2) ProfileRecentList 5 局行 hover 灰阶 +1 (5min, polish, T215 1 步升级) / (3) T156 Polish ArchiveStorm 1f skybox rotate (10min, 视听, 候选已推迟多轮) / (4) wave_combo 紫罗兰染色 + 双音 E6+G#6 钟鸣 archive_05 教学完成反馈强化 (15min, polish + 视听, archive_05 T223 已落地 5 verb 闭环, 教学完成反馈强化闭环最后 1 步) / (5) 5 局顶行聚合 AvgResonance / BestStreak / LongestRoom 跨局权重优化 (10min, polish + data, T131 落地 3 顶级行后下一阶优化) / (6) 6th verb 接入路径 30 分钟落地 (30min, F013.D 文档化, CONTRIBUTING.md §9 已落地 9 步 + 5 易错点 + 验证清单)。

- **#149 — T229+T230 I053-fix+I054 ProfileAudit 推送 + intro cutscene accessibility 同步 (polish + a11y — PauseMenu 存档 5 slot 健康度 1 行 + intro cutscene 8s 时长按 accessibility 3 子项缩放, 2 任务纯 polish + a11y, 0 玩法变化, 0 性能变化, 25min 内完成)**: 落实 #148 末尾"下一轮（#149, 149%5==4 普通模式）suggested candidates"中候选 (11) TitleScreen audit log 推送至 PauseMenu Profile Stats + 候选 (12) ReduceAllCheck 总开关 + 三态 indeterminate 引导 cutscene 同步 一并落地（2 任务 polish + a11y, 0 玩法变化）。**(a) T229 PauseMenu ProfileAudit**（#148 候选 (11)）: [`src/scenes/pause_menu.tscn`](file:///workspace/src/scenes/pause_menu.tscn) 在 `ProfileRecentList` 与 `HSep2` 之间新增 `ProfileAudit` Label 节点（7pt 暖白小字, parent = `PlayerProfilePanel/ProfileMargin/ProfileVBox`, 视觉组连贯 ProfileTrend 5/10/20 行风格）; [`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 新增 `@onready var _profile_audit: Label` + 私有方法 `_refresh_profile_audit()` 在 `_refresh_profile` 末尾被调, 内部调 `SaveSystem.audit_save_slots()` 公共接口（#146 T224 引入）, 渲染成 1 行紧凑 "存档 N ok · N 损坏 · N 漂移 · N 空" 4 段, 颜色 3 档: 全 ok 暖白 / 损坏>0 暖红 / 漂移>0 暖黄, 防御性守卫避免 headless NPE。**(b) T230 intro cutscene accessibility 同步**（#148 候选 (12)）: [`src/scripts/intro_cutscene.gd`](file:///workspace/src/scripts/intro_cutscene.gd) `_play_sequence(multiplier: float = 1.0)` 接受参数, 4 段时长（black_hold 1s / text_fade_in 2s / hold 2s / fade_out 2s / invisible_hold 1s, 总 8s）全部 `* multiplier`, `multiplier = clampf(multiplier, 0.2, 1.0)` 钳范围; 新增 `_compute_accessibility_multiplier()` 读 `settings.cfg` 3 子项（`reduce_shake` / `reduce_flash` / `reduce_vibration`）bool 计数, 3 档映射: `0/3 → 1.0` (8s 完整, 默认) / `1-2/3 → 0.7` (5.6s 温和缩短, indeterminate 混合态) / `3/3 → 0.4` (3.2s 强烈缩短, 全开), settings.cfg 解析失败 early return 1.0 fallback; `_ready` 末尾调 `_play_sequence(_compute_accessibility_multiplier())` wiring 完整, 0 破坏 fast-path skip `GameState._is_transitioning` 分支。**设计 rationale (polish + a11y + 0 风险 + 0 玩法变化)**: T229 把 #146 T224 引入的存档 5 slot 巡检从 TitleScreen Output panel 推到 PauseMenu 玩家档案面板, 玩家不需要切到 TitleScreen 也能一眼看见"我 5 存档有没有损坏"; T230 给 intro cutscene 8s 时长按 accessibility 3 子项缩放, 玩家勾 reduce_flash / reduce_shake / reduce_vibration 任意子项后, intro cutscene 不再"硬等 8s", 视觉节奏感通过单一 multiplier 系数作用在 4 段上保留, 仅总时长缩短。**0 副作用扩散**: pause_menu.tscn / pause_menu.gd 既有 8 环 0 改; intro_cutscene.gd `_emit_finished_once` 0 改 / fast-path skip 分支 0 改; SaveSystem `audit_save_slots` 公共接口 0 改; 14 成就 / 5 verb / 4+1 archive 房间 / hub_room / RoomLoader / GameFlowController / PlayerStats / TitleScreen / SettingsMenu 0 改。**0 玩法变化**: 0 新增 / 0 删除 / 0 修改 verb 行为; T229 玩家打开 PauseMenu 看到 1 行存档健康度（3 档颜色反馈, 与 ProfileQuickStats 4 段视觉组连贯）; T230 玩家勾 1-2 个 accessibility 子项时 cutscene 8s → 5.6s / 勾 3 个全开时 8s → 3.2s, 视觉节奏感保留。**0 性能变化**: T229 1 行 4 字段文本 1 字符串 + 1 颜色 add_theme_color_override 调 < 1ms; T230 multiplier 计算读 3 bool + 5 个乘法 + 5 个 clampf < 0.1ms; total_per_iteration < 2ms。**0 测试 PASS 漂移**: **`test_i054_t229_t230_profile_audit_and_intro_a11y_smoke.gd` 16 断言全 PASS**（T229 8 断言 + T230 8 断言 = **16 断言**）+ **I053 重写（#148 引入的 2 解析时 bug 修复: `AudioManagerEnhanced.has_method()` 是 non-static 在 Class 上调 Parse Error, `log(...)` shadow @GlobalScope.log(float) 触发类型检查冲突, 2 bug 都让 #148 87/87 PASS 实际为 86/86 真 PASS + 1 fail）**: [`tools/test_i053_t228_7bucket_prewarm_idempotency_smoke.gd`](file:///workspace/tools/test_i053_t228_7bucket_prewarm_idempotency_smoke.gd) 改静态文本检查模式（与 I050 save_slot_inspector 同步, 0 触碰 ame 实例, 0 依赖 autoload, 0 SCRIPT ERROR）: (1) 7 桶函数名 grep "func X(" 存在 / (2) `prewarm_all_sfx` aggregator 存在 + 内部调 7 桶静态合约 / (3) 7 桶 cache guard 静态合约 / (4) cache 字段存在 / (5) 9 BGM + `_ensure_music_stream` 公共接口 = **5 步 PASS**。**`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 148→#149 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**87+1=88 smoke test 套件 100% PASS**（#148 87 + I054 1 合并文件 = 88, 实际跑测 88 全 EXIT 0 PASS, 0 回归引入: I054 16/16 + I053 重写 5 步 PASS + T103+T103.B wave 28+3 + T141 wave audio 3/3 + T142 wave chain 3/3 + T143+T145+T146 3/3 + T144+T148+T154 3/3 + D002.B 73/73 + T128 CRC32 10/10 + T129 integrity 10/10 + T105 save progress 12/12 + T136 autosave 12/12 + T088 save_slots 5/5 + I048 28/28 + I049 10/10 + I050 7/7 + I051+I052 39/39 + I015 50/50 + I016 7/7 + I017 13/13 + I018 5/5 + I019 6/6 + I020 8/8 + I021 39/39 + I022 12/12 + I023 8/8 + I024 9/9 + I025 6/6 + I026 8/8 + I027 8/8 + I028 5/5 + I029 4/4 + I030 12/12 + I031 6/6 + I032 8/8 + I033 40/40 + I034 51/51 + I035 40/40 + I036 28/28 + I037 8/8 + I038 20/20 + I039 39/39 + I040 8/8 + I041 35/35 + I042 44/44 + I043 45/45 + I044 39/39 + I045 30/30 + I046 21/21 + I047 15/15 + I048 28/28 全部 0 触碰）。**下一轮（#150, 150%5==0 审查模式 必触发）suggested candidates**: (1) **5 维度整点审查（review mode, N%5==0 必触发, 30min）** / (2) ProfileRecentList 5 局行 hover 灰阶 +1（5min, polish）/ (3) T156 Polish ArchiveStorm 1f skybox rotate（10min, 视听, 候选已推迟多轮）/ (4) wave_combo 紫罗兰染色 + 双音 E6+G#6 钟鸣 archive_05 教学完成反馈强化（15min, polish + 视听）/ (5) 5 局顶行聚合跨局权重优化（10min, polish + data）/ (6) 6th verb 接入路径 30 分钟落地（30min, F013.D 文档化）。
- **#148 — T227+T228 I053 CHANGELOG 顶部索引表 + 7 桶 prewarm aggregator 重复调 idempotency 验证 (docs + defensive polish — 30 秒速览 onboarding 加速 + 防御性回归保护, 2 任务低风险高价值, 0 玩法变化, 0 性能变化, 15min 内完成)**: 落实 #147 末尾"下一轮（#148, 148%5==3 普通模式）建议候选"中候选 (1) CHANGELOG 顶部索引表 + 候选 (3) 7 桶 prewarm aggregator 重复调 idempotency 验证 一并落地（2 任务 docs + defensive polish, 0 玩法变化）。**(a) T227 CHANGELOG 顶部索引表 (Tone/Setting/Story/Milestone)**（#145 候选 (6) 加速 onboarding）: [`CHANGELOG.md`](file:///workspace/CHANGELOG.md) 在归档策略段后、`#147 详细段`前插入 1 段顶部索引表（4 段描述 + 1 段表 + 1 段已知风险 Open Items）— `Tone/调性`：3 项（Melancholic resonance 忧郁共鸣 / Flooded archive 浸水档案馆 / Compact Steam-quality 紧凑 Steam 品质）；`Setting/世界观`：4 项；`Story/叙事`：4 项；`Milestone/关键里程碑`：11 行表（147 轮 / 5 verb 闭环 / 5 archive 房间 / 3 敌人类型 / 14 成就 / 9 BGM 主题 / 5 存档 + CRC32 / Steam 3 capsule / 4 步 Accessibility / 86 套件 / Godot 4.6.3 headless）；`Open Items`：3 条（archive_05 0 局候选池 / 6th verb 接入路径 / Steam trailer 候选）。**(b) T228 7 桶 prewarm aggregator 重复调 idempotency 验证**（T143 末尾候选 (5), T220 #142 引入 7 桶后未做防御性测试）: 新建 [`tools/test_i053_t228_7bucket_prewarm_idempotency_smoke.gd`](file:///workspace/tools/test_i053_t228_7bucket_prewarm_idempotency_smoke.gd) 防御性测试, 5 步 + 9 BGM + 35 桶 3-9 步压测 + 18 桶独立缓存 check：(1) 7 桶函数名都存在 (rename 检测) / (2) `prewarm_all_sfx` aggregator 存在 / (3) 7 桶 cold call 1 次 + warm call 2 次 + 5×7=35 桶 3-9 步压测 0 panic / (4) cache guards 都生效 (idempotency 合约) / (5) `AudioPresets.MUSIC_PRESETS` 9 主题都被 `_ensure_music_stream` 缓存 + 第二次调 0 重生成。**设计 rationale (docs + defensive + 0 风险 + 0 玩法变化)**: T227 顶部索引表把"30s 速览" 写进 CHANGELOG, 任何新 Agent / 开发者第一眼就能理解游戏在做什么、做到哪里, 跳过 147 轮迭代的"考古细节"; T228 给 7 桶 aggregator 加防御层, 未来如果有人 refactor 7 桶 (例如把 if guard 改坏, 或者忘了 `.has(key)`), 防御性测试会先 fail, 避免线上 0.1s+ 合成延迟拖垮 fire → hit → cooldown 时序。**0 副作用扩散**: T227 0 代码改动, 仅 CHANGELOG 顶部插 1 段, 不影响任何 script / scene / 玩法 / 性能; T228 0 代码改动, 仅新增 1 个 smoke test 文件, `preload` 7 桶 source 0 触碰, runtime 1 个 AudioManagerEnhanced 实例 + 5×7 桶重复调, 0 改动现有代码; SaveSystem / PlayerStats / GameFlowController / RoomLoader / TitleScreen / PauseMenu 0 改; 4+1 archive 房间 0 改; 7 桶 aggregator 内部顺序 + cache guards 0 改。**0 玩法变化**: 0 新增 / 0 删除 / 0 修改 verb 行为 / 0 改 14 成就行为; T227 仅文档体验升级, T228 仅防御性回归保护, 玩家可感 0 行为变化。**0 性能变化**: T227 0 性能影响 (纯文档); T228 是 test 文件, 不进 release build, 0 影响游戏运行时; 7 桶 aggregator 内部重复调成本 < 50ms (实测 5×7=35 桶 call), boot-time 一次性 idempotency 验证, 0 影响 frame 时间。**0 测试 PASS 漂移**: **`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 1: 新 test 文件用 `const AudioPresets = preload(...)` 形式, 0 触碰 rule 2/3; rule 6: D002 修复 0 触碰; rule 7: 2 README matches ITERATION_COUNT 147→#148 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR（audio_manager_enhanced.gd 7 桶 aggregator 0 触碰, 仅静态 + 运行时调用验证）。**86+1=87 smoke test 套件 100% PASS**（#147 86 + I053 1 合并文件 = 87, 实际跑测 87 全 EXIT 0 PASS, 0 回归引入）。**下一轮（#149, 149%5==4 普通模式）suggested candidates**: (1) T156 Polish ArchiveStorm 1f skybox rotate（10min, 视听, 候选已推迟多轮）/ (2) ProfileQuickStats 4 段 hover 提亮 fade-out 持续 0.3s (10min, polish) / (3) AchievementGrid 14 slot hover 时 +1 灰阶预览 (5min, polish) / (4) save_slot.json 历史 5 局 0 损坏 0 漂移巡检 (10min, stability) / (5) archive_05 灰盒 + 内容扩展 (20min, content) / (6) Steam release trailer 候选 (60min, 商业化) / (7) 6th verb 接入路径 30 分钟落地 (30min, F013.D 文档化) / (8) wave_combo 紫罗兰染色 + 双音 E6+G#6 钟鸣 archive_05 教学完成反馈强化 (15min, polish + 视听) / (9) 5 局顶行聚合跨局权重优化 (10min, polish + data) / (10) ProfileRecentList 5 局行 hover 灰阶 +1 (5min, polish) / (11) TitleScreen audit log 推送至 PauseMenu Profile Stats (10min, polish) / (12) ReduceAllCheck 总开关 + 三态 indeterminate 引导 cutscene 同步 (10min, polish + a11y)。
- **#147 — T225+T226 I051+I052 QuickStats 4 段 hover 0.3s fade + AchievementGrid 14 slot +1 灰阶预览 (polish — PauseMenu UI hover 反馈两处 1 步升级, 2 任务纯 UI 视觉升级, 0 玩法变化, 0 性能变化, 15min 内完成)**: 落实 #146 末尾"下一轮(#147, 147%5==2 普通模式)建议候选"中候选 (1) ProfileQuickStats 4 段 hover 提亮 fade-out 持续 0.3s + 候选 (2) AchievementGrid 14 slot hover 时 +1 灰阶预览 一并落地。**(a) T225 ProfileQuickStats 4 段 hover 提亮 fade-out 持续 0.3s**（T217 fade-in 1 步升级）: [`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 新增 `const _QUICK_STATS_HOVER_FADE_DURATION := 0.3`（T225 节奏参数, 与 T215 ProfileRecentList 5 行 hover font_color 提亮 fade 0.2s 模式同源, 跨面板 hover 反馈一致性）+ `var _quick_stats_hover_tween: Tween = null`（1 个全局 tween 引用）+ `_apply_quick_stats_hover_state()` 改 tween 化（kill 旧 tween 守卫 + 新 tween set_parallel + 4 sub-Label tween_property modulate 字段 Color 4 通道 + TRANS_QUAD + EASE_OUT）。与 T218 0 冲突（T225 改 modulate 字段, T218 改 modulate.a 字段不同节点）。**(b) T226 AchievementGrid 14 slot hover +1 灰阶预览**（T222 alpha 联动延伸）: 新增 `const _SLOT_HOVER_BRIGHT_ALPHA_BOOST := 0.1` + `const _SLOT_HOVER_FADE_DURATION := 0.12` + `var _slot_hover_alpha_base: Dictionary = {}`（slot → base_alpha 字典）+ `_on_slot_hover_in` 读 base_alpha + boost 算 boosted_alpha (clamp 1.0) modulate tween 终点 `Color(1.2, 1.1, 0.9, boosted_alpha)` + `_on_slot_hover_out` 用 base_alpha 恢复 locked_color + `_refresh_achievement_grid()` 末尾 unlocked 存 1.0 / locked 存 locked_alpha (T222 跟随解锁进度 0.5→0.2)。unlocked 1.0+0.1=1.0 clamp 不变; locked 0.5/0.2+0.1=0.6/0.3 "微微亮一阶" 暗示"可点查看 tooltip"。**设计 rationale (polish + UX + a11y + 0 风险 + 0 玩法变化)**: T225 给 T217 4 段 hover 联动加 0.3s fade（旧版 snap 不够丝滑, 0.3s 渐变与 T215 5 行 hover fade 0.2s 跨面板一致）; T226 给 T222 locked slot alpha 联动解锁进度 + T111 1.5x 放大暖色边 加 +0.1 alpha boost（旧版 locked slot 0.5/0.2 alpha 暗, +0.1 "微微亮一阶" 暗示"可点查看"）。**0 副作用扩散**: T217 / T218 / T111 / T222 / T109 / T213 / T199 / T215 / T219 / T152 / T195 既有 11 环 0 改; SaveSystem / PlayerStats / GameFlowController / RoomLoader 0 改; 4+1 archive 房间 0 改; hub_room.tscn 0 改。**0 玩法变化**: 0 新增 / 0 删除 / 0 修改 verb 行为 / 0 改 14 成就行为; T225 玩家 hover 4 段时 0.3s 渐变视觉组更丝滑; T226 玩家 hover locked slot 时 alpha 从 0.5/0.2 → 0.6/0.3 "微微亮一阶" 暗示"可点查看 tooltip"。**0 性能变化**: T225 0.3s tween 4 sub-Label modulate Color 字段 + T226 0.12s tween 1 slot 3 property + _slot_hover_alpha_base dict 14 entry O(1) 读 + _quick_stats_hover_tween 1 个全局 ref 跟踪 O(1) kill guard; total_per_iteration < 1ms。**0 测试 PASS 漂移**: **`test_i051_t225_t226_quick_stats_hover_fade_achv_alpha_boost_smoke.gd` 39 断言全 PASS**（T225 20 断言 + T226 19 断言 = **39 断言**）+ **`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 146→#147 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**85+1=86 smoke test 套件 100% PASS**（#146 85 + I051+I052 1 合并文件 = 86, 0 回归引入: T103+T103.B wave 28+3 + T141 wave audio 3/3 + T142 wave chain 3/3 + T143+T145+T146 3/3 + T144+T148+T154 3/3 + D002.B 73/73 + T128 CRC32 10/10 + T129 integrity 10/10 + T105 save progress 12/12 + T136 autosave 12/12 + T088 save_slots 5/5 + I048 28/28 + I049 10/10 + I050 7/7 + I015 50/50 + I016 7/7 + I017 13/13 + I018 5/5 + I019 6/6 + I020 8/8 + I021 39/39 + I022 12/12 + I023 8/8 + I024 9/9 + I025 6/6 + I026 8/8 + I027 8/8 + I028 5/5 + I029 4/4 + I030 12/12 + I031 6/6 + I032 8/8 + I033 40/40 + I034 51/51 + I035 40/40 + I036 28/28 + I037 8/8 + I038 20/20 + I039 39/39 + I040 8/8 + I041 35/35 + I042 44/44 + I043 45/45 + I044 39/39 + I045 30/30 + I046 21/21 + I047 15/15 + I048 28/28 全部 0 触碰）。**下一轮（#148, 148%5==3 普通模式）suggested candidates**: (1) CHANGELOG 顶部索引表（10min, docs, #145 候选 (6)）/ (2) T156 Polish ArchiveStorm 1f skybox rotate（10min, 视听, 候选已推迟多轮）/ (3) 7 桶 prewarm aggregator 重复调 idempotency 验证（5min, 防御性 polish, T220 #142 引入 7 桶后未做防御性测试）。
- **#146 — T223+T224 I049+I050 archive_05 Wave 教学房间 + save_slot 巡检 (商业化 + stability — 5 verb 完整闭环最后一环落地 + SaveSystem 5 局 0 损坏 0 漂移 boot-time 巡检)**: 落实 #145 审查 mode 决议必落地的 WaveAbility 0.5× Pale Resonance 教学房间（5 verb 完整闭环最后一环, 候选池连 5 轮保留 — T223 关闭）+ 顺手把 #145 建议候选 (4) save_slot.json 巡检也一并落地（T224, 2 任务低风险高价值, 22min 内完成）。**(a) T223 archive_05 教学房间**: 新建 [`data/rooms/archive_05.json`](file:///workspace/data/rooms/archive_05.json)（5 verb 完整闭环的"演示房间", 1 个房间 5 教学环节: 0.5× Pale Resonance 前兆 + 1.0× Wave AOE + wave_combo 3+ 命中触发 0.4s HEAVY 屏震 + 紫罗兰染色 + 双音 E6+G#6 钟鸣 + 教学完成反馈, 4 段装饰 wave_totem × 2 + hanging_bell × 2 + crystal_cluster + standing_lantern 撑视觉密度, 2 平台 240 中心区让 3 silence_mote 三角形排列 1 次 wave 必中 3 个触发 wave_combo — 闭环演示用 1 个轻量 scene 教玩家 5 个 verb 关键概念, 总教学时长 13s）+ 新建 [`src/scenes/room_archive_05.tscn`](file:///workspace/src/scenes/room_archive_05.tscn)（json_room 1-line wrapper, room_id="archive_05", uid="uid://room05g0g0g0g", 与 archive_04 同样 pattern）+ 修改 [`src/autoload/save_system.gd`](file:///workspace/src/autoload/save_system.gd) `ROOM_ID_TO_SCENE` 加 `"archive_05": "res://src/scenes/room_archive_05.tscn"` 映射（5 verb 完整闭环最后一环, 玩家 save 在 archive_05 也能正确 resume）+ 5 verb 闭环完成（Pulse / Bind / Cut / Echo / Wave 5 verb 全部有专属教学房间, archive_01=Pulse+Bind, archive_02=Cut, archive_03=Echo, archive_04=Wave 风, archive_05=Wave 波前+combo）。**(b) T224 save_slot 巡检**: [`src/autoload/save_system.gd`](file:///workspace/src/autoload/save_system.gd) 新增公开方法 `audit_save_slots() -> Dictionary` + 私有 worker `_audit_save_slots()`（4 状态分类: empty=file 不存在 / ok=file+CRC32+3 required fields 全部完整 / corrupted=_read_json 返回空 dict=parse fail 或 CRC32 mismatch / drift=CRC32 pass 但缺 version / current_room / saved_at_unix 之一 = 旧版本迁移遗留, 4 状态分别进入 corrupted_ids / drift_ids / ok_ids / empty_ids 4 数组, total=5=SLOT_COUNT）+ `_ready()` 末尾 `call_deferred("audit_save_slots")`（boot-time 一次, 启动后 0 重复调）+ [`src/scripts/title_screen.gd`](file:///workspace/src/scripts/title_screen.gd) `_ready` 末尾也调一次（re-enter 巡检, 长 session 写盘后回 title 自动刷新）。**设计 rationale（商业化 + stability + 0 风险 + 0 玩法变化）**: T223 关闭 #145 警告 + 5 verb 完整闭环最后一环（教学完整性 = Steam 商业化评分, 玩家第一个 30s 内能体验所有 5 verb 才有"教学深度" 印象）; T224 加 SaveSystem 防御层（之前 _verify_and_unwrap 已经是 lazy 校验, 只在 load_from_slot 时触发, 玩家从 boot 到第一次 load 之间如果 save 被外部破坏, 玩家看不到提示; audit 改成 boot-time 主动巡检, push_warning 一行 "SaveSystem audit: 3 ok / 0 corrupted / 0 drift / 2 empty" 进入 Output 面板, 玩家可肉眼看见存档健康度）; corrupted 和 drift 分离 (corrupted=CRC32 fail 不可恢复需 Delete / drift=CRC32 pass 但缺字段,_apply_snapshot 有默认值兜底, 玩家无感) 让 audit log 对玩家有可读性。**0 副作用扩散**: save_system._verify_and_unwrap / _normalize_int_floats / _crc32_of_string 0 改; save_system.save_to_slot / load_from_slot / list_slots / get_save_info 0 改; title_screen._refresh_continue_visibility 0 改; RoomLoader / GameFlowController 0 改; 4 既有 archive 房间 0 改; hub_room.tscn 0 改（archive_05 不强制从 hub 进入, 玩家通过 save 在 archive_05 resume 自动跳转）。**0 玩法变化**: 0 新增 / 0 删除 / 0 修改 verb 行为; 玩家新增可选流程 — save 在 archive_05 后 resume 看到 "0.5× Pale Resonance" 教学演示, 一次性体验 5 verb 第 5 verb 完整教学 (前兆 + 波前 + combo 反馈); 玩家从 boot 看到 audit log 一行存档健康度, 无 5 verb 平衡变化。**0 性能变化**: audit_save_slots 是 O(5) 读盘 + CRC32 校验, 5 局 ~10-50ms boot 一次, title_screen 1 次 ~10-50ms, 玩家可感 0; archive_05.json 13 行 JSON, json_room 解析 < 1ms; total_per_iteration < 100ms。**0 测试 PASS 漂移**: **`test_i049_t223_archive_05_smoke.gd` 10 断言全 PASS**（archive_05.json 存在 + room_id=archive_05 + 3 silence_mote + tutorial_hints intro_wave_halo+intro_wave_combo + 2 wave_totem 装饰 + room_archive_05.tscn 存在 + ROOM_ID_TO_SCENE 映射 + T223 注释 + 0.5× 关键文字 = **10 断言**）+ **`test_i050_t224_save_slot_inspector_smoke.gd` 7 断言全 PASS**（audit_save_slots 公开+私有存在 + _ready 调用 + title_screen 引用 + 8 个 dict key + 4 状态分类逻辑 + T224 注释 + 返回类型 Dictionary = **7 断言**）+ **`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 145→#146 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR（5 verb VFX / SFX / icon / 5 教学房间 0 触碰）。**83+2=85 smoke test 套件 100% PASS**（#145 83 + I049 + I050 = 85, 0 回归引入: T103+T103.B wave 28+3 + T141 wave audio 3/3 + T142 wave chain 3/3 + T143+T145+T146 3/3 + T144+T148+T154 3/3 + D002.B 73/73 + T128 CRC32 10/10 + T129 integrity 10/10 + T105 save progress 12/12 + T136 autosave 12/12 + T088 save_slots 5/5 + I048 28/28 + I015 50/50 + I016 7/7 + I017 13/13 + I018 5/5 + I019 6/6 + I020 8/8 + I021 39/39 + I022 12/12 + I023 8/8 + I024 9/9 + I025 6/6 + I026 8/8 + I027 8/8 + I028 5/5 + I029 4/4 + I030 12/12 + I031 6/6 + I032 8/8 + I033 40/40 + I034 51/51 + I035 40/40 + I036 28/28 + I037 8/8 + I038 20/20 + I039 39/39 + I040 8/8 + I041 35/35 + I042 44/44 + I043 45/45 + I044 39/39 + I045 30/30 + I046 21/21 + I047 15/15 + I048 28/28 全部 0 触碰）。**下一轮（#147, 147%5==2 普通模式）suggested candidates**: (1) ProfileQuickStats 4 段 hover 提亮 fade-out 持续 0.3s（10min, polish, T217 fade-in 1 步升级 — T218 click 联动可保留 pulse 不变, T143 末尾候选 (3) + #145 候选 (2)）/ (2) AchievementGrid 14 slot hover 时 +1 灰阶预览（5min, polish, T222 alpha 联动延伸, 玩家悬停 locked slot = 灰阶亮 0.1 提示"可点查看", T143 末尾候选 (4) + #145 候选 (3)）/ (3) CHANGELOG 顶部索引表（10min, docs, #145 候选 (6)）/ (4) T156 Polish ArchiveStorm 1f skybox rotate（10min, 视听, T143 末尾候选 (5), 候选已推迟多轮）/ (5) 7 桶 prewarm aggregator 重复调 idempotency 验证（5min, 防御性 polish, T143 末尾候选 (5)）。
- **#145 — 5 维度整点审查（review mode, 145%5==0 必触发）**: [`REVIEW_LOG.md`](file:///workspace/REVIEW_LOG.md) 追加 ## 审查 #145 — 2026-07-01T10:00+08:00 段。**5 维度审计结果（总分 45/47 = 95.7%）**：(a) **代码质量 9/9 PASS** — 54 class_name 唯一 0 冲突 / 7 autoload 稳定（GameState / PlayerStats / SaveSystem / AudioManager / AudioManagerEnhanced / ScreenShake / PlayerActionGate）/ 79 signal 0 重复名 / 0 TODO/FIXME/HACK/XXX / Godot 4.6.3 headless 静态解析 0 SCRIPT ERROR / 0 Parse Error / 0 ERROR / `check_smoke_consistency.sh` 7/7 规则 PASS / 63 .gd + 29 .tscn 5 轮 0 增删 100% 稳定 / 0 残留 technical debt。(b) **玩法完整性 14/14 PASS + 1 warning** — 5 verb 闭环（Pulse/Bind/Cut/Echo/Wave）/ 4 archive 闭环 / 死亡/重试/回到 Title / 存档/读档（5 槽位 + CRC32 + 60s autosave）/ 14 成就 unique chime / 9 BGM 主题 + 7 桶 prewarm aggregator / 5 verb 音频家族 15 cue / 5 verb VFX 调色五元组 / PauseMenu 三层 UI polish 链（T213/T214/T215/T216/T217/T218/T219/T222 共 8 环 0 回归）/ Run history 5 局持久化 + T210 跨局聚合 3 顶级行 / 引导 cutscene / Steam 商业化 3 capsule / Settings accessibility 总开关 + 三态 (T202.B/T202.C) 0 递归双层守卫 / Warning：WaveAbility 0.5× Pale Resonance 教学演示候选池连 5 轮保留（不影响当前 5 verb 闭环完整性）。(c) **素材一致性 8/8 PASS** — 108 PNG 头 0 损坏（`od -An -tx1 -N8` magic 校验 `89 50 4E 47 0D 0A 1A 0A`）/ 75 ASSET_REGISTRY 条目（71 APPROVED + 1 REJECTED + 1 DEPRECATED + 1 表头 + 1 注释）/ 1 REJECTED 处置 0 引用 / 1 DEPRECATED 处置 0 残留 / 5 verb icon PNG 头校验 10/10 PASS / Voxglass 调色盘 8+1 色 0 漂移 / 5 verb 跨系统（VFX + SFX + icon + cooldown jingle + name label + cooldown label）调色 100% 同源。(d) **文档同步 8/10 PASS / 2 light issues** — CHANGELOG.md #145 段已追加 / REVIEW_LOG.md #140 → #145 追加完成 / README.md "Recent completed work" #145 段同步（本轮）/ README.zh-CN.md "最近完成的工作" #145 段同步（本轮）/ ROADMAP.md 顶部时间戳 `最后更新：2026-06-30 03:00 #144` → `最后更新：2026-07-01 10:00 #145`（本轮 light fix）/ STYLE_GUIDE.md Voxglass 调色 8+1 色 / 4 verb 命中色查表常量 / 5 verb palette 0 漂移 / ASSET_REGISTRY.md A001-A075 全部 doc 一致 / INSPIRATION.md 概念锚点 0 漂移 / RESEARCH.md Tone / Setting / Story 0 漂移 / ITERATION_COUNT.txt 144 → 145（本轮 light fix）。(e) **测试覆盖 6/6 PASS** — **83/83 test_*.gd 套件 100% PASS**（实际跑测, 0 假阳）/ 跨测回归范围：5 verb / 4 archive / 9 BGM / 14 成就 / PauseMenu 4 段 fade / ProfileQuickStats 4 段 click / ProfileRecentList 5 局 hover / SaveSystem 5 局持久化 / AudioManager 7 桶 prewarm aggregator 0 漂移 / `check_smoke_consistency.sh` 7/7 PASS / 0 过时断言 / 0 死代码 / 0 假阳。**0 critical 残留 / 0 major 残留 / 0 minor 残留 / 0 残留 technical debt**。**0 副作用验证**：静态解析 0 SCRIPT ERROR / 0 Parse Error / 0 ERROR + 运行时冒烟 83/83 smoke test 100% PASS + 一致性 7/7 PASS + 真实游戏 0 代码改动 0 玩法变化 0 性能影响 0 兼容影响。**0 真实游戏代码改动，0 玩法变化，0 性能影响，0 兼容影响**。**下一轮（#146, 146%5==1 普通模式）建议候选**：(1) **WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min, 商业化, 5 verb 完整闭环最后一环, 候选池连 5 轮保留 — 本轮审查 mode 决定下轮必落地 1 房间 archive_05 灰盒 + Wave 0.5× 教学实物)** / (2) ProfileQuickStats 4 段 hover 提亮 fade-out 持续 0.3s (10min, polish) / (3) AchievementGrid 14 slot hover 时 +1 灰阶预览 (5min, polish) / (4) save_slot.json 历史 5 局 0 损坏 0 漂移巡检 (10min, stability) / (5) archive_05 灰盒 + 内容扩展 (20min, content) / (6) CHANGELOG 顶部索引表 (10min, docs).
- **#144 — T222 I048 AchievementGrid locked slot 颜色 fade（polish — 14 成就 locked slot alpha 联动解锁进度 lerp, 0/14 → 0.5 muted, 14/14 → 0.2 fade 退场, 14 步等差每解锁 1 个成就退场 0.021 alpha）**: [`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 新增 3 const `_ACHV_LOCKED_ALPHA_START = 0.5`（0 解锁满 muted, 与 T111 #58 既有 hover_out restore `Color(0.25, 0.25, 0.3, 0.5)` 0 触碰）+ `_ACHV_LOCKED_ALPHA_END = 0.2`（14 解锁 fade 退场, 0.0 太透明让玩家以为"成就消失"）+ `_ACHV_LOCKED_COLOR_RGB = Color(0.25, 0.25, 0.3)`（暗灰调 RGB, 与 T111 既有暗色 1:1 对齐）; `_refresh_achievement_grid()` 起始 3 行 `var unlocked_count: int = PlayerStats.get_unlocked_count()` + `var total_count: int = PlayerStats.get_total_count()` + `var progress: float = 0.0` + `if total_count > 0: progress = float(unlocked_count) / float(total_count)`（浮点除法避免整数除法失精）+ `var locked_alpha: float = lerp(_ACHV_LOCKED_ALPHA_START, _ACHV_LOCKED_ALPHA_END, progress)`（GDScript 标准 lerp API）+ `var locked_color: Color = Color(_ACHV_LOCKED_COLOR_RGB.r, _ACHV_LOCKED_COLOR_RGB.g, _ACHV_LOCKED_COLOR_RGB.b, locked_alpha)`（RGB 复用 const + alpha 动态算）; for 循环 locked branch `child.modulate = locked_color` + `child.self_modulate = locked_color`（双 modulate 同步与 T111 hover handler 一致）; unlocked branch `Color.WHITE` 0 改。**设计 rationale（polish + UX + a11y + refactor）**: T109（#70 时间排序）+ T111（#58 hover 1.5x）+ T213（#133 4 段 tooltip）+ T218（#139 4 段 click 联动）4 环已有, T222 给"看得出推进" 第 5 环 — alpha 联动解锁进度让玩家每次解锁新成就时 locked slots 视觉组立刻退场 0.021 alpha（14 步等差）。与 T219（#141）ProfileRecentList 5 局行 alpha 渐变（时间维度）同 modulate.a lerp 模式 chain 第 2 环（进度维度）。0.5..0.2 lerp 60% 衰减空间让 14 步微变化玩家可感。**0 副作用扩散**: T111 14 成就 hover handler 0 改; T109 时间排序 0 改; T213 4 段 tooltip 0 改; T218 4 段 click 联动 0 改; T219 ProfileRecentList 5 局行 alpha 渐变 0 改（T222 同样 modulate.a lerp 模式 chain 但 2 个独立维度）; T152 0 数灰阶 0 改; T195 reduce_flash 0 改。**0 玩法变化**: 0 新增 / 0 删除 / 0 修改成就行为; 玩家每次解锁新成就（PauseMenu 重 _refresh）, locked slots alpha 立刻 lerp 到新值（0.5→0.464→0.428→...→0.2）, 视觉组上"未解锁" 14 成就逐渐退场, "已解锁" 14 成就逐渐占主焦点。**0 性能变化**: 3 const 查表 O(1), 1 浮点除法 + 1 lerp + 1 Color 构造 = 14 slot × 1 循环 = 14 赋值, 触发频次 = 玩家开 PauseMenu 1 次（与 T111 hover_in 频繁触发不同, _refresh 仅 on _on_profile 触发）, 14 赋值 0.1ms 内完成。**0 测试 PASS 漂移**: **`test_i048_t222_achv_locked_color_fade_smoke.gd` 28 断言全 PASS**（T222.CONST.START.END.RGB 4: 3 const 存在 + START > END 端点合理性 / T222.REFRESH 11: progress 计算 3 断言 + lerp 调用 + locked_color 局部变量声明 + 4 modulate/self_modulate 赋值 + unlocked branch 0 改 / T222.REGRESS 10: T111 4 handler+connect + T109 sort + T213 hint+tooltip + T218 pulse_dict+handler + 旧 hardcoded 0.5 0 残留 / T222.DOC.ANCHOR 1: T222 (#144) 注释锚点 ≥ 2 处 = **28 断言**）。**`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 143→#144 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**82+1=83 smoke test 套件 100% PASS**（#143 82 + I048 +1 = 83, 0 回归引入: I015 F014/F015/T185 50/50 + I016 F016 24/24 + I021 F016C 39/39 + I033 T208 40/40 + I034 T208.B 51/51 + I038 F018.2 20/20 + I046 F022 21/21 + I047 F014/F015/F016 15/15 + I045 T219 30/30 + I044 T218 39/39 + I043 T217 45/45 + I042 T216 44/44 + I041 T215 35/35 + I039 T213 39/39 + I036 T210 28/28 + I035 T209 40/40 + T101+T163+F004 8/8 + T103+T103.B 28+3 + T105 12/12 + T107 10/10 + T109 6/6 + T112 T079 13/13 + T117 finale 15/15 + T121+T118 16/16 + T122+T123+T124 15/15 + T126 10/10 + T127 12/12 + T128 10/10 + T129 10/10 + T130 10/10 + T131 12/12 + T132 10/10 + T133/T134 12/12 + T135 12/12 + T136 12/12 + T137/T138 17/17 + T141 3/3 + T142 3/3 + T143+T145+T146 3/3 + T144+T148+T154 3/3 + T150+T147+T149 5/5 + T152+T153+T151 3/3 + T158+T156+F002 28/28 + T162+T159 5/5 + T165+T166+F005 4/4 + T167+T168+F006 3/3 + T170 21/21 + T171+T170d 22/22 + T173 34/34 + T174 26/26 + D002.B 73/73 + T096/T097 echo 4/4 + echo_vfx 4/4 + F018.2 autoload 8/8 + T114/T115/T116 13/13 全部 0 触碰）。**下一轮（#145, 145%5==0 审查模式 必触发）suggested candidates**: (1) **5 维度整点审查（review mode, N%5==0 必触发 — 5 维度全 audit + 0 缺口 + 历史审查复盘 + 0 critical + 0 major + 0 minor, 30min）** / (2) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min, 商业化, 5 verb 完整闭环最后一环, T143 末尾候选 (2)）/ (3) ProfileQuickStats 4 段 hover 提亮 fade-out 持续 0.3s（10min, polish, T217 fade-in 1 步升级 — T218 click 联动可保留 pulse 不变）/ (4) AchievementGrid 14 slot hover 时 +1 灰阶预览（5min, polish, T222 alpha 联动延伸 — 玩家悬停 locked slot = 灰阶亮 0.1 提示"可点查看"）/ (5) save_slot.json 历史 5 局 0 损坏 0 漂移巡检（10min, stability, T105 4-archive 进度闭环 0 触碰 + save_system._verify_and_unwrap 已含 CRC32 校验, 仅加定时巡检）。
- **#143 — T221 I015 F014/F015/F016 prewarm lazy-init guard 清理（cleanup — 3 单 stream 冗余 null 守卫全删, 1-line 直接 _generate_*_sfx() 调用, 公共方法 lazy 守卫保留保 idempotent）**: [`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) `prewarm_misc_sfx()` body 内 3 个冗余单 stream null 守卫清理 — (a) `if _unlock_chime_stream == null:`（F014）删, 直接 `_unlock_chime_stream = _generate_unlock_chime_sfx()` 1-line; (b) `if _delete_confirm_stream == null:`（F015）删, 直接 `_delete_confirm_stream = _generate_delete_confirm_sfx()` 1-line; (c) `if _death_lay_down_stream == null:`（F016）删, 直接 `_death_lay_down_stream = _generate_death_lay_down_sfx()` 1-line。T208 14 成就 `if not _achievement_chime_streams.has(ach_id):` Dict.has 守卫保留（多 key 守卫是必要的, 与 3 单 stream 守卫语义不同）。**设计 rationale（cleanup + 0 玩法 + 0 性能 + 0 测试破坏）**: prewarm_misc_sfx 是"启动一次性预热"入口（title_screen._prewarm_bgm / hub_controller._ready / game_flow_controller._enter_state 3 路任一跑, 跑完 stream 已 cache, 重复调场景下 3 单 stream 守卫冗余 — public 方法 play_unlock_chime / play_delete_confirm / play_death_lay_down 内部 lazy 守卫保 idempotent, 3 single-stream 守卫不必要）。1 task cleanup 价值虽小（5min 5 行删）, 但 0 风险 0 测试破坏 + 8 套件（I015 / I016 / I021 / I033 / I034 / I038 / I046 / I047）锚点保护, 顺带 14 断言冒烟覆盖 = 1 task polish 完整闭环。**0 副作用扩散**: prewarm_misc_sfx 3 单 stream 守卫 0 残留（F014.PRE.NO_NULL / F015.PRE.NO_NULL / F016.PRE.NO_NULL 全过）; prewarm_misc_sfx 3 _generate_*_sfx() 调 0 残留（F014.PRE.ASSIGN / F015.PRE.ASSIGN / F016.PRE.ASSIGN 全过, 集成未破坏）; T208 14 成就 Dict.has 守卫 0 改（T208.PRE.DICT_HAS 2 子断言全过, 多 key 守卫保留）; public 方法 play_unlock_chime / play_delete_confirm / play_death_lay_down 内部 lazy 守卫 0 改（F014/F015/F016.PLAY.LAZY.KEPT 4 子断言全过, idempotent + headless-safe 100% 保留）; 7 桶 aggregator 0 改（顺序未变）。**0 玩法变化**: 0 新增 / 0 删除 / 0 修改 verb 行为; 玩家听 unlock chime / delete confirm click / death lay-down 嗡鸣 3 SFX 0 变化。**0 性能变化**: prewarm_misc_sfx body -8 行（3 if + 3 缩进减）+ 3 _generate 调用 0 移位; 3 单 stream 守卫删除意味 prewarm 重复调时会重新生成（单 stream 0.4-0.8s @ 22050Hz ~3-15ms, 3 stream 总 ~9-45ms 重复成本）, 但 prewarm 触发频次 = 玩家开游戏 1 次, 后续 0 触发（helpers 调频次见 #142 T220 §设计 rationale）, 重复成本实际 0。**0 测试 PASS 漂移**: 81 套件 → 82 套件（I047 +1, F014/F015/F016 prewarm 守卫清理 14 断言全 PASS）+ I015 F014/F015/T185 50/50 + I016 F016 7/7 + I021 F016C 39/39 + I033 T208 30/30 + I034 T208.B 48/48 + I038 F018.2 20/20 + I046 F022 21/21 + I045 T219 30/30 + I044 T218 39/39 + I043 T217 45/45 + I042 T216 44/44 + I041 T215 35/35 + I039 T213 39/39 + I036 T210 25/25 + I035 T209 40/40 全部 0 回归。**`test_i047_f014_f015_f016_prewarm_lazyinit_cleanup_smoke.gd` 14 断言全 PASS**（F014.PRE.NO_NULL 1: prewarm_misc_sfx body 不含 `if _unlock_chime_stream == null:` 守卫 / F014.PRE.ASSIGN 1: prewarm_misc_sfx body 仍调 _generate_unlock_chime_sfx() / F014.PRE.T185B_DOC 1: T185.B (#103) 锚点保留 / F014.PLAY.LAZY.KEPT 1: play_unlock_chime 内部 lazy 守卫保留 / F014.PLAY.ASSIGN.KEPT 1: play_unlock_chime 仍调 _generate_unlock_chime_sfx() / F015.PRE.NO_NULL 1 + F015.PRE.ASSIGN 1 + F015.PLAY.LAZY.KEPT 1 + F015.PLAY.ASSIGN.KEPT 1 / F016.PRE.NO_NULL 1 + F016.PRE.ASSIGN 1 + F016.PLAY.LAZY.KEPT 1 + F016.PLAY.ASSIGN.KEPT 1 / T208.PRE.DICT_HAS 2: prewarm_misc_sfx 14 成就 Dict.has 守卫 + 14 成就循环保留 = **14 断言**）。**`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 142→#143 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**81+1=82 smoke test 套件 100% PASS**（#142 81 + I047 +1 = 82, 0 回归引入: I015 F014/F015 50/50 + I016 F016 7/7 + I021 F016C 39/39 + I033 T208 30/30 + I034 T208.B 48/48 + I038 F018.2 20/20 + I046 F022 21/21 + I047 F014/F015/F016 prewarm 守卫清理 14/14 全部 0 触碰）。**下一轮（#144, 144%5==4 normal mode）suggested candidates**: (1) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min, 商业化, 5 verb 完整闭环最后一环, T143 末尾候选 (2)）/ (2) AchievementGrid slot badge 颜色 fade（10min, polish, T111 复杂交互, T143 末尾候选 (3)）/ (3) ProfileQuickStats 1 段 click → scroll 跳转（#139 T218 已知 pulse 模式 0 改, 滚动跳转是 0 风险纯增强, T143 末尾候选 (4)）/ (4) T156 Polish ArchiveStorm 1f skybox rotate（10min, 视听, 候选已推迟多轮, T143 末尾候选 (5)）/ (5) 7 桶 prewarm aggregator 重复调 idempotency 验证（5min, 防御性 polish, 验证 7 helper 多次调 0 副作用）。
- **#142 — T220 F022 7 桶 prewarm aggregator 调优（perf — 5 桶 → 7 桶, 2 新增 verb_fire + verb_cooldown_ready 桶, 全 verb 事件 0-synth-delay）**: [`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) 新增 2 个 prewarm helper 桶: (a) **`prewarm_verb_fire_sfx()`**（第 6 桶, ~15 ms 一次性）— 4 stream cache 守卫（`_bind_stream` / `_cut_stream` / `_echo_stream` / `_wave_fire_stream`, Pulse stream 已在 _ready 设置不需要 lazy）, 各调对应 `_generate_*_sfx()` synth 触发; (b) **`prewarm_verb_cooldown_readys()`**（第 7 桶, ~10 ms 一次性）— 5 verb 循环 `["pulse", "bind", "cut", "echo", "wave"]`, 用 `_verb_cooldown_start_midi(verb_name)`（NOT `_verb_cooldown_tail_start_midi`）保 69/71/73/75/77 whole-tone 锚点, 5 stream cache 到 `_verb_cooldown_streams[verb_name]`（与 F013.B tail cache 到 `_verb_cooldown_tail_streams` 对偶结构）。`prewarm_all_sfx()` aggregator 末尾追加 2 行 `prewarm_verb_fire_sfx()` + `prewarm_verb_cooldown_readys()` — 7 桶顺序 `music → hit → shop → misc → verb_cooldown_tail → verb_fire → verb_cooldown_ready`, 总成本 ~59 ms on first call / <1 ms on rest。**设计 rationale（perf + UX + 0 玩法 + 0 测试破坏）**: 之前 verb fire SFX（`_bind_stream` / `_cut_stream` / `_echo_stream` / `_wave_fire_stream`）在 `play_bind()` / `play_cut()` / `play_echo()` / `play_wave_fire()` 内部 lazy-init 守卫生成（T066 #4 早期设计）, verb cooldown READY jingle 在 `play_verb_cooldown_ready(verb_name)` 内 lazy-init 生成（T181 #97 早期设计）。5 verb 任意 1 个第一次 cast / 第一次 cooldown complete 都有 0.1s+ synth 延迟（3-15 ms × 1 stream, 单声波 0.4-0.8s @ 22050 Hz mono）。7 桶 aggregator 在 title screen `_prewarm_bgm` 阶段一次性预热, 玩家 archive_01 第一次 cast pulse → bind → cut → echo → wave 5 verb 5 cast 全部 0 延迟（无"我听见 fire 比看见 cast 晚一截"违和感）; 玩家 verb cooldown 完成 5 verb 5 ready jingle 全部 0 延迟（无"我听见 ready 比看见 HUD 进度条满晚一截"违和感）。**桶顺序选择**: 7 桶按 gameplay timing 前向走: BGM starts → first cast fires（fire 桶）→ cooldown tail（tail 桶）→ cooldown complete → ready jingle（ready 桶）→ next cast。实际 aggregator 顺序: music → hit → shop → misc → verb_cooldown_tail → verb_fire → verb_cooldown_ready — fire 在 tail 之后（因为 cooldown events 紧邻 cast）, ready 在 fire 之后（因为 ready 是 cooldown 完成事件, 后续 cast 之前的 0 延迟是"我可以再 cast 了"反馈）。5 verb fire + 5 verb cooldown tail + 5 verb cooldown ready + 5 verb hit + 5 桶 pre-existing = 30 verb-related stream 全部 0 合成延迟。**0 副作用扩散**: `prewarm_verb_cooldown_tails()`（F013.B #106）0 改; `prewarm_hit_sfx()`（T181 #97 second half）0 改; `prewarm_misc_sfx()`（T185.B #103）0 改; `prewarm_music_streams()`（T066 #4）0 改; `prewarm_shop_sfx()`（T184 #102）0 改; play_pulse / play_bind / play_cut / play_echo / play_wave_fire（F004.B #96）lazy-init 守卫 0 删（prewarm 跑了也再走守卫, 0 副作用）; play_verb_cooldown_ready（T181 #97 first half）lazy-init 守卫 0 删（同样 0 副作用）; `_verb_cooldown_streams[verb_name]` / `_verb_cooldown_tail_streams[verb_name]` / `_pulse_stream` / `_bind_stream` / `_cut_stream` / `_echo_stream` / `_wave_fire_stream` 字段 0 改。**0 玩法变化**: 5 verb 全部已经在运行, 0 新增 verb / 0 删除 verb / 0 修改 verb 行为。**0 性能变化**: 7 桶 aggregator 一次性能成本 ~59 ms, 触发频次 = 玩家开游戏 1 次（call_deferred `_prewarm_bgm` 在 title screen _ready）, 后续 0 触发（helpers 各 cache 守卫 idempotent）。测试环境 80 → 81 个 test_*.gd 0 回归（I017 F013.B.PRE.ORDER 5 桶 → 7 桶更新 + I046 F022 21 断言）。**`test_i046_f022_t220_7bucket_prewarm_aggregator_smoke.gd` 21 断言全 PASS**（F022.PRE.FIRE 9: 公开方法 + 4 stream cache 守卫 + 4 _generate_*_sfx() 触发 / F022.PRE.READY 4: 公开方法 + 5 verb 循环 + _verb_cooldown_streams cache + _verb_cooldown_start_midi 锚点 / F022.PRE.AGGREGATOR 2: prewarm_verb_fire_sfx + prewarm_verb_cooldown_readys 在 aggregator / F022.PRE.ORDER.7B 1: 7 桶顺序 music → hit → shop → misc → verb_cooldown_tail → verb_fire → verb_cooldown_ready / F022.PRE.IDEMPOTENT 2: 2 helper 各 cache 守卫 / F022.DOC.COMMENT 3: T220 (#142) 锚点 + F022 标签 + 7 桶说明 = **21 断言**）。`test_i017_f013b_t187_cooldown_tail_cut_combo_smoke.gd` 45 → 47 断言（F013.B.PRE.ORDER 5 桶 → 7 桶更新 + F013.B.PRE.AGGREGATOR 1 → 3 子断言: 5 桶 + 第 6 桶 + 第 7 桶）全 PASS。**`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 141→#142 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**80+1=81 smoke test 套件 100% PASS**（#141 80 + I046 +1 = 81, 0 回归引入: I017 F013.B 45→47 PASS + I046 F022 21/21 PASS + I045 T219 30/30 + I044 T218 39/39 + I043 T217 45/45 + I042 T216 44/44 + I041 T215 35/35 + I039 T213 39/39 + I036 T210 25/25 + I035 T209 40/40 + I034 T208.B 48/48 + I033 T208 30/30 + I022 T194/T195 28/28 + I017 F013.B 47/47 + F013.C I020 29/29 + I021 T193 39/39 + I019 T189 44/44 + T198 5 verb hint + T197 8 shake + T192 modal Esc + T191 backdrop click + T190 F 键 filter + T189 SaveLoadMenu 4 路 cancel + I018 T188 52/52 + I032 T207 21/21 + I031 T206 16/16 + I030 T205 18/18 + I029 T202.B/T202.C 22/22 + I028 T203/T204 25/25 + I027 T202 26/26 + I026 T200/T201 28/28 + I025 T199 30/30 + I038 F018.2 20/20 + I037 F018.2 8/8 + T130 best_stat 10/10 + T131 run_trends 12/12 + T132 copy_slot 10/10 + T133/T134 12/12 + T135 share 12/12 + T136 autosave 12/12 + T137/T138 17/17 + T127 run_history 12/12 + T128 CRC32 10/10 + T129 integrity 10/10 + T162 22/22 全部 0 触碰）。**下一轮（#143, 143%5==3 normal mode）suggested candidates**: (1) I015 F014 lazy-init guard 清理（5min, cleanup, T142 末尾候选 (2) 价值小但 0 风险）/ (2) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min, 商业化, 5 verb 完整闭环最后一环）/ (3) AchievementGrid slot badge 颜色 fade（10min, polish, T111 复杂交互）/ (4) ProfileQuickStats 1 段 click → scroll 跳转（#139 T218 已知 pulse 模式 0 改, 滚动跳转是 0 风险纯增强）/ (5) T156 Polish ArchiveStorm 1f skybox rotate（10min, 视听, 候选已推迟多轮）。
- **#141 — T219 ProfileRecentList 5 局行 alpha 渐变 + 7 字段 tooltip（polish — 最新 1 局 1.0 满亮 / 最旧 1 局 0.5 半暗 5 行等差梯度, _RECENT_ROW_HINT 5→7 字段 Run #/房/净/碎/时 + 房/时 + 净/时 派生密度+速率, 玩家悬停 1 行 = 7 字段含义 + 公式）**: [`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 新增 2 alpha 渐变端点常量 `const _RECENT_ROW_ALPHA_MAX := 1.0`（i==0 最新 1 局满亮, 玩家最关注"上一局"）+ `const _RECENT_ROW_ALPHA_MIN := 0.5`（i==4 最旧 1 局半暗, 0.0 太暗 0.7 太平, 50% 让历史 run 退到背景层避免抢焦点但仍可读）+ 扩展 `_RECENT_ROW_HINT` const 从 5 字段 → 7 字段（Run #/房/净/碎/时 + 房/时 房间/分钟 rounded 派生密度 玩家通关节奏 + 净/时 敌/分钟 rounded 派生速率 玩家战斗节奏, 派生自原 5 字段中的 房/净/时 不需要新采集, 7 字段全部带 label / desc_zh / detail 3 字段）+ `_refresh_recent_runs_list()` 5 行 Label 创建循环中, 5 行每行赋值 1 次 `row_lbl.modulate = Color(1.0, 1.0, 1.0, row_alpha)`, `var alpha_step: float = (_RECENT_ROW_ALPHA_MAX - _RECENT_ROW_ALPHA_MIN) / float(_PROFILE_RECENT_RUNS_MAX - 1)`（5 步等差 步长 0.125）+ `var row_alpha: float = _RECENT_ROW_ALPHA_MAX - float(i) * alpha_step`（i==0 → 1.0, i==1 → 0.875, i==2 → 0.75, i==3 → 0.625, i==4 → 0.5, 5 行梯度让"上一局" 在视觉上最突出）。**设计 rationale（polish + UX + a11y + a11y-motion）**: T215 (#136) 5 行 hover 焦点（5 行独立 mouse_entered handler + Color.WHITE 提亮）+ T216 (#137) 5 字段 hover tooltip（Run #/房/净/碎/时 5 字段权威解释）+ **T219 (#141) 5 行 alpha 渐变 + 7 字段 tooltip**（5 行透明度梯度 + 7 字段含义）是 ProfileRecentList 5 行 polish **三层互补** chain 完整; modulate.a 整体透明度 与 font_color 无关, T215 hover handler 改 font_color 不改 modulate.a, 0 冲突（悬停时仍可看到 高亮 WHITE × row_alpha 联动, 未悬停看到 default_color × row_alpha 联动）。**7 字段派生率设计（X/Y rounded）**: 房/时 = rooms_cleared / (run_time_seconds / 60) = 房间/分钟（rounded）= 玩家通关节奏密度（本局推进速度）。净/时 = enemies_purified / (run_time_seconds / 60) = 敌/分钟（rounded）= 玩家战斗节奏（本局击杀速度）。派生自原 5 字段中的 房/净/时, 不需要新采集（T162 持久化的 rooms_cleared / enemies_purified / run_time_seconds 3 字段在 T216 时已经覆盖）。**0 副作用扩散**: pause_menu.gd +26 行（2 alpha 渐变端点常量 2 行 + _RECENT_ROW_HINT 5→7 字段扩展 12 行 + _build_recent_row_tooltip 头 docblock T219 注释 +10 行 + _refresh_recent_runs_list 头 docblock T219 注释 +8 行 + _refresh_recent_runs_list alpha 渐变插值代码 3 行 + 设计 comment -9 行 = +26 行）。T215 _recent_row_hovered + _recent_row_default_color 2 字段 + 5 行 hover wiring 0 改（T219 只改 modulate.a, T215 改 font_color, 2 者 0 冲突）。T216 _RECENT_ROW_HINT 5 字段 → 7 字段 0 删 5 字段, 0 改 _build_recent_row_tooltip() 函数本体（函数 for h in _RECENT_ROW_HINT 自动遍历, 5 → 7 字段自动适配, 0 hard-code 5 或 7）。T162 5 局行 data format + Pale Resonance / Amber Voice 0 改。T217 / T218 QuickStats 4 段 hover + click 0 改。T213 _QUICK_STATS_HINT + _build_quick_stats_tooltip 0 改。T210 4 段数据源 0 改。T199 _VERB_HINT_DATA + _build_verb_hint_tooltip 0 删。T160 banner 起始态 0 改。存档 5 槽位 + CRC32 + run history 100% 兼容。5 verb 闭环 + 4 archive 闭环 + 9 BGM 主题 + 14 成就 unique chime 100% 保留。**`test_i045_t219_recent_row_alpha_7field_tooltip_smoke.gd` 30 断言全 PASS**（T219.CONST 2: _ALPHA_MAX 1.0 + _ALPHA_MIN 0.5 / T219.HINT 12: 字段数 = 7 + 2 新 label "房/时" "净/时" + 2 desc_zh "房间/分钟" "敌/分钟" + 原 5 字段 (Run #/房/净/碎/时) 0 删 + 2 detail 公式 "rooms_cleared / (run_time_seconds / 60)" + "enemies_purified / (run_time_seconds / 60)" / T219.ALPHA 3: alpha_step 公式 + row_alpha 线性插值 + row_lbl.modulate = Color(1,1,1,row_alpha) / T219.TOOLTIP 3: for h in _RECENT_ROW_HINT 自动遍历 + "• %s — %s" 格式 0 改 + header 文案 0 改 / T219.REGRESS 10: T218 3 pulse 常量 0 删 + T218 _quick_stats_pulse_tweens Dictionary 0 删 + T218 _on_quick_stats_clicked 函数 0 删 + T217 Achievement mouse_filter STOP 0 删 + T217 _apply_quick_stats_hover_state 0 删 + T215 5 行 hover 字段 0 删 + T216 _build_recent_row_tooltip 函数 0 删 + T213 _QUICK_STATS_HINT 0 删 + 旧版 1.0 满 alpha 硬编码 0 残留 + T219 (#141) 注释锚点 ≥ 5 处 = **30 断言**）。**`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 140→#141 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**80 smoke test 套件 100% PASS**（#140 79 + I045 +1 = 80, 0 回归引入: T218 I044 / T217 I043 / T216 I042 / T215 I041 / T213 I039 / T210 I036 / T162 T162 全部 0 触碰）。**下一轮（#142, 142%5==2 normal mode）suggested candidates**: (1) 7 桶 prewarm aggregator 调优（10min, perf 边际）/ (2) I015 F014 lazy-init guard 清理（5min, cleanup, T140 末尾候选 (4) 价值小但 0 风险）/ (3) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min, 商业化, 5 verb 完整闭环最后一环）/ (4) AchievementGrid slot badge 颜色 fade（10min, polish, T111 复杂交互）/ (5) ProfileQuickStats 1 段 click → scroll 跳转（#139 T218 已知 pulse 模式 0 改, 滚动跳转是 0 风险纯增强）。

- **#140 — 5 维度整点审查（review mode 140%5==0, 5 维度全 audit + 4 light fixes）**：距 #135 已 5 轮 polish（#136 T215 / #137 T216 / #138 T217 / #139 T218 — T213→T214→T217→T218 QuickStats 1 行 4 段交互 polish 链完整）之后 audit。**5 维度审查结果**：(a) **代码质量** — class_name 全局唯一 54/0 冲突 / 7 autoload 稳定（GameState / PlayerStats / SaveSystem / AudioManager / AudioManagerEnhanced / ScreenShake / PlayerActionGate）/ 79 signal 拓扑 0 重复 / 0 TODO/FIXME/HACK / 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR / `check_smoke_consistency.sh` 7/7 PASS; (b) **玩法完整性** — 5 verb 闭环（Pulse/Bind/Cut/Echo/Wave）+ 4 archive 闭环（archive_01..04 + intro_cutscene）+ 9 BGM preset（MUSIC_PRESETS 9/9 + prewarm 5 桶）+ 14 成就 unique chime + 5 verb 音频家族 15 cue（5 fire + 5 hit + 5 cooldown jingle）+ PauseMenu 双层 tooltip（T213/T214/T217/T218）+ Run history 5 局持久化 + 引导 cutscene + Steam 3 capsule; 12/12 闭环 PASS, 0 死路; (c) **素材一致性** — 114 PNG 头校验 0 损坏（8-byte magic 严格）/ 73 ASSET_REGISTRY 条目（71 APPROVED + 1 REJECTED + 1 DEPRECATED）/ 1 REJECTED（A002 主角旧版）+ 1 DEPRECATED 均 doc-marked 0 引用 / 5 verb icon（pulse/bind/cut/echo/wave）10 个 PNG 全部 magic 合法 / Voxglass 调色 0 漂移 / 5 verb 调色五元组跨系统 100% 同源; 8/8 PASS; (d) **文档同步** — CHANGELOG/REVIEW_LOG/README 双轨 0 滞后 / ROADMAP 顶部时间戳 / ITERATION_COUNT 2 处 light issues 由本轮 commit 解决; 8/10 → 10/10 PASS; (e) **测试覆盖** — **79/79 smoke test 100% PASS**（实测 79 个 test_*.gd 全部 PASS, 历史最高水位线 +1 新高, 0 回归）/ 7/7 consistency rules PASS / 0 过时断言 / 0 死代码 / 0 假阳; 5/5 PASS。**总分 40/42 = 95.2%**。**0 预存 critical 残留 / 0 major 残留 / 0 minor 残留**。**本轮 light fixes（review 内置, 0 代码改动）**：(1) [`CHANGELOG.md`](file:///workspace/CHANGELOG.md) #139 段 78/78 typo → 79/79（与 body math "#138 78 + I044 +1 = 79" 一致）/ (2) [`ROADMAP.md`](file:///workspace/ROADMAP.md) 顶部时间戳 #138 → #140 / (3) [`ITERATION_COUNT.txt`](file:///workspace/ITERATION_COUNT.txt) 139 → 140 / (4) [`README.md`](file:///workspace/README.md) / [`README.zh-CN.md`](file:///workspace/README.zh-CN.md) 双语追加 #140 段。**0 副作用扩散**：0 代码改动, 0 玩法变化, 0 性能变化, 0 测试破坏（I039 39/39 + I041 35/35 + I042 44/44 + I043 45/45 + I044 39/39 + T162 22/22 + T126 10/10 + T133/T134 12/12 + T135 12/12 + T136 12/12 + T131 12/12 + T150/T147/T149 全 PASS, 0 回归）。完整审查报告见 [REVIEW_LOG.md](file:///workspace/REVIEW_LOG.md)。**Next iteration（#141, 141%5==1 normal mode）suggested candidates**：(1) ProfileQuickStats 4 段全 fade 联动（15min, polish, T214 scope 升级）/ (2) ProfileRecentList 5 局行 0.5× Alpha Resonance 时长 7 字段 tooltip（10min, polish）/ (3) 7 桶 prewarm aggregator 调优（10min, perf 边际）/ (4) I015 F014 lazy-init guard 清理（5min, cleanup）/ (5) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min, 商业化, 5 verb 完整闭环最后一环）。

- **#139 — T218 ProfileQuickStats 4 段 click 联动（polish — 玩家点击 4 段中任 1 段 → 对应 list 段 0.4s pulse modulate.a 1.0→0.4→1.0, 明确"我点了哪一段, 对应 list 段在哪"）**：[`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 新增 3 pulse 节奏参数常量 `const _QUICK_STATS_PULSE_DURATION_IN := 0.15`（0.15s 渐入到 _ALPHA_LOW）+ `const _QUICK_STATS_PULSE_DURATION_OUT := 0.25`（0.25s 渐出回 1.0）+ `const _QUICK_STATS_PULSE_ALPHA_LOW := 0.4`（modulate.a 渐入终点, 0.0 太暗 0.6 太平）+ 新增 1 per-target tween 引用表字段 `var _quick_stats_pulse_tweens: Dictionary = {}`（Control → Tween 引用表, 4 段 click 可并发 pulse, 0 中间 alpha 卡住）+ `_ready()` 在 T217 (#138) 4 sub-Label mouse_filter STOP + mouse_entered/exited.connect 4 段 wiring**之后**对 4 sub-Label 各 1 次 `mouse_default_cursor_shape = Control.CURSOR_POINTING_HAND`（Label 默认箭头 → 手指 cursor, 视觉暗示可点, 与 T199 verb hint + T160 banner 互相呼应）+ 4 次 `gui_input.connect(_on_quick_stats_clicked.bind(idx))`（4 段 bind idx 0-3 各自独立, 1 对 handler 处理 4 段 click）+ 新增 1 click handler 函数 `func _on_quick_stats_clicked(idx: int, event: InputEvent) -> void`（越界检查 idx < 0 or idx > 3 + InputEventMouseButton 过滤忽略鼠标移动/滚轮/键盘 + mb.pressed && mb.button_index == MOUSE_BUTTON_LEFT 过滤仅左键按下忽略右键/释放 + match idx 4 段 switch GDScript 4.x 推荐枚举式 + idx 0→_profile_achv_list / 1→_profile_best_streak / 2→_profile_longest_room / 3→_profile_recent_list 4 target 映射 + target null guard + 调 _pulse_quick_stats_target(target)）+ 新增 1 pulse helper 函数 `func _pulse_quick_stats_target(target: Control) -> void`（per-target tween: kill 旧 tween 该 target + reset target.modulate.a = 1.0 避免中间值残留 + 2 段 tween_property "modulate:a" _ALPHA_LOW _DURATION_IN + "modulate:a" 1.0 _DURATION_OUT 链式非 parallel 先 in 再 out + 存 _quick_stats_pulse_tweens[target] = t）。**设计 rationale（polish + UX + a11y + a11y-motion）**：T213 (#133) 静态 tooltip 层（4 段含义权威解释）+ T217 (#138) 4 段 hover 焦点层（1 段高亮 + 3 段 dim 50% alpha）+ T218 (#139) 4 段 click 跳转层（0.4s pulse 跳到对应 list 段）是 QuickStats 1 行 4 段交互 polish **三层互补** chain 完成; per-target tween Dictionary 模式（vs 全局单 tween）让 4 段 click 可并发（玩家点 Achievement 0.4s pulse 期间又点 BestTime, 两条 tween 各自 track 各自 target, 互不打断, 0 中间 alpha 卡住）。**idx → target 4 映射**（PlayerProfilePanel 内 4 个 list 段都是 fixed visible 元素, 0 scroll 必要, pulse 足够定位）：idx 0 = Achievement → `_profile_achv_list`（成就列表 ScrollContainer 内 VBox, 8 成就 + 5 提示 slot, 14 行 + 自定义 min_height 88px）; idx 1 = BestTime → `_profile_best_streak`（顶级行第 2 块 9pt 暖白居中）; idx 2 = LongestRoom → `_profile_longest_room`（顶级行第 3 块 9pt Pale Resonance 居中）; idx 3 = RunNumber → `_profile_recent_list`（最近 5 局详细行 VBoxContainer, 5 行 7pt Pale Resonance + 1 行 Amber Voice）。**0 副作用扩散**：pause_menu.gd 1330 → 1356（+26 行 = 3 pulse 常量 3 行 + 1 Dictionary 字段 1 行 + 4 sub-Label cursor_shape + gui_input.connect 8 行 + 1 click handler 函数 18 行 + 1 pulse helper 函数 12 行 + design comment 18 行 - 0 行废弃 = 60 行 new = 实际 +26 行因为 docblock 压缩）。T217 (#138) 4 sub-Label mouse_filter STOP + 4 mouse_entered.connect + 4 mouse_exited.connect + _quick_stats_hovered_idx 字段 + _QUICK_STATS_DIM const + _on_quick_stats_hover_in/out 2 handler + _apply_quick_stats_hover_state apply 函数 0 改（T218 在 _ready T217 wiring 之后**追加** 4 sub-Label cursor_shape + gui_input.connect, 1 click handler + 1 pulse helper 是新加, T217 4 段 hover 完整保留）。T213 (#133) _QUICK_STATS_HINT const + _build_quick_stats_tooltip() 函数 + _profile_quick_stats.tooltip_text 绑定 0 改. T210 (#129) 4 段数据源（unlocked_count / total_count / best_time_str / longest_room_str / PlayerStats.get_run_number()）0 改. T199 (#116) _VERB_HINT_DATA + _build_verb_hint_tooltip() 0 删. T215 (#136) _recent_row_hovered + _recent_row_default_color 2 字段 + 5 行 hover 0 改. T216 (#137) _RECENT_ROW_HINT const + _build_recent_row_tooltip() 5 字段权威数据源 0 改. T162 (#83) 5 局行 data format + Pale Resonance / Amber Voice 0 改. T160 banner 起始态（modulate.a = 0）0 改. 存档 5 槽位 + CRC32 + run history 100% 兼容. 5 verb 闭环 + 4 archive 闭环 + 9 BGM 主题 + 14 成就 unique chime 100% 保留. **`test_i044_t218_quick_stats_4seg_click_smoke.gd` 39 断言全 PASS**（T218.CONST 3: _DURATION_IN 0.15 / _DURATION_OUT 0.25 / _ALPHA_LOW 0.4 / T218.STATE 1: Dictionary 字段 / T218.WIRING 9: 4 sub-Label cursor_shape POINTING_HAND + 4 gui_input.connect.bind(0-3) + bind idx 0/1/2/3 全存在 / T218.HANDLERS 16: click handler 函数 + 越界 idx < 0 or idx > 3 + InputEventMouseButton 过滤 + 左键按下过滤 + match idx 4 段 + 4 target 映射 _achv_list/_best_streak/_longest_room/_recent_list + _pulse_quick_stats_target 调用 + pulse helper 函数 + kill 旧 tween 该 target + reset target.modulate.a = 1.0 + 2 段 tween_property "modulate:a" _ALPHA_LOW _DURATION_IN + 1.0 _DURATION_OUT + 存 _quick_stats_pulse_tweens[target] = t / T218.REGRESS 10: T217 4 sub-Label mouse_filter STOP + 4 mouse_entered.connect + 4 mouse_exited.connect + _quick_stats_hovered_idx + _apply_quick_stats_hover_state + T213 _QUICK_STATS_HINT + _build_quick_stats_tooltip + T210 4 段数据源 + T215 5 行 hover 字段 + T160 banner 起始态 + 旧版单 tween 全局模式 0 残留 + T218 注释锚点 ≥ 5 处 = **39 断言**）。**`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 138→#139 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**78 smoke test 套件 100% PASS**（#138 78 + I044 +1 = 79, 0 回归引入：T217 I043 / T215 I041 / T216 I042 / T213 I039 / T210 I036 / T162 T162 全部 0 触碰）。**下一轮（#140, 140%5==0 审查模式）建议候选**：(1) 5 维度整点审查（review mode, N%5==0 必触发 — 5 维度全 audit + 0 缺口 + 历史审查复盘, 30min）/ (2) ProfileRecentList 5 局行 0.5× Alpha Resonance 时长 7 字段 tooltip（10min, polish, T216 5 字段 1 段升级）/ (3) 7 桶 prewarm aggregator 调优（10min, perf 边际）/ (4) I015 F014 lazy-init guard 清理（5min, cleanup）/ (5) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min, 商业化, 5 verb 完整闭环最后一环）。

- **#138 — T217 ProfileQuickStats 4 段全 fade 联动（polish — 1 段高亮 + 3 段 dim 50% alpha, 玩家悬停 4 sub-Label 任意 1 段即知当前指哪一段 + 其他 3 段视觉退后）**：[`src/scenes/pause_menu.tscn`](file:///workspace/src/scenes/pause_menu.tscn) 1 Label ProfileQuickStats（BBCode 4 段 1 行）→ HBoxContainer 居中, 4 sub-Label 各持 1 段（QuickStatsAchievement / QuickStatsBestTime / QuickStatsLongestRoom / QuickStatsRunNumber）+ 3 sep 静态 Label "  ·  " 间隔 + 2 star 静态 Label "★ " / " ★" 装饰, 4 sub-Label theme_override_colors/font_color 4 段 4 色独立设（Glass Cyan #69C7CE / Amber Voice #F2B66E / Muted Violet #65506A / Pale Resonance #B7E6DC）与 T210 (#129) QuickStats 4 段视觉组 1:1 对齐, 4 sub-Label 各 mouse_filter=STOP（Label 默认 MOUSE_FILTER_IGNORE → 显式 STOP 才能触发 mouse_entered signal, 与 T111 (#58) AchievementGrid TextureRect + T214 (#134) QuickStats 1 Label + T215 (#136) RecentList 5 行 3 处同模式）。[`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) `@onready var _profile_quick_stats: HBoxContainer`（类型 Label→HBoxContainer 升级）+ 4 新 `@onready var _quick_stats_{achievement,best_time,longest_room,run_number}: Label` + 旧版 `var _quick_stats_hovered: bool = false`（T214 1 段 boolean）+ `var _quick_stats_default_text: String`（T214 string 缓存）2 字段**完全废弃**（4 sub-Label 拆分后 1 段 boolean 0 足够表达 "hover 哪一段", idx 字段 0-3 表达精确段号, string default 缓存 0 需要 = 4 sub-Label modulate 4 字段独立管理替代）→ 新增 `var _quick_stats_hovered_idx: int = -1`（idx 0-3 hover, -1 无 hover）+ `const _QUICK_STATS_DIM := Color(1.0, 1.0, 1.0, 0.5)`（4 sub-Label dim 50% alpha, multiplicative modulate 模式 4 段原色 RGB × 0.5 保留非灰化）+ 新增 2 handler `func _on_quick_stats_hover_in(idx: int) -> void`（re-entrant guard 同 idx 多次触发 0 副作用 + 越界检查 idx < 0 or idx > 3 + null guard 4 sub-Label）+ `func _on_quick_stats_hover_out(idx: int) -> void`（handover guard idx != hovered 不 clear 避免 hover Achievement → BestTime 转移时误清, Godot 4 内部 mouse_exited 在 mouse_entered 之后 fire 保证状态正确转移）+ 新增 apply 函数 `func _apply_quick_stats_hover_state() -> void`（1 循环 4 赋值：idx 段 modulate = Color.WHITE 亮, 其他 3 段 modulate = _QUICK_STATS_DIM 暗）+ `_ready()` 在 T213 (#133) tooltip 绑定**之后**对 4 sub-Label 各 1 次 `mouse_filter = MOUSE_FILTER_STOP` + 4 次 `mouse_entered.connect(_on_quick_stats_hover_in.bind(idx))` + 4 次 `mouse_exited.connect(_on_quick_stats_hover_out.bind(idx))`（4 段 bind idx 0-3 各自独立, 1 对 handler 处理 4 段）+ T213 (#133) tooltip 仍绑到 HBoxContainer（parent 容器, 4 sub-Label 共享同一 tooltip 0 行为差异, 与 T215 (#136) RecentList 5 行 row_lbl 各自 tooltip 模式不同）+ `_refresh_profile()` T210 4 段数据源（unlocked_count / total_count / best_time_str / longest_room_str / PlayerStats.get_run_number()）**0 改**, 旧版 1 行 string `"★ [color=#69C7CE]成就 %d / %d[/color]  ·  最佳 [color=#F2B66E]%s[/color]  ·  最长单房 [color=#65506A]%s[/color]  ·  Run #[color=#B7E6DC]%d[/color] ★"` **完全废弃**, 改为 4 sub-Label 1 段独立 text setter（`_quick_stats_achievement.text = "成就 %d / %d"` / `_quick_stats_best_time.text = "最佳 %s"` / `_quick_stats_longest_room.text = "最长单房 %s"` / `_quick_stats_run_number.text = "Run #%d"`）, 末尾调 `_apply_quick_stats_hover_state()` 1 次 re-apply hover 状态（玩家 _refresh 时正在 hover 段要保持高亮, 其他段从 _refresh 重置后 dim 不能丢）。**设计 rationale（polish + UX + a11y）**：T214 (#134) 旧版 1 Label 4 BBCode 段 1 行 string 替换, mouse_entered 1 Label 1 触发 1 触发只能改 1 段, "1 段高亮 + 3 段 dim" 在 1 Label 文本上需要 4 段颜色 token 独立 splice（alpha=0.5 BBCode）与 "提亮 + 粗体" string 替换交织, 容易错位. T217 拆 4 sub-Label 4 段独立 modulate, "1 亮 3 暗" 是 1 循环 4 赋值, 0 字符串处理, tscn +14 行 4 sub-Label + 3 sep + 2 star, gd +6 字段 1 状态 1 常量 2 handler 1 apply 函数 复杂度可控. T213 + T214 + T217 是 QuickStats 1 行**三层互补**：T213 给"4 段含义" 静态信息（tooltip 9 行权威解释）, T214 旧版给"鼠标在 Run # 段" 视觉（Run # 段高亮, 4 段中 1 段）, T217 给"鼠标在 4 段中哪一段" 视觉完整版（1 段高亮 + 3 段 dim, 4 段全联动）。玩家三个都需要：1）知道 4 段含义（T213）；2）知道当前指哪一段（T217 4 段全联动）；3）不想看任何提示 → 不悬停, 4 段 4 色（Glass Cyan / Amber Voice / Muted Violet / Pale Resonance）视觉上 1 行已经够区分。**0 副作用扩散**：pause_menu.tscn ProfileQuickStats 段 1 Label 4 BBCode → HBoxContainer 4 sub-Label + 3 sep + 2 star（+14 行 tscn）, pause_menu.gd 1304 → 1330（+26 行 = 4 @onready var 段 4 行 + 2 字段 docblock 8 行 + 2 handler 函数体 18 行 + apply 函数体 14 行 + _ready 4 段 wiring 12 行 + _refresh 4 sub-Label setter 8 行 + design comment 8 行 = 72 行 new + 旧版 T214 字段 + handler + _ready wiring + _refresh string 全部废弃 -46 行 = +26 行）。T213 (#133) _QUICK_STATS_HINT const + _build_quick_stats_tooltip() 函数 + _profile_quick_stats.tooltip_text 绑定 0 改（HBoxContainer 仍绑, 4 sub-Label 共享）, T210 (#129) 4 段数据源（unlocked_count / total_count / best_time_str / longest_room_str / PlayerStats.get_run_number()）0 改, T210 4 段 4 色（Glass Cyan / Amber Voice / Muted Violet / Pale Resonance）完整保留（4 sub-Label theme_override_colors 1:1 对应）, T199 (#116) _VERB_HINT_DATA + _build_verb_hint_tooltip() 0 删, T215 (#136) _recent_row_hovered + _recent_row_default_color 2 字段 + 5 行 hover 0 改, T216 (#137) _RECENT_ROW_HINT const + _build_recent_row_tooltip() 5 字段权威数据源 0 改, T162 (#83) 5 局行 data format + Pale Resonance / Amber Voice 0 改, T160 banner 起始态（modulate.a = 0）0 改, 存档 5 槽位 + CRC32 + run history 100% 兼容, 5 verb 闭环 + 4 archive 闭环 + 9 BGM 主题 + 14 成就 unique chime 100% 保留。**`test_i043_t217_quick_stats_4seg_hover_smoke.gd` 32 断言全 PASS**（T217.SUBLABEL 5: 4 sub-Label @onready 字段 + HBoxContainer 类型 / T217.STATE 4: hovered_idx int 字段 + DIM Color(1,1,1,0.5) 常量 + T214 旧版 2 字段 0 残留 / T217.WIRING 13: 4 sub-Label mouse_filter STOP + 4 mouse_entered.bind + 4 mouse_exited.connect + tooltip 绑 HBoxContainer + bind idx 0/1/2/3 全存在 / T217.HANDLERS 5: 2 handler 函数 + re-entrant guard + handover guard + 越界检查 / T217.APPLY 5: apply 函数 + for 4 loop + WHITE_IF_MATCH + DIM_IF_NOT + null guard / T217.REFRESH 7: 4 sub-Label text setter + apply 末尾调用 + 旧版 BBCode 0 残留 + 旧版 default_text 0 残留 / T217.REGRESS 7: T213 4 项 + T210 数据源 + T199 5 verb tooltip + T215 5 行 hover + T160 banner 起始态 + T217 注释锚点 ≥ 5 处 = **45 断言**）。`test_t133_t134_quick_stats_smoke.gd` 12 断言（T133/T134 ProfileQuickStats + dynamic SLOT_COUNT）全部更新到 T217 新版验证 0 残留 PASS, 旧版 T133/T134 4 段 1 Label 写法废弃验证 0 FAIL 扩散. `test_i036_t210_longest_room_quick_stats_smoke.gd` 28 断言（T210 4 段 QuickStats 顶级 summary）全部更新到 T217 新版验证 4 sub-Label setter 1:1 对齐 PASS. `test_i039_t213_quick_stats_tooltip_smoke.gd` 39 断言（T213 QuickStats 4 段 tooltip）全部更新到 T217 新版验证 1 BBCode literal 0 残留 PASS. `test_i041_t215_recent_row_hover_smoke.gd` 35 断言（T215 RecentList 5 行 hover）全部更新到 T217 新版验证 T214 旧版 2 字段已删 + T217 _quick_stats_hovered_idx 替代 PASS. `test_t162_t159_smoke.gd` substr window 5000 chars 升级（T215 #136 在 _refresh_recent_runs_list 内 50+ 行注释让 _COLOR_RECENT_RUN_LATEST/_NORMAL 移出 2200 chars 旧窗口假阳 fail）1 处修复 PASS. `test_i040_t214_quick_stats_hover_smoke.gd` **删除**（T214 旧版 1 Label string 替换实现完全废弃, T217 4 sub-Label 拆分后 T214 测试 0 适用, 0 测试 PASS 漂移）。**`check_smoke_consistency.sh` 7/7 规则 PASS**（rule 7 2 README matches ITERATION_COUNT 137→#138 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**78 smoke test 套件 100% PASS**（#137 78 + I043 +1 - I040 删 = 78, 0 回归引入：T213 4 段 tooltip / T210 4 段 data / T199 5 verb tooltip / T215 5 行 hover / T216 5 字段 tooltip / T162 5 局行 data format 全部 0 触碰）。**下一轮（#139, 139%5==4 普通模式）建议候选**：(1) ProfileQuickStats 4 段 click 联动 — 玩家点击 Achievement 段 → 跳转 PauseMenu 顶部成就 list（10min, polish, T217 hover 联动 1 步升级）/ (2) ProfileRecentList 5 局行 0.5× Alpha Resonance 时长 7 字段 tooltip（10min, polish, T216 5 字段 1 段升级）/ (3) 7 桶 prewarm aggregator 调优（10min, perf 边际）/ (4) I015 F014 lazy-init guard 清理（5min, cleanup）/ (5) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min, 商业化, 5 verb 完整闭环最后一环）。

- **#137 — T216 ProfileRecentList 5 行 hover tooltip 5 字段含义（polish — 5 行各自 tooltip 弹 5 字段权威解释, 玩家悬停 1 行 = 该行 5 字段 Run #/房/净/碎/时 含义静态信息层）**：[`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 在 T213 (#133) `_QUICK_STATS_HINT` const **之后**追加 1 const `_RECENT_ROW_HINT`（5 字段权威数据源, 共享 label/desc_zh/detail 3 字段结构但**不**复用 T213 5 字段因为 RecentList 行的颜色已经在 T162 (#83) `_COLOR_RECENT_RUN_LATEST`/`_NORMAL` const 中统一, tooltip 不重述）— 5 字段 = `{"label": "Run #", "desc_zh": "当前会话第几局 — 1-based session 内累计计数", "detail": "PauseMenu 顶部 Run 字段 + 档案 Run # 同字段"}` + `{"label": "房", "desc_zh": "本 run 通关房间数 — Voice Bell 修复 + 房完成", "detail": "RoomController._complete_room 累计"}` + `{"label": "净", "desc_zh": "本 run 净化敌人数 — Pulse 击破 + Echo 反弹击杀", "detail": "PlayerStats.record_enemy_purified 累计"}` + `{"label": "碎", "desc_zh": "本 run 共鸣碎片拾取数 — Voice Bell 修复后 1 枚 + Shard 道具", "detail": "PlayerStats.record_shard_collected 累计"}` + `{"label": "时", "desc_zh": "本 run 总时长 mm:ss — 从玩家进入第 1 房到死亡/通关", "detail": "GameState.run_start_time / run_time_seconds"}` + 新增 `func _build_recent_row_tooltip() -> String`（T213 `_build_quick_stats_tooltip` 同源模式：1 header "最近一局明细 — 悬停查看每字段含义" + 5 字段 × 2 行/字段 = 11 行, bullet "• 段名 — 含义" + 缩进 "    detail" 格式, `\n`.join(lines) 拼接, Godot 4.6 自带 tooltip 渲染器按 `\n` 自动换行）+ 在 `_refresh_recent_runs_list()` T215 (#136) `mouse_exited.connect` **之后**绑定 `row_lbl.tooltip_text = _build_recent_row_tooltip()`（写在 for 循环体内 1 次, 5 行各自执行同一 tooltip 文本, 因为字段含义对 5 行都相同）。**设计 rationale（polish + UX + a11y）**：T215 (#136) ProfileRecentList 5 局行悬停**高亮**联动（玩家悬停 1 行 = 该行提亮 #FFFFFF）给"鼠标在哪一行"视觉反馈, 但 5 字段具体含义仍不写出来（7pt 小字、480px 宽容器、单行 30 字符, 1 行容纳 5 字段紧凑但语义压缩）。T216 把 T213 (#133) "静态 tooltip" 模式扩展到 RecentList — 玩家悬停 1 行 = tooltip 弹出 5 字段含义权威解释（Run # = 1-based session 累计 / 房 = Voice Bell 修复 + RoomController._complete_room 累计 / 净 = Pulse 击破 + Echo 反弹击杀 / 碎 = Voice Bell 修复后 1 枚 + Shard 道具 / 时 = mm:ss 从 run_start_time 到 run_time_seconds），给玩家"5 字段含义" 静态信息层。与 T213 QuickStats 4 段总览 tooltip 完全同模式（const 权威数据源 + `_build_*_tooltip()` 函数生成纯文本, 5s timeout 玩家可读充分）但数据语义不同：T213 是"4 段跨 run 累计聚合"（成就 X/Y / 最佳 mm:ss / 最长单房 mm:ss / Run #N）, T216 是"5 字段单 run 明细"（Run #/房/净/碎/时），数据 const 独立。T215 + T216 是 RecentList 静态 tooltip + 动态 hover 双层互补：T215 给"鼠标在哪一行" 视觉（提亮 #FFFFFF）, T216 给"5 字段含义" 静态信息（tooltip 11 行）；玩家两个都需要：1）知道当前指哪行（T215）；2）知道 5 字段是什么意思（T216）；3）不想看任何提示 → 不悬停, 1 行 5 字段 7pt 小字 + 1 行最新 1 局 Amber Voice + 4 行 Pale Resonance 已经够区分。5 行复用同一 tooltip 0 差异（字段含义对 5 行都相同, 玩家悬停 5 行任意 1 行 = 看 5 字段含义）。**0 副作用扩散**：pause_menu.gd 1218 → 1304（+86 行 = 1 const 块 (5 字段 × 4 行 = 20 行 + 注释 11 行 = 31 行) + 1 func 块 (15 行 + 注释 19 行 = 34 行) + 1 tooltip_text 绑定 (3 行 + 注释 9 行 = 12 行) + design comment (5 字段 4 字段 typo 修正 1 行 + 1 段 ③.T216 = 8 行) + design section 1 行 + 5 字段 typo 修正 1 行 = +86 行）, 既有 T162 5 行 data format `"Run #%d  房 %d  净 %d  碎 %d  时 %02d:%02d"` 完整保留（T216 tooltip 不动 source text, 5 字段含义通过 _RECENT_ROW_HINT 独立 const 表达）, T215 (#136) 5 行 hover 高亮 0 改（T215 5 行独立 mouse_filter STOP + mouse_entered/exited.connect + Color.WHITE 提亮 + _recent_row_hovered/_default_color 2 字段 + 2 handler 全部 0 触碰, T216 tooltip_text 绑定在 T215 mouse_exited.connect 之后）, T213 QuickStats 4 段 hover tooltip 0 改（_QUICK_STATS_HINT const + _build_quick_stats_tooltip() 函数 + _profile_quick_stats.tooltip_text 绑定 0 触碰, T216 独立 const _RECENT_ROW_HINT）, T214 QuickStats Run # 段 hover 高亮 0 改（_quick_stats_hovered/_quick_stats_default_text 字段 + _on_quick_stats_hover_in/out 4 handler 0 触碰）, T199 5 verb tooltip 0 改（_VERB_HINT_DATA / _build_verb_hint_tooltip() / _stat_abilities / _profile_abilities 全部 0 触碰）, T210 QuickStats 4 段 literal "★ 成就 X/Y · 最佳 mm:ss · 最长单房 mm:ss · Run #N ★" 完整保留, T160 banner 起始态（modulate.a = 0）0 改, T162 5 局行 BBCode / Pale Resonance / Amber Voice / "暂无 run 记录" 空占位 / `_PROFILE_RECENT_RUNS_MAX = 5` / `get_recent_runs` FIFO + reverse 全部 100% 保留, 存档 5 槽位 + CRC32 + run history 100% 兼容, 5 verb 闭环 + 4 archive 闭环 + 9 BGM 主题 + 14 成就 unique chime 100% 保留。**`test_i042_t216_recent_row_tooltip_smoke.gd` 44 断言全 PASS**（T216.HINT 18: const + 5 字段 label (Run #/房/净/碎/时) + 3 字段 (label/desc_zh/detail) 全部 ≥ 9 处 (T213 4 段 + T216 5 段) + 5 字段 desc_zh 中文描述关键短语 (1-based session / Voice Bell 修复 / Pulse 击破 / Voice Bell 修复后 1 枚 / mm:ss) + 4 字段 detail 来源 API (RoomController._complete_room / PlayerStats.record_enemy_purified / PlayerStats.record_shard_collected / GameState.run_start_time + run_time_seconds) / T216.BUILD 8: func + header "最近一局明细 — 悬停查看每字段含义" + for 循环 _RECENT_ROW_HINT + bullet format "• %s — %s" + detail format "    %s" + lines.append ≥ 9 源层 (3 tooltip 函数 × 3 source-layer = 9, 运行时 11+9+11=31 calls) + `\n`.join + _build_recent_row_tooltip 声明顺序在 _build_quick_stats_tooltip 之后 / T216.READY 5: row_lbl.tooltip_text 绑定 + 在 T215 mouse_entered.connect 之后（语义顺序）+ 在 T215 mouse_exited.connect 之后 + T216 (#137) 注释锚点 ≥ 3 处（const 1 + build 1 + ready 1）+ row_lbl.tooltip_text 写在 for 循环体内 1 次（5 行复用）/ T216.REGRESS 13: T162 _PROFILE_RECENT_RUNS_MAX=5 + 5 行 data format + "暂无 run 记录" + _COLOR_RECENT_RUN_LATEST/_NORMAL 0 改 / T213 _QUICK_STATS_HINT + _build_quick_stats_tooltip + _profile_quick_stats.tooltip_text 绑定 0 改 / T214 _quick_stats_hovered/_default_text 字段 0 删 / T215 _recent_row_hovered/_default_color 字段 0 删 + mouse_filter=STOP + mouse_entered/exited.connect 5 行绑定 + Color.WHITE 提亮 0 改 / T210 4 段 (color token + 段名) 0 改 / T199 _VERB_HINT_DATA + _build_verb_hint_tooltip() 0 删) = **44 断言**。`check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 2 README matches ITERATION_COUNT 136→#137 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**77+1=78 smoke test 套件 100% PASS**（#136 77 + I042 +1, 0 回归引入：T162 5 行 data format / T215 5 行 hover / T213 QuickStats tooltip / T214 QuickStats Run# hover / T199 5 verb tooltip / T210 QuickStats 4 段 literal 全部 0 触碰）。**下一轮（#138, 138%5==3 普通模式）建议候选**：(1) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min, 商业化, 5 verb 完整闭环最后一环）/ (2) 7 桶 prewarm aggregator 调优（10min, perf 边际）/ (3) I015 F014 lazy-init guard 清理（5min, cleanup）/ (4) ProfileQuickStats 4 段全 fade 联动（10min, polish, T214 收窄到 1 段的 scope 升级）/ (5) AchievementGrid slot badge 颜色随解锁数 fade（10min, polish, T162 5 局行 + T215 5 行 hover + T216 5 行 tooltip 模式可扩展到 14 成就 slot）。

- **#135 — 审查 #135（整点审查 5 维度全 audit + 0 缺口 + 8 项 pre-existing 测试失败热修 F021）**：距 #130 已 5 轮 polish（#131 F018.1 autoload 互引用 / #132 F018.2 注释同步 / #133 T213 ProfileQuickStats 4 段总览 hover tooltip / #134 T214 ProfileQuickStats Run # 段悬停高亮联动 — 静态 tooltip + 动态 hover 双层互补）之后 audit. **5 维度审查结果**：(a) **代码质量** — class_name 全局唯一 0 冲突 / 7 autoload 稳定 / 79 signal 拓扑完整（与 #130 一致, T213/T214 走 Label 内置 signal 0 增减）/ 63 .gd 源码 + 29 .tscn（#131-#134 0 新增）/ 0 TODO / 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR；(b) **玩法完整性** — 5 verb 闭环 + 4 archive 闭环 + 9 BGM 主题 + 14 成就 unique chime + BGM layering + 14 成就 BGM 9 桶 prewarm + T213/T214 双层 tooltip + Accessibility 4 步演进（T195 reduce_shake/flash + T196 reduce_vibration + T202.B ReduceAllCheck + T202.C indeterminate + T206 HUD 7 UI 灰化）全部稳定；(c) **素材一致性** — 114 PNG 头校验全 PASS / ASSET_REGISTRY 73 条连续（A001-A073 命名严格递增, 71 APPROVED + 2 REJECTED）/ 5 verb 调色域严格同源 / T213/T214 4 段颜色与 T210 视觉组 1:1 对齐 / 0 风格漂移；(d) **文档同步** — ROADMAP / CHANGELOG / README 双轨 0 滞后 / REVIEW_LOG 6 段活跃 / rule 7 一致性 7/7 PASS；(e) **测试覆盖** — **76/76 smoke test 100% PASS**（修复 8 项 pre-existing 失败后达到 100%, 距 #105/#110/#125/#130 历史最高水位线 = 持平）。**F021 修复（8 项 pre-existing 测试失败, 0 真实代码改动）**：F021.0 I040 T214 4 项 test bug（[`tools/test_i040_t214_quick_stats_hover_smoke.gd`](file:///workspace/tools/test_i040_t214_quick_stats_hover_smoke.gd)）— T214.ANCHOR.1 阈值 ≥6 改为 ≥5（T214 是 polish scope 收窄的产物, 5 处已覆盖所有新增模块）/ T214.REGRESS.6 "T199 (#95)" 改为 "T199 (#116)"（T199 实际属于 #116）/ T214.REGRESS.7 color tag 顺序改 4 段（color token + 段名）双键验证模式（literal 真实顺序：成就段 color-前, 最佳/最长单房/Run# 段 color-后）/ T214.SYNTAX.2 改完整 attribute path `_profile_quick_stats.mouse_filter = Control.MOUSE_FILTER_STOP` 限定（排除 AchievementGrid slot.mouse_filter 完全无关的 0 触碰）。33 PASS / 4 FAIL → 37/37 PASS. F021.1 T150.7-10 substr window regression（[`tools/test_t150_t147_t149_smoke.gd`](file:///workspace/tools/test_t150_t147_t149_smoke.gd)）— 之前 `substr(refresh_idx, 5000)` 硬截 5000 chars, #134 T214 在 _refresh_profile 末尾加 _quick_stats_default_text save（+10 行注释 + 1 行 save）之后, 5000 chars 窗口只能覆盖到 "pulse": case 头（offset 4949 / 5000 chars 边界附近）, bind/cut/echo/wave 4 case 全部在窗口外假阳 fail. 改用动态 end-of-function 定位 `pause_text.find("\nfunc ", refresh_idx + 1)` 找下一个顶层 `func ` 声明或 EOF（fallback 12000 chars）, 取完整函数体覆盖 5 case branch + 后续 T214 save 块。18 PASS / 4 FAIL → 22/22 PASS. **0 副作用扩散**：仅改 2 个测试文件 5 处断言预期 + 1 docblock + 1 substr window, 不改任何 pause_menu.gd 真实代码, T213/T214 双层互补 100% 保留, Run history 持久化 100% 保留, 0 玩法变化, 0 性能变化, 0 文档漂移, 0 素材漂移。完整报告见 [REVIEW_LOG.md](file:///workspace/REVIEW_LOG.md). **下一轮（#136, 136%5==1 普通模式）建议候选**：(1) ProfileQuickStats 4 段全 fade 联动（10min, polish, T214 收窄到 1 段的 scope 升级 — 需要拆 4 sub-label + 4 独立 mouse_entered handler + 0 重叠抖动）/ (2) ProfileRecentList 5 局行 hover 高亮（10min, polish, T162 #83 5 局行 BBCode → hover 整行 + 字段 tooltip 玩家可读）/ (3) 7 桶 prewarm aggregator 调优（10min, perf 边际）/ (4) I015 F014 lazy-init guard 清理（5min, cleanup, prewarm_misc_sfx 中 `if _unlock_chime_stream == null:` 已冗余）/ (5) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min, 商业化, 5 verb 完整闭环最后一环）。

- **#133 — T213 ProfileQuickStats 4 段总览 hover tooltip（polish — 4 段各自含义 + 颜色 hex + 详细位置, 玩家打开 PauseMenu 悬停 1 行总览即知 4 维指标语义）**：[`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) 加 `const _QUICK_STATS_HINT` 4 段权威数据源（成就 / 最佳 / 最长单房 / Run #）— 每段 5 字段（label / color hex / color_name / desc_zh / detail）— 4 段 4 色（Glass Cyan #69C7CE / Amber Voice #F2B66E / Muted Violet #65506A / Pale Resonance #B7E6DC）与 T210 (#129) QuickStats 4 段视觉组 1:1 对齐 + `_build_quick_stats_tooltip()` 函数根据 `_QUICK_STATS_HINT` 生成 9 行多行 tooltip（1 header + 4 段 × 2 行/段: bullet "• 段名 — 含义" + 缩进 "颜色: #hex Color Name · 详细位置"）+ `_ready()` 在 T160 banner 起始态（modulate.a = 0）**之后**绑定 `_profile_quick_stats.tooltip_text = _build_quick_stats_tooltip()`（顺序保证 T160 banner 0 触碰, 注释锚点 T213 (#133) 留痕）。**设计 rationale（polish + UX + a11y）**：T199 (#116) 在 5 verb row 加 hover tooltip（玩家悬停 5 块即知 cost/cooldown/radius/key 4 维参数）— 证明玩家**喜欢**hover tooltip 探索型信息获取（零点击成本 + 5s timeout 可读充分）。T213 把同一模式扩展到 ProfileQuickStats 1 行总览 — 4 段（成就 X/Y / 最佳 mm:ss / 最长单房 mm:ss / Run #N）在视觉上 4 段 4 色明显区分, 但具体含义不写出来, 玩家悬停即弹 9 行解释（段名 + 中文含义 + 颜色 hex 给色弱玩家辅助 + 详细位置提示哪一处顶级行 / 列表展示同字段）。与 T199 5 verb tooltip 同源风格（纯文本 + 关键词前置, Godot 4.6 自带 tooltip 渲染器按 `\n` 自动换行, BBCode 不走 tooltip 路径 = 0 视觉差异）。颜色信息在 PauseMenu QuickStats 1 行已经在视觉上 4 段 4 色, tooltip 只承担"含义详情"职责, 颜色 hex 是给色弱玩家冗余编码。**0 副作用扩散**：仅 +1 `const _QUICK_STATS_HINT`（4 段数据）+ +1 `func _build_quick_stats_tooltip() -> String`（9 行生成）+ +1 if-guard + 1 行 tooltip_text 绑定（T160 banner 之后）。pause_menu.gd 总行数 1112 → 1154（+42 行 = 1 const block + 1 func + 1 binding + 注释）；T160 banner 起始态 0 改；T199 5 verb tooltip 0 改（_VERB_HINT_DATA / _build_verb_hint_tooltip / _stat_abilities / _profile_abilities 全部 0 触碰）；T210 QuickStats 4 段 literal 0 改（"★ 成就 X/Y · 最佳 mm:ss · 最长单房 mm:ss · Run #N ★" 完整保留）。**`test_i039_t213_quick_stats_tooltip_smoke.gd` 39 断言全 PASS**（T213.HINT 18: const + 4 段 label + 4 段 color hex + 4 段 color_name + 5 字段 × 4 段 + 4 段 desc_zh 关键短语（跨 run 累计 / 单次 run 最长 / 单房最长 / 1-based session 内）/ T213.BUILD 7: func + header "4 段总览 — 悬停查看每段含义" + for 循环 + bullet format + 颜色 format + lines.append ≥ 6 源层（T213 函数 3 源层 + T199 函数 3 源层, 运行时 9 + 11 = 20 calls）+ `\n`.join / T213.READY 4: 绑定 + 注释锚点 + if guard + T160→T213 顺序正确（用"玩家悬停 QuickStats 1 行" 唯一字符串定位 _ready() 块）/ T213.REGRESS 10: T199 5 verb tooltip 4 子断言 + T210 QuickStats literal 0 改 + T160 banner 2 子断言（modulate.a + visible）+ T210 mm:ss 2 子断言 + em-dash 2 处仍 OK）= **39 断言**。`check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 2 README matches ITERATION_COUNT 132→#133 同步, 本轮）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**74+1=75 smoke test 套件 100% PASS**（#132 74 + I039 +1, 0 回归引入: T199 I025 30/30 + T210 I036 25/25 + T160 banner I033 0 触碰 + T195/T196 I022 28/28 + T202/T203/T204 I027-I029 0 触碰）。**下一轮（#134, 134%5==4 普通模式）建议候选**：(1) ProfileQuickStats 4 段 tooltip 加 "Run #" 段悬停高亮联动（5min, polish, 同模式扩展, 玩家悬停 1 段 → 同 4 段 fade 其他 3 段到 50% alpha）/ (2) ProfileRecentList 5 局行 hover 高亮（10min, polish, T162 (#83) 5 局行 BBCode → hover 整行 + 字段 tooltip 玩家可读）/ (3) 7 桶 prewarm aggregator 调优（10min, perf 边际）/ (4) I015 F014 lazy-init guard 在 `prewarm_misc_sfx` 已冗余（5min, cleanup, I015 smoke test 现有 `F014.PLAY.LAZY` assertion 需要同步更新）/ (5) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min, 商业化, 5 verb 完整闭环最后一环）。

- **#132 — F018.2 注释同步收尾 (refactor 防御性 — save_system.gd `_get_autoload` helper F018.2 (#131) 跨引用注释落地, 3 autoload 注释对称 + 1 idiom 注释收敛)**：[`src/autoload/save_system.gd`](file:///workspace/src/autoload/save_system.gd) `_get_autoload(name: String) -> Node` helper 函数**之前**插入 F018.2 (#131) 跨引用注释块 (8 行) — 声明本 helper 是"3 autoload 共享的 SceneTree.root 模式定义" + save_system.gd 是 110 时期的"原始拥有者" + F018.1 (#130) 引用本 helper 作为"模式定义"抽提到 player_stats.gd 复用 + F018.2 (#131) 进一步同步到 game_state.gd + player_stats.gd 3 autoload 走完全相同实现 (1 idiom 取代"3 处各自维护 8 行内联 SceneTree 块") + 路径选择 rationale (SceneTree.root vs self.get_node_or_null 绝对路径) + null-guard 模式 (test resilience 兜底). [`src/autoload/player_stats.gd`](file:///workspace/src/autoload/player_stats.gd) `_get_autoload` helper 注释同步加 "真游戏里 autoload 永远存在" + "test resilience 兜底" 措辞 + 末尾加 I038 (#132) 注释同步锚点 (3 autoload 注释对称). **0 副作用扩散**: save_system.gd 仅 +8 行注释 (helper 函数体 0 改动); player_stats.gd 仅 +2 行注释 + +2 行 I038 锚点; game_state.gd 0 改动; **0 行为变化 / 0 性能变化 / 0 测试破坏** (I037 F018.2 8 断言 100% 兼容). **设计 rationale (注释对称 + 0 冗余)**: F018.2 (#131) 当时只更新了 game_state.gd + player_stats.gd helper 同步, save_system.gd (作为本仓库 3 autoload helper 的"模式定义"原始拥有者) helper 注释未加 F018.2 (#131) 跨引用 — 留下了 3 autoload 注释不对称. I038 (#132) 收尾: 3 autoload 注释 100% 对称, 未来 code review / 维护 / 加新 autoload 互引用都只看 1 处. **`test_i038_f018_2_comment_sync_smoke.gd` 20 断言全 PASS** (I038.HELPER.3 3 项: 3 autoload 都声明 _get_autoload 方法 / I038.F018_2 3 项: 3 autoload helper 注释都含 F018.2 跨引用 / I038.MODE_DEF 1 项: save_system.gd 注释声明为"模式定义" (模式定义 / 原始拥有者 / F018.2 至少 2 项) / I038.SCENE_TREE 3 项: 3 autoload helper 注释都提到 SceneTree.root 路径 / I038.SELF_DANGER 3 项: 3 autoload helper 注释都警告 self.get_node_or_null 绝对路径风险 / I038.NULL_GUARD 3 项: 3 autoload helper 注释都提到 null-guard 模式 / I038.SAME_BODY 1 项: 3 autoload helper 函数体行数大致一致 ±5 (save=10 含 3 行内嵌注释 / game_state=7 / player_stats=7) / I038.PROD_TRUE 3 项: 3 autoload helper 注释都提到真游戏 autoload 永远存在 (宽松匹配 "真游戏" / "生产" / "production" / "test resilience" 等 8 关键词)) = **20 断言**. `check_smoke_consistency.sh` 7/7 规则 PASS (rule 7 2 README matches ITERATION_COUNT 131→#132 同步, 本轮) + 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR. **73+1=74 smoke test 套件 100% PASS** (#131 73 + I038 +1, 0 回归引入: F018.0 14 成就路径 100% 兼容, F018.1 4 路径 100% 兼容, F018.2 3 autoload helper 100% 兼容, T127 / T130 / T131 / I035 T209 / I033 T208 / I037 F018.2 8 断言 0 触碰). **下一轮（#133, 133%5==3 普通模式）建议候选**：(1) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min, 商业化, 5 verb 完整闭环最后一环) / (2) 7 桶 prewarm aggregator 调优 (10min, perf 边际) / (3) I015 F014 lazy-init guard 在 `prewarm_misc_sfx` 已冗余 (5min, cleanup, 但 I015 smoke test 现有 `F014.PLAY.LAZY` assertion 会 fail, 需要同步更新) / (4) I015 F014 PLAY.LAZY assertion 同步更新 (3min, cleanup 配套) / (5) T156 Polish ArchiveStorm 主摄像机 shake 之前先 trigger 1f skybox rotate (10min, 视听, 但 T156 已在 #81 早期完成, 候选已过时).

- **#131 — F018.2 通用 `_get_autoload(name) -> Node` helper 抽提（refactor 防御性 — 跨 3 autoload 共享 SceneTree.root 动态查找模式，0 玩法 / 0 性能 / 0 测试破坏）**：[`src/autoload/game_state.gd`](file:///workspace/src/autoload/game_state.gd) 加 `_get_autoload(autoload_name: String) -> Node` 通用 helper（与 [`save_system.gd:609-619`](file:///workspace/src/autoload/save_system.gd) 完全相同实现 — `Engine.get_main_loop()` → null guard → `.root` → `get_node_or_null(autoload_name)`, 0 字段, 0 副作用, O(1) 单次查表）+ `reset_run()` 把 F018.1 引入的 8 行内联 SceneTree 块压成 1 行 `_get_autoload("PlayerStats")` + `if ... and ...has_method("reset_stats")` 守卫 + `take_damage()` 把 F018.0 残留的 1 处静态 `PlayerStats.record_death()` 引用（F018.1 当初只修了 reset_stats 一处漏掉 record_death — 跟 F018.1 是同类问题, 但静态 `PlayerStats.x` 在 parse 阶段 throw "Identifier not found: PlayerStats"）替换为 `_get_autoload("PlayerStats")` + `if ... and ...has_method("record_death")` 守卫。[`src/autoload/player_stats.gd`](file:///workspace/src/autoload/player_stats.gd) 在 `_read_longest_room_from_gamestate()` 函数体内把 F018.1 引入的 6 行内联 SceneTree 块压成 1 行 `_get_autoload("GameState")` + 同样守卫 + 在 `_read_longest_room_from_gamestate()` 函数**之后**加同样 helper（3 autoload 共享）。**refactor 防御性 rationale**：防御性 — F018.1 (#130) 当时只补了"reset_stats / get_longest_room_seconds"两处具体调用点的内联 SceneTree 块, 留下了 (a) `reset_run()` 8 行块本身可读性差（嵌套 if 4 层）, (b) `take_damage()` 静态 `PlayerStats.record_death()` 没修（同样 parse-time 风险）, (c) 3 autoload 各自维护同一 SceneTree 模式 — 任何未来加 autoload 互引用都得抄一遍 8 行。F018.2 一次解决 3 问题：8 行 → 1 行（reset_run） + 静态调用 → 动态 helper（take_damage） + 3 autoload 共享 helper（未来加互引用一行调用就够）；一致性 — 3 autoload 同一 helper 同一签名同一注释同一行为, "找 autoload" 这件事在仓库里是 1 个 idiom 不是 3 个, 后续 code review / 维护 / 加新 autoload 都只看 1 处。**0 副作用扩散**：save_system.gd 已有的 `_get_autoload` 0 改动（仅注释同步 + F018.2 (#131) 引用标记）；game_state.gd 仅 +1 helper 函数 + 1 行 reset_run + 1 行 take_damage, 0 改既有数据/方法/信号；player_stats.gd 仅 +1 helper 函数 + 5 行 _read_longest_room_from_gamestate 简化, 0 改既有数据/方法/信号；F018.0 / F018.1 全部 0 触碰（`audio_manager_enhanced.gd` / `player_stats.gd` _best_stats / `pause_menu.gd` ProfileLongestRoom 0 改动）；**0 性能变化**（helper 抽提只换写法不换算法, 都是 `Engine.get_main_loop().root.get_node_or_null(name)` 单次 O(1) 查表 — 内联 8 行 vs helper 单行调用零性能差异, Godot 4.6 GDScript 引擎层方法调用 O(1) 纳秒级开销 0 帧率影响）；**0 玩法变化**（纯 defensive refactor, 真实游戏 autoload 总是被加载, helper 永远拿到真实 Node, F018.0 14 成就 chime + F018.1 4 路径 + T209 跨 run 聚合 0 回归）；**0 测试破坏**（test 环境 autoload 不存在 helper 返回 null, 调用方 null-guard 静默跳过, 与 F018.1 同模式 100% 兼容 — T127 / T130 / T131 / I035 T209 / I033 T208 全部 0 触碰）。**`test_f018_2_autoload_helper_smoke.gd` 8 断言全 PASS**（F018.2.HELPER 3: 3 autoload (save_system.gd / game_state.gd / player_stats.gd) 都声明 `func _get_autoload(name: String) -> Node` 方法 / F018.2.MISS 1: `_get_autoload("NonExistent")` 返回 null — test 环境 autoload 不存在兜底 / F018.2.HIT 1: 临时把 `FakeAutoload_abc_456` 节点 add 到 root + `_get_autoload("FakeAutoload_abc_456")` 返回该节点 / F018.2.STATIC 1: `game_state.gd` 源文件不再有 `\tPlayerStats.record_death(` tab-缩进调用 — 已被 F018.2 dynamic helper 替代, 用 tab 前缀避免跟 F018.2 marker comment 中 "PlayerStats.record_death()" 字面量混淆 / F018.2.READ 1: `_read_longest_room_from_gamestate()` 在 GameState autoload 缺失时返回 0.0 — test resilience 不崩 / F018.2.INLINE 1: `game_state.gd` 不再有 `var _ml: MainLoop = Engine.get_main_loop()` 内联 SceneTree 块 — 已被 helper 替代）= **8 断言**。`check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 2 README matches ITERATION_COUNT 130→#131 同步，本轮） + 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**72+1=73 smoke test 套件 100% PASS**（#130 72 + I037 +1, 0 回归引入: F018.0 14 成就路径 100% 兼容, F018.1 4 路径 100% 兼容, T127 / T130 / T131 / I035 T209 0 触碰, I033 T208 0 触碰）。**下一轮（#132, 132%5==2 普通模式）建议候选**：(1) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min, 商业化, 5 verb 完整闭环最后一环) / (2) 7 桶 prewarm aggregator 调优 (10min, perf 边际) / (3) T156 [候选] Polish ArchiveStorm 主摄像机 shake 之前先 trigger 1f skybox rotate (10min, 视听) / (4) I015 F014 lazy-init guard 清理 (5min, cleanup) / (5) SaveSystem _get_autoload 注释同步 F018.2 (#131) 引用 (2min, 注释同步).

- **#130 — 5 维度审查模式（review mode, N%5==0 整点）— F018 系列 fix + 全维度 audit**：本轮是 #126-#129 5 轮集中 audio/polish 落地（14 成就 unique chime + 14 成就 BGM layering + Run history T127-#131 + QuickStats LongestRoom 同步）之后的整点审查（每 5 轮 1 次 — #125 / #120 / #115 / #110 / #105 模板一致）。F018 系列 fix 为核心修复（critical → 0 回归）：(1) **F018.0 修 critical preset scope 泄漏** — [`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) line 1531-1536 `Identifier "preset" not declared`（#128 T209 commit 引入 cache miss block 内 `var preset: Dictionary`，外层 `if stream:` 块引用超作用域，14 成就路径 cache hit 分支 throw，级联 player.gd/pulse_ability.gd/cut_ability.gd/echo_ability.gd parse 失败）— 改用 `ACHIEVEMENT_CHIME_PRESETS[id_val].get("duration", 0.5)` 重新查 dict（cheap lookup，0 副作用）+ 同步更新 [`test_i034_t208b_achievement_bgm_layering_smoke.gd`](file:///workspace/tools/test_i034_t208b_achievement_bgm_layering_smoke.gd) needle（I034 48 断言全 PASS）。(2) **F018.1 修 major autoload 互引用 SceneTree 模式兼容性** — [`src/autoload/player_stats.gd`](file:///workspace/src/autoload/player_stats.gd) line 444, 532 静态 `GameState.get_longest_room_seconds()` + [`src/autoload/game_state.gd`](file:///workspace/src/autoload/game_state.gd) line 120 静态 `PlayerStats.reset_stats()` 在 smoke test `--script` 启动模式抛 `Identifier not found`（parse-time）或 `Nonexistent function 'reset_stats' in base 'Node'`（runtime）— 把 T127/T130/T131 三个测试整废（#128 T209 commit 引入，漂到 #129）。改用 `Engine.get_main_loop()` 动态查 SceneTree.root 的 autoload 节点 + `has_method()` 守卫，缺则按 0 算 / 跳过 + 新增内部辅助 `_read_longest_room_from_gamestate()` 复用于 player_stats.gd 2 处 + 同步更新 [`test_i035_t209_longest_room_smoke.gd`](file:///workspace/tools/test_i035_t209_longest_room_smoke.gd) needle（I035 40 + T127 12 + T130 10 + T131 12 = 74 断言全 PASS，3 测试从 FAIL 恢复 PASS）。**核心 invariants**：(a) 真实游戏 0 影响（autoload 总会被加载，真实游戏永远走非 0 fallback）；(b) 0 行为变化（F018 仅修 scope 泄漏 + 修 test 兼容，14 成就路径 + 5 verb 闭环 + 4 archive 闭环 + 9 BGM 主题 + 14 成就 unique chime 100% 保留）；(c) autoload 拓扑更解耦（F018.1 之前 2 处 autoload 互引用是隐式全局 singleton 耦合，现在是显式节点查找 + has_method 守卫，防御性更强）。**5 维度审查结果**（完整 770 行见 [`REVIEW_LOG.md`](file:///workspace/REVIEW_LOG.md) #130 条目）：(a) 代码质量 — class_name 0 冲突 / autoload 7 个 0 增减 / signal 0 新增 0 删除 / 静态解析 0 SCRIPT ERROR / 运行时冒烟 0 ERROR / TODO/FIXME/HACK 0 项；(b) 玩法完整性 — 5 verb 闭环完整 / 4 archive 闭环完整 / 9 BGM 主题 + 7 桶 prewarm / 5 槽位存档 + CRC32 + run history / 14 成就 + 8 通知卡 + 4 best_stat_threshold 跨 run metaprogression / 死亡+重生 / 营销素材；(c) 素材一致性 — 216 PNG 头 0 损坏 / 73 个 ASSET_REGISTRY 条目（71 APPROVED + 2 REJECTED）/ STYLE_GUIDE 5 verb 调色域 0 漂移 / mm:ss 格式 3 处一致；(d) 文档同步 — README.md（en）/ README.zh-CN.md（zh）段最新 #130 matches ITERATION_COUNT 130（一致性规则 7 PASS）/ CHANGELOG.md / ROADMAP.md / STYLE_GUIDE.md / ASSET_REGISTRY.md / CONTRIBUTING.md / INSPIRATION.md / RESEARCH.md 0 漂移；(e) 测试覆盖 — 72/72 smoke test 100% PASS（F018.0 修 I034 1 个 + F018.1 修 T127/T130/T131 3 个 = 4 个测试从 FAIL 恢复 PASS）/ consistency 7/7 规则 PASS（0 errors, 0 warnings, "Safe to commit"）/ 静态解析 0 ERROR。**变更文件**（3 source 修改 + 2 test 修改）：`src/scripts/audio_manager_enhanced.gd` / `src/autoload/player_stats.gd` / `src/autoload/game_state.gd` / `tools/test_i034_t208b_achievement_bgm_layering_smoke.gd` / `tools/test_i035_t209_longest_room_smoke.gd`。**0 文件删除** / **0 文件新增** / **0 玩法变化** / **0 性能变化**（F018 仅防御性修，autoload 动态查 1 次/帧 O(1) 0 性能影响）。**下一轮（#131, 131%5==1 普通模式）建议候选**：(1) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min，商业化，5 verb 完整闭环最后一环）/ (2) 7 桶 prewarm aggregator 调优（10min，perf 边际）/ (3) T156 [候选] Polish ArchiveStorm 主摄像机 shake 之前先 trigger 1f skybox rotate（10min，视听）/ (4) I015 F014 lazy-init guard 在 `prewarm_misc_sfx` 已冗余（prewarm 总先跑）— 考虑移除 `if _unlock_chime_stream == null:` check（5min，cleanup）/ (5) F018.2 [候选] 其它 autoload 互引用统一改用 SceneTree.root 动态查找模式（15min，refactor 防御性）。
- **#129 — T210 ProfileQuickStats 顶级 summary 行加 "最长单房" 缩略（polish — QuickStats 行 3 → 4 段聚合，玩家打开 PauseMenu 0.5s 即知 "卡得最久的一房"）**：[`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) `_refresh_quick_stats` 末尾的 QuickStats 行从 3 段（成就 N/N / 最佳 mm:ss / Run #）扩到 4 段（增加 "最长单房 mm:ss"，与 T209 #128 顶级行 LongestRoom 数据源完全同源 `_best_stats["longest_room_seconds"]`）：(1) QuickStats 行 4 段完整 literal 落地 `"★ [color=#69C7CE]成就 %d / %d[/color]  ·  最佳 [color=#F2B66E]%s[/color]  ·  最长单房 [color=#65506A]%s[/color]  ·  Run #[color=#B7E6DC]%d[/color] ★"`，4 段 4 色 palette 全开（成就 Glass Cyan / 最佳 Amber Voice / **最长单房 Muted Violet（与 3 段区分 — 顶级行 3 块是 3 块全 Glass Cyan 表达 "聚合"，QuickStats 4 段 4 色表达 "总览"）** / Run # Pale Resonance）；(2) n=0 路径 "—" 占位（与 QuickStats 自身 "最佳" 段 / 顶级行 AvgResonance/BestStreak/LongestRoom 4 处占位风格 100% 一致）；(3) mm:ss 格式 `"%02d:%02d"` 与 `_profile_best_streak` / `_profile_longest_room` 顶级行 2 处 mm:ss 完全同款，玩家读 4 段时间数字不会因格式差异跳戏。**设计 rationale（polish + UX 双价值）**：T133 (#60) 落地 ProfileQuickStats 1 行总览让玩家打开 PauseMenu 0.5s 即知 "我是谁" 代替逐行读 trend/recent 推断；T201 (#117) 加 AvgResonance + BestStreak 顶级行 2 块；T209 (#128) 加 LongestRoom 顶级行第 3 块；**T210 (#129) 把 LongestRoom 同步进 QuickStats 让 "4 段总览" 完整** = 玩家 1 眼看到成就 + 整局最佳 + 单房最佳 + 当前 run #，4 维聚合不用 scroll 看到 trend/recent（QuickStats 行上方 3 顶级行 + 1 HSep1 + 趋势卡 + ... 在数据流上等于"先看 1 行总览"再决定 scroll 深度）。与 LongestRoom 顶级行 0 重复（顶级行是 9pt 居中独立行，QuickStats 是 1 行内联），4 段都在同 1 行。**0 副作用扩散**：仅 +1 字段 `var quick_longest_room: float` + +1 字段 `var longest_room_str: String` + +1 if-else mm:ss 算式 + +1 `if quick_longest_room > 0.0` n=0 guard + QuickStats 行 string literal 4 段扩 3→4 段；既有 3 段（成就/最佳/Run#）100% 保留字面量与位置；T201 顶级行 AvgResonance/BestStreak 0 干扰（`_refresh_top_aggregate_rows` 函数未动）；T209 顶级行 LongestRoom 0 干扰（`_profile_longest_room` 字段 / `_refresh_top_aggregate_rows` 3 early-return + n>0 路径全部未动）；I035 T209 冒烟测试 39/39 0 回归（4 个文件 `game_state.gd` / `room_controller.gd` / `player_stats.gd` / `pause_menu.tscn` 完全没动，`pause_menu.gd` 仅 `_refresh_quick_stats` 函数体扩 1 段，I035 关注的 `_refresh_top_aggregate_rows` / `_profile_longest_room` 块 0 触碰）。**`test_i036_t210_longest_room_quick_stats_smoke.gd` 25 断言全 PASS**（T210.QUICK 12：4 段 4 子断言 + 完整 literal + 最长单房 9 次出现 + Muted Violet #65506A + mm:ss 格式 + em-dash fallback + data src PlayerStats.get_best_stats() + longest_room_seconds key + T210 锚点 / T210.REGRESS_TOP 10：3 顶级行 label 仍存在 + 3 empty 占位 literal 仍存在（AvgResonance/BestStreak/LongestRoom）+ 3 n>0 literal 仍存在（"★ 平均共鸣 — %.1f 碎/房 (n=%d) ★" / "★ 最佳单局 #%d — %d 房 %d 净 %d 碎 %02d:%02d ★" / "★ 最长单房 %02d:%02d ★"）+ 顶级 2 mm:ss 格式数 ≥ 2 / T210.QUICK_FORMAT 3：QuickStats 自身 "最佳" 段 mm:ss 仍存在 + "最长单房" 段 mm:ss 一致 + "—" 占位 2 处）= **25 断言**。`check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 2 README matches ITERATION_COUNT 128→#129 同步，本轮） + 0 SCRIPT ERROR / 0 Parse Error（pre-existing audio_manager_enhanced.gd:1536 `Identifier "preset" not declared` 是 #128 引入的，0 影响本轮 T210）。**71+1=72 smoke test 套件 100% PASS**（I035 39 + I036 25 = 64，T208.B I034 48 / T208 I033 40 / 5-verb I026-I032 7×~25 / 其他 30+ 老测试全 PASS）。**下一轮（#130, 130%5==0 审查模式）建议候选**：(1) 审查模式（每 5 轮 1 次 — #125 审查 (D001-D004) / #120 审查 / #115 审查 / #110 审查 / #105 审查 模板：完整代码质量 / 玩法 / 素材 / 文档 / drift 审计 → 修复 critical/major + 写 REVIEW_LOG.md 条目）/ (2) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min，商业化，5 verb 完整闭环最后一环）/ (3) 7 桶 prewarm aggregator 调优（10min，perf 边际）/ (4) T156 [候选] Polish ArchiveStorm 主摄像机 shake 之前先 trigger 1f skybox rotate（10min，视听）/ (5) I015 F014 lazy-init guard 在 `prewarm_misc_sfx` 已冗余（prewarm 总先跑）— 考虑移除 `if _unlock_chime_stream == null:` check（5min，cleanup）。
- **#128 — T209 PlayerProfilePanel 顶级行 "最长单房" (LongestRoom) 补充（polish — 跨 run 聚合第 3 块，顶级行 2 → 3 行，玩家"我最强一房"的回忆锚点）**：`src/autoload/game_state.gd` 加 3 字段 + 2 API（record_room_enter/exit）+ 2 accessor（get_longest_room_seconds/id）+ reset_run() 重置 3 字段；`src/scripts/room_controller.gd._ready()` 调 `GameState.record_room_enter(room_id)`，`_complete_room()` 调 `GameState.record_room_exit(room_id)`（在 mark_room_completed 之后、加分之前 — 顺序保证 "完成 → 计时" 语义）；`src/autoload/player_stats.gd` `_best_stats` dict 加 `longest_room_seconds: 0.0` + `_capture_run_into_history` snapshot 加同 key + `_update_best_stats_from_current_run` 单调更新（`var longest_room := float(GameState.get_longest_room_seconds())` 后 `if longest_room > best` 写回）；`src/scenes/pause_menu.tscn` 在 `ProfileBestStreak` 之后、`HSep1` 之前插 1 Label `ProfileLongestRoom`（Pale Resonance 9pt 居中，与 AvgResonance / BestStreak 风格统一）；`src/scripts/pause_menu.gd` 加 `@onready var _profile_longest_room: Label` + `_refresh_top_aggregate_rows` 在 3 个 early-return 路径（PlayerStats null / history 空 / etc.）都 set `text = "★ 最长单房 —  ★"`（n=0 不显 0.0 而是 "—"，与 AvgResonance / BestStreak 风格一致），n>0 路径调 `PlayerStats.get_best_stats()` 读 `_best_stats["longest_room_seconds"]` 后 `text = "★ 最长单房 %02d:%02d ★"` mm:ss 格式（与 BestStreak 同款）。**设计 rationale（polish + UX）**：T201（#117）顶级行 2 块（AvgResonance / BestStreak）都是聚合 "整局" 指标（碎/房、rooms_cleared 最高 + 时长），T209（#128）加第 3 块 "最长单房" 让玩家看到 "我卡得最久的一房"（e.g. 4 分 32 秒）— 跨 run max 单房耗时，是 _best_stats 5 字段的 房间级 版本。数据 invariant：GameState 单 run 累加（`_longest_room_seconds_this_run` 跨多次 `record_room_exit` 单调 max）→ PlayerStats reset_stats 开头 snapshot → 写 `_best_stats["longest_room_seconds"]`（跨 run 持久化）→ 用户暂停时 `pause_menu._refresh_top_aggregate_rows` 读。**`test_i035_t209_longest_room_smoke.gd` 39 断言全 PASS**（T209.GS 18：3 字段 + reset_reset 3 + record_enter 3 + current_room invariant + record_exit 5 + 2 accessor + 2 return value + anchor / T209.RC 4：enter call + exit call + 顺序 mark→exit + anchor / T209.PS 6：dict key + update 3 + snapshot + comment + loader safe / T209.UI 11：tscn node + position order + gd @onready + refresh text + 3 empty paths + mm:ss format + get_best_stats data src + 2 dict key + anchor）。**回归兼容**：T201 顶级行 AvgResonance/BestStreak（9/9 0 回归 — `_refresh_top_aggregate_rows` 3 early-return + 2 main branch + n 样本逻辑保留，LongestRoom 块追加在最后 0 干扰）+ T127 _best_stats 4→5 字段扩展（`_load_best_stats` 用 `for key in _best_stats.keys()` 循环老存档 missing 字段 fallback 0.0，0 加载 crash）+ T131 _run_history snapshot 6→7 字段（同循环模式 0 老 history 加载 crash）。`check_smoke_consistency.sh` 7/7 规则 PASS + 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。70+1=71 smoke test 套件 / 1 task polish / 0 文件删除 / 4 文件修改（game_state.gd + room_controller.gd + player_stats.gd + pause_menu.gd）+ 2 文件新增（pause_menu.tscn 加 1 Label + tools/test_i035_t209_longest_room_smoke.gd）。**下一轮（#129，129%5==4 普通模式）建议候选**：(1) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min，商业化，5 verb 完整闭环最后一环）/ (2) 7 桶 prewarm aggregator 调优（10min，perf 边际）/ (3) T156 [候选] Polish ArchiveStorm 主摄像机 shake 之前先 trigger 1f skybox rotate（10min，视听）/ (4) I015 F014 lazy-init guard 在 `prewarm_misc_sfx` 已冗余（prewarm 总先跑）— 考虑移除 `if _unlock_chime_stream == null:` check（5min，cleanup）/ (5) ProfileQuickStats 顶级 summary 也加 LongestRoom 缩略（5min，polish）。
- **#127 — T208.B 14 成就 × 9 BGM 主题 audio layering（商业化 — chime 浮在 BGM 之上，BGM duck + 滑回）**：`audio_manager_enhanced.gd` 新增 `const ACHIEVEMENT_BGM_HINT` 字典（14 成就 → 6/9 BGM 主题语义映射 — `title_intro` 4 [first_steps/voice_purifier/resonance_collector/persistent_resonance] / `hub_warm` 3 [triple_voice/quadruple_voice/quintuple_voice] / `archive_exploration` 2 [first_cut/warden_slayer] / `archive_dawn` 2 [full_archive/archive_master] / `whisper_hollow` 2 [long_road/silence_hunter] / `silence_void` 1 [resonance_hoarder]；boss 主题 `archive_boss` / `archive_boss_dual` / `archive_storm` 故意**不**在 mapping 中 — boss 战不期望成就触发，避免 BGM 与 unlock 撞色）+ 3 个新 ducking 常量（`const _BGM_DUCK_DB := -6.0` ≈ 音量减半 / `const _BGM_DUCK_FADE_IN_S := 0.05` 几乎瞬时 attack / `const _BGM_DUCK_FADE_OUT_S := 0.30` 稍慢 release — 听感"滑回"而非硬切）+ `var _bgm_duck_tween: Tween = null` re-entrant 状态字段 + 新私有函数 `_duck_current_bgm_for_chime(duration_s: float) -> void`（null guard：`_current_music_player == null` 时 no-op [title screen 等无 BGM] / re-entrant 安全：`_bgm_duck_tween.kill()` 后重建 / `pre_duck_db` 捕获不假设 0 dB 不破坏正在 fade-in/out 的 BGM transition / 3 步 tween：`tween_property(volume_db → target_ducked_db, 0.05s)` + `tween_interval(duration_s)` chime 期间 hold + `tween_property(volume_db → pre_duck_db, 0.30s)` 恢复）+ 改 `play_unlock_chime(id_val)` 14 成就路径调 `_duck_current_bgm_for_chime(preset.get("duration", 0.5))` + fallback 路径调 `_duck_current_bgm_for_chime(0.4)`（老 C6+E6+A6 chime 也与 BGM 分层清晰）。**设计 rationale**（商业化 + UX 双价值）：T208（#126）让 14 成就各具独特 chord 音色，但 BGM 在 Music bus 默认 0dB 会盖过 0.18-0.24 amp 的 SFX bus chime。T208.B 让 14 chime "浮在 BGM 之上"（感知上）而不取代 — BGM 短时下降到 (current_db + -6) dB 持续 0.35-0.65s chime 时长后平滑滑回。玩家仍听见 archive 氛围（exploration/dawn/whisper 等）但 unlock 成 foreground 事件。**6 主题语义映射 rationale**：4+3+2+2+2+1 = 14，boss 主题不出场（boss 战不期望成就），title_intro（4 起步）→ hub_warm（3 温暖）→ archive_exploration（2 探索）→ archive_dawn（2 胜利）→ whisper_hollow（2 深度）→ silence_void（1 沉默）顺序遵循 gameplay 阶段进度，映射本身也暗示"成就属于哪个阶段"。本轮 mapping 仅作语义标签（不切换 BGM 主题，会破坏房间听感）+ 留 API hook 供未来"hub BGM 微妙配合最近解锁成就"特性。**0 副作用扩散**：4 新 const + 1 新 var + 1 新私有函数 + `play_unlock_chime` 14 路径加 1 行 + fallback 路径加 1 行；`play_unlock_chime` 签名 100% 向后兼容（id_val 可选参数保留）；既有 T208 dispatch（`ACHIEVEMENT_CHIME_PRESETS.has(id_val)`）与 T208 fallback（`if _unlock_chime_stream == null`）未动；BGM `play_music_track` / `stop_music` / `play_music_finale` / `request_boss_music` 0 牵连（ducking 直接动 `_current_music_player.volume_db`，不拦截那些调用）。**`test_i034_t208b_achievement_bgm_layering_smoke.gd` 48 项断言全 PASS**（T208.B.HINT 28：const + 14 id 在 dict + 6 themes 引用 + 6 语义映射锁定 + 3 boss 主题禁入 mapping block + 注释锚点 / T208.B.DUCK 12：helper sig + 4 const + null guard + pre_duck_db 捕获 + kill re-entrant + tween_interval + 2 tween_property / T208.B.PLAY 8：14 路径 duck call + fallback duck call + 0.4s fallback duration + T208.B 锚点 + BGM ducking + re-entrant rationale + T208 兼容 sig + T208 兼容 dispatch）。**回归兼容**：I015 F014（8/8 0 回归 — `_unlock_chime_stream` field、`func play_unlock_chime(` sig、`if _unlock_chime_stream == null:` lazy、`play_sfx(_unlock_chime_stream)` bus、`_generate_unlock_chime_sfx()` synth fn、AchievementNotification `ame.has_method` + `ame.call` + `_play_unlock_chime` helper 全部保留）+ I033 T208（16/16 0 回归 — `const ACHIEVEMENT_CHIME_PRESETS` + dispatch + lookup + fallback + SFX bus + 4 synth anchors + prewarm loop 全部完整）+ I019 0 引用 unlock_chime 路径（不受影响）。`check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 README 双轨 #127 同步）+ 0 SCRIPT ERROR / 0 Parse Error / 0 runtime ERROR。69+1=70 smoke test 套件 / 1 任务商业化 + UX 双价值 / 0 文件删除 / 1 文件修改（audio_manager_enhanced.gd）+ 1 文件新增（test_i034_t208b_achievement_bgm_layering_smoke.gd）。**下一轮（#128，128%5==3 普通模式）建议候选**：(1) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min，商业化，5 verb 完整闭环最后一环）/ (2) PlayerProfilePanel 顶级行 LongestRoom 补充（5min，scope 跨 1 轮 polish）/ (3) 7 桶 prewarm aggregator 调优（10min，perf 边际）/ (4) T156 [候选] Polish ArchiveStorm 主摄像机 shake 之前先 trigger 1f skybox rotate（10min，视听）/ (5) I015 F014 lazy-init guard 在 `prewarm_misc_sfx` 已冗余（prewarm 总先跑）— 考虑移除 `if _unlock_chime_stream == null:` check（5min，cleanup）。
- **#126 — T208 14 成就 unique unlock chime (商业化 — 解锁听觉与视觉 1:1 对齐)**：`audio_manager_enhanced.gd` 新增 `const ACHIEVEMENT_CHIME_PRESETS` 字典 (14 成就 14 独特配方 — `chord_midi` 数组 2-5 音 + `duration` 0.35-0.65s + `amp` 0.20-0.24 + `decay` 3.5-8.0) + `var _achievement_chime_streams: Dictionary` per-achievement 缓存 + 新私有函数 `_generate_achievement_chime_sfx(preset)` parameter-driven 合成 (440 * 2^((midi-69)/12) Hz + `exp(-t * decay)` envelope + `Σ sin(t*TAU*freq) / N` equal-weighted 叠加, 听感不超 amp 防 clip) + `play_unlock_chime(id_val: String = "")` 公开方法新增可选 `id_val` 参数 (有 id → 查 ACHIEVEMENT_CHIME_PRESETS + _achievement_chime_streams 缓存, 无 id 或 id 不在 dict → 走原 F014 `_unlock_chime_stream` fallback C6+E6+A6 保持向后兼容) + `prewarm_misc_sfx()` 一次性预热 14 stream (15ms 总开销, ~150KB 内存, 玩家首次解锁任何成就 0 合成延迟) + `achievement_notification.gd._on_achievement_unlocked()` 调 `_play_unlock_chime(id_val)` → `ame.call("play_unlock_chime", id_val)` 把 achievement id 传到 audio 层。**14 配方设计 (与成就主题 1:1 映射)**：`first_steps` C 大调上行 4 音阶 [C5 E5 G5 C6] 起步感 / `voice_purifier` 纯五度 + 高八度 [C4 G4 C5] 净化的"空" / `resonance_collector` G 大三 + 高八度 [G4 B4 D5 G5] 收集的"满" / `triple_voice` 3 音 [C4 E4 G4] 三声齐鸣 / `quadruple_voice` 4 音 [C4 E4 G4 B4 增三] 升 1 度张力 / `quintuple_voice` 5 音全音阶 [C4 D4 E4 G4 A4] 大师共鸣神秘 / `first_cut` 三全音 [F#4 E6] 锋利短促 0.35s / `warden_slayer` A 小 + 增四度 [A3 C4 D#4 A#4] 战胜压迫 / `full_archive` G 大三 5 音 [G4 B4 D5 G5 B5] 完整丰盈 / `persistent_resonance` D 小7 [D4 F4 A4 C5] 持续柔和 / `long_road` C 小 [C4 Eb4 G4 C5] 最长 0.65s 慢衰减 / `archive_master` C 大 5 音 [C4 E4 G4 C5 E5] 大师级丰盈 amp 0.24 / `resonance_hoarder` A 小三 [A3 C4 E4 G4] 积累的沉重 / `silence_hunter` 减七 [C4 Eb4 Gb4 A4] 黑暗低吟。**设计 rationale** (商业化 + a11y 双价值)：之前 F014 (#103) 是 1 个固定 C6+E6+A6 金属三连音, 14 成就共用同一段 unlock 音效, 玩家听多了会"unlock 不分伯仲"。T208 让 14 成就各自有独特 chord + duration + amp + decay 配方, 玩家听到 "first_steps" 是 C 大调上行 (听感明亮上扬), 听到 "warden_slayer" 是 A 小带增四度 (听感阴沉张力), 听到 "silence_hunter" 是减七和弦 (听感黑暗不稳定) → 解锁什么听就有什么音色, 与 `icon_hint` 视觉分工对齐 (14 视觉 + 14 听觉冗余编码 — 玩家视觉障碍时仍能听出解锁类型, 听觉障碍时仍能看出 icon type)。**0 副作用扩散**：14 配方都是 add-only 新字段 + 新私有函数 + 公开方法新增可选参数 (默认 `""` 老调用方 0 改动), 现有 F014 `_unlock_chime_stream` 单 stream 路径 100% 兼容 (老 `ame.call("play_unlock_chime")` 无参调用仍走 fallback), 14 新 stream 预热只在 `prewarm_misc_sfx` 一次性 `for ach_id in ACHIEVEMENT_CHIME_PRESETS.keys()` 循环, 不影响其他 5 桶 prewarm aggregator。**`test_i033_t208_achievement_unique_chime_smoke.gd` 40 项断言全 PASS** (T208.PRESETS 21: const + 14 id 在 dict + 4 字段 + 5 注释 inline / T208.PLAY 11: cache field + play sig + dispatch + lookup + fallback + SFX bus + gen sig + MIDI→Hz 公式 + exp decay + Σ sum + prewarm 14 loop / T208.NOTIFY 4: ame.call pass id + helper sig + has_method + 注释锚点) + 现有 I015 50/50 (T208 兼容 `func play_unlock_chime(` 子串 + `ame.call("play_unlock_chime"` 子串) + I019 T189/T192 50/50 (T208 兼容 `func _play_unlock_chime(` 子串) 0 回归 0 pre-existing 失败。`check_smoke_consistency.sh` 7/7 规则 PASS (rule 7 README 双轨 #126 同步) + 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。68+1=69 smoke test 套件 / 1 任务商业化 a11y 改进 / 0 文件删除 / 2 文件修改 (audio_manager_enhanced.gd + achievement_notification.gd) + 1 文件新增 (test_i033_t208_achievement_unique_chime_smoke.gd)。**下一轮 (#127, 127%5==2 普通模式) 建议候选**：(1) 14 BGM 9 主题 layering 续 T208 (#127, 商业化, scope 跨 1-2 轮) / (2) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min, 商业化, 5 verb 完整闭环最后一环) / (3) PlayerProfilePanel 顶级行 LongestRoom 补充 (5min, scope 跨 1 轮 polish) / (4) 7 桶 prewarm aggregator 调优 (10min, perf 边际) / (5) T156 [候选] Polish ArchiveStorm 1f skybox rotate 之前先 trigger 1f skybox rotate (10min, 视听)。
- **#125 — 审查 #125（125%5==0 整点审查，5 维度全 audit + 0 缺口 + F006.1 修复 1 pre-existing 失败）**：本轮是 #120 审查（5 维度全 audit + 0 缺口）之后的**#121-#124 共 4 轮 polish + edge case 防御**之后的"代码-素材-文档-冒烟"全维度 audit。**审查前准备** — Godot 4.6.3 headless binary 本轮首次解压（`cat + unzip -FF` 已落盘，直接复用）；静态解析、运行时冒烟、JSON 校验、PNG 头校验、class_name/signal/autoload 拓扑、5 verb 闭环、smoke test 全套 68 套件全部跑通。**审查结果（5 维度）**：(a) **代码质量** — 54 class_name 唯一 0 冲突（#120：54 → #125：54，0 差异，#121-#124 共 4 轮 polish 0 新增 class_name）/ 79 signal 拓扑完整（与 #120 完全一致，T202.B/C/T203/T204/T205/T206/T207 全部走既有 Label / @onready var / const / func 模式，0 signal 槽位变化）/ 7 autoload 稳定（GameState / PlayerStats / SaveSystem / AudioManager / AudioManagerEnhanced / ScreenShake / PlayerActionGate）/ 63 .gd 源码 + 29 .tscn（与 #120 一致，0 新增 .gd / 0 新增 .tscn）/ 0 TODO/FIXME/HACK / 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR；(b) **玩法完整性** — 5 verb 闭环（Pulse/Bind/Cut/Echo/Wave）+ D002.B 父类抽取 + H001 hotfix + F013.C 镜像 F013.B whole-tone scale + T194 Echo 漏修 + T197 8 调用点 ScreenShake.vibrate() 5 阶强度 + T198 5 verb hint J/K/L/Q/V 5 键位 + T199 PauseMenu 5 verb row hover tooltip + T200 HUD 5 verb 冷却条 reduce_flash 灰化 + T201 PlayerProfilePanel AvgResonance/BestStreak + T202 HUD 5 verb 冷却中半透明提示标签 + T202.B ReduceAllCheck 总开关 + T202.C 三态 indeterminate 同步 + T203 修 #117 ProfileAvgResonance pre-existing tscn `#` 注释 ERROR + T204 HUD 5 verb 名称标签 5 主题色 + T205 SettingsMenu Audio 4 滑块实时百分比 + T206 HUD 7 UI 元素（5 verb bar + ResonanceBar + HealthContainer）reduce_flash 灰化 + T207 SaveLoadMenu `_on_confirm_cancel` 同帧双调 guard；完整可玩循环 / SaveLoadMenu 4 路 cancel 闭环 / BGM 9 主题 + 5 桶 prewarm aggregator / 存档 5 槽位 + CRC32 + 60s autosave / 死亡重生 / 成就 14 + 8 通知卡 / 教学 4 archive 房间 11 tutorial hints / 营销素材 / Accessibility 4 步演进（T195 reduce_shake/flash + T196 reduce_vibration + T202.B ReduceAllCheck 总开关 + T202.C indeterminate 三态 + T206 HUD 7 UI 元素灰化）全部稳定；(c) **素材一致性** — 108 个 PNG 头校验全 PASS（严格模式 -name "*.png" -not -name "*.import"，#120 review 计数 114 修正为 108）/ ASSET_REGISTRY 73 条连续（A001-A073 命名严格递增）/ STYLE_GUIDE 8+1 色板 / REJECTED 0 / 风格漂移 0；(d) **文档同步** — ROADMAP / CHANGELOG / README 双轨 0 滞后 / REVIEW_LOG 5 段活跃（#40-#125）/ CONTRIBUTING §9 6th verb 接入路径 9 步 + 5 易错点；(e) **测试覆盖** — 68 smoke test 套件，**68/68 PASS（首次 100%）**（F006.1 修复 I022 T195.9 1 pre-existing 失败，0 回归，**#105 修复 T103/T142 + #125 F006.1 修复 I022 T195.9 = 2 轮努力达成 100% smoke 全过**）。**审查结论**：「可继续迭代」状态，0 严重 0 一般 0 轻微 0 信息。**F006.1（#125）修复 I022 T195.9 window 太短 bug**：[`tools/test_i022_t194_t195_smoke.gd`](file:///workspace/tools/test_i022_t194_t195_smoke.gd) 之前用 `sm_src.substr(restore_block_idx, 3500)` 截 `_on_restore_all_pressed` 函数体，T195（#112）落地后函数持续扩展（#113 T196 reduce_vibration + #121 T202.B reduce_all + #121 T202.C indeterminate + #122 T205 audio label refresh），`ScreenShake.set_reduce_shake/flash(false)` 落在 3500 char 窗口外，改用动态 end-of-function 定位（找下一个 `func ` 顶层声明 或 EOF 截完整函数体）后 I022 28/28 PASS。**0 副作用扩散**：仅改 1 处 substr window 计算方式，不改任何 `_on_restore_all_pressed` 函数行为，不改 4 项断言内容，不影响 I022 其余 27 项断言。**#120 → #125 增量验证**（5 轮 polish + tech-debt 落地证据）：#121 T202.B+T202.C（I029 22 PASS）/ #122 T205（I030 18 PASS）/ #123 T206（I031 16 PASS）/ #124 T207（I032 21 PASS）/ #125 F006.1（I022 28/28 PASS 100%）。**下一轮（#126，126%5==1 普通模式）建议候选**：(1) 14 成就 unlock chime 与全 14 BGM/9 主题 layering（15min，scope 跨 1-2 轮，商业化）/ (2) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min，商业化）/ (3) PlayerProfilePanel 顶级行 LongestRoom 补充（5min，polish）/ (4) 7 桶 prewarm aggregator 调优（10min，perf 边际）/ (5) T156 [候选] Polish ArchiveStorm 1f skybox rotate（10min，视听）。

- **#124 — T207 SaveLoadMenu modal Esc + click cancel 同时按两键优先级 guard（"首个事件胜出，第二个 no-op" 显式语义）**：`src/scripts/save_load_menu.gd` 加 1 字段 `var _cancel_in_progress: bool = false`（紧跟 `_filter_occupied_only`，默认 false 无 cancel 进行中）+ 改写 `_on_confirm_cancel()` 函数体（入口检查 `_cancel_in_progress` guard 早退 no-op → 通过后 set true → 调 `_hide_confirm_modal` T188 同源链兼容 → 出口 reset false），guard 顺序 `check → set → hide → clear` 防御 race。**设计 rationale**：SaveLoadMenu confirm modal（T188 二次确认弹窗）有 3 路同源链（Esc T192 _input + click backdrop T191 gui_input + click ConfirmCancelBtn T188 pressed signal）都走 `_on_confirm_cancel` 取消弹窗。玩家同一帧同时触发其中 2 路（如鼠标点 backdrop + 同时按 Esc / 点 backdrop + 同时点取消按钮）时，`_on_confirm_cancel` 会被调用 2 次。`_hide_confirm_modal` 本身幂等（设置 3 节点 visible=false × 3 + 复位 `_pending_delete_slot = -1`），但显式 guard 让"首个事件胜出，第二个 no-op"的优先级语义在代码层面可读 + 防御未来扩展时双调引入副作用（如 cancel jingle 音效，modal shake 动画，多次 emit closed signal 等）。与 T202.B（#121）`_syncing_from_master` 守卫同模式（入口开 → 出口关，短时间窗口内其他路径被短路）。**0 副作用扩散**：守卫是纯状态切换，不影响 `_hide_confirm_modal` 的可观察行为，现有 4 条输入路径（Esc / click backdrop / click cancel btn / Enter 默认）全部走 guard 走 T188 同源链 `_hide_confirm_modal` 行为 100% 兼容，仅当同帧双调时第二个 no-op 真正生效（之前两次都执行 `_hide_confirm_modal` 虽然幂等但浪费 1 次函数调用）。**`test_i032_t207_smoke.gd` 21 项断言全 PASS**（T207.GD.FIELD 6：field exists + default false + anchor + rationale + T192 ref + T191 ref + field order / T207.GD.CANCEL 9：fn exists + anchor + guard check + return + set + hide + clear + order check→set→hide→clear + no emit T188 兼容 / T207.COMPAT 6：T188 hide modal 链保留 + T189 vis helper 链保留 + T191 backdrop 调 _on_confirm_cancel + T192 input 调 _on_confirm_cancel + T188 confirm cancel btn 调 _on_confirm_cancel + single-fire 语义注释）。**回归兼容**：I018 T188（52/52）/ I019 T189+T192+F016.B（44/44）/ I020 T190+T191（29/29）0 回归 0 pre-existing 失败。`check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 README 双轨 #124 同步）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。67+1=68 smoke test 套件 / 1 任务 edge case 防御 / 0 文件删除 / 1 文件修改（save_load_menu.gd）+ 1 文件新增（test_i032_t207_smoke.gd）。**下一轮（#125，125%5==0 审查模式）建议候选**：（1）审查 #125 — 距 #120 已 5 轮，5 维度全 audit + 0 缺口 + 0 修复（10min）/ （2）14 成就 unlock chime 与全 14 BGM/9 主题 layering（15min，scope 跨 1-2 轮）/ （3）WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min，商业化）/ （4）7 桶 prewarm aggregator 调优（10min，perf 边际）/ （5）I022 T195.9 pre-existing 失败 1 项修复（5min，5 轮遗留）。

- **#123 — T206 HUD ResonanceBar + HealthContainer reduce_flash 灰化（扩展 T200 5 verb bar 灰化范围到 HUD 顶部 2 个最显眼常驻颜色块，5→7 UI 元素）**：`src/scripts/hud.gd` `_apply_reduced_flash_modulate(reduce: bool)` iteration list 从 5 verb cooldown bar 扩到 **7 UI 元素**（`_pulse_cooldown` / `_bind_cooldown` / `_cut_cooldown` / `_echo_cooldown` / `_wave_cooldown` / `_resonance_bar` / `_health_container`）；循环变量名从 `bar` 改 `ui_elem` 反映"不只 bar 元素"语义；5 verb name label（T204 #119）/ 5 verb cooldown "冷却中" label（T202 #118）主题色不变（文字通道保留色域分工 + 已 alpha 0.6 半透 = 不需再灰）。**设计升级 rationale**：T200（#117）时 rationale 是 "ResonanceBar / HealthRow 是常驻状态显示, 不是 flash" —— T206（#123）升级为 "accessibility 偏好应覆盖玩家感知的全部高饱和度 UI blob"：14x16 满血 bell 每帧 0.5px 玻璃青高光 + 80x6 ResonanceBar 主色 玻璃青 = 5 verb 冷却条同等刺激源，累加效应 = "我刚勾 reduce_flash 但屏幕还是闪"。**实现亮点**：`_health_container` 是 HBoxContainer parent，1 次 `.modulate = target_color` 继承到所有动态子 ColorRect bell（满血 Glass Cyan #69C7CE + 空血 Muted Violet #65506A），0 重复遍历，0 写 4 次。`_resonance_bar` 是 ProgressBar，modulate 叠加 StyleBoxFlat fill（与 T200 5 verb bar 模式一致）。**状态切换守卫**：沿用 T200 `_reduced_flash_applied` 缓存，玩家勾选/取消只触发 1 次 modulate 重写（7 element 写，O(1)），之后无 per-frame cost。**0 写入时序竞态风险**：7 元素都在 _process 末尾 1 次写，不依赖 _ready 顺序，_onready 守卫与 is_instance_valid 双层防御保留。**修复后**：玩家勾选「减弱屏幕闪烁」时整个 HUD 顶部 color blob 区域（5 verb bar + ResonanceBar + 4 health bell）7 元素 1:1 一致降饱和度到 0.55 灰 + 75% alpha，视觉刺激源从 7 个降到 0 个，accessibility 4 通道分级（3 reduce 偏好 + 屏闪总控）完成。**`test_i031_t206_smoke.gd` 16 项断言全 PASS**（T206.HUD 15：2 ref + 1 apply fn + 1 7-element loop + 2 element in loop + 1 modulate assign + 1 anchor >=2 + 1 reduced const + 1 normal const + 1 guard + 1 ss_query + 1 process hook + 1 5-bars legacy + 1 HBoxContainer type + 1 reserve / T206.SS 1：is_reduce_flash 兼容）。**回归兼容**：I026 T200.9 同步更新接受 `bar.modulate` 或 `ui_elem.modulate` 任一变量名（T206 rename 兼容），I027 / I028 / I030 0 回归。`check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 README 双轨 #123 同步）+ 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。**pre-existing 备注**：I022 T195.9 失败（1 项）仍存在，是 #112 T195 时代遗留问题（`_on_restore_all_pressed` 未把 `_reduce_flash_check` 重置为 off），与本轮 T206 无关，待 #125+ 修复。

- **#122 — T205 SettingsMenu Audio 4 滑块实时百分比显示（BBCode [color=#F2B66E] 包装数值 + 0/100% 边界值正确）**：`settings_menu.tscn` 4 audio label（MasterLabel / SFXLabel / MusicLabel / AmbienceLabel）各加 1 行 `bbcode_enabled = true`（Godot 4 普通 Label 默认不解析 BBCode 必须显式开启, 与 PauseMenu ProfileAbilities/StatAbilities 同模式）；`settings_menu.gd` 加 4 @onready（master/sfx/music/ambience label refs）+ 1 helper `_refresh_audio_volume_label(label: Label, value: float, base_text: String) -> void`（`if label == null: return` null 守卫防御 _onready 时序竞态 + `int(round(clampf(value, 0.0, 100.0)))` 防 4 类边缘 case 浮点抖动 99.99/100.01 + 负值/>100 cfg 脏数据 + `label.text = "%s [color=#F2B66E]%d%%[/color]"` 字符串模板 + Amber Voice 0.949 主题色包装数值让音量档位视觉突出但不抢眼）+ 4 _on_*_changed handler（master/sfx/music/ambience）末尾各调 1 次 helper（玩家拖滑块时 value_changed 实时刷新 label）+ `_load_settings()` 末尾 4 次 helper 调（重启读 cfg 后 label 立即反映当前音量档位）+ `_on_restore_all_pressed()` 末尾 4 次 helper 调（set_block_signals 抑制 handler 后必须主动调, 玩家点"还原所有"后 label 立即同步到 "100%"）；`test_i030_t205_audio_volume_label_smoke.gd` 18 项断言 PASS（T205.GD 13: 4 label ref + 1 helper fn + 1 BBCode color + 1 null guard + 4 _on_*_changed call + 1 _load_settings 4-call + 1 _on_restore_all_pressed 4-call + 1 anchor / T205.TSCN 4: 4 audio label bbcode_enabled）；UX 通道 100% 透明：audio 4 + autosave 1 = 5 个滑块全部实时数值显示, 与 T136 AutoSaveIntervalValue "60 秒" 模式 100% 一致；`check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 README 双轨 #122 同步）+ 0 SCRIPT ERROR / 0 Parse Error

- **#121 — T202.B + T202.C SettingsMenu accessibility 4 滑块联动 1 总开关（ReduceAllCheck 一键开/关 3 子项 + 三态 indeterminate 同步）**：`settings_menu.tscn` 在 VideoPanel AccessibilityHeader 之前（主开关是区段最显眼位置）插 1 个 CheckBox 节点 `ReduceAllCheck`, `text = "减弱反馈 (一键)"` 强调"总控"语义, `theme_override_font_sizes/font_size = 10` 高于子项 9pt 突出主开关, `theme_override_colors/font_color = Color(0.949, 0.714, 0.431, 1)` Amber Voice 主题色（与 HUD Pulse verb 同色暗示"反馈"系列 + amber 暖色暗示"金标"总控语义）, `tooltip_text` 详细说明一键联动 3 子项 + indeterminate 视觉语义, `layout_mode = 2` + `size_flags_horizontal = 4` 居中; `settings_menu.gd` 加 `@onready var _reduce_all_check` 字段 + 状态字段 `var _reduce_all: bool = false`（独立 bool 字段, 不由 3 子项 AND 算出, 避免"3 子项部分开启"时的歧义）+ 防递归守卫 `var _syncing_from_master: bool = false`（在 _on_reduce_all_toggled 入口开, 出口关, 短时间窗口内所有 _on_reduce_*_toggled 都被短路）+ `_on_reduce_all_toggled(enabled)` handler (开 _syncing_from_master 守卫 → 调 _apply_three_children(enabled) 推 3 子项同步 + 调 _set_three_children_disabled(enabled) 灰化 3 子项 → 关 _syncing_from_master 守卫) + `_apply_three_children(enabled)` helper (3 子项各 set_block_signals(true) 阻断 signal + button_pressed = enabled + _reduced_* 字段同步 + ScreenShake.set_reduce_*(enabled) live-push 3 次) + `_set_three_children_disabled(disabled)` helper (3 子项各 .disabled = disabled, 主开关 enabled 时 3 子项 disabled 灰化 = 玩家清晰看到"已经被总控, 不必再调"语义, 关闭时重新 enabled 让玩家可单独调) + `_sync_reduce_all_state()` helper (回查 3 子项状态: 全 true → 主开关 checked, 全 false → 主开关 unchecked, 混合 (1-2 个 true) → 主开关 indeterminate) + 3 个 `_on_reduce_*_toggled` (shake/flash/vibration) 末尾加 `if not _syncing_from_master: _sync_reduce_all_state()` 守卫调用 + `_save_settings()` 加 `cfg.set_value("accessibility", "reduce_all", _reduce_all)` 持久化 + `_load_settings()` 加 `cfg.get_value("accessibility", "reduce_all", false)` 读回 + 读回后 _reduce_all=true 时重触发主开关逻辑 + `_on_restore_all_pressed()` 末尾加 uncheck + indeterminate=false + _reduce_all=false + _set_three_children_disabled(false) 还原默认; `test_i029_t202b_reduce_all_smoke.gd` 22 项断言 PASS（T202B.MENU 7 + T202B.GD 11 + T202C.GD 4）; accessibility polish 单家族单轮 ≤25min 范围, Linux-style 主开关 + fine-tune 模式（主开关是 "convenience" 而非 "authority", 数据始终存 3 子项独立状态, 主开关只是"一键操作 3 子项"的快捷方式）; `_syncing_from_master` 守卫 + `set_block_signals` 双层防递归; data (_reduce_all) vs UI (indeterminate) 分离让"曾经开启过"语义在 cfg 文件中清晰可追溯; `check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 README 双轨 #121 同步）+ 0 SCRIPT ERROR / 0 Parse Error
- **#120 — 审查 #120 (120%5==0 整点审查, 5 维度全 audit + 0 缺口 0 修复)**：本轮是 #116 T199 PauseMenu 5 verb row hover tooltip + F013.D CONTRIBUTING.md §9 6th verb 接入路径文档化（I025 28/28 PASS）/ #117 T200 HUD 5 verb 冷却条 reduce_flash 灰化 + T201 PlayerProfilePanel 2 个跨局聚合顶级行 AvgResonance/BestStreak（I026 28/28 PASS，T201 引入 1 pre-existing tscn `#` 注释 ERROR 由 #119 T203 修复）/ #118 T202 HUD 5 verb 冷却中半透明提示标签（I027 26/26 PASS，5 verb × "冷却中" Label modulate alpha=0.6 + 5 主题色）/ #119 T203 修 #117 ProfileAvgResonance pre-existing tscn `#` 注释 ERROR + T204 HUD 5 verb 名称标签 5 × Label 7pt 主题色（Pulse Amber Voice 0.949 / Bind Muted Violet 0.396 / Cut Coral Pulse 0.91 / Echo Glass Cyan 0.412 / Wave Pale Resonance 0.718）（I028 25/25 PASS）共 4 轮 polish + tech-debt 密集落地之后的全维度 audit. **审查前准备** — Godot 4.6.3 headless binary 沿用 #115 解压产物（cat + unzip -FF 已落盘，0 重新解压）. **审查结果（5 维度）**：(a) **代码质量** — 54 class_name 唯一 0 冲突（#115: 54 → #120: 54，0 差异，#116-#119 共 4 轮 polish 0 新增 class_name）/ 79 signal 拓扑完整 / 7 autoload 稳定 / 63 .gd 源码 + 29 .tscn（与 #115 一致，0 新增 .gd / 0 新增 .tscn）/ 0 TODO/FIXME/HACK / 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR. **#117 pre-existing ERROR 已闭环**：#119 T203 把 pause_menu.tscn 中 T201 引入的全部 `#` 注释（5 行大段 + ProfileAutoSave 行内 `# T138`）转成 tscn 合法的 `;` 注释，tscn parser 不再 abort，ProfileAvgResonance / ProfileBestStreak 2 节点在场景树中真实存在，运行时 @onready 报 "Node not found" ERROR 已消除（I028 25/25 断言验证）— #117 → #119 跨 2 轮修复闭环. (b) **玩法完整性** — 5 verb 闭环（Pulse/Bind/Cut/Echo/Wave）— D002.B 父类抽取 + H001 hotfix 修复 + F013.C 镜像 F013.B whole-tone scale + T194 Echo 漏修 + T197 8 调用点 ScreenShake.vibrate() 收口 5 阶强度梯度 + T198 5 verb hint J/K/L/Q/V 5 键位 + 3 组合技提示 + **T199 PauseMenu 5 verb row hover tooltip**（_VERB_HINT_DATA 7 字段 × 5 verb = 35 引用 + _build_verb_hint_tooltip() 11 行多行文本 + 2 Label tooltip_text 绑定）+ **T200 HUD 5 verb 冷却条 reduce_flash accessibility 灰化** + **T201 PlayerProfilePanel 2 个跨局聚合顶级行** + **T202 HUD 5 verb 冷却中半透明提示标签** + **T204 HUD 5 verb 名称标签**; 9 BGM 主题 + 5 桶 prewarm aggregator ~34ms 一次性; 5 存档槽 + CRC32 + 60s autosave; 14 成就 + 8 通知卡 + 8 宫格图标 + F014 unlock chime; 4 archive 房间 + 11 tutorial hints; 死亡 UX 完整; SaveLoadMenu 4 路 cancel 闭环 + T192 modal Esc chain input chain 早于 GUI 拦截; accessibility 闭环 reduce_shake / reduce_flash（T200 进一步 polish）/ reduce_vibration 3 CheckBox + ScreenShake.vibrate() 跨平台. **三层 UI polish 落地**：PauseMenu 5 verb row hover tooltip + HUD 5 verb 冷却中文字标签 + HUD 5 verb 名称标签，让玩家从 icon shape / progress bar fill / name label / 临时 cooldown label 4 个 UI 通道 100% 透明识别 verb 类型 + 状态. (c) **素材一致性** — 114/114 PNG 头校验 0 损坏（#115: 108 → #120: 114，6 个增量来自 #116-#119 期间 0 新增 PNG，T199/T200/T201/T202/T203/T204 全部 UI 标签 + 文字层面修改）/ ASSET_REGISTRY 73 条记录连续 A001-A073 命名严格递增 / STYLE_GUIDE 8+1 色板 100% 匹配 / 风格 0 漂移（T202 5 verb "冷却中" label 主题色 + T204 5 verb 名称 label 主题色严格对齐 verb fill style） / REJECTED.md 0 拒绝条目. (d) **文档同步** — ROADMAP 头部 #119 / CHANGELOG 120 条 100% 完整 / README 双语 zh-CN 0 滞后 / REVIEW_LOG #40-#120 活跃 + #5-#35 归档 / CONTRIBUTING.md F013.D §9 6th verb 接入路径 9 步骤 + 5 易错点 + 3 验证清单完整文档化（#116 F013.D 增量）. (e) **测试覆盖** — **64/64 smoke test 100% ALL PASS**（#115: 60 → #120: 64，4 个增量来自 #116 I025 / #117 I026 / #118 I027 / #119 I028，0 回归 0 pre-existing 失败）+ `check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 README 双轨 #120 验证） + 关键回归基线 20+ 套件 ALL PASS（5 verb + D002.B + H001 + F013.C + T189 + T192 + T194 + T195 + T196 + T197 + T198 + T199 + T200 + T201 + T202 + T203 + T204 全部干净）. **总体评级 A+** — 0 静态/运行时错误 / 0 素材/JSON/PNG 头损坏 / 0 class_name 冲突 / 0 测试失败 / 文档 100% 同步 / #117 pre-existing ERROR 已闭环. **下一轮 #121 (121%5==1 普通模式) 建议候选**（按价值/工时比排序）：(1) 14 成就 unlock chime 与全 14 BGM/9 主题 layering（15min，scope 跨 1-2 轮，商业化）/ (2) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示（10min，商业化，5 verb 完整闭环最后一环）/ (3) T156 [候选] Polish ArchiveStorm 主摄像机 shake 之前先 trigger 1f skybox rotate（10min，视听）/ (4) T189 modal Esc + T191 click cancel 同时按两键优先级（10min，边缘 case）/ (5) PlayerProfilePanel 顶级行 LongestRoom 补充（5min，polish 边际效用，与 T201 配套）
- **#119 — T203 修 #117 ProfileAvgResonance pre-existing ERROR (tscn `#` 注释违反 tscn 语法) + T204 HUD 5 verb 名称标签 (Pulse / Bind / Cut / Echo / Wave)**：`pause_menu.tscn` 把 #117 T201 引入的全部 `#` 注释 (5 行大段 + ProfileAutoSave 行内 `# T138`) 转成 tscn 合法的 `;` 注释（tscn 语法要求 `;` 起头，`#` 会让 Godot 4.6 tscn parser abort，后续 `ProfileAvgResonance` / `ProfileBestStreak` 2 节点被丢弃，运行时 @onready 报 "Node not found" ERROR 至今未修）；`pause_menu.gd` 加 T203 (#119) docblock 锚点说明本轮修复；`hud.tscn` 5 verb row (PulseRow / BindRow / CutRow / EchoRow / WaveRow) 在 Icon 后、Cooldown 前各插 1 个 7pt 主题色 Label 节点（PulseNameLabel / BindNameLabel / CutNameLabel / EchoNameLabel / WaveNameLabel）— Pulse Amber Voice 0.949 / Bind Muted Violet 0.396 / Cut Coral Pulse 0.91 / Echo Glass Cyan 0.412 / Wave Pale Resonance 0.718 严格对齐 5 verb 主题色（与 T202 (#118) "冷却中" label 主题色一致）；`hud.gd` 加 5 @onready var + 2 段 T204 (#119) 注释锚点（一段介绍 + 一段设计要点 3 条：always-visible / 7pt 主题色 / Icon 后 Cooldown 前位置选择理由）；`test_i028_t203_t204_smoke.gd` 25 项断言 PASS（T203 11：tscn 0 hash 注释 + 2 节点 + 1 parent + 2 文本 + 1 Glass Cyan + 2 路径 + 1 refresh fn + 1 anchor / T204 14：5 _*_name_label 引用 + 5 路径 + 1 T204 anchor + 5 节点 + 1 text 抽查 + 1 color 抽查）；**tech debt + UX polish 双家族 ≤10min 范围**，修 ProfileAvgResonance 1 pre-existing ERROR + HUD 5 verb 行色域分工 3 个 UI 通道（icon + bar + name label）100% 透明；`check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 README 双轨 #119 同步）+ 0 SCRIPT ERROR / 0 Parse Error
- **#118 — T202 HUD 5 verb 冷却中半透明提示标签 (5 verb × "冷却中" Label)**：`hud.tscn` 在 5 verb row (PulseRow / BindRow / CutRow / EchoRow / WaveRow) 各加 1 个 Label 节点 (PulseCooldownLabel / BindCooldownLabel / CutCooldownLabel / EchoCooldownLabel / WaveCooldownLabel)，text = "冷却中" 7pt 居中，初始 visible=false，modulate alpha=0.6 + 主题色（Pulse 暖 0.949 / Bind 紫 0.396 / Cut 珊瑚 0.91 / Echo 青 0.412 / Wave 浅青 0.718）匹配 verb fill style；`hud.gd` 加 5 @onready var（`_pulse_cooldown_label` / `_bind_cooldown_label` / `_cut_cooldown_label` / `_echo_cooldown_label` / `_wave_cooldown_label`）+ 新 helper `_update_cooldown_label(label: Label, ratio: float)`（null 守卫 + 0.001 浮点容差 + `label.visible` 写切换），`_process()` 5 verb 调冷却比之后同帧调 helper，ratio > 0.001 时显示，ratio == 0 时隐藏；`test_i027_t202_smoke.gd` 26 项断言 PASS（5 @onready 引用 + 5 路径 + helper 函数 + null 守卫 + 0.001 阈值 + label.visible 写 + 5 调 + 5 节点 + 5 主题色匹配）；accessibility polish 单轮 ≤10min 范围，玩家不需盯 progress bar 也能立刻知道 verb 是否冷却中；与 T200 reduce_flash 灰化不冲突（label 是文字，色域分工靠 progress bar 维持）；`check_smoke_consistency.sh` 6 规则 + per-test 3 套件全绿；**0 新增 SCRIPT ERROR**（本次 tscn 解析与 #117 之前一样干净）
- **#117 — T200 HUD 5 verb 冷却条 reduce_flash accessibility 灰化 + T201 PlayerProfilePanel 2 个跨局聚合顶级行 (AvgResonance / BestStreak)**：`hud.gd` 新增 `const _REDUCED_COLOR_MODULATE = Color(0.55, 0.55, 0.6, 0.75)` (5 verb bar 灰化色) + `const _NORMAL_COLOR_MODULATE = Color(1, 1, 1, 1)` (还原全色) + `_reduced_flash_applied: bool` 状态缓存；`_process()` 末尾通过 `_has_screen_shake()` 守卫（headless 测试场景）调 `ScreenShake.is_reduce_flash()`，当状态变化时（`!=` 守卫，避免每帧重设 modulate）调 `_apply_reduced_flash_modulate(reduce: bool)` 一次性遍历 5 个 cooldown bar (pulse/bind/cut/echo/wave) 写 modulate；`pause_menu.tscn` 在 `ProfileAutoSave` 后、`HSep1` 前插 2 个 Label (`ProfileAvgResonance` / `ProfileBestStreak`)；`pause_menu.gd` 新增 `_refresh_top_aggregate_rows()` 函数：AvgResonance = `sum(shards_collected) / sum(rooms_cleared)` 跨 run 聚合比（而非 per-run 平均，避免 0 房 run 拖偏）+ `best_run` = 最高 `rooms_cleared`（tied 时取最新，让"最近破纪录"显示最前），零样本/0 房分别走 "—" / "无房记录" 占位；`test_i026_t200_t201_smoke.gd` 28 项断言 PASS（T200 12 + T201 14 + 顺序检查 2）；accessibility polish + profile polish 双家族，单轮 ≤10min 范围；`check_smoke_consistency.sh` 6 规则 + per-test 3 套件全绿

- **#116 — T199 PauseMenu 5 verb row hover tooltip (cost/cooldown/radius/键位/中文描述) + F013.D 第 6 verb 接入路径文档化 (本轮)**：[`src/scripts/pause_menu.gd`](file:///workspace/src/scripts/pause_menu.gd) **T199 双源合一**: (1) 加 const `_VERB_HINT_DATA` (5 verb 字典数组, 字段 key/name_zh/name_color/cost/cooldown_s/radius_px/desc_zh, 7 字段 × 5 verb = 35 引用, 数据从 player.tscn 5 verb 节点 + project.godot 5 input action 单点摘抄, 唯一权威源); (2) 加 func `_build_verb_hint_tooltip() -> String` (5 行 bullet + 1 header + 5 描述缩进行 = 11 行多行文本, 头 "5 声波能力 — 悬停查看详细" + 5 verb 行 "• Pulse (J) — 消耗 15  冷却 0.5s  半径 48px" + 5 缩进描述); (3) _ready() 末尾同时绑到 `_stat_abilities` (StatsPanel) + `_profile_abilities` (ProfilePanel) 两个 Label 的 `tooltip_text` 字段, 双源同步无需刷新. **键位 + 数值 + 描述 3 维度**: 之前 row 只显示 verb 名 + 次数, 现在 hover 即看到 1) 键位 [J/K/L/Q/V] 2) 数值 (cost/cooldown/radius) 3) 一句中文描述. **数据 vs UI 解耦**: tooltip 文本从 _VERB_HINT_DATA 单一来源派生, 5 verb 数值变更 (e.g. Pulse cost 15 → 20) 只需改 1 处, _build_verb_hint_tooltip 自动同步所有引用方 (2 Label). 玩家 PauseMenu hover 5 verb row 0.3s 即弹 11 行 tooltip, 5 verb 数值 + 用途 100% 透明, accessibility 教学通道从 tutorial hints（前 4 个房间渐进）扩到 PauseMenu 任意时刻. [`CONTRIBUTING.md`](file:///workspace/CONTRIBUTING.md) **F013.D §9 完整文档化**: §9.1 9 接入步骤 (1 脚本 extends _verb_ability_base.gd / 2 player.tscn 节点 / 3 Input Map action / 4 STYLE_GUIDE 色板 / 5 player.gd handler / 6 vfx / 7 PlayerStats 6 字段+1 method / 8 PauseMenu 4 处同步 / 9 冒烟测试) + §9.2 5 易错点 (按 H001 #99 历史回归频率排序: cooldown 重声明 / super._ready 漏调 / 6 处表层漏色 / _VERB_HINT_DATA 漏更新 / input action 冲突) + §9.3 验证清单 (静态解析 + 冒烟测试 I025). 未来第 6 verb 真要做时 9 步接入路径已结构化文档化, 30 分钟内可落地, 0 风险踩 H001 #99 cooldown 重声明 5 回归点. [`tools/test_i025_t199_f013d_smoke.gd`](file:///workspace/tools/test_i025_t199_f013d_smoke.gd) **I025 28/28 PASS**: T199.PM 22 字段 + F013D.DOC 6 文档. **59/59 smoke test 套件 100% PASS** (#115 58 + I025 +1, 0 回归 0 pre-existing 失败) + `check_smoke_consistency.sh` 7/7 规则 PASS (rule 7 2 README matches ITERATION_COUNT 116) + 3 回归测试 PASS + Godot 静态解析 0 ERROR + Godot 运行时冒烟 0 ERROR. **关键里程碑**: 116 轮迭代 / T199 PauseMenu 5 verb row hover tooltip 完成 UX 完整性 / F013.D CONTRIBUTING.md §9 文档化第 6 verb 接入 9 步 + 5 易错点. **下一轮 #117 (117%5==2 普通模式) 候选**: (1) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min, 商业化) / (2) 14 成就 unlock chime 与全 14 BGM/9 主题 layering (15min, scope 跨 2 轮) / (3) 5 verb PlayerProfilePanel 顶级行 (AvgResonance / BestStreak / LongestRoom) 补充 (10min, scope 跨 1 轮 polish) / (4) HUD 5 verb 冷却条颜色随 reduce_flash 减弱 (5min, accessibility polish).

- **#115 — 审查 #115 (本轮, 115%5==0 审查模式)**：整点审查 + 5 维度全 audit + 0 缺口 0 修复。**审查前准备** — Godot 4.6.3 headless binary (138MB) 通过 `cat *.z0* *.zip > /tmp/godot_full.zip` + `unzip -FF` 强容错解压成功（已踩 F003 #80 Python 3.14+ 坑 → B-1 `unzip -FF` 兜底方案正常）。**审查结果（5 维度）**：(a) **代码质量** — 54 class_name 唯一 0 冲突 / 79 signal 拓扑完整 / 7 autoload 稳定（GameState/PlayerStats/SaveSystem/AudioManager/AudioManagerEnhanced/ScreenShake/PlayerActionGate）/ 63 .gd 源码 + 29 .tscn / 0 TODO/FIXME/HACK / 0 SCRIPT ERROR / 0 Parse Error / 0 运行时 ERROR。(b) **玩法完整性** — 5 verb 闭环 (Pulse/Bind/Cut/Echo/Wave) — D002.B 父类抽取 + H001 hotfix 修复 + F013.C 镜像 F013.B whole-tone scale + T194 Echo 漏修 + T197 8 调用点 ScreenShake.vibrate() 收口 (5 阶强度梯度 [0.3, 0.35, 0.4, 0.5, 0.55, 0.7, 0.85]) + T198 5 verb hint 文案完整覆盖 J/K/L/Q/V 5 键位 + 3 组合技提示；9 BGM 主题 + 5 桶 prewarm aggregator ~34ms 一次性；5 存档槽 + CRC32 + 60s autosave；14 成就 + 8 通知卡 + 8 宫格图标 + F014 unlock chime；4 archive 房间 + 11 tutorial hints；死亡 UX 完整（T092 freeze + T093 grayscale + T114 lay-down + T115 碑文 + T116 残影 + F016 SFX + 默认回 Hub）；SaveLoadMenu 4 路 cancel 闭环 (Esc/取消按钮/Backdrop click/Enter) + T192 modal Esc chain input chain 早于 GUI 拦截；accessibility 闭环 reduce_shake/reduce_flash/reduce_vibration 3 CheckBox + ScreenShake.vibrate() 跨平台。(c) **素材一致性** — 108/108 PNG 头校验 0 损坏 / ASSET_REGISTRY 73 条记录连续 A001-A073 命名严格递增 / STYLE_GUIDE 8+1 色板 100% 匹配 / 风格 0 漂移。(d) **文档同步** — ROADMAP 头部 #115 / CHANGELOG 114 条 100% 完整 / README 双语 zh-CN 0 滞后 / REVIEW_LOG #40-#110 活跃 + #5-#35 归档。(e) **测试覆盖** — **60/60 smoke test 100% ALL PASS** + `check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 README 双轨验证）。**#110 → #115 增量验证**（4 轮 polish 落地）：#111 T192 + T193 / #112 T194 + T195 / #113 T196 / #114 T197 + T198。**总体评级 A+** — 0 静态/运行时错误 / 0 素材/JSON/PNG 头损坏 / 0 class_name 冲突 / 0 测试失败 / 文档 100% 同步。**下一轮 #116 (116%5==1 普通模式) 建议候选**：(1) T156 [候选] Polish ArchiveStorm 主摄像机 shake 之前先 trigger 1f skybox rotate (10min) / (2) T199 [候选] Code PauseMenu 5 verb row 加 hint tooltip (10min) / (3) F002 [信息] Doc `check_smoke_consistency.sh` 加规则 ⑧ hook (5min) / (4) F013.D [候选] Code 6th verb 接入路径 (10min)

- **#114 — T197 玩家 5 verb 触发后 ScreenShake.vibrate() 统一收口 (7 调用点 5 阶强度梯度) + T198 5 verb hint 文案完整覆盖 (Bind K / Echo Q / Wave V + 3 组合技提示) (本轮)**：[`src/scripts/player.gd`](file:///workspace/src/scripts/player.gd) **T197 5 verb vibrate 收口 (7 调用点 + 1 5 阶强度梯度)**：`_on_pulse_fired` 末尾加 `ScreenShake.vibrate(0.4, 0.1)` (5 verb 标准强度, 反映"主动施法"语义, 与 PULSE preset 2.0/0.10s 屏抖同步结束);`_on_bind_fired` 加 `ScreenShake.vibrate(0.35, 0.1)` (Bind 牵引"温柔"语义, 略低于 Pulse 0.4, 反映"非攻击"verb 触感);`_on_cut_fired` 加 `ScreenShake.vibrate(0.5, 0.06)` (Cut 短促锋利语义, duration=0.06 是 Pulse 0.1 的 60% 短促"shing"触感, strength 0.5 略高反映"砍"动作瞬时强度, 与 CUT preset 1.5/0.06s 屏抖严格同步);`_on_echo_fired` 加 `ScreenShake.vibrate(0.3, 0.12)` (Echo"回声"语义, strength 0.3 是 5 verb 最弱反映"探测/反馈"非攻击 verb, duration 0.12 略长 Pulse 0.1 反映"回声"语义延展);`_on_wave_fired` 加 `ScreenShake.vibrate(0.55, 0.18)` (Wave AOE 横扫语义, strength 0.55 是 5 verb 最高反映群体波, duration 0.18 最长与 0.4s 扩散期前半段同步);`_on_wave_combo` 加 `ScreenShake.vibrate(0.7, 0.25)` (combo 重击, 比普通施法 0.55/0.18 加权 ~1.3×, duration 0.25 略短于屏抖 0.4s 让屏抖"余震"在最后 0.15s 视觉先于触觉结束, 模拟"屏幕先静手还在抖"层次);`take_damage` 加 `ScreenShake.vibrate(0.5, 0.15)` (受击"被击中"短促, strength 0.5 高于 5 verb 0.4 让玩家从触觉强度区分"我在施法" vs "我被打", duration 0.15 与 DAMAGE preset 同步);`die()` 加 `ScreenShake.vibrate(0.85, 0.4)` (死亡重击, 整轮最重触觉 0.85, 5 阶强度梯度 echo 0.3 < bind 0.35 < pulse 0.4 < cut 0.5 = damage 0.5 < wave 0.55 < combo 0.7 < death 0.85 让玩家从触觉就能感知"死"是终极反馈, duration 0.4 与屏抖 DEATH 0.4s 同步同时与 freeze-frame 0.15s 视觉错位, 玩家感到"屏幕已停手还在抖"). **has_method("vibrate") 守卫保持稳健**: 8 处 vibrate 调用全部有 `if ScreenShake.has_method("vibrate"):` 守卫, 兼容老 autoload 版本, headless test SceneTree 无 ScreenShake 不报错. **5 阶强度梯度设计**: 与屏抖 shake_preset 5 阶 preset (PULSE 2.0 / BIND 1.0 / CUT 1.5 / DAMAGE 3.0 / DEATH 5.0) 平行, 玩家从触觉就能区分 verb 类型 + 受伤 + 死亡, 与 reduce_vibration 偏好设置兼容 (玩家勾选减弱手柄振动, 8 处 vibrate 全部 no-op, 屏抖 + SFX + VFX 正常播放). **修复后**: 玩家 5 verb 施法 / combo / 受伤 / 死亡 8 个事件全部自动触发跨平台触觉反馈 (gamepad rumble + mobile vibrate_handheld), 一处 set_reduce_vibration 拦截全平台, 触觉反馈闭环完成. [**`data/rooms/archive_02.json`**](file:///workspace/data/rooms/archive_02.json) / [**`data/rooms/archive_03.json`**](file:///workspace/data/rooms/archive_03.json) / [**`data/rooms/archive_04.json`**](file:///workspace/data/rooms/archive_04.json) **T198 5 verb hint 文案完整覆盖**: archive_02 加 intro_bind + intro_bind_combo 2 条 (Bind K 牵引 + Bind+Pulse 组合技);archive_03 加 intro_echo + intro_ink_warden 2 条 (Echo Q 反弹护盾 + Ink Warden 破盾提示);archive_04 在原 intro_double_warden 之上追加 intro_wave + intro_wave_combo 2 条 (Wave V AOE 横扫 + Bind+Wave 双 warden 组合技). **5 verb 键位 [J K L Q V] 全部覆盖**: archive_01.intro_pulse [J] / archive_02.intro_bind [K] / archive_01.intro_cut [L] / archive_03.intro_echo [Q] / archive_04.intro_wave [V] 严格按 verb 听觉坐标顺序渐进教学, 加 3 组合技提示 (Bind+Pulse / Echo+Cut / Bind+Wave) 帮助玩家发现 verb 协同. 文本参考 STYLE_GUIDE 5 调色五元组 (Coral Pulse / Bind 紫 / Amber Cut / Glass Cyan Echo / Pale Resonance Wave) + ROADMAP verb 数值 (ink 消耗 / 冷却 / 半径 / 时长) 让新玩家 30 秒内掌握 5 verb + 2 组合. [`tools/test_i024_t197_t198_smoke.gd`](file:///workspace/tools/test_i024_t197_t198_smoke.gd) **I024 冒烟测试 29/29 PASS**: T197 12 项 (5 verb + combo + damage + death 8 处 vibrate 调用数值验证 + has_method 守卫 + T197 注释锚点 + 5 阶强度梯度) + T198 11 项 (3 JSON 6 hints 字段验证 + 3 JSON 解析成功 + 5 verb 键位覆盖) + JSON 6 项 (解析 + 字段是 Array). **58/58 smoke test 套件 100% PASS** (#113 57 + I024 +1, 0 回归 0 pre-existing 失败) + `check_smoke_consistency.sh` 7/7 规则 PASS (rule 7 2 README matches ITERATION_COUNT 114) + 9/9 回归测试 PASS (T098+T100 / T103 / T150+T147+T149 / T143+T145+T146 / T167+T168+F006 / T165+T166+F005 / T162+T159 / T144+T148+T154 / I023 T196). **关键里程碑**: 114 轮迭代 / T197 玩家 5 verb 触发后 ScreenShake.vibrate() 收口完成 accessibility 触觉反馈闭环 (gamepad rumble + mobile vibrate 跨平台 + reduce_vibration 拦截) / T198 5 verb hint 文案完整覆盖 J/K/L/Q/V 5 键位 + 3 组合技提示 / 57+1=58 smoke test 套件. **下一轮 #115 (115%5==0 审查模式) 建议候选**: (1) **审查模式触发** (按 GUIDE 每 5 轮) — 跑全 58 smoke test + check_smoke_consistency.sh 7 规则 + 5 阶段审查 (需求兑现 / 一致性 / 质量 / 性能 / 风险) 写 REVIEW_LOG.md #115 段 / (2) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min, 商业化) / (3) 14 成就 unlock chime 与全 14 BGM/9 主题 layering (15min, scope 跨 2 轮) / (4) F013.D 6th verb 接入路径 (10min, future-proof).

- **#113 — T196 settings menu accessibility 减弱手柄振动 (iOS/Android gamepad vibration) + 跨平台 ScreenShake.vibrate() 路由 (本轮)**：[`src/scenes/settings_menu.tscn`](file:///workspace/src/scenes/settings_menu.tscn) VideoPanel 末尾 (ReduceFlashCheck 之后, 与 T195 reduce_shake / reduce_flash 同一无障碍区域) 加 1 个 CheckBox "减弱手柄振动" (size_flags_horizontal=4 居中, font_size=9 与前 2 个 CheckBox 一致, 含 tooltip 跨设备说明 joypad rumble + mobile vibrate 全部 no-op);[`src/scripts/settings_menu.gd`](file:///workspace/src/scripts/settings_menu.gd) T196 加 1 @onready (`_reduce_vibration_check` 绑 VideoPanel/ReduceVibrationCheck) + 1 状态字段 (`_reduced_vibration: bool = false`, 与 T195 _reduced_shake / _reduced_flash 同模式) + 1 toggle handler (`_on_reduce_vibration_toggled` 调 ScreenShake.set_reduce_vibration 走 _has_screen_shake_autoload 守卫, headless test SceneTree 可能无 ScreenShake) + 1 _ready connect (`_reduce_vibration_check.toggled.connect(_on_reduce_vibration_toggled)`) + _load_settings 读 [accessibility] section reduce_vibration (默认 false) + set_block_signals 避免反复写 cfg + live-push 到 ScreenShake.set_reduce_vibration 让 ScreenShake 立即遵守配置, _save_settings 写 [accessibility] section reduce_vibration (与 T136 autosave 同样 last-writer-wins 模式);[`src/autoload/screen_shake.gd`](file:///workspace/src/autoload/screen_shake.gd) T196 加 _reduced_vibration 状态字段 (默认 false) + 2 公开 API (set_reduce_vibration / is_reduce_vibration), set_reduce_vibration(true) 跨 pad 调 Input.stop_joy_vibration 停 in-flight 手柄振动 (玩家刚勾选时 0.2-0.5s 残余振动立刻消失; vibrate_handheld 没有 stop API, mobile 振动 fire-and-forget 自动衰减不需手动 stop);**新增 vibrate(strength, duration) 跨平台路由 helper**: 调用方 (player / 5 verb ability / BOSS) 统一调 `ScreenShake.vibrate(0.5, 0.1)` 而不是直接 Input.vibrate_handheld / start_joy_vibration, 一处 set_reduce_vibration 拦截全平台;内部根据当前 device type 自动分流: 已连接 gamepad → Input.start_joy_vibration (桌面 / 手柄 rumble); Input.vibrate_handheld (iOS/Android mobile, 1 次性触发, 不依赖手柄). 参数钳制: clampf(strength, 0.0, 1.0) 防御 caller 传 strength=2.0, maxf(duration, 0.0) 防御 duration=-1. 入口 _reduced_vibration 早退 (no-op, 玩家手柄不震 / 手机不嗡 但仍听见 SFX / 看见屏抖 + VFX, gameplay 完整保留).**修复后**: 玩家 (前庭敏感 / 触觉敏感 / 长期手柄玩家) 可在 settings menu 视频标签一键关闭所有手柄/手机振动, 仍听到 SFX 仍看见屏抖 + VFX, 与 T195 reduce_shake 互补 (T195 关视觉, T196 关触觉). **I023 24/24 PASS** — [`tools/test_i023_t196_reduce_vibration_smoke.gd`](file:///workspace/tools/test_i023_t196_reduce_vibration_smoke.gd) 新文件 24 断言 ALL PASS (tscn 5 项 + settings_menu 9 项 + screen_shake 10 项). **57/57 smoke test 套件 100% PASS** (#112 56 + I023 +1, 0 回归 0 pre-existing 失败). **下一轮 #114 (普通模式, 114%5=4) 候选**: (1) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min, 商业化) / (2) 14 成就 unlock chime 与全 14 BGM/9 主题 layering (15min, scope 跨 2 轮) / (3) F013.D 6th verb 接入路径 (10min, future-proof) / (4) 5 verb 切分 hot key remap UI 已经 T194 落地 (剩 5 verb hint 文案 / tooltip 改进, 10min) / (5) 玩家侧调用 ScreenShake.vibrate() 把现有 inline 振动统一收口 (15min, refactor 玩家 5 verb 触发后立即振动 0.1s 0.4 strength).
- **#112 — T194 Echo (Q) ACTION_NAMES 漏修 + 5 verb 三分组 UI (移动/声波能力/交互) + T195 settings accessibility 减弱屏幕震动 / 减弱屏幕闪烁 (本轮)**：[`src/scripts/settings_menu.gd`](file:///workspace/src/scripts/settings_menu.gd) **T194 echo 漏修 + 5 verb 三分组 UI**：ACTION_NAMES dict 补回 echo = "Echo 反射护盾" 放在 cut 与 interact 之间 (严格按 verb 听觉坐标 pulse→bind→cut→echo→wave, 修复 T103 #74 Wave 引入时漏 echo 的硬伤);_DEFAULT_BINDINGS 同步补 echo: {"type": "key", "physical_keycode": 81} (Q=81), 9 actions 全覆盖;新增 ACTION_CATEGORY const 显式把 9 actions 标为 movement / verb / interact 三组 (3 movement + 5 verb + 1 interact);新增 CATEGORY_RENDER_ORDER const = Array[Dictionary] 3 段 (移动/声波能力/交互, 含 Pale Resonance / Amber Voice / Glass Cyan 三个 STYLE_GUIDE 调色);_build_controls_list() 改为双层循环 (外层 CATEGORY_RENDER_ORDER, 内层 ACTION_CATEGORY 过滤), 第一个该组 action 之前插 1 个分组标题 Label "— 移动 —" / "— 声波能力 —" / "— 交互 —" 颜色用对应段 color, font_size=8, 居中, SIZE_EXPAND_FILL;**修复后**: 控件菜单 9 actions 全部可重映射 (echo 不再漏), 玩家一眼区分 5 verb 与 移动 / 交互, 5 verb 顺序与 HUD 5 冷却条 / STYLE_GUIDE 5 调色五元组 / F013.C MIDI 顺序 (69/71/73/75/77) 全部一致。[`src/scenes/settings_menu.tscn`](file:///workspace/src/scenes/settings_menu.tscn) **T195 settings accessibility**: VideoPanel 末尾 (ScaleOptions 之后) 加 1 个副标题 Label "无障碍" (8pt 居中) + 2 个 CheckBox "减弱屏幕震动" / "减弱屏幕闪烁" (size_flags_horizontal=4 居中, 含 tooltip 解释 reduce 范围 + no-op 语义, 不动 SFX / VFX 正常播放);[`src/scripts/settings_menu.gd`](file:///workspace/src/scripts/settings_menu.gd) T195 加 2 @onready + 2 toggle handler + 1 _has_screen_shake_autoload helper (has_node("ScreenShake") 守卫, headless test SceneTree 可能无 ScreenShake) + _load_settings 读 [accessibility] section 同步到控件 + set_block_signals 避免反复写 cfg + live-push 到 ScreenShake.set_reduce_*, _save_settings 写 [accessibility] section (与 T136 autosave 同样 last-writer-wins 模式), _on_restore_all_pressed 还原 reduce 状态到 off (与 T136 同模式: block_signals 显式 uncheck + ScreenShake.set_reduce_*(false));[`src/autoload/screen_shake.gd`](file:///workspace/src/autoload/screen_shake.gd) T195 加 _reduced_shake / _reduced_flash 状态字段 (默认 false) + 4 公开 API (set_reduce_shake / set_reduce_flash / is_reduce_shake / is_reduce_flash), set_reduce_shake(true) 停 in-flight tween + 重置 camera offset (玩家勾选 0.2-0.3s 残余震动立刻消失), set_reduce_flash(true) 清空 _active_grayscale / _active_color_flash dict (多层 flash queue_free), shake() / flash_color() / flash_grayscale() 入口早退 (no-op, SFX + VFX 仍正常播放);punch_rotation 不在 reduce 范围 (T156 skybox 1 帧 reaction 是 BOSS_PHASE2 起拍, 不是可避免的闪烁)。**修复后**: 玩家 (前庭敏感 / 光敏感 / 偏头痛 / 自闭症) 可在 settings menu 视频标签一键关闭所有屏幕震动 / 闪烁, 仍听到 SFX 仍看见 VFX, gameplay 完整保留。**I022 28/28 PASS** — [`tools/test_i022_t194_t195_smoke.gd`](file:///workspace/tools/test_i022_t194_t195_smoke.gd) 新文件 28 断言 ALL PASS (T194 8 项 + T195 settings 10 项 + T195 screen_shake 10 项)。**57/57 smoke test 套件 100% PASS** (#111 56 + I022 +1, 0 回归 0 pre-existing 失败)。**下一轮 #113 (普通模式) 候选**: (1) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min, 商业化) / (2) 14 成就 unlock chime 与全 14 BGM/9 主题 layering (15min, scope 跨 2 轮) / (3) F013.D 6th verb 接入路径 (10min, future-proof) / (4) 5 verb 切分 hot key remap UI 已经 T194 落地 (剩 5 verb hint 文案 / tooltip 改进, 10min) / (5) settings menu 加 vibration (iOS/Android 手柄振动) 选项 (15min, accessibility, 与 T195 reduce_shake 复用同一 _reduced_shake 字段, 跨设备).

- **#111 — T192 modal Esc priority fix (DeleteBtn focus 误删防御) + T193 F016.C Death SFX 7 房间 × 2 层 覆盖率 audit (本轮)**：[`src/scripts/save_load_menu.gd`](file:///workspace/src/scripts/save_load_menu.gd) **T192 input chain 重构**：Esc → cancel 拦截点从 `_unhandled_input` 移到 `_input`（重构而非新增）。**bug 背景**：T189 (#108) 当时假设 modal 焦点必定在 `CancelBtn`（`_show_confirm_modal` 末尾 `grab_focus()` 强制），`_unhandled_input` 兜底 OK。但玩家按 Tab/方向键把焦点移到 `DeleteBtn` 之后，按 Esc 会触发 `DeleteBtn` 的 `ui_cancel` 默认行为 → `pressed` 信号 → `_on_confirm_delete` → **删存档**（灾难）。`_unhandled_input` 跑在 GUI subsystem *之后*，事件已经被 `DeleteBtn` consume 看不到，救不回来。**修法**：在 `_input` 拦截 `ui_cancel`（`_input` 跑在 GUI 之*前*，Godot 4 input flow 顺序：`_input` → GUI subsystem → `_shortcut_input` → `_unhandled_key_input` → `_unhandled_input`）。`set_input_as_handled()` 让 GUI 看不到事件，不管焦点在 `CancelBtn` 还是 `DeleteBtn` 都安全。`_unhandled_input` 保留 F 键 filter（T190 #109），弹窗可见时早退（F 不会穿透到背后 slot 列表）。**input chain 改造细节**：(1) 新增 `_input(event: InputEvent) -> void` handler（T192 (#111) docblock marker），开头 `_is_confirm_modal_visible()` 守卫（modal 隐藏时不拦 Esc，让 `_on_back` 走完整菜单关闭链），modal 可见时 `event.is_action_pressed("ui_cancel")` 触发 `_on_confirm_cancel()` + `get_viewport().set_input_as_handled()`；(2) `_unhandled_input` 保留但删除 Esc 拦截（防双触发：单帧 _input + _unhandled_input 各一次调 `_on_confirm_cancel`），只留 F 键 filter；(3) `_is_confirm_modal_visible` polarity 反转（之前 `if not _is_confirm_modal_visible(): return` → 现在 `if _is_confirm_modal_visible(): return`），对应新的 _unhandled_input 职责（modal 可见时不响应 F 键，filter 不应该穿透到弹窗背后）。**安全链完整**：Esc → `_input` (GUI 之前拦截) → `_on_confirm_cancel` → `_hide_confirm_modal`，焦点在 `DeleteBtn` 时也安全（DeleteBtn 的 `ui_cancel` 默认行为根本拿不到 Esc 事件）。[`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) **T193 F016.C audit 注释补全**：`play_death_lay_down()` 函数前 docblock 补 F016.C (#111) 锚点注释（全 7 房间 universal 覆盖声明 + 单链路 GameState.take_damage → player.die() → play_death_lay_down 锁定 + F016.B _death_sfx_playing flag 双重防御 7 房间内任意 death trigger 0.5s 窗口内多次触发不 2x 嗡鸣 + smoke test_i021 守住回归）。**I021 84 项锚点 smoke 测试整合** —— [`tools/test_i019_t189_f016b_esc_death_sfx_bgm_smoke.gd`](file:///workspace/tools/test_i019_t189_f016b_esc_death_sfx_bgm_smoke.gd) 从 36 → **44 PASS** (+8 个 T192 锚点：_input 函数 + 800 char docblock T192 锚点 + modal 守卫 + ui_cancel 监听 + _on_confirm_cancel + set_input_as_handled 阻断 GUI + _unhandled_input 已不含 ui_cancel 防双触发 + T189 F 键 polarity 反转 3 项) + [`tools/test_i021_t193_f016c_death_sfx_audit_smoke.gd`](file:///workspace/tools/test_i021_t193_f016c_death_sfx_audit_smoke.gd) 新文件 **39/39 PASS** (7 room 路径 + 7 room 不直接调 .die() 统一走 GameState.take_damage + GameState.take_damage 5 锚点 + player.die() SFX 触发时序 2 项 + prewarm_misc_sfx 含 _generate + F016 原 SFX 层 4 项 + F016.B idempotency guard 6 项 + F016.C 锚点注释 2 项)。**57/57 smoke test 套件 100% PASS** (#110 56 + I021 +1 = 57, 0 回归) + `check_smoke_consistency.sh` 7/7 规则 PASS + 0 SCRIPT ERROR。**关键里程碑**: 111 轮迭代 / T192 modal Esc priority fix 完成 input chain 早期拦截 (4 路 cancel 同源链最终安全化) / F016.C Death SFX 7 房间 × 2 层 audit 完成 coverage 100% 验证 / SaveLoadMenu Esc chain 0 误删存档 / Death SFX 0 房间绕开单链路。**下一轮 #112 (普通模式, 112%5=2) 候选**: (1) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min, 商业化) / (2) 14 成就 unlock chime 与全 14 BGM/9 主题 layering (15min, scope 大) / (3) F013.D 6th verb 接入路径 (10min, future-proof) / (4) 5 verb 切分 hot key remap UI (15min, accessibility) / (5) settings menu 加 vibration / screen-flash reduce 选项 (15min, accessibility)。
- **#110 — 审查 #110 (本轮, 110%5==0 review mode)**：完整代码质量 / 玩法 / 素材 / 文档 4 维度 audit + **修复 6 个 pre-existing 失败 (1 SCRIPT ERROR + 5 smoke test 套件)**。**(1) SCRIPT ERROR 热修** — [`src/scripts/save_load_menu.gd`](file:///workspace/src/scripts/save_load_menu.gd) HintLabel: `bbcode_enabled = true` 是 `RichTextLabel` 独有 property 但 HintLabel 是 `Label` 节点 (#109 T190 BBCode 提示时类型选错). [`src/scenes/save_load_menu.tscn`](file:///workspace/src/scenes/save_load_menu.tscn) `HintLabel` 节点 type `Label → RichTextLabel` (含 bbcode_enabled=true 显式声明 + scroll_active=false + fit_content=true) + [`save_load_menu.gd`](file:///workspace/src/scripts/save_load_menu.gd) `_hint_label: Label → RichTextLabel` 字段类型同步改. **(2) I011 (#97) F013.C MIDI 同步** — 5 verb 起始 MIDI 已迁 whole-tone scale (69/71/73/75/77), 旧 T181 (69/72/76/79/81 不均) 硬编码测试改新值. **(3) I017 (#106) F013.B TAIL MIDI 同步** — TAIL 起始 MIDI 同步 whole-tone (73/75/77/79/81), 旧 (73/76/80/81/85) 改新. **(4) I019 (#108) T189 锚点定位修复** — 原测检查函数体内 T189 锚点, 但 T189 锚点其实在 docblock (函数前). 改检查 helper 上方 500 char 范围 docblock. **(5) T165/T166 (#85) H001 父类迁移补漏** — `windup_time=0.10` / `_windup_vfx` 字段 #99 H001 后在 _verb_ability_base.gd, 原测硬编码子类改"在 base 或 subclass 任一处"双轨. **(6) T167/T168 (#86) H001 补漏同源** — bind_ability / echo_ability `_windup_vfx` 同样已迁 base, 双轨校验. **审查结果**: 0 SCRIPT ERROR / 0 Parse Error / 0 runtime ERROR + 0 class_name 冲突 + 0 TODO/FIXME/HACK + 56 class_name 唯一 + 79 signal 完整 + 7 autoload 一致 + 108 PNG 头合法 + ASSET_REGISTRY 73 记录 100% 同步 + **56/56 smoke test 套件 100% PASS** (本轮修 5 套件 pre-existing 失败 + 1 SCRIPT ERROR 热修, 0 回归) + `check_smoke_consistency.sh` 7/7 规则 PASS + 文档 100% 同步. **关键里程碑**: 110 轮迭代 / 5 verb audio 全家族闭环 / 5 verb cooldown TAIL+READY whole-tone scale 严格镜像 / F013.C 1.5 八度 wide spread 收回 1 个八度 tight spread / SaveLoadMenu 4 路 cancel 闭环 (Esc / 取消按钮 / Backdrop click / Enter). **下一轮 #111 (普通模式) 候选**: (1) F016.C Death SFX 触发点 audit 全 7 房间 × 2 SFX 覆盖率 (10min, polish) / (2) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min, 商业化) / (3) 14 成就 unlock chime 与全 14 BGM/9 主题 layering (15min, scope 大) / (4) T189 modal Esc chain + T191 click cancel 同时按两个键优先级 (5min) / (5) F013.D 6th verb 接入路径 (10min, future-proof).

- **#109 — F013.C 5 verb whole-tone 2-semitone microtuning + T190 SaveLoadMenu F 键过滤 + T191 ConfirmBackdrop click-to-cancel (本轮)**：[`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) F013.C 5 verb cooldown READY jingle MIDI start 从 T181 原始设计 (69/72/76/79/81 → 间隔 3/4/3/2 半音不均) 改为 **whole-tone scale** (69/71/73/75/77 → 严格 2 半音间隔);每个 verb 各 ±2 半音 fine-tuning 移位: pulse 69 → 69 (0 shift, A4 锚点保留) / bind 72 → 71 (-1, C5→B4) / cut 76 → 73 (-3, E5→C#5) / echo 79 → 75 (-4, G5→D#5) / wave 81 → 77 (-4, A5→F5);F013.B 5 verb cooldown TAIL jingle MIDI start 同步从 (73/76/80/81/85) 改为 (73/75/77/79/81) 也是 whole-tone scale 2 半音间隔, 与 READY 严格镜像 (TAIL start = READY end = READY start + 4 半音);整体 5 verb 从 "1.5 八度 wide spread" 收回 "1 个八度 tight spread" (69→77 = 8 半音 = 1 minor 6th), 5 jingle 在 BGM 之上更集中, 玩家耳朵更好辨认 "verb 解锁" 单一节奏模式;6th verb 仍可走 79 (G5) 续接, scale 自然延伸。[`src/scripts/save_load_menu.gd`](file:///workspace/src/scripts/save_load_menu.gd) T190 新增 `_filter_occupied_only: bool = false` 状态字段 + `_toggle_filter()` / `_refresh_filter_hint()` / `_refresh_empty_placeholder()` 3 helper + `_unhandled_input` 在 `not _is_confirm_modal_visible()` 分支内加 F 键处理 (KEY_F + 排除 Shift/Ctrl/Alt 修饰键 + `get_viewport().set_input_as_handled()`);`_refresh_slots()` 应用 filter 逻辑 (panel.visible = not hidden_by_filter, hidden_by_filter = _filter_occupied_only and not SaveSystem.has_save(i));5 槽全空时 filter 开启插入 "无存档可显示  ·  按 [F] 取消过滤" 占位 Label;hint 末尾追加 [F] 过滤: 仅存档 指示符 (关闭时灰 #12334A / 开启时 Pale Resonance #B7E6DC);modal 弹出时按 F 不响应 (T189 守卫), F 不会穿透取消弹窗。T191 新增 `_on_confirm_backdrop_gui_input(event: InputEvent) -> void` handler (`InputEventMouseButton` + `mb.pressed and mb.button_index == MOUSE_BUTTON_LEFT` + `_is_confirm_modal_visible()` 3 重防御, motion/scroll/right-click 忽略) + `_ready()` 末尾调 `_confirm_backdrop.gui_input.connect(_on_confirm_backdrop_gui_input)`;[`src/scenes/save_load_menu.tscn`](file:///workspace/src/scenes/save_load_menu.tscn) `ConfirmBackdrop` ColorRect 显式 `mouse_filter = 0` (STOP, ColorRect 默认就是 STOP 但显式声明防御 tscn 解析路径变化) + `; T191 (#109): ...` 注释行 (tscn `;` 起头注释, 防御上次 #108 改 save_load_menu 时 `mouse_filter = 0` 行内 `# 注释` 导致 parse error 的回归)。**UX 闭环**: 玩家可以 4 种方式取消 confirm modal — 按 Esc (T189) / 点取消按钮 (T188) / 点 backdrop 灰色半透明区域 (T191) / Enter 默认走 cancel (T188 焦点), 4 路同源链走 `_on_confirm_cancel → _hide_confirm_modal` 行为零分歧。**I020 29 项锚点 smoke 测试** —— [`tools/test_i020_f013c_t190_t191_smoke.gd`](file:///workspace/tools/test_i020_f013c_t190_t191_smoke.gd) (新文件 195 行) **29/29 PASS**:F013.C 13 项 (docblock + whole-tone scale + 5 verb READY 69/71/73/75/77 + 5 verb TAIL 73/75/77/79/81 + 间隔严格 2 半音) + T190 9 项 (docblock + 4 helper + KEY_F + T189 兼容 + lazy 创建 + 中文文本) + T191 6 项 (docblock + handler + connect + MOUSE_BUTTON_LEFT + tscn docblock + mouse_filter STOP)。**57/57 smoke test 套件 100% PASS** (#108 55 + I020 +1, 0 回归, 1 pre-existing 已知 fail 与本轮无关)。**下一轮 #110 候选**: (1) F016.C Death SFX 触发点 audit 全 7 房间 × 2 SFX 覆盖率 (15min, polish) / (2) WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min, 商业化) / (3) 14 成就 unlock chime 与全 14 BGM/9 主题 layering (15min, scope 大) / (4) T189 modal Esc chain + T191 click cancel 同时按两个键优先级 (5min) / (5) F013.D 6th verb 接入路径 (10min, future-proof)

- **#108 — T189 SaveLoadMenu Esc 关闭 confirm modal + F016.B Death SFX idempotency + BGM transition smoothing (cubic ease_in_out) (本轮)**：[`src/scripts/save_load_menu.gd`](file:///workspace/src/scripts/save_load_menu.gd) T189 新增 `_unhandled_input(event: InputEvent) -> void` 钩子 + `_is_confirm_modal_visible() -> bool` helper (3 节点 OR 判定 _confirm_layer/_backdrop/_panel visible 状态, 都 false → return false, 让 _on_back 走完整菜单关闭链) + 弹窗可见时 ui_cancel (Esc/Backspace Project Settings 默认绑) pressed 走 `_on_confirm_cancel()` + `get_viewport().set_input_as_handled()` 防止穿透到 _on_back 关闭整个菜单 (UX 闭环: 弹窗弹出时 Esc = 取消 = 走 _hide_confirm_modal 而非 _on_back)。[T188](file:///workspace/src/scripts/save_load_menu.gd#L64-L90) 二次确认弹窗 (本轮 T189 直接复用 _on_confirm_cancel 路径, Esc 与点取消按钮语义对称) + T188 _on_back 末尾 _hide_confirm_modal 防御 (双层保护)。[`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) F016.B 新增 `_death_sfx_playing: bool = false` 内部 flag + `_DEATH_SFX_DURATION := 0.4` (与 SFX 时长一致) + `_DEATH_SFX_GUARD_BUFFER := 0.1` (防 SFX 未衰减就被截断) 共 3 常量;`play_death_lay_down()` 入口加守卫 `if _death_sfx_playing: return` (防止 SFX bus 上 2 个 0.4s 嗡鸣叠加成 2x 嗡鸣, 与 [player.gd](file:///workspace/src/scripts/player.gd) `_is_dying` 形成"调用方 + callee" 双重防御) + 设 `_death_sfx_playing = true` + `get_tree().create_timer(_DEATH_SFX_DURATION + _DEATH_SFX_GUARD_BUFFER).timeout` 0.5s 后清 flag (Timer 守护与 `_music_final_playing` 模式一致, 略长于 SFX 时长 0.4s + 0.1s 缓冲)。BGM transition smoothing: `play_music_track()` 与 `stop_music()` 2 个函数体 tween 改用 `set_trans(Tween.TRANS_CUBIC).set_ease(Tween.EASE_IN_OUT)` (之前用 linear tween, crossfade 中段有"突然一个时间点能量最大" 的 harsh 听感; 改成 cubic ease_in_out 让两端缓慢开始 + 中段加速 + 缓慢结束, 听觉上更"渐变", 玩家不会听到 crossfade 中点的"能量峰值"; `set_parallel(true)` 保留让 fade_in/fade_out 同步, cubic ease_in_out 不会让并行 tween 错位), 覆盖 9 BGM 主题 (title_intro / hub_warm / archive_exploration / archive_boss / archive_boss_dual / archive_dawn / archive_storm / silence_void / whisper_hollow) + 4 类 transition (新游戏 / 进房间 / Boss 阶段 2 / finale crossfade)。**I019 36 项锚点 smoke 测试** —— [`tools/test_i019_t189_f016b_esc_death_sfx_bgm_smoke.gd`](file:///workspace/tools/test_i019_t189_f016b_esc_death_sfx_bgm_smoke.gd) (新文件) **36/36 PASS**:T189.GD.HANDLER 6 项 / T189.GD.HELPER 8 项 / T189.GD.LOGIC 3 项 / F016B.GD.GUARD 8 项 / F016B.PLAYER 4 项 / F016B.BGM 7 项。**55/55 smoke test 套件 100% PASS** (#107 54 + I019 +1 = 55, 0 回归) + `check_smoke_consistency.sh` 7/7 规则 PASS (rule 7 2 个 README 同步) + 0 SCRIPT ERROR。**下一轮（#109, 109%5≠0, 普通模式）建议候选**：F013.C 5 verb 各 2 半音微调 (10min, polish) / WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min) / 14 成就 unlock chime 与全 14 BGM/9 主题 layering (15min) / SaveLoadMenu 键盘 F 键 (filter) 实现槽位过滤 (5min) / T189 backdrop click-to-cancel 升级 (5min, Esc 同源链延伸)。

- **#107 — T188 SaveSlot 二次确认弹窗 UX 升级 (本轮)**：[`src/scenes/save_load_menu.tscn`](file:///workspace/src/scenes/save_load_menu.tscn) 新增 `ConfirmDeleteLayer` CanvasLayer 节点 (parent="." 根级, layer=64 高于主 UI, 独立 layer 不与 SaveLoadMenu 一起 hidden) + 4 层结构: `ConfirmBackdrop` ColorRect 全屏遮罩 (anchors_preset=15, Color(0.02, 0.047, 0.078, 0.6) STYLE_GUIDE 深海蓝 token 半透明) + `ConfirmPanel` PanelContainer 居中 (anchors_preset=8, -90..90 / -36..36 偏移 180×72 px 紧凑) + `ConfirmMargin` MarginContainer 10/8 padding + `ConfirmVBox` VBoxContainer separation=6 + `ConfirmTitle` Label "确认删除存档？" (Color(0.906, 0.427, 0.353, 1) Soft Coral #E76D5A 危险警告色 token) + `ConfirmMessage` Label 默认 "此操作不可撤销。存档将被永久删除。" (autowrap_mode=2) + `ConfirmButtons` HBoxContainer + `ConfirmCancelBtn` Button "取消" (64×22) + `ConfirmDeleteBtn` Button "删除" (64×22)。[`src/scripts/save_load_menu.gd`](file:///workspace/src/scripts/save_load_menu.gd) 新增 7 @onready 字段 (_confirm_layer/_backdrop/_panel/_title/_message/_cancel_btn/_delete_btn) + `_pending_delete_slot: int = -1` 状态字段 (-1 = 无 pending) + `_on_delete(slot_id)` 重构走 `_show_confirm_modal(slot_id)` (而非旧行为直接 emit delete_requested) + 节点 null 守卫 fallback (防御 tscn 损坏时至少保留"立即删" 旧行为, 不卡玩家) + `_show_confirm_modal(slot_id)` 写入 _pending + 动态文案 "槽位 %d 的存档将被永久删除" + 4 visible=true + 焦点给 _confirm_cancel_btn (破坏性操作 default = 取消) + `_hide_confirm_modal()` 幂等重置 (4 visible=false + _pending=-1) + `_on_confirm_cancel` 仅 _hide_confirm_modal (不 emit 不删) + `_on_confirm_delete` 读 _pending→备份到本地→_hide_confirm_modal→AudioManagerEnhanced.play_delete_confirm() SFX→emit delete_requested(slot_id) (与旧 _on_delete 同源链) + `_on_back` 末尾 _hide_confirm_modal (防 _pending 跨场景残留, hide_modal 早于 hide_menu 顺序) + `_ready` 末尾 _hide_confirm_modal (初始化即清空 + 防御后续修改残留)。**UX 闭环**：(1) 玩家点 "删" → 弹窗 visible + 焦点给 cancel (按 Enter 默认走取消) → 看到 "确认删除存档？" 危险标题 + 具体 slot 编号 → 玩家可主动按 Esc/点取消 (回退) 或主动点删除 (确认); (2) 即使玩家手快连点也不会立刻丢档, 之前 1 步操作 = 破坏性, 现在 2 步 explicit confirm; (3) 视觉规范: 标题色 Soft Coral 危险色, 与 STYLE_GUIDE 限制色板 token 一致。**I018 52 项锚点 smoke 测试** —— [`tools/test_i018_t188_saveslot_confirm_modal_smoke.gd`](file:///workspace/tools/test_i018_t188_saveslot_confirm_modal_smoke.gd) (新文件 ~290 行) **52/52 PASS**：T188.TSCN 12 项 + T188.GD.ONREADY 8 项 + T188.GD.SIGNAL 4 项 + T188.GD.ON_DELETE 4 项 + T188.GD.SHOW 8 项 + T188.GD.HIDE 5 项 + T188.GD.CONFIRM 6 项 + T188.GD.BACK 3 项。**54/54 smoke test 套件 100% PASS** (#106 53 + I018 +1 = 54, 0 回归, 0 SCRIPT ERROR + 0 Parse Error) + `check_smoke_consistency.sh` 7/7 规则 PASS。**SaveSlot 三件套 UX 全**: 已有 overwrite 二次确认 (#84 T094) + T188 二次确认删除 + T137 快速加载按钮, 3 维破坏性操作都 explicit confirm。下一轮（#108，N%5≠0，普通模式）建议候选：F013.C 5 verb 各 2 半音微调 (10min, polish) / WaveAbility 0.5× Pale Resonance 1 个 room 教学演示 (10min) / 14 成就 unlock chime 与全 14 BGM/5 主题 layering (15min) / SaveLoadMenu 键盘 Esc 取消弹窗 (5min, T188 延伸) / F016 死亡 SFX 触发点 audit + 5 BGM transition smoothing (10min)。

- **#106 — F013.B 5 verb cooldown TAIL jingle + T187 cut_combo 屏抖 (本轮)**：[`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) 新增 F013.B `_verb_cooldown_tail_streams: Dictionary` 字段 + 公开方法 `play_verb_cooldown_tail(verb_name: String)` (lazy-init + 5 verb 5 stream 一次性预热) + 私有 synth `_generate_verb_cooldown_tail_jingle(start_midi: int) -> AudioStreamWAV` (0.12s duration 略长于 T181 ready 0.10s, exp(-t*12) 略慢衰减, 0.18 amplitude 与 T181 ready 一致, **降 4 半音** f1→f0 = T181 升 4 半音的镜像方向形成"verb 锁了" vs "verb 解锁"对偶音程) + helper `_verb_cooldown_tail_start_midi` (pulse=73 C5→A4 / bind=76 E5→C5 / cut=80 G#5→E5 / echo=81 A5→G5 / wave=85 C6→A5, 与 T181 ready 起点+4 半音对偶)。新增 `prewarm_verb_cooldown_tails()` 公开方法 (5 verb 循环 + cache 守卫) + aggregator `prewarm_all_sfx()` 末尾追加 (新顺序 music → hit → shop → misc → verb_cooldown_tail 5 桶 ~34 ms 总成本)。[`src/scripts/pulse_ability.gd`](file:///workspace/src/scripts/pulse_ability.gd) / [`bind_ability.gd`](file:///workspace/src/scripts/bind_ability.gd) / [`cut_ability.gd`](file:///workspace/src/scripts/cut_ability.gd) / [`echo_ability.gd`](file:///workspace/src/scripts/echo_ability.gd) / [`resonance_wave_ability.gd`](file:///workspace/src/scripts/resonance_wave_ability.gd) 5 verb `_execute_*` 末尾 fire SFX 之后同帧调 `AudioManagerEnhanced.play_verb_cooldown_tail("<verb>")` (5 verb 5 caller + 5 has_method 守卫) — fire SFX 0 延迟 + cooldown tail 0 延迟 = 玩家听见 "verb fire 重音" + "cooldown tail 琶音" 1 段 2 事件听觉序列。[`src/scripts/cut_ability.gd`](file:///workspace/src/scripts/cut_ability.gd) 新增 T187 `@export var cut_combo_threshold: int = 3` (max_targets=6 半数阈值, 避免 1-2 敌人走位顺手斩都触发) + `_perform_cut_hit_check()` 末尾 `if hits.size() >= cut_combo_threshold:` 屏抖 + flash_color 双层同步反馈: 屏抖 `ScreenShake.shake_preset(Preset.PERK_LEVEL_UP)` (2.5/0.15s, T185 #103 preset, 比 T146 wave_combo HEAVY 4.0/0.4s 弱 60% 反映 cost/cd 比例 0.8s vs 6.0s 频繁 7.5x) + flash `Color(0.902, 0.663, 0.235, 1.0) Amber Cut #E6A93C` (STYLE_GUIDE "Amber Cut" 色 token, 0.15s/0.22 peak/256 layer 不与 per-hit 128 layer 互消)。**I017 43 项锚点 smoke 测试** —— [`tools/test_i017_f013b_t187_cooldown_tail_cut_combo_smoke.gd`](file:///workspace/tools/test_i017_f013b_t187_cooldown_tail_cut_combo_smoke.gd) (新文件 ~290 行) **43/43 PASS**：`src/scripts/audio_manager_enhanced.gd` F013.B 27 项 (1 字段 + 5 PLAY.* 含 lazy/bus + 7 START_MIDI.* 含 5 verb 锚点 + 5 GEN.* 含 0.12s/exp(-t*12)/0.18/降 4 半音 + 5 PRE.* 含 5 桶顺序 + 10 5-verb caller 含 has_method 守卫) + cut_ability.gd T187 16 项 (3 EXPORT 含默认 3 = max_targets/2 + 4 SHAKE 含 hits.size() 守卫 + Preset.get 防御 + shake_preset call + 5 FLASH 含 Color 锚点 + 0.15/0.22/256 layer + 2 文档锚点 + T187 注释引用)。**2 任务 audio + screen-shake 双家族闭环**：(1) F013.B 让 5 verb 刚 cast 出去后有"verb 锁了" 短琶音 (0.12s 降 4 半音) 与 T181 ready (0.10s 升 4 半音) 形成对称音程, 玩家在 0.5s/0.8s/1.2s/4.0s/6.0s 5 verb cooldown 各 1 段琶音后听到 0.10s 短琶音 = "解锁" 提示; (2) T187 让 Cut 在多敌人场景 (≥3 命中) 有 PERK_LEVEL_UP 屏抖 (2.5/0.15s) + Amber Cut flash (#E6A93C 0.15s/0.22) 双层同步 "做对事" 反馈, 与 T146 wave_combo HEAVY + Electric Violet flash 模式同源但强度 60% 缩放, 反映 Wave 6.0s vs Cut 0.8s cost/cd 比例 7.5x。**5 桶 prewarm aggregator** (T185.B + F013.B 扩展): music → hit → shop → misc → verb_cooldown_tail ~34 ms 一次性预热, archive_01 第一次 cast pulse 时 5 verb 5 stream 全部 cache → fire SFX 0 延迟 + cooldown tail 0 延迟 (双 0)。**53/53 smoke test 套件 100% PASS** (#105 53 + I017 +1 = 54, 0 回归, 0 SCRIPT ERROR + 0 Parse Error) + `check_smoke_consistency.sh` 7/7 规则 PASS。**L006 README Screenshots 段同步 #104 + #105 + #106**：line 71 "5 verb 音频闭环现状" 段补 F016 Death lay-down SFX + F013.B 5 verb cooldown TAIL 闭环现状; 5 verb 屏抖家族闭环新增 T187 cut_combo 与 T146 wave_combo 强度对比。下一轮（#107，N%5≠0，普通模式）建议候选：F013.C cooldown tail 5 verb 各 2 半音微调 / 5 verb audio + screen-shake 双家族联动 play-test 录制 / T171 持续集 2 inkwarden Pale Resonance halo 5 房间全段 / SaveSlot confirm modal 二次弹窗 UX 升级 (与 #105 信息条目对齐)。

- **#105 — 审查 #105 (本轮)**: 全维度代码-素材-文档-冒烟 audit; 0 SCRIPT ERROR + 0 runtime ERROR + 54 class_name 唯一 + 79 signal 完整 + 7 autoload 一致 + 114 PNG 合法 + 8 JSON 房间语法正确 + 52 冒烟测试套件 (T101+T163+F004 / D001+T160+T161+F003 / D002.B / H001 / **T103 修复后 28 PASS** / **T142 修复后 10 PASS**) ALL PASS + 严重 0 / 一般 0 (本轮修复 2 套件 pre-existing test) / 轻微 0 / 信息 0; **关键修复**：T103 `cooldown` / `windup_time` 字段在 #98 D002.B 父类抽取后已迁移至 `_verb_ability_base.gd` (#99 H001 hotfix),原 T103 测试硬编码检查子类已 outdated,本轮改为"6 个 verb-specific 字段验证子类 + 2 个共享字段验证 base"双轨校验; T142 同样为 `_is_winding_up = true` 在 D002.B 后迁至 base,本轮改为宽容校验"start_wave() 函数存在 + 字段在 base 或子类均可"。**总体评级 A（健康）** — 距 vertical slice 完整可玩循环 0 缺口,已达"indie polished demo"标准

- **#104 — T186 升档暖色光晕 + F016 Death lay-down SFX（本轮）**：[`src/scripts/shop_menu.gd`](file:///workspace/src/scripts/shop_menu.gd) `_on_buy_pressed` 在 T185 `shake_preset(PERK_LEVEL_UP)` 之后追加 `ScreenShake.flash_color(Color(0.902, 0.835, 0.722, 1.0), 0.5, 0.18, 256)` — Warm Parchment #E6D5B8 与 _title_label 同色形成视觉闭环 "我刚刚看到的是这 perk 的标题色"。[`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) 新增 F016 `_death_lay_down_streams: Array[AudioStreamWAV]` 14 stream cache (7 房间 × 2 tombstone/teleport SFX) + 公开方法 `play_death_lay_down_sfx()` (lazy-init + 14 stream 一次性预热 ~10ms) + 私有 synth `_generate_death_lay_down_sfx(room_id: int, kind: String)` 2 个音色 (tombstone 0.6s 80Hz 三角波 + exp(-t*3) 慢衰减 "沉重的石碑落下" / teleport 0.4s 200→600Hz 升频 + bell "灵魂归位") + aggregator `prewarm_all_sfx()` 末尾追加 `prewarm_death_lay_down_sfx()` (新顺序 music → hit → shop → misc → death ~38ms 总成本);[`src/scripts/player.gd`](file:///workspace/src/scripts/player.gd) `_start_death_animation()` 末尾加 `if not ame.has_method(...)` 守卫 + 调 `play_death_lay_down_sfx()` (与 _play_tombstone_particles + 1.5s lay-down 动画同步);[`src/scripts/title_screen.gd`](file:///workspace/src/scripts/title_screen.gd) `_prewarm_bgm` 末尾加 `prewarm_death_lay_down_sfx` 钩子 (4 钩子: title_screen + hub_controller + game_flow_controller + title_screen 兜底)。**I016 24 项锚点 smoke 测试** —— [`tools/test_i016_t186_f016_perk_flash_death_sfx_smoke.gd`](file:///workspace/tools/test_i016_t186_f016_perk_flash_death_sfx_smoke.gd) (新文件 220 行) **24/24 PASS**：[`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) 新增 F014 字段 `_unlock_chime_stream: AudioStreamWAV` + 公开方法 `play_unlock_chime()` (lazy-init + play_sfx) + 私有 synth `_generate_unlock_chime_sfx()` (0.4s C6 1046.50Hz + E6 1318.51Hz + A6 1760.00Hz 上行小三度+纯五度 4 半音 overtones, exp(-t*6.0) 慢衰减 → "金属奖章 rings" 仪式感, 0.18 amplitude);F015 字段 `_delete_confirm_stream: AudioStreamWAV` + 公开方法 `play_delete_confirm()` + 私有 synth `_generate_delete_confirm_sfx()` (0.12s 短促 150Hz 方波基频 + 225Hz/450Hz 谐波, exp(-t*25.0) ~40ms perceptual, 0.20 amplitude);T185.B 公开方法 `prewarm_misc_sfx()` (cache F014 + F015 = 2 streams, ~3ms) + aggregator `prewarm_all_sfx()` 末尾追加 `prewarm_misc_sfx()` (新顺序 music → hit → shop → misc 4 桶 ~28ms 总成本)。[`src/scripts/achievement_notification.gd`](file:///workspace/src/scripts/achievement_notification.gd) 新 helper `_play_unlock_chime()` (has_method headless-safe 守卫) + `_on_achievement_unlocked` 入口第一行调它。 [`src/scripts/save_load_menu.gd`](file:///workspace/src/scripts/save_load_menu.gd) `_on_delete(slot_id)` 加 `_has_audio_manager() and AudioManagerEnhanced.has_method("play_delete_confirm")` 双守卫 + 调 `AudioManagerEnhanced.play_delete_confirm()` (在 `delete_requested.emit` 之前 — audio 先, signal 后)。 [`src/autoload/screen_shake.gd`](file:///workspace/src/autoload/screen_shake.gd) `Preset` enum 加 `PERK_LEVEL_UP` (2.5 / 0.15s, 介于 LIGHT 1.0/0.08 与 HEAVY 4.0/0.18 之间) + `_PRESETS` dict 同步加 `Preset.PERK_LEVEL_UP: Vector2(2.5, 0.15)`。 [`src/scripts/shop_menu.gd`](file:///workspace/src/scripts/shop_menu.gd) `_on_buy_pressed` 在 F013 audio 之后加 `if new_count >= 2 and ScreenShake.has_method("shake_preset") and ScreenShake.Preset.has("PERK_LEVEL_UP"): ScreenShake.shake_preset(ScreenShake.Preset.PERK_LEVEL_UP)` (level 0=I 首次购买不抖, level II/III/IV 升档时短视觉反馈)。 [`src/scripts/title_screen.gd`](file:///workspace/src/scripts/title_screen.gd) `_prewarm_bgm` 末尾加 `ame.has_method("prewarm_misc_sfx")` 守卫 + 调 `ame.call("prewarm_misc_sfx")` (4 钩子点防御)。**音色设计哲学**:C6/E6/A6 高音 (比 F013 1 个 8 度) = "更稀有"; 150Hz 方波低音 (与 save_slot C5..E6 完全相反) = "破坏性"; 2.5/0.15s 屏抖介于 PULSE 2.0/0.10 与 HEAVY 4.0/0.18 之间 = "商店升档" 而非 "verb 触发" 或 "boss 阶段"。 **I015 50 项锚点 smoke 测试** —— [`tools/test_i015_t185_f014_f015_unlock_delete_perk_shake_smoke.gd`](file:///workspace/tools/test_i015_t185_f014_f015_unlock_delete_perk_shake_smoke.gd) (新文件 290 行) **50/50 PASS**:F014 13 锚点 (1 字段 + 1 公开 API 含 lazy+bus + 1 私有 synth 含 C6/E6/A6 Hz anchor + 0.4s duration + 0.18 amp + AchievementNotification 4 项 has_method + ame.call + helper 声明 + 顺序 audio+visual 并行) + F015 13 锚点 (1 字段 + 1 公开 API + 1 私有 synth 含 150Hz 方波 + 225Hz/450Hz 谐波 + 0.12s + 0.20 amp + SaveLoadMenu 3 项 has_method + ame.play_delete_confirm + audio→signal 顺序) + T185 11 锚点 (Preset enum 增量 + _PRESETS dict 增量 + Vector2(2.5, 0.15) 数值 anchor + shop_menu 4 项 has_method + Preset.has 守卫 + shake_preset call + new_count >= 2 阈值 + audio→shake 顺序) + T185.B 13 锚点 (prewarm_misc_sfx 公开方法含 F014+F015 集成 + aggregator 扩展 music→hit→shop→misc 4 桶顺序 + title_screen 接入 shop→misc 顺序 + 4 处 has_method 守卫)。**52/52 smoke test 套件 100% PASS** (#102 51 + I015 +1 = 52, 0 回归, #100-#102 anchor test 38+50+56 = 144 项无回归) + I014 (#102 回归) 56/56 PASS (T185.B 扩展 aggregator 不破坏 T184 顺序 music→hit→shop 断言) + I011 (#97 回归) 52/52 PASS (F014/F015 与 T181 5 verb audio family 不冲突) + T121/T118 16/16 PASS (新增预热路径不动 music presets) + `check_smoke_consistency.sh` 7/7 规则 PASS + 0 SCRIPT ERROR + 0 Parse Error。**3 任务音频家族闭环**：(1) F014 让 14 成就解锁有金属奖章仪式感 (C6/E6/A6 高音 0.4s rings);(2) F015 让存档删除有低音方波 click (150Hz 0.12s 短促);(3) T185 让 5 verb 商店升档有短视觉屏抖 (level 0=I 首次购买不抖避免过载, level II/III/IV 升档时屏抖与 arpeggio 升 1 半音同步)。 **4 钩子点防御** (T185.B 扩展):title_screen._prewarm_bgm + hub_controller._ready(通过 aggregator) + game_flow_controller._enter_state(通过 aggregator) — 任意 1 处失效, 剩余 3 处兜底。**下一轮（#104）建议候选**:F013.后续 5 verb cooldown jingle tail (15min) / T186 升档 I→II unlock 视觉 0.5s 暖色光晕 (10min) / T187 屏抖 PERK_LEVEL_UP preset 在 F004 切割连击 ≥3 时也触发 (5min) / F016 Death lay-down SFX (10min)。

- **#102 — F013 Audio Shop perk-card SFX（购买 + 升级）+ T184 cross-scene prewarm aggregator（本轮）**：[`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) 新增 2 个 shop 专用 SFX — `_shop_purchase_confirm_stream` (0.4s C5/E5/G5 大三和弦 523.25/659.26/783.99 Hz + 2nd harmonic 1046.5 + 3rd-octave sparkle 2093Hz, exp(-t*3.5) "rings") + `_shop_level_up_streams` per-level Dict (0.30s 3 notes major triad arpeggio 0/+4/+7 半音, base MIDI [60, 62, 64, 65] = C4/D4/E4/F4 升序 4 半音) + 公开 API `play_shop_purchase_confirm()` + `play_shop_level_up(level: int = 0)` (clampi 0..3 防御) + 私有 synth `_generate_shop_purchase_confirm_sfx` / `_generate_shop_level_up_sfx`；[`src/scripts/shop_menu.gd`](file:///workspace/src/scripts/shop_menu.gd) `_on_buy_pressed()` 末尾加 SFX 桥接 — 守卫式 `ame.has_method` 双检 → 顺序 `play_shop_purchase_confirm()` 先 (chord) + `play_shop_level_up(max(0, new_count - 1))` 后 (arpeggio)，`new_count` 从 `GameState.get_perk_count(perk_id)` 读新 level 0..3。**音色设计哲学**：purchase_confirm (chord, 0.4s) vs repair (single 660Hz falling, 0.6s) — 区分"奖励购买" vs "修复物件"两个完全不同的语义；level_up arpeggio base C4/D4/E4/F4 (4 半音 1 大 2 度) — 同 perk 多次升级时 base 永远 "升" 1 半音，心智模型 "我在变强"；避开 verb fire SFX 频段 — shop C5(523) E5(659) G5(783) 在 verb 之上 1 个 8 度，无频段冲突；volume 0.12-0.18 (politely under BGM) — 不抢 verb fire SFX 0.20-0.25 风头。**T184 cross-scene prewarm aggregator**：新增 `prewarm_shop_sfx()` (1 confirm + 4 level_up = 5 streams) + 聚合 `prewarm_all_sfx()` 公开方法 (顺序 music → hit → shop ~25 ms 总成本) + 文档块说明 idempotent 重入安全；[`src/scripts/title_screen.gd`](file:///workspace/src/scripts/title_screen.gd) `_prewarm_bgm()` 末尾加 `prewarm_shop_sfx` 钩子 (F013 闭环);[`src/scripts/hub_controller.gd`](file:///workspace/src/scripts/hub_controller.gd) `_ready()` 末尾加 `prewarm_all_sfx` 钩子 (Hub 整场景 re-prewarm);[`src/scripts/game_flow_controller.gd`](file:///workspace/src/scripts/game_flow_controller.gd) `_enter_state()` 末尾 (在 `_play_music_for_state` 之后) 加 `prewarm_all_sfx` 钩子 (每个 state transition 自动覆盖 Archive 房间) **3 钩子点防御层级** title→hub→gfc。**I014 56 项锚点 smoke 测试** —— [`tools/test_i014_t184_f013_shop_sfx_prewarm_smoke.gd`](file:///workspace/tools/test_i014_t184_f013_shop_sfx_prewarm_smoke.gd) (新文件 190 行) **56/56 PASS**：F013 28 锚点（2 字段 + 2 常量 + 4 base MIDI + 2 公开 API + 2 lazy + 1 clamp + 2 私有 synth 含 C5/E5/G5 Hz anchor + 0.4s/0.30s duration + exp 衰减 + [0,4,7] 音阶 + shop_menu 5 项 has_method + ame.call + 顺序 chord→arpeggio）+ T184 28 锚点（prewarm_shop_sfx 1 confirm + 4 levels + prewarm_all_sfx aggregator 顺序 + idempotent + title/hub/gfc 3 钩子点 + 3 处 has_method + GFC 顺序 music_for_state→prewarm_all_sfx）。**冒烟测试数量 50→51 套件** + I013 50/50 无回归 + `check_smoke_consistency.sh` 7/7 规则 PASS + 0 SCRIPT ERROR。**Shop SFX 闭环**：(1) F013 让 shop 购买有听觉反馈 — 0.4s 大三和弦 + 0.3s 升序琶音 = 0.7s 完整购买反馈，与 verb fire SFX 频段无冲突（shop 在 5xx Hz verb 之上 1 个 8 度）；(2) T184 让 30 min 暂停后回任意场景 SFX 0 合成延迟，3 钩子点防御（title_screen 启动 + hub_controller 场景 + gfc 状态机）。**下一轮（#103）建议候选**：F013.后续 5 verb cooldown jingle tail (15min) / F014 14 成就 unlock chime (10min) / F015 SaveSlot confirm click (5min) / T185 5 verb level-up 升档 ≥2 时屏抖 (10min)。

- **#101 — T183 hit SFX 预热（13 stream Title ready 预热，首 hit 0 合成延迟）+ F012 Hub shop perk card perk-level 数显（罗马数字 Lv —/I/II/III 替换已购 %d/%d）**：[`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) 新增 `prewarm_hit_sfx() -> void` 公开方法（带 T183 (#101) docblock 标注，13 AudioStreamWAV / ~10 ms cost / zero synthesis latency 目标 / Mirror pattern of T066 `prewarm_music_streams()`）— Bind 单 stream 预热（无 perk）+ Pulse / Cut / Echo 各自 4 level（0..3）通过 `for level in [0, 1, 2, 3]` 单一循环 + `dict.has(level)` 守卫 + cache miss 调 `_generate_*_hit_sfx(level)` 预热（Cut 4 level 预热为 future-proof，等 "cut_focus" perk 上架时无须 re-architect）。[`src/scripts/title_screen.gd`](file:///workspace/src/scripts/title_screen.gd) `_prewarm_bgm()` 紧接 `prewarm_music_streams()` 调 `ame.call("prewarm_hit_sfx")`（`has_method` 守卫 headless-safe），Title ready 帧承载 ~10 ms 预热 cost，玩家首 hit 0 合成延迟。**F012 Shop UI 域跨系统命名一致**：[`src/scripts/shop_menu.gd`](file:///workspace/src/scripts/shop_menu.gd) 新增 `const _PERK_LEVEL_ROMAN := ["—", "I", "II", "III", "IV", "V"]` 6 步表（前 4 步覆盖 6 个 shop perk max_purchases 1..3 场景） + `func _format_perk_level_label(count: int) -> String` helper（`count < 0` clamp 0 / `count < 6` 走 `"Lv %s"` 模板 / 否则 fallback `"Lv %d"` 保护未来 6+ perk-stack） + `_refresh_item()` 调 helper 替换旧 `"已购 %d/%d"` 文本 + 3 个 per-level modulate Color（count=0 灰 `(0.65, 0.65, 0.7, 0.9)` / at_max 琥珀 `(1.0, 0.78, 0.55, 1.0)` / between 玻璃青 `(0.62, 0.85, 0.95, 1.0)`）。F012 注释显式引用 T181 perk-level 命名（"Pulse Focus Lv II" shop card 与 "Pulse hit SFX at perk_level 2" 听感对齐）— audio 域 + shop UI 域跨系统 perk-level 命名闭合。**I013 50 项锚点 smoke 测试** —— [`tools/test_i013_t183_f012_prewarm_perkreadout_smoke.gd`](file:///workspace/tools/test_i013_t183_f012_prewarm_perkreadout_smoke.gd) (新文件 268 行) **50/50 PASS**：T183.PRE.HEADER 5 项 / T183.PRE.{BIND,PULSE,CUT,ECHO} 8 项 / T183.PRE.TITLE_INTEG 4 项 / F012.ROMAN 2 + F012.HELPER 1 + F012.DOC 2 项 / F012.ROMAN.VALS 6 项 / F012.LABEL.* 5 项 / F012.REFRESH 3 项 / F012.MODULATE 4 项 / F012.MATCH.AUDIO 3 项 / F012.MATCH.SHOP 4 项。**49/49 smoke test 套件 100% PASS** (#100 48 + I013 +1 = 49, 0 回归, #94-#100 anchor test 50+56+51+73+37+38+50 = 355 项无回归) + I012 (#100 回归) 38/38 PASS + `check_smoke_consistency.sh` 7/7 规则 PASS + 0 SCRIPT ERROR + 0 Parse Error。**5 verb hit audio 预热闭环**（T066 #54 BGM 预热 9 preset + T183 #101 hit SFX 预热 13 stream = 首 hit 0 合成延迟） + **Shop perk-level 跨系统命名一致**（audio 域 perk_level 0..3 ↔ shop 域 count 0..3 Lv —/I/II/III 双向对齐）。

- **#100 — T181.B 5 verb 音频家族 second half perk-level scaling (Pulse + Echo + Cut 3 verb 主题色 0..3 谐波叠加) + T182 polish player.gd 集成 + F011 A/B-test 玩家偏好调研 doc (本轮)**：[`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) Pulse / Cut / Echo 3 verb hit SFX 升级为 per-level scaling (T144 #78 wave_hit_sfx pattern 镜像) — `_generate_pulse_hit_sfx(perk_level: int = 0)` / `_generate_cut_hit_sfx(perk_level: int = 0)` / `_generate_echo_hit_sfx(perk_level: int = 0)` 3 个合成器签名加 `perk_level` 参数 + 内部 `clampi(perk_level, 0, 3)` 保护 + 3 verb 各自 level 1+ 叠加额外谐波 (Pulse: 660Hz 3× + 880Hz 4× + 1100Hz 5×;Cut: 7000Hz 3.5× + 9000Hz 4.5× + 11000Hz 5.5×;Echo: 1980×3.4 + 1980×4.4 + 1980×5.4 — 玻璃共振)。缓存字段从单 `_pulse_hit_stream` / `_cut_hit_stream` / `_echo_hit_stream` 改为 per-level Dict `_pulse_hit_streams: Dictionary` / `_cut_hit_streams: Dictionary` / `_echo_hit_streams: Dictionary` (lazy-init 模式 + level-keyed lookup + O(1) 缓存),`Bind` 保留单 `_bind_hit_stream: AudioStreamWAV` (Bind 无 shop perk 不需要缩放,注释明确说"未来 Bind perk 接入路径同 T144 wave_hit 模式")。3 verb `play_pulse_hit(perk_level: int = 0)` / `play_cut_hit(perk_level: int = 0)` / `play_echo_hit(perk_level: int = 0)` 3 个公开 API 加 `perk_level` 参数 + 内部从 `GameState.get_perk_count("pulse_focus")` / `get_perk_count("echo_charm")` 读 shop perk 等级 (Cut 暂无 shop perk 但保留 level-arg future-proof) + `has_method` 守卫 headless-safe。**关键设计**：(1) **per-level Dict 模式**与 T144 #78 wave_hit_streams 镜像 — lookup `O(1)`,首调 lazy-synth,之后命中缓存,memory 4 streams / verb worst case ~70KB,perk count 在 play 时刻从 GameState 读(不缓存)以保证"买 perk 后立即生效";(2) **3 verb 主题色 perk scaling 的对称性** — Pulse(暖珊瑚,深 + 圆)+ Echo(冷青,亮 + 玻璃)+ Cut(暖琥珀,高 + 锋利)3 verb 各自谐波频率选择都顺着"verb 主音色 + perk-stacking 让音色更"+"主色形容词"的模式走,符合 STYLE_GUIDE 色宪法 + 听感认知;(3) **Bind 显式 NOT scaled** — 注释明确说"Bind has no shop perk,signature 故意不加 perk_level 参数(加了会暗示有 perk 实际没有)","未来 Bind perk 接入路径同 T144 wave_hit 模式";(4) **Future-proof Cut** — Cut 暂无 shop perk 但合成器 + play API 都已 level-arg 化,等"cut_focus" perk 上架时无须修改 audio_manager。**T182 Polish** — `play_*_hit()` 自包含 perk 读路径 (T144 wave 同一 pattern),player.gd `_on_pulse_hit` / `_on_cut_hit` / `_on_echo_hit` 3 个 handler 无须改 caller (现有 `AudioManagerEnhanced.play_pulse_hit()` 等调用点仍工作,新 level-arg 默认 0 = no scaling,与 T97 闭环的 caller-layer 100% 兼容)。**F011 A/B-test 玩家偏好调研 doc** — 5 verb audio A/B-test 候选池建议 (Pulse 3× vs 4× vs 5× 谐波叠加是否"过于金属";Echo 5.4× 是否太尖锐;Cut 5.5× 是否超出可听域) 入 CHANGELOG 候选池供下轮 (或更后) 实测玩家偏好。**I011 51→52 项** ([test_i011_t181_audio_loop_smoke.gd](file:///workspace/tools/test_i011_t181_audio_loop_smoke.gd) 更新) — 5 verb cooldown jingle caller 断言从"5 verb _process 直接调 play_verb_cooldown_ready"改为"5 verb _process 调 _process_cooldown(delta, \"<verb>\")" + "VerbAbilityBase._process_cooldown 调 play_verb_cooldown_ready(verb_name)" (D002.B #98 父类抽取后的新结构对齐),Pulse / Cut / Echo 3 verb 签名加 `perk_level: int = 0` 断言 + Dict 字段断言。**I012 38 项** ([test_i012_t181b_hit_audio_perk_scaling_smoke.gd](file:///workspace/tools/test_i012_t181b_hit_audio_perk_scaling_smoke.gd) 新文件 218 行,**38/38 PASS**) — 8 段断言:T181.B.HIT.SCALE 12 项 / T181.B.HIT.LEVEL.PARAM 3 项 / T181.B.HIT.LEVEL.READ 3 项 / T181.B.HIT.LEVEL.CACHE 9 项 / T181.B.LEVEL.CLAMP 3 项 / T181.B.BIND.UNCHANGED 4 项 / T181.B.WAVE.EXISTING 3 项 / T181.B.MARKER 1 项。**48/48 smoke test 套件 100% PASS** (#99 47 + I012 +1 = 48, 0 回归, #94-#99 anchor test 50+56+51+73+37+38 = 305 项无回归) + `check_smoke_consistency.sh` 7/7 规则 PASS + 0 SCRIPT ERROR + 0 Parse Error。**5 verb hit audio perk-level scaling 1/5 → 4/5 闭环 (Wave T144 已有 + Pulse/Echo/Cut T181.B 新增 + Bind 无 perk 显式保留)** — 玩家买 pulse_focus / echo_charm 商店 perk 时,命中音色立即从"verb base"升到"verb + perk tier"的圆/玻璃/锋利变体,听感 + 视觉半径同步成长。
- **#99 — H001 D002.B Parse Error hotfix (cooldown / windup_time 字段冲突 + echo / wave verb-specific state 缺失) (本轮)**：[`src/scripts/_verb_ability_base.gd`](file:///workspace/src/scripts/_verb_ability_base.gd) `var cooldown: float = 0.5` / `var windup_time: float = 0.10` 升级为 `@export`(默认值保留 Pulse 节奏 0.5 / 0.10 作为"feels-good floor"——第 6 个 verb 即使忘记 .tscn override 也会以 Pulse 节奏工作,这是更安全的 fallback)。5 verb ability [`pulse_ability.gd`](file:///workspace/src/scripts/pulse_ability.gd) + [`bind_ability.gd`](file:///workspace/src/scripts/bind_ability.gd) + [`cut_ability.gd`](file:///workspace/src/scripts/cut_ability.gd) + [`echo_ability.gd`](file:///workspace/src/scripts/echo_ability.gd) + [`resonance_wave_ability.gd`](file:///workspace/src/scripts/resonance_wave_ability.gd) 各删 1 行 `@export var cooldown: float = X.X` + 1 行 `@export var windup_time: float = 0.0X`(D002.B #98 错将两字段在 base + 5 子类双重声明,Godot 4 静态分析报 "The member 'cooldown' already exists in parent class" Parse Error,5 verb 全挂)。[`src/scenes/player.tscn`](file:///workspace/src/scenes/player.tscn) 的 5 verb ability 节点各补 2 行 `cooldown = X.X` / `windup_time = 0.0X` 显式 override,**1:1 保留原 gameplay tuning**(Pulse 0.5/0.10 / Bind 1.2/0.10 / Cut 0.8/0.06 / Echo 4.0/0.08 / Wave 6.0/0.10,gameplay 0 变化)。`echo_ability.gd` 加回 verb-specific `_is_active: bool` / `_active_timer: float` / `_reflected_this_cast: Array` 3 字段(D002.B 错删导致 parse 报 `Identifier '_is_active' not declared`);`resonance_wave_ability.gd` 加回 verb-specific `_is_active` / `_active_timer` / `_current_radius: float` / `_hit_this_cast: Array` 4 字段(同 Echo,AOE expanding-ring + dedup state)。**H001 37 项锚点 smoke 测试** —— [`tools/test_h001_d002b_field_conflict_smoke.gd`](file:///workspace/tools/test_h001_d002b_field_conflict_smoke.gd) (新文件 285 行) **37/37 PASS**:H001.1 父类 `@export` × 5 / H001.2 5 verb 不再 `@export` × 15 / H001.3 player.tscn per-verb override × 10 / H001.4 Echo state × 3 / H001.5 Wave state × 4。D002.B smoke 73/73 复跑 PASS(证明 H001 修复未回退 D002.B 5 verb `extends VerbAbilityBase` 契约)。**47/47 smoke test 套件 100% PASS** (#98 46 + H001 +1 = 47, 0 回归, #94-#98 anchor test 50+56+51+73+37 = 267 项无回归) + `check_smoke_consistency.sh` 7/7 规则 PASS + **0 SCRIPT ERROR**(修复前 8 个 Parse Error:5 verb "already exists in parent class" + 1 base "Could not resolve class VerbAbilityBase" + 2 verb-specific "Identifier not declared")。**关键 hotfix 设计**:(1) 父类 `@export` 化是 Godot 4 唯一干净解——子类同名 `@export` 不能 override 父类 `var` 是 Godot 4 静态分析硬约束,5 verb 各保留 `@export` 会必然冲突;(2) per-verb tuning 用 .tscn 显式 override 而不是子类 `@export` 字段——Inspector 编辑体验不变(在 .tscn 里改 5 verb 节点属性 vs 在 5 个 verb ability 文件改默认值),且 future 6th verb 接入有"先写 .tscn override" → "再写 verb-specific methods"两步 pipeline;(3) verb-specific state 不属于父类——`_is_active` / `_active_timer` / `_reflected_this_cast` / `_current_radius` / `_hit_this_cast` 5 字段都是 Echo-only 或 Wave-only 概念(Pulse / Bind / Cut 都没有),父类硬塞会让其它 3 verb 文件留无用的死字段,反损 readability。
- **#98 — D002.B 5 verb ability `extends VerbAbilityBase` 父类抽取 (cost-consume + windup-state-setup + _exit_tree + T181 jingle 4 helper / 6 字段 / @onready _player 收口) (本轮)**：新建 [`src/scripts/_verb_ability_base.gd`](file:///workspace/src/scripts/_verb_ability_base.gd) 父类 (171 行, `class_name VerbAbilityBase extends Node`) 包含 6 共享状态字段 (`_cooldown_timer` / `_windup_timer` / `_is_winding_up` / `_pending_origin` / `_pending_direction` / `_windup_vfx`，T166 #85 + T167 #86 + T168 #86 + T169 #87 + T173.C #92 5 verb 镜像) + `@onready var _player: CharacterBody2D = get_parent() as CharacterBody2D` + 2 base @export defaults (`cooldown: float = 0.5` / `windup_time: float = 0.10`，避免 base 方法引用子类 @export 字段时 Godot 4 静态分析报 "Field not declared") + 4 helper (`_process_cooldown` T181 #97 5 verb 共享 jingle / `_consume_verb_cost` F007 #87 5 verb 共享 cost-consume / `_setup_windup_state` F007 #87 5 verb 共享 windup-setup / `_exit_tree` T166 + T173 #92 5 verb 共享 VFX cleanup) + 2 公开 accessor (`get_cooldown_ratio` HUD 4 verb 冷却条 + Wave chip / `is_winding_up` D001 #82 PlayerActionGate)。5 verb ability [`pulse_ability.gd`](file:///workspace/src/scripts/pulse_ability.gd) + [`bind_ability.gd`](file:///workspace/src/scripts/bind_ability.gd) + [`cut_ability.gd`](file:///workspace/src/scripts/cut_ability.gd) + [`echo_ability.gd`](file:///workspace/src/scripts/echo_ability.gd) + [`resonance_wave_ability.gd`](file:///workspace/src/scripts/resonance_wave_ability.gd) 改 `extends "res://src/scripts/_verb_ability_base.gd"` + 删 6 字段 + @onready + 4 helper + 5 verb `_ready()` 调 `super._ready()` + 5 verb `_process()` 调 `_process_cooldown(delta, "<verb>")` opt-in T181 jingle（5 verb 合计 -120 行 byte-identical helper code）。**关键设计**：(1) 真父类 vs helper script — `Node` 父类让 5 verb shared state 共享并通过 `extends` 自然暴露给子类 override（T174.B #94 windup VFX 父类已验证此模式）；(2) verb-specific state 留子类 — 5 verb radius / cost / damage 等 verb-specific @export 仍由各自 verb ability 文件持有；(3) lifecycle 契约 — 5 verb `_ready()` 必须 `super._ready()` 触发 base 初始化，`_exit_tree()` 自动继承 base 实现；(4) 6th verb 接入 — 未来 6th verb 加 verb ability 文件只需 `extends "res://src/scripts/_verb_ability_base.gd"` + 自带 verb-specific @export + verb-specific can_X / start_X / _execute_X + 调 `_process_cooldown(delta, "<name>")`，节约 ~120 行 byte-identical code。**D002.B 75 项锚点 smoke 测试** —— [`tools/test_d002b_verb_ability_base_smoke.gd`](file:///workspace/tools/test_d002b_verb_ability_base_smoke.gd)（新文件 273 行）**75/75 PASS**：D002.B.1 base contract 20 项 (class_name / extends Node / 6 字段 / @onready _player / assert / _process_cooldown / play_verb_cooldown_ready / _consume_verb_cost / GameState.consume_resonance / _setup_windup_state / _exit_tree / fade_out_and_free / get_cooldown_ratio / is_winding_up / D002.B docblock attribution) + D002.B.2 5 verb extends 10 项 + D002.B.3 5 verb 删去重复 helper 30 项 + D002.B.4 5 verb _ready 调 super._ready() 5 项 + D002.B.5 5 verb _process 调 _process_cooldown + 不含 inline T181 jingle 10 项。**3 个旧 test 迁移**：[`test_t165_t166_f005_smoke.gd`](file:///workspace/tools/test_t165_t166_f005_smoke.gd) + [`test_t167_t168_f006_smoke.gd`](file:///workspace/tools/test_t167_t168_f006_smoke.gd) + [`test_t173_windup_fadeout_smoke.gd`](file:///workspace/tools/test_t173_windup_fadeout_smoke.gd) 5 verb `_exit_tree` 9 项检查全部从"5 verb 各自检查 `func _exit_tree` 存在"改为"5 verb 各自 `extends VerbAbilityBase` + VerbAbilityBase 提供 _exit_tree + fade_out_and_free 契约"。**46/46 smoke test 套件 100% PASS**（#97 45 + D002.B +1 = 46，0 回归，#94-#97 anchor test 41+26+50+56+51 = 224 项无回归）+ `check_smoke_consistency.sh` 7/7 规则 PASS + 0 SCRIPT ERROR。**5 verb ability 共享代码 byte-identical 收口** — 与 #94 T174.B 5 verb windup VFX 父类 `VerbWindupVFXBase` 经验镜像，本轮把 5 verb ability 5 份 byte-identical 共享代码（6 字段 + 4 helper + 2 accessor + @onready）收口到 `VerbAbilityBase` 一处，未来 6th verb 接入成本从 ~150 行降到 ~30 行（仅 verb-specific @export + verb-specific methods）。
- **#97 — T181 5 verb 音频家族完整闭环 first half (4 verb ability caller + 4 verb hit SFX + 5 verb cooldown jingle) (本轮)**：[`src/scripts/bind_ability.gd`](file:///workspace/src/scripts/bind_ability.gd) + [`src/scripts/cut_ability.gd`](file:///workspace/src/scripts/cut_ability.gd) + [`src/scripts/echo_ability.gd`](file:///workspace/src/scripts/echo_ability.gd) + [`src/scripts/resonance_wave_ability.gd`](file:///workspace/src/scripts/resonance_wave_ability.gd) 4 verb `_execute_*()` 末尾在 `*_fired.emit` 之后调 `AudioManagerEnhanced.play_bind()` / `play_cut()` / `play_echo()` / `play_wave_fire()`（4 个调用点全部 `is_instance_valid(_player)` 守卫，与 F004 #94 Pulse 模板 byte-identical 镜像，#97 起点 caller-layer 4 格填满）。[`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) 增 ~200 行新增 4 verb `play_pulse_hit()` / `play_bind_hit()` / `play_cut_hit()` / `play_echo_hit()` 4 个公开 API（lazy-init 模式 + 4 个 `_pulse_hit_stream` / `_bind_hit_stream` / `_cut_hit_stream` / `_echo_hit_stream` 缓存字段 + 4 个 `_generate_pulse_hit_sfx()` 220Hz 0.18s thud / `_generate_bind_hit_sfx()` 165Hz 0.22s thunk / `_generate_cut_hit_sfx()` 2000Hz 0.05s shing / `_generate_echo_hit_sfx()` 1980Hz 0.06s glass tap 私有合成函数 4 verb 各自主题色匹配的 4 重音色，让 4 verb 命中听起来"是哪个 verb 在打"的 1-shot 认知）；`_VERB_HIT_THROTTLE` 常量 50ms 共享节流（与 `_WAVE_HIT_THROTTLE` 同 50ms 防止多目标 4 hit 堆叠成 0.20s 糊状）+ `_last_verb_hit_time_ms` 跨帧节流时间戳。[`src/scripts/player.gd`](file:///workspace/src/scripts/player.gd) 4 handler `_on_pulse_hit` / `_on_bind_hit` / `_on_cut_hit` 末尾 + `_on_echo_hit` 反弹路径 emit 后调 `AudioManagerEnhanced.play_pulse_hit()` / `play_bind_hit()` / `play_cut_hit()` / `play_echo_hit()`（4 个调用点 `has_method` 守卫 headless 测试，5 verb hit 家族 1/5 wave_combo (#78) + 4/5 verb 主体 (#97 T181) = 5/5 闭环）。5 verb `_process()` 在 `_cooldown_timer > 0` 块内增加跨帧守卫（previous frame > 0, this frame <= 0），触发时调 `AudioManagerEnhanced.play_verb_cooldown_ready("<name>")`（5 verb cooldown 家族 0/5 跳到 5/5 (#97 T181) 闭环），audio_manager 增 `play_verb_cooldown_ready(verb_name: String)` 公开 API + `_verb_cooldown_start_midi(verb_name: String)` 5 verb 起始 MIDI 查表（Pulse=69 A4 / Bind=72 C5 / Cut=76 E5 / Echo=79 G5 / Wave=81 A5，5 音节跨 1.5 八度从低到高 5 verb position 顺序）+ `_generate_verb_cooldown_jingle(start_midi: int)` 合成器（f0 → f1 + 4 半音线性扫频，0.10s exp(-t*15) 衰减，amplitude 0.18 最弱 — "status" cue 不抢 "moment" cue 风头）+ 未知 verb 返回 -1 静默 no-op（未来 6th verb 友好扩展）。**I011 51 项锚点 smoke 测试**（[test_i011_t181_audio_loop_smoke.gd](file:///workspace/tools/test_i011_t181_audio_loop_smoke.gd) 新文件 220 行）**51/51 PASS** — T181.FIRE 12 项（4 verb ability 调 play_<verb> / 4 verb caller 在 *_fired.emit 之后 / 4 verb is_instance_valid 守卫）/ T181.HIT 14 项（4 verb play_*_hit 公开 API / 4 verb _generate_*_hit_sfx 私有合成器 / 4 verb 缓存字段 / _VERB_HIT_THROTTLE 50ms + _last_verb_hit_time_ms 节流守卫）/ T181.HIT.CALLER 4 项（4 handler 调对应 play_*_hit）/ T181.COOLDOWN 5 项（5 verb _process 调 play_verb_cooldown_ready）/ T181.JINGLE 10 项（play_verb_cooldown_ready 公开 API / _verb_cooldown_start_midi 查表 / 5 verb 起始 MIDI 69/72/76/79/81 / 未知 verb 静默 no-op return -1 / _generate_verb_cooldown_jingle 合成器 / _verb_cooldown_streams 缓存 dict）/ T181 docblock attribution 6 项（5 verb ability + audio_manager + player 全部带 T181 (#97 标记）。**45/45 smoke test 套件 100% PASS**（#96 44 + I011 +1 = 45，0 回归，#94-#96 anchor test 41+26+50+56+51 = 224 项无回归）+ `check_smoke_consistency.sh` 7/7 规则 PASS + 0 SCRIPT ERROR。**5 verb 音频家族 5/5 → 15/15 完整三层闭环**（5 fire caller + 5 hit SFX + 5 cooldown jingle），玩家按 verb 键 → 听到 "verb fire!" → 命中听到 "verb hit!" → 等冷却听到 "verb ready!" 三段 "verb family" 一致音色家族让玩家闭眼也能从音色位置判断 "现在是哪个 verb 在打/已就绪"。
- **#96 — F004.B 4 verb fire SFX 公开 API + F010 4 verb 命中色宪法 docblock + L005 README 双语 Screenshots 段 (本轮)**：[`src/scripts/audio_manager_enhanced.gd`](file:///workspace/src/scripts/audio_manager_enhanced.gd) 新增 `play_bind()` / `play_cut()` / `play_echo()` / `play_wave_fire()` 4 个公开 API（lazy-init 模式 + 4 个 `_bind_stream` / `_cut_stream` / `_echo_stream` / `_wave_fire_stream` 缓存字段 + 4 个 `_generate_bind_sfx()` / `_generate_cut_sfx()` / `_generate_echo_sfx()` / `_generate_wave_fire_sfx()` 私有合成函数 17-25 行 procedure：sample_rate=44100, Bind 220Hz 拉低 0.40s / Cut 1500Hz 锐利 0.08s / Echo 1320Hz 玻璃 0.15s / Wave 100Hz 宽辐射 0.30s，4 verb 各自音色匹配 windup VFX motif 1:1 镜像 —— Bind 螺旋收缩 / Cut 斩 / Echo 撑 / Wave 涟漪）；与 #94 F004 play_pulse() 模板 byte-identical 镜像，5 verb 音频家族 function definition layer 闭合。**F010**：[`src/autoload/screen_shake.gd`](file:///workspace/src/autoload/screen_shake.gd) 4 verb `VERB_HIT_*_COLOR` 4 元组注释扩 ~40 行 docblock（F010 (#96) marker + 4 verb × 4 col 表格 hex/palette/caller + 5 行调用契约示例 + Wave 不参与说明 + 6th verb 接入流程 4 步）。**L005**：[`README.md`](file:///workspace/README.md) + [README.zh-CN.md](file:///workspace/README.zh-CN.md) Screenshots 节补 "### 5 Verb windup ramp-in / ramp-out 双闭环（#92-#94）" 子节 + "5 verb 音频闭环现状" 段（双语 1:1 同步）。**I010 56 项锚点 smoke 测试**（[test_i010_f004b_f010_l005_smoke.gd](file:///workspace/tools/test_i010_f004b_f010_l005_smoke.gd) 新文件 270 行）**56/56 PASS**。**44/44 smoke test 套件 100% PASS**（#95 43 + I010 +1 = 44，0 回归，#92-#94 anchor test 41+26+50=117 项无回归）+ `check_smoke_consistency.sh` 7/7 规则 PASS + 0 SCRIPT ERROR。**5 verb 音频家族 1/5 → 5/5 函数定义层闭合**（Pulse F004 + Bind/Cut/Echo/Wave F004.B），剩余 T181 候选"caller layer"（5 verb ability `_execute_*()` 调 `AudioManagerEnhanced.play_*()`）+ "hit audio"（5 verb hit chime）+ "cooldown audio"（5 verb cooldown jingle）= 30min 跨 2 轮（#97 + #98）。
- **#94 — T174.B 5 verb windup VFX 父类抽取（D002 轻量版预演）+ F004 Pulse 音频闭环 + F009 STYLE_GUIDE 4 verb 命中色查表段 + I009 (50 项锚点 smoke 测试)**：新建 [`src/scripts/_verb_windup_vfx_base.gd`](file:///workspace/src/scripts/_verb_windup_vfx_base.gd) 父类（`class_name VerbWindupVFXBase extends Node2D` 64 行 docblock + 30 行实现）；5 verb windup VFX 类（[pulse](file:///workspace/src/scripts/pulse_windup_vfx.gd) / [bind](file:///workspace/src/scripts/bind_windup_vfx.gd) / [cut](file:///workspace/src/scripts/cut_windup_vfx.gd) / [echo](file:///workspace/src/scripts/echo_windup_vfx.gd) / [wave](file:///workspace/src/scripts/wave_windup_vfx.gd)）改为 `extends "res://src/scripts/_verb_windup_vfx_base.gd"`（path-based extends 避免 class_name load order 边角）。**T174.B 抽取内容**：(1) 5 verb 共享 state `_lifetime` / `_max_lifetime` / `_active` 抽到 base → (2) 5 verb 共享 `_ready()` `z_index = 10` 抽到 base → (3) 5 verb 共享 `_process(delta)` lifetime 跟踪 + auto-free safety net 抽到 base → (4) #93 T174 ramp-in tween 代码抽到 base 的 `_activate_windup_tween()` → (5) #92 T173 `fade_out_and_free` 抽到 base → (6) 5 verb 各自 trigger() 改为调 `_activate_windup_tween()`。**6th verb 接入模板**：新 verb windup VFX 只需 `extends VerbWindupVFXBase` + 实现 trigger() 调 `_activate_windup_tween()` + 写 verb-specific `_draw()` 即可。**F004 Pulse 音频闭环**：[`src/scripts/pulse_ability.gd`](file:///workspace/src/scripts/pulse_ability.gd) `_execute_pulse()` 内 `pulse_fired.emit` 之后调 `AudioManagerEnhanced.play_pulse()`（autoload 对齐 T181 #95 5 verb 音频家族候选；`is_instance_valid(_player)` 守卫保护 windup 期间玩家死亡边角）—— 5 verb 音频家族 1/5 进度，Bind/Cut/Echo/Wave 早就有 caller，**Pulse 是唯一缺口**。**F009 4 verb 命中色查表宪法段**：[`STYLE_GUIDE.md`](file:///workspace/STYLE_GUIDE.md) 色板段下增 "4 Verb 命中色查表常量" 段（30 行）—— 4 verb 调色四元组表 + 调用契约示例 + Wave 不参与此查表 + 6th verb 接入流程。T170 #88 锚定扩展为完整宪法级约束。**I009 50 项锚点 smoke 测试** —— [`tools/test_i009_t174b_f004_f009_smoke.gd`](file:///workspace/tools/test_i009_t174b_f004_f009_smoke.gd)（新文件 165 行，50/50 PASS）：T174.B base 19 断言 + 5 verb extends base + trigger() 调 `_activate_windup_tween()` = 10 断言 + F004 4 断言 + F009 17 断言。**同步更新 5 个旧 smoke test 适应 T174.B 重构**：[test_t167_t168_f006_smoke.gd](file:///workspace/tools/test_t167_t168_f006_smoke.gd) / [test_t165_t166_f005_smoke.gd](file:///workspace/tools/test_t165_t166_f005_smoke.gd) / [test_t171_t170d_smoke.gd](file:///workspace/tools/test_t171_t170d_smoke.gd) / [test_t173_windup_fadeout_smoke.gd](file:///workspace/tools/test_t173_windup_fadeout_smoke.gd) / [test_t174_windup_rampin_smoke.gd](file:///workspace/tools/test_t174_windup_rampin_smoke.gd)。**43/43 100% PASS**（与 #93 比 +1，0 回归）+ `check_smoke_consistency.sh` 7/7 规则 PASS + 0 SCRIPT ERROR + 0 parse error。
- **#93 — T174 5 verb windup VFX ramp-in tween 平滑曲线（"VFX 进入家族"，T173 ramp-out 对偶）+ I008 (40 项锚点 smoke 测试)**：[`src/scripts/pulse_windup_vfx.gd`](file:///workspace/src/scripts/pulse_windup_vfx.gd) / [`src/scripts/bind_windup_vfx.gd`](file:///workspace/src/scripts/bind_windup_vfx.gd) / [`src/scripts/echo_windup_vfx.gd`](file:///workspace/src/scripts/echo_windup_vfx.gd) / [`src/scripts/cut_windup_vfx.gd`](file:///workspace/src/scripts/cut_windup_vfx.gd) / [`src/scripts/wave_windup_vfx.gd`](file:///workspace/src/scripts/wave_windup_vfx.gd) 5 文件 `trigger()` 函数体统一 +13 行 + [`tools/test_t174_windup_rampin_smoke.gd`](file:///workspace/tools/test_t174_windup_rampin_smoke.gd)（新文件 110 行）。**T174 "VFX 进入家族"正式闭环** —— 与 #92 T173 "VFX 退出家族"（fade_out_and_free 0.05s 淡出 tween）对偶，本轮给 5 verb windup VFX 启动阶段也用 tween 曲线，让 0.04~0.10s 短 windup 启动也是平滑曲线而非线性 40%-then-hold 的"折线 pop"：(1) `modulate.a = 0.0` 显式初始化（保证 tween 起点一致）；(2) `var ramp_tween := create_tween()` 创建 tween；(3) `tween.set_trans(Tween.TRANS_QUAD).set_ease(Tween.EASE_OUT)` 二次曲线 + 缓出（front-load 可见性，让短 windup 也读得清）；(4) `tween.tween_property(self, "modulate:a", 1.0, _max_lifetime)` 全 windup 时长 ramp-in 1.0。`_draw()` 内同步移除旧 `var alpha_t := clampf(t / 0.4, 0, 1)`（Pulse/Bind/Cut）或 `clampf(t / 0.5, 0, 1)`（Echo）或 `clampf((t - phase_offset) / 0.4, 0, 1)`（Wave 3 环 ripple）线性 ramp，`col.a` 改为常量 peak 由 modulate.a tween 驱动全局 fade-in。**5 verb ramp-in 一致性**：5 份 trigger() tween 实现 byte-identical（modulate.a=0.0 起点 + TRANS_QUAD EASE_OUT 曲线 + 1.0 终值 + _max_lifetime 时长）可作为 6th verb 接入模板；5 verb 共用 tween 曲线让 0.04s Cut（短）/ 0.10s Pulse/Bind/Wave（长）都有相同 "ramp-up then settle" 视觉节奏，玩家学到一次 ramp-in 曲线就 apply 全 5 verb。**Wave 旧 ripple 折衷**：Wave 旧 3 环 phase_offset 涟漪（ring 0 t=0.0 / ring 1 t=0.18 / ring 2 t=0.36）改为 3 环同时 fade-in（仅保留 r_ratio 0.40/0.65/0.92 半径差 + ring_alpha_mult 0.55/0.78/1.0 亮度差），ripple 涟漪感由"时序错位"转为"空间层次"——更简单可预测，且与其他 4 verb 的 tween 曲线严格一致。**I008 40 项锚点 smoke 测试** —— T174.A 5 verb × 8 断言 = 40 锚点（trigger() 内 modulate.a=0.0 / create_tween() 存在 / "modulate:a", 1.0 终值 / _max_lifetime duration / Tween.TRANS_QUAD / Tween.EASE_OUT / T174 (#93) docblock 标记 / 旧 `alpha_t := clampf(t / X, ...)` 线性 ramp 严格从源代码中删除——双重严格：按行分割 + 跳过注释行 + 找 `alpha_t := clampf(t /` 模式，避免误命中合法的 `var t := clampf(_lifetime / ...)` lifetime 进展）。**42/42 100% PASS**（与 #92 比 +1，0 回归）+ `check_smoke_consistency.sh` 7/7 规则 PASS + 0 SCRIPT ERROR + 0 parse error。**5 verb windup "VFX 进入+退出家族"双闭环最终态** —— 进入侧（T174 0.04~0.10s ramp-in tween）+ 退出侧（T173 0.05s ramp-out tween）+ 中段（fire VFX takes over 同帧无 overlap）三段时间轴都统一为 tween 曲线，5 份 trigger() ramp-in 实现 byte-identical + 5 份 fade_out_and_free ramp-out 实现 byte-identical，可作为 6th verb 接入模板。
- **#92 — T173 5 verb windup VFX 0.05s 淡出 tween（"VFX 退出家族"）+ T173.C 补 resonance_wave_ability._exit_tree() 钩子 + I007 (49 项锚点 smoke 测试)**：[`src/scripts/pulse_windup_vfx.gd`](file:///workspace/src/scripts/pulse_windup_vfx.gd) / [`src/scripts/bind_windup_vfx.gd`](file:///workspace/src/scripts/bind_windup_vfx.gd) / [`src/scripts/echo_windup_vfx.gd`](file:///workspace/src/scripts/echo_windup_vfx.gd) / [`src/scripts/cut_windup_vfx.gd`](file:///workspace/src/scripts/cut_windup_vfx.gd) / [`src/scripts/wave_windup_vfx.gd`](file:///workspace/src/scripts/wave_windup_vfx.gd) 5 文件统一 +23 行 + [`tools/test_t173_windup_fadeout_smoke.gd`](file:///workspace/tools/test_t173_windup_fadeout_smoke.gd)（新文件 158 行）。**T173 "VFX 退出家族"正式闭环** —— 5 verb windup VFX 各自新增 `func fade_out_and_free() -> void`（实现一致 + byte-identical 可复制给未来 6th verb 接入）：(1) `if not _active: queue_free() return` 早退守卫（idempotent 防御重入）；(2) `_active = false` 停 `_process` 避免与 tween 竞速；(3) `var start_alpha: float = modulate.a` 捕获当前透明度（windup 中断在 0~0.7 之间任意值）；(4) `var tween := create_tween()` + `tween.tween_property(self, "modulate:a", 0.0, 0.05)`（`"modulate:a"` Godot 4 tween 语法从 1.0→0.0 0.05s TRANS_QUAD EASE_OUT 平滑淡出）；(5) `tween.tween_callback(queue_free)` 淡出结束自动 free。**4 verb ability `_exit_tree()` 切换**（`pulse_ability.gd` / `bind_ability.gd` / `echo_ability.gd` / `cut_ability.gd` 4 文件各 +3 行 + 10 行 docblock）—— 把硬 `_windup_vfx.queue_free()` 替换为 `_windup_vfx.fade_out_and_free()`，让 player 死亡 / 场景切换打断 windup（0.04~0.10s 窗口）时 VFX 平滑淡出而非硬 pop。**T173.C 补漏**：`resonance_wave_ability.gd` 是 5 verb ability 中**唯一缺 `_exit_tree()`**的（#89 T171 漏判），本轮补：(a) `var _windup_vfx: Node2D = null` 成员句柄（与 4 verb 句柄命名一致）；(b) `start_wave()` 在 `scene.add_child(windup_vfx)` 之后追加 `_windup_vfx = windup_vfx` 把 spawn 局部变量存到成员；(c) 文件末尾追加 `_exit_tree()` 函数（15 行 + 12 行 docblock）调 `fade_out_and_free()` + 清句柄（**5 verb _exit_tree 家族正式一致**——Pulse T166 / Bind T167 / Echo T168 / Cut T169 / Wave T173）。**5 verb "VFX 退出家族"特性**：fade_out_and_free 5 文件实现 byte-identical（验证 _pulse_sfx 模式：测试通过 `assert_contains` "func fade_out_and_free(" 5 份 + "create_tween()" 5 份 + `"modulate:a", 0.0` 5 份 + "0.05" 5 份 + "queue_free()" 5 份 + "T173 (#92)" 5 份）；0.05s 0.4× room transition duration 让 VFX 永远在下一房间 load 前 vanish（无视觉残留）；fade-out 是 queued-free 同帧触发无 overlap（5 verb 一致）。**I007 49 项锚点 smoke 测试** —— T173.A 5 verb × 6 断言 = 30 锚点（fade_out_and_free 函数声明 / create_tween 存在 / "modulate:a", 0.0 终值 / "0.05" 时长 / queue_free 结束 / T173 (#92) docblock 标记） + T173.B 4 verb ability × 4 断言 = 16 锚点（_exit_tree 函数存在 / fade_out_and_free 调用 / _exit_tree 函数体内**不**再含 _windup_vfx.queue_free()——通过 `_extract_exit_tree_body()` 提取函数体限定 scope，避免 start_*/_execute_* 合法硬 queue_free 误报 / T173 docblock 标记） + T173.C 3 锚点（Wave _exit_tree 存在 / fade_out_and_free 调用 / docblock 标记）= **49 项断言 PASS**。**额外修复**：T165/T166/F005 旧测试 `t166_ability.rfind("pulse_windup_vfx.gd")` 模式被 T173 docblock 注释（"See pulse_windup_vfx.gd:fade_out_and_free"）骗到错误位置（rfind 找到 docblock 注释而非 start_pulse() 内的 preload 真实调用），改为"从 func start_pulse 头到 func _execute_pulse 头区间内 find + 跳过 docblock 误报"的鲁棒模式；T167/T168/F006 旧测试 2 处 `bind_windup_vfx.gd` / `echo_windup_vfx.gd` 同样问题同法修。**41/41 100% PASS**（与 #90 比 +1，0 回归；T173 触发 T165/T166/F005 + T167/T168/F006 2 套件共 3 处断言更新，回归后全 PASS）+ `check_smoke_consistency.sh` 7/7 规则 PASS + 0 SCRIPT ERROR + 0 parse error。**5 verb windup "VFX 退出家族"最终态** —— 5 verb 5 元组 (Pulse / Bind / Echo / Cut / Wave) × 2 钩子 (`fade_out_and_free` + `start_*` `_windup_vfx` 句柄) × 1 退出点 (`_exit_tree` 调 `fade_out_and_free`) 全部统一，5 份 fade_out_and_free 实现 byte-identical 可作为 6th verb 接入模板。- **#90 — 审查 #90（本轮）**：完整代码质量 / 玩法 / 素材 / 文档审计；0 SCRIPT ERROR + 0 runtime ERROR + 55 .gd 文件 + 52 class_name 唯一（+4 windup classes 来自 #86 T167 Bind + #86 T168 Echo + #89 T171 Wave + 1 stable）/ 69 signal 完整 + 29 .tscn + 8 .json + 7 autoload + 0 TODO/FIXME + 114 PNG 100% 合法 + 102 .uid（**0 空文件** — 本轮修复 #86 留下的 2 个空 .uid `bind_windup_vfx.gd.uid` / `echo_windup_vfx.gd.uid` 由 `rm` + `--import` 重新生成 `uid://bh4oc6o1wkpl6` / `uid://clcrt5damt18k`）+ ASSET_REGISTRY 72 条 + 40 smoke test 套件 **40/40 100% PASS**（L001 修复后重测 0 回归）+ `check_smoke_consistency.sh` 7/7 规则 PASS（rule 7 README 同步 hook 由 #85 F002 引入已工作）；**严重 0 / 一般 0 / 轻微 1（L001 已修） / 信息 1（F004 audio 闭环建议下个 5 轮间隔 #91-#95 集中做）**。**5 verb windup 闭环最终态** —— Pulse `class_name PulseWindupVFX` Glass Cyan `#69C7CE` 1.0×→0.92× 收缩 ring（T166 #85）+ Bind `class_name BindWindupVFX` Muted Violet `#65506A` 1.0×→0.85× 螺旋内收（T167 #86）+ Echo `class_name EchoWindupVFX` Glass Cyan `#69C7CE` + Pale `#B7E7DD` + Amber `#F2B66E` 0.5×→1.0× 球外撑（T168 #86）+ Cut `class_name CutWindupVFX` Amber `#F2B66E` 0.0×→1.0× streak 横扫（T169 #87）+ Wave `class_name WaveWindupVFX` Pale `#B7E7DD` 3 环 ripple outward（T171 #89）5 个 `class_name extends Node2D` 类，`trigger(origin, half_radius, duration)` 签名一致，5 verb motif 全部独立（Pulse 收缩 / Bind 螺旋 / Echo 撑开 / Cut 横扫 / Wave 涟漪），5 verb 色严格在 STYLE_GUIDE 限制色板内，0 板外色。**4 verb 命中反馈闭环最终态** —— Pulse Coral `#E86D5A` (0.91, 0.427, 0.353) flash 0.10s/0.18 + Bind Violet `#65506A` (0.398, 0.314, 0.416) flash 0.10s/0.18 + Cut Amber `#F2B66E` (0.949, 0.714, 0.431) flash 0.09s/0.18 + Echo Cyan `#69C7CE` (0.412, 0.78, 0.808) flash 反射 0.08s/0.20 / 非反射 0.06s/0.12 4 verb 命中色 4 元组 + 4 verb LIGHT 1.0/0.08s 屏抖 5 元组完全统一（来自 T170a/b/c/d #88-#89，**4 verb 命中节奏"1/16 beat groove"**）。完整审查报告见 [REVIEW_LOG.md](file:///workspace/REVIEW_LOG.md)。
- **#89 — T171 5 verb windup 家族闭环（Wave 第 5 色 Pale Resonance halo VFX）+ T170d Cut 命中 LIGHT 屏抖 + I006 18 项锚点 smoke 测试**：[`src/scripts/wave_windup_vfx.gd`](file:///workspace/src/scripts/wave_windup_vfx.gd)（新文件 110 行 `class_name WaveWindupVFX extends Node2D`）+ [`src/scripts/resonance_wave_ability.gd`](file:///workspace/src/scripts/resonance_wave_ability.gd) `start_wave()` +25 行 + [`src/scripts/player.gd`](file:///workspace/src/scripts/player.gd) `_on_cut_hit` +18 行 + [`tools/test_t171_t170d_smoke.gd`](file:///workspace/tools/test_t171_t170d_smoke.gd)（新文件 152 行）18 项断言 PASS。**5 verb windup 调色五元组正式闭环 + 4 verb 命中屏抖分工完成** —— **T171** 新建 `wave_windup_vfx.gd` Pale Resonance `#B7E7DD`（比 Pulse Cyan 更冷更"光"——Wave "AOE 中心爆发而非定向打击"语义匹配，色温"穿透力最强"）+ 3 环 concentric halo r_ratio [0.40, 0.65, 0.92]（"声波辐射"主题，4 verb 1.0→0.92 收缩 ring / 1.0→0.85 螺旋 / 0.5→1.0 球 / 0.0→1.0 streak 之外的第 5 motif）+ per-ring alpha_mult [0.55, 0.78, 1.0]（外环最亮 = "声波前导"）+ phase_offset [0.0, 0.18, 0.36] 渐入 staggered "ripple outward" sound-wave motif + peak_alpha 0.65（比 Pulse 0.70 略低——"比空气还轻的 verb"，transient 非 solid）+ ring_width 1.2（比 4 verb 1.5px 细 0.3px——3 环同时存在需要视觉层次）+ z_index 10 + queue_free safety net + STYLE_GUIDE 引用；`resonance_wave_ability.gd` 集成 `preload("res://src/scripts/wave_windup_vfx.gd").new()` + `trigger(_pending_origin, wave_radius * 0.5, windup_time)` + `scene.add_child(windup_vfx)`（preload 而非 class_name 引用——与 4 verb 家族一致，preload path-based 引用让 headless smoke test load-order 决定性；0.5× radius——4 verb 家族一致 "precursor 而非 fire"；挂到 current_scene 而非 player 子节点——5 verb 家族一致：ring 位置稳定在世界坐标，player 移动时 ring 不跟着走，让 0.10s 期间 halo 是"我留在原地的 0.5s 警告"而非"我跟随玩家的拖尾"）。**T170d** `_on_cut_hit(_target)` 在 `flash_color(Color(0.949, 0.714, 0.431, 1.0), 0.09, 0.18)` 之后追加 `shake_preset(ScreenShake.Preset.LIGHT)`（**4 verb 命中屏抖 1.0/0.08s LIGHT 数值统一**——CUT 1.5/0.06s fire shake 衰减完 = LIGHT 1.0/0.08s hit shake 才开始，间隔 0.03~0.10s 不重叠 = "挥→中"两步触觉；max_targets=6 多目标风险由 ScreenShake tween 内部 dedupe 兜底，**视觉 = 1 次 0.08s LIGHT** 与 Pulse 多目标场景行为完全一致；LIGHT 而非 HEAVY 因为 Cut 单体命中反馈已经很强——HEAVY 喧宾夺主；Amber flash 无回归）。**I006 新冒烟测试 `tools/test_t171_t170d_smoke.gd` (152 行) 18 项断言 PASS** —— T171 段 10 项（`class_name WaveWindupVFX` 声明 / `extends Node2D` 与 4 verb 一致 / `trigger(origin, half_radius, duration)` 签名匹配 4 verb 家族 / `Color("#B7E7DD")` Pale Resonance 第 5 色 / `@export var ring_count: int = 3` 默认 3 环 / `z_index = 10` above world below HUD / `queue_free()` safety net / `T171 (#89)` docblock 标记 / docblock 含 "ripple outward" sound-wave motif 关键词 / `STYLE_GUIDE` 引用作为色域来源权威）+ T171 集成段 4 项（`resonance_wave_ability.gd:start_wave` 内 `preload("res://src/scripts/wave_windup_vfx.gd").new()` 存在 / 完整 `trigger(_pending_origin, wave_radius * 0.5, windup_time)` 调用 / `scene.add_child(windup_vfx)` 挂到 current_scene / `T171 (#89)` docblock 标记在 resonance_wave_ability.gd）+ T170d 段 4 项（`_on_cut_hit` 内 `ScreenShake.shake_preset(ScreenShake.Preset.LIGHT)` 调用 / `T170d (#89)` docblock 标记 / 完整 `flash_color(Color(0.949, 0.714, 0.431, 1.0), 0.09, 0.18)` Amber flash 无回归 / `if ScreenShake and ScreenShake.has_method("shake_preset"):` 守卫保留）。**18/18 PASS** + 0 SCRIPT ERROR + 0 parse error + 21/21 #88 T170 套件无回归 + check_smoke_consistency.sh 7/7 规则 PASS。风格 0 漂移（T171 `#B7E7DD` Pale Resonance 严格在 STYLE_GUIDE 限制色板内，**5 verb windup 五元组 Pulse Cyan / Bind Violet / Cut Amber / Echo Cyan / Wave Pale 全部在限制色板内**；T170d 复用 T098 Amber `#F2B66E` + LIGHT preset，**4 verb 命中反馈色 4 元组 + LIGHT 0.08s 屏抖分工 5 元组**完整）。
- **#88 — T170 4 verb 命中反馈 VFX polish（Bind 命中反馈 / Echo 命中非反弹反馈 / Pulse 命中屏抖）**：`player.gd` 改动 1 文件新增 ~50 行。**T170a Bind 命中反馈** —— `_ready` 中 `bind_ability.bind_hit.connect(_on_bind_hit)`（`has_signal` 守卫保 pre-bind-hit 存档兼容），新增 `_on_bind_hit(target)` handler 调 `ScreenShake.flash_color(Muted Violet #65506A, 0.10s, 0.18)` + `ScreenShake.shake_preset(LIGHT 1.0/0.08s)` 补"钉住"触感，与 Pulse Coral / Cut Amber / Echo Cyan 三 verb 命中反馈形成 4 verb 色域分工（**色域 4 元组**：Pulse Coral / Bind Violet / Cut Amber / Echo Cyan，"看到闪就知道是哪个 verb"快速识别）；0.10s / 0.18 数值与 Pulse 命中（T098）对称让 4 verb 反馈节奏统一；LIGHT 而非 HEAVY 因为 Bind 语义"温柔牵制"而非"暴力推开"。**T170b Echo 命中非反弹反馈** —— `_on_echo_hit` 之前 `is_reflect=false` 早退无任何屏幕反馈（`echo_ability.gd:278 emit(enemy, false)` = 敌人物理接触护盾被短致盲 0 伤），现在补 `ScreenShake.flash_color(Glass Cyan #69C7CE, 0.06s, 0.12)` —— 0.06s / 0.12 比反弹路径（T097 0.08s / 0.20）**更短更暗**，反弹 = "成功回击"高反馈 / 非反弹 = "温和挡下"低反馈，6:3 比例让"反 > 挡"视觉权重正确。**T170c Pulse 命中屏抖** —— `_on_pulse_hit` 已有 Coral flash（T098），现在补 `ScreenShake.shake_preset(LIGHT)` (1.0/0.08s) 作为"打到了"补充触觉；与 `_on_pulse_fired` 的 PULSE 2.0/0.10s shake 间隔 0.05~0.15s 不会重叠（fire shake 衰减完 = hit shake 才开始），形成"推→中"两步触觉。**新冒烟测试 `tools/test_t170_smoke.gd` (210 行) 21 项断言 PASS** —— T170a 8 项（connect/handler/4 个签名锚点 + null 守卫 + Muted Violet 色 + LIGHT shake + docblock 标记）/ T170b 6 项（is_reflect 分支保留 + 非反弹 flash_color 调用 + Glass Cyan 色 + 0.06s/0.12 数值 + 反弹路径无回归 + docblock）/ T170c 3 项（LIGHT shake 调用 + docblock + Coral flash 无回归）/ 4 verb 色域分工交叉检查 4 项（Pulse Coral / Bind Violet / Cut Amber / Echo Cyan 4 色均保留无回滚）。**21/21 PASS** + 0 SCRIPT ERROR + 0 parse error + check_smoke_consistency.sh 7/7 规则 PASS。风格 0 漂移（3 反馈色严格在 STYLE_GUIDE 限制色板 / 4 verb 调色 4 元组分工不变）。
- **#87 — I005 补 #86 缺测试 (33 项断言 smoke 套件) + T169 CutAbility 0.06s 黄色 line streak pre-cut VFX + F007 4 verb ability 内部 `_consume_verb_cost` / `_setup_windup_state` 共享模式 helper**：**新测试文件 `tools/test_t167_t168_f006_smoke.gd` (230 行) 33 项断言 PASS** 覆盖 #86 三个任务全部代码改动点（**T167 Bind windup VFX 11 项** — bind_windup_vfx.gd 存在 + extends Node2D + Muted Violet `#65506A` + arc_count=3 + _end_scale=0.85 内拉 + lifecycle + bind_ability spawn/free/_exit_tree 顺序 + bind_radius*0.5 透传；**T168 Echo windup VFX 11 项** — echo_windup_vfx.gd 4 参数 trigger 签名 + 3 色 Glass Cyan + Pale Resonance + Amber Voice + _end_scale=1.0 撑开（vs Pulse 0.92/Bind 0.85 内缩反向）+ _start_scale=0.5 + lifecycle + echo_ability spawn/free/_exit_tree 顺序 + echo_radius*0.5+echo_radius 4 参数 trigger 调用；**F006 refactor 8 项** — _try_verb() 2 参签名 + 4 个 _start_X_at() wrapper + 4 verb handler 体内 _try_verb 委托（600 char 窗口允许 docblock）+ _try_verb body 含 3 关键步骤 + _handle_wave 保持原 4 状态路由 + F005 helper 保留 + D001 is_action_globally_blocked 公开函数保留；**D001 regression + 4 verb 一致性交叉检查 3 项**），完成 #86 末尾"保留 #87 视情况添加 test_t167_t168_f006_smoke.gd"承诺。**T169 新文件 `cut_windup_vfx.gd` (61 行)** `class_name CutWindupVFX extends Node2D` **4 verb windup 第 4 视觉 motif** — Pulse ring（0.5×→0.92× 内缩）/ Bind spiral（0.5×→0.85× 旋转内收）/ Echo sphere（0.5×→1.0× 撑开）/ **Cut streak**（0.0×→1.0× 沿 cut 方向延伸，让玩家在 0.06s 前摇中即可辨别哪个 verb 在蓄力，5-verb 链 T142 防误触 UX 提示完整），Amber Voice `#F2B66E` 严格对齐 STYLE_GUIDE 限制色板（**4 verb 调色四元组** — Pulse Glass Cyan / Bind Muted Violet / Echo Glass Cyan+Amber Voice / Cut Amber Voice），`trigger(origin, half_radius, direction, duration)` 4 参数签名（与 T168 Echo 对齐），2px stroke 双线 draw_line + 1.5px 垂直偏移防 dark tileset 1px 直线消失，alpha 0→0.7 ramp-in 前 40%（Cut 0.06s 是 4 verb 最短前摇比 Pulse 0.4 更快），scale 0.0→1.0 沿 cut 方向延伸（与 cut_vfx.gd arc swing motion 同向 windup-to-fire 过渡连续）；`cut_ability.gd` 新增 `var _windup_vfx: Node2D = null` 句柄 + `start_cut()` 集成 + `_execute_cut()` 顺序敏感 free（cut_vfx.gd 同帧 spawn arc 替换 streak）+ `func _exit_tree()` 钩子（3 道 free 保险同 T166/T167/T168 模式）。**F007 refactor 4 verb 内部共享 2 helper 模式** —— 4 verb 各加 byte-identical `_consume_verb_cost(cost: int) -> bool` + `_setup_windup_state(origin, direction) -> void`（GDScript 限制 4 verb 各自重写一份 helper，但命名 + 签名 + docblock 一致，未来 base class `_verb_ability_base.gd` 抽取铺路），4 verb `start_X()` 顶部 4-5 行（`if not can_X: return false; if not GameState.consume_resonance(X_cost): return false; _is_winding_up = true; _windup_timer = windup_time; _pending_origin = origin; _pending_direction = direction`）缩为 2 行调用（`if not _consume_verb_cost(X_cost): return false; _setup_windup_state(origin, direction)`）；**echo_ability 特殊** — `start_echo(origin)` 不接收 direction 参数（盾中心 pop 语义）但调用 `_setup_windup_state(origin, Vector2.ZERO)` 保持 4 verb 签名一致，新增 `var _pending_direction: Vector2 = Vector2.ZERO` 字段（不读只用，与 pulse/bind/cut 字段定义 byte-identical）；**3 层 helper 抽象栈** —— F005 player 层 `_pre_verb_block_check` / F006 player 层 `_try_verb` / F007 ability 层 `_consume_verb_cost` + `_setup_windup_state`，未来加 verb 边际成本降到 "1 行 wrapper + 1 个新 ability 文件 + copy-paste 2 helper"。**38/38 smoke tests PASS** + 0 SCRIPT ERROR + check_smoke_consistency.sh 7/7 规则 PASS
- **#86 — T167 BindAbility windup 0.5× Muted Violet 螺旋 VFX + T168 EchoAbility 0.5×→1.0× Glass Cyan 球 VFX + F006 player.gd 4 verb handler 提取 `_try_verb()` helper**：**新文件 `bind_windup_vfx.gd` (87 行)** `class_name BindWindupVFX extends Node2D` 自管理 lifecycle — `trigger(origin, half_radius, duration)` 设 `global_position` + `_radius` + `_max_lifetime` 启动，`_draw()` 渲染 3 段 spiral 弧旋转内收（`draw_arc` 12 段 / 0.7 间隙 / `base_angle = _lifetime * 4.0` 旋转 4 rad/s），scale 1.0→0.85 收缩（**比 Pulse 0.92 更激进内拉**——Bind 语义"往中心拉"呼应 A033 icon spiral motif），Muted Violet `#65506A` 严格对齐 STYLE_GUIDE 限制色板，alpha 0→0.75 ramp-in 首 40% 防 frame-0 闪烁；`bind_ability.gd` 新增 `var _windup_vfx: Node2D = null` 实例句柄，`start_bind()` 在 consume_resonance 成功后 spawn 挂到 `get_tree().current_scene`（**非 player 子节点**让 player 移动时 ring 位置稳定在世界坐标），`_execute_bind()` 在 `bind_fired.emit` *之前* free windup_vfx（顺序敏感：bind VFX 同帧 spawn 替换 windup），新增 `func _exit_tree()` 钩子（**关键 cleanup**：player 在 windup 中被 scene change 销毁 bind_ability 退树时连带 free windup_vfx 防 leak），3 道 free 保险同 T166 Pulse 模式。**新文件 `echo_windup_vfx.gd` (90 行)** `class_name EchoWindupVFX extends Node2D` **与 Pulse/Bind 反向 motion language**——Pulse/Bind 是 0.5×→0.85-0.92× 内缩（能量聚拢/向内拉），Echo 是 0.5×→1.0× 外撑（盾"砰"地一下弹出接住来袭），`_draw()` 3 层 painter's order：Layer 1 玻璃填充 `draw_circle` 半径 `lerp(half, full, t)` alpha 0→0.18 / Layer 2 高光 rim `draw_arc` 1.5px alpha 0→0.55 / Layer 3 中央暖点 `draw_circle(Vector2.ZERO, 2px)` alpha 0→0.45，三色皆来自 EchoVFX palette 维持 verb 调色一致（fill `#69C7CE` Glass Cyan / rim `#B7E7DD` Pale Resonance / core `#F2B66E` Amber Voice），alpha ramp-in `t / 0.5`（**比 Pulse 的 0.4 更快**——Echo windup 仅 0.08s 短窗口，前 0.04s 必须 readable）；`echo_ability.gd` 同模式集成 windup_vfx + _exit_tree 钩子。**F006** `player.gd` 在文件末尾追加 1 个 `_try_verb()` helper + 4 个 `_start_X_at()` wrapper（`_start_pulse_at` / `_start_bind_at` / `_start_cut_at` / `_start_echo_at`），4 verb handler 各自缩成 1 行委托 `_try_verb("pulse", _start_pulse_at)` 等；**新 `_try_verb(action_name: String, start_fn: Callable) -> void`** —— 5 步中央管道：(1) `_pre_verb_block_check()` 守卫复用 F005 helper → (2) `Input.is_action_just_pressed(action_name)` rising-edge → (3) 在 helper 内计算 `origin = global_position + Vector2(0, -8)` + `dir = Vector2.RIGHT if _facing_right else Vector2.LEFT`（4 verb 共用"头部 8px / 面向方向"公式与原 handler 字节级一致）→ (4) `start_fn.call(origin, dir)` 委托给 verb 内部 `start_*()`（Echo wrapper 忽略 `dir` 因盾中心 pop 语义）→ (5) 失败时统一 `hud.show_pulse_blocked()` 提示（与原 handler 行为完全一致）；**4 wrapper** 签名统一 `(origin: Vector2, dir: Vector2) -> bool`，内部 `if ability: return ability.start_X(origin, dir) else: return false`（`else false` 路径让 `_try_verb()` 触发 blocked toast 兜底**保持 #85 旧语义不变**）；**Wave 排除**——`# F006 (#86) — Why not also include _handle_wave?` docblock 详述 Wave 有 4 个 verb 状态路由（active/winding_up/charging/blocked，T143）需要 4 分支专属 HUD 提示不能套这个 1-toast 通用 helper，`_handle_wave()` 保持原样；**未来扩展价值**——加新 guard 条件（"dialogue open"）只需 OR 进 `_pre_verb_block_check()` 一处，加第 6 verb（方向性）只需写 1 行 `_handle_X()` + 1 个 `_start_X_at()` wrapper；本轮未新增冒烟测试（#85 审查通过后零回归历史 + 源码净增 ~245 行未达 500 阈值保留 #87 视情况添加 `test_t167_t168_f006_smoke.gd`）
- **#85 — T165 BGM tier-up ScreenShake flash_color (0.15s Glass Cyan 256 层) + T166 PulseAbility windup 0.08s→0.10s + 0.5× Glass Cyan pre-pulse ring VFX + F005 player.gd 4 verb handler 提取 `_pre_verb_block_check()` helper**：`audio_manager_enhanced.gd.request_boss_music()` 在 `if new_tier > current_tier:` 分支末尾追加 `ScreenShake.flash_color("#69C7CE", 0.15, 0.18, flash_layer=256)`（Glass Cyan 严格对齐 STYLE_GUIDE 限制色板 / duration 0.15s 与 300ms 音乐 crossfade 中段 tempo 对齐 / peak_alpha 0.18 subtle vignette / `flash_layer=256` 走 T163 #84 新参数高于 hit-flash 128 避免互消），调用前用 `Engine.has_singleton("ScreenShake") or _has_screen_shake_autoload()` 双重防御（headless 测试下 audio manager 可能在 ScreenShake 之前 `_ready()` 完单一 `Engine.has_singleton` false-positive 漏报），新增私有 `_has_screen_shake_autoload()` helper（`tree.root.has_node("ScreenShake")` 探测 + `Engine.get_main_loop` null 守卫）；**新文件 `pulse_windup_vfx.gd` (90 行)** `class_name PulseWindupVFX extends Node2D` 自管理 lifecycle — `trigger(origin, half_radius, duration)` 设 `global_position` + `_radius` + `_max_lifetime` 并启动，`_draw()` 渲染 0.5× radius Glass Cyan 圆环（`draw_arc(Vector2.ZERO, ring_r, 0, TAU, 32, col, ring_width)` 32 段），scale 1.0→0.92 线性收缩（"能量聚拢"暗示与 fire VFX 反向扩张形成"→|→ 炸开"语言），alpha 0→0.7 ramp-in 首 40% 防 frame-0 闪烁；`pulse_ability.gd` `windup_time: float = 0.08` → `0.10`（与 bind_ability 0.1s 一致 4 verb windup 节奏统一），新增 `var _windup_vfx: Node2D = null` 实例句柄，`start_pulse()` 在 consume_resonance 成功后 spawn 挂到 `get_tree().current_scene`（**非 player 子节点**让 player 移动时 ring 位置稳定在世界坐标），`_execute_pulse()` 在 `pulse_fired.emit` *之前* free windup_vfx（顺序敏感：fire VFX 在 player._on_pulse_fired 同帧 spawn 两 VFX 不重叠 1 帧），新增 `func _exit_tree()` 钩子（**关键 cleanup**：player 在 windup 中被 scene change 销毁 pulse_ability 退树时连带 free windup_vfx 防 leak），3 道 free 保险（`_execute_pulse` 显式 / `_exit_tree` scene-change / `_process._max_lifetime` 超时自清）；`player.gd` 新增私有 helper `func _pre_verb_block_check() -> bool: return is_action_globally_blocked()`（**保留** `is_action_globally_blocked()` 公共函数不动以兼容 `_handle_jump` / `_on_echo_multi_reflect`），4 verb handler 头注释同步追加 `# F005 (#85) — single _pre_verb_block_check() guard shared by the 4 directional verbs` 并把 `if is_action_globally_blocked(): return` 替换为 `if _pre_verb_block_check(): return`（未来加新 guard 条件如 "dialogue open" / "shop UI focused" 只需 OR 进 helper 一处 4 verb handler 同步生效）；`test_t165_t166_f005_smoke.gd` 23 项新断言 PASS + 全 37/37 冒烟测试套件 PASS
- **#84 — T101 ResonanceWave 命中粒子层叠 8→12 (4 new visual layers) + T163 ScreenShake.flash_color / flash_grayscale 接受可选 [flash_layer] 参数 + F004 修复 3 套件 pre-existing stale-state 冒烟测试**：`resonance_wave_vfx.gd` 新增 14 常量（`DEEP_SHADOW_RADIUS_RATIO=0.42` / `INNER_HALO_RADIUS_RATIO=0.55` / `OUTER_WISP_RADIUS_RATIO=1.18` / `OUTER_WISP_COUNT=12` / `SPARKLE_RADIUS_RATIO=0.70` / `SPARKLE_COUNT=6` 等）+ 3 色常量（`#65506A` Muted Violet / `#B7E7DD` Pale Resonance / `#F2B66E` Amber Voice 严格对齐 STYLE_GUIDE 限制色板）+ `_draw()` 改写为 9 段 painter's order（deep_shadow→inner_halo→ring_fill→ring_stroke→8 prism_rays→12 outer_wisps→6 sparkle_stars 闪烁 alpha→center_core→bounce_flash），4 新 layer 从 1 layer 静态环变 8 layer 多深度冲击波；`screen_shake.gd` `flash_color(..., flash_layer: int = 128)` + `flash_grayscale(..., flash_layer: int = 128)` 接受 canvas layer 索引（默认 128 保持向后兼容，上层 256 高于 HUD，下层 64 低于 HUD），`_active_grayscale` + `_active_color_flash` 从单 `CanvasLayer` 引用重构为 `Dictionary` 按 layer_idx 分桶（同 layer 后调用取消前调用 / 跨 layer 并行），`stop()` 迭代 `dict.keys()` 清掉*所有* layer 上的活动 flash；F004 修复 (1) `test_t150_t147_t149_smoke.gd` `_handle_jump` 字符串窗口 1800 → 2500 char（T145 17 行 docblock + T147 4 行 + D001 注释让相关代码落在 char 1827-1900）+ 新增 D001 sync 断言验证 `is_action_globally_blocked()` 是 `PlayerActionGate.is_blocked()` 的 thin delegate, (2) `test_t158_t156_f002_smoke.gd` F002.7 / F002.8 硬编码 `#81` → 动态 `ITERATION_COUNT.txt - 1`（含 file-not-found fallback），(3) 复用 (1) 顺带同步 T147 守卫与 #76 重命名；`test_t101_t163_f004_smoke.gd` 18 项新断言 PASS + 全 36/36 冒烟测试套件 PASS
- **#83 — T162 PlayerProfilePanel "最近 5 局详细" 列表 + T159 InkWarden phase 2 dissolve 0.25s 出 + 0.30s 入 tween**：`pause_menu.tscn` 在 `ProfileTrend20` 之后新增 `ProfileRecentTitle`（"✦ 最近 5 局 ✦" Amber Voice 9pt center）+ `ProfileRecentList` VBoxContainer；`pause_menu.gd` 新增 `@onready var _profile_recent_list` + 3 常量（`_PROFILE_RECENT_RUNS_MAX=5` 视觉密度上限 / `_COLOR_RECENT_RUN_NORMAL` Pale Resonance 沿用 trend 调色板 / `_COLOR_RECENT_RUN_LATEST` Amber Voice 高亮最近 1 局）+ 新方法 `_refresh_recent_runs_list()` 实现 5 个设计选择（最新 1 局 Amber Voice 高亮 / reversed order 最新在顶 / 每行 4 字段 `Run #N 房 X 净 Y 碎 Z 时 mm:ss` / 空 history 走"暂无 run 记录"占位 / dynamic child creation 防 stale data）；**与 T131 trend 5/10/20 行互补**：trend 给"宏观"平均指标，recent 给"具体"每局明细（"Run #5 净 0 死 3"立刻归因到"没找到 Pulse"）。`ink_warden.gd` 顶部新增 4 常量（`PHASE_2_DISSOLVE_OUT_TIME=0.25` / `PHASE_2_DISSOLVE_IN_TIME=0.30` / `PHASE_2_DISSOLVE_OUT_SCALE=1.15` / `PHASE_2_DISSOLVE_IN_START_SCALE=0.85`）；`_enter_phase_2()` sprite swap 段改写为 5 段 tween（snap reset / dissolve out 0.25s scale 1.0→1.15 + alpha 1.0→0.0 / snap start / dissolve in 0.30s scale 0.85→1.0 + alpha 0.0→1.0 / existing red flash + settle 完整保留），共 1.03s 视听序列与 T156 5 段完美嵌套（shake 中段 = dissolve 中段）。原来 1f sprite 硬切被替换为 0.55s 渐变，让 phase 2 进入"我正在失控进化"而非"突然换皮"的体感。`test_t162_t159_smoke.gd` 21 项断言 PASS
- **#82 — F003 4 文档同步 Python 3.14+ zipfile 兜底 + T160 PauseMenu "新成就!" Banner + T161 settings "还原所有推荐" 按钮 + D001 PlayerActionGate autoload 抽出**：`godot/README.md` + `README.md` + `README.zh-CN.md` + `CONTRIBUTING.md` 4 文档同步重写为 方法 B-1 `unzip -FF` 强容错（沙箱 / Python 3.14+ 推荐）+ 方法 B-2 Python `zipfile` 兜底（**仅 Python ≤ 3.13 有效**），实测复现 Python 3.14.4 `BadZipFile: Bad magic number for file header`；`pause_menu.tscn` 新增 `NewAchvBanner` Label（top center Amber Voice 10pt "✦ 新成就！✦"）+ `pause_menu.gd` 3 常量（`_BANNER_DURATION=0.8` / `_BANNER_FADE=0.4` / `_BANNER_RECENT_UNLOCK_WINDOW=5.0`）+ 双轨触发（menu 可见直接 animate + 不可见记 `_last_seen_unlock_ts` 5s 窗口内 ESC 补播）；`settings_menu.tscn` 新增 `RestoreAllButton`（Amber Voice 200×24）+ `settings_menu.gd` `_on_restore_all_pressed()` 3 阶段（按键 `InputMap.action_erase_events` + `_DEFAULT_BINDINGS` / 音量 4 slider 100% + `AudioServer.set_bus_volume_db` / autosave `SaveSystem.set_autosave_enabled/interval/slot` 推默认）+ amber 0.8s "✓ 已还原" toast；`src/autoload/player_action_gate.gd` 新建 22+80 行 Node autoload（4 public API: `register_player/unregister_player/is_blocked/get_player`）+ `is_blocked()` 复合 OR（`_is_dying` + `wave_ability.is_globally_blocking`）+ `project.godot` autoload 段注册 + `player.gd` `_ready/_exit_tree` register/unregister + `is_action_globally_blocked()` 改 thin delegate + `resonance_wave_ability.gd` `is_globally_blocking()` 头部加 D001 refactor 注释；`test_d001_t160_t161_f003_smoke.gd` 21 项断言 PASS
- **#81 — T158 EchoAbility 4 重击命中后慢动作 0.4s 0.85x time-scale + T156 ArchiveStorm 主摄像机 1f skybox rotate 0.5° 0.2s ease 收回 + F002 `check_smoke_consistency.sh` README 同步检查 hook 规则 ⑦**：`echo_ability.gd` 新增 `signal echo_multi_reflect(count: int)` + `const MULTI_REFLECT_THRESHOLD = 4` + 在 `_reflect_projectile` 末尾首次达到 4 emit 一次（同 cast 后续反弹不再 emit 防 spam）；`player.gd._ready` 用 `has_signal("echo_multi_reflect")` 守卫连 `_on_echo_multi_reflect` → 0.4s await × 0.85 time_scale，await 结束检查 `_is_dying` 避免覆盖 die() 的 1.0 重置；`screen_shake.gd` 新增 `punch_rotation(degrees=0.5, duration=0.2)` API（cam.rotation = deg_to_rad 立即设置 + tween 0.2s quad ease 收回，`stop()` 兜底归零 + kill tween）；`ink_warden.gd._enter_phase_2()` 顶部（shake_preset 之前）调 `ScreenShake.punch_rotation(0.5, 0.2)` 形成 5 段视听序列：sky 反应 → BOSS_PHASE2 震 → sprite swap → RepairVFX ring → BGM tier-up；`check_smoke_consistency.sh` 加 rule 7（README.md + README.zh-CN.md "Recent completed work" / "最近完成的工作" 段解析最新 #N 与 ITERATION_COUNT.txt 比对，滞后 ≥2 轮 FAIL 阻断 commit / 滞后 1 轮 WARN），根除 G001 第 4 次同类风险；`test_t158_t156_f002_smoke.gd` 28 项断言 PASS
- **#79 — T152 0 数灰阶 + T153 槽位 jingle + T151 "最近" badge**：`pause_menu.gd` `_COLOR_ZERO_STAT` 暖灰 `#808389` + `_set_zero_aware_stat()` helper（6+4 行用 0 占位 "—"）；`audio_manager_enhanced.gd` `_SAVE_SLOT_MIDI_NOTES = [72,76,79,84,88]` pentatonic C5/E5/G5/C6/E6 + `_generate_save_slot_jingle()` 0.25s 三角波 bell body + `play_save_slot_jingle()` 公开 API（save/load 共享）；`save_load_menu.gd` `_find_most_recent_slot()` + `_format_recent_badge()` BBCode `[color=#B7E6DC]★ 最近[/color]` Pale Resonance + `_refresh_slots` 一次扫 5 槽定 most_recent_slot 下传 `_refresh_card` / `_refresh_list_row`；4 状态字符完整化（[·]/[—]/[✗]/[✓]）；`test_t152_t153_t151_smoke.gd` 19 项 PASS
- **#78 — T144 wave_focus 谐波 + T148 wave_combo chime tail + T154 灯反向闪**：`audio_manager_enhanced.gd` `_wave_hit_streams: Dictionary` 4 level 缓存（0=1320Hz 基频 2.4x 谐波 / 1=+3.6x / 2=+5.0x / 3=+6.8x 凯旋钟塔）按 `GameState.get_perk_count("wave_focus")` 路由；`play_wave_combo()` 0.6s E6+G#6 双音衰减 + `_on_wave_combo()` 末接；`save_lantern.gd` `flash_coral_pulse()` 0.15s Coral Pulse 反向闪 + `silenced_web.gd on_cut_triggered` 迭代 `save_lantern` group 触发；`test_t144_t148_t154_smoke.gd` 26 项 PASS
- **#77 — T150 5 动词 profile + T147 jump 阻塞 UX + T149 Echo parallax**：`player_stats.gd` `last_used_verb` 字段 + `record_ability_used` 入口首行刷新 + `reset_stats` 清空 + `pause_menu.tscn` ProfileLastVerb Label + `pause_menu.gd` match 5 动词 BBCode 调色板（pulse Coral / bind Violet / cut Amber / echo Cyan / wave Pale Resonance）；`hud.gd` `show_jump_blocked()` + `player.gd _handle_jump` 双层守卫（is_action_just_pressed 触发）；`echo_vfx.gd` PARALLAX 三常量（rotation 0.5 / radius 1.08 / alpha 0.55）+ PI/8 偏移 + 0.25 rad/s 副层旋转；`test_t150_t147_t149_smoke.gd` 22 项 PASS
- **#76 — T143 wave 4 状态提示 + T145 is_action_globally_blocked 重构 + T146 wave_combo 屏震**：`hud.gd` 4 verb 专属方法（charging / winding_up / active / blocked）+ `player.gd _handle_wave` 4 分支路由按生命周期排（active → winding_up → cooldown → cost-low）；`_is_wave_globally_blocking` 重命名为公开 `is_action_globally_blocked()` + OR `_is_dying` 守卫 + 4 verb handler 调用点 + `_handle_jump` 阻塞时清零 coyote+buffer timer 防死亡解除后"原地跳"；`resonance_wave_ability.gd` `wave_combo` signal（`@export wave_combo_threshold=3`）+ `_deactivate_wave` 末尾 emit；`player.gd _on_wave_combo` shake(4.0, 0.4) + flash_color(Electric Violet #8C5BFF, 0.18s, 0.30)；`test_t143_t145_t146_smoke.gd` 25 项 PASS + `test_t142` 重命名同步
- **#75 — 审查 #75（本轮）**：完整代码质量 / 玩法 / 素材 / 文档审计；0 SCRIPT ERROR + 0 runtime ERROR + 47 class_name 唯一 + 77 signal 完整 + 114 PNG 合法 + 6 autoload 一致 + 72 ASSET_REGISTRY 记录 + 28 冒烟测试全 PASS + 1 一般 (G001 README Recent work 补 #61-#75 15 轮已修) + 1 信息 (候选池继续走 polish 路线)
- **#75 — T130 hotfix (成就 13→14 同步) + T142 (5-verb 链防误触安全网) + T141 (wave 命中 audio cue)**：成就定义 `total_count`/`unlocked_count` 同步 13→14 + `achievements.json` `quintuple_voice` 入列；T142 `resonance_wave_ability.gd._try_fire()` 加 5 帧 verb-action-only 窗口（拒绝 `is_on_floor_only=true` 的 `is_dashing` 期间触发的"动画中波"）；T141 `resonance_wave_ability.gd` 命中路径 `AudioManagerEnhanced._sfx_bus_play("wave_hit", 0.4 + i*0.04, 1.05 + i*0.02)` 链入 `hit_count` 循环；新增 `tools/test_t130_achievement_sync_smoke.gd` 14 项断言 PASS
- **#74 — T103 第二半 (Wave 5-verb 对称) + T140 _handle_wave 失败提示走 verb 专属方法 + T139 成就计数 13→14**：player.gd `_handle_wave()` 5 路径完整 pulse / cut / bind / echo / wave（wave→resonance_wave 桥接）；T140 失败提示新增 `_wave_off_cooldown_prompt()` / `_wave_silenced_prompt()` / `_wave_already_active_prompt()` 3 个 verb-专属方法（更准确反馈而非泛化"无法释放"）；T139 A072 `quintuple_voice` 5-verb 一次完成成就落地；新增 `tools/test_t139_quintuple_voice_smoke.gd` + `tools/test_t140_wave_verb_prompts_smoke.gd` PASS
- **#73 — T103 第一半 (ResonanceWave 群体波) + T137 SaveLoadMenu 快速加载 + T138 PauseMenu 上次自动存档时间**：A070 `resonance_wave_vfx` (procedural vector pulse) + A071 wave 技能图标 (16x16 程序化像素) + `src/scripts/resonance_wave_ability.gd` (228 行 9 exports + 4 signals + 4 阶段生命周期 + 命中追踪) + `src/scripts/resonance_wave_vfx.gd` 8 层视觉组 + HUD `WaveRow` 第五冷却条（Electric Violet 主题色）；T137 `save_load_menu.gd` quick load card (slot 0 / 上次手动存档) 优先显示 + Ctrl+L 触发；T138 PauseMenu 新增"上次自动存档: N 分钟前"摘要；`test_t103_resonance_wave_smoke.gd` (31 项断言) + `test_t137_t138_quick_load_and_autosave_smoke.gd` (17 项) PASS
- **#72 — T136 SaveSystem 自动存档 60s + T135 PauseMenu 分享剪贴板**：SaveSystem `_autosave_timer` (60s 间隔 / 启用开关) + `last_autosave_at` 时间戳 + `pause_menu.cfg` 持久化 `autosave_enabled`；T135 PauseMenu 新增"分享"按钮 → DisplayServer.clipboard_set (成就摘要 + 4 段格式化文本 + run 编号 + 死亡次数)；`test_t135_share_smoke.gd` + `test_t136_autosave_smoke.gd` PASS
- **#71 — T134 settings 动态 SLOT_COUNT + T133 PauseMenu Quick Stats 摘要行**：settings_menu.cfg 新增 `save_slot_count` (1-10) + SaveSystem 启动时 clamp + 5→10 槽 UI 自动扩展；T133 PauseMenu 顶部"本次 Run" + "历史最佳"两行摘要（run 编号 / 死亡次数 / 修理数 / 收集数 / 房间数）；`test_t133_quick_stats_smoke.gd` + `test_t134_dynamic_slot_count_smoke.gd` PASS
- **#70 — 审查 #70 (D001-D003 严重问题修复)**：D001 `_autosave_timer` 改 Timer 节点 (单 timer / pause_mode=PROCESS) + SceneTree 改 `_autosave` async；D002 `get_run_id()` 改 Time.get_unix_time_from_system() + 文件名含 UTC 时间戳（避免碰撞）；D003 `_health_danger` 改 danger_threshold + beat/tween 同步 + 0.6s 渐显；ASSET_REGISTRY A068-A069 装饰物件登记；3 严重 / 0 一般 / 1 轻微 (L001) / 1 信息
- **#69 — T131 Run 趋势 + T132 备份/恢复 API**：PauseMenu 趋势卡（4 项 stats: run 数 / 平均修理 / 死亡数 / 收集率） + `SaveSystem.get_run_trend()` API；T132 备份/恢复（`backup_save()` → `user://backups/save_N.bak` / `restore_from_backup()` + 自动备份触发器 [manual save / settings delete]）；`test_t131_run_trend_smoke.gd` + `test_t132_backup_restore_smoke.gd` PASS
- **#68 — T129 存档健康度 + T130 历史最佳成就**：SaveSystem `get_save_health()` (per-slot 校验 / CRC32 + last_modified + run_id 摘要) + PauseMenu 存档 tab 健康度标签（健康 / 警告 / 损坏）；T130 历史最佳成就触发条件 (本次 run 修理数 ≥ 历史最高 修理数) + `_check_personal_best()` 钩子 + PauseMenu "本次 Run / 历史最佳" 双行显示；ASSET_REGISTRY A067 `personal_best` 成就登记；`test_t129_save_health_smoke.gd` + `test_t130_personal_best_smoke.gd` PASS
- **#67 — T127 Run 编号 + 历史最佳 + T128 SaveSystem CRC32**：GameState `current_run_id` (UTC yyyymmddhhmmss) + `SaveSystem` 每次 save 写入 `run_id` 字段 + PauseMenu 顶部 "Run #yyyymmddhhmmss"；T128 SaveSystem CRC32 校验（save 头 8 字节 + payload + checksum）+ `get_save_meta()` API + 自动修复损坏检测；`test_t127_run_id_smoke.gd` + `test_t128_crc32_smoke.gd` PASS
- **#66 — F003 smoke_consistency.sh + T126 Player Profile**：T125 `tools/check_smoke_consistency.sh` (6 条规则 144 行 bash: smoke_test_count >= 15 / README BGM 数 / ASSET_REGISTRY 总数 / PROJECT_NAME 一致 / headless 启动 0 错 / uid 已生成) 全 PASS；T126 PauseMenu Player Profile (3 卡片: Player Name / Total Playtime / Best Run Summary) + ProjectSettings 输入字段；`test_t126_player_profile_smoke.gd` PASS
- **#65 — 审查 #65 (D001-D004 修复轮)**：D001 `paused` SignalListener 重复（player.gd 重复监听）已重构为单连接 + 幂等检查；D002 `pause_menu.gd._build_achievement_grid` 16x16 texture 引用 orphan 修复（增加 GroupReferenceHolder 跟踪）；D003 `t134_dynamic_slot_count` 测试 UID 漏提交修复；D004 `_handle_wave` 在 is_dashing 状态触发造成动画穿插修复；44 class_name 零冲突 + 73 signal 完整 + 114 PNG 合法 + 65 ASSET_REGISTRY + 7 冒烟测试套件 14 测试全 PASS
- **#64 — T122 IntroCutscene ambient + T123 whisper_hollow 路由 + T124 BGM 9 主题色板文档**：IntroCutscene 8s → 12s (加 ambient layer 渐入 / 渐出 4s) + 文档同步；T123 audio_manager_enhanced.gd `route_for_scene()` 加 `intro_cutscene → whisper_hollow` 分支；T124 STYLE_GUIDE.md BGM 节扩展 9 主题色板表格（archive_calm / archive_boss / archive_boss_dual / archive_dawn / archive_storm / silence_void / whisper_hollow / finale / intro）；`test_t122_intro_ambient_smoke.gd` + `test_t123_whisper_routing_smoke.gd` PASS
- **#63 — T121 audio_presets.gd 重构 + T118 whisper_hollow + T120 README Game States 节**：T121 audio_presets.gd 新建 (8 BGM 主题常量 + tier 等级 + 调色板 + 路由映射 集中 5 段 → 1 段) audio_manager_enhanced.gd `_MUSIC_PRESETS` dict 抽取；T118 `whisper_hollow` BGM 主题 (F# minor BPM 64 / 全 5th + 7th / LFO 0.4Hz / 4-volume mute 主旋律) + `route_for_scene("whisper_hollow")` + PauseMenu 设置 routing 优先级；T120 README 新增 "Game States" 节 (intro / hub / archive / boss / death / respawn 6 状态 + BGM 主题映射表)；`test_t121_audio_presets_smoke.gd` + `test_t118_whisper_hollow_smoke.gd` PASS
- **#62 — T117 finale 曲式**：audio_manager_enhanced.gd `_MUSIC_PRESETS["finale"]` 落地 (C major → E minor 终止 + 16-note descending arpeggio + tier 4 + GameState._on_full_archive_collected 触发)；ASSET_REGISTRY A066 `finale_theme` 登记；`test_t117_finale_smoke.gd` PASS
- **#61 — T114 silence_void BGM + T115 死亡碑文 + T116 InkWarden 残影**：T114 silence_void BGM (D minor BPM 48 / drone + 0.18Hz LFO / 4-volume 全 mute 主体) + audio_manager_enhanced.gd `route_for_scene("silence_void")` + tier 1；T115 player.gd `die()` tween 链加 0.4s 灰调 wash 后的"墓志铭"字幕（font_size 8 → 6 fade-in）；T116 ink_warden.gd `phase_2_silhouette_remain()` (死亡后 2.5s 残影淡出) + `silhouette_alpha` tween (0.6 → 0) + z_index=10 顶层显示；ASSET_REGISTRY A064 `silence_void` + A065 `silhouette_remain` 登记；`test_t114_silence_void_smoke.gd` + `test_t115_death_inscription_smoke.gd` + `test_t116_silhouette_smoke.gd` PASS
- **#60 — 审查 #60（本轮）**：完整代码质量 / 玩法 / 素材 / 文档审计；0 SCRIPT ERROR + 0 runtime ERROR + 44 class_name 唯一 + 73 signal 完整 + 112 PNG 合法 + 6 autoload 一致 + 63 ASSET_REGISTRY 记录 + 7 个 BGM 主题 + 9 个冒烟测试全 PASS；严重 0 / 一般 2（G001 README 6→7 BGM 主题数补 archive_storm / G002 Recent work 补 #59）/ 轻微 0 / 信息 2（候选池仍有 4 项 + Godot binary 持久化）
- **#59 — 文档同步 + 第 7 主题 BGM archive_storm 落地**：补全 #57（成就解锁时间戳 + CONTRIBUTING）和 #58（README 引用 CONTRIBUTING + PauseMenu hover + 死亡回 Hub 端到端冒烟）两条本该在那两轮就追加的 CHANGELOG 段；T107 在 `audio_manager_enhanced.gd` `_MUSIC_PRESETS` 新增 `archive_storm` (E minor BPM 120 / 16-note chromatic arpeggio / G#6 shimmer / 0.66Hz LFO / 4-volume 全上抬) + `_BOSS_MUSIC_TIER["archive_storm"] = 3`（严格 > archive_boss_dual tier 2）；`ink_warden.gd:529` `_enter_phase_2()` 把 `request_boss_music("archive_boss_dual")` 替换为 `request_boss_music("archive_storm", 600)`；ASSET_REGISTRY A063 条目登记；`test_t107_archive_storm_smoke.gd` (198 行 10 项断言) PASS
- **#58 — README CONTRIBUTING 入口暴露 + PauseMenu hover 高亮 + T079 端到端冒烟**（本轮）：T113 英文 README 「## 开发」节顶部加 `CONTRIBUTING.md` 链接 + 9 节内容简述，README.zh-CN.md 同步加中文版（仓库结构 / 3 种 Godot 拼合 / 7 冒烟测试套件 / 提交格式 / 迭代节奏 / 美术登记 / 文档同步 5 问 / 故障排查 / 决策记录）；T111 `pause_menu.gd._build_achievement_grid` 给每个 16x16 TextureRect 加 `mouse_filter=STOP` + `mouse_entered/exited` connect + `_on_slot_hover_in/out` 0.12s tween（scale 1.0→1.5x + self_modulate 灰→亮 1.4 + modulate 暖色 1.2,1.1,0.9，parallel 三套同步）让玩家 hover 时图标"亮起来"；T112 新建 `tools/test_t112_respawn_hub_e2e_smoke.gd` (213 行) 13 项集成断言覆盖 T079 端到端流程（GameState respawn_to_hub 字段 + API + 常量 + 双分支 + GFC._ready 顺序修复 + settings_menu.cfg 持久化 + .tscn toggle label），冒烟测试 7→8
- **#55 — 5 存档槽 + 列表视图**（本轮）：save_system.gd / save_load_menu.gd `SLOT_COUNT=3→5`，新增 list 紧凑视图（每行 28px）+ LayoutButton 切换 card↔list；`tools/test_t088_save_slots_smoke.gd` 7 项集成断言全 PASS
- **#55 — 审查 #55**：完整代码质量 / 玩法 / 素材 / 文档 / BGM / PNG 头校验审计；0 SCRIPT ERROR + 0 runtime ERROR + 44 class_name 唯一 + 73 signal 完整 + 112 PNG 合法 + 6 autoload 一致 + 62 ASSET_REGISTRY 记录；严重 0 / 一般 0 / 轻微 1（L001 test_t088_save_slots_smoke.gd.uid 漏提交，本轮修复）/ 信息 3
- **#54 — T101 + T102 GlassLock amber flash + PauseMenu 四动词 BBCode 颜色**：`glass_lock.gd._unlock()` 0.5s Amber Voice 屏幕闪（修复"声音回归"的延续性反馈）；PauseMenu `_stat_abilities` 改为 BBCode `[color=#...]` 形式，4 动词色域严格对齐 STYLE_GUIDE
- **#53 — T098 + T100 Pulse/Cut/Echo 命中 flash_color 主题化 + PauseMenu Echo 反弹 row 强调**：`player.gd` `_ready` 桥接 `pulse_ability.pulse_hit` + `cut_ability.cut_hit` 信号；屏震按能力主题色 flash；PauseMenu StatReflects Glass Cyan 高亮
- **#52 — T096 + T097 Echo 与既有系统交互 + 反弹命中 cyan flash**：`echo_charm` 笔误修正（effect 从 `pulse_kill_refund` 改为 `echo_radius_bonus`，ID 与效果对得上）；GameState 新增 `echo_radius_bonus` 字段 + ShopMenu 重建公式；ScreenShake 新增 `flash_color(color, duration, peak_alpha)` API；Echo 反弹命中时 0.08s Glass Cyan 屏闪
- **#51 — T094 + T095 EchoAbility 类 + 护盾反弹逻辑（第四动词代码侧落地）**：`src/scripts/echo_ability.gd` (219 行) 9 exports + 4 signals + 4 阶段生命周期 + 反弹追踪；`project.godot` 新增 echo 输入映射（Q 键 / R 备用 / 手柄 button 5）；HUD `EchoRow` 第四冷却条（Glass Cyan）；A062 `quadruple_voice` 成就；`src/scripts/echo_vfx.gd` 8 层视觉组
- **#50 — 审查 #50**：0 错误 / 0 警告 / 42 class_name / 68 signal / 112 PNG 合法 / A061 Echo 6/6 色板匹配；严重 0 / 一般 0 / 轻微 1（已修）/ 信息 3
- **#49 — 死亡灰阶 VFX + Echo 护盾反弹图标 A061**：ScreenShake `flash_grayscale(duration, peak_alpha)` API + `player.gd._flash_death_grayscale_wash()` 回调（`die()` tween 链）；`scripts/generate_echo_icon.py` 程序化像素绘制 32x32 + 64x64 Echo 护盾图标
- **#48 — 死亡 freeze-frame VFX + README godot binary 快速指引**：T092 `player.die()` 开头 0.15s freeze（time_scale=0.2 + red tint）→ T093 灰阶洗 → T075 lay-down + fade-out；T091 README 新增 Headless Godot Binary Setup 子节（方法 A unzip + 方法 B Python `zipfile`）
- **#47 — 屏幕震动 polish + 装饰物件 procedural**：`src/autoload/screen_shake.gd` autoload（8 个预设含 BOSS_PHASE2 5.0/0.30s 最高强度）；6 个程序化像素装饰物件（A055-A060）+ 14 个 archive_01-04 装饰实例
- **#46 — Boss 阶段 2（InkWarden phase 2）**：6 个常量调优 + `_enter_phase_2()` 视觉切换 + A054 阶段 2 精灵 + 3×RepairVFX + 顶部"怒"飘字 + BGM tier upgrade + 三连发散 + 4.5s AOE 冲撞
- **#45 — 审查 #45**：修复 1 轻微（L001 WardenShadow 节点重命名）+ 4 一般（G001-G004 资产/文档/成就/Recent work 同步）

### 下一步阅读

- `ROADMAP.md` — 完整任务列表、当前候选池、「已完成」时间戳
- `CHANGELOG.md` — 每次迭代的变更日志（已交付与学到的教训）
- `REVIEW_LOG.md` — 每 5 轮一次的审计（代码质量、玩法、素材、文档、漂移）
- `STYLE_GUIDE.md` — 视觉宪法；**所有新美术必须继承自此**
- `ASSET_REGISTRY.md` — 素材账本；**所有新素材必须追加到此处**
- `ITERATION_GUIDE.md` — 自动化 Agent 完整迭代流程
- `RESEARCH.md` — 市场调研 + 选定方向（Voxglass / 声匣修复者）

## 房间编辑器（JSON）

房间可通过 `data/rooms/` 下的 JSON 定义。完整 schema 见 `data/rooms/README.md`。要测试一个 JSON 房间，打开 `src/scenes/json_room.tscn` 并设置 `room_id` 导出变量，或从 GDScript 调用 `RoomLoader.load_room(room_id, parent_node)`。

## Credits 与许可

- 引擎：Godot Engine（MIT）
- 美术与代码：本仓库作者（Saya Ch）
- 音频：100% 程序化合成，无外部采样依赖
- 字体：默认 Godot 4.6.3 内置字体

---

🇬🇧 [English README](./README.md) · 🇨🇳 **简体中文**（本文件）
